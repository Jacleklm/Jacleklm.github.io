<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Bundler源码编写</title>
    <url>/2019/09/30/Bundler%E6%BA%90%E7%A0%81%E7%BC%96%E5%86%99/</url>
    <content><![CDATA[<h3 id="实现代码"><a href="#实现代码" class="headerlink" title="实现代码"></a>实现代码</h3><p>实现一个类似webpack的打包工具，代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">'fs'</span>)</span><br><span class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">'path'</span>)</span><br><span class="line"><span class="keyword">const</span> parser = <span class="built_in">require</span>(<span class="string">'@babel/parser'</span>)</span><br><span class="line"><span class="keyword">const</span> traverse = <span class="built_in">require</span>(<span class="string">'@babel/traverse'</span>).default</span><br><span class="line"><span class="keyword">const</span> babel = <span class="built_in">require</span>(<span class="string">'@babel/core'</span>)</span><br><span class="line"> </span><br><span class="line"><span class="keyword">const</span> moduleAnalyser = <span class="function">(<span class="params">filename</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> content = fs.readFileSync(filename, <span class="string">'utf-8'</span>) <span class="comment">//  content为用fs得到的入口文件的源代码</span></span><br><span class="line">  <span class="keyword">const</span> ast = parser.parse(content, &#123; <span class="comment">//  ast为由源代码得到的抽象语法树</span></span><br><span class="line">    sourceType: <span class="string">'module'</span></span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="keyword">const</span> dependencies = &#123;&#125;</span><br><span class="line">  traverse(ast, &#123; <span class="comment">//  用traverse在抽象语法树中找到有ImportDeclaration的部分（代码中的import的语句）</span></span><br><span class="line">    ImportDeclaration(&#123; node &#125;) &#123;</span><br><span class="line">      <span class="keyword">const</span> dirname = path.dirname(filename)</span><br><span class="line">      <span class="keyword">const</span> newFile = <span class="string">'./'</span> + path.posix.join(dirname, node.source.value)</span><br><span class="line">      dependencies[node.source.value] = (newFile) </span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="keyword">const</span> &#123; code &#125; = babel.transformFromAst(ast, <span class="literal">null</span>, &#123;</span><br><span class="line">    presets: [<span class="string">'@babel/preset-env'</span>]</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="keyword">return</span> &#123; <span class="comment">//  这是分析结果</span></span><br><span class="line">    filename,</span><br><span class="line">    dependencies,</span><br><span class="line">    code</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> makeDependencoesGraph = <span class="function">(<span class="params">entry</span>) =&gt;</span> &#123; </span><br><span class="line">  <span class="keyword">const</span> entryModule = moduleAnalyser(entry) <span class="comment">//  拿到分析结果</span></span><br><span class="line">  <span class="keyword">const</span> graphArray = [entryModule] <span class="comment">//  用队列，遍历加递归，实现将各层依赖加进这个数组中，得到依赖图谱</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; graphArray.length; i++) &#123;</span><br><span class="line">    <span class="keyword">const</span> item = graphArray[i]</span><br><span class="line">    <span class="keyword">const</span> &#123; dependencies &#125; = item</span><br><span class="line">    <span class="keyword">if</span> (dependencies) &#123;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">let</span> j <span class="keyword">in</span> dependencies) &#123;</span><br><span class="line">        graphArray.push(</span><br><span class="line">          moduleAnalyser(dependencies[j])</span><br><span class="line">        )</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">const</span> graph = &#123;&#125;  <span class="comment">//  对图谱做一个简化，方便之后用</span></span><br><span class="line">  graphArray.forEach(<span class="function"><span class="params">item</span> =&gt;</span> &#123;</span><br><span class="line">    graph[item.filename] = &#123;</span><br><span class="line">      dependencies: item.dependencies,</span><br><span class="line">      code: item.code</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="keyword">return</span> graph</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> generateCode = <span class="function">(<span class="params">entry</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> graph = <span class="built_in">JSON</span>.stringify(makeDependencoesGraph(entry))</span><br><span class="line">  <span class="comment">//  依赖图谱中各个依赖的代码其实都有require和export，但是浏览器是无法识别的，所以我们要自己写</span></span><br><span class="line">  <span class="comment">//  require('$&#123;entry&#125;')中要加单引号，因为我们的要拼接一些代码而不是真正去执行一些代码</span></span><br><span class="line">  <span class="keyword">return</span> <span class="string">`</span></span><br><span class="line"><span class="string">    (function(graph) &#123;</span></span><br><span class="line"><span class="string">      function require(module) &#123;</span></span><br><span class="line"><span class="string">        function localRequire(relativePath) &#123;</span></span><br><span class="line"><span class="string">          return require(graph[module].dependencies[relativePath])</span></span><br><span class="line"><span class="string">        &#125;</span></span><br><span class="line"><span class="string">        var exports = &#123;&#125;;</span></span><br><span class="line"><span class="string">        (funcrion(require, exports, code) &#123;</span></span><br><span class="line"><span class="string">          eval(code);</span></span><br><span class="line"><span class="string">        &#125;)(localRequire, exports, graph[module].code);</span></span><br><span class="line"><span class="string">        return exports;</span></span><br><span class="line"><span class="string">      &#125;;</span></span><br><span class="line"><span class="string">      require('<span class="subst">$&#123;entry&#125;</span>')</span></span><br><span class="line"><span class="string">    &#125;)(<span class="subst">$&#123;graph&#125;</span>);</span></span><br><span class="line"><span class="string">  `</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="代码解读"><a href="#代码解读" class="headerlink" title="代码解读"></a>代码解读</h3><h4 id="moduleAnalyser"><a href="#moduleAnalyser" class="headerlink" title="moduleAnalyser"></a>moduleAnalyser</h4><p>要对入口文件进行打包，我们应该对入口文件进行分析：</p>
<ul>
<li>拿到入口文件的源代码。借助fs模块实现</li>
<li>找到入口文件的依赖。借助@babel/parser，把源代码转换成AST，再用@babel/traverse找到源代码中的声明的语句而找到依赖的路径。创建一个对象来保存这些依赖即可</li>
<li>ES6转ES5。@babel/core，@babel/preset-env</li>
</ul>
<h4 id="makeDependencoesGraph"><a href="#makeDependencoesGraph" class="headerlink" title="makeDependencoesGraph"></a>makeDependencoesGraph</h4><p>模块的依赖里又会引用其他依赖，想完全分析好必须弄清所有依赖的关系，创建依赖图谱</p>
<h4 id="generateCode"><a href="#generateCode" class="headerlink" title="generateCode"></a>generateCode</h4><p>生成最终打包完成的代码。return的应该是打包生成的代码（字符串），同时这些代码应该是一个闭包，避免污染全局环境</p>
]]></content>
      <categories>
        <category>Engineering</category>
      </categories>
      <tags>
        <tag>webpack</tag>
      </tags>
  </entry>
  <entry>
    <title>AST详解</title>
    <url>/2020/06/25/AST%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<p>TODO：还有这篇 <a href="https://mp.weixin.qq.com/s/lWj1_rPm75dXROKEMf-eQg" target="_blank" rel="noopener">字节分享</a><br>几乎完全转自 <a href="https://chengyuming.cn/views/webpack/AST.html" target="_blank" rel="noopener">AST 团队分享</a> ，可以理解为 读后 &amp; 自己敲一遍 的笔记</p>
<h1 id="What-amp-Why"><a href="#What-amp-Why" class="headerlink" title="What &amp; Why"></a>What &amp; Why</h1><ul>
<li>What: 抽象语法树（Abstract Syntax Tree，简称 AST）是<strong>源代码的抽象语法结构的树状表现形式</strong></li>
<li>Why:<ul>
<li>webpack、eslint 等很多工具库的核心都是通过抽象语法树来实现对代码的检查、分析等操作</li>
<li>浏览器就是通过将 js 代码转化为抽象语法树来进行下一步的分析等其他操作，所以将 js 转化为抽象语法树更利于程序的分析</li>
</ul>
</li>
<li>一个简单的例子:<br><img src="/img/AST/AST.png"><ul>
<li>首先<strong>一段</strong>代码转换成的抽象语法树是一个对象，该对象会有一个顶级的 type 属性 Program；第二个属性是 body 是一个数组</li>
<li>body 数组中存放的每一项都是一个对象，里面包含了所有的对于该语句的描述信息</li>
</ul>
</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">type: 描述该语句的类型  --&gt; 变量声明的语句</span><br><span class="line">kind: 变量声明的关键字  --&gt; var</span><br><span class="line">declaration: 声明内容的数组，里面每一项也是一个对象</span><br><span class="line">  type: 描述该语句的类型</span><br><span class="line">  id: 描述变量名称的对象</span><br><span class="line">      type: 定义</span><br><span class="line">      name: 变量的名字</span><br><span class="line">  init: 初始化变量值的对象</span><br><span class="line">      type: 类型</span><br><span class="line">      value: 值 &quot;is tree&quot; 不带引号</span><br><span class="line">      row:  &quot;\&quot;is tree&quot;\&quot; 带引号</span><br></pre></td></tr></table></figure>

<h1 id="词法分析和语法分析"><a href="#词法分析和语法分析" class="headerlink" title="词法分析和语法分析"></a>词法分析和语法分析</h1><p><code>JavaScript</code> 是<strong>解释型语言</strong>，一般通过 <code>词法分析 -&gt; 语法分析 -&gt; 语法树</code>，就可以开始解释执行了。从 code 分析成 AST ，我们可以在<a href="http://esprima.org/" target="_blank" rel="noopener">这里</a>看到效果</p>
<ul>
<li>词法分析：也叫扫描 (scans)，是将字符流转换为记号流 (tokens)，它会读取我们的代码然后按照一定的规则合成一个个的标识 (scans: code =&gt; tokens)<br>。当词法分析源代码的时候，它会一个一个字符的读取代码；当它遇到空格、操作符，或者特殊符号的时候，它会认为一个话已经完成了。比如说：var a = 2 ，这段代码通常会被分解成 var、a、=、2</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">;[</span><br><span class="line">  &#123; <span class="attr">type</span>: <span class="string">'Keyword'</span>, <span class="attr">value</span>: <span class="string">'var'</span> &#125;,</span><br><span class="line">  &#123; <span class="attr">type</span>: <span class="string">'Identifier'</span>, <span class="attr">value</span>: <span class="string">'a'</span> &#125;,</span><br><span class="line">  &#123; <span class="attr">type</span>: <span class="string">'Punctuator'</span>, <span class="attr">value</span>: <span class="string">'='</span> &#125;,</span><br><span class="line">  &#123; <span class="attr">type</span>: <span class="string">'Numeric'</span>, <span class="attr">value</span>: <span class="string">'2'</span> &#125;,</span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<ul>
<li>语法分析：也称解析器，将词法分析出来的数组转换成树的形式，同时验证语法。语法如果有错的话，抛出语法错误 (analyze: tokens =&gt; syntax)</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  ...</span><br><span class="line">  <span class="string">"type"</span>: <span class="string">"VariableDeclarator"</span>,</span><br><span class="line">  <span class="string">"id"</span>: &#123;</span><br><span class="line">    <span class="string">"type"</span>: <span class="string">"Identifier"</span>,</span><br><span class="line">    <span class="string">"name"</span>: <span class="string">"a"</span></span><br><span class="line">  &#125;,</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>syntax =&gt; tree</li>
</ul>
<h1 id="What-can-AST-do"><a href="#What-can-AST-do" class="headerlink" title="What can AST do"></a>What can AST do</h1><ul>
<li>语法检查、代码风格检查、格式化代码、语法高亮、错误提示、自动补全等</li>
<li>代码混淆压缩</li>
<li>优化变更代码，改变代码结构等</li>
</ul>
<p>eg. 有个函数 <code>function a() {}</code> 我想把它变成 <code>function b() {}</code><br>eg. 在 webpack 中代码编译完成后 <code>require(&#39;a&#39;)</code> –&gt; <code>__webapck__require__(&quot;*/**/a.js&quot;)</code></p>
<h1 id="The-analysis-process-of-AST"><a href="#The-analysis-process-of-AST" class="headerlink" title="The analysis process of AST"></a>The analysis process of AST</h1><h2 id="How-to-analysis"><a href="#How-to-analysis" class="headerlink" title="How to analysis"></a>How to analysis</h2><p>先说结果：AST 是<strong>深度优先遍历</strong><br>比如说一段代码 <code>function getUser() {}</code>，我们把函数名字更改为 hello<br>推荐另一个常用的 AST 在线转换网站<a href="https://astexplorer.net/" target="_blank" rel="noopener">ast explorer</a>；可以在网站看转换效果，也可以用下面的工具看效果</p>
<ul>
<li>工具 (这三个是操作 AST 的三个重要模块，也是实现 babel 的核心依赖)<ul>
<li>esprima：code =&gt; ast</li>
<li>estraverse: traverse ast</li>
<li>escodegen: ast =&gt; code</li>
</ul>
</li>
<li>实例代码</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> esprima = <span class="built_in">require</span>(<span class="string">'esprima'</span>)</span><br><span class="line"><span class="keyword">const</span> estraverse = <span class="built_in">require</span>(<span class="string">'estraverse'</span>)</span><br><span class="line"><span class="keyword">const</span> code = <span class="string">`function getUser() &#123;&#125;`</span></span><br><span class="line"><span class="comment">// 生成 AST</span></span><br><span class="line"><span class="keyword">const</span> ast = esprima.parseScript(code)</span><br><span class="line"><span class="comment">// 转换 AST，只会遍历 type 属性</span></span><br><span class="line"><span class="comment">// traverse 方法中有进入和离开两个钩子函数</span></span><br><span class="line">estraverse.traverse(ast, &#123;</span><br><span class="line">  enter(node) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'enter -&gt; node.type'</span>, node.type)</span><br><span class="line">  &#125;,</span><br><span class="line">  leave(node) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'leave -&gt; node.type'</span>, node.type)</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<ul>
<li>打印结果<br><img src="/img/AST/output.png"></li>
<li>所以是深度优先遍历<br><img src="/img/AST/tree.png"></li>
</ul>
<h2 id="Demo-修改函数名字"><a href="#Demo-修改函数名字" class="headerlink" title="Demo: 修改函数名字"></a>Demo: 修改函数名字</h2><p>此时我们发现函数的名字在 <code>type</code> 为 <code>Identifier</code> 的时候就是该函数的名字，我们就可以直接修改它便可实现一个更改函数名字的 AST 工具</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 转换树</span></span><br><span class="line">estraverse.traverse(ast, &#123;</span><br><span class="line">  <span class="comment">// 进入离开修改都是可以的</span></span><br><span class="line">  enter(node) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'enter -&gt; node.type'</span>, node.type)</span><br><span class="line">    <span class="keyword">if</span> (node.type === <span class="string">'Identifier'</span>) &#123;</span><br><span class="line">      node.name = <span class="string">'hello'</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  leave(node) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'leave -&gt; node.type'</span>, node.type)</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;)</span><br><span class="line"><span class="comment">// 生成新的代码</span></span><br><span class="line"><span class="keyword">const</span> result = escodegen.generate(ast)</span><br><span class="line"><span class="built_in">console</span>.log(result)</span><br><span class="line"><span class="comment">// function hello() &#123;&#125;</span></span><br></pre></td></tr></table></figure>

<h1 id="Application-of-AST"><a href="#Application-of-AST" class="headerlink" title="Application of AST"></a>Application of AST</h1><h2 id="babel"><a href="#babel" class="headerlink" title="babel"></a>babel</h2><h3 id="The-working-mechanism-of-babel"><a href="#The-working-mechanism-of-babel" class="headerlink" title="The working mechanism of babel"></a>The working mechanism of babel</h3><blockquote>
<p>babel 的主要作用就是 Es6 代码转换为 Es5 的代码，以兼容所有浏览器，主要原理就是运用了 AST。有个 class 语法转为 ES5 语法的 demo，见博客 <a href="https://segmentfault.com/a/1190000016706589?utm_medium=referral&utm_source=tuicool" target="_blank" rel="noopener">AST 抽象语法树</a></p>
</blockquote>
<p>babel两个工具包重要的包： <code>@babel/core</code> (打通webpack和babel的一个通道)、<code>@babel/preset-env</code> (翻译器) (还有一个@babel/polyfill（添加ES6中的新函数新变量，eg. Promise和map）)。当我们配置 babel 的时候，不管是在 <code>.babelrc</code> 或者 <code>babel.config.js</code> 文件里面配置的都有 presets 和 plugins 两个配置项</p>
<h4 id="presets和plugins的区别"><a href="#presets和plugins的区别" class="headerlink" title="presets和plugins的区别"></a>presets和plugins的区别</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// .babelrc</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">"presets"</span>: [<span class="string">"@babel/preset-env"</span>],</span><br><span class="line">  <span class="string">"plugins"</span>: []</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当我们配置了 presets 中有 <code>@babel/preset-env</code>，那么 <code>@babel/core</code> 就会去找 <code>preset-env</code> 预设的插件包，它是一套预设好的插件配置。</p>
<p><code>@babel/core</code>并不会去转换代码，只提供一些核心 API，真正的代码转换工作由插件或者预设来完成，比如要转换箭头函数，会用到这个 plugin：<code>@babel/plugin-transform-arrow-functions</code>，当需要转换的要求增加时，我们不可能去一一配置相应的 plugin，这个时候就可以用到预设了，也就是 presets。<strong>presets 是 plugins 的集合</strong>，一个 presets 内部包含了很多 plugin</p>
<h4 id="babel中转换代码一个demo"><a href="#babel中转换代码一个demo" class="headerlink" title="babel中转换代码一个demo"></a>babel中转换代码一个demo</h4><p>现在我们有一个箭头函数，要想把它转成普通函数，我们就可以直接这么写：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> babel = <span class="built_in">require</span>(<span class="string">'@babel/core'</span>)</span><br><span class="line"><span class="keyword">const</span> code = <span class="string">`const fn = (a, b) =&gt; a + b`</span></span><br><span class="line"><span class="comment">// babel 有 transform 方法会帮我们自动遍历，使用相应的预设或者插件转换相应的代码</span></span><br><span class="line"><span class="keyword">const</span> r = babel.transform(code, &#123;</span><br><span class="line">  presets: [<span class="string">'@babel/preset-env'</span>],</span><br><span class="line">&#125;)</span><br><span class="line"><span class="built_in">console</span>.log(r.code)</span><br><span class="line"><span class="comment">// 打印结果如下</span></span><br><span class="line"><span class="comment">// "use strict";</span></span><br><span class="line"><span class="comment">// var fn = function fn() &#123; return a + b; &#125;;</span></span><br></pre></td></tr></table></figure>

<p>此时我们可以看到最终代码会被转成普通函数，但是我们，只需要箭头函数转通函数的功能，不需要用这么大一套包，只需要一个箭头函数转普通函数的包，我们其实是可以在 <code>node_modules</code> 下面找到有个叫做 <code>plugin-transform-arrow-functions</code> 的插件，这个插件是专门用来处理 箭头函数的，我们就可以这么写：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> r = babel.transform(code, &#123;</span><br><span class="line">  plugins: [<span class="string">'@babel/plugin-transform-arrow-functions'</span>],</span><br><span class="line">&#125;)</span><br><span class="line"><span class="built_in">console</span>.log(r.code)</span><br><span class="line"><span class="comment">// 打印结果如下</span></span><br><span class="line"><span class="comment">// const fn = function () &#123; return a + b; &#125;;</span></span><br></pre></td></tr></table></figure>

<p>我们可以从打印结果发现此时并没有转换我们变量的声明方式还是 const 声明，只是转换了箭头函数</p>
<h3 id="编写一个箭头函数转普通函数的babel插件"><a href="#编写一个箭头函数转普通函数的babel插件" class="headerlink" title="编写一个箭头函数转普通函数的babel插件"></a>编写一个箭头函数转普通函数的babel插件</h3><p>首先应该看 <a href="https://juejin.im/post/5b14257ef265da6e5546b14b" target="_blank" rel="noopener">Babel插件开发入门指南</a><br>现在我们来个实战把 <code>const fn = (a, b) =&gt; a + b</code> 转换为 <code>const fn = function(a, b) { return a + b }</code></p>
<h4 id="分析-AST-结构"><a href="#分析-AST-结构" class="headerlink" title="分析 AST 结构"></a>分析 AST 结构</h4><p>首先我们在在线分析 AST 的网站上分析 <code>const fn = (a, b) =&gt; a + b</code> 和 <code>const fn = function(a, b) { return a + b }</code> 看两者语法树的区别<br><img src="/img/AST/code.png"></p>
<p>分析可得：</p>
<ul>
<li>变成普通函数之后他就不叫箭头函数了 <code>ArrowFunctionExpression</code>，而是函数表达式了 <code>FunctionExpression</code></li>
<li>所以首先我们要把 箭头函数表达式(<code>ArrowFunctionExpression</code>) 转换为 函数表达式(<code>FunctionExpression</code>)</li>
<li>要把 二进制表达式(<code>BinaryExpression</code>) 放到一个 代码块中(<code>BlockStatement</code>)的 <code>ExpressionStatement</code>/ <code>ReturnStatement</code> 中。</li>
<li>其实我们要做就是把一棵树变成另外一颗树，说白了其实就是拼成另一颗树的结构，然后生成新的代码，就可以完成代码的转换</li>
</ul>
<h4 id="访问者模式"><a href="#访问者模式" class="headerlink" title="访问者模式"></a>访问者模式</h4><p>在 babel 中，我们开发 plugins 的时候要用到<strong>访问者模式</strong>，就是说在访问到某一个路径的时候进行匹配，然后在对这个节点进行修改。<strong>这里的节点指的是AST树中的节点</strong>，比如下面是访问到 ArrowFunctionExpression 节点就执行下面的 ArrowFunctionExpression() 函数</p>
<p>那么我们就可以这么写：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> babel = <span class="built_in">require</span>(<span class="string">'@babel/core'</span>)</span><br><span class="line"><span class="keyword">const</span> code = <span class="string">`const fn = (a, b) =&gt; a + b`</span> <span class="comment">// 转换后 const fn = function(a, b) &#123; return a + b &#125;</span></span><br><span class="line"><span class="keyword">const</span> arrowFnPlugin = &#123;</span><br><span class="line">  <span class="comment">// 访问者模式</span></span><br><span class="line">  visitor: &#123;</span><br><span class="line">    <span class="comment">// 当访问到某个路径的时候进行匹配，比如下面是访问到 ArrowFunctionExpression 就执行下面的 ArrowFunctionExpression() 函数</span></span><br><span class="line">    ArrowFunctionExpression(path) &#123;</span><br><span class="line">      <span class="comment">// 拿到节点</span></span><br><span class="line">      <span class="keyword">const</span> node = path.node</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">'ArrowFunctionExpression -&gt; node'</span>, node)</span><br><span class="line">    &#125;,</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> r = babel.transform(code, &#123;</span><br><span class="line">  plugins: [arrowFnPlugin],</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(r)</span><br></pre></td></tr></table></figure>

<h4 id="修改-AST-结构"><a href="#修改-AST-结构" class="headerlink" title="修改 AST 结构"></a>修改 AST 结构</h4><p>此时我们拿到的结果是这样的节点结果是 <a href="https://chengyuming.cn/json/arrowFn.json" target="_blank" rel="noopener">这样的</a>，其实就是 <code>ArrowFunctionExpression</code> 的 AST，此时我们要做的是把 <code>ArrowFunctionExpression</code> 的结构替换成 <code>FunctionExpression的结构</code>。但要我们自己手写替换得毫无差错是很麻烦的，所以 babel 为我们提供了一个神仙工具叫做 <a href="https://babeljs.io/docs/en/babel-types" target="_blank" rel="noopener"><code>@babel/types</code></a> <a href="https://www.babeljs.cn/docs/babel-types" target="_blank" rel="noopener">中文版</a></p>
<p><code>@babel/types</code> 有两个作用：</p>
<ul>
<li>判断当前节点是不是某种特定节点（eg. 相当于之前的 node.type === ‘ArrowFunctionExpression’ ）</li>
<li>生成对应的表达式</li>
</ul>
<p>那么接下来我们就开始生成一个 FunctionExpression，然后把之前的 <code>ArrowFunctionExpression</code> 替换掉，我们可以看 <code>@babel/types</code> 文档，找到 functionExpression 方法，该方法接受相应的参数我们传递过去即可生成一个 <code>FunctionExpression</code></p>
<blockquote>
<p>t.functionExpression(id, params, body, generator, async)</p>
</blockquote>
<ul>
<li>id: Identifier (default: null) id 可传递 null</li>
<li>params: Array<lval> (required) 函数参数，可以把之前的参数拿过来</lval></li>
<li>body: BlockStatement (required) 函数体，接受一个 BlockStatement 我们需要生成一个</li>
<li>generator: boolean (default: false) 是否为 generator 函数，当然不是了</li>
<li>async: boolean (default: false) 是否为 async 函数，肯定不是了</li>
</ul>
<p>还需要生成一个 <code>BlockStatement</code>，我们接着看文档找到 <code>BlockStatement</code> 接受的参数</p>
<blockquote>
<p>t.blockStatement(body, directives)</p>
</blockquote>
<p>看文档说明，blockStatement 接受一个 body，那我们把之前的 body 拿过来就可以直接用，不过这里 body 接受一个数组</p>
<p>我们细看 AST 结构，函数表达式中的 <code>BlockStatement</code> 中的 body 是一个 <code>ReturnStatement</code>，所以我们还需要生成一个 <code>ReturnStatement</code></p>
<p>现在我们就可以改写 AST 了</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> babel = <span class="built_in">require</span>(<span class="string">'@babel/core'</span>)</span><br><span class="line"><span class="keyword">const</span> t = <span class="built_in">require</span>(<span class="string">'@babel/types'</span>)</span><br><span class="line"><span class="keyword">const</span> code = <span class="string">`const fn = (a, b) =&gt; a + b`</span> <span class="comment">// const fn = function(a, b) &#123; return a + b &#125;</span></span><br><span class="line"><span class="keyword">const</span> arrowFnPlugin = &#123;</span><br><span class="line">  <span class="comment">// 访问者模式</span></span><br><span class="line">  visitor: &#123;</span><br><span class="line">    <span class="comment">// 当访问到某个路径的时候进行匹配</span></span><br><span class="line">    ArrowFunctionExpression(path) &#123;</span><br><span class="line">      <span class="comment">// 拿到节点然后替换节点</span></span><br><span class="line">      <span class="keyword">const</span> node = path.node</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">'ArrowFunctionExpression -&gt; node'</span>, node)</span><br><span class="line">      <span class="comment">// 拿到函数的参数</span></span><br><span class="line">      <span class="keyword">const</span> params = node.params</span><br><span class="line">      <span class="keyword">const</span> body = node.body</span><br><span class="line">      <span class="keyword">const</span> functionExpression = t.functionExpression(</span><br><span class="line">        <span class="literal">null</span>,</span><br><span class="line">        params,</span><br><span class="line">        t.blockStatement([body])</span><br><span class="line">      )</span><br><span class="line">      <span class="comment">// 替换原来的函数</span></span><br><span class="line">      path.replaceWith(functionExpression)</span><br><span class="line">    &#125;,</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">const</span> r = babel.transform(code, &#123;</span><br><span class="line">  plugins: [arrowFnPlugin],</span><br><span class="line">&#125;)</span><br><span class="line"><span class="built_in">console</span>.log(r.code) <span class="comment">// const fn = function (a, b) &#123; return a + b; &#125;;</span></span><br></pre></td></tr></table></figure>

<h4 id="特殊情况"><a href="#特殊情况" class="headerlink" title="特殊情况"></a>特殊情况</h4><p>我们知道在剪头函数中是可以省略 return 关键字，我们上面是处理了省略关键字的写法，但是如果用户写了 return 关键字后，我们写的这个插件就有问题了，所以我们可以在优化一下</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> fn = <span class="function">(<span class="params">a, b</span>) =&gt;</span> &#123; retrun a + b &#125; -&gt; <span class="keyword">const</span> fn = <span class="function"><span class="keyword">function</span>(<span class="params">a, b</span>) </span>&#123; <span class="keyword">return</span> a + b &#125;</span><br></pre></td></tr></table></figure>

<p>观察代码我们发现，我们就不需要把 body 转换成 <code>blockStatement</code> 了，直接放过去就可以了，那么我们就可以这么写</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">ArrowFunctionExpression(path) &#123;</span><br><span class="line">  <span class="comment">// 拿到节点然后替换节点</span></span><br><span class="line">  <span class="keyword">const</span> node = path.node</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">"ArrowFunctionExpression -&gt; node"</span>, node)</span><br><span class="line">  <span class="comment">// 拿到函数的参数</span></span><br><span class="line">  <span class="keyword">const</span> params = node.params</span><br><span class="line">  <span class="keyword">let</span> body = node.body</span><br><span class="line">  <span class="comment">// 判断是不是 blockStatement，不是的话让他变成 blockStatement</span></span><br><span class="line">  <span class="keyword">if</span> (!t.isBlockStatement(body)) &#123;</span><br><span class="line">    body = t.blockStatement([body])</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">const</span> functionExpression = t.functionExpression(<span class="literal">null</span>, params, body)</span><br><span class="line">  <span class="comment">// 替换原来的函数</span></span><br><span class="line">  path.replaceWith(functionExpression)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="编写一个claas转ES5的babel插件"><a href="#编写一个claas转ES5的babel插件" class="headerlink" title="编写一个claas转ES5的babel插件"></a>编写一个claas转ES5的babel插件</h3><p>见 <a href="https://segmentfault.com/a/1190000016706589?utm_medium=referral&utm_source=tuicool" target="_blank" rel="noopener">AST 抽象语法树</a><br>类似的实际插件是 <code>@babel/plugin-transform-classes</code></p>
<h2 id="按需引入"><a href="#按需引入" class="headerlink" title="按需引入"></a>按需引入</h2><p>我们用 element-ui 或 antd 的时候，样式都支持全局引入和按需引入。按需引入需要安装一个 <code>babel-plugin-import</code> 的插件，将全局的写法变成按需引入的写法。其实也是用了AST语法树转换的原理，实现</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; Button &#125; <span class="keyword">from</span> <span class="string">'antd'</span>;</span><br><span class="line">      ↓ ↓ ↓ ↓ ↓ ↓</span><br><span class="line"><span class="keyword">var</span> _button = <span class="built_in">require</span>(<span class="string">'antd/lib/button'</span>);</span><br><span class="line"><span class="built_in">require</span>(<span class="string">'antd/lib/button/style'</span>);</span><br></pre></td></tr></table></figure>

<p>(这个插件详见另一篇博客 <a href="https://jacleklm.github.io/2020/06/27/babel-plugin-import/" target="_blank" rel="noopener">按需加载 &amp; 样式自动加载 —— babel-plugin-import</a>)</p>
<h2 id="babylon"><a href="#babylon" class="headerlink" title="babylon"></a>babylon</h2><blockquote>
<p>在 babel 官网上有一句话 <a href="https://babeljs.io/docs/en/babylon" target="_blank" rel="noopener">Babylon</a> is a JavaScript parser used in Babel。（即是Babel 的解析引擎）</p>
</blockquote>
<h3 id="babel-与-babylon-的关系"><a href="#babel-与-babylon-的关系" class="headerlink" title="babel 与 babylon 的关系"></a>babel 与 babylon 的关系</h3><p>from <a href="http://www.alloyteam.com/2017/04/analysis-of-babel-babel-overview/" target="_blank" rel="noopener">alloyteam-剖析babel</a> ：<br>Babel 使用的引擎是 babylon，babylon 并非由 babel 团队自己开发的，而是 fork 的 acorn 项目，acorn 的项目本人在很早之前在兴趣部落 1.0 在构建中使用，为了是做一些代码的转换，是很不错的一款引擎，不过 acorn 引擎只提供基本的解析 ast 的能力，遍历还需要配套的 acorn-travesal, 替换节点需要使用 acorn-，而这些开发，在 Babel 的插件体系开发下，变得一体化了 </p>
<h3 id="使用-babylon-编写一个数组扩展运算符-转-Es5-语法的插件"><a href="#使用-babylon-编写一个数组扩展运算符-转-Es5-语法的插件" class="headerlink" title="使用 babylon 编写一个数组扩展运算符 转 Es5 语法的插件"></a>使用 babylon 编写一个数组扩展运算符 转 Es5 语法的插件</h3><p>把 <code>const arr = [ ...arr1, ...arr2 ]</code> 转成 <code>var arr = [].concat(arr1, arr2)</code></p>
<p>我们使用 babylon 的话就不需要使用 <code>@babel/core</code> 了，只需要用到他里面的 <code>traverse</code> 和 <code>generator</code>，用到的包有 <code>babylon</code>、<code>@babel/traverse</code>、<code>@babel/generator</code>、<code>@babel/types</code></p>
<h3 id="分析语法树"><a href="#分析语法树" class="headerlink" title="分析语法树"></a>分析语法树</h3><p>看一下两棵语法树的区别<br><img src="/img/AST/bablon.png"><br>根据上图我们分析得出：</p>
<ul>
<li>两棵树都是变量声明的方式，不同的是他们声明的关键字不一样</li>
<li>他们初始化变量值的时候是不一样的，一个数组表达式（ArrayExpression）另一个是调用表达式（CallExpression）</li>
<li>那我们要做的就很简单了，就是把 数组表达式转换为调用表达式就可以</li>
</ul>
<h3 id="分析类型"><a href="#分析类型" class="headerlink" title="分析类型"></a>分析类型</h3><p>这段代码的核心生成一个 callExpression 调用表达式，所以对应官网上的类型，我们分析需要用到的 api</p>
<ul>
<li><p>先来分析 init 里面的，首先是 callExpression</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param <span class="type">&#123;Expression&#125;</span> </span>callee  (required)</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param <span class="type">&#123;Array&lt;Expression | SpreadElement | JSXNamespacedName&gt;&#125;</span> </span>source (required)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">t.callExpression(callee, <span class="built_in">arguments</span>)</span><br></pre></td></tr></table></figure></li>
<li><p>对应语法树上 callee 是一个 MemberExpression，所以要生成一个成员表达式</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param <span class="type">&#123;Expression&#125;</span> </span>object  (required)</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param <span class="type">&#123;if computed then Expression else Identifier&#125;</span> </span>property (required)</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param <span class="type">&#123;boolean&#125;</span> </span>computed (default: false)</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param <span class="type">&#123;boolean&#125;</span> </span>optional (default: null)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">t.memberExpression(object, property, computed, optional)</span><br></pre></td></tr></table></figure></li>
<li><p>在 callee 的 object 是一个 ArrayExpression 数组表达式，是一个空数组</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param <span class="type">&#123;Array&lt;null | Expression | SpreadElement&gt;&#125;</span> </span>elements  (default: [])</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">t.arrayExpression(elements)</span><br></pre></td></tr></table></figure></li>
<li><p>对了里面的东西分析完了，我们还要生成 VariableDeclarator 和 VariableDeclaration 最终生成新的语法树</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param <span class="type">&#123;LVal&#125;</span> </span>id  (required)</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param <span class="type">&#123;Expression&#125;</span> </span>init (default: null)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">t.variableDeclarator(id, init)</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param <span class="type">&#123;"var" | "let" | "const"&#125;</span> </span>kind  (required)</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param <span class="type">&#123;Array&lt;VariableDeclarator&gt;&#125;</span> </span>declarations (required)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">t.variableDeclaration(kind, declarations)</span><br></pre></td></tr></table></figure></li>
</ul>
<p>其实倒着分析语法树，分析完怎么写也就清晰了，那么我们开始上代码吧</p>
<h3 id="上代码"><a href="#上代码" class="headerlink" title="上代码"></a>上代码</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> babylon = <span class="built_in">require</span>(<span class="string">'babylon'</span>)</span><br><span class="line"><span class="comment">// 使用 babel 提供的包，traverse 和 generator 都是被暴露在 default 对象上的</span></span><br><span class="line"><span class="keyword">const</span> traverse = <span class="built_in">require</span>(<span class="string">'@babel/traverse'</span>).default</span><br><span class="line"><span class="keyword">const</span> generator = <span class="built_in">require</span>(<span class="string">'@babel/generator'</span>).default</span><br><span class="line"><span class="keyword">const</span> t = <span class="built_in">require</span>(<span class="string">'@babel/types'</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> code = <span class="string">`const arr = [ ...arr1, ...arr2 ]`</span> <span class="comment">// var arr = [].concat(arr1, arr2)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> ast = babylon.parse(code, &#123;</span><br><span class="line">  sourceType: <span class="string">'module'</span>,</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 转换树</span></span><br><span class="line">traverse(ast, &#123;</span><br><span class="line">  VariableDeclaration(path) &#123;</span><br><span class="line">    <span class="keyword">const</span> node = path.node</span><br><span class="line">    <span class="keyword">const</span> declarations = node.declarations</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'VariableDeclarator -&gt; declarations'</span>, declarations)</span><br><span class="line">    <span class="keyword">const</span> kind = <span class="string">'var'</span></span><br><span class="line">    <span class="comment">// 边界判定</span></span><br><span class="line">    <span class="keyword">if</span> (node.kind !== kind &amp;&amp; declarations.length === <span class="number">1</span> &amp;&amp; t.isArrayExpression(declarations[<span class="number">0</span>].init)) &#123;</span><br><span class="line">      <span class="comment">// 取得之前的 elements</span></span><br><span class="line">      <span class="keyword">const</span> args = declarations[<span class="number">0</span>].init.elements.map(<span class="function">(<span class="params">item</span>) =&gt;</span> item.argument)</span><br><span class="line">      <span class="keyword">const</span> callee = t.memberExpression(t.arrayExpression(), t.identifier(<span class="string">'concat'</span>), <span class="literal">false</span>)</span><br><span class="line">      <span class="keyword">const</span> init = t.callExpression(callee, args)</span><br><span class="line">      <span class="keyword">const</span> declaration = t.variableDeclarator(declarations[<span class="number">0</span>].id, init)</span><br><span class="line">      <span class="keyword">const</span> variableDeclaration = t.variableDeclaration(kind, [declaration])</span><br><span class="line">      path.replaceWith(variableDeclaration)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h1 id="具体语法书"><a href="#具体语法书" class="headerlink" title="具体语法书"></a>具体语法书</h1><p>和抽象语法树相对的是具体语法树（Concrete Syntax Tree）简称 CST（通常称作分析树）。一般的，在源代码的翻译和编译过程中，语法分析器创建出分析树。一旦 AST 被创建出来，在后续的处理过程中，比如语义分析阶段，会添加一些信息。可参考抽象语法树和具体语法树有什么区别？</p>
<h1 id="补充"><a href="#补充" class="headerlink" title="补充"></a>补充</h1><p>关于 node 类型，全集大致如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(parameter) node: Identifier | SimpleLiteral | RegExpLiteral | Program | FunctionDeclaration | FunctionExpression | </span><br><span class="line">ArrowFunctionExpression | SwitchCase | CatchClause | VariableDeclarator | ExpressionStatement | BlockStatement | </span><br><span class="line">EmptyStatement | DebuggerStatement | WithStatement | ReturnStatement | LabeledStatement | BreakStatement | </span><br><span class="line">ContinueStatement | IfStatement | SwitchStatement | ThrowStatement | TryStatement | WhileStatement | DoWhileStatement | </span><br><span class="line">ForStatement | ForInStatement | ForOfStatement | VariableDeclaration | ClassDeclaration | ThisExpression | </span><br><span class="line">ArrayExpression | ObjectExpression | YieldExpression | UnaryExpression | UpdateExpression | BinaryExpression | </span><br><span class="line">AssignmentExpression | LogicalExpression | MemberExpression | ConditionalExpression | SimpleCallExpression | </span><br><span class="line">NewExpression | SequenceExpression | TemplateLiteral | TaggedTemplateExpression | ClassExpression | MetaProperty | </span><br><span class="line">AwaitExpression | Property | AssignmentProperty | Super | TemplateElement | SpreadElement | ObjectPattern |</span><br><span class="line"> ArrayPattern | RestElement | AssignmentPattern | ClassBody | MethodDefinition | ImportDeclaration | </span><br><span class="line"> ExportNamedDeclaration | ExportDefaultDeclaration | ExportAllDeclaration | ImportSpecifier | </span><br><span class="line"> ImportDefaultSpecifier | ImportNamespaceSpecifier | ExportSpecifier</span><br></pre></td></tr></table></figure>

<p>Babel 有文档对 AST 树的详细定义，可参考<a href="https://github.com/babel/babylon/blob/master/ast/spec.md" target="_blank" rel="noopener">这里</a></p>
<p>参考资料<br><a href="https://juejin.im/post/5b14257ef265da6e5546b14b" target="_blank" rel="noopener">Babel插件开发入门指南</a><br><a href="https://chengyuming.cn/views/webpack/AST.html" target="_blank" rel="noopener">AST 团队分享</a><br><a href="http://www.alloyteam.com/2017/04/analysis-of-babel-babel-overview/" target="_blank" rel="noopener">alloyteam-剖析babel</a><br><a href="https://cheogo.github.io/learn-javascript/201709/runtime.html" target="_blank" rel="noopener">JavaScript 语法解析、AST、V8、JIT</a><br><a href="https://blog.csdn.net/huangpb123/article/details/84799198" target="_blank" rel="noopener">详解AST抽象语法树</a></p>
]]></content>
      <categories>
        <category>Engineering</category>
      </categories>
      <tags>
        <tag>webpack</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS Modules</title>
    <url>/2020/04/30/CSS%20Modules/</url>
    <content><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>CSS Modules 不同于 less，sass 等，不是把CSS改造成编程语言，而是功能很单纯，只加入了局部作用域和模块依赖</p>
<h2 id="解决的问题"><a href="#解决的问题" class="headerlink" title="解决的问题"></a>解决的问题</h2><ul>
<li>全局样式冲突（主要问题）</li>
<li>css选择器嵌套过深，有性能问题</li>
<li>代码冗余（无继承）</li>
<li>模块化</li>
</ul>
<h1 id="局部作用域"><a href="#局部作用域" class="headerlink" title="局部作用域"></a>局部作用域</h1><p>CSS的规则都是全局的，任何一个组件的样式规则，都对整个页面有效。这使得在开发中很可能有类名相同造成相互影响的情况，多层嵌套css又会造成性能问题。</p>
<p>产生局部作用域的唯一方法，就是使用一个独一无二的<strong>class的名字</strong>，不会与其他选择器重名。这就是 CSS Modules 的做法</p>
<h2 id="基本写法"><a href="#基本写法" class="headerlink" title="基本写法"></a>基本写法</h2><p>一个React组件App.js。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">import React from &apos;react&apos;;</span><br><span class="line">import style from &apos;./App.css&apos;;</span><br><span class="line"></span><br><span class="line">export default () =&gt; &#123;</span><br><span class="line">  return (</span><br><span class="line">    &lt;h1 className=&#123;style.title&#125;&gt;</span><br><span class="line">      Hello World</span><br><span class="line">    &lt;/h1&gt;</span><br><span class="line">  );</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>App.css</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.title &#123;</span><br><span class="line">  color: red;</span><br><span class="line">&#125;</span><br><span class="line">// 这种写法等于：</span><br><span class="line">:local &#123;</span><br><span class="line">  .title &#123;</span><br><span class="line">    color: red;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>构建工具会将类名style.title编译成一个哈希字符串。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;h1 class=&quot;_3zyde4l1yATCOkgn-DBWEL&quot;&gt;</span><br><span class="line">  Hello World</span><br><span class="line">&lt;/h1&gt;</span><br></pre></td></tr></table></figure>

<p>App.css也会同时被编译。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">._3zyde4l1yATCOkgn-DBWEL &#123;</span><br><span class="line">  color: red;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="webpack的支持"><a href="#webpack的支持" class="headerlink" title="webpack的支持"></a>webpack的支持</h2><p>要开启 CSS Modules 功能，在webpack的loader配置中要有：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  test: /\.css$/,</span><br><span class="line">  loader: &quot;style-loader!css-loader?modules&quot;</span><br><span class="line">&#125;,</span><br></pre></td></tr></table></figure>

<p><code>style-loader!css-loader?modules</code>，它在css-loader后面加了一个查询参数 <code>modules</code>，表示打开 CSS Modules 功能<br>与 sass， less 联用时同理</p>
<h1 id="全局作用域"><a href="#全局作用域" class="headerlink" title="全局作用域"></a>全局作用域</h1><p>允许使用<code>:global(.className)</code>的语法，声明一个全局规则。凡是这样声明的class，都不会被编译成哈希字符串，而是保留类名<br>css:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.title &#123;</span><br><span class="line">  color: red;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">:global(.title) &#123;</span><br><span class="line">  color: green;</span><br><span class="line">&#125;</span><br><span class="line">// 更常用的写法：</span><br><span class="line">:global &#123;</span><br><span class="line">  .title &#123;</span><br><span class="line">    color: green;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>js:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// ...</span><br><span class="line">  &lt;div className=&quot;title&quot;&gt;Hello World!&lt;div&gt;</span><br></pre></td></tr></table></figure>

<p>结果会是 green</p>
<h1 id="定制哈希类名"><a href="#定制哈希类名" class="headerlink" title="定制哈希类名"></a>定制哈希类名</h1><p>css-loader默认的哈希算法是<code>[hash:base64]</code>，这会将.title编译成.<code>_3zyde4l1yATCOkgn-DBWEL</code>这样的字符串。</p>
<p>webpack.config.js里面可以定制哈希字符串的格式。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">module: &#123;</span><br><span class="line">  loaders: [</span><br><span class="line">    // ...</span><br><span class="line">    &#123;</span><br><span class="line">      test: /\.css$/,</span><br><span class="line">      loader: &quot;style-loader!css-loader?modules&amp;localIdentName=[path][name]---[local]---[hash:base64:5]&quot;</span><br><span class="line">    &#125;,</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>你会发现<code>.title</code>被编译成了<code>demo03-components-App---title---GpMto</code></p>
<h1 id="一些技巧"><a href="#一些技巧" class="headerlink" title="一些技巧"></a>一些技巧</h1><h2 id="组合-composition"><a href="#组合-composition" class="headerlink" title="组合 composition"></a>组合 composition</h2><p>在App.css中，让.title继承 .className 。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.className &#123;</span><br><span class="line">  background-color: blue;</span><br><span class="line">&#125;</span><br><span class="line">.title &#123;</span><br><span class="line">  composes: className;</span><br><span class="line">  color: red;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="输入其他模块"><a href="#输入其他模块" class="headerlink" title="输入其他模块"></a>输入其他模块</h2><p>选择器也可以继承其他CSS文件里面的规则。<br>another.css:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.className &#123;</span><br><span class="line">  background-color: blue;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>App.css可以继承another.css里面的规则。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.title &#123;</span><br><span class="line">  composes: className from &apos;./another.css&apos;;</span><br><span class="line">  color: red;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h2><p>这些和 less，sass 等联合使用</p>
<h2 id="关于样式覆盖问题"><a href="#关于样式覆盖问题" class="headerlink" title="关于样式覆盖问题"></a>关于样式覆盖问题</h2><p>因为CSS Modules 不会覆盖属性选择器,所以可以利用属性选择器来解决这个问题<br>// 组件中</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">return (</span><br><span class="line">  &lt;div data-role=&apos;test&apos;&gt;</span><br><span class="line">    测试</span><br><span class="line">  &lt;/div&gt;</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p>// 样式</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[data-role=&quot;test&quot;] &#123;</span><br><span class="line">  color: red;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>参考资料<br><a href="http://www.ruanyifeng.com/blog/2016/06/css_modules.html" target="_blank" rel="noopener">阮一峰</a><br><a href="https://www.cnblogs.com/kugeliu/p/7889018.html" target="_blank" rel="noopener">博客园</a><br><a href="https://juejin.im/post/5b8373b2e51d4538e6332dbb" target="_blank" rel="noopener">掘金</a></p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>ES6小结</title>
    <url>/2019/10/14/ES6%E5%B0%8F%E7%BB%93/</url>
    <content><![CDATA[<h3 id="变量提升与var、let及const"><a href="#变量提升与var、let及const" class="headerlink" title="变量提升与var、let及const"></a>变量提升与var、let及const</h3><ul>
<li>函数提升优先于变量提升（var），函数提升会把整个函数挪到作用域顶部，变量提升只会把声明挪到作用域顶部</li>
<li>var 存在变量提升，我们能在声明之前使用。let、const 因为暂时性死区的原因，不能在声明前使用</li>
<li><strong>var 在全局作用域下声明变量会导致变量挂载在 window 上（变成window的属性），其他两者不会</strong></li>
<li>let 和 const 作用基本一致，但是后者声明的变量不能再次赋值</li>
</ul>
<h5 id="var、let及const的区别"><a href="#var、let及const的区别" class="headerlink" title="var、let及const的区别"></a>var、let及const的区别</h5><ul>
<li>let（建议用let取代var）<ul>
<li>作用域是<strong>块级作用域</strong>（之前只有函数作用域和全局作用域）（并且for循环的括号和{}是在两个不同的作用域，可以独立声明同名变量并使用）</li>
<li><strong>不存在变量声明提前 （在let之前使用，会报错（因为暂时性死区）</strong> is not defined）</li>
<li><strong>不可以重复定义</strong> （var可以，不会报错，但是let 会说has already been declared)</li>
<li>存在存在暂时性死区<br>在块级作用域内存在let/const，它所声明的变量就“绑定”了这个区域。在这个代码块（块级作用域）内，使用let/const命令声明变量之前，该变量都是不可用。意义：标准化代码，将所有的变量声明放在最前面</li>
</ul>
</li>
<li>const（所有的函数都应该用const）<ul>
<li>一般用来声明常量，不允许修改</li>
<li>一旦声明变量，就必须立即赋值</li>
<li>和let一样，都是块级作用域，存在暂时性死区，没有变量声明提前，不允许重复定义</li>
</ul>
</li>
<li>var<ul>
<li>var的作用域是函数作用域，在一个函数内部利用var声明一个变量，则这个变量只在函数内有效</li>
<li>存在变量声明提前（但是赋值并没有提前，提前访问会返回undefined)</li>
</ul>
</li>
</ul>
<h3 id="变量的结构赋值"><a href="#变量的结构赋值" class="headerlink" title="变量的结构赋值"></a>变量的结构赋值</h3><p>如下所述</p>
<ul>
<li><p>数组的解构赋值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> [a, b, c] = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];  <span class="comment">// 可以一次性为三个变量赋值</span></span><br><span class="line"><span class="keyword">let</span> [ , , third] = [<span class="string">"foo"</span>, <span class="string">"bar"</span>, <span class="string">"baz"</span>];  <span class="comment">// third为'baz</span></span><br><span class="line"><span class="keyword">let</span> [head, ...tail] = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>];</span><br><span class="line">head <span class="comment">// 1</span></span><br><span class="line">tail <span class="comment">// [2, 3, 4]</span></span><br></pre></td></tr></table></figure></li>
<li><p>对象的解构赋值。eg. let { foo, bar } = { foo: ‘aaa’, bar: ‘bbb’ };   变量必须与属性同名，才能取到正确的值</p>
</li>
<li><p>字符串的解构赋值。eg. const [a, b, c, d, e] = ‘hello’;</p>
</li>
<li><p>函数参数也可以用解构赋值。可以实现：函数的参数表面上是一个数组/对象，但在传入参数的那一刻，<strong>数组参数就被解构成变量x和y。对于函数内部的代码来说，它们能感受到的参数就是x和y</strong></p>
</li>
<li><p><strong>… 就是让数组把item全部铺开的意思</strong></p>
</li>
</ul>
<p>此外还可以设置默认值:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> &#123;x, y = <span class="number">5</span>&#125; = &#123;<span class="attr">x</span>: <span class="number">1</span>&#125;;</span><br><span class="line">x <span class="comment">// 1</span></span><br><span class="line">y <span class="comment">// 5</span></span><br></pre></td></tr></table></figure>

<p><strong>应用</strong>：使用数组成员对变量赋值；函数的参数如果是对象的成员，优先使用解构赋值；如果函数返回多个值，优先使用对象的解构赋值；使用扩展运算符（…）拷贝数组，eg.拷贝item数组： <code>const itemsCopy = [...items]; </code></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// bad</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getFullName</span>(<span class="params">user</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> firstName = user.firstName;</span><br><span class="line">  <span class="keyword">const</span> lastName = user.lastName;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// best</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getFullName</span>(<span class="params">&#123; firstName, lastName &#125;</span>) </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="常用的扩展方法"><a href="#常用的扩展方法" class="headerlink" title="常用的扩展方法"></a>常用的扩展方法</h3><ul>
<li>Array.from() 将类似数组的对象转为数组（arguement，Nodelist），也能将普通对象的key值取出来生成一个数组。</li>
</ul>
<h3 id="Symbol，Set和Map"><a href="#Symbol，Set和Map" class="headerlink" title="Symbol，Set和Map"></a>Symbol，Set和Map</h3><ul>
<li>新的原始数据类型Symbol，表示独一无二的值。可以作为对象的属性名使用，但此时Symbol为名属性，不会出现在for…in和for…if循环中也不会被Object.keys()、Object.getOwnPropertyNames()、JSON.stringify()返回。Object.getOwnPropertySymbols方法返回一个数组，成员是当前对象的所有用作属性名的 Symbol 值。Reflect.ownKeys方法可以返回所有类型的键名，包括常规键名和 Symbol 键名，生成一个数组。</li>
<li>新的数据结构 Set。它类似于数组，但是成员的值都是唯一的，没有重复的值</li>
<li>新的数据结构 Map。类似于对象，也是键值对的集合，但是“键”的范围不限于字符串，各种类型的值（包括对象）都可以当作键</li>
</ul>
<h3 id="Promise-Generator-async函数"><a href="#Promise-Generator-async函数" class="headerlink" title="Promise, Generator, async函数"></a>Promise, Generator, async函数</h3><p>见我写的另一篇文章：<a href="https://jacleklm.github.io/2019/11/01/%E5%BC%82%E6%AD%A5%E7%BC%96%E7%A8%8B/#more" target="_blank" rel="noopener">《异步编程》</a></p>
<h3 id="Proxy"><a href="#Proxy" class="headerlink" title="Proxy"></a>Proxy</h3><p>参考资料<br><a href="https://es6.ruanyifeng.com/" target="_blank" rel="noopener">ECMAScript6标准入门</a><br><a href="https://blog.nowcoder.net/yiizgo" target="_blank" rel="noopener">呓语</a><br><a href="https://github.com/poetries/" target="_blank" rel="noopener">poetries</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS知识点</title>
    <url>/2019/10/08/CSS%E7%9F%A5%E8%AF%86%E7%82%B9/</url>
    <content><![CDATA[<h3 id="CSS-基础"><a href="#CSS-基础" class="headerlink" title="CSS 基础"></a>CSS 基础</h3><p>1、选择器<br>2、盒模型<br>3、伪类（处理 a 标签的那些和用来处理选择器的:first-child，:nth-child 等）和伪元素(eg. 处理 p 标签首行或首字母的:first-line 和:first-letter ；在元素的内容之前或之后插入内容的:before 和:after)<br>4、页面导入样式时，使用 link 和@import 的区别</p>
<ul>
<li>link 属于 XHTML 标签，除了加载 CSS 外，定义 rel 连接属性等作用；而@import 是 CSS 提供的，只能用于加载 CSS;</li>
<li>页面被加载的时，link 会同时被加载，而@import 引用的 CSS 会等到页面被加载完再加载;</li>
<li>@import 是 CSS2.1 提出的，只在 IE5 以上才能被识别，而 link 是 XHTML 标签，无兼容问题;</li>
</ul>
<p>5、</p>
<ul>
<li>行内元素和块级元素的区别：行内元素会在一行水平方向排列，块级元素独占一行，自动填满父级元素；块级元素可以包含行内元素和块级元素，行内只能包含文本和其他行内元素；行内元素不能设置宽高</li>
<li>inline-block、inline 和 block 的区别：block 除了上述特性外，还有前后都有换行符；inline 设置宽高无效，margin 在竖直方向无效，padding 有效（但最好也不要设，不会挤开其他盒子的内容），前后无换行符；inline-block 可以设置宽高，margin/padding 有效，前后无换行符</li>
</ul>
<p>6、为什么 img 是 inline 还可以设置宽高<br>img 是置换元素。置换元素是具有固有尺寸（intrinsic dimensions），浏览器根据其标签和属性决定显示内容的元素。<br>浏览器根据 img 的 src 属性渲染出对应路径的图片。图片具有固有尺寸，即你不做设置时，它就会照其固有尺寸渲染。<br>这类元素的展现效果不是由 CSS 来控制的。他们是一种外部对象，外观的渲染独立于 CSS。内容不受当前文档的样式影响，CSS 可以影响置换的位置，但是不会影响到置换自身的内容。（比如 iframe，可能有自己的样式表，不会继承父文档的样式）置换元素有内置宽高，性质同设置了 inline-block 一样。置换元素还有视觉元素 img、object, video；表单元素 input，select，textarea；audio 和 canvas</p>
<p>7、CSS 选择器的权重排序</p>
<ol>
<li>！important 权重无限大</li>
<li>内联样式 写在 html 标签里的，权值为 1000</li>
<li>id，权值为 100</li>
<li>类 伪类 和属性选择器，权值为 10</li>
<li>标签选择器和伪元素选择器 div p:after，权值为 1</li>
<li>通配符、子选择器、相邻选择器</li>
<li><strong>继承的样式没有权值</strong></li>
</ol>
<p>8、外边距重叠<br><strong>相邻</strong>的两个盒子（可能是兄弟关系也可能是祖先关系）的外边距结合成一个单独的外边距。这种合并外边距的方式被称为折叠，并且因而所结合成的外边距称为折叠外边距（所以有时候让其不发生可以加个空格或者空标签）。其折叠结果遵循下列规则：</p>
<ul>
<li>两个相邻的外边距都是正数时，折叠结果是它们两者之间较大的值</li>
<li>两个相邻的外边距都是负数时，折叠结果是两者绝对值的较大值</li>
<li>两个外边距一正一负时，折叠结果是两者的相加的和</li>
</ul>
<p>解决方法：两者间加个空格或空盒子；给其中一个盒子加 overflow: hidden 形成 BFC</p>
<p>9、层叠上下文<br>层叠上线文就是结界，其中的元素如果跟层叠上下文之外的元素发生层叠，就比较他们的层叠水平高低来显示。<br>创建的方法：postion 为 relative、absolute、fixed 的元素设置 z-index<br>顺序是：底层的 border、background，负值 z-index，块级盒子，浮动盒子，内联盒子，z-index：auto, 正 z-index<br>10、元素隐藏方法和区别</p>
<ul>
<li>display：none 元素不可见，不占据空间，资源会加载，DOM 可以访问</li>
<li>visibility:hidden 元素不可见，不能点击，但占据空间，资源会加载，可以使用。</li>
<li>opacity：0 元素不可见、可以点击，占据空间，可以使用。（不占据的话再加一个 position absolute)(不能点击不占据空间 postion absolute+z-index:-1)(不能点击、占据空间 postion relative z-index:-1)</li>
</ul>
<p><strong><code>display：none</code>和<code>visibility:hidden</code>的区别</strong></p>
<ul>
<li>display：none 元素不占据空间，visibility:hidden 空间保留</li>
<li>display：none 会影响 opacity 过渡效果</li>
<li>display 会产生重绘回流，visibility:hidden 只重绘</li>
<li>display：none 节点和子孙节点都不见，visibility:hidden 的子孙节点可以设置 visibility:visible 显示。</li>
<li>visibility:hidden 不会影响计数器计数（ol 标签）</li>
</ul>
<p>11、img 和 background-image 的区别</p>
<ul>
<li>解析机制：img 属于 html 标签，background-img 属于 css。img 先解析</li>
<li>SEO：img 标签有一个 alt 属性可以指定图像的替代文本，有利于 SEO，并且在图片加载失败时有利于阅读</li>
<li>语义化角度：img 语义更加明确</li>
</ul>
<p>12、rgba()和 opacity 的区别</p>
<ul>
<li>opacity 作用于元素及元素中所有的内容（包括文字、图片） 有继承性</li>
<li>rgba()只用于元素的颜色及背景色</li>
<li>当 opacity 属性的值应用于某个元素上时，把这个元素和它内容当作一个整体来看待，即使这个值没有被子元素继承。因此一个元素和它包含的元素都会有与元素背景相同的透明度，哪怕父子元素由不同的 opacity 的值。</li>
</ul>
<p>13、CSS 哪些属性可以被继承</p>
<ul>
<li>字体相关属性<ul>
<li>font-family 字体系列</li>
<li>font-weight 字体粗细程度</li>
<li>font-size 字体大小</li>
<li>font-style 字体风格</li>
</ul>
</li>
<li>文本相关属性<ul>
<li>text-indent 文本缩进</li>
<li>text-align 文本对齐方式</li>
<li>line-height 行高</li>
<li>color 文本颜色</li>
</ul>
</li>
<li>元素可见性<ul>
<li>visibility 控制元素的显示和隐藏</li>
</ul>
</li>
<li>列表布局属性<ul>
<li>list-style 列表风格 包括 list-style-type list-style-image 等</li>
</ul>
</li>
<li>光标属性<ul>
<li>cursor 光标显示的各种形态</li>
</ul>
</li>
</ul>
<p>14、transition、animation 的区别<br>二者都能实现动画，animation 和 transition 大部分属性相同，都是随时间改变元素的属性值，区别是 transition 需要触发一个事件（eg. :hover）才能改变属性；animation 不需要触发任何事件随时间改变属性。transition 为 2 帧，from……to ，animation 可以一帧一帧的。</p>
<p>15、重绘和回流（重排）是什么，如何避免？<br>重绘是当节点需要更改外观而不会影响布局的，比如改变 color 就叫称为重绘<br>回流是布局或者几何属性需要改变就称为回流。<br>回流必定会发生重绘，重绘不一定会引发回流。回流所需的成本比重绘高的多，改变父节点里的子节点很可能会导致父节点的一系列回流。<br>导致回流发生的因素：</p>
<ol>
<li>调整窗口大小，浏览器窗口尺寸改变——resize 事件发生时</li>
<li>元素尺寸改变——边距、填充、边框、宽度和高度；改变字体大小</li>
<li>页面渲染初始化</li>
<li>元素位置改变</li>
<li>样式表变动</li>
<li>元素内容变化，尤其是输入控件</li>
<li>CSS 伪类激活</li>
<li>DOM 操作，添加或者删除可见的 DOM 元素</li>
<li>offsetWidth, width, clientWidth, scrollTop/scrollHeight 的计算， 会使浏览器将渐进回流队列 Flush，立即执行回流。内容改变——比如文本改变或者图片大小改变而引起的计算值宽度和高度改变</li>
</ol>
<p><strong>减少重绘和回流</strong></p>
<ul>
<li>使用 transform 替代 top</li>
<li>使用 visibility 替换 display: none ，因为前者只会引起重绘，后者会引发回流（改变了布局）</li>
<li>不要使用 table 布局，可能很小的一个小改动会造成整个 table 的重新布局</li>
<li>CSS 选择符从右往左匹配查找，避免节点层级过多</li>
<li>将频繁重绘或者回流的节点设置为图层，图层能够阻止该节点的渲染行为影响别的节点。比如对于 video 标签来说，浏览器会自动将该节点变为图层。通过以下几个常用属性可以生成新图层：<ul>
<li>position: fixed</li>
<li>video、iframe 标签</li>
<li>通过动画实现的 opacity 动画转换</li>
</ul>
</li>
</ul>
<p>16、CSS Sprites<br>是一种网页图片应用处理方式。它允许将一个页面涉及到的所有零星图片都包含到一张大图中， 利用 CSS 的“background-image”，“background- repeat”，“background-position”的组合进行背景定位， 访问页面时避免图片载入缓慢的现象。<br><strong>优点</strong>：减少 HTTP 请求数，极大地提高页面加载速度；提高压缩比，减少图片大小，减少图片的字节；更换风格方便，只需在一张或几张图片上修改颜色或样式即可实现<br><strong>缺点</strong>：图片合并麻烦；维护麻烦，修改一个图片可能需要从新布局整个图片，样式</p>
<p>17、padding颜色是背景色 </p>
<h3 id="CSS3"><a href="#CSS3" class="headerlink" title="CSS3"></a>CSS3</h3><h5 id="新特性"><a href="#新特性" class="headerlink" title="新特性"></a>新特性</h5><ul>
<li>新增选择器：伪类选择器 E：nth-child(n) E:nth-last-child(n)</li>
<li>Transition、Transform 和 Animation</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">transition</span>：</span><br><span class="line"><span class="selector-tag">div</span>&#123;</span><br><span class="line">    <span class="attribute">transition-property</span>:width;</span><br><span class="line">    <span class="attribute">transition-duration</span>:<span class="number">1s</span>;</span><br><span class="line">    <span class="attribute">transition-timing-function</span>:linear;</span><br><span class="line">    <span class="attribute">transition-delay</span>:<span class="number">25</span>;</span><br><span class="line">    <span class="comment">/*简写形式*/</span></span><br><span class="line">    <span class="attribute">transition</span>:width <span class="number">1s</span> linear <span class="number">25</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">transform</span>:</span><br><span class="line"><span class="selector-tag">scale</span>()大小, <span class="selector-tag">translate</span>()位置</span><br><span class="line"><span class="selector-tag">div</span>&#123;</span><br><span class="line">    <span class="attribute">transform</span>:<span class="built_in">rotate</span>(<span class="number">7deg</span>);</span><br><span class="line">    <span class="attribute">-ms-transform</span>:<span class="built_in">rotate</span>(<span class="number">7deg</span>);<span class="comment">/*IE9*/</span></span><br><span class="line">    <span class="attribute">-moz-transform</span>:<span class="built_in">rotate</span>(<span class="number">7deg</span>);<span class="comment">/*FireFox*/</span></span><br><span class="line">    <span class="attribute">-webkit-transform</span>:<span class="built_in">rotate</span>(<span class="number">7deg</span>);<span class="comment">/*Safari和Chrome*/</span></span><br><span class="line">    <span class="attribute">-o-transform</span>:<span class="built_in">rotate</span>(<span class="number">7deg</span>);<span class="comment">/*Opwea*/</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">animation</span>:</span><br><span class="line"><span class="keyword">@keyframes</span> myAnimation&#123;</span><br><span class="line">    0%&#123;<span class="attribute">background</span>:red;<span class="attribute">width</span>:<span class="number">100px</span>;&#125;</span><br><span class="line">    25%&#123;<span class="attribute">background</span>:orange;<span class="attribute">width</span>:<span class="number">200px</span>;&#125;</span><br><span class="line">    50%&#123;<span class="attribute">background</span>:yellow;<span class="attribute">width</span>:<span class="number">100px</span>;&#125;</span><br><span class="line">    75%&#123;<span class="attribute">background</span>:green;<span class="attribute">width</span>:<span class="number">200px</span>;&#125;</span><br><span class="line">    100%&#123;<span class="attribute">background</span>:blue:width:<span class="number">100px</span>;&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-id">#box</span>&#123;</span><br><span class="line">    <span class="attribute">animation</span>:myAnimation <span class="number">5s</span>;</span><br><span class="line">    <span class="attribute">width</span>:<span class="number">100px</span>;</span><br><span class="line">    <span class="attribute">height</span>:<span class="number">100px</span>;</span><br><span class="line">    <span class="attribute">background</span>:red;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>边框：box-shadow, border-radius, border-image</li>
<li>背景：background-clip, 自定义背景图片的裁剪区域 background-size</li>
<li>文字：text-shadow, text-overflow 当文本溢出包含元素时发生的事情</li>
<li>字体：@font-face 可以引用 Web 服务器的字体</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@font-face</span> &#123;</span><br><span class="line">  <span class="attribute">font-family</span>: myFirstFont;</span><br><span class="line">  <span class="attribute">src</span>: <span class="built_in">url</span>(sansation_light, woff);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>17、Rem,em 和 px 的区别<br>px 是绝对长度单位；em 是相对长度单位，继承父级元素的字体大小，所有字体都是相对于父元素大小的；rem 是相对长度单位，根元素（html),不会像 em 造成混乱。<br>元素的 margin，padding 设置百分数，百分数是相对于最近的块级父元素的 width 计算<br>18、移动端 0.5px 边框（直接写 border 是 0.5px 在安卓下会显示不出来）<br>利用 伪元素 结合 transform: scale(0.5, 0.5)<br>思路:</p>
<ol>
<li>目标盒子设置为相对定位。给目标盒子加伪元素:before，并设置为绝对定位；</li>
<li>给伪元素添加 1px 的边框，box-sizing: border-box，宽和高设置为 200%，整个盒子模型缩小 transform: scale(0.5, 0.5)；</li>
<li>调整盒子模型的位置，以左上角为基准 transform-origin: 0 0;</li>
</ol>
<p>19、 calc 结合 vw 、vh、vmin 、vmax 的使用<br>eg. width：calc(100% - 10px) 。括号中可以加一些表达式</p>
<ul>
<li>vw：视窗宽度的百分比（1vw 代表视窗的宽度为 1%）</li>
<li>vh：视窗高度的百分比</li>
<li>vmin：当前 vw 和 vh 中较小的一个值</li>
<li>vmax：当前 vw 和 vh 中较大的一个值</li>
</ul>
<h3 id="CSS-模块化"><a href="#CSS-模块化" class="headerlink" title="CSS 模块化"></a>CSS 模块化</h3><h5 id="vue-中-style-标签中设置-scoped-的原理"><a href="#vue-中-style-标签中设置-scoped-的原理" class="headerlink" title="vue 中 style 标签中设置 scoped 的原理"></a>vue 中 style 标签中设置 scoped 的原理</h5><p>这个局部样式是通过 PostCSS 给组件中所有的 DOM 添加了一个独一无二的动态属性，然后通过 CSS 属性选择器来选择组件中的 DOM</p>
<h5 id="CSS-预处理"><a href="#CSS-预处理" class="headerlink" title="CSS 预处理"></a>CSS 预处理</h5><p><strong>优点</strong><br>用一种专门的编程语言，进行 Web 页面样式设计，然后再编译成正常的 CSS 文件，以便项目使用；CSS 更加简洁、适应性更强，更易于代码的维护<br>比如可以用 sass，stylus 和 less</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>HTML</title>
    <url>/2019/10/08/HTML/</url>
    <content><![CDATA[<h3 id="HTML基础"><a href="#HTML基础" class="headerlink" title="HTML基础"></a>HTML基础</h3><p>1、 Doctype作用？严格模式与混杂模式如何区分？它们有何意义?</p>
<ul>
<li>作用：声明文件类型定义（DTD），位于文档中最前面，作用是为了告知浏览器应该用哪种文档类型规范来解析文档</li>
<li>定义<ul>
<li>严格模式（标准模式)，浏览器按照W3C标准来解析</li>
<li>混杂模式，向后兼容的解析方法，浏览器用自己的方式解析代码</li>
</ul>
</li>
<li>如何区分？<br>用DTD来判断<ul>
<li>有严格格式DTD——严格模式；</li>
<li>有URL的过渡DTD——严格模式，没有URL的过渡DTD——混杂模式；</li>
<li>DTD不存在/格式不对——混杂模式；</li>
<li>HTML5没有严格和混杂之分</li>
</ul>
</li>
<li>区分的意义<br>严格模式的排版和js运行模式以浏览器支持的最高标准运行。如果只存在严格模式，那么很多旧网站站点无法工作。</li>
</ul>
<p>2、 head中的meta：提供关于 HTML 文档的元数据(规定页面的描述、关键词、文档的作者、编码方式等)，属性有 http-equiv + content； name + content。<br>3、 无序列表ul，有序列表ol,表格table-tr-td<br>4、 表单form（action=”定义在submit时候的动作” method=”GET/POST”）（一般在form正下方会接个fieldset标签和legend标签（定义标题））<br>表单元素：input（type=”text/radio/submit…）,下拉列表select-option,大文本输入框textarea<br>HTML5新增表单元素：datalist，结合input、option使用，能实现输入框有下拉菜单效果。还有相对少用的keygen、output<br>5、 em来表示强调的文本，应该使用strong来表示重要文本(内容重要性)<br>6、 src和herf的区别<br>href指向网络资源位置，建立当前文档和资源的连接，一般用于超链接<br>src将所指向资源嵌入到当前文档标签中，在请求src资源时会将其指向的资源下载并应用到文档内，例如js脚本，img图片和frame等元素。当浏览器解析到该元素时，会暂停其他资源的下载和处理，直到将该资源加载、编译、执行完毕。这也是为什么将js脚本放在底部而不是头部。<br>7、 WEB标准以及W3C标准是什么?<br>标签闭合、标签小写、不乱嵌套、属性值用双引号、使用外链css和js、结构行为表现的分离<br>8、 语义化的理解</p>
<ul>
<li>用正确的标签做正确的事情！</li>
<li>HTML语义化就是让页面的内容结构化，便于对浏览器、搜索引擎解析；</li>
<li>在没有样式CSS情况下也以一种文档格式显示，并且是容易阅读的。</li>
<li>搜索引擎的爬虫依赖于标记来确定上下文和各个关键字的权重，利于 SEO。</li>
<li>使阅读源代码的人对网站更容易将网站分块，便于阅读维护理解</li>
</ul>
<p>9、 XHTML和HTML有什么区别?<br>XHTML 是更严谨更纯净的HTML版本</p>
<ul>
<li>一个是功能上的差别：主要是XHTML可兼容各大浏览器、手机以及PDA，并且浏览器也能快速正确地编译网页</li>
<li>另外是书写习惯的差别：XHTML元素必须被正确地嵌套，闭合，区分大小写，文档必须拥有根元素</li>
</ul>
<p>10、行内元素有哪些？块级元素有哪些？ 空(void)元素有那些？行内元素和块级元素有什么区别？</p>
<ul>
<li>行内元素有：a b span strong img input select </li>
<li>块级元素有：div ul ol li dl dt dd（都是列表类） h1 h2 h3 h4… p <strong>form</strong> <strong>hr</strong></li>
<li>空元素：br hr（水平线） img input link meta （不需要闭标签）</li>
<li>行内元素不可以设置宽高，不独占一行</li>
<li>块级元素可以设置宽高，独占一行</li>
</ul>
<p>11、渐进增强和优雅降级<br>渐进增强：针对低版本浏览器进行构建页面，保证最基本的功能，然后再针对高级浏览器进行效果、交互等改进和追加功能达到更好的用户体验。<br>优雅降级：一开始就构建完整的功能，然后再针对低版本浏览器进行兼容。</p>
<blockquote>
<p>区别：优雅降级是从复杂的现状开始，并试图减少用户体验的供给，而渐进增强则是从一个非常基础的，能够起作用的版本开始，并不断扩充，以适应未来环境的需要。降级（功&gt; 能衰减）意味着往回看；而渐进增强则意味着朝前看，同时保证其根基处于安全地带</p>
</blockquote>
<p>12、a标签href不跳转<br>当页面中a标签不需要任何跳转时，从原理上来讲，可分如下两种方法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 标签属性href，使其指向空或不返回任何内容。如：</span></span><br><span class="line">&lt;a href=<span class="string">"javascript:void(0);"</span> &gt;点此无反应javascript:<span class="keyword">void</span>(<span class="number">0</span>)&lt;<span class="regexp">/a&gt;</span></span><br><span class="line"><span class="regexp">&lt;a href="javascript:;" &gt;点此无反应javascript:&lt;/</span>a&gt;</span><br><span class="line"><span class="comment">// 标签事件onclick，阻止其默认行为。如：</span></span><br><span class="line">&lt;a href=<span class="string">""</span> onclick=<span class="string">"return false;"</span>&gt;<span class="keyword">return</span> <span class="literal">false</span>;&lt;<span class="regexp">/a&gt;</span></span><br><span class="line"><span class="regexp">&lt;a href="#" onclick="return false;"&gt;return false;&lt;/</span>a&gt;</span><br><span class="line"><span class="comment">// 注意：只有一个href="#"是不可以的。</span></span><br></pre></td></tr></table></figure>

<p>13、<code>ifame</code>标签：一个页面有多个window</p>
<ul>
<li>可以通过contentWindow和contentDocument两个API获取iframe的window对象和document对象</li>
<li>通过window.self，window.parent，window.top这三个属性分别获取自身window对象，父级window对象，顶级window对象</li>
</ul>
<p>14、如果一个元素拥有ID属性,那么会创建同名的全局变量</p>
<h3 id="HTML5"><a href="#HTML5" class="headerlink" title="HTML5"></a>HTML5</h3><p>1、html5有哪些新特性、移除了那些元素？</p>
<ul>
<li>HTML5 现在已经不是 SGML 的子集，主要是关于图像，位置，存储，多任务等功能的增加<ul>
<li>新增元素<ul>
<li>通过 JavaScript 来绘制2D图形的 canvas 画布标签</li>
<li>使用 XML 描述 2D 图形的语言：SVG （好比HTML相对于文本）</li>
<li>视频 video 和 音频 audio 元素</li>
<li>语意化更好的内容元素，比如 article、footer、header、nav、section</li>
</ul>
</li>
<li>API及功能<ul>
<li>获取用户地理位置的 geolocationAPI</li>
<li>draggable 属性可拖动</li>
<li>Web存储。本地离线存储 localStorage 长期存储数据，浏览器关闭后数据不丢失；sessionStorage 的数据在浏览器关闭后自动删除</li>
<li>应用程序缓存（Application Cache），通过创建 cache manifest 文件，可创建 web 应用的离线版本。</li>
<li>新的技术webworker（是运行在后台的 JavaScript，位于外部文件中，不会影响页面的性能）、 websocket、 </li>
</ul>
</li>
</ul>
</li>
<li>移除的元素：<ul>
<li>纯表现的元素：basefont、big、center、font、 s、strike、tt、u</li>
<li>对可用性产生负面影响的元素：frame、frameset、noframes</li>
</ul>
</li>
<li>支持HTML5新标签：<ul>
<li>IE8/IE7/IE6支持通过document.createElement方法产生的标签</li>
<li>可以利用这一特性让这些浏览器支持HTML5新标签</li>
<li>浏览器支持新标签后，还需要添加标签默认的样式</li>
</ul>
</li>
<li>当然也可以直接使用成熟的框架、比如html5shim</li>
</ul>
<p>2、 HTML5 为什么只需要写 <code>!DOCTYPE HTML</code><br>HTML5 不基于SGML（不是SGML的子集），因此不需要对DTD进行引用，但是需要doctype来规范浏览器的行为<br>而HTML4.01基于SGML,所以需要对DTD进行引用，才能告知浏览器文档所使用的文档类型</p>
<blockquote>
<p>SGML：标准通用标记语言,国际上定义电子文件结构和内容描述的标准，一个庞大的标准，应用范围很广<br>XML：可扩展标识语言（eXtensible Markup Language），一种精简的SGML版本，一个用来定义其他语言的元语言</p>
</blockquote>
<p>3、HTML5的离线储存怎么使用及其原理</p>
<ul>
<li>功能：在用户没有网络连接时，可以正常访问站点，在用户与有网络连接时，更新用户机器上的缓存文件</li>
<li>原理：基于一个新建的.appcache文件（缓存清单），浏览器打开的并有网络连接的时候，会通过这个清单进行资源缓存。之后离线就可以根据缓存显示页面</li>
<li>使用：在head标签加manifest，指向一个.appcache文件，对该文件编写即可。文件内容分为三部分：CACHE MANIFEST（需要缓存的资源）；NETWORK（不缓存的资源）；FALLBACK（若无法建立因特网连接，就用某文件替代某个文件）。离线状态时，操作window.applicationCache进行需求实现</li>
</ul>
<p>4、cookies，sessionStorage 和 localStorage 的区别</p>
<ul>
<li>cookie是网站为了标示用户身份而储存在用户本地终端（Client Side）上的数据（通常经过加密）</li>
<li>cookie数据始终在同源的http请求中携带（即使不需要），记会在浏览器和服务器间来回传递</li>
<li>sessionStorage和localStorage不会自动把数据发给服务器，仅在本地保存</li>
<li>存储大小：<ul>
<li>cookie数据大小不能超过4k</li>
<li>sessionStorage和localStorage虽然也有存储大小的限制，但比cookie大得多，可以达到5M或更大</li>
</ul>
</li>
<li>有期时间：<ul>
<li>localStorage 存储持久数据，浏览器关闭后数据不丢失除非主动删除数据</li>
<li>sessionStorage 数据在当前浏览器窗口关闭后自动删除</li>
<li>cookie 设置的cookie过期时间之前一直有效，即使窗口或浏览器关闭</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>HTML</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTP</title>
    <url>/2019/10/02/HTTP/</url>
    <content><![CDATA[<h3 id="HTTP-方法"><a href="#HTTP-方法" class="headerlink" title="HTTP 方法"></a>HTTP 方法</h3><ul>
<li>GET 获取资源。当前网络请求中，绝大部分使用的是 GET 方法</li>
<li>POST 传输实体主体。POST 主要用来传输数据，而 GET 主要用来获取资源</li>
<li>HEAD 获取报文首部。和 GET 方法类似，但是不返回报文实体主体部分。主要用于确认 URL 的有效性以及资源更新的日期时间等。</li>
<li>PUT 上传文件</li>
<li>OPTIONS 查询支持的方法</li>
<li>*DELETE 删除文件</li>
<li>*PATCH 对资源进行部分修改</li>
<li>*CONNECT 要求在与代理服务器通信时建立隧道</li>
</ul>
<h3 id="GET-和-POST-的区别"><a href="#GET-和-POST-的区别" class="headerlink" title="GET 和 POST 的区别"></a>GET 和 POST 的区别</h3><p>从用法上说，GET 一般用于无副作用、幂等的场景；POST 多用于有副作用、不幂等的情况（一次POST请求会服务器会添加一份新资源）。</p>
<blockquote>
<p>冥等的定义：发送 M 和 N 次请求，服务器上资源状态一致。比如说，注册 10 个账号和 11 个账号是不冥等的，对文章进行了 10 次 11 次修改是幂等的，因为前者多了一个账号（资源），后者是更新同一个资源。<br>副作用的定义：副作用是指对服务器上资源做改变。比如搜索是无副作用的，但更新是有副作用的。</p>
</blockquote>
<p>从本质上说，Post 和 Get 都取决于 HTTP，使用哪个方法与应用层传输没有必然的联系。HTTP 没有要求，如果是 POST，数据就要放在 BODY 中。也没有要求 GET，数据（参数）就一定要放在 URL 中而不能放在 BODY 中。</p>
<p>细节上有一些区别：</p>
<ol>
<li>Get 能请求缓存，但是 Post 不可以</li>
<li>Post 支持更多编码类型</li>
<li>Get 回退无害，Post 会再次提交</li>
<li>Get 能被保存为书签，Post 不可以</li>
<li>POST有消息体（body），长度无限制；GET一般没有body，数据只能放在url中，而url长度是有限的。其实也没有强制规定GET不能有body中，但是一般不会这么做</li>
</ol>
<p>简而言之，就是“安全”和“不安全”的区别。eg. 点击同意某个协议，最好用POST</p>
<h3 id="PUT-和-POST-的区别"><a href="#PUT-和-POST-的区别" class="headerlink" title="PUT 和 POST 的区别"></a>PUT 和 POST 的区别</h3><p>用于向指定URL传送<strong>更新</strong>资源，是<strong>幂等</strong>的。比如修改用户密码，虽然提交的还是账户名跟用户密码这个俩个必填参数，但是每次提交都只是<strong>更新</strong>该用户密码,每次请求都只是覆盖原先的值。此时就该用PUT<br>POST用于提交请求，可以更新或者创建资源，是非幂等的。eg. 注册新用户用POST给服务器传输新用户名和密码</p>
<h3 id="HTTP-常用首部"><a href="#HTTP-常用首部" class="headerlink" title="HTTP 常用首部"></a>HTTP 常用首部</h3><ul>
<li>通用<ul>
<li>cache-control ： 控制缓存行为</li>
<li>connection ： 连接的性质，比如 keep-alive</li>
<li>user-Agent ：用户信息</li>
<li>Date ：报文创建时间</li>
</ul>
</li>
<li>请求<ul>
<li>Referrer Policy : 表示来源的（浏览器所访问的前一个页面）,可以用于辅助检测 crsf 攻击，一般浏览器的默认值是 no-referrer-when-downgrade，意思是 https 降级 http 的时候不传原地址。</li>
<li>Accept : 能正确接收的媒体类型</li>
<li>Accept-XX(Accept-Charset/Accept-Encoding/Accept-Language):能正确接收的 xx</li>
<li>Expect :期待服务端的指定行文</li>
<li>If-Match :两端资源标记比较</li>
<li>If-Modified-Since : 比较时间 未修改返回 304 Not Modified</li>
<li>If-None-Match :比较标记 未修改返回 304 Not Modified</li>
<li>Host: 请求的主机名</li>
</ul>
</li>
<li>响应<ul>
<li>Location : 客户端重定向的 URI</li>
<li>Server : 服务器名字</li>
<li>ETag</li>
<li>Age :响应存在时间</li>
<li>Accept-Ranges ：可以接受的范围类型</li>
</ul>
</li>
<li>实体首部<ul>
<li>Content-Length 实体主体的大小</li>
<li>Expires 实体主体的过期时间</li>
<li>Last-Modified 资源的最后修改时间</li>
</ul>
</li>
<li>非首部字段<ul>
<li>Cookie 和 Set-cookie</li>
<li>Content-Disponsition</li>
</ul>
</li>
</ul>
<p>更多关于首部的资料可以查看：<a href="https://www.cnblogs.com/jycboy/p/http_head.html" target="_blank" rel="noopener">《HTTP 首部字段详细介绍》</a></p>
<h5 id="cache-control-的参数"><a href="#cache-control-的参数" class="headerlink" title="cache-control 的参数"></a>cache-control 的参数</h5><ul>
<li>可缓存性<ul>
<li>public：浏览器/任何代理服务器等都能缓存</li>
<li>private：只有发起请求的浏览请能缓存</li>
<li>no-cache：强制向源服务器再次验证，同意了才能用缓存</li>
<li>no-store：不缓存</li>
</ul>
</li>
<li>到期<ul>
<li>max-age=[secend]：缓存的保存时间（s）</li>
<li>s-maxage=[secend]：和 max-age 并存时会替代 max-age，但是只有在代理服务器中才会生效</li>
<li>max-stale：在和 max-age 并存的时候，意思是在发起请求的地方（浏览器）缓存过了 max-age 之后但是还没过 max-stale，则浏览器还可以继续用这个过期了的缓存</li>
</ul>
</li>
<li>重新验证<ul>
<li>must-revalidate：在设了 max-age 的缓存中，如果过期了必须去原服务端发起请求</li>
<li>proxy-revalidate：在设了 max-age 的缓存服务器中，如果过期了必须去源服务端发起请求</li>
</ul>
</li>
<li>其他<ul>
<li>no-transform：用在代理服务器中。缓存不能进行压缩改动等</li>
</ul>
</li>
</ul>
<h5 id="帮助缓存的资源验证-Last-Modified-和-Etag（见-浏览器-缓存策略）"><a href="#帮助缓存的资源验证-Last-Modified-和-Etag（见-浏览器-缓存策略）" class="headerlink" title="帮助缓存的资源验证 Last-Modified 和 Etag（见 浏览器-缓存策略）"></a>帮助缓存的资源验证 Last-Modified 和 Etag（见 <a href="https://jacleklm.github.io/2019/10/29/%E6%B5%8F%E8%A7%88%E5%99%A8/#more" target="_blank" rel="noopener">浏览器-缓存策略</a>）</h5><h5 id="connection-keep-alive（长连接）"><a href="#connection-keep-alive（长连接）" class="headerlink" title="connection=keep-alive（长连接）"></a>connection=keep-alive（长连接）</h5><p>在 http 请发送之前会建立 TCP 连接，如果请求发送完之后连接不会关闭，则就是长连接，这样有新请求的时候就可以直接发送，不用经过 TCP 三次握手阶段,避免了连接建立和释放的开销。<br>但是，长时间的 TCP 连接容易导致系统资源无效占用，可以设置 keep-alive timeout（过多长时间没有新请求连接就自动关闭）。</p>
<h5 id="cookie-和-session"><a href="#cookie-和-session" class="headerlink" title="cookie 和 session"></a>cookie 和 session</h5><p><strong>cookie</strong></p>
<ol>
<li>服务端返回数据的时候通过 set-cookie 设置。浏览器保存 cookie 之后，在下次的同域请求中就会带上这个 cookie。cookie 是通过键值对的形式保存的，可以设置很多个。</li>
<li>cookie 属性</li>
</ol>
<ul>
<li>max-age=[second]或 expires: 设置过期时间（若没有设置则默认关闭浏览器 cookie 就被清除，请求不会带上 cookie）</li>
<li>Secure: 只在 https 的时候发送 cookie</li>
<li>HttpOnly: 无法通过 JS 的 document.cookie 访问（提高安全性）</li>
</ul>
<ol start="3">
<li>set-cookie 怎么写：例如在服务器的 response.writeHeader 写 <code>&#39;set-cookie&#39;: [&#39;id=123; max-age=10&#39;, &#39;name=Jacleklm;HttpOnly&#39;]</code></li>
</ol>
<p><strong>session</strong><br>在网站开发中，最常用的就是用 cookie 来保存 session，但是二者并不等价。cookie 是存在客户端，session 是存在服务器端。服务端根据客户发过来的 cookie（eg.cookie 中有 session_id）定位到这个客户的信息（session），给他提供最合适的响应</p>
<h5 id="数据协商"><a href="#数据协商" class="headerlink" title="数据协商"></a>数据协商</h5><p>客户端发送给服务端请求时候，请求头中会声明希望拿到的数据格式，以及其他与数据相关的一些限制，服务端会根据它的请求中的声明，来做针对性的返回<br>分类中包含请求和返回两种<br><strong>请求</strong><br>Accept（想要的数据类型），Accept-Encoding（数据是怎样的编码方式，主要是压缩方式）,Accept-Language, User-Agent（浏览器的相关信息，移动端/PC）<br><strong>返回</strong><br>Content-Type，Content-Encoding，Content-Language</p>
<h3 id="HTTP-状态码"><a href="#HTTP-状态码" class="headerlink" title="HTTP 状态码"></a>HTTP 状态码</h3><ul>
<li>1XX 信息性状态码，接收的请求正在处理<ul>
<li>100 Continue 等待状态码，客户端应重新发送请求</li>
<li>101 Switching Protocols 改用协议 http 到 https 或 http1.1 到 2.0 之类</li>
</ul>
</li>
<li>2XX 成功状态码，请求正常处理完毕<ul>
<li>200 OK 操作成功</li>
<li>202 Accepted 服务器已接受请求，但尚未处理 (最后可能处理也可能不处理)</li>
<li>204 No Content 请求成功，但是报文不含实体的主体部分</li>
<li>206 Partial Content 进行范围请求</li>
</ul>
</li>
<li>3XX 重定向状态码，需要附加操作以完成请求<ul>
<li>301 Move Permanently 禁止从 POST 变成 GET。 永久性重定向，资源已经被分配到了新的 URI，以后应该使用资源现在的 URI 进行访问。</li>
<li>302 Found 禁止从 POST 变成 GET。临时重定向，资源已经被分配到了新的 URI，希望用户（本次）能用新的 URI 访问 实际上发部分客户端把它当成 303 处理</li>
<li>303 See Other 表示资源存在另一个 URI。应用 Get 获取资源</li>
<li>304 Not Modified 当客户端发送附带条件的请求后，服务端允许访问资源，但因为条件不符合，返回实体主体为空（也可能是服务器端资源未变，建议客户端直接用缓存）</li>
<li>307 Temporary Redirect 不会从 POST 变成 GET。 临时重定向，资源临时分配了 URL，但是希望客户端能够保持方法不变请求新地址（解决 302 被当成 303 处理的问题）</li>
</ul>
</li>
<li>4XX 客户端错误状态码，服务器无法处理请求<ul>
<li>400 Bad Request 请求报文存在语法错误</li>
<li>401 Unauthorized 需要认证，如果之前已经进行一次请求，表示认证失败</li>
<li>403 Forbidden 请求资源存在但被拒绝，常用于一个资源只允许在特定时间段内访问（如果不想透露可以谎报 404）</li>
<li>404 Not Found 找不到请求的资源</li>
<li>405 Method Not Found 不支持的请求方法，比如只支持 Get，但是收到了 Post 请求</li>
</ul>
</li>
<li>5XX 服务器错误状态码，服务器处理请求错误<ul>
<li><strong>500</strong> Inernal Server Error 执行请求出现错误</li>
<li>501 Not Implemented 不支持此请求方法（和 405 区别在于，405 是访问的资源不支持，而 501 表示服务器不能操作此方法）</li>
<li><strong>502</strong> Bad Gateway 代理与上行服务器之间出现问题</li>
<li><strong>503</strong> Service Unavailable 超负荷，停机维护</li>
<li><strong>504</strong> Gateway Timeout 网关等待服务器相应超时</li>
</ul>
</li>
</ul>
<h3 id="HTTP1-1"><a href="#HTTP1-1" class="headerlink" title="HTTP1.1"></a>HTTP1.1</h3><p>HTTP版本迭代详见 <a href="http://www.ruanyifeng.com/blog/2016/08/http.html" target="_blank" rel="noopener">HTTP 协议入门</a></p>
<h4 id="和-1-0-的区别"><a href="#和-1-0-的区别" class="headerlink" title="和 1.0 的区别"></a>和 1.0 的区别</h4><ul>
<li>持久链接</li>
<li>管道机制。在同一个TCP连接里面，客户端可以同时发送多个请求。以前的做法是，在同一个TCP连接里面，先发送A请求，然后等待服务器做出回应，收到后再发出B请求</li>
<li>Content-Length。声明本次响应的数据长度，单位是字节。作用是区分数据包是属于哪一个响应的</li>
<li>分块传输编码。产生一块数据，就发送一块，采用”流模式”（stream）取代”缓存模式”（buffer）。1.1版规定可以不使用Content-Length字段，而使用”分块传输编码”（chunked transfer encoding）。只要请求或回应的头信息有 <code>Transfer-Encoding: chunked</code> 字段，就表明回应将由数量未定的数据块组成</li>
<li>新增方法：PUT、PATCH、HEAD、 OPTIONS、DELETE</li>
</ul>
<h4 id="1-1的缺点"><a href="#1-1的缺点" class="headerlink" title="1.1的缺点"></a>1.1的缺点</h4><ul>
<li>队首阻塞。一个TCP无法并行进行多个http传输，只能排队。如果前面请求的响应特别慢的话，就会造成许 多请求排队等待的情况，这种情况被称为“队头堵塞”</li>
<li>连接过多。为了解决队首阻塞问题，解决方法就是同时打开多个连接请求</li>
</ul>
<h3 id="HTTP2"><a href="#HTTP2" class="headerlink" title="HTTP2"></a>HTTP2</h3><p>基于 SPDY 协议</p>
<h4 id="HTTP1-1-和-HTTP2-的区别"><a href="#HTTP1-1-和-HTTP2-的区别" class="headerlink" title="HTTP1.1 和 HTTP2 的区别"></a>HTTP1.1 和 HTTP2 的区别</h4><ol>
<li>多路复用，用一个 TCP 进行连接共享，一个请求对应一个 id，这样就可以发送多个请求，接收方通过 id 来响应不同的请求，解决了 http1.1 队首阻塞和连接过多的问题。因为 http2.0 在同一域名不论访问多少文件都只有一个连接，所以对服务器而言，提升的并发量是很大的。</li>
<li>二进制数据帧和流的概念，数据拆分成数据帧传输，并进行顺序标识，接收方收到数据后按序组合即可获取正确数据。这样就可以并行传输了，解决了 http1.0 只能串行传输的问题。</li>
<li>Sever Push。 服务端可以在客户端某个请求之后，主动推送其他资源（用 Link 头部字段）。</li>
<li>压缩头部，解决了 http1.1 中头部反复传输资源浪费（请求的很多字段都是重复的，比如Cookie和User Agent）的问题。一方面，头信息使用gzip或compress压缩后再发送；另一方面，客户端和服务器同时维护一张头信息表，所有字段都会存入这个表，生成一个索引号，以后就不发送同样字段了，只发送索引号，这样就提高速度了</li>
</ol>
<h4 id="存在问题"><a href="#存在问题" class="headerlink" title="存在问题"></a>存在问题</h4><p>http2.0 使用了多路复用，一般来说同一域名下只需要使用一个 TCP 连接。<br>但是当连接中出现丢包时，整个 TCP 都要开始等待重传，后面的数据也都被阻塞了。而 http1.0 可以开启多个连接，只会影响一个，不会影响其他的。<br>所以在丢包情况下，http2.0 的情况反而不如 http1.0。</p>
<h3 id="HTTP3-0"><a href="#HTTP3-0" class="headerlink" title="HTTP3.0"></a>HTTP3.0</h3><p>为了解决 2.0 丢包性能的问题，Google 基于 UDP 提出了 QUIC 协议。<br>HTTP3.0 中的底层支撑协议就是 QUIC。所以 http3.0 也叫 HTTP-over-QUIC。</p>
<h5 id="QUIC-协议"><a href="#QUIC-协议" class="headerlink" title="QUIC 协议"></a>QUIC 协议</h5><p>UDP 协议高效，但不可靠。QUIC 基于 UDP，在原来的基础上结合了 tcp 和 http 的精华使它可靠。</p>
<h6 id="特点："><a href="#特点：" class="headerlink" title="特点："></a>特点：</h6><ul>
<li>多路复用<br>HTTP2 虽然是多路复用，但是 TCP 协议是没有这个功能的。QUIC 原始就包含此功能，并且传输的单个数据流可以保证有序交付且不会影响其他数据流<br>其在移动端也会比 TCP 好，因为 TCP 基于 IP+端口识别连接，不适合多变的网络环境，但是 QUIC 是通过 ID 识别连接，不论网络如何变化，只要 ID 不变，就能迅速连上（实时手游就是这样实现的）</li>
<li>纠错机制<br>假如说这次我要发送三个包，协议会算出这三个包的异或值并单独发出一个校验包，也就是总共发出了四个包。<br>当出现其中的非校验包丢包的情况时，可以通过另外三个包计算出丢失的数据包的内容。<br>当然这种技术只能使用在丢失一个包的情况下，如果出现丢失多个包就不能使用纠错机制了，只能使用重传的方式了。</li>
<li>0-RTT<br>通过使用类似 TCP 快速打开的技术，缓存当前会话的上下文，在下次恢复会话的时候，只需要将之前的缓存传递给服务端验证通过就可以进行传输了。</li>
</ul>
]]></content>
      <categories>
        <category>Basic knowledge</category>
      </categories>
      <tags>
        <tag>Network</tag>
      </tags>
  </entry>
  <entry>
    <title>GoWhere项目总结</title>
    <url>/2019/11/10/GoWhere%E9%A1%B9%E7%9B%AE%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p>缘起：该项目是 7 月末刚开始学 Vue 的时候，一开始看官方文档，觉得不够扎实，需要一些实战练习，所以在慕课网上看《Vue2.5 开发去哪儿网 App 从零基础入门到实战项目》做了此项目。后来又自己从头到尾把项目敲了一遍。最近复习又把这个项目的实现过了一遍，总结成此博文<br>后来又给这个项目搭了个基于Koa2的服务器，完善登录验证等功能</p>
<h1 id="组件实现"><a href="#组件实现" class="headerlink" title="组件实现"></a>组件实现</h1><h2 id="首页"><a href="#首页" class="headerlink" title="首页"></a>首页</h2><ul>
<li>header 组件：用 Vuex 获取城市数据</li>
<li>轮播效果是通过 vue-awesome-swiper 实现的</li>
</ul>
<h2 id="切换城市页"><a href="#切换城市页" class="headerlink" title="切换城市页"></a>切换城市页</h2><ul>
<li>选择城市后会且切换到首页并把城市数据更新到 Vuex 中</li>
<li>搜索框：对 input 做 v-model=’key’, 对 key 这个变量 watch，有变化就遍历数据把复合的 push 到 result 数组中（可以设置 timer 进行防抖）。</li>
<li>字母表的滑动效果：<ul>
<li>安装 better-scroll 实现滑动。用它的 scrollToElement( DOM 节点 ) 实现点击滑动到某个字母</li>
<li></li>
</ul>
</li>
</ul>
<h2 id="详情页"><a href="#详情页" class="headerlink" title="详情页"></a>详情页</h2><p>评论组件的星星（虽然在这个项目里没做不过需要的话也可以讲一下怎么实现的）</p>
<h1 id="数据"><a href="#数据" class="headerlink" title="数据"></a>数据</h1><p>form 后端。部分 json 是通过自己写的一个爬虫到去哪儿网抓取生成的。不过爬虫代码太垃圾了了又没优化所以大家忘了这个事吧</p>
<h1 id="后端和数据交互"><a href="#后端和数据交互" class="headerlink" title="后端和数据交互"></a>后端和数据交互</h1><h2 id="后端"><a href="#后端" class="headerlink" title="后端"></a>后端</h2><p>用 Koa2 搭建服务器，没有用数据库，数据放在 json 文件中，通过对 json 文件的读，写和查询实现需要的功能</p>
<h2 id="前端"><a href="#前端" class="headerlink" title="前端"></a>前端</h2><h3 id="proxy-转发请求"><a href="#proxy-转发请求" class="headerlink" title="proxy 转发请求"></a>proxy 转发请求</h3><p>用 proxy 转发请求到服务器接口 localhost:3000，这个功能能直接实现跨域？还是说我们服务器默认设置就是 Access-Control-Allow-Orgin: *<br>答：proxy 的 changeOrgin 是 true 的话，能在开发环境下虚拟一个服务端接收你的请求并代你发送该请求，不会有跨域问题；Koa2 在开发环境下似乎也不会有问题，真正想用跨域 Koa2 得在 app.js 中引入 cors 中间件并 use</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// /config/index.js</span></span><br><span class="line">proxyTable: &#123;</span><br><span class="line">     <span class="string">'/api'</span>: &#123;<span class="comment">//  在开发环境中（dev），当去请求api目录的时候</span></span><br><span class="line">       target: <span class="string">'http://localhost:3000'</span>,<span class="comment">// 会把请求转发到当前服务器的3000端口上</span></span><br><span class="line">       changeOrigin: <span class="literal">true</span>, <span class="comment">// 是否跨域</span></span><br><span class="line">       pathRewrite: &#123;<span class="comment">//  不过希望把路径做一个替换</span></span><br><span class="line">         <span class="string">'^/api'</span>: <span class="string">'/'</span><span class="comment">//  一旦请求的地址是以/app开头的，那么就替换请求到...中</span></span><br><span class="line">     &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="实现用户登录及-token-验证"><a href="#实现用户登录及-token-验证" class="headerlink" title="实现用户登录及 token 验证"></a>实现用户登录及 token 验证</h3><ol>
<li>本项目暂时注册和验证登录功能不完善，默认用户在登录页输入用户名和密码都是新用户。点击登录则向服务器 POST 用户名和密码；服务器收到后给新用户生成随机字符 token（暂时还无法保证是唯一性的）和 token 过期时间，把这一套用户数据都写进数据库（json）中，返回给组件用户名和 token；组件收到后把用户名和 token 存进 State 和 localStorage 中</li>
<li>设置导航守卫。用 router.beforeEach 这个路由钩子，对 to.path = 某些页面（eg. 购物车） 在跳转前检验时候是否有 token，没有就强制跳转到登录页面</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 设置导航守卫。用router.beforeEach注册一个全局守卫，判断是否有登录状态</span></span><br><span class="line">router.beforeEach(<span class="function">(<span class="params">to, <span class="keyword">from</span>, next</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (to.path === <span class="string">'/login'</span>) &#123;</span><br><span class="line">    next()</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> token = localStorage.getItem(<span class="string">'token'</span>)</span><br><span class="line">    <span class="keyword">if</span> (token === <span class="string">''</span> || token === <span class="literal">null</span>) &#123;</span><br><span class="line">      next(<span class="string">'/login'</span>)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      next()</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>让浏览器每次请求都在头部带 token。通过请求拦截器实现，通过对 axios 的配置新增一个请求拦截让请求头带上 token</li>
<li>axios 的响应拦截用来处理 token 过期的情况，收到服务器响应 401 就清空 localStorage 的 token 并跳转到登录页（这个 清空 localStorage 的 token 并跳转到登录页 其实也是点击 退出登录 按钮要做的事，可以把这个动作封装好放在 Vuex 的 actions 中）。所以服务器要做的事情是：收到除了注册外的请求，都会验证请求头的 token 是否存在，拿 token 去数据库（json）查这个 token 对应的过期时间，过期了则返回 401（这个实现在请求 json 数据的时候验证，现在只在 getAllCity 这个接口有这个验证）；没过期则正常返回数据</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// /src/api/axios.js axios的配置文件</span></span><br><span class="line"><span class="comment">// 3.设置拦截器</span></span><br><span class="line">Axios.interceptors.request.use(</span><br><span class="line">  config =&gt; &#123;</span><br><span class="line">    <span class="keyword">if</span> (localStorage.getItem(<span class="string">'token'</span>)) &#123;</span><br><span class="line">      config.headers.token = localStorage.getItem(<span class="string">'token'</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> config</span><br><span class="line">  &#125;,</span><br><span class="line">  error =&gt; &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(error)</span><br><span class="line">  &#125;</span><br><span class="line">)</span><br><span class="line"><span class="comment">// 响应拦截用来检测服务器返回401，token过期</span></span><br><span class="line">Axios.interceptors.response.use(</span><br><span class="line">  res =&gt; &#123;</span><br><span class="line">    <span class="keyword">return</span> res</span><br><span class="line">  &#125;,</span><br><span class="line">  error =&gt; &#123;</span><br><span class="line">    <span class="keyword">if</span> (error.response) &#123;</span><br><span class="line">      <span class="keyword">switch</span> (error.response.status) &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">401</span>:</span><br><span class="line">          <span class="comment">// 服务器检测到token和数据库存的token的过期时间已经过期了的话返回401。跳转登录</span></span><br><span class="line">          localStorage.removeItem(<span class="string">'token'</span>)</span><br><span class="line">          localStorage.removeItem(<span class="string">'username'</span>)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 随机字符token</span></span><br><span class="line"><span class="keyword">let</span> token = <span class="built_in">Math</span>.random()</span><br><span class="line">  .toString(<span class="number">16</span>)</span><br><span class="line">  .slice(<span class="number">2</span>, <span class="number">18</span>)</span><br><span class="line"><span class="comment">// 过期时间</span></span><br><span class="line"><span class="keyword">let</span> expire = <span class="keyword">new</span> <span class="built_in">Date</span>()</span><br><span class="line">expire = expire.getTime() + <span class="number">604800000</span> <span class="comment">// 一周的有效期</span></span><br></pre></td></tr></table></figure>

<p>实现过程中参考的博客贴<br><a href="https://blog.csdn.net/weixin_34336292/article/details/88611230" target="_blank" rel="noopener">基于 Koa2 对 json 文件进行增删改查</a><br><a href="https://blog.csdn.net/qq_41772754/article/details/88075391" target="_blank" rel="noopener">axios 配置</a><br><a href="https://www.cnblogs.com/web-record/p/9876916.html" target="_blank" rel="noopener">Vue 项目中实现用户登录及 token 验证</a><br><a href="https://www.cnblogs.com/zdd2017/p/9871925.html" target="_blank" rel="noopener">Vuex 登录验证及保持登录状态</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>项目小结</tag>
      </tags>
  </entry>
  <entry>
    <title>JS函数柯里化</title>
    <url>/2019/11/30/JS%E5%87%BD%E6%95%B0%E6%9F%AF%E9%87%8C%E5%8C%96/</url>
    <content><![CDATA[<h1 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h1><p>柯里化是指将一个函数分解为一系列函数的过程，每个函数都只接收一个参数<br>函数柯里化，英语：Currying，高阶函数的一个特殊用法。是把接受多个参数的函数变换成接受一个<strong>单一参数</strong>（最初函数的第一个参数）的函数，并且返回接受余下的参数而且返回结果的新函数的技术</p>
<p>一个简单的例子:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">a, b</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> a + b</span><br><span class="line">&#125;</span><br><span class="line">add(<span class="number">1</span>, <span class="number">5</span>) <span class="comment">// 6</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">curryingadd</span>(<span class="params">x</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">y</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x + y</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">curryadd(<span class="number">1</span>)(<span class="number">5</span>) <span class="comment">// 6</span></span><br></pre></td></tr></table></figure>

<h1 id="柯里化的用途"><a href="#柯里化的用途" class="headerlink" title="柯里化的用途"></a>柯里化的用途</h1><h2 id="参数复用"><a href="#参数复用" class="headerlink" title="参数复用"></a>参数复用</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 正常正则验证字符串 reg.test(txt)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 函数封装后</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">check</span>(<span class="params">reg, txt</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> reg.test(txt)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">check(<span class="regexp">/\d+/g</span>, <span class="string">'test'</span>) <span class="comment">//false</span></span><br><span class="line">check(<span class="regexp">/[a-z]+/g</span>, <span class="string">'test'</span>) <span class="comment">//true</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Currying后</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">curryingCheck</span>(<span class="params">reg</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">txt</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> reg.test(txt)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> hasNumber = curryingCheck(<span class="regexp">/\d+/g</span>)</span><br><span class="line"><span class="keyword">var</span> hasLetter = curryingCheck(<span class="regexp">/[a-z]+/g</span>)</span><br><span class="line"></span><br><span class="line">hasNumber(<span class="string">'test1'</span>) <span class="comment">// true</span></span><br><span class="line">hasNumber(<span class="string">'testtest'</span>) <span class="comment">// false</span></span><br><span class="line">hasLetter(<span class="string">'21212'</span>) <span class="comment">// false</span></span><br></pre></td></tr></table></figure>

<p>如果有很多地方都要校验是否有数字，如果用 check 函数，得反复传递第一个参数；curryingCheck 能将第一个参数 reg 进行复用，这样别的地方就能够直接调用 hasNumber，hasLetter 等函数，让参数能够复用，调用起来也更方便</p>
<h2 id="提前确认"><a href="#提前确认" class="headerlink" title="提前确认"></a>提前确认</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> on = <span class="function"><span class="keyword">function</span>(<span class="params">element, event, handler</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">document</span>.addEventListener) &#123;</span><br><span class="line">    <span class="keyword">if</span> (element &amp;&amp; event &amp;&amp; handler) &#123;</span><br><span class="line">      element.addEventListener(event, handler, <span class="literal">false</span>)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (element &amp;&amp; event &amp;&amp; handler) &#123;</span><br><span class="line">      element.attachEvent(<span class="string">'on'</span> + event, handler)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> on = (<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">document</span>.addEventListener) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">element, event, handler</span>) </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (element &amp;&amp; event &amp;&amp; handler) &#123;</span><br><span class="line">        element.addEventListener(event, handler, <span class="literal">false</span>)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">element, event, handler</span>) </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (element &amp;&amp; event &amp;&amp; handler) &#123;</span><br><span class="line">        element.attachEvent(<span class="string">'on'</span> + event, handler)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)()</span><br><span class="line"></span><br><span class="line"><span class="comment">//换一种写法可能比较好理解一点，上面就是把isSupport这个参数给先确定下来了</span></span><br><span class="line"><span class="keyword">var</span> on = <span class="function"><span class="keyword">function</span>(<span class="params">isSupport, element, event, handler</span>) </span>&#123;</span><br><span class="line">  isSupport = isSupport || <span class="built_in">document</span>.addEventListener</span><br><span class="line">  <span class="keyword">if</span> (isSupport) &#123;</span><br><span class="line">    <span class="keyword">return</span> element.addEventListener(event, handler, <span class="literal">false</span>)</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> element.attachEvent(<span class="string">'on'</span> + event, handler)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们在做项目的过程中，封装一些 dom 操作可以说再常见不过，上面第一种写法也是比较常见，但是我们看看第二种写法，它相对一第一种写法就是自执行然后返回一个新的函数，这样其实就是提前确定了会走哪一个方法，避免每次都进行判断</p>
<h2 id="延迟运行"><a href="#延迟运行" class="headerlink" title="延迟运行"></a>延迟运行</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Function</span>.prototype.bind = <span class="function"><span class="keyword">function</span>(<span class="params">context</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> _this = <span class="keyword">this</span></span><br><span class="line">  <span class="keyword">var</span> args = [...arguments].slice(<span class="number">1</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> _this.apply(context, args.concat(...arguments))</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>bind 实现的机制就是 Currying，虽然上面这个 bind 有点简单没有考虑所有情况</p>
<h1 id="柯里化的封装"><a href="#柯里化的封装" class="headerlink" title="柯里化的封装"></a>柯里化的封装</h1><p>其实和之前写的 bind 的实现很像</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 初步封装</span></span><br><span class="line"><span class="keyword">var</span> currying = <span class="function"><span class="keyword">function</span>(<span class="params">fn</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// args 获取第一个方法内的全部参数</span></span><br><span class="line">  <span class="keyword">var</span> args = [...arguments].slice(<span class="number">1</span>)</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 把合并后的参数通过apply作为fn的参数并执行</span></span><br><span class="line">    <span class="keyword">return</span> fn.apply(<span class="keyword">this</span>, args.concat(...arguments))</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样返回的话其实只能多扩展一个参数，currying(a)(b)(c)的情况无法支持，所以需要递归</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 支持多参数传递</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">progressCurrying</span>(<span class="params">fn, ...args</span>) </span>&#123; <span class="comment">// fn 是原本的接受很多个参数的复杂函数</span></span><br><span class="line">  <span class="keyword">var</span> _this = <span class="keyword">this</span></span><br><span class="line">  <span class="keyword">var</span> len = fn.length <span class="comment">// fn.length === fn 原本参数个数</span></span><br><span class="line">  <span class="keyword">var</span> args = args || []</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> _args = [...arguments]</span><br><span class="line">    args.push(...arguments)</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 如果参数个数小于最初的fn.length，则递归调用，继续收集参数</span></span><br><span class="line">    <span class="keyword">if</span> (_args.length &lt; len) &#123;</span><br><span class="line">      <span class="keyword">return</span> progressCurrying.call(_this, fn, _args)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 参数收集完毕，则执行fn</span></span><br><span class="line">    <span class="keyword">return</span> fn.apply(<span class="keyword">this</span>, args)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="一道经典面试题"><a href="#一道经典面试题" class="headerlink" title="一道经典面试题"></a>一道经典面试题</h1><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 实现一个add方法，使计算结果能够满足如下预期：</span></span><br><span class="line">add(<span class="number">1</span>)(<span class="number">2</span>)(<span class="number">3</span>) = <span class="number">6</span></span><br><span class="line">add(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)(<span class="number">4</span>) = <span class="number">10</span></span><br><span class="line">add(<span class="number">1</span>)(<span class="number">2</span>)(<span class="number">3</span>)(<span class="number">4</span>)(<span class="number">5</span>) = <span class="number">15</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 第一次执行时，定义一个数组专门用来存储所有的参数</span></span><br><span class="line">  <span class="keyword">let</span> _args = [...arguments]</span><br><span class="line">  <span class="comment">// 在内部声明一个函数，利用闭包的特性保存_args并收集所有的参数值</span></span><br><span class="line">  <span class="keyword">let</span> _result = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="comment">// 这里不能用箭头函数</span></span><br><span class="line">    _args.push(...arguments)</span><br><span class="line">    <span class="keyword">return</span> _result</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 利用toString隐式转换的特性，当最后执行时隐式转换，并计算最终的值返回</span></span><br><span class="line">  _result.toString = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="comment">// 这里不能用箭头函数</span></span><br><span class="line">    <span class="keyword">return</span> _args.reduce(<span class="function">(<span class="params">acc, curr</span>) =&gt;</span> acc + curr, <span class="number">0</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> _result</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">add(<span class="number">1</span>)(<span class="number">2</span>)(<span class="number">3</span>) <span class="comment">// 6</span></span><br><span class="line">add(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)(<span class="number">4</span>) <span class="comment">// 10</span></span><br><span class="line">add(<span class="number">1</span>)(<span class="number">2</span>)(<span class="number">3</span>)(<span class="number">4</span>)(<span class="number">5</span>) <span class="comment">// 15</span></span><br><span class="line">add(<span class="number">2</span>, <span class="number">6</span>)(<span class="number">1</span>) <span class="comment">// 9</span></span><br></pre></td></tr></table></figure>

<h2 id="toString-或-valueOf-的改写"><a href="#toString-或-valueOf-的改写" class="headerlink" title="toString 或 valueOf 的改写"></a>toString 或 valueOf 的改写</h2><p>上面那道题的的 toString 的改写也是非常有意思的一个知识点</p>
<p>当变量对象（函数也是对象）遇到一些情况的时候，会偷偷地调用 toString 和 valueOf 方法（隐式调用），那我们就可以改写这两个方法做一些事</p>
<p><strong>这些情况会触发隐式调用：</strong></p>
<ul>
<li>加减乘除</li>
<li>比较运算符&gt;</li>
<li>==</li>
<li>String()</li>
<li>Number()</li>
</ul>
<p>两个都重写的情况：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  i: <span class="number">10</span>,</span><br><span class="line">  valueOf: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'执行了valueOf()'</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.i + <span class="number">20</span></span><br><span class="line">  &#125;,</span><br><span class="line">  toString: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'执行了toString()'</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.valueOf() + <span class="number">20</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(+obj) <span class="comment">// 执行了valueOf()  30</span></span><br><span class="line"><span class="built_in">console</span>.log(obj &gt; <span class="number">40</span>) <span class="comment">// 执行了valueOf()  false</span></span><br><span class="line"><span class="built_in">console</span>.log(obj == <span class="number">30</span>) <span class="comment">// 执行了valueOf()  true</span></span><br></pre></td></tr></table></figure>

<p>只重写了 toString</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> aa = &#123;</span><br><span class="line">  i: <span class="number">10</span>,</span><br><span class="line">  toString: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'toString'</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.i</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(+aa) <span class="comment">// 10 toString</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">''</span> + aa) <span class="comment">// 10 toString</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">String</span>(aa)) <span class="comment">// 10 toString</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Number</span>(aa)) <span class="comment">// 10 toString</span></span><br><span class="line"><span class="built_in">console</span>.log(aa == <span class="string">'10'</span>) <span class="comment">// true toString</span></span><br></pre></td></tr></table></figure>

<p>只重写了 valueOf</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> aa = &#123;</span><br><span class="line">  i: <span class="number">10</span>,</span><br><span class="line">  valueOf: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'valueOf'</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.i</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(+aa) <span class="comment">// 10 valueOf</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">''</span> + aa) <span class="comment">// 10 valueOf</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">String</span>(aa)) <span class="comment">// [object Object]</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Number</span>(aa)) <span class="comment">// 10 valueOf</span></span><br><span class="line"><span class="built_in">console</span>.log(aa == <span class="string">'10'</span>) <span class="comment">// true valueOf</span></span><br></pre></td></tr></table></figure>

<p>结论：如果只重写了 toString，对象转换时会无视 valueOf 的存在来进行转换。但是，如果只重写了 valueOf 方法，在要转换为字符串的时候会优先考虑 valueOf 方法。在不能调用 toString 的情况下，只能让 valueOf 上阵了<br><strong>当两者都重写的时候，优先 valueOf</strong></p>
<p>结论二：所以上面那道面试题，是触发了隐式调用，偷偷地执行了重写的toString方法。虽然还是没懂是怎么触发隐式调用的好像都不是我所知的那几种情况0.0</p>
<p>参考资料<br><a href="https://www.zhangxinxu.com/wordpress/2013/02/js-currying/" target="_blank" rel="noopener">张鑫旭-JS 中的柯里</a><br><a href="https://www.jianshu.com/p/2975c25e4d71" target="_blank" rel="noopener">详解 JS 函数柯里化</a><br><a href="https://www.cnblogs.com/barrior/p/4598354.html" target="_blank" rel="noopener">JavaScript 中 valueOf、toString 的隐式调用</a><br><a href="https://www.cnblogs.com/aNd1coder/archive/2013/03/09/2951959.html" target="_blank" rel="noopener">JavaScript 中 valueOf 函数与 toString 方法深入理解</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS之页面布局详解</title>
    <url>/2019/10/09/CSS%E4%B9%8B%E9%A1%B5%E9%9D%A2%E5%B8%83%E5%B1%80%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<h3 id="布局基础知识"><a href="#布局基础知识" class="headerlink" title="布局基础知识"></a>布局基础知识</h3><p>1、display 属性：block（块级元素），inline-block，none（会在页面看不到，并且不会在页面占据空间），flex（flex 布局），inline-block（应用场景：eg. 让很多个<strong>有宽高</strong>的 div 从左到右铺满页面；此时最好也设置 vertical-align：top）<br>2、div { max-width: 600px; margin: 0 auto; }<br>用 max-width 代替 width，防止浏览器页面表现出现水平滚动条；margin：0 auto 实现盒子的左右居中<br>3、盒模型的 box-sizing 属性：设成 border-box，元素的 padding 和 border 不会算进元素的 width 和 height 中;content-box 是常规盒模型；padding-box—高宽包括了内边距<br>4、positoin：static（默认值，元素不会被“positioned”，设成其他值元素都会被“positioned”）；relative；absolute（相对于最近的“positioned”祖先元素，也是一个<strong>破环属性</strong>，脱离文档流）；fixed（脱离文档流）。设置成后面三个属性之后，一般还需要写 top，right 等（relative 时不写的话会和 static 一样，没什么变化，但这时候其子元素可以写 position: absolute;）<br><strong>z-index 属性</strong>：只对被“positioned”的元素有效；对父子元素，子元素 z-index 的默认值是等于其父元素的；当子的 z-index 是大于等于 0 的时候，就算小于父 z-index，也会显示在父元素之上，只有设置成负值才会被父元素覆盖<br>5、float 属性<br>其设计初衷是为了实现文字环绕效果。有以下特性：</p>
<ul>
<li>破坏性：一个 div 中只有一个 img（或有少量文字），当设置 img {float:left}之后，父 div 会坍塌，img 会溢出。因为设置了 float 的 img 会<strong>脱离文档流</strong></li>
<li>包裹性：div 被设置 float 之后，会变得不会默认铺满页面宽度，而是紧包裹住 div 中的文字</li>
<li>清空格：当一个 div 中有多个 img 的时候，页码显示 img 从左到右且相互有<strong>间隔</strong>，但是 img 设置 float 之后，这种间隔会消失，img 紧紧挨着（有时候不希望有间隔，可用此法来清间隔）</li>
</ul>
<p>6、<strong>clear 属性</strong>，用来控制浮动。<br>基本功能：有两个盒子，第一个写了 float：left，第二个没有，则页面效果是两个盒子会重叠而不是上下分布。此时在<strong>第二个</strong>盒子(被影响的盒子，<strong>同级元素</strong>）中写 clear：left，即能实现上下分布（clear：both 也可以）。<br>7、清除浮动<strong>overflow 属性</strong>：也是写在被影响的盒子中（应用场景：over，即内外嵌套，内 div 超出的时候），大部分时候是写在外盒，写在同级盒子的情况是 BFC 的时候（见下面 BFC 部分）。有以下四种<strong>清除浮动的方法</strong>：</p>
<ul>
<li>父元素设置 overflow:hidden，这样父元素就有高度了，不会坍塌（<strong>常用</strong>）</li>
<li>浮动父元素</li>
<li>给父元素添加一个子 div，给其设置 clear：both</li>
<li><strong>clearfix</strong>：给父元素添加一个:after 伪元素，里面写{content: ‘’, display: table, clear: both}; 父元素身要写 *zoom: 1（缩放比例）; 以提高兼容性。其实本质和第三种方法是一样的。</li>
</ul>
<p>8、百分比宽度：width 和 max-width 都可用百分比，相对父级元素的 width<br>9、媒体查询<br>@media screen and (min-width/max-width:600px) { }<br>使用 meta 标签中的 viewport 之后可以布局在移动浏览器上显示的更好<br>10、在一个 div 中实现里面文字/元素呈现多列分布：用<strong>column</strong>属性。设置 column-count 和 column-gap 即可<br>11、布局模型<br>流动模型（Flow）是浏览器默认的布局模式。块状元素默认从上到下分布，行内元素默认从左到右分布<br>浮动模型（Float）。设置了 float 属性的时候<br>层模型（Layer）。设置了 position 属性的时候</p>
<h3 id="对齐方式"><a href="#对齐方式" class="headerlink" title="对齐方式"></a>对齐方式</h3><ul>
<li><p>水平居中</p>
<ul>
<li>行内元素的水平居中<br>是 img，span 等行内元素的时候，父级元素写 text-align: center 即可</li>
<li>块状元素的水平居中（定宽）<br>margin： 0 auto</li>
<li>块状元素的水平居中（不定宽）<br>把块状元素写 display: inline-block/inline，再用行内元素的方法实现居中。定宽也能用此法</li>
</ul>
</li>
<li><p>垂直居中（假设父元素是盒子且高度已定）</p>
<ul>
<li>子元素是行内元素，高度是其内容撑开的。设定父元素的 line-height 即可</li>
<li>（差）子元素是块级元素且高度还没有设定。给父元素设定 display: table; vertical: middle 可解决.</li>
<li>（差）子元素是块级元素且高度已经设定。设子元素的 margin-top 为（父元素高度-子元素高度）/2</li>
<li><strong>flex 布局法</strong>。对父元素写 display: flex; flex-direction: column; justify-content: center;（似乎 display: flex;align-items: center;也可以）</li>
<li><strong>相对+绝对定位法</strong></li>
</ul>
</li>
<li><p>垂直水平居中</p>
<ul>
<li>（差）在父元素写 text-align：center; vertical-align: middle; display: table-cell;并把子元素设置为 inline-block</li>
<li>若子元素是图像，父元素写 text-align：center; 用行高替代高度，且字体大小设为 0。子元素本身设置 vertical-align:middle</li>
<li><strong>相对+绝对定位法</strong>。父元素设置为相对，子元素设为绝对，在 top、left、right、bottm 都为确定值（可都写成 0）的情况下，写 margin: auto</li>
<li><strong>flex 布局</strong>。对父元素写 display: flex;justify-content: center;align-items: center;</li>
</ul>
</li>
</ul>
<h3 id="两列布局-左盒定宽，右盒自适应"><a href="#两列布局-左盒定宽，右盒自适应" class="headerlink" title="两列布局(左盒定宽，右盒自适应)"></a>两列布局(左盒定宽，右盒自适应)</h3><p>需要知道左侧盒子宽度的方案：</p>
<ul>
<li>双 float 并且右盒设 margin-left = 左盒 width；或者双 float 并且右盒写 width: calc(100% - 左盒 width)，此时由于右盒的 width 是计算的，所以子盒子都得写 box-sizing: border-box 。父元素都要清除浮动。</li>
<li>双 inline-block 方案（inline-block + 设 vertical-align）并且 右盒写 width: calc(100% - 左盒 width)，box-sizeing。并且父元素要写 font-size: 0，这也和 width 的计算有关</li>
<li>（少用）父元素设置 position：relative，左元素设成 absolute，右盒子 margin-left: 左盒子宽度，并且（当左盒高度大于右盒时要）设定父容器的 min-height</li>
</ul>
<p><strong>不需要知道左侧盒子宽度的方案</strong>：</p>
<ul>
<li>float+BFC 方法。左盒子 float: left，右盒子通过 overflow: auto，形成了 BFC，因此右侧盒子不会与浮动的元素重叠。父元素清除浮动</li>
<li><strong>flex 布局（最好的）</strong>。父写 display: flex;align-items: flex-start（这一项能让两个子盒高度自动）。左写定宽，右写 flex: 1。</li>
<li>grid 布局。父元素写 display: grid;grid-template-columns: 120px 1fr;align-items: start。子元素都需要 box-sizing: border-box。左写 grid-column: 1，右写 grid-column: 2</li>
</ul>
<h3 id="三列布局-两侧定宽中间自适应"><a href="#三列布局-两侧定宽中间自适应" class="headerlink" title="三列布局(两侧定宽中间自适应)"></a>三列布局(两侧定宽中间自适应)</h3><ul>
<li><p>左右盒子 float + 中间盒子 margin</p>
</li>
<li><p>绝对定位法。父元素设置 position：relative; 左右盒子元素设成 absolute，再用 top、left、right 固定位置。中间盒子用左右 margin 撑开</p>
</li>
<li><p><a href="https://www.cnblogs.com/pink-chen/p/10582741.html" target="_blank" rel="noopener">margin 负值法</a> 都是三个盒子都 float:left。两种方案都是把中间盒子写在 DOM 最前面，优先渲染</p>
<ul>
<li>圣杯布局(父盒子写左右 padding，左右盒子通过相对定位确定位置)</li>
<li>双飞翼布局（middle 的 div 里又插入一个 div，通过调整内部 div 的 margin 值，实现中间栏自适应，内容写到内部 div 中）</li>
</ul>
</li>
<li><p>flex 布局：两侧定好宽度,父元素写 display: flex; 中间的子元素写 flex: 1 (DOM 中必须是先写左再写中最后写右)</p>
</li>
</ul>
<h3 id="BFC"><a href="#BFC" class="headerlink" title="BFC"></a>BFC</h3><p><a href="https://www.cnblogs.com/asheng2016/p/7281784.html" target="_blank" rel="noopener">帮助理解</a><br>BFC 全称”Block Formatting Context”, 中文为“块级格式化上下文”。是布局过程中<strong>生成块级盒子的区域</strong>（同时保持流体特性，存在于文档流中），也是浮动元素与其他元素的交互限定区域<br>（BFC 一般是出现在浮动元素周围的元素）当元素有下面情况的时候<strong>变成 BFC 元素</strong>：</p>
<ul>
<li>float 的值不为 none</li>
<li>overflow 的值为 auto,scroll 或 hidden</li>
<li>display 的值为 table-cell, table-caption, inline-block 中的任何一个</li>
<li>position 的值不为 relative 和 static ……</li>
</ul>
<p>变成 BFC 元素后会有以下<strong>特性</strong>：</p>
<ul>
<li>BFC 就是页面上的一个隔离的独立容器，容器里面的布局与外面互不影响</li>
<li>BFC 的区域不会与其它 float 的元素区域重叠</li>
<li>计算 BFC 的高度时，浮动子元素也参与计算</li>
</ul>
<p>有用的其实是<strong>overflow:auto/hidden</strong>，display:inline-block 和 display:table-cell。<strong>主要用途</strong>：清除浮动，防止 margin 重叠，阻止文字环绕。</p>
<h3 id="flex-布局"><a href="#flex-布局" class="headerlink" title="flex 布局"></a><a href="http://www.ruanyifeng.com/blog/2015/07/flex-grammar.html" target="_blank" rel="noopener">flex 布局</a></h3><p><a href="https://www.jianshu.com/p/4896e6936ce3" target="_blank" rel="noopener">告诉你一个将 footer 保持在底部的最好方法</a></p>
<p>1、flex 容器的属性：flex-direction 主轴方向，justify-content 项目在主轴上的对齐方式，align-items 定义项目在交叉轴上如何对齐……<br>2、flex item 的属性：flex 属性是 flex-grow（项目的放大比例（与其他项目相比），默认为 0）, flex-shrink（项目的缩小比例） 和 flex-basis 的简写，默认值为 0 1 auto。….<br><strong>注意</strong>：设置为 Flex 布局后，子元素的 float 、clear 和 vertical-align 属性将失效<br><a href="http://www.ruanyifeng.com/blog/2015/07/flex-examples.html" target="_blank" rel="noopener">flex 布局</a>：<br>1、可以实现骰子任意面的任意布局（一个点位一个盒子）<br>2、网格布局（在骰子布局的基础上实现 item 的缩放）<br>3、圣杯布局 …</p>
<h3 id="grid-布局"><a href="#grid-布局" class="headerlink" title="grid 布局"></a><a href="http://www.ruanyifeng.com/blog/2019/03/grid-layout-tutorial.html" target="_blank" rel="noopener">grid 布局</a></h3><p>1、Grid 布局（二维布局）远比 Flex （一维布局）布局强大。它将网页划分成一个个网格，可以任意组合不同的网格，做出各种各样的布局。<br>2、概念：容器，项目，行，列，单元格（行与列的交叉部分），网格线。<br><strong>注意</strong>：设置为 Grid 布局后，子元素的 float、display: inline-block、display: table-cell、vertical-align 和 column-*属性将失效<br>3、容器属性：grid-template-columns 和 grid-template-rows 定义每一列列宽和每一行行高（多少行/列就有多少个值，可以是 px、是百分比、fr；也可以直接写 repeat(3, 33.33%);），grid-gap: grid-row-gap grid-column-gap，单元格内容的水平和垂直位置 place-items: align-items justify-items ，整个内容区域在容器里面的水平和垂直位置 place-content: align-content justify-content ……</p>
<h3 id="响应式布局"><a href="#响应式布局" class="headerlink" title="响应式布局"></a><a href="https://blog.nowcoder.net/n/3aba8826a43c47f999875e07ca6b9eeb" target="_blank" rel="noopener">响应式布局</a></h3><h3 id="移动端布局"><a href="#移动端布局" class="headerlink" title="移动端布局"></a>移动端布局</h3><ol>
<li>媒体查询，选取主流设备宽度尺寸作为断点进行样式编写（后面的样式会覆盖前面的样式）</li>
<li>加标签<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"viewport"</span> <span class="attr">content</span>=<span class="string">"width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no"</span>/&gt;</span></span><br></pre></td></tr></table></figure></li>
</ol>
<p>告诉浏览器，使用的设备的宽度作为视图宽度，并禁止初始的缩放<br>3. 总结方案：（1）媒体查询+px ;（2）媒体查询+rem</p>
<p>从 Vue 项目中总结的<br>初始化：<br>1、在 index.html 的 meta 标签中的 viewport 的 content 中加 minimum-scale=1.0, maximum-scale=1.0, user-scalable=no<br>2、引入 reset.css 重置页面的样式，设定不同手机浏览器的初始化样式<br>3、引入 border.css 解决移动端的 1px 边框的问题<br>4、300ms 点击延迟的问题：在某些浏览器用 click 事件的时候，会延迟 300ms。用 npm 安装 fastclick 模块解决之<br>设计：<br>1、把 html 的 font-size 设置为 50px（reset.css 中），然后项目中所有的宽高和 font-size 都用 rem 来做单位。这是因为移动端的设计稿一半是二倍图，这样设计稿中的 10px 其实是 5px，写 css 的时候就是 0.1rem，比较方便。<br>2、图片加载防抖四件套：overflow:hidden; width:0; height:0; padding-bottom: 100%<br>3、加 1px 边框的方法：直接在 div 上添加 class=”border-top/bottom”<br>4、长文本省略：overflow: hidden; white-space: nowrap; text-overflow: ellipsis</p>
<p><strong>参考资料</strong><br><a href="http://zh.learnlayout.com/" target="_blank" rel="noopener">学习 CSS 布局</a><br><a href="https://www.cnblogs.com/chaixiaozhi/p/8481778.html" target="_blank" rel="noopener">CSS 布局(四) float 详解</a><br><a href="https://www.cnblogs.com/chaixiaozhi/p/8490725.html" target="_blank" rel="noopener">CSS 布局(六) 对齐方式</a><br><a href="https://segmentfault.com/a/1190000010698609" target="_blank" rel="noopener">七种实现左侧固定，右侧自适应两栏布局的方法</a><br><a href="https://www.zhangxinxu.com/wordpress/2009/11/%E6%88%91%E7%86%9F%E7%9F%A5%E7%9A%84%E4%B8%89%E7%A7%8D%E4%B8%89%E6%A0%8F%E7%BD%91%E9%A1%B5%E5%AE%BD%E5%BA%A6%E8%87%AA%E9%80%82%E5%BA%94%E5%B8%83%E5%B1%80%E6%96%B9%E6%B3%95/" target="_blank" rel="noopener">我熟知的三种三栏网页宽度自适应布局方法</a><br><a href="http://www.imooc.com/wenda/detail/254035" target="_blank" rel="noopener">双飞翼布局</a><br><a href="https://www.zhangxinxu.com/wordpress/2015/02/css-deep-understand-flow-bfc-column-two-auto-layout/" target="_blank" rel="noopener">CSS 深入理解流体特性和 BFC 特性下多栏自适应布局</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTPS</title>
    <url>/2019/10/03/HTTPS/</url>
    <content><![CDATA[<h3 id="HTTP为什么不安全："><a href="#HTTP为什么不安全：" class="headerlink" title="HTTP为什么不安全："></a>HTTP为什么不安全：</h3><ul>
<li>通信使用明文（不加密），内容可能被窃听</li>
<li>不验证通信方的身份，因此有可能遭遇伪装</li>
<li>无法证明报文的完整性，所以有可能被篡改</li>
</ul>
<h3 id="HTTP-加密-认证-完整性保护-HTTPS-HTTP-Secure）"><a href="#HTTP-加密-认证-完整性保护-HTTPS-HTTP-Secure）" class="headerlink" title="HTTP+加密+认证+完整性保护=HTTPS(HTTP Secure）"></a>HTTP+加密+认证+完整性保护=HTTPS(HTTP Secure）</h3><ul>
<li>HTTPS则是具有安全性的SSL加密传输协议</li>
<li>HTTPS协议需要CA申请证书</li>
<li>HTTP和HTTPS使用的是完全不同的连接方式，用的端口也不一样，前者是80，后者是443</li>
<li>HTTPS通过HTTP来传输信息，但是信息通过TLS协议进行了加密</li>
</ul>
<h3 id="TLS"><a href="#TLS" class="headerlink" title="TLS"></a>TLS</h3><p>https仍用http传输信息，但信息通过TLS进行了加密。<br>TLS作用于表示层</p>
<h5 id="两种加密技术"><a href="#两种加密技术" class="headerlink" title="两种加密技术"></a>两种加密技术</h5><ul>
<li>对称加密<br>两边有相同的密钥，都知道如何加密解密</li>
<li>非对称加密<br>数据公钥加密，私钥解密，私钥只有发出公钥的一方知道</li>
</ul>
<p>HTTPS使用两种加密方式混用的加密机制（先用非对称加密方式传递对称加密中要使用的密钥，然后再用对称加密方式传递数据）。流程如下：<br>服务器将公钥发散出去。之后客户端创建一个密钥，用此公钥加密后发送给服务器，服务器用私钥解密，就能得知此密钥。双方都知道密码，之后就可以采用对称加密的方式进行数据传输了。</p>
<h5 id="TSL的握手过程"><a href="#TSL的握手过程" class="headerlink" title="TSL的握手过程"></a>TSL的握手过程</h5><ol>
<li>客户端向服务器发送Client Hello报文，包含：</li>
</ol>
<ul>
<li>支持的SSL协议版本</li>
<li>加密组件（支持的加密方法等）</li>
</ul>
<ol start="2">
<li>服务器回应，向客户端发起回应，称为Server Hello</li>
</ol>
<ul>
<li>确认通信协议版本</li>
<li>确认使用加密方法</li>
<li>服务器生成的随机数</li>
<li>服务器公开密钥证书</li>
</ul>
<ol start="3">
<li>客户端回应（Client Key Exchange报文），首先验证服务器证书，如果证书不是可信机构办法的，或者证书中域名和实际域名不一样，或者证书已经过期，就会向访问者显示一个警告，由其选择是否还要继续通信。如果证书没有问题，客户端从证书中取出服务器的公钥。然后向服务器发送下面三项信息：</li>
</ol>
<ul>
<li>一个随机数Pre master（用服务器提供的公钥加密）</li>
<li>编码改变通知（之后的通信会采用Pre master密钥加密（对称加密））</li>
<li>客户端握手结束通知（Finish报文）。该报文包含前面发送的所有内容的hash值，供服务器校验</li>
</ul>
<ol start="4">
<li>服务端收到第三个随机数之后，计算本次会话所用的会话加密，然后向客户端发送下面信息：</li>
</ol>
<ul>
<li>编码改变通知</li>
<li>服务器握手结束，同时也是前面所有内容的hash值，供服务器校验</li>
</ul>
<p>此时两端都有了最终的协商密钥了，接下里的传输就使用这个密钥加密解密（对称加密）</p>
<h3 id="HTTPS的缺点"><a href="#HTTPS的缺点" class="headerlink" title="HTTPS的缺点"></a>HTTPS的缺点</h3><ul>
<li>解密加密过程消耗CPU、内存等资源</li>
<li>加密运算和多次握手降低了访问速度</li>
<li>SSL证书信用体系不绝对安全，并且增加费用开销</li>
<li>在开发阶段，加大了页面调试难度 。由于信息都被加密了，所以用代理工具的话，需要先解密然后才能看到真实信息</li>
<li>用HTTPS访问的页面，页面内的外部资源都得用HTTPS请求，包括脚本中的AJAX请求</li>
</ul>
]]></content>
      <categories>
        <category>Basic knowledge</category>
      </categories>
      <tags>
        <tag>Network</tag>
      </tags>
  </entry>
  <entry>
    <title>JS基础知识点</title>
    <url>/2019/10/12/JS%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86%E7%82%B9/</url>
    <content><![CDATA[<h3 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h3><h5 id="数据类型-1"><a href="#数据类型-1" class="headerlink" title="数据类型"></a>数据类型</h5><p>ES 中有 6 种基本数据类型：undefined（未声明），null（空对象指针，可以说一个变量想用来保存对象，未保存之前最好使其等于 null），boolean，number，string，symbol（fromES6））；一种复杂数据类型：Object<br>引用类型有：Object，Array，Function，Date，RegExp</p>
<h5 id="基本类型与引用类型区别"><a href="#基本类型与引用类型区别" class="headerlink" title="基本类型与引用类型区别"></a>基本类型与引用类型区别</h5><p>引用类型（对象类型）和基本类型（原始类型）不同的是，基本类型存储的是值，引用类型存储的是地址（指针）。<br>当创建了一个引用类型的时候，计算机会在内存中帮我们开辟一个空间来存放值，但是我们需要找到这个空间，这个空间会拥有一个地址（指针）。</p>
<h5 id="typeof-vs-instanceof"><a href="#typeof-vs-instanceof" class="headerlink" title="typeof vs instanceof"></a>typeof vs instanceof</h5><p>typeof 对于基本类型来说，除 null 会显示成 object（null 不是 object，这是 JS 一个古老的 bug），其余都可以显示正确的类型；<br>typeof 对于引用类型来说，除了函数（会显示 function）都会显示 object，所以说 typeof 并不能准确判断变量到底是什么类型。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typeof</span> []; <span class="comment">// 'object'</span></span><br><span class="line"><span class="keyword">typeof</span> &#123;&#125;; <span class="comment">// 'object'</span></span><br><span class="line"><span class="keyword">typeof</span> <span class="built_in">console</span>.log; <span class="comment">// 'function'</span></span><br></pre></td></tr></table></figure>

<p>如果我们想判断一个引用类型的正确类型，这时候可以考虑使用 instanceof，因为内部机制是通过原型链来判断的；但是 instanceof 不能用来判断基本类型</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> Person = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;&#125;;</span><br><span class="line"><span class="keyword">const</span> p1 = <span class="keyword">new</span> Person();</span><br><span class="line">p1 <span class="keyword">instanceof</span> Person; <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> str = <span class="string">"hello world"</span>;</span><br><span class="line">str <span class="keyword">instanceof</span> <span class="built_in">String</span>; <span class="comment">// false</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> str1 = <span class="keyword">new</span> <span class="built_in">String</span>(<span class="string">"hello world"</span>);</span><br><span class="line">str1 <span class="keyword">instanceof</span> <span class="built_in">String</span>; <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<h5 id="如何判断一个引用类型是数组"><a href="#如何判断一个引用类型是数组" class="headerlink" title="如何判断一个引用类型是数组"></a>如何判断一个引用类型是数组</h5><ul>
<li>根据构造函数来判断 xxx instanceof Array</li>
<li>直接用 Array.isArray() 判断</li>
<li>根据 class 属性判断 <code>Object.prototype.toString.call(obj) === &#39;[object Array]&#39;</code></li>
</ul>
<h3 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h3><p>在 JS 中类型转换只有三种情况，分别是：转换为布尔值；数字；字符串</p>
<h5 id="转-Boolean"><a href="#转-Boolean" class="headerlink" title="转 Boolean"></a>转 Boolean</h5><p>用 Boolean()进行转换。在条件判断时，除了 undefined， null， false， NaN， ‘’， 0， -0，其他所有值都转为 true，包括所有对象。<br>eg. <code>if([]){console.log(3)}</code>是能输出<code>3</code>的</p>
<h5 id="对象转原始类型"><a href="#对象转原始类型" class="headerlink" title="对象转原始类型"></a>对象转原始类型</h5><p>对象在转换类型的时候，会调用内置的 [[ToPrimitive]] 函数，对于该函数来说，算法逻辑一般来说如下：</p>
<ul>
<li>如果已经是原始类型了，那就不需要转换了</li>
<li>如果需要转字符串类型就调用 x.toString()，转换为基础类型的话就返回转换的值。不是字符串类型的话就先调用 valueOf，结果不是基础类型的话再调用 toString</li>
<li>调用 x.valueOf()，如果转换为基础类型，就返回转换的值</li>
<li>如果都没有返回原始类型，就会报错</li>
</ul>
<p>当然可以重写 Symbol.toPrimitive ，该方法在转原始类型时调用优先级最高。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;</span><br><span class="line">  valueOf() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  &#125;,</span><br><span class="line">  toString() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"1"</span>;</span><br><span class="line">  &#125;,</span><br><span class="line">  [<span class="built_in">Symbol</span>.toPrimitive]() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="number">1</span> + a; <span class="comment">// =&gt; 3</span></span><br></pre></td></tr></table></figure>

<h5 id="四则运算符"><a href="#四则运算符" class="headerlink" title="四则运算符"></a>四则运算符</h5><p>加法运算符不同于其他几个运算符，它有以下几个特点：</p>
<ul>
<li>运算中其中一方为字符串，那么就会把另一方也转换为字符串</li>
<li>如果一方不是字符串或者数字，那么会将它转换为数字或者字符串</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span> + <span class="string">"1"</span>; <span class="comment">// '11'</span></span><br><span class="line"><span class="literal">true</span> + <span class="literal">true</span>; <span class="comment">// 2</span></span><br><span class="line"><span class="number">4</span> +</span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>] + <span class="comment">// "41,2,3"</span></span><br><span class="line"><span class="keyword">new</span> <span class="built_in">Array</span>(<span class="number">017</span>) + <span class="comment">// NaN</span></span><br><span class="line"><span class="literal">null</span> + <span class="comment">// 0</span></span><br><span class="line">  <span class="literal">undefined</span>; <span class="comment">// NaN</span></span><br></pre></td></tr></table></figure>

<p>如果你对于答案有疑问的话，请看解析：</p>
<ul>
<li>对于第一行代码来说，触发特点一，所以将数字 1 转换为字符串，得到结果 ‘11’</li>
<li>对于第二行代码来说，触发特点二，所以将 true 转为数字 1</li>
<li>对于第三行代码来说，触发特点二，所以将数组通过 toString 转为字符串 1,2,3，得到结果 41,2,3</li>
<li>对于第四行。new Array(017)，里面 017 是八进制，相当于 15，所以是创建了长度为 15 但内容都是空的数组。<strong>单独的 + 会使其转 Number</strong>。<strong>对数组而言</strong>，转 Number 时候数组长度大于等于 2 时都为 NaN。数组长度为 0 时是 0，为 1 的时候对数组第一项用 Number()。</li>
</ul>
<p>另外对于加法还需要注意这个表达式 ‘a’ + + ‘b’</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">"a"</span> + +<span class="string">"b"</span>; <span class="comment">// -&gt; "aNaN"</span></span><br></pre></td></tr></table></figure>

<p>因为 + ‘b’ 等于 NaN，所以结果为 “aNaN”。<br>对于除了加法的运算符来说，只要其中一方是数字，那么另一方就会被转为数字</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="number">4</span> * <span class="string">"3"</span>; <span class="comment">// 12</span></span><br><span class="line"><span class="number">4</span> * []; <span class="comment">// 0</span></span><br><span class="line"><span class="number">4</span> * [<span class="number">1</span>, <span class="number">2</span>]; <span class="comment">// NaN</span></span><br></pre></td></tr></table></figure>

<p>运算符前置和后置的区别</p>
<ul>
<li>如果该运算符作为后置操作符，则返回它递减之前的值。</li>
<li>如果该运算符作为前置操作符，则返回它递减之后的值。</li>
</ul>
<p>后置：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> i = <span class="number">5</span>;</span><br><span class="line"><span class="keyword">var</span> a = i--;</span><br><span class="line"><span class="built_in">console</span>.log(i); <span class="comment">//输出4</span></span><br><span class="line"><span class="built_in">console</span>.log(a); <span class="comment">//输出5</span></span><br></pre></td></tr></table></figure>

<p>前置：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> j = <span class="number">5</span>;</span><br><span class="line"><span class="keyword">var</span> b = --j;</span><br><span class="line"><span class="built_in">console</span>.log(j); <span class="comment">//输出4</span></span><br><span class="line"><span class="built_in">console</span>.log(b); <span class="comment">//输出4</span></span><br></pre></td></tr></table></figure>

<h5 id="比较运算符"><a href="#比较运算符" class="headerlink" title="比较运算符"></a>比较运算符</h5><p>注意：+ 的优先级高于 三目运算符 ? : ; + 的优先级高于比较运算符 &gt;</p>
<p>1、如果是对象，就通过 toPrimitive 转换对象<br>2、如果是字符串，就通过 unicode 字符索引来比较</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;</span><br><span class="line">  valueOf() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  &#125;,</span><br><span class="line">  toString() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"1"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line">a &gt; <span class="number">-1</span>; <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>在以上代码中，因为 a 是对象，所以会通过 valueOf 转换为原始类型再比较值。</p>
<h5 id="比较"><a href="#比较" class="headerlink" title="==比较"></a>==比较</h5><ul>
<li>Boolean，number，string 三类比较的时候把值转换成<strong>数字</strong>，在看转换结果是否相等。证明：（’1’==true) 是真 （’abc’==true）是假。</li>
<li>undefined 参与比较，换成了 NaN,所以其他三个类型跟它比较都是 false，跟 null 类型比较的时候是 true。（NaN==NaN)是假</li>
<li>null 参与比较，被当成对象，因为 null 没有 valueof 和 toString，除了 undefined 谁跟他比较都是 false。</li>
<li>值类型与对象比较：先调用对象 valueof 如果仍返回对象，调用 tostring，如果还是没有就不等。**{}转数字是 NaN，[]转数字是 0**</li>
</ul>
<h5 id="与"><a href="#与" class="headerlink" title="==与==="></a>==与===</h5><p>对于 == 来说，如果对比双方的类型不一样的话，就会进行类型转换。<br>假如我们需要对比 x 和 y 是否相同，就会进行如下判断流程：</p>
<ol>
<li>首先会判断两者类型是否相同。相同的话就是比大小了</li>
<li>类型不相同的话，那么就会进行类型转换</li>
<li>会先判断是否在对比 null 和 undefined，是的话就会返回 true</li>
<li>判断两者类型是否是 string 和 number，是的话就会将字符串转换为 number</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span> == <span class="string">'1'</span></span><br><span class="line">      ↓</span><br><span class="line"><span class="number">1</span> ==  <span class="number">1</span></span><br></pre></td></tr></table></figure>

<ol start="5">
<li>判断其中一方是否为 boolean，是的话就会把 boolean 转为 number 再进行判断</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">'1'</span> == <span class="literal">true</span></span><br><span class="line">        ↓</span><br><span class="line"><span class="string">'1'</span> ==  <span class="number">1</span></span><br><span class="line">        ↓</span><br><span class="line"> <span class="number">1</span>  ==  <span class="number">1</span></span><br></pre></td></tr></table></figure>

<ol start="6">
<li>判断其中一方是否 object 且另一方为 string、number 或者 symbol，是的话就会把 object 转为原始类型再进行判断</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">'1'</span> == &#123; <span class="attr">name</span>: <span class="string">'xxx'</span> &#125;</span><br><span class="line">        ↓</span><br><span class="line"><span class="string">'1'</span> == <span class="string">'[object Object]'</span></span><br></pre></td></tr></table></figure>

<p>现在来想想[]==![]输出什么？<br>首先先执行的是![]，它会得到 false。<br>然后[]==false，返回 true。<br>那么[]==[],{}=={}又输出什么呢？<br>类型一致，它们是引用类型，地址是不一样的，所以为 false!<br>对于 === 来说就简单多了，就是判断两者类型和值是否相同。</p>
<h3 id="各种运算符和操作符"><a href="#各种运算符和操作符" class="headerlink" title="各种运算符和操作符"></a>各种运算符和操作符</h3><h5 id="逻辑运算符-或运算，左假看右；与运算，左真看右"><a href="#逻辑运算符-或运算，左假看右；与运算，左真看右" class="headerlink" title="逻辑运算符 (或运算，左假看右；与运算，左真看右)"></a>逻辑运算符 (或运算，左假看右；与运算，左真看右)</h5><ul>
<li>只要 || 前面为 false,不管 || 后面是 true 还是 false，都返回 || 后面的值。</li>
<li>只要 || 前面为 true,不管 || 后面是 true 还是 false，都返回 || 前面的值。</li>
<li>只要 &amp;&amp; 前面是 false，无论 &amp;&amp; 后面是 true 还是 false，结果都将返 &amp;&amp; 前面的值;</li>
<li>只要 &amp;&amp; 前面是 true，无论 &amp;&amp; 后面是 true 还是 false，结果都将返 &amp;&amp; 后面的值;</li>
</ul>
<h5 id="位操作符"><a href="#位操作符" class="headerlink" title="位操作符"></a>位操作符</h5><p>内存中是用 32 位的二进制来表示数值，没用到的位用 0 填充。负数是用二进制补码表示（绝对值的二进制，再反码，再+1）</p>
<ul>
<li>按位非。用 ~ 表示，返回数值的反码</li>
<li>按位与 &amp; 。将两个数值每一位对齐，对相同位置上的两个数进行对比。只有两个都是 1 时才返回 1，否则都是 0。</li>
<li>按位或 | 。同理，但是是只有两个都是 0 是才返回 0。</li>
<li>按位异或 ^ 。 同理，只有一个是 1 时候才返回 1。</li>
<li>左移 &lt;&lt; 。 二进制中左移几位</li>
<li>有符号的右移 &gt;&gt; 。 同理 保留符号位</li>
<li>无符号的右移 &gt;&gt;&gt; 。 以 0 来填充右移产生的空位（新的符号位），所以正数无影响，负数变化很大</li>
</ul>
<h3 id="This"><a href="#This" class="headerlink" title="This"></a>This</h3><p><a href="https://www.cnblogs.com/xiaohuochai/p/5735901.html" target="_blank" rel="noopener">更详细的解释</a><br>严格模式下禁止 this 关键字指向全局对象</p>
<h5 id="默认绑定"><a href="#默认绑定" class="headerlink" title="默认绑定"></a>默认绑定</h5><ul>
<li>全局环境，默认绑定到 window</li>
<li>函数独立调用的时候，this 默认绑定到 window</li>
<li>被嵌套的函数独立调用时，this 默认绑定到 window</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//虽然test()函数被嵌套在obj.foo()函数中，但test()函数是独立调用，而不是方法调用。所以this默认绑定到window</span></span><br><span class="line"><span class="keyword">var</span> a = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  a: <span class="number">2</span>,</span><br><span class="line">  foo: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">test</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="keyword">this</span>.a);</span><br><span class="line">    &#125;</span><br><span class="line">    test();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line">obj.foo(); <span class="comment">//0</span></span><br></pre></td></tr></table></figure>

<ul>
<li>立即执行的函数 this 是 window</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">0</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  (<span class="function"><span class="keyword">function</span> <span class="title">test</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.a);</span><br><span class="line">  &#125;)();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  a: <span class="number">2</span>,</span><br><span class="line">  foo: foo</span><br><span class="line">&#125;;</span><br><span class="line">obj.foo(); <span class="comment">//0</span></span><br><span class="line"><span class="comment">//  这串代码本质上和上个例子的代码是一样的</span></span><br></pre></td></tr></table></figure>

<ul>
<li>闭包的 this 默认绑定到 window</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">0</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">test</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.a);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> test;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">  a: <span class="number">2</span>,</span><br><span class="line">  foo: foo</span><br><span class="line">&#125;;</span><br><span class="line">obj.foo()(); <span class="comment">//0</span></span><br></pre></td></tr></table></figure>

<h5 id="隐式绑定"><a href="#隐式绑定" class="headerlink" title="隐式绑定"></a>隐式绑定</h5><p>被直接对象所包含的函数调用时，也称为方法调用，this 隐式绑定到该直接对象</p>
<h5 id="显示绑定"><a href="#显示绑定" class="headerlink" title="显示绑定"></a>显示绑定</h5><p>通过 call()、apply()、bind()方法把对象绑定到 this 上，叫做显式绑定。对于被调用的函数来说，叫做间接调用</p>
<h5 id="new-绑定"><a href="#new-绑定" class="headerlink" title="new 绑定"></a>new 绑定</h5><p>如果函数或者方法调用之前带有关键字 new，它就构成构造函数调用。对于 this 绑定来说，称为 new 绑定</p>
<h5 id="优先级"><a href="#优先级" class="headerlink" title="优先级"></a>优先级</h5><p><strong>首先，new 的方式优先级最高，接下来是 bind 这些函数，然后是 obj.foo() 这种调用方式，最后是 foo 这种调用方式，同时，箭头函数的 this 一旦被绑定，就不会再被任何方式所改变</strong><br>实例与解析<br>先来看几个函数调用的场景：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.a)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> a = <span class="number">1</span></span><br><span class="line">foo();</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> obj = &#123;</span><br><span class="line">  a: <span class="number">2</span>,</span><br><span class="line">  foo: foo</span><br><span class="line">  foo2: foo2() &#123;</span><br><span class="line">    foo</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">obj.foo()</span><br><span class="line">obj.foo2()</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> c = <span class="keyword">new</span> foo()</span><br></pre></td></tr></table></figure>

<p>接下来一个个分析上面几个场景</p>
<ul>
<li>对于直接调用 foo 来说，不管 foo 函数被放在了什么地方，this 一定是 window</li>
<li>对于 obj.foo() 来说，我们只需要记住，<strong>谁调用了函数</strong>，谁就是 this，所以在这个场景下 foo 函数中的 this 就是 obj 对象。但是如果是 foo2 的情况，this 是指向 window 的</li>
<li>对于 new 的方式来说，<strong>this 被永远绑定在了 c 上面，不会被任何方式改变 this</strong> (在构造函数中 this 指向函数名)</li>
</ul>
<p>说完了以上几种情况，其实很多代码中的 this 应该就没什么问题了。<br>下面让我们看看箭头函数中的 this</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="keyword">this</span>);</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(a()()());</span><br></pre></td></tr></table></figure>

<p>首先箭头函数其实是没有 this 的，<strong>箭头函数中的 this 只取决包裹箭头函数的第一个普通函数的 this</strong>。在这个例子中，因为包裹箭头函数的第一个普通函数是 a，所以此时的 this 是 window。另外<strong>对箭头函数使用 bind 这类函数是无效的</strong>。<br>最后种情况也就是 bind 这些改变上下文的 API 了，对于这些函数来说，this 取决于第一个参数，如果第一个参数为空，那么就是 window。<br>那么说到 bind，<strong>如果对一个函数进行多次 bind，那么上下文会是什么</strong>呢？</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;&#125;;</span><br><span class="line"><span class="keyword">let</span> fn = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>);</span><br><span class="line">&#125;;</span><br><span class="line">fn.bind().bind(a)(); <span class="comment">// =&gt; ?</span></span><br></pre></td></tr></table></figure>

<p>如果认为输出结果是 a，那么就错了。<br>其实可以把上述代码转换成另一种形式：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// fn.bind().bind(a) 等价于</span></span><br><span class="line"><span class="keyword">let</span> fn2 = <span class="function"><span class="keyword">function</span> <span class="title">fn1</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> fn.apply();</span><br><span class="line">  &#125;.apply(a);</span><br><span class="line">&#125;;</span><br><span class="line">fn2();</span><br></pre></td></tr></table></figure>

<p>可以从上述代码中发现，<strong>不管我们给函数 bind 几次，fn 中的 this 永远由第一次 bind 决定</strong>，所以结果永远是 window。这个特性可以用来创建绑定了固定 this 的函数（见 MDN）</p>
<h3 id="闭包与作用域链"><a href="#闭包与作用域链" class="headerlink" title="闭包与作用域链"></a>闭包与作用域链</h3><p>更详细的解析见<a href="https://www.cnblogs.com/wangfupeng1988/p/3977924.html" target="_blank" rel="noopener">深入理解 javascript 原型和闭包（完结）</a></p>
<h5 id="执行环境（（执行）上下文环境）"><a href="#执行环境（（执行）上下文环境）" class="headerlink" title="执行环境（（执行）上下文环境）"></a>执行环境（（执行）上下文环境）</h5><p>execution context。红宝书的说法：定义了变量或函数有权访问其他数据，决定了它们各自的行为。<br>全局执行环境是 window 对象，即<strong>全局变量和函数都是作为 window 对象的属性和方法创建的</strong>，每个函数都有自己的执行环境（理解为函数的执行环境就是函数的归属者？）<br>From 博客的理解：</p>
<ul>
<li>通俗定义：在执行代码之前，把将要用到的所有的变量都事先拿出来，有的直接赋值了，有的先用 undefined 占个空，即是一种代码执行前的准备工作。eg1. 在全局代码段中，“准备工作”包括：知道有函数声明并赋值；知道有 this 并赋值；知道有变量，赋值为 undefined。eg2. 在函数体内的代码段中，“准备工作”包括了全部工作的那部分之外，还有知道有参数和 argument<strong>并赋值</strong>，确定了函数体内部自由变量的作用域。**(预编译)**</li>
<li>执行全局代码时，会产生一个执行上下文环境，每次调用函数都又会产生执行上下文环境。这么多上下文环境该如何管理，以及如何销毁而释放内存呢？答案是<a href="https://www.cnblogs.com/wangfupeng1988/p/3989357.html" target="_blank" rel="noopener">执行环境栈</a>。即当函数调用完成时，这个上下文环境以及其中的数据都会被消除，再重新回到全局上下文环境，<strong>处于活动状态的执行上下文环境只有一个</strong>。所以其实是一个进栈和出栈的过程，活动状态的执行环境就是栈尾的环境。</li>
</ul>
<h5 id="作用域"><a href="#作用域" class="headerlink" title="作用域"></a>作用域</h5><p>scope。一段程序代码中所用到的名字并不总是有效/可用的，而限定这个名字的可用性的代码范围就是这个名字的作用域。</p>
<ul>
<li>JS 没有块级作用域。“块”就是大括号“{}”中间的语句。其实就是 if 和 for 的{}中声明的变量在其{}之外的地方也能访问</li>
<li><strong>除了全局作用域之外，只有函数可以创建作用域。所以某函数的{}中就可以看做是该函数的作用域</strong>。函数有嵌套关系的时候，他们的作用域就有上下级关系。作用域在函数定义时就已经确定了。而不是在函数调用时确定。</li>
<li>作用域、执行环境、执行环境栈的关系，**详情见<a href="https://www.cnblogs.com/wangfupeng1988/p/3991995.html" target="_blank" rel="noopener">这里</a>**。总结为：作用域只是一个“地盘”，一个抽象的概念，其中没有变量。要通过作用域对应的执行上下文环境来获取变量的值。同一个作用域下，不同的调用会产生不同的执行上下文环境，继而产生不同的变量的值。所以，作用域中变量的值是在执行过程中产生的确定的，而作用域却是在函数创建时就确定了。所以，如果要查找一个作用域下某个变量的值，就需要找到这个作用域对应的执行上下文环境，再在其中寻找变量的值。</li>
</ul>
<h5 id="变量对象"><a href="#变量对象" class="headerlink" title="变量对象"></a>变量对象</h5><p>每个执行环境都有一个与之相关的变量对象，执行环境中定义的所有变量和函数都会保存在这个对象中</p>
<h5 id="作用域链"><a href="#作用域链" class="headerlink" title="作用域链"></a>作用域链</h5><ul>
<li><strong>代码执行过程中会创建变量对象的一个作用域链，作用域链的前端是当前执行环境的变量对象（某函数的作用域），末端是全局执行环境的变量对象（全局作用域）</strong>。</li>
<li>作用域链的作用是，保证执行环境里有权访问的变量和函数是有序的，作用域链的变量只能向上访问，变量访问到 window 对象即被终止，作用域链向下访问不行</li>
<li>自由变量：在 A 作用域中使用的变量 x，却没有在 A 作用域中声明（即在其他作用域中声明的），对于 A 作用域来说，x 就是一个自由变量。自由变量跨作用域取值时，<strong>要去创建这个函数的作用域取值（去创建这个函数的作用域沿作用域链找），而不是“父作用域”</strong>（eg. 闭包（函数作为参数例子）有时会出现这种情况）</li>
<li><strong>变量查找过程：先在自己的作用域中找，如果找不到就沿着作用域链往上找。</strong></li>
<li>延长作用域链：执行环境类型一般只有全局和局部函数两种 try-catch 语句的 catch 块和 with 语句会在作用域链的前端增加一个变量对象（with 现在已经少用了）</li>
</ul>
<h5 id="闭包"><a href="#闭包" class="headerlink" title="闭包"></a><a href="https://www.cnblogs.com/wangfupeng1988/p/3994065.html" target="_blank" rel="noopener">闭包</a></h5><ul>
<li>定义：<strong>闭包就是能够读取其他函数内部变量的函数，其实就是利用了作用域链向上查找的特点。创建闭包的方式：函数作为返回值；函数作为参数</strong></li>
<li>作用：读取函数内部变量，让这些变量的值一直保持在内存中。但是会增加内容开销，记得及时销毁。</li>
<li>核心内容：函数调用完成之后，其执行上下文环境不一定会接着被销毁，这种不被销毁的情况就是闭包（函数作为返回值的情况下）。eg. fn1 中 return fn2; 外部有 var test = fn1(); 这里 fn1 执行之后其执行环境不会被销毁，因为 test 被赋值成函数，函数的特别之处在于可以创建一个独立的作用域。这里只有再执行一个 test()，那 fn1 和 fn2 的执行环境才会被销毁，否则会一直存在，所以说闭包会增加内容开销。</li>
</ul>
<h3 id="面向对象"><a href="#面向对象" class="headerlink" title="面向对象"></a>面向对象</h3><p>JS 的对象有两种属性值：数据属性 访问器属性</p>
<h5 id="数据属性"><a href="#数据属性" class="headerlink" title="数据属性"></a>数据属性</h5><p>包含一个数据值的位置（就是我们常用的属性类型）</p>
<h6 id="数据属性有四个特性"><a href="#数据属性有四个特性" class="headerlink" title="数据属性有四个特性"></a>数据属性有四个特性</h6><p>Configurable 表示能否通过 delete 删除属性从而重新定义属性，默认为 true<br>Enumerable 表示能否通过 for-in 循环返回属性，默认为 true<br>Writable 表示能否修改属性的值，默认为 true<br>Value 包含这个属性的数据值 默认为 undefined</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//使用字面量创建对象，前三个属性默认值为true，value为指定的值</span></span><br><span class="line"><span class="keyword">var</span> person = &#123;</span><br><span class="line">  name: <span class="string">"xxxx"</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>修改属性默认特性用 Object.defineProperty(obj,attr,{})</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person=&#123;&#125;</span><br><span class="line"><span class="built_in">Object</span>.defineProperty(person,<span class="string">'name'</span>,&#123;</span><br><span class="line">    wirtable:<span class="literal">false</span>,</span><br><span class="line">    value:<span class="string">'xxxx'</span>,</span><br><span class="line">    configurable:<span class="literal">false</span>;<span class="comment">//一旦被设置为false，不可以更改</span></span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">person.name=<span class="string">'xx'</span></span><br><span class="line"><span class="built_in">console</span>.log(person.name);<span class="comment">//xxxx ，严格模式下会报错</span></span><br><span class="line"><span class="keyword">delete</span> person.name<span class="comment">//无效 严格模式下会报错</span></span><br></pre></td></tr></table></figure>

<h5 id="访问器属性"><a href="#访问器属性" class="headerlink" title="访问器属性"></a>访问器属性</h5><p>访问器属性不包含数据值。但是包含 getter 和 setter 函数，定义读取和写入访问器属性的时候调用的函数（类似 Vue，或者 Vue 说就是借助访问器属性）</p>
<h6 id="四个特性"><a href="#四个特性" class="headerlink" title="四个特性"></a>四个特性</h6><p>Configuable 表示能否通过 delete 删除属性从而重新定义属性，默认是 true<br>enumerable 表示能否通过 for-in 循环返回属性，默认是 true<br>get 读取属性时调用函数，默认 undefined<br>ste 写入属性时调用函数，默认 undefined</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;</span><br><span class="line">  _age: <span class="number">20</span>, <span class="comment">//下划线写法表示只能通过对象方法访问的属性</span></span><br><span class="line">  state: <span class="string">"young"</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="built_in">Object</span>.defineProperty(person, <span class="string">"age"</span>, &#123;</span><br><span class="line">  <span class="keyword">get</span>: function() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>._age;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="keyword">set</span>: function(newVal) &#123;</span><br><span class="line">    <span class="keyword">if</span> (newVal &gt; <span class="number">50</span>) &#123;</span><br><span class="line">      <span class="keyword">this</span>._age = newVal;</span><br><span class="line">      <span class="keyword">this</span>.stae = <span class="string">"old"</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">this</span>._age = newVal;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>定义多个属性（数据属性和访问器属性都可以用），使用 Object.defineProperties();</p>
<h5 id="读取属性的特性"><a href="#读取属性的特性" class="headerlink" title="读取属性的特性"></a>读取属性的特性</h5><p>使用 Object.getOwnPropertyDescriptor()</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person=&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>.defineProperties(person,&#123;</span><br><span class="line">    _age:&#123;</span><br><span class="line">        values:<span class="number">20</span>;</span><br><span class="line">    &#125;,</span><br><span class="line">    state:&#123;</span><br><span class="line">        value:<span class="string">'young'</span>;</span><br><span class="line">    &#125;,</span><br><span class="line">    age:&#123;</span><br><span class="line">        <span class="keyword">get</span>:function()&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">this</span>._age;</span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="keyword">set</span>:function(newVal)&#123;</span><br><span class="line">            <span class="keyword">if</span>(newVal&gt;<span class="number">50</span>)&#123;</span><br><span class="line">                <span class="keyword">this</span>._age=newVal;</span><br><span class="line">                <span class="keyword">this</span>.stae=<span class="string">'old'</span>;</span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                 <span class="keyword">this</span>._age=newVal;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> descriptor=<span class="built_in">Object</span>.getOwnPropertyDescriptor(person,<span class="string">'_age'</span>);</span><br><span class="line">descriptor.value<span class="comment">//20</span></span><br><span class="line">descriptor.configurable<span class="comment">//false</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> descriptor=<span class="built_in">Object</span>.getOwnPropertyDescriptor(person,<span class="string">'age'</span>);</span><br><span class="line">descriptor.value<span class="comment">//undefined</span></span><br><span class="line">descriptor.configurable<span class="comment">//false</span></span><br><span class="line"><span class="keyword">typeof</span> descriptor <span class="comment">//function</span></span><br></pre></td></tr></table></figure>

<h5 id="函数与对象的关系"><a href="#函数与对象的关系" class="headerlink" title="函数与对象的关系"></a>函数与对象的关系</h5><p>js 中的内部对象包括 Array、Boolean、Date、Function、Global、Math、Number、Object、RegExp、String 以及各种错误类对象，包括 Error、EvalError、RangeError、ReferenceError、SyntaxError 和 TypeError。<br>其中 Global 和 Math 这两个对象又被称为“内置对象”，这两个对象在脚本程序初始化时被创建，不必实例化这两个对象。<br><strong>一切（引用类型）都是对象，对象是属性的集合</strong><br>eg. 构造函数可以用 this.的方式说明函数也是可以用 . 的方式来写属性的，其实是因为函数就是对象的一种。同理数组也是可以的，不过写属性方式可能不同，因为数组就像是对象的一个子集一样。但是函数和对象之间的关系比较复杂：</p>
<ul>
<li>对象都是通过函数创建的。（就算是字面量创建对象，其实也是构造函数法的一种语法糖（简单写法））</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//var obj = &#123; a: 10, b: 20 &#125;;</span></span><br><span class="line"><span class="keyword">var</span> obj = <span class="keyword">new</span> <span class="built_in">Object</span>();</span><br><span class="line">obj.a = <span class="number">10</span>;</span><br><span class="line">obj.b = <span class="number">20</span>;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">typeof</span> <span class="built_in">Object</span>); <span class="comment">// function</span></span><br></pre></td></tr></table></figure>

<ul>
<li>每个对象都有一个<code>__proto__</code>属性，指向创建该对象的函数的 prototype（即 Object.prototype）。但是 Object.prototype 是一个特例，它的<code>__proto__</code>指向的是 null。同理每个函数的<code>__proto__</code>追溯到祖先都是 Function.prototype。总结关系如下：<br><img src="/img/JS%E5%92%8CES6/%E5%8E%9F%E5%9E%8B.png"></li>
</ul>
<h3 id="继承及原型链"><a href="#继承及原型链" class="headerlink" title="继承及原型链"></a>继承及原型链</h3><h5 id="原型链"><a href="#原型链" class="headerlink" title="原型链"></a>原型链</h5><p>在 js 中，每个函数都有 prototype 属性，这个属性值是一个对象（同时这个对象带有 constructor 属性，指向函数本身）<br>通过 new 可以创建一个函数的实例，每个实例都有<em>proto</em>属性，指向构造函数的 prototype 对象。<br>当我们在一个对象上（或方法）找某个属性的时候，会先查找实例上是否存在这个属性，如果没有的话，沿着原型链向上查找<br><img src="/img/JS%E5%92%8CES6/%E5%8E%9F%E5%9E%8B%E9%93%BE.png"></p>
<h6 id="怎么判断一个属性是对象上的属性还是其原型对象上的属性"><a href="#怎么判断一个属性是对象上的属性还是其原型对象上的属性" class="headerlink" title="怎么判断一个属性是对象上的属性还是其原型对象上的属性"></a>怎么判断一个属性是对象上的属性还是其原型对象上的属性</h6><p>使用 hasOwnProperty()返回 true，说明是这个对象上的；如果返回 false，但是属性 in 这个对象返回了 true，说明是原型对象上的属性。如果都是 false，那么不存在这个属性<br><strong>创建对象采用构造函数和原型模式组合的方式</strong>。构造函数用来定义实例属性，原型模式用来定义方法和共享属性</p>
<h5 id="继承的原理"><a href="#继承的原理" class="headerlink" title="继承的原理"></a>继承的原理</h5><p><strong>还是跟原型链有关。每个函数都有个原型对象，这个对象用来存储通过这个函数所创建的所有实例的共有属性和方法。在读取某个对象属性的时候，从实例开始，如果实例有就返回，如果没有就找构造函数的原型对象，找到了就返回。通过实例只能访问原型对象里的值，但是不能修改。这就实现了继承</strong>。</p>
<h5 id="组合继承-（原型链-用-call-借用构造函数）"><a href="#组合继承-（原型链-用-call-借用构造函数）" class="headerlink" title="组合继承 （原型链 + 用 call 借用构造函数）"></a>组合继承 （原型链 + 用 call 借用构造函数）</h5><p>组合继承是最常用的继承方式。<br><strong>单纯用原型链继承的问题是</strong>：包含引用类型的值的属性会被所有实例共享，某个实例改了这个属性则所有实例都会改。eg. <code>this.food = [&#39;apple&#39;, &#39;banana&#39;]</code><br><strong>单纯用借用构造函数继承的</strong></p>
<ul>
<li>优点：子类型构造函数可以传参：不会与父类引用类型的值的属性共享</li>
<li>缺点：在父类中定义的方法在子类中并不可见（因为这样的话方法就是引用类型属性）eg. <code>this.getName = function() { xxx }</code></li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Parent</span>(<span class="params">age</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.age = age;</span><br><span class="line">&#125;</span><br><span class="line">Parent.prototype.getAge = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.age);</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Child</span>(<span class="params">age</span>) </span>&#123;</span><br><span class="line">  Parent.call(<span class="keyword">this</span>, age);</span><br><span class="line">&#125;</span><br><span class="line">Child.prototype = <span class="keyword">new</span> Parent();</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> child = <span class="keyword">new</span> Child(<span class="number">5</span>);</span><br><span class="line"></span><br><span class="line">child.getAge(); <span class="comment">// 5</span></span><br><span class="line"><span class="built_in">console</span>.log(child <span class="keyword">instanceof</span> Parent); <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>以上继承的方式核心是在子类的构造函数中通过 Parent.call(this) 继承父类的属性，然后改变<strong>子类的原型为 new Parent()<strong>来继承父类的函数。<br>这种继承方式</strong>优点在于子类型构造函数可以传参，不会与父类引用属性共享</strong>，可以复用父类的函数，但是也存在一个<strong>缺点就是在继承父类函数的时候调用了父类构造函数，导致子类的原型上多了不需要的父类属性，存在内存上的浪费</strong>。</p>
<h5 id="寄生组合继承"><a href="#寄生组合继承" class="headerlink" title="寄生组合继承"></a>寄生组合继承</h5><p>这种继承方式对组合继承进行了优化，组合继承缺点在于继承父类函数时调用了构造函数，我们只需要优化掉这点就行了。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Parent</span>(<span class="params">value</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">this</span>.val = value;</span><br><span class="line">&#125;</span><br><span class="line">Parent.prototype.getValue = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.val);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Child</span>(<span class="params">value</span>) </span>&#123;</span><br><span class="line">  Parent.call(<span class="keyword">this</span>, value);</span><br><span class="line">&#125;</span><br><span class="line">Child.prototype = <span class="built_in">Object</span>.create(Parent.prototype, &#123;</span><br><span class="line">  <span class="keyword">constructor</span>: &#123;</span><br><span class="line">    value: Child,</span><br><span class="line">    enumerable: <span class="literal">false</span>,</span><br><span class="line">    writable: <span class="literal">true</span>,</span><br><span class="line">    configurable: <span class="literal">true</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> child = <span class="keyword">new</span> Child(<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">child.getValue(); <span class="comment">// 1</span></span><br><span class="line">child <span class="keyword">instanceof</span> Parent; <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>以上继承实现的核心就是将父类的原型赋值给了子类，并且将构造函数设置为子类，这样既解决了无用的父类属性问题，还能正确的找到子类的构造函数。</p>
<h5 id="ES6-的-Class-继承"><a href="#ES6-的-Class-继承" class="headerlink" title="ES6 的 Class 继承"></a>ES6 的 Class 继承</h5><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Parent</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(value) &#123;</span><br><span class="line">    <span class="keyword">this</span>.val = value;</span><br><span class="line">  &#125;</span><br><span class="line">  getValue() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.val);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.val;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">Parent</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(value) &#123;</span><br><span class="line">    <span class="keyword">super</span>(value);</span><br><span class="line">  &#125;</span><br><span class="line">  toString() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.value + <span class="string">" "</span> + <span class="keyword">super</span>.getValue(); <span class="comment">// 调用父类的getValue()</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> child = <span class="keyword">new</span> Child(<span class="number">1</span>);</span><br><span class="line">child.getValue(); <span class="comment">// 1</span></span><br><span class="line">child <span class="keyword">instanceof</span> Parent; <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<p>super 关键字指向当前对象的原型对象<br>class 实现继承的核心在于使用 extends 表明继承自哪个父类，并且在子类构造函数中必须调用 super，因为这段代码可以看成 Parent.call(this, value)。</p>
<h5 id="Class-中的箭头函数和普通函数有何区别"><a href="#Class-中的箭头函数和普通函数有何区别" class="headerlink" title="Class 中的箭头函数和普通函数有何区别"></a>Class 中的箭头函数和普通函数有何区别</h5><p>箭头函数相当于绑定上了 this，类似构造函数内的一个值。<br>箭头函数会绑定在实例对象上，普通函数最后会绑定在原型上。</p>
<h3 id="DOM"><a href="#DOM" class="headerlink" title="DOM"></a>DOM</h3><p>DOM 是针对 HTML 和 XML 文档的一个 API。将文档看作一个层次化的节点树，可以用 JS 来操作这个节点树。</p>
<h5 id="节点层次"><a href="#节点层次" class="headerlink" title="节点层次"></a>节点层次</h5><p>文档节点（Document）是每个文档的根节点，它有且仅有一个子节点：html 元素（节点与元素的关系类似于键与值的关系）。</p>
<h5 id="DOM-如何创建元素"><a href="#DOM-如何创建元素" class="headerlink" title="DOM 如何创建元素"></a>DOM 如何创建元素</h5><p>创建：createElement(‘div’);<br>添加：appendChild(element) ； insertBefore(insertdom.chosendom)</p>
<h5 id="DOM-获取元素的方式"><a href="#DOM-获取元素的方式" class="headerlink" title="DOM 获取元素的方式"></a>DOM 获取元素的方式</h5><ul>
<li>通过元素类型获取<ul>
<li>document.getElementById();//id 名，在实际开发中较少使用，选择器中多用 class id 一般只用在顶级层存在 不能太过依赖 id</li>
<li>document.getElementsByTagName();//标签名</li>
<li>document.getElementsByClassName();//类名</li>
<li>document.getElementsByName();//DOM 般不用</li>
<li>document.querySelector();//css 选择符模式，返回与该模式匹配的第一个元素，结果为一个元素；如果没找到匹配的元素，则返回 null</li>
<li>document.querySelectorAll()//css 选择符模式，返回与该模式匹配的所有元素，结果为一个类数组</li>
</ul>
</li>
<li>根据关系树来选择<ul>
<li>parentNode//获取所选节点的父节点，最顶层的节点为#document</li>
<li>childNodes //获取所选节点的子节点们</li>
<li>firstChild //获取所选节点的第一个子节点</li>
<li>lastChild //获取所选节点的最后一个子节点</li>
<li>nextSibling //获取所选节点的后一个兄弟节点 列表中最后一个节点的 nextSibling 属性值为 null</li>
<li>previousSibling //获取所选节点的前一兄弟节点 列表中第一个节点的 previousSibling 属性值为 null</li>
</ul>
</li>
<li>根据元素节点树来选择<ul>
<li>parentElement 　//返回当前元素的父元素节点（IE9 以下不兼容）</li>
<li>children // 返回当前元素的元素子节点</li>
<li>firstElementChild //返回的是第一个元素子节点（IE9 以下不兼容）</li>
<li>lastElementChild //返回的是最后一个元素子节点（IE9 以下不兼容）</li>
<li>nextElementSibling //返回的是后一个兄弟元素节点（IE9 以下不兼容）</li>
<li>previousElementSibling //返回的是前一个兄弟元素节点（IE9 以下不兼容）</li>
</ul>
</li>
</ul>
<h5 id="获取-DOM-节点-get-系列和-query-系列哪种性能好？"><a href="#获取-DOM-节点-get-系列和-query-系列哪种性能好？" class="headerlink" title="获取 DOM 节点 get 系列和 query 系列哪种性能好？"></a>获取 DOM 节点 get 系列和 query 系列哪种性能好？</h5><p>1.从性能上说 get 系列的性能都比 query 系列好，get 系列里面各有差异，这些差异可以结合算法如何遍历搜索去理解，都解释得通。<br>2.getElementsByTagName 比 querySelectorAll 快的原因在于：getElementsByTagName 创建的过程不需要做任何操作，只需要返回一个指针即可。而 querySelectorAll 会循环遍历所有的的结果，然后创建一个新的 NodeList。 3.实际在用的过程中取决于要获取的是什么，再进行选择。</p>
<h5 id="节点属性中-children-和-childNodes-有什么区别？"><a href="#节点属性中-children-和-childNodes-有什么区别？" class="headerlink" title="节点属性中 children 和 childNodes 有什么区别？"></a>节点属性中 children 和 childNodes 有什么区别？</h5><p>childNodes 返回的是节点的子节点集合（NodeLists),包括元素节点、文本节点还有属性节点。<br>children 返回的只是节点的元素节点集合（HTMLCollection)</p>
<h5 id="HTMLCollection-和-NodeList-的比较"><a href="#HTMLCollection-和-NodeList-的比较" class="headerlink" title="HTMLCollection 和 NodeList 的比较"></a>HTMLCollection 和 NodeList 的比较</h5><ul>
<li>共同点<ul>
<li>都是类数组对象，都有 length 属性</li>
<li>都有共同的方法：item,可以通过 item(index)获取返回结果的元素</li>
<li>都是实时变动的，document 上面的更改会反映到相关的对象上</li>
<li>注：querySeletorAll 返回的 NodeList 是个浅拷贝的类数组对象，在节点数目上是非实时的，不过对节点属性进行修改，还是实时反映的</li>
</ul>
</li>
<li>区别<ul>
<li>NodeList 可以包含任何节点类型，HTMLCollection 只包含元素节点。elementNode 就是 HTML 中的标签</li>
<li>HTMLCollection 比 NodeList 多一个方法：nameitem(),除了可以用 id，还可以用 name 来获取节点信息</li>
</ul>
</li>
</ul>
<h3 id="BOM"><a href="#BOM" class="headerlink" title="BOM"></a>BOM</h3><p>浏览器对象模型（BOM）以 window 对象为依托，表示浏览器窗口以及页面的可见区域。</p>
<ul>
<li>window 对象：表示浏览器的一个实例。它既是 JS 访问浏览器窗口的一个接口，也是 ES 规定的 Global 对象</li>
<li>location 对象：提供了当前窗口中加载的文档有关的信息。它既是 window 对象的属性，也是 document 对象的属性。对象包含有关当前 URL 的信息</li>
<li>navigator 对象（提供与浏览器有关的信息）、screen 对象、history 对象包含用户（在浏览器窗口中）访问过的 URL</li>
</ul>
<h3 id="事件"><a href="#事件" class="headerlink" title="事件"></a>事件</h3><h5 id="事件流"><a href="#事件流" class="headerlink" title="事件流"></a>事件流</h5><p>事件流描述的是从页面中接收事件的顺序。包括三个阶段：事件捕获阶段（从 window 往事件触发处传播（从外到内），遇到注册的捕获事件会触发），处于目标阶段（传播到事件触发处时触发注册的事件），事件冒泡阶段（从事件触发处往 window 传播，遇到注册的冒泡事件会触发）。</p>
<h5 id="如何给元素注册事件"><a href="#如何给元素注册事件" class="headerlink" title="如何给元素注册事件"></a>如何给元素注册事件</h5><ul>
<li>在 HTML 元素中绑定事件 onclick=function()</li>
<li>获取 dom，dom.onclick</li>
<li>addEventListener(click,function,true/false)。第三个参数默认为 false，表示在冒泡阶段调用事件处理程序；true 则是捕获阶段</li>
<li>如果一个节点中的子节点是动态生成的，那么子节点需要注册事件的话应该注册在父节点上（因为有冒泡）。</li>
</ul>
<h5 id="如何阻止事件冒泡？如何取消默认事件？如何阻止事件的默认行为？"><a href="#如何阻止事件冒泡？如何取消默认事件？如何阻止事件的默认行为？" class="headerlink" title="如何阻止事件冒泡？如何取消默认事件？如何阻止事件的默认行为？"></a>如何阻止事件冒泡？如何取消默认事件？如何阻止事件的默认行为？</h5><ul>
<li>阻止事件冒泡：<br>W3C: <strong>stopPropagation()</strong>;<br>IE: e.cancelBubble=true;<br>写法 :<br>window.event ? window.event.cancelBubble=true:e.stop(Propagation)</li>
<li>取消默认事件<br>W3C：<strong>preventDefault()</strong><br>IE: e.returnValue:false;</li>
<li>阻止默认行为：<br><strong>return false</strong><br>原生的 js 会阻止默认行为，但会继续冒泡；<br>jquery 会阻止默认行为，并停止冒泡。</li>
</ul>
<h3 id="严格模式"><a href="#严格模式" class="headerlink" title="严格模式"></a>严格模式</h3><p>在JS文件顶部写<code>&#39;use strict&#39;</code>即可</p>
<ol>
<li><p>未声明变量赋值报错</p>
</li>
<li><p>指向window的this变为undifined；用 apply 、call、bind 改变 this 的值，this指向null的话就是null而不是window</p>
</li>
<li><p>函数名不能相同；在函数中修改了参数不能反映到arument；不能用caller和callee</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="meta">"use strict"</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">test</span>(<span class="params">n</span>) </span>&#123;</span><br><span class="line">  n = <span class="number">1</span></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="built_in">arguments</span>[<span class="number">0</span>])</span><br><span class="line">&#125;</span><br><span class="line">test(<span class="number">100</span>) <span class="comment">// 100</span></span><br></pre></td></tr></table></figure></li>
<li><p>删除了with()；0 开头的八进制字面量无效</p>
</li>
<li><p>保护保留关键字（static之类）</p>
</li>
</ol>
<h3 id="那些会让同事打你的简便写法"><a href="#那些会让同事打你的简便写法" class="headerlink" title="那些会让同事打你的简便写法"></a>那些会让同事打你的简便写法</h3><h4 id><a href="#" class="headerlink" title="~~"></a>~~</h4><p>对于 <code>~~</code> 运算符，它其实表示为： </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">var testData= 2.1; </span><br><span class="line">var testResult=(typeof testData===&quot;number&quot;&amp;&amp;!isNaN(testData)&amp;&amp;testData!==Infinity)?(testData&gt;0)?Math.floor(testData):Math.ceil(testData):0;</span><br></pre></td></tr></table></figure>

<p>参考资料<br><a href="https://blog.nowcoder.net/yiizgo" target="_blank" rel="noopener">呓语</a><br><a href="https://github.com/poetries/" target="_blank" rel="noopener">poetries</a><br><a href="https://www.cnblogs.com/wangfupeng1988/p/3977924.html" target="_blank" rel="noopener">深入理解 javascript 原型和闭包（完结）</a><br>《Javascript 高级程序设计》<br><a href="https://es6.ruanyifeng.com/" target="_blank" rel="noopener">ECMAScript 6 入门</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Nodejs基础</title>
    <url>/2019/11/02/Nodejs%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<h1 id="Node-js的特性"><a href="#Node-js的特性" class="headerlink" title="Node.js的特性"></a>Node.js的特性</h1><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>Node.js 是一个基于 Chrome V8 引擎的 JavaScript 运行环境（runtime）, 是一个让 JavaScript 运行在服务端的开发平台， 使用了一个事件驱动、非阻塞 I/O 的模型</p>
<h2 id="非阻塞IO"><a href="#非阻塞IO" class="headerlink" title="非阻塞IO"></a><a href="http://www.ruanyifeng.com/blog/2013/10/event_loop.html" target="_blank" rel="noopener">非阻塞IO</a></h2><p>以JS为例，JavaScript为一种单线程语言，所以如果有很多任务需要执行，只能等前面的任务执行完了，再执行后面的任务。一旦遇到大量任务或者遇到一个耗时的任务（一个I/O），网页就会被卡住。这种就是<strong>阻塞 I/O</strong>。而Node.js在I/O时函数立即返回，进程不等待I/O完成，因为有Event Loop 的概念来解决这个问题</p>
<p><strong>Event Loop</strong>是一个程序结构，用于等待和发送消息和事件。简单说，就是在程序中设置两个线程：一个负责程序本身的运行，称为”主线程”；另一个负责主线程与其他进程（主要是各种I/O操作）的通信，被称为”Event Loop线程”（可以译为”消息线程”）。当程序遇到耗时的I/O操作的时候，就交给Event Loop线程去处理，同时生成一个<strong>事件队列</strong>来放置这些未执行完的任务，并且<strong>监听</strong>这些I/O操作是否执行完成，主进程继续执行其他任务；等到I/O操作完成后，Event Loop线程再把结果返回主线程。主线程就调用事先设定的回调函数，完成整个任务。这样就提高了任务执行的效率，这种运行模式就是<strong>非阻塞IO</strong> 和<strong>事件驱动</strong><br><img src="/img/Node/EventLoop.png"></p>
<h2 id="事件驱动"><a href="#事件驱动" class="headerlink" title="事件驱动"></a>事件驱动</h2><p>I/O等异步操作结束后的通知，如上所述。其实就是观察者模式</p>
<h2 id="事件循环-Event-Loop"><a href="#事件循环-Event-Loop" class="headerlink" title="事件循环 Event Loop"></a><a href="https://jacleklm.github.io/2019/11/03/%E4%BA%8B%E4%BB%B6%E5%BE%AA%E7%8E%AF/" target="_blank" rel="noopener">事件循环 Event Loop</a></h2><h2 id="异步编程"><a href="#异步编程" class="headerlink" title="异步编程"></a><a href="https://jacleklm.github.io/2019/11/01/%E5%BC%82%E6%AD%A5%E7%BC%96%E7%A8%8B/#more" target="_blank" rel="noopener">异步编程</a></h2><h2 id="常用API"><a href="#常用API" class="headerlink" title="常用API"></a>常用API</h2><p>path，fs，Buffer，EventEmitter，process </p>
<h3 id="Web为什么最适合Node-js"><a href="#Web为什么最适合Node-js" class="headerlink" title="Web为什么最适合Node.js"></a>Web为什么最适合Node.js</h3><p>用JS；在处理高并发（单位时间内访问量大）、I/O密集场景性能优势明显<br>CPU密集：做计算、逻辑判断（压缩、解压、加密、解密）<br>I/O密集：文件操作（文件存取）、网络操作（http）、数据库。CPU的处理速度是远大于I/O的，也就是分析web页面需求很快，但是从计算机拿文件比较慢</p>
<h2 id="缺点-amp-优点"><a href="#缺点-amp-优点" class="headerlink" title="缺点 &amp; 优点"></a>缺点 &amp; 优点</h2><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><p>Node 是基于事件驱动和无阻塞的，所以非常适合处理并发请求，因此构建在Node 上的代理服务器相比其他技术实现（如Ruby）的服务器表现要好得多</p>
<h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><p>Node 是一个相对新的开源项目，所以不太稳定，它总是一直在变，而且缺少足够多的第三方库支持。</p>
<h4 id="容错性较差，一旦出错就整个线程挂掉"><a href="#容错性较差，一旦出错就整个线程挂掉" class="headerlink" title="容错性较差，一旦出错就整个线程挂掉"></a>容错性较差，一旦出错就整个线程挂掉</h4><p>配合pm2等包，自动重启；</p>
<h2 id="用途"><a href="#用途" class="headerlink" title="用途"></a>用途</h2><h3 id="前后端分离"><a href="#前后端分离" class="headerlink" title="前后端分离"></a>前后端分离</h3><p>模板渲染</p>
<h3 id="同构"><a href="#同构" class="headerlink" title="同构"></a>同构</h3><p>提供Javascript run time进行服务端渲染</p>
<h3 id="日志上报"><a href="#日志上报" class="headerlink" title="日志上报"></a>日志上报</h3><h2 id="Nodejs的常用场景"><a href="#Nodejs的常用场景" class="headerlink" title="Nodejs的常用场景"></a>Nodejs的常用场景</h2><h2 id="http-server与其他语言搭建的服务器对比"><a href="#http-server与其他语言搭建的服务器对比" class="headerlink" title="http server与其他语言搭建的服务器对比"></a>http server与其他语言搭建的服务器对比</h2><p>参考资料<br><a href="https://coding.imooc.com/class/146.html" target="_blank" rel="noopener">慕课网-Node.js入门到企业Web开发中的应用</a><br>阮一峰的博客</p>
]]></content>
      <categories>
        <category>后端</category>
      </categories>
      <tags>
        <tag>Nodejs</tag>
      </tags>
  </entry>
  <entry>
    <title>JS功能与原理的实现</title>
    <url>/2019/10/15/JS%E5%8A%9F%E8%83%BD%E4%B8%8E%E5%8E%9F%E7%90%86%E7%9A%84%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<h2 id="JS-原理的实现"><a href="#JS-原理的实现" class="headerlink" title="JS 原理的实现"></a>JS 原理的实现</h2><h3 id="实现一个-Promise"><a href="#实现一个-Promise" class="headerlink" title="实现一个 Promise()"></a>实现一个 Promise()</h3><p>Promise 的特点：</p>
<ul>
<li>new Promise 时需要传递一个函数 fn 作为执行器（执行器会立刻执行）</li>
<li>执行器中传递了两个参数：resolve 成功的函数、reject 失败的函数（他们调用时可以接受任何值的参数 value）</li>
<li>promise 状态只能从 pending 态转到 resolved 或者 rejected，<strong>如果状态发生改变执行相应缓存队列中的任务</strong></li>
<li>promise 实例,每个实例都有一个 then 方法，这个方法传递两个参数，一个是成功回调 onfulfilled,另一个是失败回调 onrejected</li>
<li>promise 实例调用 then 时，会判断当前状态，如果 pending，就…；如果 resolved，就让 onfulfilled 执行；如果 rejectd，就 onrejected 执行</li>
<li>promise 中可以同一个实例 then 多次,如果状态是 pengding 需要将函数存放起来 等待状态确定后 在依次将对应的函数执行 (发布订阅)</li>
</ul>
<p>基于上述特点，实现的 Promise 如下</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">MyPromise</span>(<span class="params">fn</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> that = <span class="keyword">this</span>;</span><br><span class="line">  <span class="comment">// value 变量用于保存 resolve 或者 reject 中传入的值</span></span><br><span class="line">  <span class="keyword">this</span>.value = <span class="literal">null</span>;</span><br><span class="line">  <span class="keyword">this</span>.status = <span class="string">"pending"</span>;</span><br><span class="line">  <span class="comment">// 用于保存 then 中的回调（可能有多个所以是数组），因为当执行完 Promise 时状态可能还是等待中，这时候应该把 then 中的回调保存起来用于状态改变时使用</span></span><br><span class="line">  <span class="keyword">this</span>.resolvedAry = [];</span><br><span class="line">  <span class="keyword">this</span>.rejectedAry = [];</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">resolve</span>(<span class="params">value</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 首先两个函数都得判断当前状态是否为等待中</span></span><br><span class="line">    <span class="keyword">if</span> (that.status === <span class="string">"pending"</span>) &#123;</span><br><span class="line">      that.status === <span class="string">"resolved"</span>;</span><br><span class="line">      that.value = value;</span><br><span class="line">      <span class="comment">// 遍历回调数组并执行</span></span><br><span class="line">      that.resolvedAry.map(<span class="function"><span class="params">cb</span> =&gt;</span> cb(that.value));</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">reject</span>(<span class="params">value</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (that.status === <span class="string">"pending"</span>) &#123;</span><br><span class="line">      that.status = <span class="string">"rejected"</span>;</span><br><span class="line">      that.value = value;</span><br><span class="line">      that.rejectedAry.map(<span class="function"><span class="params">cb</span> =&gt;</span> cb(that.value));</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 完成以上两个函数以后，我们就该实现如何执行 Promise 中传入的函数了</span></span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    fn(resolve, reject);</span><br><span class="line">  &#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">    reject(e);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 最后我们来实现较为复杂的 then 函数。接收两个回调函数为参数</span></span><br><span class="line">MyPromise.prototype.then = <span class="function"><span class="keyword">function</span>(<span class="params">onfulfilled, onrejected</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> that = <span class="keyword">this</span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 判断两个参数是否为函数类型</span></span><br><span class="line">  onfulfilled = <span class="keyword">typeof</span> onfulfilled === <span class="string">"function"</span> ? onfulfilled : <span class="function"><span class="params">f</span> =&gt;</span> f;</span><br><span class="line">  onrejected =</span><br><span class="line">    <span class="keyword">typeof</span> onrejected === <span class="string">"function"</span></span><br><span class="line">      ? onrejected</span><br><span class="line">      : <span class="function"><span class="params">e</span> =&gt;</span> &#123;</span><br><span class="line">          <span class="keyword">throw</span> e;</span><br><span class="line">        &#125;;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 当状态不是等待态时，就去执行相对应的函数。如果状态是等待态的话，就往回调函数中 push 函数</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">this</span>.status === <span class="string">"pending"</span>) &#123;</span><br><span class="line">    <span class="keyword">this</span>.resolvedAry.push(onfulfilled);</span><br><span class="line">    <span class="keyword">this</span>.rejectedAry.push(onrejected);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">this</span>.status === <span class="string">"resolved"</span>) &#123;</span><br><span class="line">    onfulfilled(that.value);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">this</span>.status === <span class="string">"rejected"</span>) &#123;</span><br><span class="line">    onrejected(that.value);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="实现Promise-all"><a href="#实现Promise-all" class="headerlink" title="实现Promise.all()"></a>实现Promise.all()</h3><p>Promise.all 接收一个 promise 对象的数组作为参数，当这个数组里的所有 promise 对象全部变为resolve或 有 reject 状态出现的时候，它才会去调用 .then 方法,它们是并发执行的。eg:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">var p1 = Promise.resolve(1),</span><br><span class="line">    p2 = Promise.resolve(2),</span><br><span class="line">    p3 = Promise.resolve(3); // 若任一 Promise 状态是reject，则无法执行 .then</span><br><span class="line">Promise.all([p1, p2, p3]).then(function (results) &#123;</span><br><span class="line">    console.log(results);  // [1, 2, 3]</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>总结 promise.all 的特点</p>
<ul>
<li>接收一个 Promise 实例的数组或具有 Iterator 接口的对象</li>
<li>如果元素不是 Promise 对象，则使用 Promise.resolve 转成 Promise 对象</li>
<li>如果全部成功，状态变为 resolved，返回值将组成一个数组传给回调</li>
<li>只要有一个失败，状态就变为 rejected，返回值将直接传递给回调all() 的返回值也是新的 Promise 对象</li>
</ul>
<p>实现：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function promiseAll(promises) &#123;</span><br><span class="line">  return new Promise(function(resolve, reject) &#123;</span><br><span class="line">    if (!isArray(promises)) &#123;</span><br><span class="line">      return reject(new TypeError(&apos;arguments must be an array&apos;));</span><br><span class="line">    &#125;</span><br><span class="line">    var resolvedCounter = 0;</span><br><span class="line">    var promiseNum = promises.length;</span><br><span class="line">    var resolvedValues = new Array(promiseNum);</span><br><span class="line">    for (var i = 0; i &lt; promiseNum; i++) &#123;</span><br><span class="line">      (function(i) &#123;</span><br><span class="line">        Promise.resolve(promises[i]).then(function(value) &#123;</span><br><span class="line">          resolvedCounter++</span><br><span class="line">          resolvedValues[i] = value</span><br><span class="line">          if (resolvedCounter == promiseNum) &#123;</span><br><span class="line">            return resolve(resolvedValues)</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;, function(reason) &#123;</span><br><span class="line">          return reject(reason)</span><br><span class="line">        &#125;)</span><br><span class="line">      &#125;)(i)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="手写实现-call-apply-bind"><a href="#手写实现-call-apply-bind" class="headerlink" title="手写实现 call(), apply(), bind()"></a>手写实现 call(), apply(), bind()</h3><h4 id="call"><a href="#call" class="headerlink" title="call()"></a>call()</h4><ul>
<li>首先 context 为可选参数，如果不传的话默认上下文为 window</li>
<li>接下来给 context 创建一个 fn 属性，并将值设置为需要调用的函数</li>
<li>因为 call 可以传入多个参数作为调用函数的参数，所以需要将参数剥离出来</li>
<li>然后调用函数并将对象上的函数删除</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Function</span>.prototype.myCall = <span class="function"><span class="keyword">function</span>(<span class="params">context</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> <span class="keyword">this</span> !== <span class="string">"function"</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">TypeError</span>(<span class="string">"Error"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  context = context || <span class="built_in">window</span>;</span><br><span class="line">  context.fn = <span class="keyword">this</span>;</span><br><span class="line">  <span class="keyword">const</span> args = [...arguments].slice(<span class="number">1</span>);</span><br><span class="line">  <span class="keyword">const</span> result = context.fn(...args);</span><br><span class="line">  <span class="keyword">delete</span> context.fn;</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h4 id="apply"><a href="#apply" class="headerlink" title="apply()"></a>apply()</h4><p>类似，区别在于对参数的处理</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Function</span>.prototype.myApply = <span class="function"><span class="keyword">function</span>(<span class="params">context</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> <span class="keyword">this</span> !== <span class="string">"function"</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">TypeError</span>(<span class="string">"Error"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  context = context || <span class="built_in">window</span>;</span><br><span class="line">  context.fn = <span class="keyword">this</span>;</span><br><span class="line">  <span class="keyword">let</span> result;</span><br><span class="line">  <span class="comment">// 处理参数和 call 有区别</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">arguments</span>[<span class="number">1</span>]) &#123;</span><br><span class="line">    result = context.fn(...arguments[<span class="number">1</span>]);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    result = context.fn();</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">delete</span> context.fn;</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h4 id="bind"><a href="#bind" class="headerlink" title="bind()"></a>bind()</h4><p>bind 返回了一个函数，对于函数来说有两种方式调用，一种是直接调用，一种是通过 new 的方式</p>
<ul>
<li>对于直接调用来说，这里选择了 apply 的方式实现，但是对于参数需要注意以下情况：因为 bind 可以实现类似这样的代码 <code>f.bind(obj, 1)(2)</code>，所以我们需要将两边的参数拼接起来，于是就有了这样的实现 <code>args.concat(...arguments)</code></li>
<li>通过 new 的方式，在之前的章节中我们学习过如何判断 this，对于 new 的情况来说，不会被任何方式改变 this，所以对于这种情况我们需要忽略传入的 this。（似乎这种 new 的用法很少见）</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Function</span>.prototype.myBind = <span class="function"><span class="keyword">function</span>(<span class="params">context</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> <span class="keyword">this</span> !== <span class="string">"function"</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">TypeError</span>(<span class="string">"Error"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">const</span> _this = <span class="keyword">this</span>;</span><br><span class="line">  <span class="keyword">const</span> args = [...arguments].slice(<span class="number">1</span>);</span><br><span class="line">  <span class="comment">// 返回一个函数</span></span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> <span class="title">F</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 因为返回了一个函数，我们可以 new F()，所以需要判断</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span> <span class="keyword">instanceof</span> F) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> _this(...args, ...arguments);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 直接调用的情况</span></span><br><span class="line">    <span class="keyword">return</span> _this.apply(context, args.concat(...arguments));</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="instanceof"><a href="#instanceof" class="headerlink" title="instanceof"></a>instanceof</h3><p>instanceof 可以正确的判断对象的类型，其内部机制是通过判断对象的原型链中是不是能找到类型的 prototype。实现原理如下：</p>
<ul>
<li>首先获取类型的原型</li>
<li>然后获得对象的原型</li>
<li>然后一直循环判断对象的原型是否等于类型的原型，直到对象原型为 null，因为原型链最终为 null</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">muInstanceof</span>(<span class="params">left, right</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> prototype = right.prototype;</span><br><span class="line">  left = left.__proto__;</span><br><span class="line">  <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (left === <span class="literal">null</span> || left === <span class="literal">undefined</span>) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">if</span> (prototype === left) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    left = left.__proto__;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="手写实现-new"><a href="#手写实现-new" class="headerlink" title="手写实现 new"></a>手写实现 new</h3><p>在调用 new 的过程中会发生四件事情:</p>
<ul>
<li>新生成了一个对象</li>
<li>链接到原型</li>
<li>绑定 this(使用构造函数的 this)</li>
<li>返回新对象（原始类型的话忽略，如果是引用类型的话就返回这个对象）</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Function</span>.prototype.myNew() = <span class="function"><span class="keyword">function</span>(<span class="params">func, ...args</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// func是new后面的那个构造函数</span></span><br><span class="line">  <span class="keyword">let</span> obj = &#123;&#125;;</span><br><span class="line">  obj._proto_ = func.prototype;</span><br><span class="line">  <span class="keyword">let</span> result = func.apply(obj, args);</span><br><span class="line">  <span class="keyword">return</span> result <span class="keyword">instanceof</span> <span class="built_in">Object</span> ? result : obj;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="手写实现-Object-create"><a href="#手写实现-Object-create" class="headerlink" title="手写实现 Object.create()"></a>手写实现 Object.create()</h3><p>Object.create()方法创建一个新对象，使用参数对象（而不是参数对象.prtototype）来提供新创建的对象的<code>__proto__</code></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Object</span>.prototype.mycreate = <span class="function"><span class="keyword">function</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">F</span>(<span class="params"></span>) </span>&#123;&#125;</span><br><span class="line">  F.prototype = obj;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> F();</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="0-1-0-2-0-3"><a href="#0-1-0-2-0-3" class="headerlink" title="0.1 +0.2 !== 0.3"></a>0.1 +0.2 !== 0.3</h3><p>对于纯小数来说，十进制的 0.375 会被存储为： 0.011 其代表 (1/2)^2 + (1/2)^3 = 1/4 + 1/8 = 0.375<br>但是对 0.1 这种数值来说，算下来是 0.000110011… 由于存储空间有限，最后计算机会舍弃后面的数值，所以我们最后就只能得到一个近似值。JS 采用 IEEE 754 双精度版本（64 位）浮点数标准，也是只能取得一个近似值。除了那些能表示成 (x/2)^n 的数可以被精确表示以外，其余小数都是以近似值得方式存在的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="number">0.1000000000000001</span>);</span><br><span class="line"><span class="comment">// 0.1000000000000001 (中间14个0，会打印出它本身)</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">0.10000000000000001</span>);</span><br><span class="line"><span class="comment">// 0.1 (中间15个0，js会认为这两个值足够接近，所以会显示0.1)</span></span><br></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="number">0.100000000000000002</span> === <span class="number">0.1</span>; <span class="comment">// true （16个0）</span></span><br><span class="line"><span class="number">0.200000000000000002</span> === <span class="number">0.2</span>; <span class="comment">// true</span></span><br><span class="line"><span class="number">0.1</span> + <span class="number">0.2</span> === <span class="number">0.30000000000000004</span>; <span class="comment">// true。此时对于JS来说，其不够近似于0.3，于是就出现了0.1 + 0.2 != 0.3 这个现象</span></span><br></pre></td></tr></table></figure>

<p><strong>解决：</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">parseFloat</span>((<span class="number">0.1</span> + <span class="number">0.2</span>).toFixed(<span class="number">10</span>)) === <span class="number">0.3</span>; <span class="comment">// true  toFixed()将数值格式化为字符串</span></span><br></pre></td></tr></table></figure>

<h3 id="双向数据绑定实现原理"><a href="#双向数据绑定实现原理" class="headerlink" title="双向数据绑定实现原理"></a>双向数据绑定实现原理</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//双向数据绑定</span></span><br><span class="line"><span class="keyword">let</span> obj = &#123;&#125;;</span><br><span class="line"><span class="keyword">let</span> input = <span class="built_in">document</span>.getElementById(<span class="string">"input"</span>);</span><br><span class="line"><span class="keyword">let</span> span = <span class="built_in">document</span>.getElementById(<span class="string">"span"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">//数据劫持</span></span><br><span class="line"><span class="built_in">Object</span>.defineProperty(obj, <span class="string">"text"</span>, &#123;</span><br><span class="line">  configurable: <span class="literal">true</span>,</span><br><span class="line">  enumerable: <span class="literal">true</span>,</span><br><span class="line">  <span class="keyword">get</span>() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"获取数据"</span>);</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="keyword">set</span>(newVal) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"数据更新"</span>);</span><br><span class="line">    input.value = newVal; <span class="comment">// 要双向绑定所以有这句</span></span><br><span class="line">    span.innerHTML = newVal;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">//监听</span></span><br><span class="line">input.addEventListener(<span class="string">"keyup"</span>, <span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">  obj.text = e.target.value;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="JS-功能的实现"><a href="#JS-功能的实现" class="headerlink" title="JS 功能的实现"></a>JS 功能的实现</h2><h3 id="数组去重"><a href="#数组去重" class="headerlink" title="数组去重"></a>数组去重</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [<span class="number">1</span>, <span class="number">5</span>, <span class="number">8</span>, <span class="number">3</span>, <span class="number">7</span>, <span class="number">5</span>, <span class="number">1</span>, <span class="number">8</span>, <span class="number">100</span>];</span><br></pre></td></tr></table></figure>

<ul>
<li>sort() + reduce</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> result = arr.sort().reduce(<span class="function">(<span class="params">acc, curr</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (acc.length === <span class="number">0</span> || curr !== acc[acc.length - <span class="number">1</span>]) &#123;</span><br><span class="line">    acc.push(curr);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> acc;</span><br><span class="line">&#125;, []);</span><br></pre></td></tr></table></figure>

<ul>
<li>Array.from(new Set(arr))</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> result = <span class="built_in">Array</span>.from(<span class="keyword">new</span> <span class="built_in">Set</span>(arr));</span><br></pre></td></tr></table></figure>

<ul>
<li>for…of + Object (性能最优）。利用对象的属性不会重复这一特性，校验数组元素是否重复</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> result = [];</span><br><span class="line">obj = &#123;&#125;;</span><br><span class="line"><span class="keyword">for</span> (i <span class="keyword">of</span> arr) &#123;</span><br><span class="line">  <span class="keyword">if</span> (!obj[i]) &#123;</span><br><span class="line">    result.push(i);</span><br><span class="line">    obj[i] = <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="数组扁平化"><a href="#数组扁平化" class="headerlink" title="数组扁平化"></a>数组扁平化</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = (arr = [<span class="number">1</span>, [<span class="number">2</span>, [<span class="number">3</span>, [<span class="number">4</span>, <span class="number">5</span>, , <span class="string">"tset"</span>]]], <span class="number">6</span>]);</span><br></pre></td></tr></table></figure>

<ul>
<li>Array.prototype.flat()。参数是要提取的嵌套数组的深度（数字），默认值是 1；也可以直接写 Infinity 作为参数。但是会移除数组中的空项</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> result = arr.flat(<span class="literal">Infinity</span>); <span class="comment">// [1, 2, 3, 4, 5, "tset", 6]</span></span><br></pre></td></tr></table></figure>

<ul>
<li>reduce 写一个递归函数。似乎也会移除空项</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">myFlat</span>(<span class="params">arr</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> arr.reduce(</span><br><span class="line">    (acc, curr) =&gt; acc.concat(<span class="built_in">Array</span>.isArray(curr) ? myFlat(curr) : curr),</span><br><span class="line">    []</span><br><span class="line">  );</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// [1, 2, 3, 4, 5, "tset", 6]</span></span><br></pre></td></tr></table></figure>

<ul>
<li>扩展运算符 + Array.prototype.some() 这里的 isArray 用法稍微有点特殊。并且空项不会被移除，会保留为<code>undefined</code></li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (arr.some(<span class="built_in">Array</span>.isArray)) &#123;</span><br><span class="line">  arr = [].concat(...arr);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="函数的-arguement-怎么转数组"><a href="#函数的-arguement-怎么转数组" class="headerlink" title="函数的 arguement 怎么转数组"></a>函数的 arguement 怎么转数组</h3><p>Array.from(), 扩展运算符</p>
<h3 id="for…in-for…of-forEach"><a href="#for…in-for…of-forEach" class="headerlink" title="for…in, for…of, forEach()"></a>for…in, for…of, forEach()</h3><h4 id="for…in"><a href="#for…in" class="headerlink" title="for…in"></a>for…in</h4><p>一般用来遍历对象。有几个缺点：</p>
<ul>
<li>for…in 会遍历手动添加的原型链上的键。比如你自己写个<code>Object.prototype.test = function { xxx }</code>，是能遍历到这个<code>test</code>属性的</li>
<li>用来遍历数组的时候是遍历数组的键名（index），并且这个 index 是字符串</li>
<li>某些情况下，for…in 循环会以任意顺序遍历键名</li>
</ul>
<h4 id="for…of"><a href="#for…of" class="headerlink" title="for…of"></a>for…of</h4><ul>
<li>遍历数组得到数组的 value</li>
<li><code>for (let [k, v] of Object.entries(obj)) { }</code> 用来遍历对象，能获取对象的键和键值</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> obj = &#123;</span><br><span class="line">  name: <span class="string">"jacle"</span>,</span><br><span class="line">  age: <span class="number">22</span>,</span><br><span class="line">  job: <span class="string">"student"</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">let</span> arr = [<span class="string">"jacle"</span>, <span class="number">22</span>, <span class="string">"student"</span>];</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> [key, value] <span class="keyword">of</span> <span class="built_in">Object</span>.entries(obj)) &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(key, <span class="string">":"</span>, value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="forEach"><a href="#forEach" class="headerlink" title="forEach()"></a>forEach()</h4><p>遍历数组，问题是无法用 break，return 跳出循环<br>如何中断 forEach 循环：</p>
<ul>
<li>使用 try 监视代码块，在需要中断的地方抛出异常</li>
<li>官方推荐方法（替换方法）：用 every 和 some 替代 forEach 函数。every 在碰到 return false 的时候，中止循环。some 在碰到 return ture 的时候，中止循环</li>
</ul>
<h3 id="数组中是否包含某个值"><a href="#数组中是否包含某个值" class="headerlink" title="数组中是否包含某个值"></a>数组中是否包含某个值</h3><ul>
<li><code>Array.prototype.indexOf(值)</code>。返回 index 或 -1 。该方法也可用于 String</li>
<li><code>Array.prototype.includes(值)</code>。返回布尔值。该方法也可用于 String</li>
<li><code>Array.prototype.find(callback[,thisVal])</code>。返回数组中满足条件的<strong>第一个元素的值</strong>，如果没有，返回 undefined</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>];</span><br><span class="line"><span class="keyword">let</span> result = arr.find(<span class="function"><span class="params">item</span> =&gt;</span> item &gt; <span class="number">100</span>); <span class="comment">// 4</span></span><br></pre></td></tr></table></figure>

<ul>
<li><code>Array.prototype.findeIndex(callback[,thisArg])</code>。返回数组中满足条件的第一个元素的下标，如果没有找到，返回-1]</li>
</ul>
<h3 id="对象按规则排序"><a href="#对象按规则排序" class="headerlink" title="对象按规则排序"></a>对象按规则排序</h3><p>有一个数组，里面都是对象，现在要针对对象中的某一个 key 进行排序，顺序是已给定的数组。<br>比如原数组为<code>[{a:&#39;ww&#39;},{a:&#39;ff&#39;},{a:&#39;pe&#39;}]</code>，<br>顺序是<code>[{ww:1},{pe:3},{hf:2},{oo:4},{ff:5}]</code><br>那么输出是 <code>[{a:&#39;ww&#39;},{a:&#39;pe&#39;},{a:&#39;ff&#39;}]</code></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> objarr = [&#123; <span class="attr">a</span>: <span class="string">"ww"</span> &#125;, &#123; <span class="attr">a</span>: <span class="string">"ff"</span> &#125;, &#123; <span class="attr">a</span>: <span class="string">"pe"</span> &#125;];</span><br><span class="line"><span class="keyword">var</span> rulearr = [&#123; <span class="attr">ww</span>: <span class="number">1</span> &#125;, &#123; <span class="attr">pe</span>: <span class="number">3</span> &#125;, &#123; <span class="attr">hf</span>: <span class="number">2</span> &#125;, &#123; <span class="attr">oo</span>: <span class="number">4</span> &#125;, &#123; <span class="attr">ff</span>: <span class="number">5</span> &#125;];</span><br><span class="line"></span><br><span class="line"><span class="comment">//暴力解决</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">sortByRule</span>(<span class="params">objarr, key, rulearr</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> ResultArr = [];</span><br><span class="line">  rulearr.forEach(<span class="function"><span class="params">item</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">//获取当前的value和规则的位次</span></span><br><span class="line">    <span class="keyword">let</span> value = <span class="built_in">Object</span>.getOwnPropertyNames(item)[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">let</span> order = item[value];</span><br><span class="line">    <span class="comment">//找到对应的obj放入对应位次的位置</span></span><br><span class="line">    ResultArr[order] = objarr.find(<span class="function"><span class="params">item</span> =&gt;</span> item[key] === value);</span><br><span class="line">  &#125;);</span><br><span class="line">  <span class="comment">//去掉那些为空的</span></span><br><span class="line">  <span class="keyword">return</span> ResultArr.filter(<span class="function"><span class="params">item</span> =&gt;</span> item);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="n-到-m-范围的随机整数数"><a href="#n-到-m-范围的随机整数数" class="headerlink" title="n 到 m 范围的随机整数数"></a>n 到 m 范围的随机整数数</h3><ul>
<li>Math.random()生成[0, 1)的数，所以</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Math</span>.random() * m; <span class="comment">// 生成&#123;0,m)的数；</span></span><br></pre></td></tr></table></figure>

<ul>
<li>要整数有向下取整 Math.floor()，向上 Math.ceil()。所以[1, m]的数是</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Math</span>.ceil(<span class="built_in">Math</span>.random() * m); <span class="comment">// [1, m]的数</span></span><br><span class="line"><span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * m) + <span class="number">1</span>; <span class="comment">// [1, m]的数</span></span><br><span class="line"><span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * m); <span class="comment">// [0, m-1]的数</span></span><br></pre></td></tr></table></figure>

<ul>
<li>所以希望生成[n, m]的随机数</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * (max - min + <span class="number">1</span>) + min);</span><br></pre></td></tr></table></figure>

<h3 id="深拷贝与浅拷贝的实现"><a href="#深拷贝与浅拷贝的实现" class="headerlink" title="深拷贝与浅拷贝的实现"></a>深拷贝与浅拷贝的实现</h3><h4 id="赋值（-），浅拷贝与深拷贝的区别"><a href="#赋值（-），浅拷贝与深拷贝的区别" class="headerlink" title="赋值（=），浅拷贝与深拷贝的区别"></a>赋值（=），浅拷贝与深拷贝的区别</h4><p><img src="/img/JS%E5%92%8CES6/%E6%B7%B1%E6%B5%85%E6%8B%B7%E8%B4%9D.png">  </p>
<h4 id="浅拷贝"><a href="#浅拷贝" class="headerlink" title="浅拷贝"></a>浅拷贝</h4><p>浅拷贝只复制一层对象的属性，并不包括对象里面的为引用类型的数据</p>
<ul>
<li>Object.assign 只会拷贝所有的属性值到新的对象中，如果属性值是对象的话，拷贝的是地址，是浅拷贝</li>
</ul>
<p>Object.assign() 方法用于将所有可枚举属性的值从一个或多个源对象复制到目标对象。它将返回目标对象。Object.assign(target, source)。直接用 Object.assign(target, source) 这种用法就能更新target对象。当然也能用来赋值新对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;</span><br><span class="line">  age: <span class="number">1</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">let</span> b = <span class="built_in">Object</span>.assign(&#123;&#125;, a);</span><br><span class="line">a.age = <span class="number">2</span>;</span><br><span class="line"><span class="built_in">console</span>.log(b.age); <span class="comment">// 1</span></span><br></pre></td></tr></table></figure>

<ul>
<li>展开运算符 …</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;</span><br><span class="line">  age: <span class="number">1</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">let</span> b = &#123; ...a &#125;;</span><br><span class="line">a.age = <span class="number">2</span>;</span><br><span class="line"><span class="built_in">console</span>.log(b.age); <span class="comment">// 1</span></span><br></pre></td></tr></table></figure>

<ul>
<li>数据的浅拷贝还有 <code>let arr2 = [].concat(arr1)</code> 和 <code>let arr2 = arr1.slice()</code><h4 id="深拷贝"><a href="#深拷贝" class="headerlink" title="深拷贝"></a>深拷贝</h4></li>
</ul>
<p>对对象以及对象的所有子对象进行拷贝<br>通过 JSON.parse(JSON.stringify(object))来实现</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a = &#123;</span><br><span class="line">  age: <span class="number">1</span>,</span><br><span class="line">  jobs: &#123;</span><br><span class="line">    first: <span class="string">"FE"</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">let</span> b = <span class="built_in">JSON</span>.parse(<span class="built_in">JSON</span>.stringify(a));</span><br><span class="line">a.jobs.first = <span class="string">"native"</span>;</span><br><span class="line"><span class="built_in">console</span>.log(b.jobs.first); <span class="comment">// FE</span></span><br></pre></td></tr></table></figure>

<p>局限性 ：会忽略 undefined；会忽略 symbol；不能序列化函数；不能解决循环引用的对象<br>自己实现一个深拷贝的函数：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">deepClone</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 判断传进来的obj是否是引用类型</span></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">isObj</span>(<span class="params">o</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="keyword">typeof</span> o === <span class="string">"object"</span> || <span class="keyword">typeof</span> o === <span class="string">"function"</span>) &amp; (o !== <span class="literal">null</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 判断传进来的obj这个引用类型是对象还是数组，按正确的方式浅拷贝一次</span></span><br><span class="line">  <span class="keyword">let</span> newobj = <span class="built_in">Array</span>.isArray(obj) ? [...obj] : &#123; ...obj &#125;;</span><br><span class="line">  <span class="comment">//  对新对象的key遍历，原来obj的key是基本类型还是引用类型，基本类型就直接复制，引用类型就深拷贝一次</span></span><br><span class="line">  <span class="built_in">Reflect</span>.ownKeys(newobj).forEach(<span class="function"><span class="params">key</span> =&gt;</span> &#123;</span><br><span class="line">    newobj[key] = isObj(obj[key]) ? deepClone(obj[key]) : obj[key];</span><br><span class="line">  &#125;);</span><br><span class="line">  <span class="keyword">return</span> newobj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="时间戳的获取"><a href="#时间戳的获取" class="headerlink" title="时间戳的获取"></a>时间戳的获取</h4><p>时间戳是指格林威治时间1970年01月01日00时00分00秒起至当下的总秒数。JS中拿到时间戳：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">第一种方法：</span><br><span class="line">var timestamp = Date.parse(new Date());</span><br><span class="line"></span><br><span class="line">第二种方法：</span><br><span class="line">var timestamp = (new Date()).valueOf();</span><br><span class="line"></span><br><span class="line">第三种方法：</span><br><span class="line">var timestamp = new Date().getTime()；</span><br><span class="line"> </span><br><span class="line">第四种方法：</span><br><span class="line">var timestamp = Date.now();</span><br></pre></td></tr></table></figure>

<p>参考资料<br><a href="https://segmentfault.com/a/1190000010765655" target="_blank" rel="noopener">实现 JavaScript 异步方法 Promise.all</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JS的事件循环和Node.js的事件循环</title>
    <url>/2019/11/03/JS%E7%9A%84%E4%BA%8B%E4%BB%B6%E5%BE%AA%E7%8E%AF%E5%92%8CNode.js%E7%9A%84%E4%BA%8B%E4%BB%B6%E5%BE%AA%E7%8E%AF/</url>
    <content><![CDATA[<h1 id="JS-的事件循环"><a href="#JS-的事件循环" class="headerlink" title="JS 的事件循环"></a>JS 的事件循环</h1><h2 id="宿主环境"><a href="#宿主环境" class="headerlink" title="宿主环境"></a>宿主环境</h2><p>JS 运行的环境称之为宿主环境，eg. 浏览器，服务器，桌面等</p>
<h2 id="执行环境栈"><a href="#执行环境栈" class="headerlink" title="执行环境栈"></a>执行环境栈</h2><p>放执行环境的地方，栈尾就是活动的执行环境</p>
<h2 id="浏览器内核"><a href="#浏览器内核" class="headerlink" title="浏览器内核"></a>浏览器内核</h2><p>浏览器内核是多线程的：GUI 渲染线程、JavaScript 引擎线程、定时触发器线程、事件触发线程、异步 http 请求线程</p>
<ol>
<li>渲染引擎和 JS 引擎是互斥的，所以渲染引擎在解析 DOM 的时候遇到 srcipt 会被暂停，阻塞渲染</li>
<li>主线程依次执行代码时，遇到定时器，会将定时器交给<strong>定时器触发线程</strong>处理，当计数完毕后，<strong>事件触发线程</strong>会将计数完毕后的事件加入到任务队列的尾部，等待 JS 引擎线程执行</li>
<li>事件触发线程遇到事件，eg. setTimeout 计时结束，Ajax 异步请求成功触发回调，该线程会将对应的事件处理程序加入任务队列的尾部，等待 JS 引擎的执行</li>
<li>http 请求线程处理 fetch，ajax，axios 等，也是主线程遇到异步请求就交给该线程处理，当监听到状态码变更，就把回调函数给<strong>事件线程</strong>，事件线程把回调函数加进任队列尾部</li>
</ol>
<h2 id="Event-Loop"><a href="#Event-Loop" class="headerlink" title="Event Loop"></a>Event Loop</h2><blockquote>
<p>JS 引擎对任务队列的取出执行方式，以及与宿主环境的配合，称之为<strong>事件循环</strong>（Event Loop）</p>
</blockquote>
<p>当上面的线程发生了某些事，事件循环发现这些事有对应的处理程序（eg. 异步代码，延时器），事件循环会把处理程序挂起并放进<strong>任务队列</strong>中。一旦执行栈为空，事件循环就会从事件队列队列中拿出需要执行的代码并放入执行栈中执行，所以本质上来说 JS 中的异步还是同步行为。</p>
<h3 id="微任务与宏任务"><a href="#微任务与宏任务" class="headerlink" title="微任务与宏任务"></a>微任务与宏任务</h3><blockquote>
<p>不同的任务源会被分配到不同的事件队列中，任务源可以分为 微任务（microtask） 和 宏任务（macrotask）。<strong>宏任务队列可以有多个，微任务队列只有一个</strong></p>
</blockquote>
<p><strong>微任务:</strong></p>
<ul>
<li>process.nextTick</li>
<li>promise.prototype.then</li>
<li>Object.observe</li>
<li>MutationObserver</li>
</ul>
<p><strong>宏任务:</strong></p>
<ul>
<li>script</li>
<li>setTimeout(比 setImmediate 前)</li>
<li>setInterval</li>
<li>setImmediate</li>
<li>I/O</li>
<li>UI rendering</li>
</ul>
<p>所以正确的一次 Event loop 顺序是这样的:</p>
<ul>
<li>执行同步代码，这属于宏任务</li>
<li>执行栈为空，查询是否有微任务需要执行</li>
<li>执行所有微任务</li>
<li>必要的话渲染 UI</li>
<li>然后开始下一轮 Event loop，执行宏任务中的异步代码</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="string">'script start'</span>)</span><br><span class="line"></span><br><span class="line">setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'setTimeout'</span>)</span><br><span class="line">&#125;, <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="params">resolve</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'Promise'</span>)</span><br><span class="line">  resolve()</span><br><span class="line">&#125;)</span><br><span class="line">  .then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'promise1'</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">  .then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'promise2'</span>)</span><br><span class="line">  &#125;)</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'script end'</span>)</span><br><span class="line"><span class="comment">// script start =&gt; Promise =&gt; script end =&gt; promise1 =&gt; promise2 =&gt; setTimeout</span></span><br></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="string">'script start'</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">async1</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">await</span> async2()</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'async1 end'</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">async2</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'async2 end'</span>)</span><br><span class="line">&#125;</span><br><span class="line">async1()</span><br><span class="line"></span><br><span class="line">setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'setTimeout'</span>)</span><br><span class="line">&#125;, <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="params">resolve</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'Promise'</span>)</span><br><span class="line">  resolve()</span><br><span class="line">&#125;)</span><br><span class="line">  .then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'promise1'</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">  .then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'promise2'</span>)</span><br><span class="line">  &#125;)</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'script end'</span>)</span><br><span class="line"><span class="comment">// 把async理解为promise的另一种写法即可</span></span><br><span class="line"><span class="comment">// script start =&gt; async2 end =&gt; Promise =&gt; script end =&gt;async1 end =&gt; promise1 =&gt; promise2 =&gt; setTimeout</span></span><br></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">Promise</span>.resolve().then(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'Promise1'</span>)</span><br><span class="line">  setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'setTimeout2'</span>)</span><br><span class="line">  &#125;, <span class="number">0</span>)</span><br><span class="line">&#125;)</span><br><span class="line">setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'setTimeout1'</span>)</span><br><span class="line">  <span class="built_in">Promise</span>.resolve().then(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'Promise2'</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;, <span class="number">0</span>)</span><br><span class="line"><span class="comment">// Promise1 =&gt; setTimeout1 =&gt; Promise2 =&gt; setTimeout2</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>一道腾讯PCG的面试题。同理把 async 理解为 Promise 的写法即可，这样 async1 函数中 的 async2 其实就是Promise中的同步部分，await后的部分就是 .then</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">async1</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'async1 start'</span>);</span><br><span class="line">  <span class="keyword">await</span> async2();</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'async1 end'</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">async2</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'async2'</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'script start'</span>);</span><br><span class="line"></span><br><span class="line">setTimeout(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'setTimeout'</span>);</span><br><span class="line">&#125;, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">async1();</span><br><span class="line"></span><br><span class="line"><span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'promise1'</span>);</span><br><span class="line">  resolve();</span><br><span class="line">&#125;).then(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'promise2'</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'script end'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// script start - async1 start - async2 - promise1 - script end - async1 end - promise2 - setTimeout</span></span><br></pre></td></tr></table></figure>

<h1 id="Node-js-的事件循环"><a href="#Node-js-的事件循环" class="headerlink" title="Node.js 的事件循环"></a>Node.js 的事件循环</h1><p>在 Node.js 中，Event Loop 是一个消息线程。和 JS 的 Event Loop 是完！全！不！同！的！想起昨天我面试时的回答，我一直以为它们两个是一个机制，顿时觉得凉凉…</p>
<h2 id="Node-js"><a href="#Node-js" class="headerlink" title="Node.js"></a>Node.js</h2><p>Node.js 采用 V8 作为 js 的解析引擎，而 I/O 处理方面使用了自己设计的 libuv，<strong>libuv</strong> 是一个基于事件驱动的跨平台抽象层，封装了不同操作系统一些底层特性，对外提供统一的 API，事件循环机制也是它里面的实现<br><img src="/img/EventLoop/Node.png"></p>
<h2 id="六个阶段"><a href="#六个阶段" class="headerlink" title="六个阶段"></a>六个阶段</h2><p>libuv 引擎中的事件循环分为 6 个阶段，它们会按照顺序反复运行。每当进入某一个阶段的时候，都会从对应的回调队列中取出函数去执行<br><img src="/img/EventLoop/%E9%98%B6%E6%AE%B5.png"><br>所以 node 中事件循环的顺序是：<br>外部输入数据–&gt;轮询阶段(poll)–&gt;检查阶段(check)–&gt;关闭事件回调阶段(close callback)–&gt;定时器检测阶段(timer)–&gt;I/O 事件回调阶段(I/O callbacks)–&gt;闲置阶段(idle, prepare)–&gt;轮询阶段（按照该顺序反复运行）…</p>
<p><strong>我目前是理解为 node 把宏任务分类了，分为了 timer 队列， IO 队列，setImmediate 队列…这些不同的宏任务队列在 node 的不同阶段执行</strong></p>
<ul>
<li><strong>timers 阶段</strong>：这个阶段执行 timer（setTimeout、setInterval）<strong>的回调</strong>，即<strong>执行完</strong>timer 队列中的任务</li>
<li><strong>I/O callbacks 阶段</strong>：处理一些上一轮循环中的少数未执行的 I/O 回调</li>
<li>idle, prepare 阶段：仅 node 内部使用</li>
<li>poll 阶段：获取新的 I/O 事件, 适当的条件下 node 将阻塞在这里</li>
<li>check 阶段：执行 <strong>setImmediate()</strong> 的回调</li>
<li>close callbacks 阶段：执行 socket 的 close 事件回调</li>
</ul>
<p>Node 中 process.nextTick 是独立于 Event Loop 之外的，它有一个自己的队列，当每个阶段完成后，如果存在 nextTick 队列，就会执行，并且优<strong>先于其他 microtask 执行</strong>（可以理解为优先度最高的微任务？）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'timer1'</span>)</span><br><span class="line">  <span class="built_in">Promise</span>.resolve().then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'promise1'</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;, <span class="number">0</span>)</span><br><span class="line">process.nextTick(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'nextTick'</span>)</span><br><span class="line">  process.nextTick(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'nextTick'</span>)</span><br><span class="line">    process.nextTick(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">'nextTick'</span>)</span><br><span class="line">      process.nextTick(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">'nextTick'</span>)</span><br><span class="line">      &#125;)</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="comment">// nextTick=&gt;nextTick=&gt;nextTick=&gt;nextTick=&gt;timer1=&gt;promise1</span></span><br></pre></td></tr></table></figure>

<h1 id="两种事件循环的区别"><a href="#两种事件循环的区别" class="headerlink" title="两种事件循环的区别"></a>两种事件循环的区别</h1><p>浏览器环境下，<strong>microtask 的任务队列是每个 macrotask 执行完之后执行</strong>。而在 Node.js 中，microtask <strong>会在事件循环的各个阶段之间执行</strong>，也就是一类宏任务都执行完，再去执行 microtask 队列的任务<br>我现在理解为浏览器中是执行完<strong>一个</strong>宏任务再执行微任务，node是执行完<strong>一类</strong>宏任务再执行微任务</p>
<p><img src="/img/EventLoop/%E5%B7%AE%E5%BC%82.png"><br>我们看一个例子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'timer1'</span>)</span><br><span class="line">  <span class="built_in">Promise</span>.resolve().then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'promise1'</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;, <span class="number">0</span>)</span><br><span class="line">setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'timer2'</span>)</span><br><span class="line">  <span class="built_in">Promise</span>.resolve().then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'promise2'</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;, <span class="number">0</span>)</span><br></pre></td></tr></table></figure>

<p>浏览器端运行结果：<code>timer1=&gt;promise1=&gt;timer2=&gt;promise2</code> 这个不用过多解释，常规思路<br><img src="/img/EventLoop/%E6%B5%8F%E8%A7%88%E5%99%A8.gif"><br>Node 端运行结果：<code>timer1=&gt;timer2=&gt;promise1=&gt;promise2</code><br><img src="/img/EventLoop/Nodejs.gif"></p>
<p>参考文章<br><a href="https://blog.csdn.net/Fundebug/article/details/86487117" target="_blank" rel="noopener">浏览器与 Node 的事件循环(Event Loop)有何区别?</a><br><a href="http://www.ruanyifeng.com/blog/2014/10/event-loop.html" target="_blank" rel="noopener">JavaScript 运行机制详解：再谈 Event Loop</a><br>掘金小册-前端面试之道</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>React基本原理 &amp; 一些特性 &amp; 性能优化</title>
    <url>/2020/02/04/React%E5%B0%8F%E7%BB%93/</url>
    <content><![CDATA[<h1 id="基本原理"><a href="#基本原理" class="headerlink" title="基本原理"></a>基本原理</h1><h2 id="Virtual-Dom"><a href="#Virtual-Dom" class="headerlink" title="Virtual Dom"></a>Virtual Dom</h2><p>虚拟 DOM 本质上是 JavaScript 对象,是对真实 DOM 的抽象，状态变更时，通过 diff 算法计算出记录新树和旧树的差异，最后把差异更新到真正的 dom 中</p>
<h3 id="虚拟-DOM-的实现"><a href="#虚拟-DOM-的实现" class="headerlink" title="虚拟 DOM 的实现"></a>虚拟 DOM 的实现</h3><p>相较于 DOM 来说，<strong>操作 JS 对象会快很多</strong>，并且我们也可以通过 JS 来模拟 DOM</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> ul = &#123;</span><br><span class="line">  tag: <span class="string">'ul'</span>,</span><br><span class="line">  props: &#123;</span><br><span class="line">    class: 'list'</span><br><span class="line">  &#125;,</span><br><span class="line">  children: &#123;</span><br><span class="line">    tag: <span class="string">'li'</span>,</span><br><span class="line">    children: <span class="string">'1'</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上述代码对应的 DOM 就是</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;ul <span class="class"><span class="keyword">class</span></span>=<span class="string">"list"</span>&gt;</span><br><span class="line">  &lt;li&gt;<span class="number">1</span>&lt;<span class="regexp">/li&gt;</span></span><br><span class="line"><span class="regexp">&lt;/u</span>l&gt;</span><br></pre></td></tr></table></figure>

<p>那么既然 DOM 可以通过 <strong>JS 对象来模拟</strong>，反之也可以<strong>通过 JS 对象来渲染出对应的 DOM</strong>。难点在于如何<strong>判断新旧两个 JS 对象的最小差异</strong>并且<strong>实现局部更新</strong> DOM。这就需要 Diff 算法了</p>
<h3 id="虚拟-DOM-真的能提升性能吗？"><a href="#虚拟-DOM-真的能提升性能吗？" class="headerlink" title="虚拟 DOM 真的能提升性能吗？"></a>虚拟 DOM 真的能提升性能吗？</h3><p>使用虚拟 DOM，<strong>在 DOM 阶段操作少了通讯的确是变高效了，但代价是在 JS 阶段需要完成额外的工作</strong>（diff 计算），这项额外的工作是需要耗时的！<br>虚拟 DOM<strong>并不是说比原生 DOM API 的操作快，而是说不管数据怎么变化，都可以以最小的代价来进行更新 DOM</strong>。在每个点上，其实用手工的原生方法会比 diff 好很多。比如说仅仅是修改了一个属性，需要整体重绘吗？显然这不是虚拟 DOM 提出来的意义。框架的意义在于掩盖底层的 DOM 操作，用更声明式的方式来描述，从而让代码更容易维护。</p>
<h2 id="diff-算法"><a href="#diff-算法" class="headerlink" title="diff 算法"></a>diff 算法</h2><p><a href="https://zhuanlan.zhihu.com/p/20346379" target="_blank" rel="noopener">详解</a><br>首先 DOM 是一个多叉树的结构，如果需要完整的对比两颗树的差异，那么需要的时间复杂度会是 O(n3)。React 团队优化了算法，实现了 O(n) 的复杂度来对比差异。 实现 O(n) 复杂度的关键就是<strong>只对比同层的节点，而不是跨层对比</strong>，这也是考虑到在实际业务中很少会去跨层的移动 DOM 元素。 所以判断差异的算法就分为了两步</p>
<ul>
<li>首先从上至下，从左往右遍历对象，也就是树的深度遍历，这一步中会给每个节点添加索引 ，便于最后渲染差异</li>
<li>一旦节点有子元素，就去判断子元素是否有不同</li>
</ul>
<p>在第一步算法中，需要判断新旧节点的 tagName 是否相同，如果不相同的话就代表节点被替换了。如果没有更改 tagName 的话，就需要判断是否有子元素，有的话就进行第二步算法。<br>在第二步算法中，需要判断原本的列表中是否有节点被移除，在新的列表中需要判断是否有新的节点加入，还需要判断节点是否有移动。<br>举个例子来说，假设页面中只有一个列表，我们对列表中的元素进行了变更</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 假设这里模拟一个 ul，其中包含了 5 个 li</span></span><br><span class="line">;[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>][</span><br><span class="line">  <span class="comment">// 这里替换上面的 li</span></span><br><span class="line">  (<span class="number">1</span>, <span class="number">2</span>, <span class="number">5</span>, <span class="number">4</span>)</span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<p>从上述例子中，我们一眼就可以看出先前的 ul 中的第三个 li 被移除了，四五替换了位置。<br>那么在实际的算法中，我们如何去识别改动的是哪个节点呢？这就引入了 key 这个属性。<strong>这个属性是用来给每一个节点打标志的，用于判断是否是同一个节点</strong>。<br>当然在判断以上差异的过程中，我们还需要判断节点的属性是否有变化等等。<br>当我们判断出以上的差异后，就可以把这些差异记录下来。当对比完两棵树以后，就可以通过差异去局部更新 DOM，实现性能的最优化。</p>
<h2 id="如何正确使用key"><a href="#如何正确使用key" class="headerlink" title="如何正确使用key"></a><a href="http://www.conardli.top/blog/article/React%E6%B7%B1%E5%85%A5%E7%B3%BB%E5%88%97/React%E4%B8%ADkey%E7%9A%84%E6%AD%A3%E7%A1%AE%E4%BD%BF%E7%94%A8%E6%96%B9%E5%BC%8F.html#_1-%E4%B8%BA%E4%BB%80%E4%B9%88%E8%A6%81%E4%BD%BF%E7%94%A8key" target="_blank" rel="noopener">如何正确使用key</a></h2><h2 id="生命周期管理"><a href="#生命周期管理" class="headerlink" title="生命周期管理"></a>生命周期管理</h2><p><a href="https://zhuanlan.zhihu.com/p/36062486" target="_blank" rel="noopener">详解</a><br><img src="/img/React%E5%8E%9F%E7%90%86/%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F.png"><br>React 16.8 +的生命周期分为三个阶段,分别是挂载阶段、更新阶段、卸载阶段</p>
<h3 id="挂载阶段"><a href="#挂载阶段" class="headerlink" title="挂载阶段:"></a>挂载阶段:</h3><ul>
<li>constructor: 构造函数，最先被执行,我们通常在构造函数里初始化 state 对象或者给自定义方法绑定 this</li>
<li>getDerivedStateFromProps: static getDerivedStateFromProps(nextProps, prevState),这是个静态方法,当我们接收到新的属性想去修改我们 state，可以使用 getDerivedStateFromProps</li>
<li>render: render 函数是纯函数，只返回需要渲染的东西，不应该包含其它的业务逻辑,可以返回原生的 DOM、React 组件、Fragment、Portals、字符串和数字、Boolean 和 null 等内容</li>
<li>componentDidMount: 组件装载之后调用，此时我们可以获取到 DOM 节点并操作，比如对 canvas，svg 的操作，<strong>服务器请求，订阅</strong>都可以写在这个里面，但是记得在 componentWillUnmount 中取消订阅</li>
</ul>
<h3 id="更新阶段"><a href="#更新阶段" class="headerlink" title="更新阶段:"></a>更新阶段:</h3><ul>
<li>getDerivedStateFromProps: 此方法在更新个挂载阶段都可能会调用</li>
<li>shouldComponentUpdate: shouldComponentUpdate(nextProps, nextState),有两个参数 nextProps 和 nextState，表示新的属性和变化之后的 state，返回一个布尔值，true 表示会触发重新渲染，false 表示不会触发重新渲染，默认返回 true,我们通常利用此生命周期来优化 React 程序性能</li>
<li>render: 更新阶段也会触发此生命周期</li>
<li>getSnapshotBeforeUpdate: getSnapshotBeforeUpdate(prevProps, prevState),这个方法在 render 之后，componentDidUpdate 之前调用，有两个参数 prevProps 和 prevState，表示之前的属性和之前的 state，这个函数有一个返回值，会作为第三个参数传给 componentDidUpdate，如果你不想要返回值，可以返回 null，此生命周期必须与 componentDidUpdate 搭配使用</li>
<li>componentDidUpdate: componentDidUpdate(prevProps, prevState, snapshot),该方法在 getSnapshotBeforeUpdate 方法之后被调用，有三个参数 prevProps，prevState，snapshot，表示之前的 props，之前的 state，和 snapshot。第三个参数是 getSnapshotBeforeUpdate 返回的,如果触发某些回调函数时需要用到 DOM 元素的状态，则将对比或计算的过程迁移至 getSnapshotBeforeUpdate，然后在 componentDidUpdate 中统一触发回调或更新状态。</li>
</ul>
<h3 id="卸载阶段"><a href="#卸载阶段" class="headerlink" title="卸载阶段:"></a>卸载阶段:</h3><ul>
<li>componentWillUnmount: 当我们的组件被卸载或者销毁了就会调用，我们可以在这个函数里去清除一些定时器，取消网络请求，清理无效的 DOM 元素等垃圾清理工作</li>
</ul>
<h3 id="废弃"><a href="#废弃" class="headerlink" title="废弃"></a>废弃</h3><p>React 16 之后有三个生命周期被废弃(但并未删除)：componentWillMount，componentWillReceiveProps，componentWillUpdate</p>
<h3 id="周期内禁忌的事"><a href="#周期内禁忌的事" class="headerlink" title="周期内禁忌的事"></a>周期内禁忌的事</h3><ul>
<li>componentWillMount 里 请求数据，订阅，setState</li>
</ul>
<h2 id="React-Fiber"><a href="#React-Fiber" class="headerlink" title="React Fiber"></a>React Fiber</h2><p>ReaxtV16 版本中引入了 Fiber 机制，生命周期部分发生变化。<br>React Fiber 是一种基于浏览器的单线程调度算法.<br>React 16之前 ，diff 算法实际上是递归，想要中断递归是很困难的，React 16 开始使用了循环来代替之前的递归。Fiber将diff拆分成无数个小任务的算法；它随时能够停止，恢复。停止恢复的时机取决于当前的一帧（16ms）内，还有没有足够的时间允许计算<br>Fiber 本质上是一个虚拟的堆栈帧，新的调度器会按照优先级自由调度这些帧，从而将之前的同步渲染改成了异步渲染，在不影响体验的情况下去分段计算更新<br>异步渲染，现在渲染有两个阶段：reconciliation 和 commit 。前者过程是可以打断的，后者不能暂停，会一直更新界面直到完成。<br>Reconciliation 阶段</p>
<ul>
<li>componentWillMount</li>
<li>componentWillReceiveProps</li>
<li>shouldComponentUpdate</li>
<li>componentWillUpdate</li>
</ul>
<p>Commit 阶段</p>
<ul>
<li>componentDidMount</li>
<li>componentDidUpdate</li>
<li>componentWillUnmount</li>
</ul>
<p>因为 Reconciliation 阶段是可以被打断的，所以 Reconciliation 阶段会执行的生命周期函数就可能会出现调用多次的情况，从而引起 Bug。由此对于 Reconciliation 阶段调用的几个函数，除了 shouldComponentUpdate 以外，其他都应该避免去使用，并且 V16 中也引入了新的 API 来解决这个问题。</p>
<ul>
<li>getDerivedStateFromProps 用于替换 componentWillReceiveProps ，该函数会在初始化和 update 时被调用</li>
<li>getSnapshotBeforeUpdate 用于替换 componentWillUpdate ，该函数会在 update 后 DOM 更新前被调用，用于读取最新的 DOM 数据。</li>
</ul>
<h2 id="setState-机制"><a href="#setState-机制" class="headerlink" title="setState 机制"></a>setState 机制</h2><p>PS: useState 它不香吗</p>
<h3 id="理想情况："><a href="#理想情况：" class="headerlink" title="理想情况："></a>理想情况：</h3><p>setState 是“异步”的，调用 setState 只会提交一次 state 修改到队列中，不会直接修改 this.state，等到满足一定条件时，react 会合并队列中的所有修改，触发一次 update 流程，更新 this.state。因此 setState 机制减少了 update 流程的触发次数，从而提高了性能。所以会有以下情况：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">handle() &#123;</span><br><span class="line">  <span class="comment">// 初始化 `count` 为 0</span></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.state.count) <span class="comment">// -&gt; 0</span></span><br><span class="line">  <span class="keyword">this</span>.setState(&#123; <span class="attr">count</span>: <span class="keyword">this</span>.state.count + <span class="number">1</span> &#125;)</span><br><span class="line">  <span class="keyword">this</span>.setState(&#123; <span class="attr">count</span>: <span class="keyword">this</span>.state.count + <span class="number">1</span> &#125;)</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.state.count) <span class="comment">// -&gt; 0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当然你也可以通过以下方式来实现调用三次 setState 使得 count 为 3</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">handle() &#123;</span><br><span class="line">  <span class="keyword">this</span>.setState(<span class="function">(<span class="params">prevState</span>) =&gt;</span> (&#123; <span class="attr">count</span>: prevState.count + <span class="number">1</span> &#125;))</span><br><span class="line">  <span class="keyword">this</span>.setState(<span class="function">(<span class="params">prevState</span>) =&gt;</span> (&#123; <span class="attr">count</span>: prevState.count + <span class="number">1</span> &#125;))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>由于 setState 会触发 update 过程，因此在 update 过程中必经的生命周期中调用 setState 会存在循环调用的风险。</p>
<p>另外用于监听 state 更新完成，可以使用 setState 方法的第二个参数，回调函数。在这个回调中读取 this.state 就是已经批量更新后的结果。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">handle() &#123;</span><br><span class="line">  <span class="keyword">this</span>.setState(<span class="function">(<span class="params">prevState</span>) =&gt;</span> <span class="function">(<span class="params">&#123; count: prevState.count + <span class="number">1</span> &#125;</span>), <span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="keyword">this</span>.state)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="特殊情况："><a href="#特殊情况：" class="headerlink" title="特殊情况："></a>特殊情况：</h3><p>在实际开发中，setState 的表现有时会不同于理想情况。主要是以下两种</p>
<ul>
<li>在 mount 流程中调用 setState。</li>
<li>在 setTimeout/Promise 回调中调用 setState。</li>
</ul>
<p>在第一种情况下，不会进入 update 流程，队列在 mount 时合并修改并 render<br>在第二种情况下，setState 将不会进行队列的批更新，而是直接触发一次 update 流程。这是由于 setState 的两种更新机制导致的，只有在批量更新模式中，才会是“异步”的。</p>
<h2 id="React-事件机制"><a href="#React-事件机制" class="headerlink" title="React 事件机制"></a>React 事件机制</h2><p><a href="https://juejin.im/post/5bd32493f265da0ae472cc8e" target="_blank" rel="noopener">更详细的源码解析版</a><br>React 其实自己实现了一套事件机制，首先我们考虑一下以下代码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> Test = <span class="function">(<span class="params">&#123; list, handleClick &#125;</span>) =&gt;</span> (&#123;</span><br><span class="line">    list.map(<span class="function">(<span class="params">item, index</span>) =&gt;</span> (</span><br><span class="line">        &lt;span onClick=&#123;handleClick&#125; key=&#123;index&#125;&gt;&#123;index&#125;&lt;<span class="regexp">/span&gt;</span></span><br><span class="line"><span class="regexp">    ))</span></span><br><span class="line"><span class="regexp">&#125;)</span></span><br></pre></td></tr></table></figure>

<p>上面的点击事件是否绑定在了每一个标签上？当然不是，JSX 上写的事件并没有绑定在对应的真实 DOM 上，而是通过<strong>事件代理</strong>的方式，将所有的事件都统一绑定在了 document 上。这样的方式不仅减少了内存消耗，还能在组件挂载销毁时统一订阅和移除事件。</p>
<p>另外冒泡到 document 上的事件也不是原生浏览器事件，而是 React 自己实现的合成事件（SyntheticEvent）。因此在React中，用 return false阻止默认行为，而应该在事件处理程序中调用 event.preventDefault 或 event.stopPropagation 来阻止冒泡（所以也不会有兼容性问题）</p>
<p>合成事件的优点</p>
<ul>
<li>抹平了浏览器之间的兼容问题</li>
<li>性能优化。对于原生浏览器事件来说，浏览器会给监听器创建一个事件对象。如果你有很多的事件监听，那么就需要分配很多的事件对象，造成高额的内存分配问题。但是对于合成事件来说，有一个事件池专门来管理它们的创建和销毁，当事件需要被使用时，就会从池子中复用对象，事件回调结束后，就会销毁事件对象上的属性，从而便于下次复用事件对象</li>
</ul>
<h1 id="一些特性"><a href="#一些特性" class="headerlink" title="一些特性"></a>一些特性</h1><h2 id="React-组件"><a href="#React-组件" class="headerlink" title="React 组件"></a>React 组件</h2><p><a href="https://jacleklm.github.io/2019/12/29/React%E7%BB%84%E4%BB%B6/" target="_blank" rel="noopener">见此博文</a></p>
<h2 id="React-Router-amp-React-Redux"><a href="#React-Router-amp-React-Redux" class="headerlink" title="React Router &amp; React Redux"></a>React Router &amp; React Redux</h2><p><a href="https://jacleklm.github.io/2020/01/04/React%E8%B7%AF%E7%94%B1%20&%20Redux/" target="_blank" rel="noopener">见此博文</a></p>
<h2 id="通信"><a href="#通信" class="headerlink" title="通信"></a>通信</h2><ul>
<li><p>父子组件通信：单向数据流，父组件通过 props 传递数据，子组件不能直接修改 props， 而是必须通过调用父组件函数的方式告知父组件修改数据</p>
</li>
<li><p>兄弟组件通信：可以通过共同的父组件来管理状态和事件函数。比如说其中一个兄弟组件调用父组件传递过来的事件函数修改父组件中的状态，然后父组件将状态传递给另一个兄弟组件</p>
</li>
<li><p>跨多层次组件通信</p>
<ul>
<li><p><a href="https://zh-hans.reactjs.org/docs/context.html" target="_blank" rel="noopener">Context API</a>。此外，还可以通过 context 传递一个函数，使得 consumers 组件更新 context</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 创建 Context，可以在开始就传入值</span></span><br><span class="line"><span class="keyword">const</span> StateContext = React.createContext()</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Parent</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">  render () &#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">      <span class="comment">// value 就是传入 Context 中的值</span></span><br><span class="line">      &lt;StateContext.Provider value=<span class="string">'yck'</span>&gt;</span><br><span class="line">        &lt;Child /&gt;</span><br><span class="line">      &lt;<span class="regexp">/StateContext.Provider&gt;</span></span><br><span class="line"><span class="regexp">    )</span></span><br><span class="line"><span class="regexp">  &#125;</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br><span class="line"><span class="regexp">class Child extends React.Component &#123;</span></span><br><span class="line"><span class="regexp">  render () &#123;</span></span><br><span class="line"><span class="regexp">    return (</span></span><br><span class="line"><span class="regexp">      &lt;ThemeContext.Consumer&gt;</span></span><br><span class="line"><span class="regexp">        /</span><span class="regexp">/ 取出值</span></span><br><span class="line"><span class="regexp">        &#123;context =&gt; (</span></span><br><span class="line"><span class="regexp">          name is &#123; context &#125;</span></span><br><span class="line"><span class="regexp">        )&#125;</span></span><br><span class="line"><span class="regexp">      &lt;/</span>ThemeContext.Consumer&gt;</span><br><span class="line">    );</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p><a href="https://zh-hans.reactjs.org/docs/hooks-reference.html#usecontext" target="_blank" rel="noopener">useContext</a>不香吗，其实是同理的</p>
</li>
</ul>
</li>
<li><p>任意组件：Redux等状态管理工具 或者 Event Bus 解决，另外如果你不怕麻烦的话，可以使用这种方式解决上述所有的通信情况</p>
</li>
</ul>
<h2 id="render-props"><a href="#render-props" class="headerlink" title="render props"></a><a href="https://zh-hans.reactjs.org/docs/render-props.html" target="_blank" rel="noopener">render props</a></h2><h1 id="性能优化"><a href="#性能优化" class="headerlink" title="性能优化"></a>性能优化</h1><p>react 中性能主要耗费在于 update 阶段的 diff 算法，因此性能优化也主要是减少 diff 算法触发次数</p>
<h2 id="setState"><a href="#setState" class="headerlink" title="setState"></a>setState</h2><p>setState 机制在正常运行时，由于批更新策略，已经降低了 update 过程的触发次数。<br>因此，setState 优化主要在于非批更新阶段中(timeout/Promise 回调)，减少 setState 的触发次数。<br>常见的业务场景即处理接口回调时，无论数据处理多么复杂，保证最后只调用一次 setState</p>
<h2 id="父组件-Render"><a href="#父组件-Render" class="headerlink" title="父组件 Render"></a>父组件 Render</h2><p>当组件的 state 或 props 变化时，自身 render() 会重新执行<br>父组件的 render 必然会触发子组件 render，子组件会进入 update 阶段（无论 props 是否更新）</p>
<ul>
<li>此时最常用的优化方案即为 shouldComponentUpdate 方法。在 shouldComponentUpdate 函数中我们可以通过返回布尔值来决定当前组件是否需要更新，一般来说不推荐完整地对比当前 state 和之前的 state 是否相同，一般只比较 stated 某个或某几个值。最常见的方式为进行 this.props 和 this.state 的浅比较来判断组件是否需要更新，如下，组件只有当 props.color 或者 state.count 的值改变才会更新</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">PureComponent</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(props) &#123;</span><br><span class="line">    <span class="keyword">super</span>(props)</span><br><span class="line">    <span class="keyword">this</span>.state = &#123; <span class="attr">count</span>: <span class="number">1</span> &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  shouldComponentUpdate(nextProps, nextState) &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>.props.color !== nextProps.color) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>.state.count !== nextState.count) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">  &#125;</span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span>PureComponent<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>或者直接使用 PureComponent，原理一致</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">PureComponent</span> </span>&#123;</span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span>PureComponent<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>函数组件用不了 shouldComponentUpdate，可以使用 React.memo 来实现相同的功能</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> Test = React.memo(<span class="function"><span class="params">()</span> =&gt;</span> <span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span>PureComponent<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span>)</span><br></pre></td></tr></table></figure>

<h2 id="DOM-操作方面减少-diff"><a href="#DOM-操作方面减少-diff" class="headerlink" title="DOM 操作方面减少 diff"></a>DOM 操作方面减少 diff</h2><ul>
<li>不使用跨层级移动节点的操作</li>
<li>对于条件渲染多个节点时，尽量采用隐藏等方式切换节点，而不是替换节点</li>
<li>尽量避免将后面的子节点移动到前面的操作，当节点数量较多时，会产生一定的性能问题</li>
</ul>
<h1 id="React的优点"><a href="#React的优点" class="headerlink" title="React的优点"></a>React的优点</h1><h2 id="React速度很快"><a href="#React速度很快" class="headerlink" title="React速度很快"></a>React速度很快</h2><p>它并不直接对DOM进行操作，引入了一个叫做虚拟DOM的概念，安插在javascript逻辑和实际的DOM之间，性能好。最大限度减少DOM交互。</p>
<h2 id="跨浏览器兼容"><a href="#跨浏览器兼容" class="headerlink" title="跨浏览器兼容"></a>跨浏览器兼容</h2><p>虚拟DOM帮助我们解决了跨浏览器问题，它为我们提供了标准化的API，甚至在IE8中都是没问题的</p>
<h2 id="一切都是component"><a href="#一切都是component" class="headerlink" title="一切都是component"></a>一切都是component</h2><p>代码更加模块化，重用代码更容易，可维护性高。这样当某个或某些组件出现问题是，可以方便地进行隔离。每个组件都可以进行独立的开发和测试，并且它们可以引入其它组件。这等同于提高了代码的可维护性。</p>
<h2 id="单向数据流"><a href="#单向数据流" class="headerlink" title="单向数据流"></a>单向数据流</h2><p>Flux是一个用于在JavaScript应用中创建单向数据层的架构，它随着React视图库的开发而被Facebook概念化。减少了重复代码，这也是它为什么比传统数据绑定更简单。</p>
<h2 id="同构、纯粹的javascript"><a href="#同构、纯粹的javascript" class="headerlink" title="同构、纯粹的javascript"></a>同构、纯粹的javascript</h2><p>因为搜索引擎的爬虫程序依赖的是服务端响应而不是JavaScript的执行，预渲染你的应用有助于搜索引擎优化。</p>
<h2 id="兼容性好"><a href="#兼容性好" class="headerlink" title="兼容性好"></a>兼容性好</h2><p>比如使用RequireJS来加载和打包，而Browserify和Webpack适用于构建大型应用。它们使得那些艰难的任务不再让人望而生畏。</p>
<h2 id="Vue"><a href="#Vue" class="headerlink" title="Vue"></a>Vue</h2><p>Vue 从一开始的定位就是尽可能的降低前端开发的门槛，让更多的人能够更快地上手开发。Vue 首先考虑的是假设用户只掌握了 web 基础知识 (HTML, CSS, JS) 的情况下，如何能够最快理解和上手，实现一个看得见摸得着的应用</p>
<h1 id="React设计模式"><a href="#React设计模式" class="headerlink" title="React设计模式"></a>React设计模式</h1><p>见<a href="https://sangka.github.io/react-in-patterns-cn/chapter-2/" target="_blank" rel="noopener">React模式</a>。这篇文章写的非常好，观点简明扼要。看完想写一篇笔记发现这篇文章已经总结的很精简了，直接多看几遍吧吧233</p>
<blockquote>
<p>单向数据流这种模式十分适合跟 React 搭配使用。它的主要思想是组件不会改变接收的数据。它们只会监听数据的变化，当数据发生变化时它们会使用接收到的新值，而不是去修改已有的值。当组件的更新机制触发后，它们只是使用新值进行重新渲染而已</p>
</blockquote>
<p>参考资料<br><a href="https://segmentfault.com/a/1190000015648248" target="_blank" rel="noopener">react 基本原理及性能优化</a><br><a href="https://zh-hans.reactjs.org/docs/optimizing-performance.html" target="_blank" rel="noopener">React 官方文档-性能优化</a><br><a href="https://zhuanlan.zhihu.com/p/36062486" target="_blank" rel="noopener">React v16.3 版本新生命周期函数浅析及升级方案</a><br>前端面试之道<br><a href="https://juejin.im/post/5bd32493f265da0ae472cc8e" target="_blank" rel="noopener">React事件机制源码解析</a><br><a href="http://www.conardli.top/blog/" target="_blank" rel="noopener">conardli</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>框架</tag>
      </tags>
  </entry>
  <entry>
    <title>React - Hook</title>
    <url>/2020/05/23/React%20-%20Hook/</url>
    <content><![CDATA[<p>本文大部分来自<a href="http://www.conardli.top/blog/article/React%E6%B7%B1%E5%85%A5%E7%B3%BB%E5%88%97/%E4%BB%8EMixin%E5%88%B0HOC%E5%86%8D%E5%88%B0Hook%EF%BC%88%E4%B8%80%EF%BC%89.html#%E5%AF%BC%E8%AF%BB" target="_blank" rel="noopener">ConardLi 的 blog</a>，建议直接看原博客</p>
<h1 id="What-amp-Why"><a href="#What-amp-Why" class="headerlink" title="What &amp; Why"></a>What &amp; Why</h1><p>使用 Hooks，你可以在将含有 state 的逻辑从组件中抽象出来，这将可以让这些逻辑容易被测试。同时，Hooks 可以帮助你在不重写组件结构的情况下复用这些逻辑。所以，它也可以作为一种实现状态逻辑复用的方案<br>解决了 HOC 嵌套地狱的问题，使得逻辑复用更加清晰</p>
<h1 id="State-Hook"><a href="#State-Hook" class="headerlink" title="State Hook"></a>State Hook</h1><p>useState 是一个钩子，他可以为函数式组件增加一些状态，并且提供改变这些状态的函数，同时它接收一个参数，这个参数作为状态的默认值</p>
<h1 id="Effect-Hook"><a href="#Effect-Hook" class="headerlink" title="Effect Hook"></a>Effect Hook</h1><p>Effect Hook 可以让你在函数组件中执行一些具有 side effect（副作用）的操作</p>
<h2 id="参数"><a href="#参数" class="headerlink" title="参数"></a>参数</h2><ul>
<li>第一个参数是回调函数，在第组件一次 render 和之后的每次 update 后运行，React 保证在 DOM 已经更新完成之后才会运行回调；这个回调函数可以 return 一个函数，<strong>在执行下一个 useEffect 之前，会执行这个函数</strong>，常常用来对上一次调用 useEffect 进行清理，并且可以拿到上一次的状态（原理是闭包）</li>
<li>第二个参数是状态依赖</li>
</ul>
<h2 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h2><h3 id="模拟生命周期"><a href="#模拟生命周期" class="headerlink" title="模拟生命周期"></a>模拟生命周期</h3><ul>
<li>componentDidMount：状态依赖为空数组即可（其实官方并不推荐这种写法）</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">useDidMount</span>(<span class="params">callback</span>) </span>&#123;</span><br><span class="line">  useEffect(callback, [])</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>componentWillUnmount：作为回调函数的 return 函数</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">useUnMount</span>(<span class="params">callback</span>) </span>&#123;</span><br><span class="line">  useEffect(<span class="function"><span class="params">()</span> =&gt;</span> callback, [])</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="ref-Hook"><a href="#ref-Hook" class="headerlink" title="ref Hook"></a>ref Hook</h1><p>用来获取 DOM 的 ref</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="function"><span class="keyword">function</span> <span class="title">Input</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> inputEl = useRef(<span class="literal">null</span>)</span><br><span class="line">  <span class="keyword">const</span> onButtonClick = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    inputEl.current.focus()</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    &lt;div&gt;</span><br><span class="line">      &lt;input ref=&#123;inputEl&#125; type=<span class="string">"text"</span> /&gt;</span><br><span class="line">      &lt;button onClick=&#123;onButtonClick&#125;&gt;Focus the input&lt;<span class="regexp">/button&gt;</span></span><br><span class="line"><span class="regexp">    &lt;/</span>div&gt;</span><br><span class="line">  )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意 useRef()并不仅仅可以用来当作获取 ref 使用，使用 useRef 产生的 ref 的 current 属性是可变的，这意味着你可以用它来保存一个任意值</p>
<h2 id="模拟-componentDidUpdate"><a href="#模拟-componentDidUpdate" class="headerlink" title="模拟 componentDidUpdate"></a>模拟 componentDidUpdate</h2><p>componentDidUpdate 就相当于除去第一次调用的 useEffect，我们可以借助 useRef 生成一个标识，来记录是否为第一次执行：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">useDidUpdate</span>(<span class="params">callback, prop</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> init = useRef(<span class="literal">true</span>)</span><br><span class="line">  useEffect(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (init.current) &#123;</span><br><span class="line">      init.current = <span class="literal">false</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> callback()</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;, prop)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="自定义-Hook"><a href="#自定义-Hook" class="headerlink" title="自定义 Hook"></a>自定义 Hook</h1><p>自定义 Hook 非常简单，我们只需要定义一个函数，并且把相应需要的状态和 effect 封装进去，同时，Hook 之间也是可以相互引用的。使用 use 开头命名自定义 Hook，这样可以方便 eslint 进行检查。<br>自定义 Hook 不需要具有特殊的标识。我们可以自由的决定它的参数是什么，以及它应该返回什么（如果需要的话）。换句话说，<strong>它就像一个正常的函数</strong></p>
<h2 id="使用-Hook-的动机"><a href="#使用-Hook-的动机" class="headerlink" title="使用 Hook 的动机"></a>使用 Hook 的动机</h2><ul>
<li>减少状态逻辑复用的风险<ul>
<li>多个 Hook 之前不会相互影响，不会相互覆盖；Hook 也可以避免 HOC 使用不规范时的 props 覆盖</li>
</ul>
</li>
<li>避免嵌套地狱</li>
<li>让组件更容易理解：能用 Hook 讲公共逻辑抽离成函数，将一个组件分割成更小的函数，而不是强制基于生命周期方法进行分割</li>
<li>函数代替 class</li>
</ul>
<h1 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h1><ul>
<li>useState 的 setXX 是异步函数，所以 setXXX 后立刻 console.log（XXX）的话是得不到的</li>
<li>在 setTimeout 这种闭包中中读不到其他状态的新值</li>
<li>记住 Hook 不能写在条件（if），循环（for），和嵌套函数中使用</li>
</ul>
<p>参考资料<br><a href="http://www.conardli.top/blog/article/React%E6%B7%B1%E5%85%A5%E7%B3%BB%E5%88%97/%E4%BB%8EMixin%E5%88%B0HOC%E5%86%8D%E5%88%B0Hook%EF%BC%88%E5%9B%9B%EF%BC%89.html#effect-hook" target="_blank" rel="noopener">ConardLi 的 blog</a><br><a href="https://zh-hans.reactjs.org/docs/hooks-intro.html" target="_blank" rel="noopener">官方文档</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>框架</tag>
      </tags>
  </entry>
  <entry>
    <title>React Router &amp; Redux</title>
    <url>/2020/01/04/React%E8%B7%AF%E7%94%B1%20&amp;%20Redux/</url>
    <content><![CDATA[<h1 id="React-Router"><a href="#React-Router" class="headerlink" title="React Router"></a>React Router</h1><h2 id="路由原理"><a href="#路由原理" class="headerlink" title="路由原理"></a>路由原理</h2><p>前端路由的本质是监听 URL 的变化，然后匹配路由规则，显示相应的页面，并且无须刷新页面，核心是<strong>改变视图的同时不会向后端发出请求</strong>。目前前端使用的路由就只有两种实现方式:<br>Hash 模式和 History 模式</p>
<h3 id="Hash-模式"><a href="#Hash-模式" class="headerlink" title="Hash 模式"></a>Hash 模式</h3><p>Vue-router 默认是 hash 模式</p>
<ul>
<li><a href="http://www.test.com/#/" target="_blank" rel="noopener">www.test.com/#/</a> 就是 Hash URL（有 /#/ 就是），当 # 后面的哈希值发生变化时</li>
<li><strong>hash 的修改不会导致浏览器刷新，因为</strong> window.location 处理哈希的改变时不会重新渲染页面，而是当作新页面加到历史记录中</li>
<li>所以我们可以通过 hashchange 事件来监听到 URL 的变化，写一些逻辑进行组件替换实现更新页面的效果；同时浏览器监听到 hash 变化，会把更新历史记录，并且按后退键能回到上个位置</li>
<li>*无论哈希值如何变化，服务端接收到的 URL 请求永远是 <a href="http://www.test.com/" target="_blank" rel="noopener">www.test.com</a></li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">window</span>.addEventListener(<span class="string">'hashchange'</span>, () =&gt; &#123;</span><br><span class="line">  <span class="comment">// ... 具体逻辑</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>Hash 模式优点：</p>
<ul>
<li>简单，兼容性也更好（ie8）</li>
<li>不需要服务器端进行任何设置和开发</li>
<li>除了资源加载和ajax请求以外，不会发起其他请求</li>
</ul>
<p>缺点：</p>
<ul>
<li>不太美观</li>
<li>对于部分需要重定向的操作，后端无法获取hash部分内容，导致后台无法取得url中的数据，典型的例子就是微信公众号的oauth验证</li>
<li>服务器端无法准确跟踪前端路由信息</li>
<li>对于需要锚点功能的需求会与目前路由机制冲突</li>
</ul>
<h3 id="History-模式"><a href="#History-模式" class="headerlink" title="History 模式"></a>History 模式</h3><p>History 模式是 HTML5 新推出的功能，主要使用 history.pushState 和 history.replaceState 改变 URL。<br>通过 History 模式改变 URL 同样<strong>不会引起页面的刷新，只会更新浏览器的历史记录</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 新增历史记录</span></span><br><span class="line">history.pushState(stateObject, title, URL) <span class="comment">// 前两个参数可以写成null，url必须是同源的</span></span><br><span class="line"><span class="comment">// 替换当前历史记录</span></span><br><span class="line">history.replaceState(stateObject, title, URL)</span><br></pre></td></tr></table></figure>

<p>当用户做出浏览器动作时，比如点击后退按钮时会触发 popState 事件</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">window</span>.addEventListener(<span class="string">'popstate'</span>, e =&gt; &#123;</span><br><span class="line">  <span class="comment">// e.state 就是 pushState(stateObject) 中的 stateObject</span></span><br><span class="line">  <span class="built_in">console</span>.log(e.state)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h3 id="两种模式对比"><a href="#两种模式对比" class="headerlink" title="两种模式对比"></a>两种模式对比</h3><p>history 的优点：</p>
<ul>
<li>pushState() 设置的新 URL 可以是与当前 URL 同源的任意 URL；而 hash 只可修改 # 后面的部分，因此只能设置与当前 URL 同文档的 URL；</li>
<li>pushState() 设置的新 URL 可以与当前 URL 一模一样，这样也会把记录添加到栈中；而 hash 设置的新值必须与原来不一样才会触发动作将记录添加到栈中；</li>
<li>pushState() 通过 stateObject 参数可以添加任意类型的数据到记录中；而 hash 只可添加短字符串；</li>
<li>pushState() 可额外设置 title 属性供后续使用</li>
</ul>
<p>history 的缺点：一是兼容性，二是需要后端的支持，当真正想输入 url 发起 http 请求的时候，eg. 用户手动输入 URL 后回车</p>
<ul>
<li>hash 模式下，仅 # 之前的内容会被包含在请求中，所以对后端来说，即使没有做到对路由的全覆盖，也不会返回 404 错误</li>
<li>history 模式下，前端的 URL 必须和实际向后端发起请求的 URL 一致，如 <a href="http://www.abc.com/book/id%E3%80%82%E5%A6%82%E6%9E%9C%E5%90%8E%E7%AB%AF%E7%BC%BA%E5%B0%91%E5%AF%B9" target="_blank" rel="noopener">http://www.abc.com/book/id。如果后端缺少对</a> /book/id 的路由处理，将返回 404 错误。Vue-Router 官网里如此描述：“不过这种模式要玩好，还需要后台配置支持……所以呢，你要在服务端增加一个覆盖所有情况的候选资源：如果 URL 匹配不到任何静态资源，则应该返回同一个 index.html 页面，这个页面就是你 app 依赖的页面。”</li>
</ul>
<h2 id="功能"><a href="#功能" class="headerlink" title="功能"></a>功能</h2><p>感觉 React 的路由比 Vue 的路由更复杂些<br>exect 的区别<br><code>&lt;Route&gt;</code>,<code>&lt;Router&gt;</code>,<code>&lt;Route&gt;</code>, <a href="https://juejin.im/post/5d53e885f265da03bc1270ee" target="_blank" rel="noopener">https://juejin.im/post/5d53e885f265da03bc1270ee</a> 。应该确实是组件被<route>包裹和 withRouter 是等价的<br>props.history.push()</route></p>
<h1 id="Redux"><a href="#Redux" class="headerlink" title="Redux"></a>Redux</h1><h2 id="工作流程"><a href="#工作流程" class="headerlink" title="工作流程"></a>工作流程</h2><ul>
<li>Store：保存数据的地方，你可以把它看成一个容器，整个应用只能有一个 Store。</li>
<li>State：Store 对象包含所有数据，如果想得到某个时点的数据，就要对 Store 生成快照，这种时点的数据集合，就叫做 State。</li>
<li>Action：State 的变化，会导致 View 的变化。但是，用户接触不到 State，只能接触到 View。所以，State 的变化必须是 View 导致的。Action 就是 View 发出的通知，表示 State 应该要发生变化了。</li>
<li>Action Creator：View 要发送多少种消息，就会有多少种 Action。如果都手写，会很麻烦，所以我们定义一个函数来生成 Action，这个函数就叫 Action Creator。</li>
<li>Reducer：Store 收到 Action 以后，必须给出一个新的 State，这样 View 才会发生变化。这种 State 的计算过程就叫做 Reducer。Reducer 是一个函数，它接受 Action 和当前 State 作为参数，返回一个新的 State。</li>
<li>dispatch：是 View 发出 Action 的唯一方法。</li>
</ul>
<p>然后我们过下整个工作流程：</p>
<ul>
<li>首先，用户（通过 View）发出 Action，发出方式就用到了 dispatch 方法。</li>
<li>然后，Store 自动调用 Reducer，并且传入两个参数：当前 State 和收到的 Action，Reducer 会返回新的 State</li>
<li>State 一旦有变化，Store 就会调用监听函数，来更新 View。</li>
</ul>
<p>到这儿为止，一次用户交互流程结束。可以看到，在整个流程中数据都是单向流动的，这种方式保证了流程的清晰。<br><img src="/img/React%E5%8E%9F%E7%90%86/Redux.png"></p>
<h2 id="react-redux-工作流程"><a href="#react-redux-工作流程" class="headerlink" title="react-redux 工作流程"></a>react-redux 工作流程</h2><ul>
<li>Provider: Provider 的作用是从最外部封装了整个应用，并向 connect 模块传递 store</li>
<li>connect: 负责连接 React 和 Redux<ul>
<li>获取 state: connect 通过 context 获取 Provider 中的 store，通过 store.getState()获取整个 store tree 上所有 state</li>
<li>包装原组件: 将 state 和 action 通过 props 的方式传入到原组件内部 wrapWithConnect 返回一个 ReactComponent 对象 Connect，Connect 重新 render 外部传入的原组件 WrappedComponent，并把 connect 中传入的 mapStateToProps, mapDispatchToProps 与组件上原有的 props 合并后，通过属性的方式传给 WrappedComponent</li>
<li>监听 store tree 变化: connect 缓存了 store tree 中 state 的状态,通过当前 state 状态和变更前 state 状态进行比较,从而确定是否调用 this.setState()方法触发 Connect 及其子组件的重新渲染</li>
</ul>
</li>
</ul>
<p><img src="/img/React%E5%8E%9F%E7%90%86/React-redux.png"></p>
<h2 id="Redux-VS-Mobx"><a href="#Redux-VS-Mobx" class="headerlink" title="Redux VS Mobx"></a>Redux VS Mobx</h2><p>两者对比:</p>
<ul>
<li>redux 将数据保存在单一的 store 中，mobx 将数据保存在分散的多个 store 中</li>
<li>redux 使用 plain object 保存数据，需要手动处理变化后的操作；mobx 适用 observable 保存数据，数据变化后自动处理响应的操作</li>
<li>redux 使用不可变状态，这意味着状态是只读的，不能直接去修改它，而是应该返回一个新的状态，同时使用纯函数；mobx 中的状态是可变的，可以直接对其进行修改</li>
<li>mobx 相对来说比较简单，在其中有很多的抽象，mobx 更多的使用面向对象的编程思维；redux 会比较复杂，因为其中的函数式编程思想掌握起来不是那么容易，同时需要借助一系列的中间件来处理异步和副作用</li>
<li>mobx 中有更多的抽象和封装，调试会比较困难，同时结果也难以预测；而 redux 提供能够进行时间回溯的开发工具，同时其纯函数以及更少的抽象，让调试变得更加的容易</li>
</ul>
<p>场景辨析:</p>
<ul>
<li>mobx 更适合数据不复杂的应用: mobx 难以调试,很多状态无法回溯,面对复杂度高的应用时,往往力不从心.mobx 适合短平快的项目: mobx 上手简单,样板代码少,可以很大程度上提高开发效率.</li>
<li>redux 适合有回溯需求的应用: 比如一个画板应用、一个表格应用，很多时候需要撤销、重做等操作，由于 redux 不可变的特性，天然支持这些操作.</li>
</ul>
<p>参考资料<br><a href="https://juejin.im/post/5d5f44dae51d4561df7805b4#heading-12" target="_blank" rel="noopener">高频 React 面试题及详解</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>框架</tag>
      </tags>
  </entry>
  <entry>
    <title>MeiTuan-app项目总结</title>
    <url>/2019/11/16/MeiTuan-app%E9%A1%B9%E7%9B%AE%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p>缘起：前段时间面试SXF二面的时候发现自己项目经验有点少，一些组件实现思路也不清晰，决定恶补一波。该项目来源是慕课网上的《Vue全家桶+SSR+Koa2全栈开发美团网》，于近期学习完毕。</p>
<h1 id="基础知识"><a href="#基础知识" class="headerlink" title="基础知识"></a>基础知识</h1><h2 id="Koa2"><a href="#Koa2" class="headerlink" title="Koa2"></a><a href="https://koa.bootcss.com/#introduction" target="_blank" rel="noopener">Koa2</a></h2><p>Koa2是基于Nodejs的web框架<br>对Node.js的http进行了封装（封装了一个服务器），用 Promise + async 实现异步 （Koa1是用Generator，Express是用Promise）<br><strong>ctx</strong>是一个全局的信息对象，包含所有信息</p>
<h3 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h3><p>全局安装koa、 koa-generator。用koa2 -e ${项目名} 来初始化项目<br>主配置文件是 /app.js ， 接口都是写在 /routers 文件夹中</p>
<h3 id="中间件的原理"><a href="#中间件的原理" class="headerlink" title="中间件的原理"></a>中间件的原理</h3><p>每个Request和Response都要经过中间件，并且进和出中间件的执行顺序的是相反的，所以中间件就类似于洋葱的层<br><img src="/img/%E7%BE%8E%E5%9B%A2%E7%BD%91/%E6%B4%8B%E8%91%B1%E5%9B%BE.png"></p>
<h2 id="Mongodb"><a href="#Mongodb" class="headerlink" title="Mongodb"></a>Mongodb</h2><h3 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h3><ul>
<li>mongodb是非关系型数据库，mySQL是关系型数据库</li>
<li>在mongodb中：database数据库，collection 数据表，一行的数据叫document，列叫filed</li>
<li><a href="http://www.mongoosejs.net/docs/guide.html" target="_blank" rel="noopener">mongoose</a>是用来操作mongodb的，能使操作更简单；可视化的数据管理工具RoBo 3T</li>
</ul>
<h3 id="使用方法-1"><a href="#使用方法-1" class="headerlink" title="使用方法"></a>使用方法</h3><p>能在Koa2的项目中连接mongodb，然后写一些能操作数据库的接口</p>
<h2 id="Redis"><a href="#Redis" class="headerlink" title="Redis"></a>Redis</h2><p>是一个<strong>快速读写</strong>的数据库，一般用来存session的数据库</p>
<h2 id="Nuxt-js"><a href="#Nuxt-js" class="headerlink" title="Nuxt.js"></a><a href="https://zh.nuxtjs.org/" target="_blank" rel="noopener">Nuxt.js</a></h2><h3 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h3><p>Nuxt.js是一个基于Vue的通用应用框架，基于Vue，包含Vue-router，支持Vuex、Vue SSR和vue-meta。SSR能解决Vue首屏时间长和SEO差的缺点；用Nuxt.js做Vue SSR很方便</p>
<h3 id="生命周期"><a href="#生命周期" class="headerlink" title="生命周期"></a>生命周期</h3><p><img src="/img/%E7%BE%8E%E5%9B%A2%E7%BD%91/%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F.png"><br>重要的是写在Vuex的action中的<strong>nuxtServerInit</strong>和写在组件中的<strong>asyncData()&amp;fetch()</strong></p>
<h3 id="项目目录"><a href="#项目目录" class="headerlink" title="项目目录"></a>项目目录</h3><p>/layout 是放布局，或者说是页面的模板。把页面共同的组件放到这里，eg. footer，登录框<br>/page 是页面的入口文件<br>/components 只放组件<br>/server 放服务器<br>/store 放Vuex</p>
<h3 id="路由"><a href="#路由" class="headerlink" title="路由"></a>路由</h3><p><code>&lt;nuxt-link&gt;</code>标签相当于<code>&lt;router-link&gt;</code>，用法是完全一样的；<code>&lt;nuxt /&gt;</code>相当于是<code>&lt;router-view&gt;</code>；page文件下的入口文件，其文件名就是页面路由地址</p>
<h3 id="asyncData"><a href="#asyncData" class="headerlink" title="asyncData"></a><a href="https://zh.nuxtjs.org/guide/async-data" target="_blank" rel="noopener">asyncData</a></h3><h4 id="一般情况"><a href="#一般情况" class="headerlink" title="一般情况"></a>一般情况</h4><p>如果我们的组件需要获取异步数据，可以把mounted写成async mounted，然后在其中写axios.get()去获取数据</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> mounted() &#123;</span><br><span class="line">    <span class="keyword">let</span> self = <span class="keyword">this</span>;</span><br><span class="line">    <span class="comment">//  status是响应的状态码, data属性是响应的数据</span></span><br><span class="line">    <span class="keyword">let</span> &#123;</span><br><span class="line">      status,</span><br><span class="line">      data: &#123; list &#125;</span><br><span class="line">    &#125; = <span class="keyword">await</span> axios.get(<span class="string">"/city/list"</span>);</span><br><span class="line">    <span class="keyword">if</span> (status === <span class="number">200</span>) &#123;</span><br><span class="line">      self.list = list;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>这种情况下，我们在页面的源码中是找不到异步数据的，是浏览器拿到服务器的响应的页面，加载，在加载的mounted阶段去获取。</p>
<h4 id="SSR"><a href="#SSR" class="headerlink" title="SSR"></a><a href="https://ssr.vuejs.org/zh/" target="_blank" rel="noopener">SSR</a></h4><p>SSR是在服务端就完成页面所有操作（包括请求异步数据），然后渲染好，服务器再响应。<br>所以在组件中我们可以用asyncData的写法（一般用来组件获取数据<br>）。fetch() 一般用来拿到数据修改Vuex状态</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> asyncData() &#123;</span><br><span class="line">    <span class="keyword">let</span> &#123;</span><br><span class="line">      status,</span><br><span class="line">      data: &#123; list &#125;</span><br><span class="line">    &#125; = <span class="keyword">await</span> axios.get(<span class="string">"http://localhost:3000/city/list"</span>);</span><br><span class="line">    <span class="keyword">if</span> (status === <span class="number">200</span>) &#123;</span><br><span class="line">      <span class="comment">// 这种return的写法就相当于把asyncData得到的list复制给data中的list</span></span><br><span class="line">      <span class="keyword">return</span> &#123;</span><br><span class="line">        list</span><br><span class="line">      &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p><strong>注意</strong> 由于asyncData方法是在组件<strong>初始化</strong>前被调用的，所以在方法内是没有办法通过<strong>this</strong>来引用组件的实例对象</p>
<h3 id="Vuex"><a href="#Vuex" class="headerlink" title="Vuex"></a>Vuex</h3><p>由于Nuxt有更新，现在的写法见<a href="https://www.jianshu.com/p/4861099edbab" target="_blank" rel="noopener">链接</a></p>
<h4 id="nuxtServerInit"><a href="#nuxtServerInit" class="headerlink" title="nuxtServerInit"></a>nuxtServerInit</h4><p>一般是写在 /store/index.js 入口文件的actions中，用来获取异步数据并把数据添加到state中</p>
<h1 id="项目小结"><a href="#项目小结" class="headerlink" title="项目小结"></a>项目小结</h1><h2 id="布局"><a href="#布局" class="headerlink" title="布局"></a>布局</h2><ul>
<li>页面的header+body+footer的布局可以用Element UI提供的container布局。在Nuxt项目中，header和footer如果多个页面是一样的，可以写在 /layout/default.vue 中或者新建布局文件</li>
<li>组件的布局用Element UI的layout布局-分栏间隔</li>
</ul>
<h2 id="组件实现"><a href="#组件实现" class="headerlink" title="组件实现"></a>组件实现</h2><h3 id="首页"><a href="#首页" class="headerlink" title="首页"></a>首页</h3><h4 id="登录状态组件-user-vue"><a href="#登录状态组件-user-vue" class="headerlink" title="登录状态组件 user.vue"></a>登录状态组件 user.vue</h4><ul>
<li>需求：显示用户登录状态/用户名</li>
<li>思路：在接收请求时，服务器就通过请求带的cookie在Redis中找是否有这个用户，有就返回 user: 用户名 为响应；否则就是 user: ‘’</li>
<li>实现：首先组件的DOM分为两部分，一部分v-if=’username’，里面写this.username，这样有username这个数据的时候显示用户信息的状态；另一部分v-else，里面写立刻登录，就是没有用户信息的时候的状态。在组件的async mounted 去axios.get()一个写好的接口，这个接口能按上述逻辑去获取user（如果没有则 user: ‘’）</li>
</ul>
<p>这里的async mounted写法缺点是页面刷新的时候页面的用户名那里会闪一下才书来数据，如果认为不影响的话可以这么写。也可以用SSR，写成async Data()。写成async fetch的话，把state.user写成得到的user；然后DOM 里写$store.state.user</p>
<p><strong>问：如果在Vue中，没有后台的话怎么实现</strong><br>先在Vuex中，设置state.user=’’。在组件中导入mapState后就能直接用state.user。组件一部分v-if=这个值，里面的用户也是这个值；组件另一部分写v-else，里面写立即登录</p>
<h4 id="城市服务组件-geo-vue"><a href="#城市服务组件-geo-vue" class="headerlink" title="城市服务组件 geo.vue"></a>城市服务组件 geo.vue</h4><ul>
<li>需求：第一次访问网站的时候显示定位是用户目前在的城市。若用户手动修改则x显示新城市</li>
<li>思路：在浏览器向服务器请求首页资源的时候，服务器就解读IP地址找到city，加进首页中，把整个页面响应回去。用Vuex + SSR 就能实现</li>
<li>实现：async fetch中去axios.get()一个接口，能接口能根据IP地址定位city。然后写进store.state中。DOM里写$store.state.city</li>
</ul>
<h4 id="nav-vue组件"><a href="#nav-vue组件" class="headerlink" title="nav.vue组件"></a>nav.vue组件</h4><ul>
<li>需求：用简单的DOM实现下图<br><img src="/img/%E7%BE%8E%E5%9B%A2%E7%BD%91/nav1.png"><br><img src="/img/%E7%BE%8E%E5%9B%A2%E7%BD%91/nav2.png"></li>
<li><strong>DOM的实现</strong>：用了一种障眼法。分为两部分：美团规则，网站导航这些是一部分；里面的展开内容自己是一部分。第一部分用 ul&gt;li 实现；第二部分用一个或多个 dl&gt;dt 实现</li>
</ul>
<h4 id="搜索框-search-vue组件"><a href="#搜索框-search-vue组件" class="headerlink" title="搜索框 search.vue组件"></a>搜索框 search.vue组件</h4><ul>
<li>需求：不focus是普通状态；focus时出现下拉框，显示 热门搜索；输入内容的时候下拉框显示 搜索推荐</li>
<li>实现：<ul>
<li>DOM：input框，热门搜索，搜索推荐是兄弟节点。后两者是通过v-if显示</li>
<li>事件:<ul>
<li>@focus：<code>this.isFocus = true</code></li>
<li>@blur（失去焦点）：<code> let self = this; setTimeout(function() {self.isFocus = false;}, 200);</code>  setTimeout是防止点击推荐内容的同时推荐内容会消失。</li>
<li>@input：访问一个接口，这个接口能根据input的输入内容去返回一些数据。这些数据能v-for生成 搜索推荐</li>
</ul>
</li>
<li>input的v-model=’search’。v-if后面是跟计算属性，计算属性是返回 isFocus 和 search 这两个变量的判断</li>
<li>热门搜索 的数据是直接在Vuex拿，Vuex在 nuxtServerInit 中 axios.get() 接口，并把数据commit到state中；推荐搜索的，是绑定搜索框的input事件，向后端接口获取数据/ search变量的watch</li>
</ul>
</li>
</ul>
<h4 id="菜单-menu-vue组件"><a href="#菜单-menu-vue组件" class="headerlink" title="菜单 menu.vue组件"></a>菜单 menu.vue组件</h4><ul>
<li><p>需求：见下图。鼠标移动到左边的item会出现右边。移开别处则右边会消失；移到右边则不会消失<br><img src="/img/%E7%BE%8E%E5%9B%A2%E7%BD%91/menu.png"></p>
</li>
<li><p>实现：</p>
<ul>
<li><p><strong>DOM</strong>： 左边的部分是 dl&gt;dt（全部分类）&gt;dd（每个栏）；右边的部分是 div&gt;template&gt;h4+span 实现，右边的v-if=’kind’</p>
</li>
<li><p>事件</p>
<ul>
<li><p><strong>左边item的@mouseenter</strong>：<code>this.kind =e.target.querySelector(&#39;i&#39;).classname</code>。e是函数参数（鼠标移动到的节点）</p>
</li>
<li><p>左边的@mouseleave:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> self = <span class="keyword">this</span>; <span class="comment">// 这里用设置timer的方法，让鼠标移动到右框的时候就clearTimeout让右框不消失</span></span><br><span class="line">self._timer = setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;self.kind = <span class="string">''</span>&#125;,<span class="number">150</span>)</span><br></pre></td></tr></table></figure></li>
<li><p>右边的@mouseenter：<code>clearTimeout(this._timer)</code></p>
</li>
<li><p>右边的@mouseleave：<code>this.kind=&#39;&#39;</code></p>
</li>
</ul>
</li>
</ul>
</li>
<li><p>数据结构：见下图。name是左边item的内容，eg. 美食；type用来绑定左边item的class属性（其实是为了便于显示前面的icon）；child是item对应的子栏（右边）<br><img src="/img/%E7%BE%8E%E5%9B%A2%E7%BD%91/menu-data.png"></p>
</li>
<li><p>左边部分直接<code>v-for in $store.state.home.menu</code>生成；右边部分由两层v-for生成，第一层v-for一个计算属性，<strong>该属性</strong> <code>return this.$store.state.home.menu.filter(item =&gt; item.type===this.kind)[0]</code>。所以这个组件的数据也是Vuex在 nuxtServerInit 中 axios.get() 接口，并把数据commit到state中 </p>
<h4 id="城市组件-geo-vue"><a href="#城市组件-geo-vue" class="headerlink" title="城市组件 geo.vue"></a>城市组件 geo.vue</h4><p>功能：一开始是显示用户所在的城市；用户也可以在切换城市页面进行城市切换<br>实现：组件中读取Vuex的State的city<br>Vuex中在action中nuxtServerInit阶段就请求一个接口，这个接口根据IP地址找到城市，返回。Vuex得到城市后就保存在state.city中；<br>而切换城市页面调用的Vuex的mutations的changCity这个方法，会让state.city = city 并localstorage.city = city;<br>同时Vuex的state读取city是 if(localstorage.city) { city = localstorage.city} 如果localstorage没有就读取ip地址的</p>
</li>
</ul>
<h3 id="注册页"><a href="#注册页" class="headerlink" title="注册页"></a>注册页</h3><h4 id="需求"><a href="#需求" class="headerlink" title="需求"></a>需求</h4><p>用户输入昵称和邮箱后，给注册者的邮箱法验证码；输入正确的验证码书和遍密码后，就能进行注册</p>
<h4 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h4><ul>
<li>样式：通过Element UI的 表单-表单验证 实现 样式 和 <strong>校验规则</strong>。eg. 验证前后两次输入的密码是否一致（这个是Element UI的表单验证-自定义校验规则）</li>
<li>发送验证<br>点击发送验证码的按钮之后，绑定的函数会POST注册者的昵称和邮箱给一个接口。这个接口会用腾讯邮件提供的SMTP功能，给注册者的邮箱发送随机生成的验证码，并把验证码、过期时间、验证码和昵称的对应关系放进Redis中</li>
<li>注册<br>点击注册的按钮之后，绑定的函数会POST昵称、用户名、密码（<strong>加密传输</strong>，用<br>crypto-js）和验证码给一个接口。这个接口先校验注册者的验证码和Redis中保存的是否一致，然后从数据库中看改昵称是否被注册等，都符合的话就对新用户信息进写库</li>
</ul>
<h3 id="登录页"><a href="#登录页" class="headerlink" title="登录页"></a>登录页</h3><p>用 passport 验证，据说有固定写法 </p>
<h3 id="切换城市页"><a href="#切换城市页" class="headerlink" title="切换城市页"></a>切换城市页</h3><p>由于数据结构上的麻烦，我们这里点击城市后跳转到首页后，首页的geo.vue那里还是显示ip地址对应的城市。按理应该把Vuex中的city给改一下。</p>
<h4 id="iselect-vue-组件"><a href="#iselect-vue-组件" class="headerlink" title="iselect.vue 组件"></a>iselect.vue 组件</h4><ul>
<li>需求：按 省份+城市 进行下拉框选择；直接搜索</li>
<li>DOM：Element UI的 选择器 和 输入框-远程搜索</li>
<li>实现<ul>
<li>省份 + 城市 下拉框选择：在mounted的时候请求得到省份数据，写进 province 变量中，v-for就能得到省份的下拉框；对省份下拉框v-model=pvalue，对pvalue变量进行watch，有变化就请求城市数据写进 city 变量中</li>
<li>直接搜索。input事件直接访问接口，最好用loadshd的debounce做延时处理<h4 id="按拼音选择的category-vue组件"><a href="#按拼音选择的category-vue组件" class="headerlink" title="按拼音选择的category.vue组件"></a>按拼音选择的category.vue组件</h4></li>
</ul>
</li>
<li>需求：按拼音字母选择城市；点某个字母页面会move到该字母城市所在位置</li>
<li>实现<ul>
<li>DOM: dl&gt;dt&gt;dd</li>
<li>点击字母实现页面滚动。把字母写成超链接<code>&lt;a :href=&quot;# item.id&quot;&gt;</code>，然后后面的栏都写id属性即可</li>
<li>通过async mounted得到后端给我们的中国所有城市的数据后，我们怎么进行<strong>拼音分类</strong>？<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> mounted() &#123;</span><br><span class="line">  <span class="keyword">let</span> self = <span class="keyword">this</span>;</span><br><span class="line">  <span class="keyword">let</span> blocks = [];</span><br><span class="line">  <span class="keyword">let</span> &#123;</span><br><span class="line">    status,</span><br><span class="line">    data: &#123; city &#125;</span><br><span class="line">  &#125; = <span class="keyword">await</span> self.$axios.get(<span class="string">"/geo/city"</span>);</span><br><span class="line">  <span class="comment">// 对全国的城市做拼音处理并分类</span></span><br><span class="line">  <span class="keyword">if</span> (status === <span class="number">200</span>) &#123;</span><br><span class="line">    <span class="keyword">let</span> p;</span><br><span class="line">    <span class="keyword">let</span> c;</span><br><span class="line">    <span class="keyword">let</span> d = &#123;&#125;;</span><br><span class="line">    <span class="comment">// 拼音处理</span></span><br><span class="line">    city.forEach(<span class="function"><span class="params">item</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="comment">// p 变量保存城市名拼音的首位</span></span><br><span class="line">      p = pyjs</span><br><span class="line">        .getFullChars(item.name)</span><br><span class="line">        .toLocaleLowerCase()</span><br><span class="line">        .slice(<span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line">      <span class="comment">// c 变量保存城市名拼音的首位的 ASCII 码。大写的A-Z是65-90，小写的a-z是97-122。我们通过ASCII码进行排序</span></span><br><span class="line">      c = p.charCodeAt(<span class="number">0</span>);</span><br><span class="line">      <span class="keyword">if</span> (c &gt; <span class="number">96</span> &amp;&amp; c &lt; <span class="number">123</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!d[p]) &#123;</span><br><span class="line">          d[p] = [];</span><br><span class="line">        &#125;</span><br><span class="line">        d[p].push(item.name);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="comment">// 整理成我们想要的数据结构</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> [k, v] <span class="keyword">of</span> <span class="built_in">Object</span>.entries(d)) &#123;</span><br><span class="line">      blocks.push(&#123;</span><br><span class="line">        title: k.toUpperCase(),</span><br><span class="line">        city: v</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 排序</span></span><br><span class="line">    blocks.sort(<span class="function">(<span class="params">a, b</span>) =&gt;</span> a.title.charCodeAt(<span class="number">0</span>) - b.title.charCodeAt(<span class="number">0</span>));</span><br><span class="line">    self.block = blocks;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<p><code>for (let [k, v] of Object.entries(d)) {  }</code> 用来<strong>遍历对象，能获取对象的键和键值</strong></p>
<h3 id="产品列表页"><a href="#产品列表页" class="headerlink" title="产品列表页"></a>产品列表页</h3><h4 id="category-vue组件"><a href="#category-vue组件" class="headerlink" title="category.vue组件"></a>category.vue组件</h4><ul>
<li>需求<br><img src="/img/%E7%BE%8E%E5%9B%A2%E7%BD%91/%E5%88%86%E7%B1%BB.png"></li>
<li>实现<ul>
<li>DOM：dl（分类）&gt;dt（全部）&gt;dd（item），item是另一个组件，能有下拉框</li>
</ul>
</li>
</ul>
<h4 id="map-vue-组件"><a href="#map-vue-组件" class="headerlink" title="map.vue 组件"></a>map.vue 组件</h4><ul>
<li>需求：显示产品的地图位置</li>
<li>实现：用高德开放平台的JS API。使用方法见代码、笔记和官方文档</li>
</ul>
<h3 id="详情页"><a href="#详情页" class="headerlink" title="详情页"></a>详情页</h3><p>页面的路由可以是/detail/:id<br>由其他页面跳转到详情页可以有<strong>登录拦截</strong>，实现对登录和没登录的使用者呈现不同页面。实现方法还是获取数据的时候（eg. async Data）的时候请求接口，由接口根据passport判断登录状态</p>
<h3 id="购物车页面"><a href="#购物车页面" class="headerlink" title="购物车页面"></a>购物车页面</h3><p>用Element UI的 表格-多选，表格-自定义 和 计时器 实现。<br>纯手写实现的购物车见 /mycart 页面 。两个注意点:</p>
<ul>
<li>checkbox的v-model=item.choose（源数据的item可以没有choose这个属性）</li>
<li>@click=item.count++ 可以直接写在html中，方便获取具体的item.count</li>
</ul>
<h2 id="后端与数据库"><a href="#后端与数据库" class="headerlink" title="后端与数据库"></a>后端与数据库</h2><p>说实话，还是不太熟，只能大概看懂；数据库操作相关的接口也只能大概看懂</p>
<h3 id="接口设计"><a href="#接口设计" class="headerlink" title="接口设计"></a>接口设计</h3><p>见笔记和代码吧，这部分只能大概看懂而已<br>其中一些接口（eg. 由ip地址定位，中国所有城市的数据）是访问作者写的一个接口实现的</p>
<h2 id="一些样式"><a href="#一些样式" class="headerlink" title="一些样式"></a>一些样式</h2><h2 id="一些技巧"><a href="#一些技巧" class="headerlink" title="一些技巧"></a>一些技巧</h2><ul>
<li><p>用lodash的debounce做函数延时执行的处理。第一个参数是要执行的函数，第二个参数是延迟的毫秒数。使用方法如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> _ <span class="keyword">from</span> <span class="string">"lodash"</span>;</span><br><span class="line">...</span><br><span class="line">method: &#123;</span><br><span class="line">    test: _.debounce(<span class="keyword">async</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; ... &#125;, <span class="number">200</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p><code>for (let [k, v] of Object.entries(d)) {  }</code> 用来<strong>遍历对象，能获取对象的键和键值</strong></p>
</li>
</ul>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>项目小结</tag>
      </tags>
  </entry>
  <entry>
    <title>Web Component</title>
    <url>/2019/10/23/Web%20Component/</url>
    <content><![CDATA[<p><a href="http://www.ruanyifeng.com/blog/2019/08/web_components.html" target="_blank" rel="noopener">http://www.ruanyifeng.com/blog/2019/08/web_components.html</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>Web</tag>
      </tags>
  </entry>
  <entry>
    <title>React - HOC</title>
    <url>/2020/05/16/React%20-%20HOC/</url>
    <content><![CDATA[<p>本文大部分来自<a href="http://www.conardli.top/blog/article/React%E6%B7%B1%E5%85%A5%E7%B3%BB%E5%88%97/%E4%BB%8EMixin%E5%88%B0HOC%E5%86%8D%E5%88%B0Hook%EF%BC%88%E4%B8%80%EF%BC%89.html#%E5%AF%BC%E8%AF%BB" target="_blank" rel="noopener">ConardLi 的 blog</a>，建议直接看原博客</p>
<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><h2 id="Mixin-的危害"><a href="#Mixin-的危害" class="headerlink" title="Mixin 的危害"></a>Mixin 的危害</h2><p>React 官方文档在<a href="https://react.docschina.org/blog/2016/07/13/mixins-considered-harmful.html" target="_blank" rel="noopener">Mixins Considered Harmful</a>一文中提到了 Mixin 带来了危害：</p>
<ul>
<li>Mixin 可能会相互依赖，相互耦合，不利于代码维护</li>
<li>不同的 Mixin 中的方法可能会相互冲突</li>
<li>Mixin 非常多时，组件是可以感知到的，甚至还要为其做相关处理，这样会给代码造成滚雪球式的复杂性</li>
</ul>
<p>React 现在已经不再推荐使用 Mixin 来解决<strong>代码复用</strong>问题，因为 Mixin 带来的危害比他产生的价值还要巨大，并且 React 全面推荐使用高阶组件来替代它</p>
<h2 id="HOC-简介"><a href="#HOC-简介" class="headerlink" title="HOC 简介"></a>HOC 简介</h2><p><a href="https://zh-hans.reactjs.org/docs/higher-order-components.html" target="_blank" rel="noopener">HOC-官方文档</a><br>高阶组件（HOC）是 React 中用于复用组件逻辑的一种高级技巧。HOC 自身<strong>不是 React API 的一部分</strong>，它是一种基于 React 的组合特性而形成的<strong>设计模式</strong></p>
<p>具体而言，高阶组件是参数为组件，返回值为新组件的函数。实现在原有组件的基础上对其新增功能和行为</p>
<h1 id="HOC-的实现方式"><a href="#HOC-的实现方式" class="headerlink" title="HOC 的实现方式"></a>HOC 的实现方式</h1><h2 id="属性代理"><a href="#属性代理" class="headerlink" title="属性代理"></a>属性代理</h2><p>常用方式，一般是在 render() 返回被包裹的组件，并把需要的 props 传递给它</p>
<h3 id="一个简单的例子："><a href="#一个简单的例子：" class="headerlink" title="一个简单的例子："></a>一个简单的例子：</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> visibleHOC = <span class="function">(<span class="params">InnerComponent</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">    render() &#123;</span><br><span class="line">      <span class="keyword">const</span> &#123; visible, ...restProps &#125; = <span class="keyword">this</span>.props</span><br><span class="line">      <span class="keyword">if</span> (visible === <span class="literal">false</span>) <span class="keyword">return</span> <span class="literal">null</span></span><br><span class="line">      <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">InnerComponent</span> &#123;<span class="attr">...restProps</span>&#125; /&gt;</span></span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="另一个例子"><a href="#另一个例子" class="headerlink" title="另一个例子"></a>另一个例子</h3><p>假设有一个组件要在 localStorage 拿数据，然后渲染出来，它的代码应该差不多长这样：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyComponent</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">  componentWillMount() &#123;</span><br><span class="line">    <span class="keyword">let</span> data = localStorage.getItem(<span class="string">'data'</span>)</span><br><span class="line">    <span class="keyword">this</span>.setState(&#123; data &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span>&#123;this.state.data&#125;<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果有很多其他组件也需要从 LocalStorage 中获取同样的数据展示出来时，那我要写很多次这段代码，为了效率我决定写用装饰器模式包装一下，所以就有了高阶组件（高阶组件本质是函数，所以这里写成函数）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">localHOC</span>(<span class="params">InnerComponent</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">    componentWillMount() &#123;</span><br><span class="line">      <span class="keyword">let</span> data = localStorage.getItem(<span class="string">'data'</span>)</span><br><span class="line">      <span class="keyword">this</span>.setState(&#123; data &#125;)</span><br><span class="line">    &#125;</span><br><span class="line">    render() &#123;</span><br><span class="line">      <span class="keyword">return</span> (</span><br><span class="line">        <span class="comment">// 通过&#123;...this.props&#125; 把传递给当前组件的属性继续传递给被包装的组件WrappedComponent</span></span><br><span class="line">        &lt;InnerComponent data=&#123;<span class="keyword">this</span>.state.data&#125; &#123;...this.props&#125; /&gt;</span><br><span class="line">      )</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对比原生组件增强的项：</p>
<ul>
<li>可操作所有传入的 props</li>
<li>可操作组件的生命周期</li>
<li>可操作组件的 static 方法</li>
<li>获取 refs</li>
</ul>
<h2 id="反向继承"><a href="#反向继承" class="headerlink" title="反向继承"></a>反向继承</h2><p>返回一个组件，继承原组件，在 render 中用 super.render() 调用原组件的 render。由于继承了原组件，能通过 this 访问到原组件的生命周期、props、state、render 等，相比属性代理它能操作更多的属性</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">inheritHOC</span>(<span class="params">InnerComponent</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="keyword">extends</span> <span class="title">InnerComponent</span> </span>&#123;</span><br><span class="line">    render() &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">super</span>.render()</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对比原生组件增强的项：</p>
<ul>
<li>可操作所有传入的 props</li>
<li>可操作组件的生命周期</li>
<li>可操作组件的 static 方法</li>
<li>获取 refs</li>
<li>可操作 state</li>
<li>可以渲染劫持</li>
</ul>
<h1 id="HOC-可以实现什么功能"><a href="#HOC-可以实现什么功能" class="headerlink" title="HOC 可以实现什么功能"></a>HOC 可以实现什么功能</h1><h2 id="组合渲染（常用）"><a href="#组合渲染（常用）" class="headerlink" title="组合渲染（常用）"></a>组合渲染（常用）</h2><p>可使用任何其他组件和原组件进行组合渲染，达到样式、布局复用等效果</p>
<p>eg. 一个在外层给组件包裹 Antd 的 Card 的 HOC。这里用了函数组件的写法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; Card &#125; <span class="keyword">from</span> <span class="string">'antd'</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> CardHOC = <span class="function">(<span class="params">InnerComponent</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function">(<span class="params">props</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; title = <span class="literal">null</span>, ...restProps &#125; = props</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">      &lt;Card title=&#123;title&#125;&gt;</span><br><span class="line">        &lt;InnerComponent &#123;...restProps&#125; /&gt;</span><br><span class="line">      &lt;<span class="regexp">/Card&gt;</span></span><br><span class="line"><span class="regexp">    )</span></span><br><span class="line"><span class="regexp">  &#125;</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>通过反向继承再实现一次。这里是 class 组件</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> CardHOC = <span class="function">(<span class="params">InnerComponent</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="keyword">extends</span> <span class="title">InnerComponent</span> </span>&#123;</span><br><span class="line">    render() &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">Card</span> <span class="attr">title</span>=<span class="string">&#123;this.props.title&#125;</span>&gt;</span>&#123;super.render()&#125;<span class="tag">&lt;/<span class="name">Card</span>&gt;</span></span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="条件渲染（常用）"><a href="#条件渲染（常用）" class="headerlink" title="条件渲染（常用）"></a>条件渲染（常用）</h2><p>根据特定的属性决定原组件是否渲染。第一个 DEMO 就是这个</p>
<h2 id="操作-props"><a href="#操作-props" class="headerlink" title="操作 props"></a>操作 props</h2><p>可以对传入组件的 props 进行增加、修改、删除或者根据特定的 props 进行特殊的操作</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> propsHOC = <span class="function">(<span class="params">InnerComponent</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function">(<span class="params">props</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> newProps = &#123;</span><br><span class="line">      ...props,</span><br><span class="line">      user_name: <span class="string">'Jacle'</span>,</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">InnerComponent</span> &#123;<span class="attr">...newProps</span>&#125; /&gt;</span></span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="获取-ref"><a href="#获取-ref" class="headerlink" title="获取 ref"></a>获取 ref</h2><p>高阶组件中可获取原组件的<a href="https://react.docschina.org/docs/refs-and-the-dom.html" target="_blank" rel="noopener">ref</a>，通过 ref 获取组件实例，如下面的代码，当程序初始化完成后调用原组件的 log 方法 （默认情况下，你不能在函数组件上使用 ref 属性，因为它们没有实例）。适合用 ref 的情况：管理 input 的 focus；触发强制动画；…</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">refHOC</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">    componentDidMount() &#123;</span><br><span class="line">      <span class="keyword">this</span>.wapperRef.log()</span><br><span class="line">    &#125;</span><br><span class="line">    render() &#123;</span><br><span class="line">      <span class="keyword">return</span> (</span><br><span class="line">        &lt;WrappedComponent</span><br><span class="line">          &#123;...this.props&#125;</span><br><span class="line">          ref=&#123;(ref) =&gt; &#123;</span><br><span class="line">            <span class="keyword">this</span>.wapperRef = ref</span><br><span class="line">          &#125;&#125;</span><br><span class="line">        /&gt;</span><br><span class="line">      )</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里注意：调用高阶组件的时候并不能获取到原组件的真实 ref，需要手动进行传递。详见<a href="https://react.docschina.org/docs/forwarding-refs.html" target="_blank" rel="noopener">refs 转发</a></p>
<h2 id="状态管理"><a href="#状态管理" class="headerlink" title="状态管理"></a>状态管理</h2><p>将原组件的状态提取到 HOC 中进行管理，如下面的代码，我们将 Input 的 value 提取到 HOC 中进行管理，使它变成受控组件，同时不影响它使用 onChange 方法进行一些其他操作。基于这种方式，我们可以实现一个简单的双向绑定</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">proxyHoc</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">    <span class="keyword">constructor</span>(props) &#123;</span><br><span class="line">      <span class="keyword">super</span>(props)</span><br><span class="line">      <span class="keyword">this</span>.state = &#123; <span class="attr">value</span>: <span class="string">''</span> &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    onChange = <span class="function">(<span class="params">event</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">const</span> &#123; onChange &#125; = <span class="keyword">this</span>.props</span><br><span class="line">      <span class="keyword">this</span>.setState(</span><br><span class="line">        &#123;</span><br><span class="line">          value: event.target.value,</span><br><span class="line">        &#125;,</span><br><span class="line">        () =&gt; &#123;</span><br><span class="line">          <span class="keyword">if</span> (<span class="keyword">typeof</span> onChange === <span class="string">'function'</span>) &#123;</span><br><span class="line">            onChange(event)</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      )</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    render() &#123;</span><br><span class="line">      <span class="keyword">const</span> newProps = &#123;</span><br><span class="line">        value: <span class="keyword">this</span>.state.value,</span><br><span class="line">        onChange: <span class="keyword">this</span>.onChange,</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">WrappedComponent</span> &#123;<span class="attr">...this.props</span>&#125; &#123;<span class="attr">...newProps</span>&#125; /&gt;</span></span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HOC</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">  render() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">input</span> &#123;<span class="attr">...this.props</span>&#125;&gt;</span><span class="tag">&lt;/<span class="name">input</span>&gt;</span></span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> proxyHoc(HOC)</span><br></pre></td></tr></table></figure>

<h2 id="操作-state"><a href="#操作-state" class="headerlink" title="操作 state"></a>操作 state</h2><p>上面的例子通过属性代理利用 HOC 的 state 对原组件进行了一定的增强，但并不能直接控制原组件的 state，而通过<strong>反向继承</strong>，我们可以直接操作原组件的 state。但是并<strong>不推荐直接修改或添加原组件的 state</strong>，因为这样有可能和组件内部的操作构成冲突</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">debugHOC</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="keyword">extends</span> <span class="title">WrappedComponent</span> </span>&#123;</span><br><span class="line">    render() &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">'props'</span>, <span class="keyword">this</span>.props)</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">'state'</span>, <span class="keyword">this</span>.state)</span><br><span class="line">      <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">div</span> <span class="attr">className</span>=<span class="string">"debuging"</span>&gt;</span>&#123;super.render()&#125;<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="渲染劫持"><a href="#渲染劫持" class="headerlink" title="渲染劫持"></a>渲染劫持</h2><p>通过反向继承的方式，可直接增强（而不是更改）由原组件 render 函数产生的 React 元素，eg：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">hijackHOC</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="keyword">extends</span> <span class="title">WrappedComponent</span> </span>&#123;</span><br><span class="line">    render() &#123;</span><br><span class="line">      <span class="keyword">const</span> tree = <span class="keyword">super</span>.render() <span class="comment">// render函数内实际上是调用React.creatElement产生的React元素。它所有属性的writable特性均被配置为了false，即所有属性是不可变的。所以我们只能重新创造一个元素，用React.cloneElement()</span></span><br><span class="line">      <span class="keyword">let</span> newProps = &#123;&#125;</span><br><span class="line">      <span class="keyword">if</span> (tree &amp;&amp; tree.type === <span class="string">'input'</span>) &#123;</span><br><span class="line">        newProps = &#123; <span class="attr">value</span>: <span class="string">'渲染被劫持了'</span> &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">const</span> props = <span class="built_in">Object</span>.assign(&#123;&#125;, tree.props, newProps)</span><br><span class="line">      <span class="keyword">const</span> newTree = React.cloneElement(tree, props, tree.props.children)</span><br><span class="line">      <span class="keyword">return</span> newTree</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>React.cloneElement()克隆并返回一个新的 React 元素，使用 element 作为起点。生成的元素将会拥有原始元素 props 与新 props 的浅合并。新的子级会替换现有的子级。来自原始元素的 key 和 ref 将会保留。</p>
</blockquote>
<h1 id="HOC-的优雅写法"><a href="#HOC-的优雅写法" class="headerlink" title="HOC 的优雅写法"></a>HOC 的优雅写法</h1><h2 id="compose"><a href="#compose" class="headerlink" title="compose"></a>compose</h2><p>在实际应用中，一个组件可能被多个 HOC 增强，我们使用的是被所有的 HOC 增强后的组件。直接写的话代码可读性会很差：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> newComponent = aHOC(bHOC(cHOC(InnerComponent)))</span><br></pre></td></tr></table></figure>

<p>我们可以用类似柯里化的方式，手动封装一个简单的函数组合工具，将写法改写如下</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> compose = <span class="function">(<span class="params">...fns</span>) =&gt;</span> fns.reduce(<span class="function">(<span class="params">f, g</span>) =&gt;</span> <span class="function">(<span class="params">...args</span>) =&gt;</span> g(f(...args)))</span><br><span class="line">compose(aHOC, bHOC, cHOC)(InnerComponent)</span><br></pre></td></tr></table></figure>

<h2 id="ES7-的-Decorators"><a href="#ES7-的-Decorators" class="headerlink" title="ES7 的 Decorators"></a>ES7 的 Decorators</h2><p><a href="https://es6.ruanyifeng.com/#docs/decorator" target="_blank" rel="noopener">ES7-Decorators</a></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">aHOC</span>(<span class="params">InnerComponent</span>) </span>&#123; ... &#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bHOC</span>(<span class="params">InnerComponent</span>) </span>&#123; ... &#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">cHOC</span>(<span class="params">InnerComponent</span>) </span>&#123; ... &#125;</span><br><span class="line"></span><br><span class="line">@aHOC</span><br><span class="line">@bHOC</span><br><span class="line">@cHOC</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">InnerComponent</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 上面这个InnerComponent等同于</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">InnerComponent</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;&#125;</span><br><span class="line">InnerComponent = aHOC(InnerComponent) || InnerComponent</span><br></pre></td></tr></table></figure>

<p>Decorators 是 ES7 的一个提案，还没有被标准化，但目前 Babel 转码器已经支持，我们需要提前配置 babel-plugin-transform-decorators-legacy：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">"plugins"</span>: [<span class="string">"transform-decorators-legacy"</span>]</span><br></pre></td></tr></table></figure>

<p>还可以结合上面的 compose 函数使用：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> hoc = compose(aHOC, bHOC, cHOC)</span><br><span class="line">@hoc</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Input</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="使用-HOC-的注意事项"><a href="#使用-HOC-的注意事项" class="headerlink" title="使用 HOC 的注意事项"></a>使用 HOC 的注意事项</h1><ul>
<li>静态属性拷贝<br>当我们应用 HOC 去增强另一个组件时，我们实际使用的组件已经不是原组件了，所以我们拿不到原组件的任何静态属性，我们可以在 HOC 的结尾手动拷贝他们：</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">proxyHOC</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line">  <span class="class"><span class="keyword">class</span> <span class="title">HOCComponent</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">    render() &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">WrappedComponent</span> &#123;<span class="attr">...this.props</span>&#125; /&gt;</span></span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  HOCComponent.staticMethod = WrappedComponent.staticMethod</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">  <span class="keyword">return</span> HOCComponent</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果原组件有非常多的静态属性，这个过程是非常痛苦的，而且你需要去了解需要增强的所有组件的静态属性是什么，我们可以使用<a href="https://github.com/mridgway/hoist-non-react-statics" target="_blank" rel="noopener">hoist-non-react-statics</a>来帮助我们解决这个问题，它可以自动帮我们拷贝所有非 React 的静态方法，使用方式如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> hoistNonReactStatic <span class="keyword">from</span> <span class="string">'hoist-non-react-statics'</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">proxyHOC</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line">  <span class="class"><span class="keyword">class</span> <span class="title">HOCComponent</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">    render() &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">WrappedComponent</span> &#123;<span class="attr">...this.props</span>&#125; /&gt;</span></span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  hoistNonReactStatic(HOCComponent, WrappedComponent)</span><br><span class="line">  <span class="keyword">return</span> HOCComponent</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>Refs 不会被传递给被包装组件</li>
<li>不要在组件的 render 方法中使用高阶组件，尽量也不要在组件的其他生命周期方法中使用高阶组件。因为高阶组件每次都会返回一个新的组件，在 render 中使用会导致每次渲染出来的组件都不相等（===），于是每次 render，组件都会卸载（unmount），然后重新挂载（mount），既影响了效率，又丢失了组件及其子组件的状态。高阶组件最适合使用的地方是在组件定义的外部，这样就不会受到组件生命周期的影响了</li>
<li>约定：不要改变原始组件。高阶组件应该是一个没有副作用的纯函数 (函数的调用参数相同，则永远返回相同的结果)</li>
<li>约定：透传不相关的 props。即 props 传递</li>
<li>约定：最好使用<a href="https://zh-hans.reactjs.org/docs/higher-order-components.html#convention-wrap-the-display-name-for-easy-debugging" target="_blank" rel="noopener">displayName</a>，方便调试。官方推荐使用 HOCName(WrappedComponentName)。eg. 赋予 return 组件一个静态属性：</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> displayName = <span class="string">`aHOC(<span class="subst">$&#123;WrappedComponent.displayName&#125;</span>)`</span></span><br></pre></td></tr></table></figure>

<h1 id="实际应用场景"><a href="#实际应用场景" class="headerlink" title="实际应用场景"></a>实际应用场景</h1><h2 id="逻辑复用"><a href="#逻辑复用" class="headerlink" title="逻辑复用"></a>逻辑复用</h2><p>多个组件拥有类似的逻辑，我们要对重复的逻辑进行复用。eg，上面写过的 localstorage 的例子</p>
<h2 id="可用、权限控制"><a href="#可用、权限控制" class="headerlink" title="可用、权限控制"></a>可用、权限控制</h2><p>当没权限的时候不展示某个组件，就是上面提到的组合渲染</p>
<h2 id="表单校验"><a href="#表单校验" class="headerlink" title="表单校验"></a>表单校验</h2><p>eg. <a href="https://semi.bytedance.net/zh-CN/components/form" target="_blank" rel="noopener">Semi</a> 中用 withField 封装自定义表单控件<br>通过 withField，你可以将其他自定义组件扩展成为表单控件，由 Form 接管其行为（value, onChange; 还有在上方插入Field 的<code>&lt;Label&gt;</code>， 下方插入 Field 的<code>&lt;ErrorMessage&gt;</code>等）</p>
<p>注意：自定义组件必须为受控组件，关于受控组件/非受控组件请参考<a href="https://zh-hans.reactjs.org/docs/glossary.html#%E5%8F%97%E6%8E%A7%E7%BB%84%E4%BB%B6-vs-%E9%9D%9E%E5%8F%97%E6%8E%A7%E7%BB%84%E4%BB%B6" target="_blank" rel="noopener">react docs</a></p>
<h2 id="Redux-的-connect"><a href="#Redux-的-connect" class="headerlink" title="Redux 的 connect"></a>Redux 的 <a href="https://github.com/reduxjs/react-redux/blob/master/docs/api/connect.md#connect" target="_blank" rel="noopener">connect</a></h2><p><img src="/img/React-HOC/connect.png"><br>下面就是一个简化版的connect实现:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">xport <span class="keyword">const</span> connect = <span class="function">(<span class="params">mapStateToProps, mapDispatchToProps</span>) =&gt;</span> <span class="function">(<span class="params">WrappedComponent</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="class"><span class="keyword">class</span> <span class="title">Connect</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> contextTypes = &#123;</span><br><span class="line">      store: PropTypes.object</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">constructor</span> () &#123;</span><br><span class="line">      <span class="keyword">super</span>()</span><br><span class="line">      <span class="keyword">this</span>.state = &#123;</span><br><span class="line">        allProps: &#123;&#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    componentWillMount () &#123;</span><br><span class="line">      <span class="keyword">const</span> &#123; store &#125; = <span class="keyword">this</span>.context</span><br><span class="line">      <span class="keyword">this</span>._updateProps()</span><br><span class="line">      store.subscribe(<span class="function"><span class="params">()</span> =&gt;</span> <span class="keyword">this</span>._updateProps())</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    _updateProps () &#123;</span><br><span class="line">      <span class="keyword">const</span> &#123; store &#125; = <span class="keyword">this</span>.context</span><br><span class="line">      <span class="keyword">let</span> stateProps = mapStateToProps ? mapStateToProps(store.getState(), <span class="keyword">this</span>.props): &#123;&#125; </span><br><span class="line">      <span class="keyword">let</span> dispatchProps = mapDispatchToProps? mapDispatchToProps(store.dispatch, <span class="keyword">this</span>.props) : &#123;&#125; </span><br><span class="line">      <span class="keyword">this</span>.setState(&#123;</span><br><span class="line">        allProps: &#123;</span><br><span class="line">          ...stateProps,</span><br><span class="line">          ...dispatchProps,</span><br><span class="line">          ...this.props</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    render () &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">WrappedComponent</span> &#123;<span class="attr">...this.state.allProps</span>&#125; /&gt;</span></span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> Connect</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>connect函数其实就做了一件事: 将mapStateToProps和mapDispatchToProps分别解构后传给原组件，这样我们在原组件内就可以直接用props获取state以及dispatch函数了</p>
<h1 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h1><ul>
<li>嵌套太多 HOC，代码可读性会比较差</li>
<li>HOC 可以劫持 props，在不遵守约定的情况下也可能造成冲突</li>
</ul>
<p>参考资料<br><a href="http://www.conardli.top/blog/article/React%E6%B7%B1%E5%85%A5%E7%B3%BB%E5%88%97/%E4%BB%8EMixin%E5%88%B0HOC%E5%86%8D%E5%88%B0Hook%EF%BC%88%E4%B8%80%EF%BC%89.html#%E5%AF%BC%E8%AF%BB" target="_blank" rel="noopener">ConardLi 的 blog</a><br><a href="https://zh-hans.reactjs.org/docs/higher-order-components.html" target="_blank" rel="noopener">官网</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>框架</tag>
      </tags>
  </entry>
  <entry>
    <title>Web安全</title>
    <url>/2019/10/27/Web%E5%AE%89%E5%85%A8/</url>
    <content><![CDATA[<h2 id="XSS"><a href="#XSS" class="headerlink" title="XSS"></a>XSS</h2><h3 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h3><p><strong>跨站脚本攻击</strong>(Cross Site Scripting)，缩写为XSS（避免和CSS混淆就没缩写成CSS）。恶意攻击者往Web页面里插入恶意Script代码，当用户浏览该页之时，嵌入其中Web里面的Script代码会被执行，从而达到恶意攻击用户的目的</p>
<h3 id="XSS攻击注入点"><a href="#XSS攻击注入点" class="headerlink" title="XSS攻击注入点"></a>XSS攻击注入点</h3><h4 id="HTML节点内容"><a href="#HTML节点内容" class="headerlink" title="HTML节点内容"></a>HTML节点内容</h4><p>节点中包含用户输入的内容。例如</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span>#&#123;content&#125;<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">// content能被输入成包含一段脚本</span><br><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span><span class="tag">&lt;<span class="name">script</span>&gt;</span> <span class="tag">&lt;/<span class="name">script</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="HTML属性"><a href="#HTML属性" class="headerlink" title="HTML属性"></a>HTML属性</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 例如，这个img的src的属性来自用户的输入或者是数据库存储的用户之前提交的</span></span><br><span class="line">&lt;img src=<span class="string">"#&#123;img&#125;"</span>/&gt;</span><br><span class="line"><span class="comment">// 如果用户输入的时候输入了 1" onerror="alert(1) ，则能被执行成</span></span><br><span class="line">&lt;img src=<span class="string">"1"</span> onerror=<span class="string">"alert(1)"</span>/&gt;</span><br></pre></td></tr></table></figure>

<h4 id="JS代码"><a href="#JS代码" class="headerlink" title="JS代码"></a>JS代码</h4><p>跟之前的原理一样，某个属性是来自于用户的输入，用户可以通过输入引号的方式增加一些代码</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> data = <span class="string">"#&#123;data&#125;"</span></span><br><span class="line"><span class="comment">//  能被用户输入写成</span></span><br><span class="line"><span class="keyword">var</span> data = <span class="string">"hello"</span>;alert(<span class="number">1</span>);<span class="string">""</span>;</span><br></pre></td></tr></table></figure>

<h4 id="富文本"><a href="#富文本" class="headerlink" title="富文本"></a>富文本</h4><p>写邮件的时候我们能定义字体的各种格式，提交的时候其实就是提交这一段的html。提交这么多一段html是有XSS攻击风险的。这种情况要用白名单防御方法</p>
<h3 id="攻击类型"><a href="#攻击类型" class="headerlink" title="攻击类型"></a>攻击类型</h3><ul>
<li><strong>反射型</strong>： 通过<strong>修改 URL 参数</strong>的方式加入攻击代码，诱导用户访问链接从而进行攻击<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// http://www.domain.com?name=&lt;script&gt;alert(1)&lt;/script&gt;</span></span><br><span class="line">&lt;div&gt;&#123;&#123;name&#125;&#125;&lt;<span class="regexp">/div&gt;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<p>上述 URL 输入可能会将 HTML 改为<code>&lt;div&gt;&lt;script&gt;alert(1)&lt;/script&gt;&lt;/div&gt;</code> ，这样页面中就凭空多了一段可执行脚本。这种攻击类型是反射型攻击，也可以说是 DOM-based 攻击<br>还有另一个例子见<a href="https://blog.csdn.net/ganyingxie123456/article/details/70230486" target="_blank" rel="noopener">Web安全之XSS攻防</a></p>
<ul>
<li><strong>存储型</strong>: 攻击的代码被服务端写入进数据库中。比如在一些论坛的评论中，写script标签加alert语句，如果前后端没有做好防御的话，这段评论就会被存储到数据库中，这样每个打开该页面的用户都会被攻击到</li>
</ul>
<h3 id="防御措施"><a href="#防御措施" class="headerlink" title="防御措施"></a>防御措施</h3><h4 id="转义字符（普遍）"><a href="#转义字符（普遍）" class="headerlink" title="转义字符（普遍）"></a>转义字符（普遍）</h4><p>转义输入输出的内容，对于引号，尖括号，斜杠进行转义</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">escape</span>(<span class="params">str</span>) </span>&#123;</span><br><span class="line">	str = str.replace(<span class="regexp">/&amp;/g</span>, <span class="string">"&amp;amp;"</span>);</span><br><span class="line">	str = str.replace(<span class="regexp">/&lt;/g</span>, <span class="string">"&amp;lt;"</span>);</span><br><span class="line">	str = str.replace(<span class="regexp">/&gt;/g</span>, <span class="string">"&amp;gt;"</span>);</span><br><span class="line">	str = str.replace(<span class="regexp">/"/g</span>, <span class="string">"&amp;quto;"</span>);</span><br><span class="line">	str = str.replace(<span class="regexp">/'/g</span>, <span class="string">"&amp;##39;"</span>);</span><br><span class="line">	str = str.replace(<span class="regexp">/`/g</span>, <span class="string">"&amp;##96;"</span>);</span><br><span class="line">    str = str.replace(<span class="regexp">/\//g</span>, <span class="string">"&amp;##x2F;"</span>);</span><br><span class="line">    <span class="keyword">return</span> str</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通过转义可以将攻击代码<code>&lt;script&gt;alert(1)&lt;/script&gt;</code>变成</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// -&gt; &amp;lt;script&amp;gt;alert(1)&amp;lt;&amp;##x2F;script&amp;gt;</span></span><br><span class="line"><span class="built_in">escape</span>(<span class="string">'&lt;script&gt;alert(1)&lt;/script&gt;'</span>)</span><br></pre></td></tr></table></figure>

<h4 id="CSP白名单"><a href="#CSP白名单" class="headerlink" title="CSP白名单"></a>CSP白名单</h4><p>对于显示富文本来说，不能通过上面的办法来转义所有字符，因为这样会把需要的格式也过滤掉。这种情况通常采用白名单过滤的办法，明确告诉浏览器哪些外部资源可以加载和执行。<br>通过两种方式来开启 CSP：</p>
<ul>
<li>设置 HTTP Header 中的 Content-Security-Policy: ‘某某策略’<blockquote>
<p>写法以多个键值对的形式存在，键和值用空格隔开，不同对用分号分开。键的类型有限制全局default-src，资源类型的connect-src、mainfest-src、img-src、font-src、media-src、style-src、frame-src、script-src… ； 值可以写成 * ，或某个url</p>
</blockquote>
</li>
<li>设置 meta 标签的方式 <code>&lt;meta http-equiv=&quot;Content-Security-Policy&quot; content=&quot;某某策略&quot;&gt;</code></li>
</ul>
<h2 id="CSRF"><a href="#CSRF" class="headerlink" title="CSRF"></a>CSRF</h2><h3 id="概念及攻击原理"><a href="#概念及攻击原理" class="headerlink" title="概念及攻击原理"></a>概念及攻击原理</h3><p><strong>跨站请求伪造</strong>（Cross-site request forgery）,<br>CSRF攻击的本质在于<strong>利用用户的身份，执行非本意的操作</strong>。重点在于：<strong>CSRF的请求是跨域且伪造的</strong><br>跨站请求伪造的攻击是攻击者通过一些技术手段欺骗用户的浏览器去访问用户曾经认证过的网站并执行一些操作（如发送邮件、发消息、甚至财产操作如转账和购买商品等。攻击者的网站借用用户身份向目标网站发送一些请求）。由于浏览器曾经认证过，所以被访问的网站会认为是真正的用户操作而去执行。这利用了web登录身份认证的一个漏洞：<strong>简单的身份认证只能保证请求来自用户的浏览器，但不能识别请求是用户自愿发出的</strong></p>
<blockquote>
<p>假设网站中有一个通过 Get 请求提交用户评论的接口，那么攻击者就可以在钓鱼网站中加入一个图片，图片的地址就是评论接口</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;img src=<span class="string">"http://www.domain.com/xxx?comment='attack'"</span>/&gt;</span><br></pre></td></tr></table></figure>

<h3 id="如何防范"><a href="#如何防范" class="headerlink" title="如何防范"></a>如何防范</h3><p>可以遵循以下几种规则：</p>
<ol>
<li>Get 请求不对数据进行修改</li>
<li>不让第三方网站访问到用户 Cookie</li>
<li>阻止第三方网站请求接口</li>
<li>请求时附带验证信息，比如验证码或者 Token</li>
</ol>
<h4 id="对Cookie设置SameSite属性"><a href="#对Cookie设置SameSite属性" class="headerlink" title="对Cookie设置SameSite属性"></a>对Cookie设置SameSite属性</h4><p>该属性表示 Cookie 不随着跨域请求发送，可以很大程度减少 CSRF 的攻击，但是该属性目前并不是所有浏览器都兼容</p>
<h4 id="验证Referer"><a href="#验证Referer" class="headerlink" title="验证Referer"></a>验证Referer</h4><p>可以通过验证 Referer 来判断该请求是否为第三方网站发起的</p>
<h4 id="利用Token"><a href="#利用Token" class="headerlink" title="利用Token"></a>利用Token</h4><p>服务器下发一个随机 Token，每次发起请求时将 Token 携带上，服务器验证 Token 是否有效</p>
<h2 id="Cookie"><a href="#Cookie" class="headerlink" title="Cookie"></a>Cookie</h2><h3 id="Cookie的安全策略"><a href="#Cookie的安全策略" class="headerlink" title="Cookie的安全策略"></a>Cookie的安全策略</h3><table>
<thead>
<tr>
<th align="center">属性</th>
<th align="center">作用</th>
</tr>
</thead>
<tbody><tr>
<td align="center">value</td>
<td align="center">如果用于保存用户登录态，应该将该值加密，不能使用明文的用户标识</td>
</tr>
<tr>
<td align="center">http-only</td>
<td align="center">不能通过 JS 访问 Cookie，减少 XSS 攻击</td>
</tr>
<tr>
<td align="center">secure</td>
<td align="center">只能在协议为 HTTPS 的请求中携带</td>
</tr>
<tr>
<td align="center">same-site</td>
<td align="center">规定浏览器不能在跨域请求中携带 Cookie，减少 CSRF 攻击</td>
</tr>
</tbody></table>
<h2 id="点击劫持"><a href="#点击劫持" class="headerlink" title="点击劫持"></a>点击劫持</h2><h3 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h3><p>点击劫持是一种视觉欺骗的攻击手段。攻击者将需要攻击的网站通过 iframe 嵌套的方式嵌入自己的网页中，并将 iframe 设置为透明，在页面中透出一个按钮诱导用户点击。</p>
<h3 id="防范措施"><a href="#防范措施" class="headerlink" title="防范措施"></a>防范措施</h3><p><strong>设置HTTP头：X-FRAME-OPTIONS</strong><br>这个 HTTP 响应头 就是为了防御用 iframe 嵌套的点击劫持攻击<br>该响应头有三个值可选，分别是</p>
<ul>
<li>DENY，表示页面不允许通过 iframe 的方式展示</li>
<li>SAMEORIGIN，表示页面可以在相同域名下通过 iframe 的方式展示</li>
<li>ALLOW-FROM，表示页面可以在指定来源的 iframe 中展示</li>
</ul>
<h3 id="JS-防御"><a href="#JS-防御" class="headerlink" title="JS 防御"></a>JS 防御</h3><p>对于某些远古浏览器来说，并不能支持上面的这种方式，那我们只有通过 JS 的方式来防御点击劫持了<br>当通过 iframe 的方式加载页面时，攻击者的网页直接不显示所有内容了</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;head&gt;</span><br><span class="line">  &lt;style id=<span class="string">"click-jack"</span>&gt;</span><br><span class="line">    html &#123;</span><br><span class="line">      display: none !important;</span><br><span class="line">    &#125;</span><br><span class="line">  &lt;<span class="regexp">/style&gt;</span></span><br><span class="line"><span class="regexp">&lt;/</span>head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">  &lt;script&gt;</span><br><span class="line">    <span class="keyword">if</span> (self == top) &#123;</span><br><span class="line">      <span class="keyword">var</span> style = <span class="built_in">document</span>.getElementById(<span class="string">'click-jack'</span>)</span><br><span class="line">      <span class="built_in">document</span>.body.removeChild(style)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      top.location = self.location</span><br><span class="line">    &#125;</span><br><span class="line">  &lt;<span class="regexp">/script&gt;</span></span><br><span class="line"><span class="regexp">&lt;/</span>body&gt;</span><br></pre></td></tr></table></figure>

<h2 id="中间人攻击"><a href="#中间人攻击" class="headerlink" title="中间人攻击"></a>中间人攻击</h2><h3 id="概念-1"><a href="#概念-1" class="headerlink" title="概念"></a>概念</h3><p>中间人攻击是攻击方同时与服务端和客户端建立起了连接，并让对方认为连接是安全的，但是实际上整个通信过程都被攻击者控制了。攻击者不仅能获得双方的通信信息，还能修改通信信息</p>
<h3 id="如何防范-1"><a href="#如何防范-1" class="headerlink" title="如何防范"></a>如何防范</h3><p>防御中间人攻击其实并不难，只需要增加一个安全通道来传输信息。HTTPS 就可以用来防御中间人攻击，但是并不是说使用了 HTTPS 就可以高枕无忧了，因为如果你没有完全关闭 HTTP 访问的话，攻击方可以通过某些方式将 HTTPS 降级为 HTTP 从而实现中间人攻击</p>
<p>参考资料<br><a href="https://coding.imooc.com/class/104.html" target="_blank" rel="noopener">慕课网-Web前后端漏洞分析与防御</a><br>《白帽子讲Web安全》<br><a href="https://blog.csdn.net/ganyingxie123456/article/details/70230486" target="_blank" rel="noopener">Web安全之XSS攻防</a><br><a href="https://blog.nowcoder.net/yiizgo" target="_blank" rel="noopener">呓语</a><br><a href="https://github.com/poetries/" target="_blank" rel="noopener">poetries</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>Web安全</tag>
      </tags>
  </entry>
  <entry>
    <title>Web Worker</title>
    <url>/2019/11/22/Web%20Worker/</url>
    <content><![CDATA[<h1 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h1><p>解决JS单线程，遇到计算密集型或高延迟的任务时发生阻塞的问题<br>Web Worker 的作用，就是允许主线程创建 Worker 线程，承担一些费时的任务<br>Worker线程可以有多个，但是使用完最好计时关闭</p>
<h1 id="一个简单的例子帮助理解"><a href="#一个简单的例子帮助理解" class="headerlink" title="一个简单的例子帮助理解"></a>一个简单的例子帮助理解</h1><p><strong>改进前：</strong></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">button</span> <span class="attr">id</span>=<span class="string">"btn"</span>&gt;</span>点击<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">h1</span> <span class="attr">id</span>=<span class="string">"num"</span>&gt;</span><span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="actionscript">        <span class="comment">//在子线程 模拟一个耗时操作</span></span></span><br><span class="line"><span class="actionscript">        <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="number">1000000000</span>; i++) &#123; &#125;</span></span><br><span class="line">        num.innerHTML = i;</span><br><span class="line"><span class="actionscript">        box.onclick = <span class="function"><span class="keyword">function</span> <span class="params">()</span> </span>&#123;</span></span><br><span class="line"><span class="actionscript">          alert(<span class="string">"耗时操作导致页面阻塞，打开慢"</span>);</span></span><br><span class="line">        &#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>主线程中存在耗时任务，发生阻塞<br><strong>改进后：</strong></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">button</span> <span class="attr">id</span>=<span class="string">"btn"</span>&gt;</span>点击<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">button</span> <span class="attr">id</span>=<span class="string">"stop"</span>&gt;</span>停止子线程<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">h1</span> <span class="attr">id</span>=<span class="string">"num"</span>&gt;</span><span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="actionscript">    <span class="comment">// 创建子线程。把耗时操作放在子线程的文件中，Worker不能读取本地文件，脚本【必须来自网络，并且同源】</span></span></span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> webWorker = <span class="keyword">new</span> Worker(<span class="string">'./worker.js'</span>)</span></span><br><span class="line"><span class="actionscript">    webWorker.onmessage = <span class="function"><span class="keyword">function</span> <span class="params">(e)</span> </span>&#123;</span></span><br><span class="line">      num.innerHTML = e.data</span><br><span class="line">    &#125;</span><br><span class="line"><span class="actionscript">    btn.onclick = <span class="function"><span class="keyword">function</span> <span class="params">()</span> </span>&#123;</span></span><br><span class="line"><span class="actionscript">      alert(<span class="string">"有子线程后主线程快得多了"</span>);</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="actionscript">    stop.onclick = <span class="function"><span class="keyword">function</span> <span class="params">()</span> </span>&#123;</span></span><br><span class="line"><span class="actionscript">      <span class="comment">// 【终止Web Worker】</span></span></span><br><span class="line">      webworker.terminate();</span><br><span class="line">    &#125;</span><br><span class="line">  <span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>子线程</strong>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>  (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="comment">//模拟一个耗时操作</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="number">1000000000</span>; i++) &#123; &#125;</span><br><span class="line">  <span class="comment">//耗时操作完毕后，调用postMessage方法回到主线程，并且把数据传回去</span></span><br><span class="line">  postMessage(i);</span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>

<p>在主线程创建web worker子线程，让子线程承担费时任务，主线程不会阻塞</p>
<h1 id="Web-Worker的限制"><a href="#Web-Worker的限制" class="headerlink" title="Web Worker的限制"></a>Web Worker的限制</h1><ul>
<li>new 传递的脚本文件必须来源网络并同源</li>
<li>无法访问DOM节点；</li>
<li>无法调用alert()或者confirm之类的函数；</li>
<li>无法访问window、document之类的浏览器全局变量；但是可以用navigator对象和location对象</li>
</ul>
<p>不过Web Worker中的Javascript依然可以使用setTimeout(),setInterval()之类的函数，也可以<strong>使用XMLHttpRequest对象来做Ajax通信</strong></p>
<h1 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h1><h2 id="主线程"><a href="#主线程" class="headerlink" title="主线程"></a>主线程</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> worker = <span class="keyword">new</span> Worker(<span class="string">'work.js'</span>) <span class="comment">// 用new创建子线程，参数是一个同源网络的脚本文件</span></span><br><span class="line">worker.postMessage(<span class="string">'Hello World'</span>); <span class="comment">// 主线程向 Worker 发消息</span></span><br><span class="line">worker.onmessage = <span class="function"><span class="keyword">function</span>(<span class="params">ev</span>) </span>&#123; <span class="built_in">console</span>.log(ev.data) &#125; <span class="comment">// 主线程通过监听message事件，接收子线程发回来的消息(事件的data属性)</span></span><br><span class="line">worker.terminate() <span class="comment">// 关闭 Worker</span></span><br></pre></td></tr></table></figure>

<h2 id="worker线程"><a href="#worker线程" class="headerlink" title="worker线程"></a>worker线程</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">addEventListener(<span class="string">'message'</span>, <span class="function"><span class="keyword">function</span> (<span class="params">evt</span>) </span>&#123; <span class="built_in">console</span>.log(ev.data) &#125; <span class="comment">// 同理通过监听message事件，接收主线程发回来的消息(事件的data属性)</span></span><br><span class="line">self.postMessage(<span class="string">'Hello World'</span>) <span class="comment">/// 向主线程发送消息</span></span><br><span class="line">self.close() <span class="comment">// 关闭 Worker</span></span><br></pre></td></tr></table></figure>

<h2 id="使用XMLHttpRequest与服务端通信"><a href="#使用XMLHttpRequest与服务端通信" class="headerlink" title="使用XMLHttpRequest与服务端通信"></a>使用XMLHttpRequest与服务端通信</h2><p>在子线程文件中写：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">addEventListener(<span class="string">"message"</span>, <span class="function"><span class="keyword">function</span>(<span class="params">evt</span>)</span>&#123;  </span><br><span class="line"><span class="keyword">var</span> xhr = <span class="keyword">new</span> XMLHttpRequest();  </span><br><span class="line">xhr.open(<span class="string">"GET"</span>, <span class="string">"某个服务器的url"</span>);  </span><br><span class="line">xhr.onload = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;  </span><br><span class="line">postMessage(xhr.responseText);  </span><br><span class="line">&#125;;</span><br><span class="line">xhr.send();  </span><br><span class="line">&#125;,<span class="literal">false</span>);</span><br></pre></td></tr></table></figure>

<p>上面的代码向服务器发出GET请求，并注册了获取数据后的onload事件。结果是获得数据后把数据传给主线程</p>
<h2 id="通过Error事件捕捉错误信息"><a href="#通过Error事件捕捉错误信息" class="headerlink" title="通过Error事件捕捉错误信息"></a>通过Error事件捕捉错误信息</h2><p>Web Worker恰恰提供了error事件，供开发者捕捉错误信息</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> worker = <span class="keyword">new</span> Worker(<span class="string">"scripts/lengthytask.js"</span>);  </span><br><span class="line">worker.addEventListener(<span class="string">"error"</span>, <span class="function"><span class="keyword">function</span>(<span class="params">evt</span>)</span>&#123;  </span><br><span class="line">alert(<span class="string">"Line #"</span> + evt.lineno + <span class="string">" - "</span> + evt.message + <span class="string">" in "</span> + evt.filename);  </span><br><span class="line">&#125;, <span class="literal">false</span>);  </span><br><span class="line">worker.postMessage(<span class="number">10000</span>);  </span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>如上可见， Worker对象可以绑定error事件；而且evt对象中包含错误所在的代码文件（evt.filename）、错误所在的代码行数（evt.lineno）、以及错误信息</p>
<p>参考资料<br><a href="http://www.ruanyifeng.com/blog/2018/07/web-worker.html" target="_blank" rel="noopener">阮一峰-Web Worker 使用教程</a><br><a href="https://www.cnblogs.com/sunshq/p/4187283.html" target="_blank" rel="noopener">关于Web Worker你必须知道的7件事</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>Browser</tag>
      </tags>
  </entry>
  <entry>
    <title>TypeScript中的类型编程</title>
    <url>/2020/12/07/TypeScript%E4%B8%AD%E7%9A%84%E7%B1%BB%E5%9E%8B%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>本篇文章是读了林不渡的<a href="https://juejin.cn/post/6885672896128090125#heading-0" target="_blank" rel="noopener">TypeScript 的另一面：类型编程</a>，码了一些 demo 并查了其他文档等最后留下的学习笔记。建议直接读原文，本文可读性较差</p>
<h2 id="类型编程的特点-看法"><a href="#类型编程的特点-看法" class="headerlink" title="类型编程的特点/看法"></a>类型编程的特点/看法</h2><ul>
<li>它会带来代码量大大增多（可能接近甚至超过业务代码），编码耗时增长等问题，而带来的唯一好处就是<strong>类型安全</strong>，包括的类型提示，进一步减少可能存在的调用错误，以及降低维护成本。看起来似乎有得有失，但实际上，假设你花费 1 单位脑力使用基础的 TS 以及简单的类型编程，你就能够获得 5 个单位的回馈。但接下来，有可能你花费 10 个单位脑力，也只能再获得 2 个单位的回馈</li>
<li>另外一个类型编程不受重视的原因则是实际业务中并不会需要多么苛刻的类型定义，通常是底层框架类库才会有此类需求</li>
</ul>
<h1 id="泛型-Generic-Type"><a href="#泛型-Generic-Type" class="headerlink" title="泛型 Generic Type"></a>泛型 Generic Type</h1><p>见<a href="https://jacleklm.github.io/2020/04/14/TypeScript%E5%9F%BA%E7%A1%80%E8%AF%AD%E6%B3%95%E5%B0%8F%E7%BB%93/" target="_blank" rel="noopener">TypeScript 基础语法小结</a></p>
<blockquote>
<p>在类型编程里，泛型就是变量</p>
</blockquote>
<p>比如我们要写一个类似于 Pick 功能的函数，从一个 obj 中挑选一些键值对出来，可以这么写：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">getValueListOfObj</span>&lt;<span class="title">T</span> <span class="title">extends</span> <span class="title">object</span>, <span class="title">U</span> <span class="title">extends</span> <span class="title">keyof</span> <span class="title">T</span>&gt;(<span class="params">obj: T, keys: U[]</span>): <span class="title">T</span>[<span class="title">U</span>][] </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> keys.map(<span class="function">(<span class="params">key: U</span>) =&gt;</span> obj[key]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里还用 extends 做了一定的泛型约束</p>
<h1 id="索引类型与映射类型"><a href="#索引类型与映射类型" class="headerlink" title="索引类型与映射类型"></a>索引类型与映射类型</h1><h2 id="索引类型"><a href="#索引类型" class="headerlink" title="索引类型"></a>索引类型</h2><p>索引类型见<a href="https://jacleklm.github.io/2020/04/14/TypeScript%E5%9F%BA%E7%A1%80%E8%AF%AD%E6%B3%95%E5%B0%8F%E7%BB%93/" target="_blank" rel="noopener">TypeScript 基础语法小结</a></p>
<h2 id="映射类型"><a href="#映射类型" class="headerlink" title="映射类型"></a>映射类型</h2><p>映射类型 Mapped Types 通常用于在旧有类型的基础上进行改造，包括接口包含字段、字段的类型、修饰符（readonly 与?）等等。可以利用 Readonly，Partial 等这些 TS 内置<strong>工具类型</strong>(也叫类型接口)实现这个过程。</p>
<blockquote>
<p>简单地说就是 TS 允许将一个类型映射成另外一个类型</p>
</blockquote>
<p>一个很实用的实现就是 clone，把 T 里面的都拷贝一次</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> cloneT&lt;T&gt; = &#123;</span><br><span class="line">  [K <span class="keyword">in</span> keyof T]: T[K];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>上面这种已经算是工具类型的实现了。相当于类型编程中的 utils</p>
</blockquote>
<p>这部分详见 <a href="https://jacleklm.github.io/2020/12/09/TypeScript%E4%B8%AD%E7%9A%84%E5%86%85%E7%BD%AE%E5%B7%A5%E5%85%B7%E7%B1%BB%E5%9E%8B%E5%8F%8A%E5%85%B6%E5%AE%9E%E7%8E%B0/9" target="_blank" rel="noopener">TypeScript 中的内置工具类型及其实现</a></p>
<h1 id="条件类型-Conditional-Types"><a href="#条件类型-Conditional-Types" class="headerlink" title="条件类型 Conditional Types"></a>条件类型 Conditional Types</h1><p>条件类型的语法实际上就是三元表达式。其中一个常见应用就是用来实现更精准的泛型约束，<strong>使得泛型收窄</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">T extends U ? X : Y</span><br></pre></td></tr></table></figure>

<blockquote>
<p>这种场景下 T 一般是联合类型。如果你觉得这里的 extends 不太好理解，可以暂时简单理解为 U 中的属性在 T 中都有</p>
</blockquote>
<p>条件类型理解起来更直观，唯一需要有一定理解成本的就是<strong>何时条件类型系统会收集到足够的信息来确定类型</strong>，也就是说，条件类型有可能不会被立刻完成判断。<br>有时候，条件类型的推导会被延迟（deferred），因为此时类型系统没有足够的信息来完成判断：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 单纯声明而已</span></span><br><span class="line"><span class="keyword">declare</span> <span class="function"><span class="keyword">function</span> <span class="title">strOrNum</span>&lt;<span class="title">T</span> <span class="title">extends</span> <span class="title">boolean</span>&gt;(<span class="params">input: T</span>): <span class="title">T</span> <span class="title">extends</span> <span class="title">true</span> ? <span class="title">string</span> : <span class="title">number</span></span>;</span><br></pre></td></tr></table></figure>

<p>只有给出了所需信息（在这里是input值），才可以完成推导</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> strReturnType = strOrNum(<span class="literal">true</span>);</span><br></pre></td></tr></table></figure>

<h2 id="嵌套的条件类型"><a href="#嵌套的条件类型" class="headerlink" title="嵌套的条件类型"></a>嵌套的条件类型</h2><p>此外，就像三元表达式可以嵌套，条件类型也可以嵌套，如果你看过一些框架源码，也会发现其中存在着许多嵌套的条件类型，条件类型可以将类型约束收拢到非常精确的范围内</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> TypeName&lt;T&gt; = T <span class="keyword">extends</span> <span class="built_in">string</span></span><br><span class="line">  ? <span class="string">'string'</span></span><br><span class="line">  : T <span class="keyword">extends</span> <span class="built_in">number</span></span><br><span class="line">    ? <span class="string">'number'</span></span><br><span class="line">    : T <span class="keyword">extends</span> <span class="built_in">boolean</span></span><br><span class="line">      ? <span class="string">'boolean'</span></span><br><span class="line">      : T <span class="keyword">extends</span> <span class="literal">undefined</span></span><br><span class="line">        ? <span class="string">'undefined'</span></span><br><span class="line">        : T <span class="keyword">extends</span> <span class="built_in">Function</span></span><br><span class="line">          ? <span class="string">'function'</span></span><br><span class="line">          : <span class="string">'object'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> good: TypeName&lt;<span class="built_in">number</span>&gt; = <span class="string">'number'</span>;</span><br><span class="line"><span class="comment">// Error</span></span><br><span class="line"><span class="keyword">const</span> bad: TypeName&lt;<span class="built_in">string</span>&gt; = <span class="string">'number'</span>;</span><br></pre></td></tr></table></figure>

<h2 id="分布式条件类型-Distributive-Conditional-Types"><a href="#分布式条件类型-Distributive-Conditional-Types" class="headerlink" title="分布式条件类型 Distributive Conditional Types"></a>分布式条件类型 Distributive Conditional Types</h2><h3 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h3><p>分布式条件类型实际上不是一种特殊的条件类型，而是其特性之一。概括地说，就是<strong>对于属于裸类型参数的检查类型，条件类型会在实例化时期自动分发到联合类型上</strong></p>
<blockquote>
<p>原文: Conditional types in which the checked type is a <strong>naked type parameter</strong> are called distributive conditional types.  Distributive conditional types are automatically <strong>distributed over union types</strong> during instantiation</p>
</blockquote>
<h3 id="不负责任可能不太准确的简介"><a href="#不负责任可能不太准确的简介" class="headerlink" title="不负责任可能不太准确的简介"></a>不负责任可能不太准确的简介</h3><p>对这样一个场景：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> someTypeName&lt;T&gt; = T <span class="keyword">extends</span> U ? X : Y</span><br></pre></td></tr></table></figure>

<p>若<code>T</code>为联合类型且<code>T</code>不被包裹，则<code>T</code>中的项会被拆开分别做<code>extends U</code>判断，再把结果合并组成一个联合类型作为推断结果</p>
<p>抽象起来就是：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line">( A | B | C ) <span class="keyword">extends</span> U ? X : Y</span><br><span class="line"><span class="comment">// 相当于</span></span><br><span class="line">(A <span class="keyword">extends</span> U ? X : Y) | (B <span class="keyword">extends</span> U ? X : Y) | (B <span class="keyword">extends</span> U ? X : Y)</span><br></pre></td></tr></table></figure>

<h3 id="正确版的理清概念"><a href="#正确版的理清概念" class="headerlink" title="正确版的理清概念"></a>正确版的理清概念</h3><p>先提取几个关键词，然后我们再通过例子理清这个概念：</p>
<ul>
<li>裸类型参数</li>
<li>实例化</li>
<li>分发到联合类型</li>
</ul>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 使用上面的TypeName类型别名</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// "string" | "function"</span></span><br><span class="line"><span class="keyword">type</span> T1 = TypeName&lt;<span class="built_in">string</span> | <span class="function">(<span class="params">(<span class="params"></span>) =&gt; <span class="built_in">void</span></span>)&gt;</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">// "<span class="params">string</span>" | "<span class="params">object</span>"</span></span><br><span class="line"><span class="function"><span class="params">type</span> <span class="params">T2</span> = <span class="params">TypeName</span>&lt;<span class="params">string</span> | <span class="params">string</span>[]&gt;</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">// "<span class="params">object</span>"</span></span><br><span class="line"><span class="function"><span class="params">type</span> <span class="params">T3</span> = <span class="params">TypeName</span>&lt;<span class="params">string</span>[] | <span class="params">number</span>[]&gt;</span></span><br></pre></td></tr></table></figure>

<p>我们发现在上面的例子里，条件类型的推导结果都是联合类型（T3实际上也是，只不过相同所以被合并了），并且就是类型参数被依次进行条件判断的结果。<br>但是当我们这样做：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Naked&lt;T&gt; = T <span class="keyword">extends</span> <span class="built_in">boolean</span> ? <span class="string">"Y"</span> : <span class="string">"N"</span>;</span><br><span class="line"><span class="keyword">type</span> Wrapped&lt;T&gt; = [T] <span class="keyword">extends</span> [<span class="built_in">boolean</span>] ? <span class="string">"Y"</span> : <span class="string">"N"</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * 先分发到 Naked&lt;number&gt; | Naked&lt;boolean&gt;</span></span><br><span class="line"><span class="comment"> * 结果是 "N" | "Y"</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">type</span> Distributed = Naked&lt;<span class="built_in">number</span> | <span class="built_in">boolean</span>&gt;;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * 不会分发 直接是 [number | boolean] extends [boolean]</span></span><br><span class="line"><span class="comment"> * 然后结果是 "N"</span></span><br><span class="line"><span class="comment"> * 在这两个例子中，T 都是 number | boolean。区别是 T 有没有被【包裹】过</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">type</span> NotDistributed = Wrapped&lt;<span class="built_in">number</span> | <span class="built_in">boolean</span>&gt;;</span><br></pre></td></tr></table></figure>

<p>现在我们可以来讲讲这几个概念了：</p>
<ul>
<li><strong>裸类型参数</strong>，没有额外被接口/类型别名包裹过的，就像被Wrapped包裹后就不能再被称为裸类型参数。</li>
<li><strong>实例化</strong>，其实就是条件类型的判断过程，在这里两个例子的实例化过程实际上是不同的，具体会在下一点中介绍。</li>
<li><strong>分发至联合类型的过程</strong>：<br>对于TypeName，它内部的类型参数T是没有被包裹过的，所以<br><code>TypeName&lt;string | (() =&gt; void)&gt;</code> 会被分发为 <code>TypeName&lt;string&gt; | TypeName&lt;(() =&gt; void)&gt;</code>, 然后再次进行判断，最后分发为<code>&quot;string&quot; | &quot;function&quot;</code><br>抽象下具体过程：<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line">( A | B | C ) <span class="keyword">extends</span> T ? X : Y</span><br><span class="line"><span class="comment">// 相当于</span></span><br><span class="line">(A <span class="keyword">extends</span> T ? X : Y) | (B <span class="keyword">extends</span> T ? X : Y) | (B <span class="keyword">extends</span> T ? X : Y)</span><br></pre></td></tr></table></figure></li>
</ul>
<p>一句话概括：没有被额外包装的联合类型参数T，在条件类型进行判定时会<strong>将联合类型</strong>分发，分别进行判断。</p>
<h1 id="infer关键字"><a href="#infer关键字" class="headerlink" title="infer关键字"></a><a href="https://jkchao.github.io/typescript-book-chinese/tips/infer.html#%E4%BB%8B%E7%BB%8D" target="_blank" rel="noopener">infer关键字</a></h1><p>infer是inference的缩写，通常的使用方式是<code>infer R</code>，<code>R</code>表示 <strong>待推断的类型</strong>。通常infer不会被直接使用，而是被放置在底层工具类型中，需要在条件类型中使用<br>看一个简单的例子，用于获取函数返回值类型的工具类型<code>ReturnType</code>，其用法：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> getStr = (): <span class="function"><span class="params">string</span> =&gt;</span> <span class="string">'hello'</span>;</span><br><span class="line"><span class="keyword">type</span> fn = ReturnType&lt;<span class="keyword">typeof</span> getStr&gt;; <span class="comment">// 推断结果是 'string'</span></span><br></pre></td></tr></table></figure>

<p><code>ReturnType</code>源码如下</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> ReturnType&lt;T&gt; = T <span class="keyword">extends</span> (</span><br><span class="line">  ...args: <span class="built_in">any</span>[]</span><br><span class="line">) =&gt; infer R</span><br><span class="line">  ? R</span><br><span class="line">  : <span class="built_in">any</span>;</span><br></pre></td></tr></table></figure>

<p>这里的<code>infer R</code>就是声明一个变量来承载传入函数签名的返回值类型, 简单说就是用它取到函数返回值的类型方便之后使用。相当于预留一个变量来存函数的返回类型<br>类似前端中的loading占位符，infer也是这个思路，<strong>类型系统在获得足够的信息后，就能将infer后跟随的类型参数推导出来</strong>，最后返回这个推导结果</p>
<h1 id="类型守卫-类型保护-Type-Guards-amp-amp-is、in-关键字"><a href="#类型守卫-类型保护-Type-Guards-amp-amp-is、in-关键字" class="headerlink" title="类型守卫(类型保护)Type Guards &amp;&amp; is、in 关键字"></a>类型守卫(类型保护)Type Guards &amp;&amp; is、in 关键字</h1><p>见<a href="https://jacleklm.github.io/2020/04/14/TypeScript%E5%9F%BA%E7%A1%80%E8%AF%AD%E6%B3%95%E5%B0%8F%E7%BB%93/" target="_blank" rel="noopener">TypeScript 基础语法小结</a></p>
<h1 id="工具类型Tool-Type-及其实现"><a href="#工具类型Tool-Type-及其实现" class="headerlink" title="工具类型Tool Type 及其实现"></a>工具类型Tool Type 及其实现</h1><p>工具类型就像我们自己的util或者我们用的lodash一样，虽然即使你还是不太懂这些工具类型的底层实现，也不影响你把它用好，不够我们自己还是得了解下比较好。推荐在完成学习后记录你觉得比较有价值的工具类型，并在自己的项目里新建一个<code>.d.ts</code>文件存储它</p>
<h2 id="内置工具类型"><a href="#内置工具类型" class="headerlink" title="内置工具类型"></a>内置工具类型</h2><p><a href="https://jacleklm.github.io/2020/12/09/TypeScript%E4%B8%AD%E7%9A%84%E5%86%85%E7%BD%AE%E5%B7%A5%E5%85%B7%E7%B1%BB%E5%9E%8B%E5%8F%8A%E5%85%B6%E5%AE%9E%E7%8E%B0/" target="_blank" rel="noopener">TypeScript中的内置工具类型及其实现</a></p>
<h2 id="社区工具类型"><a href="#社区工具类型" class="headerlink" title="社区工具类型"></a>社区工具类型</h2><p>参考资料<br><a href="https://juejin.cn/post/6885672896128090125#heading-0" target="_blank" rel="noopener">TypeScript 的另一面：类型编程</a><br><a href="https://jkchao.github.io/typescript-book-chinese/" target="_blank" rel="noopener">深入理解 TypeScript</a><br>组内分享: TS 最佳实践</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>TypeScript</tag>
      </tags>
  </entry>
  <entry>
    <title>按需加载 &amp; 样式自动加载 —— babel-plugin-import</title>
    <url>/2020/06/27/babel-plugin-import/</url>
    <content><![CDATA[<p>几乎完全转自 <a href="https://mp.weixin.qq.com/s/KjkIud4-q0Nm-YBLG6D1zg" target="_blank" rel="noopener">一篇文章搞定 babel-plugin-import 插件</a> ，可以理解为 读后 &amp; 自己敲一遍 的笔记，建议直接读原文章</p>
<h1 id="Background"><a href="#Background" class="headerlink" title="Background"></a>Background</h1><p>我们用 element-ui 或 antd 的时候，样式都支持全局引入和按需引入。按需引入需要安装一个 <a href="https://github.com/ant-design/babel-plugin-import" target="_blank" rel="noopener"><code>babel-plugin-import</code></a> 的插件，将全局的写法变成按需引入的写法。其实也是用了AST语法树转换的原理，实现</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; Button &#125; <span class="keyword">from</span> <span class="string">'antd'</span>;</span><br><span class="line">      ↓ ↓ ↓ ↓ ↓ ↓</span><br><span class="line"><span class="keyword">var</span> _button = <span class="built_in">require</span>(<span class="string">'antd/lib/button'</span>);</span><br><span class="line"><span class="built_in">require</span>(<span class="string">'antd/lib/button/style'</span>);</span><br></pre></td></tr></table></figure>

<h1 id="Usage"><a href="#Usage" class="headerlink" title="Usage"></a>Usage</h1><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="string">"libraryName"</span>: <span class="string">"antd"</span>,     <span class="comment">// 包名</span></span><br><span class="line">  <span class="string">"libraryDirectory"</span>: <span class="string">"lib"</span>, <span class="comment">// 目录，默认 lib</span></span><br><span class="line">  <span class="string">"style"</span>: <span class="literal">true</span>,             <span class="comment">// 是否引入 style</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其它的看 <a href="https://github.com/ant-design/babel-plugin-import#usage" target="_blank" rel="noopener">文档</a></p>
<h1 id="Working-mechanism"><a href="#Working-mechanism" class="headerlink" title="Working mechanism"></a>Working mechanism</h1><h2 id="What-does-babel-plugin-import-do"><a href="#What-does-babel-plugin-import-do" class="headerlink" title="What does babel-plugin-import do"></a>What does babel-plugin-import do</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 当options为以下时候：</span></span><br><span class="line">&#123; <span class="string">"libraryName"</span>: <span class="string">"antd"</span>, <span class="attr">style</span>: <span class="literal">true</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> &#123; Button &#125; <span class="keyword">from</span> <span class="string">'antd'</span>;</span><br><span class="line">ReactDOM.render(<span class="xml"><span class="tag">&lt;<span class="name">Button</span>&gt;</span>xxxx<span class="tag">&lt;/<span class="name">Button</span>&gt;</span></span>);</span><br><span class="line">      ↓ ↓ ↓ ↓ ↓ ↓</span><br><span class="line"><span class="keyword">var</span> _button = <span class="built_in">require</span>(<span class="string">'antd/lib/button'</span>);</span><br><span class="line"><span class="built_in">require</span>(<span class="string">'antd/lib/button/style'</span>);</span><br><span class="line">ReactDOM.render(<span class="xml"><span class="tag">&lt;<span class="name">_button</span>&gt;</span>xxxx<span class="tag">&lt;/<span class="name">_button</span>&gt;</span></span>);</span><br></pre></td></tr></table></figure>

<h2 id="How"><a href="#How" class="headerlink" title="How"></a>How</h2><p>以这段代码为例</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; Button, AutoComplete &#125; <span class="keyword">from</span> <span class="string">'antd'</span>;</span><br><span class="line">ReactDOM.render(<span class="xml"><span class="tag">&lt;<span class="name">Button</span>&gt;</span>提交<span class="tag">&lt;/<span class="name">Button</span>&gt;</span></span>);</span><br></pre></td></tr></table></figure>

<h3 id="第一步-依赖收集"><a href="#第一步-依赖收集" class="headerlink" title="第一步 依赖收集"></a>第一步 依赖收集</h3><p><code>babel-plubin-import</code> 会在 <code>ImportDeclaration</code> 里将所有的 <code>specifier</code> 收集起来，详见如下 ast<br><img src="/img/AST/antd.png"><br>可以从这个 ImportDeclaration 语句中提取几个关键点：</p>
<ul>
<li>source.value: antd</li>
<li>specifier.imported.name: Button</li>
<li>specifier.imported.name: AutoComplete</li>
</ul>
<p>PS: local.name 是导入进来的别名，比如 <code>import { Button as MyButton } from &#39;antd&#39;</code> 的 <code>MyButton</code> ; imported.name 是真实导入的变量名</p>
<p>所以要做的事：收集 依赖名 和 依赖来源。插件的代码可以实现如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">ImportDeclaration(path, state) &#123;</span><br><span class="line">  <span class="keyword">const</span> &#123; node &#125; = path;</span><br><span class="line">  <span class="keyword">if</span> (!node) <span class="keyword">return</span>;</span><br><span class="line">  <span class="comment">// 代码里 import 的包名</span></span><br><span class="line">  <span class="keyword">const</span> &#123; value &#125; = node.source;</span><br><span class="line">  <span class="comment">// 配在插件 options 的包名</span></span><br><span class="line">  <span class="keyword">const</span> &#123; libraryName &#125; = <span class="keyword">this</span>;</span><br><span class="line">  <span class="comment">// @babel/types 工具函数</span></span><br><span class="line">  <span class="keyword">const</span> &#123; types &#125; = <span class="keyword">this</span>;</span><br><span class="line">  <span class="comment">// 内部状态</span></span><br><span class="line">  <span class="keyword">const</span> pluginState = <span class="keyword">this</span>.getPluginState(state);</span><br><span class="line">  <span class="comment">// 判断是不是需要使用该插件的包</span></span><br><span class="line">  <span class="keyword">if</span> (value === libraryName) &#123;</span><br><span class="line">    <span class="comment">// node.specifiers 表示 import 了什么</span></span><br><span class="line">    node.specifiers.forEach(<span class="function"><span class="params">spec</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="comment">// 判断是不是 ImportSpecifier 类型的节点，也就是是否是大括号的</span></span><br><span class="line">      <span class="keyword">if</span> (types.isImportSpecifier(spec)) &#123;</span><br><span class="line">        <span class="comment">// 收集依赖</span></span><br><span class="line">        <span class="comment">// 也就是 pluginState.specified.Button = Button</span></span><br><span class="line">        <span class="comment">// local.name 是导入进来的别名，比如 import &#123; Button as MyButton &#125; from 'antd' 的 MyButton</span></span><br><span class="line">        <span class="comment">// imported.name 是真实导入的变量名</span></span><br><span class="line">        pluginState.specified[spec.local.name] = spec.imported.name;</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123; </span><br><span class="line">        <span class="comment">// ImportDefaultSpecifier 和 ImportNamespaceSpecifier</span></span><br><span class="line">        pluginState.libraryObjs[spec.local.name] = <span class="literal">true</span>;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">    pluginState.pathsToRemove.push(path);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>遍历了所有的 <code>ImportDeclaration</code> 类型的节点之后，就收集好了依赖关系，下一步就是如何加载它们了。</p>
<h3 id="第二步-判断是否使用"><a href="#第二步-判断是否使用" class="headerlink" title="第二步 判断是否使用"></a>第二步 判断是否使用</h3><p>判断import进来的组件是否有在后续代码中使用，即是下面代码中 <code>createElement</code> 的第一个参数，我们需要判断收集的依赖中是否有被 createElement 使用</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ReactDOM.render(&lt;Button&gt;Hello&lt;/Button&gt;);</span><br><span class="line">      ↓ ↓ ↓ ↓ ↓ ↓</span><br><span class="line">React.createElement(Button, null, &quot;Hello&quot;);</span><br></pre></td></tr></table></figure>

<p>所以我们可以看下 <code>React.createElement(Button, null, &quot;Hello&quot;)</code> 的AST<br><img src="/img/AST/create.png"><br>代码如下:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">CallExpression(path, state) &#123;</span><br><span class="line">  const &#123; node &#125; = path;</span><br><span class="line">  const file = (path &amp;&amp; path.hub &amp;&amp; path.hub.file) || (state &amp;&amp; state.file);</span><br><span class="line">  // 方法调用者的 name</span><br><span class="line">  const &#123; name &#125; = node.callee;</span><br><span class="line">  // babel-type 工具函数</span><br><span class="line">  const &#123; types &#125; = this;</span><br><span class="line">  // 内部状态</span><br><span class="line">  const pluginState = this.getPluginState(state);</span><br><span class="line"></span><br><span class="line">  // 如果方法调用者是 Identifier 类型</span><br><span class="line">  if (types.isIdentifier(node.callee)) &#123;</span><br><span class="line">    if (pluginState.specified[name]) &#123;</span><br><span class="line">      node.callee = this.importMethod(pluginState.specified[name], file, pluginState);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  // 遍历 arguments 找我们要的 specifier</span><br><span class="line">  node.arguments = node.arguments.map(arg =&gt; &#123;</span><br><span class="line">    const &#123; name: argName &#125; = arg;</span><br><span class="line">    if (</span><br><span class="line">      pluginState.specified[argName] &amp;&amp;</span><br><span class="line">      path.scope.hasBinding(argName) &amp;&amp;</span><br><span class="line">      path.scope.getBinding(argName).path.type === &apos;ImportSpecifier&apos;</span><br><span class="line">    ) &#123;</span><br><span class="line">      // 找到 specifier，调用 importMethod 方法</span><br><span class="line">      return this.importMethod(pluginState.specified[argName], file, pluginState);</span><br><span class="line">    &#125;</span><br><span class="line">    return arg;</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>除了 <code>React.createElement(Button)</code> 之外，还有 <code>const btn = Button / [Button]</code> … 等多种情况会使用 Button，源码中都有对应的处理方法，感兴趣的可以自己<a href="https://github.com/ant-design/babel-plugin-import/blob/master/src/Plugin.js#L163-L272" target="_blank" rel="noopener">看一下</a></p>
<h3 id="第三步-生成引入代码（核心）"><a href="#第三步-生成引入代码（核心）" class="headerlink" title="第三步 生成引入代码（核心）"></a>第三步 生成引入代码（核心）</h3><p>即删除原来的import，引入我们生成的import。创建import的时候会用到一个插件 @babel/helper-module-imports。后续部分建议直接看原博客<a href="https://mp.weixin.qq.com/s/KjkIud4-q0Nm-YBLG6D1zg" target="_blank" rel="noopener">一篇文章搞定 babel-plugin-import 插件</a></p>
<h1 id="插件实现"><a href="#插件实现" class="headerlink" title="插件实现"></a>插件实现</h1><p>见原文章</p>
<p>Reference<br><a href="https://mp.weixin.qq.com/s/KjkIud4-q0Nm-YBLG6D1zg" target="_blank" rel="noopener">一篇文章搞定 babel-plugin-import 插件</a><br><a href="https://chengyuming.cn/views/webpack/AST.html" target="_blank" rel="noopener">AST 团队分享</a></p>
]]></content>
      <categories>
        <category>Engineering</category>
      </categories>
      <tags>
        <tag>webpack</tag>
      </tags>
  </entry>
  <entry>
    <title>hope statistics puppeteer 工具小结</title>
    <url>/2020/11/03/hope%20statistics%20puppeteer%E5%B7%A5%E5%85%B7%E5%B0%8F%E7%BB%93/</url>
    <content><![CDATA[<h1 id="Puppeteer"><a href="#Puppeteer" class="headerlink" title="Puppeteer"></a>Puppeteer</h1><p><a href="https://github.com/puppeteer/puppeteer#readme" target="_blank" rel="noopener">Puppeteer</a> 是 Google Chrome 团队官方的无界面（Headless）Chrome 工具。Chrome 作为浏览器市场的领头羊，Chrome Headless 将成为 web 应用 <strong>自动化测试</strong> 的行业标杆</p>
<blockquote>
<p>Headless浏览器是指没有窗口的浏览器</p>
</blockquote>
<h2 id="用处"><a href="#用处" class="headerlink" title="用处"></a>用处</h2><p>通过 Puppeteer 我们可以让浏览器帮我们自动完成很多事情, 例如 ：</p>
<ul>
<li>生成页面的截图或者PDF</li>
<li>自动表单提交 (模拟登陆等)，UI测试，键盘输入</li>
<li>创建自动化测试环境</li>
</ul>
<p>具体API见<a href="https://github.com/puppeteer/puppeteer/blob/v5.4.1/docs/api.md#pageevalselector-pagefunction-args" target="_blank" rel="noopener">官方文档</a></p>
<h1 id="hope-statistics-puppeteer"><a href="#hope-statistics-puppeteer" class="headerlink" title="hope statistics puppeteer"></a>hope statistics puppeteer</h1><p>一个基于 puppeteer 和 node 编写的小工具，用于统计工作室成员当月的日志、笔记、小结数目，代码见<a href="https://github.com/Jacleklm/hope-statistics-puppeteer" target="_blank" rel="noopener">仓库</a></p>
<h2 id="交互"><a href="#交互" class="headerlink" title="交互"></a>交互</h2><p>考虑到需要的输入并不复杂，交互方面直接用了 <a href="http://nodejs.cn/api/readline.html" target="_blank" rel="noopener">readline</a> 模块。涉及到更多输入的可用<a href="https://www.npmjs.com/package/prompt" target="_blank" rel="noopener">prompt</a></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> app = <span class="built_in">require</span>(<span class="string">'./app'</span>)</span><br><span class="line"><span class="keyword">const</span> readline = <span class="built_in">require</span>(<span class="string">'readline'</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> rl = readline.createInterface(&#123;</span><br><span class="line">  input: process.stdin,</span><br><span class="line">  output: process.stdout</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">rl.question(<span class="string">'请输入当前的年份和月份，然后按回车键结束。eg: 202010    '</span>, (month) =&gt; &#123;</span><br><span class="line"></span><br><span class="line">  rl.question(<span class="string">'请输入本月小结会的月份和日期，然后按回车键结束。eg: 1130    '</span>, (date) =&gt; &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'数据统计中, 请勿触碰键盘...'</span>);</span><br><span class="line">    app(<span class="built_in">Number</span>(month), <span class="built_in">Number</span>(date));</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="计算当月任务量"><a href="#计算当月任务量" class="headerlink" title="计算当月任务量"></a>计算当月任务量</h2><p>由于每月要求的日志、学习笔记数量都是不定的，这里调用了一个 <a href="https://www.kancloud.cn/xiaoggvip/holiday_free/1606802" target="_blank" rel="noopener">免费API</a> ，用于获取当月的<strong>法定节假日情况</strong>，再封装成一个 getTask 方法完成这部分计算</p>
<h2 id="日志、笔记、小结数爬取"><a href="#日志、笔记、小结数爬取" class="headerlink" title="日志、笔记、小结数爬取"></a>日志、笔记、小结数爬取</h2><p>这里用了 puppeteer 提供的爬虫的功能，直接进行页面爬取并计算，具体见代码</p>
<p>参考资料<br>了解Puppeteer[<a href="https://juejin.im/post/6844903564880379912]" target="_blank" rel="noopener">https://juejin.im/post/6844903564880379912]</a></p>
]]></content>
      <categories>
        <category>Engineering</category>
      </categories>
      <tags>
        <tag>Node</tag>
      </tags>
  </entry>
  <entry>
    <title>TypeScript中的内置工具类型及其实现</title>
    <url>/2020/12/09/TypeScript%E4%B8%AD%E7%9A%84%E5%86%85%E7%BD%AE%E5%B7%A5%E5%85%B7%E7%B1%BB%E5%9E%8B%E5%8F%8A%E5%85%B6%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<p>先来看下基础实现 clone，把 T 里面的都拷贝一次：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> cloneT&lt;T&gt; = &#123;</span><br><span class="line">  [K <span class="keyword">in</span> keyof T]: T[K];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h1 id="Partial"><a href="#Partial" class="headerlink" title="Partial"></a>Partial</h1><h2 id="用法"><a href="#用法" class="headerlink" title="用法"></a>用法</h2><p>将类型定义的所有属性<strong>都修改为可选</strong></p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Foo = &#123;</span><br><span class="line">  name: <span class="built_in">string</span>;</span><br><span class="line">  age: <span class="built_in">number</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">type</span> Coord = Partial&lt;Foo&gt;</span><br><span class="line"><span class="comment">// 等同于</span></span><br><span class="line"><span class="keyword">type</span> Coord = &#123;</span><br><span class="line">  name?: <span class="built_in">string</span>;</span><br><span class="line">  age?: <span class="built_in">number</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Partial&lt;T&gt; = &#123;</span><br><span class="line">  [K <span class="keyword">in</span> keyof T]?: T[K];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h1 id="类似Partial的实现，还有-Required，Readonly"><a href="#类似Partial的实现，还有-Required，Readonly" class="headerlink" title="类似Partial的实现，还有 Required，Readonly"></a>类似Partial的实现，还有 Required，Readonly</h1><p>Required 的作用是将传入的属性变为必选项, Readonly是将所有属性定义为自读，源码如下</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Required&lt;T&gt; = &#123; </span><br><span class="line">  [K <span class="keyword">in</span> keyof T]-?: T[K];</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">type</span> Readonly&lt;T&gt; = &#123;</span><br><span class="line">  readonly [K <span class="keyword">in</span> keyof T]: T[K];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>类似 <code>-?</code>, <code>readonly</code>, 同理还有 <code>-readonly</code></p>
</blockquote>
<h1 id="Record"><a href="#Record" class="headerlink" title="Record"></a>Record</h1><h2 id="用法-1"><a href="#用法-1" class="headerlink" title="用法"></a>用法</h2><p>以 typeof 格式快速创建一个类型，此类型包含一组指定的属性且<strong>都是必填</strong></p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Coord = Record&lt;<span class="string">'x'</span> | <span class="string">'y'</span>, <span class="built_in">number</span>&gt;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等同于</span></span><br><span class="line"><span class="keyword">type</span> Coord = &#123;</span><br><span class="line">	x: <span class="built_in">number</span>;</span><br><span class="line">	y: <span class="built_in">number</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="实现-1"><a href="#实现-1" class="headerlink" title="实现"></a>实现</h2><h1 id="Readonly"><a href="#Readonly" class="headerlink" title="Readonly"></a>Readonly</h1><p>不管是从字面意思，还是定义上都很好理解：将所有属性定义为自读</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Coord = Readonly&lt;Record&lt;<span class="string">'x'</span> | <span class="string">'y'</span>, <span class="built_in">number</span>&gt;&gt;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等同于</span></span><br><span class="line"><span class="keyword">type</span> Coord = &#123;</span><br><span class="line">  readonly x: <span class="built_in">number</span>;</span><br><span class="line">  readonly x: <span class="built_in">number</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果进行了修改，则会报错：</span></span><br><span class="line"><span class="keyword">const</span> c: Coord = &#123; x: <span class="number">1</span>, y: <span class="number">1</span> &#125;;</span><br><span class="line">c.x = <span class="number">2</span>; <span class="comment">// Error: Cannot assign to 'x' because it is a read-only property.</span></span><br></pre></td></tr></table></figure>

<h1 id="Pick"><a href="#Pick" class="headerlink" title="Pick"></a>Pick</h1><p>从类型定义的属性中，选取指定一组属性，返回一个新的类型定义。</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Coord = Record&lt;<span class="string">'x'</span> | <span class="string">'y'</span>, <span class="built_in">number</span>&gt;;</span><br><span class="line"><span class="keyword">type</span> CoordX = Pick&lt;Coord, <span class="string">'x'</span>&gt;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等用于</span></span><br><span class="line"><span class="keyword">type</span> CoordX = &#123;</span><br><span class="line">	x: <span class="built_in">number</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>一个很实用的实现就是 clone，把 T 里面的都拷贝一次</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> cloneT&lt;T&gt; = &#123;</span><br><span class="line">  [K <span class="keyword">in</span> keyof T]: T[K];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>重要的就是这个in操作符，你完全可以把它理解为就是for…in，也就包含了遍历的过程</p>
<h1 id="Partial-1"><a href="#Partial-1" class="headerlink" title="Partial"></a>Partial</h1><p>clone再可选</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> Partial&lt;T&gt; = &#123;</span><br><span class="line">  [K <span class="keyword">in</span> keyof T]?: T[K];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>参考文献</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>TypeScript</tag>
      </tags>
  </entry>
  <entry>
    <title>webpack之Code Splitting</title>
    <url>/2019/09/26/webpack%E4%B9%8BCode%20Splitting/</url>
    <content><![CDATA[<h3 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h3><p>当我们的业务逻辑很大的时候（index.js很大），我们打包生成的文件会很大：用户打开页面要一次性加载这个很大的文件，需要很长的加载时间；当文件有一点点修改，用户又要重新加载这个很大的文件，又需要很长加载时间。所以可以用code splitting（代码分割）把代码分离到不同的 bundle 中，可以实现按需加载或并行加载这些文件，优化加载时间。</p>
<h3 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h3><p>借用SplitChunksPlugin，可以实现对同步加载的代码或者异步加载的代码实现代码分割。把spliChunk写在webpack配置文件的optimization配置项中。代码如下</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">optimization: &#123;</span><br><span class="line">  splitChunks: &#123;</span><br><span class="line">    chunks: <span class="string">"async"</span>, </span><br><span class="line">    minSize: <span class="number">30000</span>,</span><br><span class="line">    minChunks: <span class="number">1</span>,  </span><br><span class="line">    maxAsyncRequests: <span class="number">5</span>, </span><br><span class="line">    maxInitialRequests: <span class="number">3</span>, </span><br><span class="line">    automaticNameDelimiter: <span class="string">'~'</span>, </span><br><span class="line">    name: <span class="literal">true</span>,</span><br><span class="line">    cacheGroups: &#123; </span><br><span class="line">      vendors: <span class="literal">false</span>, </span><br><span class="line">      <span class="keyword">default</span>: <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="SplitChunksPlugin配置项详解"><a href="#SplitChunksPlugin配置项详解" class="headerlink" title="SplitChunksPlugin配置项详解"></a>SplitChunksPlugin配置项详解</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">splitChunks: &#123;</span><br><span class="line">  chunks: <span class="string">"async"</span>,  <span class="comment">//  做代码分割的时候只对异步代码生效，也可以是initial（同步）或all</span></span><br><span class="line">  minSize: <span class="number">30000</span>,  <span class="comment">//  引入的库大于这个字节才会做代码分割</span></span><br><span class="line">  minChunks: <span class="number">1</span>,  <span class="comment">//  当一个模块至少被用了x次的时候，就对它进行代码分割</span></span><br><span class="line">  maxAsyncRequests: <span class="number">5</span>,   <span class="comment">//  最多只能对x个库进行代码分割，大于这个数，后面的库就不进行代码分割了</span></span><br><span class="line">  maxInitialRequests: <span class="number">3</span>,   <span class="comment">//  入口文件引入的库/js文件最多只能是这个数才都进行代码分割</span></span><br><span class="line">  automaticNameDelimiter: <span class="string">'~'</span>,   <span class="comment">//  文件名连接符</span></span><br><span class="line">  name: <span class="literal">true</span>,</span><br><span class="line">  cacheGroups: &#123;   <span class="comment">//  对同步的代码分割才会走这个配置项</span></span><br><span class="line">    vendors: <span class="literal">false</span>,   <span class="comment">//  异步的时候用false，同步的时候可用官方文档默认配置；</span></span><br><span class="line">    <span class="comment">//  符合这个方案的打包后文件名前缀是vender~，不符合则是走default方案，可用官方文档默认配置</span></span><br><span class="line">    <span class="keyword">default</span>: <span class="literal">false</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Engineering</category>
      </categories>
      <tags>
        <tag>webpack</tag>
      </tags>
  </entry>
  <entry>
    <title>TypeScript基础语法小结</title>
    <url>/2020/04/14/TypeScript%E5%9F%BA%E7%A1%80%E8%AF%AD%E6%B3%95%E5%B0%8F%E7%BB%93/</url>
    <content><![CDATA[<h1 id="初识-TypeScript"><a href="#初识-TypeScript" class="headerlink" title="初识 TypeScript"></a>初识 TypeScript</h1><h2 id="什么是-TS"><a href="#什么是-TS" class="headerlink" title="什么是 TS"></a>什么是 TS</h2><blockquote>
<p>TypeScript 是 JavaScript 的一个超集，主要提供了类型系统和对 ES6 的支持，它由 Microsoft 开发，代码开源于 GitHub 上</p>
</blockquote>
<h2 id="TS-的优点和缺点"><a href="#TS-的优点和缺点" class="headerlink" title="TS 的优点和缺点"></a>TS 的优点和缺点</h2><p>优点：</p>
<ul>
<li>增加了代码的可读性和可维护性。写代码的时候有更多的编译提示，代码语义更清晰易懂</li>
<li>包容性</li>
<li>拥有活跃的社区</li>
</ul>
<p>缺点：</p>
<ul>
<li>一定的学习成本；短期可能会增加一些开发成本，毕竟要多写一些类型的定义</li>
<li>集成到构建流程需要一些工作量</li>
<li>可能和一些库结合的不是很完美</li>
</ul>
<p>详见 <a href="https://ts.xcatliu.com/introduction/what-is-typescript" target="_blank" rel="noopener">什么是 TS</a></p>
<h2 id="基本使用方法"><a href="#基本使用方法" class="headerlink" title="基本使用方法"></a>基本使用方法</h2><ul>
<li>编译代码：在命令行用 tsc &lt;文件名&gt; 的方式编译 .ts 文件，</li>
<li>运行代码：然后用 node 执行生成的 js 文件</li>
<li>安装了 ts-node 之后可以在命令行直接 ts-node &lt;文件名&gt; 的方式编译并执行 .ts 文件</li>
</ul>
<h2 id="静态类型的深度理解"><a href="#静态类型的深度理解" class="headerlink" title="静态类型的深度理解"></a>静态类型的深度理解</h2><ul>
<li>指定变量的类型</li>
<li>变量会具有这个类型的属性和方法</li>
</ul>
<h1 id="常用语法"><a href="#常用语法" class="headerlink" title="常用语法"></a>常用语法</h1><h2 id="基础类型"><a href="#基础类型" class="headerlink" title="基础类型"></a>基础类型</h2><p>个人理解：<strong>对象类型</strong>就是<strong>非基础类型</strong>，包括 函数，自定义类型，类 等</p>
<p>基础类型中，布尔值，数字，字符串基本和 JS 一样；数组有两种定义方式：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第一种（较常用）</span></span><br><span class="line"><span class="keyword">let</span> list: <span class="built_in">number</span>[] = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br><span class="line"><span class="comment">// 第二种</span></span><br><span class="line"><span class="keyword">let</span> list: <span class="built_in">Array</span>&lt;<span class="built_in">number</span>&gt; = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]</span><br></pre></td></tr></table></figure>

<h3 id="元组-Tuple"><a href="#元组-Tuple" class="headerlink" title="元组 Tuple"></a>元组 Tuple</h3><p>一种数组，表示一个已知元素数量和类型的数组，各元素的类型不必相同。eg：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> list: [<span class="built_in">string</span>, <span class="built_in">number</span>] = [<span class="string">'Jacle'</span>, <span class="number">23</span>]</span><br></pre></td></tr></table></figure>

<p>当访问一个越界的元素，会使用联合类型替代 (联合类型：联合类型表示一个值可以是几种类型之一。用 | 产生)</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> list: [<span class="built_in">string</span>, <span class="built_in">number</span>] = [<span class="string">'Jacle'</span>, ’<span class="number">23</span>’]</span><br><span class="line">List[<span class="number">3</span>] = <span class="string">'Chen'</span> <span class="comment">// 可以执行，是string，number 的一种. // 其实还是会报错，说这个元组没有index3</span></span><br><span class="line">list[<span class="number">4</span>] = <span class="literal">true</span> <span class="comment">// Error</span></span><br></pre></td></tr></table></figure>

<h3 id="枚举-enum"><a href="#枚举-enum" class="headerlink" title="枚举 enum"></a>枚举 enum</h3><p>enum 类型是对 JavaScript 标准数据类型的一个补充。 像 C# 等其它语言一样，使用枚举类型可以为一组数值赋予友好的名字</p>
<h4 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h4><blockquote>
<p>普通 JS 的情况</p>
</blockquote>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> Status = &#123; <span class="comment">// eg. 处理一些后端code会用到这种写法</span></span><br><span class="line">  OFFLINE: <span class="number">0</span>,</span><br><span class="line">  ONLINE: <span class="number">1</span>,</span><br><span class="line">  DELETED: <span class="number">2</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getResult</span>(<span class="params">status</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (status === Status.OFFLINE) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">'offline'</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (status === Status.ONLINE) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">'online'</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (status === Status.DELETED) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">'deleted'</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> status;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">const</span> result = getResult(Status.OFFLINE);</span><br><span class="line"><span class="built_in">console</span>.log(result);</span><br></pre></td></tr></table></figure>

<blockquote>
<p>应用枚举的情况</p>
</blockquote>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> Status &#123;</span><br><span class="line">  OFFLINE,</span><br><span class="line">  ONLINE,</span><br><span class="line">  DELETED</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getResult</span>(<span class="params">status</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (status === Status.OFFLINE) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">'offline'</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (status === Status.ONLINE) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">'online'</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (status === Status.DELETED) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">'deleted'</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> status;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">const</span> result = getResult(Status.OFFLINE);</span><br></pre></td></tr></table></figure>

<h4 id="枚举成员的初始化"><a href="#枚举成员的初始化" class="headerlink" title="枚举成员的初始化"></a>枚举成员的初始化</h4><ul>
<li>不对枚举成员的元素进行初始化的时候，默认其值是 0，1，2 … ；给某个成为初始化为某个数字都时候，后面的成员会默认接着这个数字进行初始化</li>
</ul>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> Status &#123;</span><br><span class="line">  OFFLINE,</span><br><span class="line">  ONLINE,</span><br><span class="line">  DELETED = <span class="number">100</span>,</span><br><span class="line">  TEST</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(Status.OFFLINE) <span class="comment">// 0</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">typeof</span> Status.OFFLINE) <span class="comment">// number</span></span><br><span class="line"><span class="built_in">console</span>.log(Status[<span class="number">0</span>]) <span class="comment">// OFFLINE 反向查枚举成员的名称</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">typeof</span> Status[<span class="number">0</span>]) <span class="comment">// string</span></span><br><span class="line"><span class="built_in">console</span>.log(Status.ONLINE) <span class="comment">// 1</span></span><br><span class="line"><span class="built_in">console</span>.log(Status.DELETED) <span class="comment">// 100</span></span><br><span class="line"><span class="built_in">console</span>.log(Status.TEST) <span class="comment">// 101</span></span><br></pre></td></tr></table></figure>

<ul>
<li>对某个成员初始化为非数字类型时，接下来的其他成员都得进行显式初始化，直到某个成员被显示初始化为数字类型</li>
</ul>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> Status &#123;</span><br><span class="line">  OFFLINE,</span><br><span class="line">  ONLINE = <span class="string">'jacle'</span>,</span><br><span class="line">  DELETED = <span class="string">'chen'</span>,</span><br><span class="line">  TEST = <span class="number">10</span>,</span><br><span class="line">  TETS2</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(Status.OFFLINE) <span class="comment">// 0</span></span><br><span class="line"><span class="built_in">console</span>.log(Status.ONLINE) <span class="comment">// jacle</span></span><br><span class="line"><span class="built_in">console</span>.log(Status.DELETED) <span class="comment">// chen</span></span><br><span class="line"><span class="built_in">console</span>.log(Status.TEST) <span class="comment">// 10</span></span><br><span class="line"><span class="built_in">console</span>.log(Status.TETS2) <span class="comment">// 11</span></span><br></pre></td></tr></table></figure>

<p>更详见 <a href="https://www.tslang.cn/docs/handbook/enums.html" target="_blank" rel="noopener">官方文档：枚举</a></p>
<h3 id="any-常用"><a href="#any-常用" class="headerlink" title="any (常用)"></a>any (常用)</h3><p>我们不希望类型检查器对这些值进行检查而是直接让它们通过编译阶段的检查。 那么我们可以使用 any 类型来标记这些变量</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> notSure: <span class="built_in">any</span> = <span class="number">4</span></span><br><span class="line">notSure = <span class="string">'maybe a string instead'</span></span><br><span class="line"><span class="keyword">let</span> list: <span class="built_in">any</span>[] = [<span class="number">1</span>, <span class="literal">true</span>, <span class="string">'free'</span>]</span><br></pre></td></tr></table></figure>

<h3 id="void"><a href="#void" class="headerlink" title="void"></a>void</h3><p>void 类型像是与 any 类型相反，它表示没有任何类型。一般用于函数没返回值的时候使用；void 变量则只能赋值为 undefined 和 null</p>
<h3 id="undefined-和-null-少用"><a href="#undefined-和-null-少用" class="headerlink" title="undefined 和 null (少用)"></a>undefined 和 null (少用)</h3><p>两者各自有自己的类型分别叫做 undefined 和 null。 和 void 相似，它们的本身的类型用处不是很大</p>
<h3 id="never-少用"><a href="#never-少用" class="headerlink" title="never (少用)"></a>never (少用)</h3><p>never 类型表示的是那些永不存在的值的类型。 例如， never 类型是那些总是会抛出异常或根本就不会有返回值的函数表达式或箭头函数表达式的返回值类型。eg：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 返回never的函数必须存在无法达到的终点</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">infiniteLoop</span>(<span class="params"></span>): <span class="title">never</span> </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="object"><a href="#object" class="headerlink" title="object"></a>object</h3><p>object 表示非原始类型，也就是除 number，string，boolean，symbol，null 或 undefined 之外的类型</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">declare</span> <span class="function"><span class="keyword">function</span> <span class="title">create</span>(<span class="params">o: object | <span class="literal">null</span></span>): <span class="title">void</span></span></span><br><span class="line"><span class="function"><span class="title">create</span>(<span class="params">&#123; prop: 0 &#125;</span>) // <span class="title">OK</span></span></span><br><span class="line"><span class="function"><span class="title">create</span>(<span class="params"><span class="literal">null</span></span>) // <span class="title">OK</span></span></span><br><span class="line"><span class="function"><span class="title">create</span>(<span class="params">42</span>) // <span class="title">Error</span></span></span><br></pre></td></tr></table></figure>

<h3 id="类型断言"><a href="#类型断言" class="headerlink" title="类型断言"></a>类型断言</h3><p>使用类型断言的时候，TS 认为程序员已经很确定这个变量的类型了，会假设程序员已经做了必须的检查，常和 <code>any</code> 搭配使用。有两种写法：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// &lt;&gt;写法</span></span><br><span class="line"><span class="keyword">let</span> someVal: <span class="built_in">any</span> = <span class="string">'this is a string, I sure'</span></span><br><span class="line"><span class="keyword">let</span> strLength: <span class="built_in">number</span> = (&lt;<span class="built_in">string</span>&gt;someVal).length</span><br><span class="line"><span class="built_in">console</span>.log(strLength) <span class="comment">// 24</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// as写法 (似乎比较好记)</span></span><br><span class="line"><span class="keyword">let</span> someVal: <span class="built_in">any</span> = <span class="string">'this is a string, I sure'</span></span><br><span class="line"><span class="keyword">let</span> strLength: <span class="built_in">number</span> = (someVal <span class="keyword">as</span> <span class="built_in">string</span>).length</span><br></pre></td></tr></table></figure>

<h2 id="类型注解（type-annotation）-amp-类型推断-（type-inference）"><a href="#类型注解（type-annotation）-amp-类型推断-（type-inference）" class="headerlink" title="类型注解（type annotation） &amp; 类型推断 （type inference）"></a>类型注解（type annotation） &amp; 类型推断 （type inference）</h2><p>类型注解：明显写出来的，我们告诉 TS 变量是什么类型</p>
<p>类型推断：TS 自动的去尝试分析变量类型。当 TS 无法分析出来的时候，就需要我们写类型注解</p>
<h2 id="变量声明"><a href="#变量声明" class="headerlink" title="变量声明"></a>变量声明</h2><p>同 JS：var，let， const</p>
<h2 id="类型别名（type-alias）"><a href="#类型别名（type-alias）" class="headerlink" title="类型别名（type alias）"></a>类型别名（type alias）</h2><p>就是 type 这种语法</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> User = &#123;</span><br><span class="line">  name: <span class="built_in">string</span>,</span><br><span class="line">  age: <span class="built_in">number</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> arr: User[] = [&#123;</span><br><span class="line">  name: <span class="string">'jacle'</span>,</span><br><span class="line">  age: <span class="number">23</span></span><br><span class="line">&#125;]</span><br><span class="line"><span class="keyword">type</span> UserName = <span class="string">'jacle'</span> | <span class="string">'jaclee'</span></span><br></pre></td></tr></table></figure>

<p>个人理解：类型别名 和 接口 的区别不大，类型别名 能表示基础类型，接口 只能表示对象类型；能用接口的时候尽量用接口</p>
<h2 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h2><blockquote>
<p>TypeScript 的核心原则之一是对值所具有的结构进行类型检查。 在 TypeScript 里，接口的作用就是为这些类型命名和为你的代码或第三方代码定义契约</p>
</blockquote>
<p>个人理解是：接口是对一类数据结构的约定和描述</p>
<h3 id="可选属性"><a href="#可选属性" class="headerlink" title="可选属性"></a>可选属性</h3><p>可有可无的属性。eg：<code>color?: string;</code></p>
<h3 id="只读属性"><a href="#只读属性" class="headerlink" title="只读属性"></a>只读属性</h3><p>只能在刚创建的时候赋值，后面不能修改，在接口的属性前加 <code>readonly</code>。eg:</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> Point &#123;</span><br><span class="line">  readonly x: <span class="built_in">number</span>,</span><br><span class="line">  readonly y: <span class="built_in">string</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> point1: Point = &#123; x: <span class="number">2</span>, y: <span class="string">'test'</span>&#125;</span><br><span class="line">point1.x = <span class="number">3</span> <span class="comment">// error</span></span><br></pre></td></tr></table></figure>

<p>此外，TypeScript 的数组具有 <code>ReadonlyArray&lt;T&gt;</code> 类型，效果一样</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> ro: ReadonlyArray&lt;<span class="built_in">number</span>&gt; = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br></pre></td></tr></table></figure>

<p>总结：不能变的属性用 readonly，不能变的变量用 const</p>
<h4 id="一个作用：接口可能带有任意数量的其它属性"><a href="#一个作用：接口可能带有任意数量的其它属性" class="headerlink" title="一个作用：接口可能带有任意数量的其它属性"></a>一个作用：接口可能带有任意数量的其它属性</h4><p>如果我们创建一个接口，该接口除了我们明确约定好的属性，还可能会带有任意数量的其它属性，我们这时候就可以用 添加一个字符串索引签名</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> People &#123;</span><br><span class="line">  age: <span class="built_in">number</span>,</span><br><span class="line">  name: <span class="built_in">string</span>,</span><br><span class="line">  [propName: <span class="built_in">string</span>]: <span class="built_in">any</span>,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但是其实这种情况还有一个不用索引类型的知识点（虽然不太严谨）：直接传的对象有多余属性会报错，传的是变量的时候就不会</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> Person &#123;</span><br><span class="line">  name: <span class="built_in">string</span></span><br><span class="line">  age?: <span class="built_in">number</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getName</span>(<span class="params">person: Person</span>): <span class="title">void</span> </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(person.name)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> jacle = &#123;</span><br><span class="line">  name: <span class="string">'jacle'</span>,</span><br><span class="line">  school: <span class="string">'SYSU'</span></span><br><span class="line">&#125;</span><br><span class="line">getName(&#123; <span class="comment">// Error，school 不在 Person 中</span></span><br><span class="line">  name: <span class="string">'jacle'</span>,</span><br><span class="line">  school: <span class="string">'SYSU'</span></span><br><span class="line">&#125;)</span><br><span class="line">getName(jacle) <span class="comment">// 正常</span></span><br></pre></td></tr></table></figure>

<h3 id="函数类型"><a href="#函数类型" class="headerlink" title="函数类型"></a>函数类型</h3><p>接口也可以描述函数类型</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> searchFun &#123;</span><br><span class="line">  (sour: <span class="built_in">string</span>, data: <span class="built_in">number</span>): <span class="built_in">boolean</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> testFun: searchFun = <span class="function"><span class="keyword">function</span>(<span class="params">sour: <span class="built_in">string</span>, data: <span class="built_in">number</span></span>): <span class="title">boolean</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> data &gt; <span class="number">0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="类类型"><a href="#类类型" class="headerlink" title="类类型"></a>类类型</h3><p>接口也能用来描述类，强制一个类去满足某种契约。用 <code>implements</code><br>接口描述了类的公共部分，而不是公共和私有两部分。 它不会帮你检查类是否具有某些私有成员</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> ClockInt &#123;</span><br><span class="line">  currTime: <span class="built_in">Date</span>,</span><br><span class="line">  setTime(d: <span class="built_in">Date</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> Clock <span class="keyword">implements</span> ClockInt &#123;</span><br><span class="line">  currTime: <span class="built_in">Date</span></span><br><span class="line">  setTime(d: <span class="built_in">Date</span>) &#123;</span><br><span class="line">    <span class="keyword">this</span>.currTime = d</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">constructor</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">this</span>.currTime = <span class="keyword">new</span> <span class="built_in">Date</span>()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="接口的继承"><a href="#接口的继承" class="headerlink" title="接口的继承"></a>接口的继承</h3><p>一个接口可以继承一个或多个接口，创建出多个接口的合成接口。用 <code>extends</code></p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> class1 &#123;</span><br><span class="line">  color: <span class="built_in">string</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">interface</span> class2 &#123;</span><br><span class="line">  age: <span class="built_in">number</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">interface</span> class3 &#123;</span><br><span class="line">  city: <span class="built_in">string</span>[]</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">interface</span> people <span class="keyword">extends</span> class1, class2, class3 &#123;</span><br><span class="line">  ishandsome: <span class="built_in">boolean</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> jacle: people = &#123;</span><br><span class="line">  color: <span class="string">'blue'</span>,</span><br><span class="line">  age: <span class="number">23</span>,</span><br><span class="line">  city: [<span class="string">'beijing'</span>, <span class="string">'guangzhou'</span>],</span><br><span class="line">  ishandsome: <span class="literal">true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="可索引类型-amp-amp-索引签名"><a href="#可索引类型-amp-amp-索引签名" class="headerlink" title="可索引类型 &amp;&amp; 索引签名"></a>可索引类型 &amp;&amp; 索引签名</h2><p>接口也可以描述那些能够“通过索引得到”的类型，比如数组或对象: <code>a[10]</code> 或 <code>ageMap[&#39;daniel&#39;]</code>。 可索引类型具有一个<strong>索引签名</strong>，它描述了<strong>对象索引的类型，还有相应的索引返回值类型</strong>(两部分)</p>
<blockquote>
<p>一般情况下， 索引 和 索引签名 是一个概念；当我们<strong>声明一个索引签名</strong>的时候，就是声明了索引的类型，还有相应的索引返回值类型（两部分）</p>
</blockquote>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> StrArr &#123;</span><br><span class="line">  [index: <span class="built_in">number</span>]: <span class="built_in">string</span> <span class="comment">// 暂时理解为这部分是索引签名。</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> strA: StrArr = [<span class="string">'a'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>]</span><br><span class="line"><span class="keyword">let</span> myStr: <span class="built_in">string</span> = strA[<span class="number">0</span>];</span><br></pre></td></tr></table></figure>

<blockquote>
<p>这个 index 只是发挥可读性的作用，你可以随便命名</p>
</blockquote>
<h3 id="索引签名"><a href="#索引签名" class="headerlink" title="索引签名"></a><a href="https://jkchao.github.io/typescript-book-chinese/typings/indexSignatures.html#typescript-%E7%B4%A2%E5%BC%95%E7%AD%BE%E5%90%8D" target="_blank" rel="noopener">索引签名</a></h3><p>可看文档</p>
<h3 id="JS中"><a href="#JS中" class="headerlink" title="JS中"></a>JS中</h3><ol>
<li><p>在JS中，一般认为索引签名的类型是字符串和数字</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> jacle = &#123;</span><br><span class="line">  name: <span class="string">'jacle'</span>,</span><br><span class="line">  age: <span class="number">17</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(jacle[<span class="string">'name'</span>]) <span class="comment">// 索引签名是字符串</span></span><br><span class="line"><span class="keyword">let</span> people = [jacle]</span><br><span class="line"><span class="built_in">console</span>.log(people[<span class="number">0</span>]) <span class="comment">// 索引签名是数字</span></span><br></pre></td></tr></table></figure></li>
<li><p>当我们把索引签名搞成对象时，JavaScript 会在得到结果之前会先调用 .toString 方法：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> obj = &#123;</span><br><span class="line">  toString() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'toString called'</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">'Hello'</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> foo: <span class="built_in">any</span> = &#123;&#125;;</span><br><span class="line">foo[obj] = <span class="string">'World'</span>; <span class="comment">// toString called</span></span><br><span class="line"><span class="built_in">console</span>.log(foo[obj]); <span class="comment">// toString called, World</span></span><br><span class="line"><span class="built_in">console</span>.log(foo[<span class="string">'Hello'</span>]); <span class="comment">// World</span></span><br></pre></td></tr></table></figure></li>
</ol>
<blockquote>
<p>只要索引位置使用了 obj，toString 方法都将会被调用</p>
</blockquote>
<h3 id="TS中"><a href="#TS中" class="headerlink" title="TS中"></a>TS中</h3><p>TypeScript 的索引签名必须是 string 或者 number。我们搞 obj 进去会报错的，强制用户必须明确的写出 toString()</p>
<h4 id="声明一个索引签名"><a href="#声明一个索引签名" class="headerlink" title="声明一个索引签名"></a>声明一个索引签名</h4><figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> foo: &#123;</span><br><span class="line">  [index: <span class="built_in">string</span>]: &#123; message: <span class="built_in">string</span> &#125;;</span><br><span class="line">&#125; = &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 储存的东西必须符合结构</span></span><br><span class="line"><span class="comment">// ok</span></span><br><span class="line">foo[<span class="string">'a'</span>] = &#123; message: <span class="string">'some message'</span> &#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Error, 必须包含 `message`</span></span><br><span class="line">foo[<span class="string">'a'</span>] = &#123; messages: <span class="string">'some message'</span> &#125;;</span><br></pre></td></tr></table></figure>

<h4 id="所有成员都必须符合字符串的索引签名"><a href="#所有成员都必须符合字符串的索引签名" class="headerlink" title="所有成员都必须符合字符串的索引签名"></a>所有成员都必须符合字符串的索引签名</h4><figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ok</span></span><br><span class="line"><span class="keyword">interface</span> Foo &#123;</span><br><span class="line">  [key: <span class="built_in">string</span>]: <span class="built_in">number</span>;</span><br><span class="line">  x: <span class="built_in">number</span>;</span><br><span class="line">  y: <span class="built_in">number</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Error</span></span><br><span class="line"><span class="keyword">interface</span> Bar &#123;</span><br><span class="line">  [key: <span class="built_in">string</span>]: <span class="built_in">number</span>;</span><br><span class="line">  x: <span class="built_in">number</span>;</span><br><span class="line">  y: <span class="built_in">string</span>; <span class="comment">// Error: y 属性必须为 number 类型</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="使用字符串字面量-字符串组成的联合类型-当索引签名"><a href="#使用字符串字面量-字符串组成的联合类型-当索引签名" class="headerlink" title="使用字符串字面量(字符串组成的联合类型)当索引签名"></a>使用字符串字面量(字符串组成的联合类型)当索引签名</h4><p>一个索引签名可以通过<strong>映射类型</strong>来使索引字符串为联合类型中的一员：</p>
<blockquote>
<p>映射类型是 in、keyof、typeof 这些？</p>
</blockquote>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> index = <span class="string">'a'</span> | <span class="string">'b'</span> | <span class="string">'c'</span>;</span><br><span class="line"><span class="keyword">type</span> fromIndex = &#123;</span><br><span class="line">  [K <span class="keyword">in</span> index]: <span class="built_in">number</span>; </span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">const</span> good: FromIndex = &#123; b: <span class="number">1</span>, c: <span class="number">2</span> &#125;;</span><br><span class="line"><span class="comment">// Error:</span></span><br><span class="line"><span class="comment">// `&#123; b: 1, c: 2, d: 3 &#125;` 不能分配给 'FromIndex'</span></span><br><span class="line"><span class="comment">// 对象字面量只能指定已知类型，'d' 不存在 'FromIndex' 类型上</span></span><br><span class="line"><span class="keyword">const</span> bad: FromIndex = &#123; b: <span class="number">1</span>, c: <span class="number">2</span>, d: <span class="number">3</span> &#125;;</span><br></pre></td></tr></table></figure>

<h4 id="同时拥有-string-和-number-类型的索引签名-少用"><a href="#同时拥有-string-和-number-类型的索引签名-少用" class="headerlink" title="同时拥有 string 和 number 类型的索引签名(少用)"></a>同时拥有 string 和 number 类型的索引签名(少用)</h4><p>TypeScript 可以同时使用字符串和数字两种类型的索引，但是数字索引的返回值必须是字符串索引返回值类型的子类型</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> ArrStr &#123;</span><br><span class="line">  [key: <span class="built_in">string</span>]: <span class="built_in">string</span> | <span class="built_in">number</span>; <span class="comment">// 必须包括所用成员类型</span></span><br><span class="line">  [index: <span class="built_in">number</span>]: <span class="built_in">string</span>; <span class="comment">// 字符串索引类型的子级</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// example</span></span><br><span class="line">  length: <span class="built_in">number</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="类"><a href="#类" class="headerlink" title="类"></a>类</h2><h3 id="基本例子"><a href="#基本例子" class="headerlink" title="基本例子"></a>基本例子</h3><figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> Greeter &#123;</span><br><span class="line">  greeting: <span class="built_in">string</span></span><br><span class="line">  <span class="keyword">constructor</span>(<span class="params">message: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">    <span class="keyword">this</span>.greeting = message</span><br><span class="line">  &#125;</span><br><span class="line">  greet() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'Hello, '</span> + <span class="keyword">this</span>.greeting)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> greeter = <span class="keyword">new</span> Greeter(<span class="string">'world'</span>)</span><br><span class="line">greeter.greet()</span><br></pre></td></tr></table></figure>

<p>创建了一个 Greeter 类，有一个 greeting 属性，一个构造函数 constructor（所以构造函数中也得有 this.greeting）和一个 greet 方法</p>
<h3 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h3><p>同 ES6</p>
<p>此外，子类可以通过直接重写方法的形式重写父类的方法；重写的时候如果想调用父类的方法可以用 super</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> Person &#123;</span><br><span class="line">  name = <span class="string">'jacle'</span></span><br><span class="line">  getName() &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">this</span>.name</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">class</span> Teacher <span class="keyword">extends</span> Person &#123;</span><br><span class="line">  getName() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">super</span>.getName() + <span class="string">' new'</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> jacle = <span class="keyword">new</span> Teacher()</span><br><span class="line"><span class="built_in">console</span>.log(jacle.getName()) <span class="comment">// ‘jacle new'</span></span><br></pre></td></tr></table></figure>

<h3 id="公共，私有与受保护的修饰符"><a href="#公共，私有与受保护的修饰符" class="headerlink" title="公共，私有与受保护的修饰符"></a>公共，私有与受保护的修饰符</h3><p>类的属性，静态属性，方法，构造函数 都可以加这些修饰符前缀，默认都是 public</p>
<ul>
<li>默认为 public。外部可访问</li>
<li>private。外部不可访问，只有内部（这个类自己）的方法可以访问</li>
<li>protected。只有 类自己 和 类的后代 可以访问</li>
<li>也可加 readonly 修饰符，使得属性无法被后续修改</li>
</ul>
<p>注意下面这两种写法都可以：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 写法一</span></span><br><span class="line"><span class="keyword">class</span> Greeter &#123;</span><br><span class="line">  <span class="keyword">protected</span> greeting: <span class="built_in">string</span></span><br><span class="line">  <span class="keyword">constructor</span>(<span class="params">message: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">    <span class="keyword">this</span>.greeting = message</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 写法二</span></span><br><span class="line"><span class="keyword">class</span> Greeter &#123;</span><br><span class="line">  <span class="keyword">constructor</span>(<span class="params"><span class="keyword">protected</span> message: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">    <span class="keyword">this</span>.greeting = message</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="getter-和-setter"><a href="#getter-和-setter" class="headerlink" title="getter 和 setter"></a>getter 和 setter</h3><p>可以把属性写成 pravite 的，然后定义 getter 和 setter 进行属性的读改</p>
<p>用法：用 <code>get</code> 和 <code>set</code>，一般会和 pravite 属性配合使用</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> Person3 &#123;</span><br><span class="line">  <span class="keyword">constructor</span>(<span class="params"><span class="keyword">private</span> _name: <span class="built_in">string</span></span>) &#123; <span class="comment">// 内部属性可以用 _ 进行区分，可以和 get 相配合</span></span><br><span class="line">    <span class="keyword">this</span>._name = _name</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">get</span> name() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>._name</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">set</span> name(name: <span class="built_in">string</span>) &#123;</span><br><span class="line">    <span class="comment">// 可以加额外的处理逻辑</span></span><br><span class="line">    <span class="keyword">this</span>._name = name</span><br><span class="line"></span><br><span class="line">  &#125;</span><br><span class="line">  getName() &#123; <span class="comment">// low 的写法</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>._name</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> jacle2 = <span class="keyword">new</span> Person3(<span class="string">'jacle'</span>)</span><br><span class="line"><span class="built_in">console</span>.log(jacle2.name) <span class="comment">// 用了 get 之后可以直接这么用</span></span><br><span class="line"><span class="built_in">console</span>.log(jacle2.getName())</span><br><span class="line">jacle2.name = <span class="string">'jack'</span> <span class="comment">// 调用了 setter</span></span><br></pre></td></tr></table></figure>

<h3 id="静态属性"><a href="#静态属性" class="headerlink" title="静态属性"></a>静态属性</h3><p>属性加 static 前缀。静态属性存在于<strong>类本身</strong>上面而不是类的实例上，但是实例会用到这个属性，访问方法是 <code>类.静态属性</code></p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> ChenFamilyPeo &#123;</span><br><span class="line">  <span class="keyword">static</span> lastName = <span class="string">'Chen'</span></span><br><span class="line">  <span class="keyword">public</span> firstname: <span class="built_in">string</span></span><br><span class="line">  <span class="keyword">constructor</span>(<span class="params">firstname: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">    <span class="keyword">this</span>.firstname = firstname</span><br><span class="line">  &#125;</span><br><span class="line">  getFullName() &#123;</span><br><span class="line">    <span class="keyword">return</span> ChenFamilyPeo.lastName + <span class="string">' '</span> + <span class="keyword">this</span>.firstname</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> jacle = <span class="keyword">new</span> chenFamilyPeo(<span class="string">'Junjia'</span>)</span><br><span class="line"><span class="built_in">console</span>.log(jacle.getFullName())  <span class="comment">// Chen Junjia</span></span><br><span class="line"><span class="built_in">console</span>.log(ChenFamilyPeo.lastName)  <span class="comment">// Chen</span></span><br></pre></td></tr></table></figure>

<h4 id="应用举例：TS-中的单例模式"><a href="#应用举例：TS-中的单例模式" class="headerlink" title="应用举例：TS 中的单例模式"></a>应用举例：TS 中的单例模式</h4><figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> SingleObj &#123;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">constructor</span>(<span class="params"><span class="keyword">private</span> name: <span class="built_in">string</span>, <span class="keyword">private</span> age: <span class="built_in">number</span></span>) &#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">    <span class="keyword">this</span>.age = age</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> instance: SingleObj</span><br><span class="line">  <span class="keyword">static</span> getInstance(name: <span class="built_in">string</span>, age: <span class="built_in">number</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (!<span class="keyword">this</span>.instance) &#123;</span><br><span class="line">      <span class="keyword">this</span>.instance = <span class="keyword">new</span> SingleObj(name, age)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.instance</span><br><span class="line">  &#125;</span><br><span class="line">  getAge() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.age</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> person1 = SingleObj.getInstance(<span class="string">'jacle'</span>, <span class="number">23</span>)</span><br><span class="line"><span class="built_in">console</span>.log(person1.getAge()) <span class="comment">// 23</span></span><br><span class="line"><span class="keyword">let</span> person2 = SingleObj.getInstance(<span class="string">'jacle'</span>, <span class="number">24</span>)</span><br><span class="line"><span class="built_in">console</span>.log(person1 === person2) <span class="comment">// true</span></span><br><span class="line"><span class="built_in">console</span>.log(person2.getAge()) <span class="comment">// 23</span></span><br></pre></td></tr></table></figure>

<h3 id="抽象类-少用"><a href="#抽象类-少用" class="headerlink" title="抽象类 (少用)"></a>抽象类 (少用)</h3><p>抽象类做为其它派生类的基类使用（抽象子类 === 派生类）。 它们一般不会直接被实例化。 abstract 关键字是用于定义抽象类和在抽象类内部定义抽象方法<br>并且派生类一定要有抽象类中定义的属性和方法。</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="keyword">class</span> Department &#123;</span><br><span class="line">  name: <span class="built_in">string</span></span><br><span class="line">  <span class="keyword">constructor</span>(<span class="params">name: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">  &#125;</span><br><span class="line">  printName(): <span class="built_in">void</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'Department name: '</span> + <span class="keyword">this</span>.name)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">abstract</span> printMeeting(): <span class="built_in">void</span> <span class="comment">// 必须在派生类中实现</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> AccountingDepartment <span class="keyword">extends</span> Department &#123;</span><br><span class="line">  <span class="keyword">constructor</span>(<span class="params">name: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">    <span class="keyword">super</span>(name) <span class="comment">// 在派生类的构造函数中必须调用 super()</span></span><br><span class="line">  &#125;</span><br><span class="line">  printMeeting(): <span class="built_in">void</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'The Accounting Department meets each Monday at 10am.'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  generateReports(): <span class="built_in">void</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'Generating accounting reports...'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> department: Department <span class="comment">// 允许创建一个对抽象类型的引用</span></span><br><span class="line">department = <span class="keyword">new</span> Department() <span class="comment">// 错误: 不能创建一个抽象类的实例</span></span><br><span class="line">department = <span class="keyword">new</span> AccountingDepartment(<span class="string">'客增研发'</span>) <span class="comment">// 允许对一个抽象子类进行实例化和赋值</span></span><br><span class="line"><span class="built_in">console</span>.log(department.name) <span class="comment">// 客增研发</span></span><br><span class="line">department.printName() <span class="comment">// Department name: 客增研发</span></span><br><span class="line">department.printMeeting() <span class="comment">// The Accounting Department meets each Monday at 10am.</span></span><br><span class="line">department.generateReports() <span class="comment">// 错误: 方法在声明的抽象类中不存在</span></span><br></pre></td></tr></table></figure>

<h2 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h2><h3 id="函数类型-1"><a href="#函数类型-1" class="headerlink" title="函数类型"></a>函数类型</h3><figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数定义类型</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">x: <span class="built_in">number</span>, y:<span class="built_in">number</span></span>): <span class="title">number</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> x + y</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> add = <span class="function"><span class="keyword">function</span>(<span class="params">x: <span class="built_in">number</span>, y:<span class="built_in">number</span></span>): <span class="title">number</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> x + y</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//完整函数类型</span></span><br><span class="line"><span class="keyword">let</span> add: <span class="function">(<span class="params">x:<span class="built_in">number</span>, y:<span class="built_in">number</span></span>) =&gt;</span> <span class="built_in">number</span> =</span><br><span class="line"><span class="function"><span class="keyword">function</span>(<span class="params">x: <span class="built_in">number</span>, y:<span class="built_in">number</span></span>): <span class="title">number</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> x + y</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="可选参数-amp-默认参数"><a href="#可选参数-amp-默认参数" class="headerlink" title="可选参数 &amp; 默认参数"></a>可选参数 &amp; 默认参数</h3><ul>
<li>TS 中参数是必须的，不能是 undifined，不能多也不能少。可以用 <code>?</code> 表示可选参数</li>
<li>默认参数同 ES6 如果位置靠前但是想启用默认参数，则传 undefined 即可</li>
</ul>
<p>当我们想传好几个参数，但是参数不是必选的时候，有时候执行起来会不太优雅：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> testFn = <span class="function">(<span class="params">a?: <span class="built_in">number</span>, b?: <span class="built_in">number</span>, c?: <span class="built_in">number</span> </span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'a'</span>, a);</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'c'</span>, c);</span><br><span class="line">&#125;</span><br><span class="line">testFn(<span class="number">1</span>, <span class="literal">undefined</span>, <span class="number">2</span>);</span><br></pre></td></tr></table></figure>

<p>可以改成这种对象的写法:</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> testFn = <span class="function">(<span class="params">ops: &#123; a?: <span class="built_in">number</span>, b?: <span class="built_in">number</span>, c?: <span class="built_in">number</span> &#125;</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'a'</span>, ops.a);</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'c'</span>, ops.c);</span><br><span class="line">&#125;</span><br><span class="line">testFn(&#123; a: <span class="number">1</span>, c: <span class="number">2</span>&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="函数重载"><a href="#函数重载" class="headerlink" title="函数重载"></a>函数重载</h3><p>个人理解函数重载就是：函数根据传入不同的参数而返回不同类型的数据的场景 / 执行不同的逻辑。<br>在 TS 当中，除了在函数内部写判断类型并具体执行的逻辑之外，还要在函数之前 为同一个函数提供多个函数类型定义来进行函数重载</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">pickCard</span>(<span class="params">x: &#123;suit: <span class="built_in">string</span>; card: <span class="built_in">number</span> &#125;[]</span>): <span class="title">number</span></span></span><br><span class="line"><span class="function"><span class="function"><span class="keyword">function</span> <span class="title">pickCard</span>(<span class="params">x: <span class="built_in">number</span></span>): </span>&#123;<span class="title">suit</span>: <span class="title">string</span></span>; card: <span class="built_in">number</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// function pickCard(x): any 并不是重载列表的一部分，因此这里只有两个重载：一个是接收对象数组，另一个接收数字</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">pickCard</span>(<span class="params">x</span>): <span class="title">any</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">Array</span>.isArray(x)) &#123;</span><br><span class="line">    <span class="comment">// 逻辑部分</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span></span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="keyword">typeof</span> x === <span class="string">'number'</span>) &#123;</span><br><span class="line">    <span class="comment">// 逻辑部分</span></span><br><span class="line">    <span class="keyword">return</span> &#123; suit: <span class="string">'jack'</span>, card: <span class="number">1</span> &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>在定义重载的时候，一定要把最精确的定义放在最前面</p>
</blockquote>
<h2 id="泛型"><a href="#泛型" class="headerlink" title="泛型"></a>泛型</h2><p>泛型，泛指的类型</p>
<p>个人理解：泛型主要是用来<strong>提高数据类型方面的复用性</strong>：组件不仅能够支持当前的数据类型，同时也能支持未来的数据类型，比单纯写 string | number 等这种联合类型写法复用性更高<br>进一步理解：泛形应该理解为一个储存池，用来保存可能会用到的类型，相当于<strong>加了若干个类型参数</strong>。捕获输入类型只是泛型的常见应用</p>
<h3 id="类型变量-T"><a href="#类型变量-T" class="headerlink" title="类型变量 T"></a>类型变量 T</h3><p>语法是 <code>&lt;T&gt;</code>，可以帮助我们捕获用户传入的类型，eg：定义一个会返回任何传入它的值的函数</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">identity</span>&lt;<span class="title">T</span>&gt;(<span class="params">arg: T</span>): <span class="title">T</span> </span>&#123; <span class="comment">// 编译器会认为 T 是任意类型，所以该函数具有强的通用性</span></span><br><span class="line">  <span class="keyword">return</span> arg</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 不好的写法：</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">identity</span>(<span class="params">arg: <span class="built_in">any</span></span>): <span class="title">any</span> </span>&#123; <span class="comment">// 用any能有同样的效果，但是我们会丢失 参数类型 ，这个信息</span></span><br><span class="line">  <span class="keyword">return</span> arg</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们把这个版本的 identity 函数叫做泛型，因为它可以适用于多个类型。可以使用泛型来创建可重用的组件<br>我们可以这么用该函数</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> str = identity(<span class="string">'jacle'</span>) <span class="comment">// 编译器会根据传入的参数自动地帮助我们确定 T 的类型</span></span><br></pre></td></tr></table></figure>

<h3 id="泛型类型-amp-接口-amp-类"><a href="#泛型类型-amp-接口-amp-类" class="headerlink" title="泛型类型 &amp; 接口 &amp; 类"></a>泛型类型 &amp; 接口 &amp; 类</h3><figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">rec</span>&lt;<span class="title">T</span>&gt;(<span class="params">arg: T</span>): <span class="title">T</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> arg</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> newre: &lt;U&gt;<span class="function">(<span class="params">arg: U</span>) =&gt;</span> U = rec</span><br></pre></td></tr></table></figure>

<p>泛型接口</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> GenericIdentityFn&lt;T&gt; &#123;</span><br><span class="line">  (arg: T): T</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">identity</span>&lt;<span class="title">T</span>&gt;(<span class="params">arg: T</span>): <span class="title">T</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> arg</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> myIdentity: GenericIdentityFn&lt;<span class="built_in">number</span>&gt; = identity</span><br></pre></td></tr></table></figure>

<p>泛型类，与接口类似</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">join</span>&lt;<span class="title">T</span>&gt;(<span class="params">a: T, b: T</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;a&#125;</span><span class="subst">$&#123;b&#125;</span>`</span></span><br><span class="line">&#125;</span><br><span class="line">join&lt;<span class="built_in">string</span>&gt;(<span class="string">'a'</span>, <span class="number">2</span>) <span class="comment">// 函数使用的时候可以显式地生命泛型是什么类型，显然这里会报错；不显式则会被推断出来</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">join</span>&lt;<span class="title">T</span>, <span class="title">P</span>&gt;(<span class="params">a: T, b: P</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;a&#125;</span><span class="subst">$&#123;b&#125;</span>`</span></span><br><span class="line">&#125;</span><br><span class="line">join&lt;<span class="built_in">string</span>, <span class="built_in">number</span>&gt;(<span class="string">'a'</span>, <span class="number">2</span>) <span class="comment">// 泛型还可以定义多个，这里不会报错</span></span><br></pre></td></tr></table></figure>

<h3 id="泛型约束"><a href="#泛型约束" class="headerlink" title="泛型约束"></a>泛型约束</h3><p><strong>泛型约束是一个很常用的用法</strong>，用来<strong>把类型约束成一个更严谨的类型</strong> (<strong>使得泛型收窄</strong>)，eg. 用来<strong>确保属性存在</strong>。其实泛型约束的方法挺多：extends这种简单的也算</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">rec</span>&lt;<span class="title">T</span>&gt;(<span class="params">arg: T</span>): <span class="title">T</span> </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(arg.length) <span class="comment">// error。编译的时候认为arg是任何类型，所以不一定有length属性</span></span><br><span class="line">  <span class="keyword">return</span> arg</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>所以我们可以<strong>定义一个接口做一些约束</strong></p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> lengthwise &#123;</span><br><span class="line">  length: <span class="built_in">number</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">rec</span>&lt;<span class="title">T</span> <span class="title">extends</span> <span class="title">lengthwise</span>&gt;(<span class="params">arg: T</span>): <span class="title">T</span> </span>&#123; <span class="comment">// 意思是：T类型应该有length属性，并且该属性返回number</span></span><br><span class="line">  <span class="built_in">console</span>.log(arg.length)</span><br><span class="line">  <span class="keyword">return</span> arg</span><br><span class="line">&#125;</span><br><span class="line">rec(<span class="string">'jack'</span>)</span><br><span class="line">rec(<span class="number">6</span>) <span class="comment">// error</span></span><br></pre></td></tr></table></figure>

<h4 id="keyof"><a href="#keyof" class="headerlink" title="keyof"></a>keyof</h4><p>keyof 操作符是在 TypeScript 2.1 版本引入的，该操作符可以用于获取某种类型的所有键，其返回类型是联合类型。eg:</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> Person &#123;</span><br><span class="line">  name: <span class="built_in">string</span>;</span><br><span class="line">  age: <span class="built_in">number</span>;</span><br><span class="line">  location: <span class="built_in">string</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> K1 = keyof Person; <span class="comment">// "name" | "age" | "location"</span></span><br><span class="line"><span class="keyword">type</span> K2 = keyof Person[];  <span class="comment">// number | "length" | "push" | "concat" | ...</span></span><br><span class="line"><span class="keyword">type</span> K3 = keyof &#123; [x: <span class="built_in">string</span>]: Person &#125;;  <span class="comment">// string | number</span></span><br></pre></td></tr></table></figure>

<p>所以有了 keyof 之后我们可以更愉快地和 泛型约束 玩耍</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getProperty</span>&lt;<span class="title">T</span>, <span class="title">K</span> <span class="title">extends</span> <span class="title">keyof</span> <span class="title">T</span>&gt;(<span class="params">obj: T, key: K</span>): <span class="title">T</span>[<span class="title">K</span>] </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> obj[key];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>开发中还遇到不懂的泛型骚操作，详见 <a href="https://mp.weixin.qq.com/s/9mvQcBM13Rt75dJpftVTIg" target="_blank" rel="noopener">一文读懂 TypeScript 泛型及应用</a></p>
<h4 id="条件类型"><a href="#条件类型" class="headerlink" title="条件类型"></a>条件类型</h4><p>见<a href="https://jacleklm.github.io/2020/12/07/TypeScript%E4%B8%AD%E7%9A%84%E7%B1%BB%E5%9E%8B%E7%BC%96%E7%A8%8B/" target="_blank" rel="noopener">TypeScript中的类型编程</a></p>
<h4 id="有时可发挥类似-interface-的作用"><a href="#有时可发挥类似-interface-的作用" class="headerlink" title="有时可发挥类似 interface 的作用"></a>有时可发挥类似 interface 的作用</h4><figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> FeatureContext = React.createContext&lt;&#123;</span><br><span class="line">  featureList: IFeature[];</span><br><span class="line">  supplementaryData: ISupplementaryData;</span><br><span class="line">  fetchLoading: <span class="built_in">boolean</span>;</span><br><span class="line">  fetchFeatureData(): <span class="built_in">void</span>;</span><br><span class="line">  searchFeature(queryKey: <span class="built_in">string</span>): <span class="built_in">void</span>;</span><br><span class="line">  verifyId: <span class="built_in">number</span>;</span><br><span class="line">&#125;&gt;(<span class="literal">null</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> [fetchLoading, setFetchLoadingStatus] = useState&lt;<span class="built_in">boolean</span>&gt;(<span class="literal">true</span>);</span><br></pre></td></tr></table></figure>

<h2 id="高级类型-其实用的还挺多的"><a href="#高级类型-其实用的还挺多的" class="headerlink" title="高级类型 (其实用的还挺多的)"></a>高级类型 (其实用的还挺多的)</h2><h3 id="交叉类型"><a href="#交叉类型" class="headerlink" title="交叉类型"></a>交叉类型</h3><p>交叉类型是将多个类型合并为一个类型。用 &amp; 产生</p>
<h3 id="联合类型"><a href="#联合类型" class="headerlink" title="联合类型"></a>联合类型</h3><p>联合类型表示一个值可以是几种类型之一。用 | 产生</p>
<h3 id="类型保护"><a href="#类型保护" class="headerlink" title="类型保护"></a>类型保护</h3><h4 id="通俗版"><a href="#通俗版" class="headerlink" title="通俗版"></a>通俗版</h4><p><strong>个人理解</strong>：类型保护一般出现在有联合类型的地方，类型保护就是用来判断变量类型的，并且在对应的 if 分支内能确定变量类型，进而能提前调用那种类型的函数 (比如提前判定是 string，然后在那个分支就可以用 .substr() )</p>
<h4 id="类型保护的方式"><a href="#类型保护的方式" class="headerlink" title="类型保护的方式"></a>类型保护的方式</h4><ul>
<li><p>类型断言</p>
</li>
<li><p>in语法</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> Bird &#123;</span><br><span class="line">  fly: <span class="built_in">boolean</span></span><br><span class="line">  sing: <span class="function"><span class="params">()</span> =&gt;</span> &#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">interface</span> Dog &#123;</span><br><span class="line">  fly: <span class="built_in">boolean</span></span><br><span class="line">  bark: <span class="function"><span class="params">()</span> =&gt;</span> &#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 类型断言的类型保护</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">trains1</span>(<span class="params">animal: Bird | Dog</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span>(animal.fly) &#123;</span><br><span class="line">    (animal <span class="keyword">as</span> Bird).sing()</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    (animal <span class="keyword">as</span> Dog).bark()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// in 语法的类型保护</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">trains2</span>(<span class="params">animal: Bird | Dog</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span>(<span class="string">'sing'</span> <span class="keyword">in</span> animal) &#123;</span><br><span class="line">    animal.sing()</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    animal.bark()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>typeof</p>
</li>
<li><p>instanceof</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// typeof 和 instanceof 语法的类型保护</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">first: <span class="built_in">string</span> | <span class="built_in">number</span>, second: <span class="built_in">string</span> | <span class="built_in">number</span></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span>(<span class="keyword">typeof</span> first === <span class="string">'string'</span> || <span class="keyword">typeof</span> second === <span class="string">'string'</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;first&#125;</span><span class="subst">$&#123;second&#125;</span>`</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> first + second</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// instanceof 语法的类型保护 ，只用于能用 instanceof 的，eg. class (interface不能)</span></span><br><span class="line"><span class="keyword">class</span> numberObj &#123;</span><br><span class="line">  count: <span class="built_in">number</span></span><br><span class="line">  <span class="keyword">constructor</span>(<span class="params">count: <span class="built_in">number</span></span>) &#123;</span><br><span class="line">    <span class="keyword">this</span>.count = count</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add2</span>(<span class="params">first: object | numberObj, second: object | numberObj</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 这里不能写成 first instanceof numberObj === true</span></span><br><span class="line">  <span class="keyword">if</span>(first <span class="keyword">instanceof</span> numberObj &amp;&amp; second <span class="keyword">instanceof</span> numberObj) &#123;</span><br><span class="line">    <span class="keyword">return</span> first.count + second.count</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="官方版"><a href="#官方版" class="headerlink" title="官方版"></a>官方版</h4><p>类型保护就是一些表达式，它们会在运行时检查以确保在某个作用域里的类型</p>
<p>定义一个<strong>类型保护</strong>，我们只要简单地定义一个<strong>函数</strong>，它的<strong>返回值是一个类型谓词</strong>：这里 <code>pet is Fish</code> 就是类型谓词</p>
<p>就是当下面这个函数返回true的时候，就会有 <code>pet is Fish</code>，在下面的 if 中相当于一种类型断言的作用了。<strong>所以这种函数类型其实也是一种类型保护</strong>，或者说是一种底层是类型断言的类型保护</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> Fish &#123;</span><br><span class="line">  <span class="keyword">constructor</span>(<span class="params"><span class="keyword">public</span> name: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">  &#125;</span><br><span class="line">  swim() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'I can swim, i am fish'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">class</span> Bird &#123;</span><br><span class="line">  <span class="keyword">constructor</span>(<span class="params"><span class="keyword">public</span> name: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">  &#125;</span><br><span class="line">  fly() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'I can fly, i am bird'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> petUnknow = <span class="keyword">new</span> Fish(<span class="string">'pet'</span>)</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">isFish</span>(<span class="params">pet: Fish | Bird</span>): <span class="title">pet</span> <span class="title">is</span> <span class="title">Fish</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> (pet <span class="keyword">as</span> Fish).swim !== <span class="literal">undefined</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">trainAnimal</span>(<span class="params">pet: Fish | Bird</span>): <span class="title">void</span></span>&#123;</span><br><span class="line">  <span class="keyword">if</span>(isFish(pet)) &#123;</span><br><span class="line">    pet.swim() <span class="comment">// 这里能这样调用而不会报错，因为这个分支认定 pet is Fish</span></span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    pet.fly()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">trainAnimal(petUnknow) <span class="comment">// I can swim, i am fish</span></span><br></pre></td></tr></table></figure>

<h3 id="字符串字面量类型"><a href="#字符串字面量类型" class="headerlink" title="字符串字面量类型"></a>字符串字面量类型</h3><p>字符串字面量类型允许你指定字符串必须具有的确切值.</p>
<blockquote>
<p>字符串字面量 === 字符串组成的联合类型</p>
</blockquote>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 你只能从三种允许的字符中选择其一来做为参数传递，传入其它值则会产生错误</span></span><br><span class="line"><span class="keyword">type</span> Easing = <span class="string">'ease-in'</span> | <span class="string">'ease-out'</span> | <span class="string">'ease-in-out'</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getEase</span>(<span class="params">ease: Easing</span>) </span>&#123;&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Typescript-中的-Record-Partial-Readonly-Pick"><a href="#Typescript-中的-Record-Partial-Readonly-Pick" class="headerlink" title="Typescript 中的 Record, Partial, Readonly , Pick"></a>Typescript 中的 Record, Partial, Readonly , Pick</h2><p><a href="https://jacleklm.github.io/2020/12/09/TypeScript%E4%B8%AD%E7%9A%84%E5%86%85%E7%BD%AE%E5%B7%A5%E5%85%B7%E7%B1%BB%E5%9E%8B%E5%8F%8A%E5%85%B6%E5%AE%9E%E7%8E%B0/" target="_blank" rel="noopener">TypeScript中的内置工具类型及其实现</a></p>
<h2 id="命名空间"><a href="#命名空间" class="headerlink" title="命名空间"></a>命名空间</h2><p><a href="https://jkchao.github.io/typescript-book-chinese/project/namespaces.html" target="_blank" rel="noopener">文档</a></p>
<h1 id="工程化"><a href="#工程化" class="headerlink" title="工程化"></a>工程化</h1><h2 id="单文件"><a href="#单文件" class="headerlink" title="单文件"></a>单文件</h2><p>使用方法。编译代码：在命令行用 tsc &lt;文件名&gt; 的方式编译 .ts 文件，然后用 node 执行生成的js文件</p>
<h2 id="tsconfig-json-配置文件及其常用语法"><a href="#tsconfig-json-配置文件及其常用语法" class="headerlink" title="tsconfig.json 配置文件及其常用语法"></a>tsconfig.json 配置文件及其常用语法</h2><p>在项目当中，根目录运行 <code>tsc --init</code> 能生成该项目的 TS 配置文件: tsconfig.json</p>
<h3 id="运行方法"><a href="#运行方法" class="headerlink" title="运行方法"></a>运行方法</h3><p>在根目录命令行单纯地执行 tsc ，会根据配置文件编译整个目录下所有的 .ts 文件（用ts-node ${fileName} 其实也会用这个配置文件) ；想只编译某些文件，可以 files / include 或 exculde (数组写法）eg. </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  &quot;include&quot;: [&quot;./single.ts&quot;],</span><br><span class="line">  &quot;compilerOptions&quot;: &#123;</span><br><span class="line">  …</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="compilerOptions"><a href="#compilerOptions" class="headerlink" title="compilerOptions"></a>compilerOptions</h3><p><a href="https://www.tslang.cn/docs/handbook/compiler-options.html" target="_blank" rel="noopener">官方文档</a></p>
<p>常用配置项：</p>
<ul>
<li>常规<ul>
<li>removeComments: true  编译时去除注释</li>
<li>Incremental: true  上次编译过并且没变化的文件这次不会再编译</li>
</ul>
</li>
<li>JS<ul>
<li>allowJs: true    允许编译javascript文件（转成ES5）</li>
<li>checkJs: true   检查JS语法</li>
</ul>
</li>
<li>检查<ul>
<li>noImplicitAny: true  当一个变量是 any 的时候，必须显示地写出来，不然会报错</li>
<li>strictNullChecks: true  在严格的 null检查模式下， null和 undefined值不包含在任何类型里，只允许用它们自己和 any来赋值</li>
</ul>
</li>
<li>输入输出<ul>
<li>outDir: “./build”</li>
<li>rootDir: “./src”  此时src文件外不能有 .ts 文件</li>
</ul>
</li>
<li>额外检查<ul>
<li>noUnusedLocals    若有未使用的局部变量则抛错</li>
<li>noUnusedParameters  同理，函数参数</li>
</ul>
</li>
</ul>
<h2 id="杂"><a href="#杂" class="headerlink" title="杂"></a>杂</h2><ul>
<li>项目的script命令中写成 tsc -w ，这个 -w 能监控项目中 TS 文件的变化，有变化就自动重新编译</li>
</ul>
<p>参考资料<br><a href="https://ts.xcatliu.com/introduction/what-is-typescript" target="_blank" rel="noopener">TypeScript 入门教程</a><br><a href="https://www.tslang.cn/docs/handbook/basic-types.html" target="_blank" rel="noopener">TypeScript 官方文档</a><br><a href="https://coding.imooc.com/class/330.html" target="_blank" rel="noopener">慕课网：基于 TypeScript 从零重构 axios</a><br><a href="https://coding.imooc.com/class/412.html" target="_blank" rel="noopener">慕课网：TypeScript－系统入门到项目实战</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>TypeScript</tag>
      </tags>
  </entry>
  <entry>
    <title>webpack之Tree Shaking</title>
    <url>/2019/09/26/webpack%E4%B9%8BTree%20Shaking/</url>
    <content><![CDATA[<h3 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h3><p>我们打包的源文件中引用了某个模块的一部分的时候，打包的时候却会把整个模块都打包进去，会造成打包后文件体积过大。此时就可以用tree shaking仅把我们需要的那部分代码打包进去，而把模块中我们用不到的部分“shaking”掉。正式一点的说法就是：移除上下文中的未引用代码。</p>
<h3 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h3><p>由于webpack中的tree shaking是依赖于ES5中的静态结构特性，所以仅适用于模块是用ES Module引入方式的模块（即用import）。</p>
<h3 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h3><h4 id="开发模式下："><a href="#开发模式下：" class="headerlink" title="开发模式下："></a>开发模式下：</h4><p>在webpack配置文件中新增一个配置项：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">optimization: &#123;</span><br><span class="line">    usedExports: <span class="literal">true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在package.json中新增一个配置项：‘sideEffests’。可以把不需要tree shaking的文件用数组的写法写在这个配置项中，eg. css文件；如果没有则写为false。</p>
<p>PS：在开发环境中，tree shaking并打包之后并不会把模块多余的代码从打包完成的文件中删除，而只是会注释哪些是我们用到的代码，哪些是不用的代码。</p>
<h4 id="生产模式下："><a href="#生产模式下：" class="headerlink" title="生产模式下："></a>生产模式下：</h4><p>只需要在package.json中新增一个配置项：‘sideEffests’。打包过程会默认进行tree shaking。并且会把多余的代码从打包完成的文件中删除</p>
]]></content>
      <categories>
        <category>Engineering</category>
      </categories>
      <tags>
        <tag>webpack</tag>
      </tags>
  </entry>
  <entry>
    <title>内存泄露和垃圾回收</title>
    <url>/2019/11/26/%E5%86%85%E5%AD%98%E6%B3%84%E9%9C%B2%E5%92%8C%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6/</url>
    <content><![CDATA[<h2 id="什么是内存泄露"><a href="#什么是内存泄露" class="headerlink" title="什么是内存泄露"></a>什么是内存泄露</h2><p>程序的运行需要内存。只要程序提出要求，操作系统就会给内存。进程应及时释放不再用到的内存。<strong>不再用到的内存，没有及时释放</strong>，就叫做<strong>内存泄漏</strong>（memory leak）<br>大多数语言提供自动内存管理，减轻程序员的负担，这被称为<strong>垃圾回收机制</strong>（garbage collector）</p>
<h2 id="垃圾回收"><a href="#垃圾回收" class="headerlink" title="垃圾回收"></a>垃圾回收</h2><h3 id="引用计数"><a href="#引用计数" class="headerlink" title="引用计数"></a>引用计数</h3><p><strong>思想：跟踪记录所有值被引用的次数</strong><br>（阮一峰说引用计数是最常使用的，红宝书说到 2008 年为止标记清除是最常用的）<br>JS 引擎有一张”引用表”，保存了内存里面所有的资源（通常是各种<strong>值</strong>，比如数组）的引用次数。<strong>如果一个值的引用次数是 0，就表示这个值不再用到了，因此可以将这块内存释放</strong><br>如果一个值不再需要了，引用数却不为 0，垃圾回收机制无法释放这块内存，从而导致内存泄漏</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>];</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">"hello world"</span>);</span><br><span class="line"></span><br><span class="line">arr = <span class="literal">null</span>;</span><br></pre></td></tr></table></figure>

<p>上面代码中，数组[1, 2, 3, 4]是一个值，会占用内存。变量 arr 是仅有的对这个值的引用，因此引用次数为 1。尽管后面的代码没有用到 arr，它还是会持续占用内存<br>可以<strong>手动解除引用</strong>：让变量 = null。则数值这个值就能被垃圾回收机制释放了</p>
<h4 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h4><p><strong>循环引用的情况</strong>，eg. 对象 A 包含一个指向对象 B 的指针，而对象 B 中也包含一个指向 A 的指针。采用引用计数策略时，函数执行后，两个对象还将继续存在，因为<strong>它们的引用次数永远不会为 0</strong>。</p>
<h3 id="标记清除"><a href="#标记清除" class="headerlink" title="标记清除"></a>标记清除</h3><p>思路：给当前不使用的值加上标记，然后再回收其内存<br>只有在环境中的变量及被环境中的变量引用的变量（闭包）标记</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">test</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> a = <span class="number">10</span>; <span class="comment">//被标记"进入环境"</span></span><br><span class="line">  <span class="keyword">var</span> b = <span class="string">"hello"</span>; <span class="comment">//被标记"进入环境"</span></span><br><span class="line">&#125;</span><br><span class="line">test(); <span class="comment">//执行完毕后之后，a和b又被标记"离开环境"，被回收</span></span><br></pre></td></tr></table></figure>

<h2 id="常见-JavaScript-内存泄露"><a href="#常见-JavaScript-内存泄露" class="headerlink" title="常见 JavaScript 内存泄露"></a>常见 JavaScript 内存泄露</h2><h3 id="意外的全局变量"><a href="#意外的全局变量" class="headerlink" title="意外的全局变量"></a>意外的全局变量</h3><p>eg. 如下代码，这种生命方式等于 <code>this.bar=.....</code>，函数独立调用的话 this 为 window，所以 bar 是全局变量</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params">arg</span>) </span>&#123;</span><br><span class="line">  bar = <span class="string">"this is a hidden global variable"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>JavaScript 文件头部加上 ‘use strict’，可以避免此类错误发生</p>
</blockquote>
<p>但是其实我们自己设全局变量也是有风险的，因为全局变量被认为的不可回收的（标记清除机制无法清除，因为全局环境到最后才会销毁）。如果必须使用全局变量存储大量数据时，确保用完以后把它设置为 null</p>
<h3 id="被遗忘的-setInterval-或回调函数"><a href="#被遗忘的-setInterval-或回调函数" class="headerlink" title="被遗忘的 setInterval 或回调函数"></a>被遗忘的 setInterval 或回调函数</h3><p>用完 setInterval 记得 clearInterval</p>
<h3 id="闭包"><a href="#闭包" class="headerlink" title="闭包"></a>闭包</h3><p>不解释</p>
<h2 id="内存泄漏的识别方法"><a href="#内存泄漏的识别方法" class="headerlink" title="内存泄漏的识别方法"></a>内存泄漏的识别方法</h2><h3 id="Chrome-浏览器"><a href="#Chrome-浏览器" class="headerlink" title="Chrome 浏览器"></a>Chrome 浏览器</h3><p>控制台-profiles</p>
<h3 id="Node"><a href="#Node" class="headerlink" title="Node"></a>Node</h3><p>process.memoryUsage()，返回一个对象，包含了 Node 进程的内存占用信息</p>
<h2 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h2><ol>
<li>找到问题后手动释放内存</li>
<li>使用两种新的数据结构：WeakSet 和 WeakMap。它们对于值的引用都是不计入垃圾回收机制</li>
</ol>
<p>参考资料</p>
<p>《Javascript 高级程序设计》</p>
<p><a href="http://www.ruanyifeng.com/blog/2017/04/memory-leak.html" target="_blank" rel="noopener">阮一峰-JavaScript 内存泄漏教程</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>二叉树的遍历</title>
    <url>/2019/09/26/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E9%81%8D%E5%8E%86/</url>
    <content><![CDATA[<p>对于树的遍历来说，有三种遍历方法，分别是先序遍历、中序遍历、后序遍历。三种遍历的区别在于何时访问节点。在遍历树的过程中，每个节点都会遍历三次，分别是遍历到自己，遍历左子树和遍历右子树。如果需要实现先序遍历，那么只需要第一次遍历到节点时进行操作即可</p>
<h3 id="先序遍历"><a href="#先序遍历" class="headerlink" title="先序遍历"></a>先序遍历</h3><p>先序遍历可用于打印树的结构。先序遍历先访问根节点，然后访问左节点，最后访问右节点。如下图所示：</p>
<p><img src="/img/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E9%81%8D%E5%8E%86/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E9%81%8D%E5%8E%86_1.png"><br>用JS实现如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">preTraversal() &#123;</span><br><span class="line">    <span class="keyword">this</span>._pre(<span class="keyword">this</span>.root)</span><br><span class="line">&#125;</span><br><span class="line">_pre(node) &#123;</span><br><span class="line">    <span class="keyword">if</span> (node) &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(node.value)</span><br><span class="line">        <span class="keyword">this</span>._pre(node.left)</span><br><span class="line">        <span class="keyword">this</span>._pre(node.right)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="中序遍历"><a href="#中序遍历" class="headerlink" title="中序遍历"></a>中序遍历</h3><p>中序遍历可用于排序。对二叉搜索树来说，中序遍历可以实现一次遍历就得到有序的值。中序遍历先访问左节点，然后访问根节点，最后访问右节点。如下图所示：</p>
<p><img src="/img/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E9%81%8D%E5%8E%86/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E9%81%8D%E5%8E%86_2.png"><br>用JS实现如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">midTraversal() &#123;</span><br><span class="line">    <span class="keyword">this</span>._mid(<span class="keyword">this</span>.root)</span><br><span class="line">&#125;</span><br><span class="line">_mid(node) &#123;</span><br><span class="line">    <span class="keyword">if</span> (node) &#123;</span><br><span class="line">        <span class="keyword">this</span>._mid(node.left)</span><br><span class="line">        <span class="built_in">console</span>.log(node.value)</span><br><span class="line">        <span class="keyword">this</span>._mid(node.right)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="后序遍历"><a href="#后序遍历" class="headerlink" title="后序遍历"></a>后序遍历</h3><p>后序遍历可用于先操作子节点，再操作父节点的场景。后续遍历先访问左节点，然后访问右节点，最后访问根节点。如下图所示：</p>
<p><img src="/img/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E9%81%8D%E5%8E%86/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E9%81%8D%E5%8E%86_3.png"><br>用JS实现如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">backTraversal() &#123;</span><br><span class="line">    <span class="keyword">this</span>._back(<span class="keyword">this</span>.root)</span><br><span class="line">&#125;</span><br><span class="line">_back(node) &#123;</span><br><span class="line">    <span class="keyword">if</span> (node) &#123;</span><br><span class="line">        <span class="keyword">this</span>._back(node.left)</span><br><span class="line">        <span class="keyword">this</span>._back(node.right)</span><br><span class="line">        <span class="built_in">console</span>.log(node.value)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Basic knowledge</category>
      </categories>
      <tags>
        <tag>Data structure</tag>
      </tags>
  </entry>
  <entry>
    <title>webpack之Lazy Loading</title>
    <url>/2019/09/27/webpack%E4%B9%8BLazy%20Loading/</url>
    <content><![CDATA[<h3 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h3><p>懒加载就是<strong>将不关键的资源延后加载</strong>，是一种很好的优化网页或应用的方式。<br>这种方式实际上是先把你的代码在一些逻辑断点处分离开，然后在一些代码块中完成某些操作后，立即引用或即将引用另外一些新的代码块。这样加快了应用的初始加载速度，减轻了它的总体体积，因为某些代码块可能永远不会被加载。</p>
<h3 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h3><p>对于图片来说，先设置图片标签的<code>src</code>属性为一张占位图，将真实的图片资源放入一个自定义属性中，当进入自定义区域时，就将自定义属性替换为<code>src</code>属性，这样图片就会去下载资源，实现了图片懒加载。<br>懒加载不仅可以用于图片，也可以使用在别的资源上。例如下面这段代码实现click之后再执行异步函数在页面生成文字。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//  利用异步加载的代码，可以实现懒加载</span></span><br><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">getComponent</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> &#123; <span class="attr">default</span>: _ &#125; = <span class="keyword">await</span> <span class="keyword">import</span>(<span class="comment">/* webpackChunkName:"lodash"*/</span><span class="string">'lodash'</span>)</span><br><span class="line">  <span class="keyword">const</span> element = <span class="built_in">document</span>.createElement(<span class="string">'div'</span>)</span><br><span class="line">  element.innerHTML = _.join([<span class="string">'a'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>], <span class="string">'***'</span>)</span><br><span class="line">  <span class="keyword">return</span> element</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">document</span>.addEventListener(<span class="string">'click'</span>, () =&gt; &#123;</span><br><span class="line">    getComponent().then(<span class="function"><span class="params">element</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">document</span>.body.appendChild(element)</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;)</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Engineering</category>
      </categories>
      <tags>
        <tag>webpack</tag>
      </tags>
  </entry>
  <entry>
    <title>博客项目上线部署总结</title>
    <url>/2020/02/27/%E5%8D%9A%E5%AE%A2%E9%A1%B9%E7%9B%AE%E4%B8%8A%E7%BA%BF%E9%83%A8%E7%BD%B2/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>经过一番折腾之后项目终于上线了（喜极而泣，坑好多），博客项目部署在 <a href="http://jacleklm.com/" target="_blank" rel="noopener">JacleKlm.com</a>，先写一篇文章记录下近期的折腾所得吧。</p>
<h1 id="云服务器"><a href="#云服务器" class="headerlink" title="云服务器"></a>云服务器</h1><p>买的<a href="https://cloud.tencent.com/act/season" target="_blank" rel="noopener">腾讯云服务器</a>最便宜的那种，买域名，备案审批</p>
<p>云服务器中，更多-安全组-配置安全组-点连接，可以设置端口的开启及可以访问的人</p>
<h1 id="Linux"><a href="#Linux" class="headerlink" title="Linux"></a>Linux</h1><p>Linux 是一个性能稳定的多用户网络操作系统，经常用来跑服务器，eg. Linux + Nginx + MySQL + Node.js</p>
<h2 id="Linux-常见命令"><a href="#Linux-常见命令" class="headerlink" title="Linux 常见命令"></a>Linux 常见命令</h2><ul>
<li>Vim（用来打开文件）<ul>
<li>Vim 下，<code>i</code> 是进入编辑模式；<code>esc</code> 再<code>:wq</code> 是保存退出，<code>:q</code> 是退出，<code>:q!</code> 是强制退出；</li>
<li>Vim 查找。在 normal 模式下按下 <code>/</code> 即可进入查找模式，输入要查找的字符串并按下回车。在查找模式中加入 <code>\c</code> 表示大小写不敏感查找，<code>\C</code> 表示大小写敏感查找，例如： <code>/foo\c</code></li>
<li>Vim 翻页：<code>control+f</code>; <code>control+b</code></li>
</ul>
</li>
<li>查看开启的端口号<ul>
<li><code>netstat -tlnp</code></li>
</ul>
</li>
<li>查看当前路径<ul>
<li><code>pwd [-lp]</code></li>
</ul>
</li>
<li>查看端口号 <code>show global variables like &#39;port&#39;;</code></li>
<li>删除<ul>
<li><code>rm -rf svn/</code> 将会删除当前目录下的 svn/ 文件夹及其下所有文件夹，包括文件</li>
<li><code>rm -f /opt/test.txt</code> 将会强制删除 /opt/test.txt 这个文件</li>
</ul>
</li>
<li>解压文件 <code>unzip react-blog.zip</code></li>
<li>更换 node 版本</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">cd /usr/local/bin/</span><br><span class="line"><span class="comment">// 查看已有软连接：</span></span><br><span class="line">ll -all</span><br><span class="line"><span class="comment">// 删除旧node软连接</span></span><br><span class="line">rm -rf node</span><br><span class="line">rm -rf npm</span><br><span class="line">rm -rf cnpm</span><br><span class="line">cd ~</span><br><span class="line"><span class="comment">// 下载跟本地node版本相同的node</span></span><br><span class="line">wget https:<span class="comment">//nodejs.org/dist/v12.3.1/node-v12.3.1-linux-x64.tar.xz</span></span><br><span class="line"><span class="comment">// 解压安装包</span></span><br><span class="line">tar xvf node-v12<span class="number">.3</span><span class="number">.1</span>-linux-x64.tar.xz</span><br><span class="line"><span class="comment">// 重建软连接</span></span><br><span class="line">ln -s /root/node-v12<span class="number">.3</span><span class="number">.1</span>-linux-x64/bin/node /usr/local/bin/node</span><br><span class="line">ln -s /root/node-v12<span class="number">.3</span><span class="number">.1</span>-linux-x64/bin/npm /usr/local/bin/npm</span><br><span class="line">sudo ln -s /root/node-v12<span class="number">.3</span><span class="number">.1</span>-linux-x64/bin/cnpm /usr/local/bin/cnpm</span><br></pre></td></tr></table></figure>

<p>更多常见命令详见 <a href="https://mp.weixin.qq.com/s/80UEbeEfPBV2LMmRqJFT7w" target="_blank" rel="noopener">让你牛 B 加身的前端必会 Linux 命令</a><br>更多 Linux 教程也可见 <a href="https://www.runoob.com/linux/linux-tutorial.html" target="_blank" rel="noopener">Linux 教程-菜鸟教程</a></p>
<h2 id="安装-cnpm"><a href="#安装-cnpm" class="headerlink" title="安装 cnpm"></a>安装 cnpm</h2><ul>
<li>Linux 安装 cnpm 后 <code>npm install -g cnpm --registry=https://registry.npm.taobao.org</code></li>
<li>sudo 创建软连接 <code>sudo ln -s /root/node-v&lt;版本号&gt;-linux-x64/bin/cnpm /usr/local/bin/cnpm</code></li>
</ul>
<h2 id="从-Mac-本地拷贝文件到云服务器"><a href="#从-Mac-本地拷贝文件到云服务器" class="headerlink" title="从 Mac 本地拷贝文件到云服务器"></a>从 Mac 本地拷贝文件到云服务器</h2><h3 id="scp"><a href="#scp" class="headerlink" title="scp"></a>scp</h3><p>eg. 在本地命令行执行 <code>scp -r /Users/chenjunjia/React/react_blog/admin.zip root@xx.xx.xx.xx:/root/blog</code> ，再输入服务器密码即可</p>
<h3 id="sftp"><a href="#sftp" class="headerlink" title="sftp"></a>sftp</h3><p>参见<a href="https://blog.csdn.net/a419419/article/details/85050585" target="_blank" rel="noopener">教程</a><br>eg. <code>Put React/react_blog/react-blog.zip /root/blog</code> 。注意这里的 <code>/User/chenjunjia/</code> 就被省略了（至少我的 Mac 中是这样）</p>
<h1 id="Nginx"><a href="#Nginx" class="headerlink" title="Nginx"></a>Nginx</h1><p>Nginx 是一款轻量级的 HTTP 服务器，采用事件驱动的异步非阻塞处理方式框架，这让其具有极好的 IO 性能，时常用于服务端的反向代理和负载均衡</p>
<p>目前本项目的部署其实没用到 Nginx</p>
<h2 id="Nginx-常见命令"><a href="#Nginx-常见命令" class="headerlink" title="Nginx 常见命令"></a>Nginx 常见命令</h2><ul>
<li>启动<ul>
<li>直接法：<code>nignx</code> 。会没有任何返回信息，这是成功状态。此时用 <code>ps aux | grep nginx</code> 看 nginx 的启动信息</li>
<li>用通用的 Linux 命令：<code>systemctl start nginx.service</code></li>
</ul>
</li>
<li>关闭<ul>
<li>最常用：<code>nginx -s quit</code></li>
<li>不常用的关闭（关闭效果等级从轻到重）：<code>nginx -s stop</code>，<code>killall nginx</code>, <code>systemctl stop nginx.service</code></li>
</ul>
</li>
<li>重启（修改 Nginx 文件后都要重启）<ul>
<li><code>nginx -s reload</code></li>
<li><code>systemctl restart nginx.service</code></li>
</ul>
</li>
</ul>
<h2 id="Nginx-基础配置文件"><a href="#Nginx-基础配置文件" class="headerlink" title="Nginx 基础配置文件"></a>Nginx 基础配置文件</h2><p>配置文件 <code>/etc/nginx/nginx.config</code> ，用 vim 打开后看内容</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"># 运行用户，默认即是nginx，可以不进行设置</span><br><span class="line">user  nginx;</span><br><span class="line"># Nginx进程，一般设置为和CPU核数一样</span><br><span class="line">worker_processes  <span class="number">1</span>;</span><br><span class="line"># 错误日志存放目录</span><br><span class="line">error_log  /<span class="keyword">var</span>/log/nginx/error.log warn;</span><br><span class="line"># 进程pid存放位置</span><br><span class="line">pid        /<span class="keyword">var</span>/run/nginx.pid;</span><br><span class="line"></span><br><span class="line">events &#123;</span><br><span class="line">    worker_connections  1024; # 单个后台进程的最大并发数</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">http &#123;</span><br><span class="line">    include       /etc/nginx/mime.types;   # 文件扩展名与类型映射表</span><br><span class="line">    default_type  application/octet-stream;  # 默认文件类型</span><br><span class="line">    #设置日志模式</span><br><span class="line">    log_format  main  <span class="string">'$remote_addr - $remote_user [$time_local] "$request" '</span></span><br><span class="line">                      <span class="string">'$status $body_bytes_sent "$http_referer" '</span></span><br><span class="line">                      <span class="string">'"$http_user_agent" "$http_x_forwarded_for"'</span>;</span><br><span class="line"></span><br><span class="line">    access_log  /var/log/nginx/access.log  main;   # nginx访问日志存放位置</span><br><span class="line"></span><br><span class="line">    sendfile        on;   # 开启高效传输模式</span><br><span class="line">    #tcp_nopush     on;    # 减少网络报文段的数量</span><br><span class="line"></span><br><span class="line">    keepalive_timeout  65;  # 保持连接的时间，也叫超时时间</span><br><span class="line"></span><br><span class="line">    #gzip  on;  # 开启gzip压缩</span><br><span class="line"></span><br><span class="line">    include /etc/nginx/conf.d<span class="comment">/*.conf; #包含的子配置项位置和文件</span></span><br></pre></td></tr></table></figure>

<p>用 vim 打开 include 的子配置项文件 <code>/etc/nginx/conf.d/default.config</code></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">server &#123;</span><br><span class="line">    listen       80;   # 配置监听端口</span><br><span class="line">    server_name  localhost;  <span class="comment">//配置域名</span></span><br><span class="line"></span><br><span class="line">    #charset koi8-r;</span><br><span class="line">    #access_log  /var/log/nginx/host.access.log  main;</span><br><span class="line"></span><br><span class="line">    location / &#123;</span><br><span class="line">        root   /usr/share/nginx/html;     # 服务默认启动目录</span><br><span class="line">        index  index.html index.htm;    # 默认访问文件</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    #error_page  404              /404.html;   # 配置404页面</span><br><span class="line"></span><br><span class="line">    # redirect server error pages to the static page /50x.html</span><br><span class="line">    #</span><br><span class="line">    error_page   500 502 503 504  /50x.html;   #错误状态码的显示页面。现在是这几种错误都是用现实这个html文件</span><br><span class="line">    location = <span class="regexp">/50x.html &#123;</span></span><br><span class="line"><span class="regexp">        root   /u</span>sr/share/nginx/html;</span><br><span class="line">    &#125;</span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>cd 到 <code>/usr/share/nginx/html</code> 默认文件夹，对 index.html 默认文件编辑并保存，然后用 nginx 命令即能启动服务器。访问 ip 地址就能看到内容了</p>
<p>同理编辑 <code>/usr/share/nginx/html/404.html</code> 是 404 时显示的页面；也可以让 404 指向其他页面：<code>error_page 404 http://github.com;</code></p>
<h2 id="Nginx-实现访问控制"><a href="#Nginx-实现访问控制" class="headerlink" title="Nginx 实现访问控制"></a>Nginx 实现访问控制</h2><blockquote>
<p>nginx 的指令是谁写在前面就谁先生效（如果有冲突）</p>
</blockquote>
<p>只允许某个主机进行访问</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">location / &#123; <span class="comment">// 这里的 / 是全部的意思</span></span><br><span class="line">        allow  xx.xx.xx.xx;  <span class="comment">//  // 允许访问的ip</span></span><br><span class="line">        deny   all; <span class="comment">// 也可以是某个ip</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>这里的 <code>/</code> 是全部的意思；改成 <code>=/img</code> 则是网站下的 img 目录；也可以用正则，<code>~\.php$</code></p>
<h2 id="Ngingx-设置虚拟机"><a href="#Ngingx-设置虚拟机" class="headerlink" title="Ngingx 设置虚拟机"></a>Ngingx 设置虚拟机</h2><p>通过设置虚拟主机，一台云服务器也能同时跑好几个 Web 服务器。配置虚拟主机可以基于端口号，基于 IP，基于域名。最常用的是基于域名设置虚拟机<br>都是改 <code>/etc/nginx/conf.d/default.config</code> 配置文件</p>
<h3 id="基于端口号"><a href="#基于端口号" class="headerlink" title="基于端口号"></a>基于端口号</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">server&#123;</span><br><span class="line">        listen <span class="number">8001</span>;</span><br><span class="line">        server_name localhost; <span class="comment">// localhost:8001端口新开虚拟机</span></span><br><span class="line">        root /usr/share/nginx/html/html8001;</span><br><span class="line">        index index.html;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>编辑 <code>/usr/share/nginx/html/html8001</code> 下的 index.html ，即可访问 8001 端口了</p>
<h3 id="基于端-IP"><a href="#基于端-IP" class="headerlink" title="基于端 IP"></a>基于端 IP</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">server&#123;</span><br><span class="line">        listen <span class="number">80</span>;</span><br><span class="line">        server_name xx.xx.xx.xx; <span class="comment">// 某新ip</span></span><br><span class="line">        root /usr/share/nginx/html/html8001;</span><br><span class="line">        index index.html;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="基于域名（最常用）"><a href="#基于域名（最常用）" class="headerlink" title="基于域名（最常用）"></a>基于域名（最常用）</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">server&#123;</span><br><span class="line">        listen <span class="number">80</span>;</span><br><span class="line">        server_name xxx.com; <span class="comment">// 如果有多个域名就可以配多个</span></span><br><span class="line">        location / &#123;</span><br><span class="line">                root /usr/share/nginx/html/html8001;</span><br><span class="line">                index index.html;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Nginx-实现反向代理"><a href="#Nginx-实现反向代理" class="headerlink" title="Nginx 实现反向代理"></a>Nginx 实现反向代理</h2><h3 id="反向代理"><a href="#反向代理" class="headerlink" title="反向代理"></a>反向代理</h3><p>正向代理（为客户端做代理）：普通的 proxy 代理，把我们不让访问的服务器的网页请求，代理到一个可以访问该网站的代理服务器上来，一般叫做 proxy 服务器，再转发给客户。eg. 翻墙（你没有权限，但是 proxy 服务器有权限）<br><img src="/img/%E5%8D%9A%E5%AE%A2%E4%B8%8A%E7%BA%BF%E9%83%A8%E7%BD%B2/%E6%AD%A3%E5%90%91%E4%BB%A3%E7%90%86.png"><br>反向代理（为服务器做代理）：反向代理跟代理正好相反（需要说明的是，现在基本所有的大型网站的页面都是用了反向代理），客户端发送的请求，想要访问 server 服务器上的内容。发送的内容被发送到代理服务器上，这个代理服务器再把请求发送到自己设置好的内部服务器上，而用户真实想获得的内容就在这些设置好的服务器上。<br><img src="/img/%E5%8D%9A%E5%AE%A2%E4%B8%8A%E7%BA%BF%E9%83%A8%E7%BD%B2/%E5%8F%8D%E5%90%91%E4%BB%A3%E7%90%86.png"></p>
<h3 id="反向代理的用途和好处"><a href="#反向代理的用途和好处" class="headerlink" title="反向代理的用途和好处"></a>反向代理的用途和好处</h3><ul>
<li>安全性：正向代理的客户端能够在隐藏自身信息的同时访问任意网站，这个给网络安全代理了极大的威胁。因此，我们必须把服务器保护起来，使用反向代理客户端用户只能通过外来网来访问代理服务器，并且用户并不知道自己访问的真实服务器是那一台，可以很好的提供安全保护。</li>
<li>功能性：反向代理的主要用途是为多个服务器提供负债均衡、缓存等功能。负载均衡就是一个网站的内容被部署在若干服务器上，可以把这些机子看成一个集群，那 Nginx 可以将接收到的客户端请求“均匀地”分配到这个集群中所有的服务器上，从而实现服务器压力的平均分配。</li>
</ul>
<h3 id="配置方法"><a href="#配置方法" class="headerlink" title="配置方法"></a>配置方法</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">server&#123; <span class="comment">// 访问 nginx2.jspang.com 反向代理到 jspang.com 这个网站。所以当我们访问nginx2.jspang.com的时候呈现的是jspang.com的内容</span></span><br><span class="line">        listen <span class="number">80</span>;</span><br><span class="line">        server_name nginx2.jspang.com;</span><br><span class="line">        location / &#123;</span><br><span class="line">               proxy_pass http:<span class="comment">//jspang.com;</span></span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Nginx-适配-PC-或移动设备"><a href="#Nginx-适配-PC-或移动设备" class="headerlink" title="Nginx 适配 PC 或移动设备"></a>Nginx 适配 PC 或移动设备</h2><p>大型网站，eg. 淘宝和京东的网站，根据 PC 端或移动端会有不同的站点，而不是用自适应布局。<br>用 Nginx 的 <code>$http_user_agent</code> 可以获取到客户端的 userAgent ，进而展示不同的页面给用户</p>
<h1 id="Linux-中搭建-MYSQL"><a href="#Linux-中搭建-MYSQL" class="headerlink" title="Linux 中搭建 MYSQL"></a>Linux 中搭建 MYSQL</h1><p>安装，搜教程</p>
<h2 id="Linux-操作数据库命令大全（结尾一定要带分号！）"><a href="#Linux-操作数据库命令大全（结尾一定要带分号！）" class="headerlink" title="Linux 操作数据库命令大全（结尾一定要带分号！）"></a>Linux 操作数据库命令大全（结尾一定要带分号！）</h2><p>参考自<a href="https://www.cnblogs.com/xiaochaohuashengmi/archive/2011/10/18/2216279.html" target="_blank" rel="noopener">博客 1</a>，<a href="https://blog.csdn.net/stamhe/article/details/3924638" target="_blank" rel="noopener">博客 2</a></p>
<ul>
<li>开启 mysql <code>service mysqld start</code>，<code>mysql -u root</code></li>
<li>关闭 <code>mysql&gt; exit;</code></li>
<li>进入 mysql 后，使用 SHOW 语句找出在服务器上当前存在什么数据库 <code>mysql&gt; SHOW DATABASES;</code></li>
<li>创建一个数据库 test <code>mysql&gt; CREATE DATABASE test character set utf8;</code></li>
<li>选择你所创建的数据库 <code>mysql&gt; USE test;</code></li>
<li>使用 SHOW 语句看数据库有几个表 <code>mysql&gt; SHOW TABLES;</code></li>
<li>创建一个表 <code>CREATE TABLE mytable (name VARCHAR(20), sex CHAR(1),birth DATE, birthaddr VARCHAR(20));</code><br>CREATE TABLE <code>type</code> (<br><code>id</code> int(10) unsigned NOT NULL auto_increment,<br><code>flag_deleted</code> enum(‘Y’,’N’) character set utf8 NOT NULL default ‘N’,<br><code>flag_type</code> int(5) NOT NULL default ‘0’,<br><code>type_name</code> varchar(50) character set utf8 NOT NULL default ‘’,<br>PRIMARY KEY (<code>id</code>)<br>) DEFAULT CHARSET=utf8;</li>
<li>显示表的结构 <code>mysql&gt; DESCRIBE mytable;</code></li>
<li>用 SELECT 命令来查看表中的数据 <code>mysql&gt; select * from mytable;</code></li>
<li>给表加入数据 <code>mysql&gt; insert into mytable values (′jacle′,′m′,′1996-02-04′,′china′);</code></li>
<li>用文本方式将数据装入一个数据库。创建一个文本文件“mysql.txt”，每行包含一个记录，用定位符(tab)把值分开，并且以在 CREATE TABLE 语句中列出的列次序给出,例如<br>abccs f 1977-07-07 china 　<br>mary f 1978-12-12 usa<br>tom m 1970-09-02 usa<br>使用下面命令将文本文件“mytable.txt”装载到 mytable 表中:<br><code>mysql&gt; LOAD DATA LOCAL INFILE &quot;/data/mytable.txt” INTO TABLE mytable;</code></li>
<li>修改表结构，更详见 <a href="https://www.jianshu.com/p/a6771d05ad59" target="_blank" rel="noopener">博客 3</a>，其中添加字段是 <code>ALTER TABLE &lt;表名&gt; ADD &lt;字段名称&gt; &lt;字段定义&gt;</code></li>
<li>删除表 <code>DROP TABLE table_name ;</code></li>
<li>查看表的编码 <code>show create table &lt;表名&gt;;</code></li>
<li>修改数据库成 utf8 的 <code>mysql&gt; alter database name character set utf8;</code></li>
<li>修改表默认用 utf8 <code>mysql&gt; alter table type character set utf8;</code></li>
<li>修改字段用 utf8 <code>mysql&gt; alter table type modify type_name varchar(50) CHARACTER SET utf8;</code></li>
<li>更新数据 <code>UPDATE &lt;表名&gt; SET id=2 WHERE type_id=2;</code></li>
</ul>
<h2 id="一些-bug"><a href="#一些-bug" class="headerlink" title="一些 bug"></a>一些 bug</h2><p><a href="https://blog.csdn.net/lkforce/article/details/79006838" target="_blank" rel="noopener">mysql 的 char，varchar，text 类型的区别总结</a><br>Linux 中 mysql 表无法写入中文，解决方法是 varchar 类型建议用 utf-8</p>
<h1 id="Linux-中搭建-Node-js-，npm-和-PM2"><a href="#Linux-中搭建-Node-js-，npm-和-PM2" class="headerlink" title="Linux 中搭建 Node.js ，npm 和 PM2"></a>Linux 中搭建 Node.js ，npm 和 PM2</h1><p>安装方法自行谷歌</p>
<h2 id="pm2"><a href="#pm2" class="headerlink" title="pm2"></a>pm2</h2><p>pm2 是一个带有负载均衡功能的应用进程管理器，也可以用来做进程守护。<a href="https://juejin.im/post/5be406705188256dbb5176f9" target="_blank" rel="noopener">pm2 简易使用手册</a></p>
<h3 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h3><ul>
<li>查看进程 <code>pm2 list</code></li>
<li>停止所有 <code>pm2 stop all</code></li>
<li>运行某个 node 文件 <code>pm2 start xxx.js</code></li>
<li>运行项目 <code>pm2 start npm —- run start/dev</code></li>
<li>重启 <code>pm2 restart XXX</code></li>
<li>停止 <code>pm2 stop XXX</code></li>
<li>删除 <code>pm2 delete XXX</code></li>
<li>查看某个进程/应用具体情况 <code>pm2 describe Travel</code></li>
<li>查看进程/应用的资源消耗情况 <code>pm2 monit</code></li>
<li>查看所有日志 <code>pm2 logs (Travel)</code></li>
<li>查看 7001 端口的进程 （PID 就是进程 id）<code>lsof -i:7001</code></li>
<li>关闭进程 <code>kill -9 &lt;进程id&gt;</code></li>
</ul>
<h3 id="pm2-项目管理"><a href="#pm2-项目管理" class="headerlink" title="pm2 项目管理"></a>pm2 项目管理</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">cd &lt;项目目录&gt;</span><br><span class="line"><span class="comment">// 在项目目录生成pm2配置文件：ecosystem.config.js。改该文件的name属性为自定义进程名字，script属性为react项目npm run start时执行的脚本（需要先npm run eject才会出现这个脚本）</span></span><br><span class="line">pm2 ecosystem</span><br><span class="line"><span class="comment">// 执行进程</span></span><br><span class="line">pm2 start ecosystem.config.js</span><br><span class="line"><span class="comment">// 查看执行的进程，发现会有自定义名字的进程</span></span><br><span class="line">pm2 list</span><br></pre></td></tr></table></figure>

<h1 id="部署流程"><a href="#部署流程" class="headerlink" title="部署流程"></a>部署流程</h1><blockquote>
<p>每次更改项目后应该先 build，再用 pm2 start；测试的时候应该用 dev</p>
</blockquote>
<h2 id="前台"><a href="#前台" class="headerlink" title="前台"></a>前台</h2><p>对于博客项目，前台是 Next.js 项目，拷贝之后先 <code>build</code> ，然后再 <code>pm2 start npm — run start</code> 就可以了<br>http 默认是 80 端口，可以把前台项目改成 80 端口；其次，你还要在安全组开放相关的端口才能成功访问的（其实我没做这一步）</p>
<h2 id="中台"><a href="#中台" class="headerlink" title="中台"></a>中台</h2><p>中台是 Egg.js 项目，直接 npm run start 就行（自带进程守护）。中台连接云服务器数据库方法暂时是把 <code>config.default.js</code> 改成下面如下配置，可以通过 IP 或者域名+端口号访问接口</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">config.cluter = &#123;</span><br><span class="line">  listen: &#123;</span><br><span class="line">    port: <span class="number">8000</span>,</span><br><span class="line">    hostname: <span class="string">'0.0.0.0'</span> <span class="comment">// 其实就是localhost</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="后台"><a href="#后台" class="headerlink" title="后台"></a>后台</h2><p>后台是 React 项目，是直接用 pm2 进行管理直接 start</p>
<h1 id="踩坑集合"><a href="#踩坑集合" class="headerlink" title="踩坑集合"></a>踩坑集合</h1><h2 id="跨域"><a href="#跨域" class="headerlink" title="跨域"></a>跨域</h2><ul>
<li>前后台项目中请求应该以服务器 ip 地址为准，而不是 localhost</li>
<li>数据中台的跨域设置是参考<a href="https://github.com/eggjs/egg/issues/3160" target="_blank" rel="noopener">该贴</a>，最后配置如下</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 如果只想让 domainWhiteList 的可以跨域，那就不用配置 origin</span></span><br><span class="line">config.security = &#123;</span><br><span class="line">  csrf: &#123; <span class="attr">enable</span>: <span class="literal">false</span> &#125;,</span><br><span class="line">  domainWhiteList: [<span class="string">'*'</span>]</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 如果你要用 origin: '*' 又想带 withCredentials，配置改成这样</span></span><br><span class="line">config.cors = &#123;</span><br><span class="line">  credentials: <span class="literal">true</span>,</span><br><span class="line">  origin: <span class="function"><span class="params">ctx</span> =&gt;</span> ctx.get(<span class="string">'origin'</span>),</span><br><span class="line">  allowMethods: <span class="string">'GET, HEAD, PUT, POST, DELETE, PATCH, OPTIONS'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="修改项目默认接口"><a href="#修改项目默认接口" class="headerlink" title="修改项目默认接口"></a>修改项目默认接口</h2><ul>
<li>React 项目修改默认接口：<code>node_modules</code> 文件夹里的可以看到 <code>react-scripts</code> 文件夹，在 <code>start.js</code> 里可以找到修改端口的代码。直接搜索 <code>||</code></li>
<li>Next 项目改默认端口是直接在<code>script</code>命令最后加 <code>-p &lt;端口号&gt;</code></li>
</ul>
<h2 id="React-项目-TypeError-ERR-INVALID-ARG-TYPE-The-“path”-argument-must-be-of-type-string-Received-type-undefined"><a href="#React-项目-TypeError-ERR-INVALID-ARG-TYPE-The-“path”-argument-must-be-of-type-string-Received-type-undefined" class="headerlink" title="React 项目 TypeError [ERR_INVALID_ARG_TYPE]: The “path” argument must be of type string. Received type undefined"></a>React 项目 TypeError [ERR_INVALID_ARG_TYPE]: The “path” argument must be of type string. Received type undefined</h2><p>react-scripts 版本问题，升级到 3.4.0。果然成功了！！！哭了！</p>
<p>参考文章<br><a href="https://mp.weixin.qq.com/s/80UEbeEfPBV2LMmRqJFT7w" target="_blank" rel="noopener">让你牛 B 加身的前端必会 Linux 命令-奇舞团</a><br><a href="https://www.runoob.com/linux/linux-tutorial.html" target="_blank" rel="noopener">Linux 教程-菜鸟教程</a><br><a href="https://jspang.com/detailed?id=39" target="_blank" rel="noopener">一个前端必会的 Nginx 免费教程-技术胖</a><br><a href="https://juejin.im/post/5be406705188256dbb5176f9" target="_blank" rel="noopener">pm2 简易使用手册</a></p>
]]></content>
      <categories>
        <category>上线部署</category>
      </categories>
      <tags>
        <tag>上线部署</tag>
      </tags>
  </entry>
  <entry>
    <title>前后端交互&amp;跨域</title>
    <url>/2019/10/31/%E5%89%8D%E5%90%8E%E7%AB%AF%E4%BA%A4%E4%BA%92&amp;%E8%B7%A8%E5%9F%9F/</url>
    <content><![CDATA[<h1 id="前后端数据交互"><a href="#前后端数据交互" class="headerlink" title="前后端数据交互"></a>前后端数据交互</h1><h2 id="利用-Cookie"><a href="#利用-Cookie" class="headerlink" title="利用 Cookie"></a>利用 Cookie</h2><p>Cookie 是一些数据, 存储于电脑上的文本文件中,只要客户端 cookie 开放且有数据，每一次请求都会自动添加到 http 报文中，后台可以实时接收观察获取这些 Cookie 。</p>
<p>Cookie 的作用就是用于解决 “如何记录客户端的用户信息”:</p>
<ul>
<li>当用户访问 web 页面时，他的名字可以记录在 cookie 中。</li>
<li>在用户下一次访问该页面时，可以在 cookie 中读取用户访问记录。</li>
</ul>
<h2 id="利用-Session-对象"><a href="#利用-Session-对象" class="headerlink" title="利用 Session 对象"></a>利用 Session 对象</h2><p>session 对象表示特定会话 session 的用户数据。</p>
<p>客户第一次访问支持 session 的 JSP 网页，服务器会创建一个 session 对象记录客户的信息。当客户访问同一网站的不同网页时，仍处于同一个 session 中。</p>
<p>request.getSession().setAttribute();<br>request.getSession().getAttribute();<br>只要浏览器不关闭，就能使用。所以用户访问网站整个生命都会用到的数据一般都用 session 来存储，比如用户名、登录状态之类的</p>
<h2 id="Ajax"><a href="#Ajax" class="headerlink" title="Ajax"></a>Ajax</h2><p>PS：相对有点老了，用 fetch 方便很多<br>Ajax = 异步 JavaScript 和 XML。是一种在无需重新加载整个网页的情况下，能够更新部分网页的技术</p>
<h3 id="XHR-对象"><a href="#XHR-对象" class="headerlink" title="XHR 对象"></a>XHR 对象</h3><p>XMLHttpRequest（XHR) 对象是 Ajax 的基础，用于在后台与服务器交换数据。用 var 变量名 = new XMLHttpRequest(); 创建</p>
<h4 id="XHR-的方法"><a href="#XHR-的方法" class="headerlink" title="XHR 的方法"></a>XHR 的方法</h4><p>如需将请求发送到服务器，我们使用 XMLHttpRequest 对象的</p>
<ul>
<li>open(method, url, async)方法。第一个参数是’GET’/‘POST’；第三个参数是一个布尔值，同步/异步<br>eg. <code>request.open(&#39;GET&#39;, &#39;/api/categories&#39;，true);</code></li>
<li>send(string)方法。用于将请求及参数发送到服务器，有参数时仅用于 POST。对于一些没带参数的请求方法，可以写成 send(null)</li>
</ul>
<h4 id="获取响应"><a href="#获取响应" class="headerlink" title="获取响应"></a>获取响应</h4><p>需获得来自服务器的响应，请使用 XMLHttpRequest 对象的 responseText 或 responseXML 属性。<br>eg. <code>document.getElementById(&quot;myDiv&quot;).innerHTML=xmlhttp.responseText;</code></p>
<h4 id="状态信息及监听"><a href="#状态信息及监听" class="headerlink" title="状态信息及监听"></a>状态信息及监听</h4><p><strong>readyState</strong>属性存有 XMLHttpRequest 的状态信息，有以下几个属性值：</p>
<ul>
<li>0: （未初始化）XHR 对象已创建，open()未调用</li>
<li>1: （载入）send()已调用，正在发送请求</li>
<li>2: （载入完成）send()方法执行完成，已经收到全部响应的内容</li>
<li>3: （交互）正在解析内容</li>
<li><strong>4</strong>: （完成）响应内容解析完成，可以在客户端使用了</li>
</ul>
<p>status 属性，该属性有两个属性值：200，404<br>当 readyState 改变时，就会触发 onreadystatechange 事件；此外 XHR 对象还提供了其他监听事件：process 事件，load 事件（加载完成），error 事件，abort 事件（加载终止）。可以用 addEventListener 来监听。<br>一般 open()并监听（监听后执行异步代码）后还会 send()，这才是一个完整的流程</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//  普通版</span></span><br><span class="line"><span class="keyword">const</span> xhr = <span class="keyword">new</span> XMLHttpRequest()</span><br><span class="line">xhr.open(<span class="string">'GET'</span>, <span class="string">'https:www.baidu.com'</span>, <span class="literal">true</span>)</span><br><span class="line">xhr.onreadystatechange = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (xhr.readyState === <span class="number">4</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (xhr.status === <span class="number">200</span>) &#123;</span><br><span class="line">      alert(xhr.responseText)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">xhr.send()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 上面的 onreadystatechange 部分也可以写成如下：</span></span><br><span class="line">xhr.onload = <span class="function"><span class="keyword">function</span> (<span class="params">e</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (xhr.readyState === <span class="number">4</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (xhr.status === <span class="number">200</span>) &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(xhr.responseText);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="built_in">console</span>.error(xhr.statusText);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line">xhr.onerror = <span class="function"><span class="keyword">function</span> (<span class="params">e</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.error(xhr.statusText);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//  Promise版</span></span><br><span class="line"><span class="keyword">const</span> myAjax = <span class="function"><span class="keyword">function</span>(<span class="params">url</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> promise = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span>(<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> handler = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (<span class="keyword">this</span>.readystate !== <span class="number">4</span>) <span class="keyword">return</span></span><br><span class="line">      <span class="keyword">if</span> (<span class="keyword">this</span>.status === <span class="number">200</span>) &#123;</span><br><span class="line">        resolve(<span class="keyword">this</span>.response)</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        reject(<span class="keyword">new</span> <span class="built_in">Error</span>(<span class="keyword">this</span>.statustext))</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">const</span> client = <span class="keyword">new</span> XMLHttpRequest()</span><br><span class="line">    client.open(<span class="string">'GET'</span>, url)</span><br><span class="line">    client.onreadystatechange = handle</span><br><span class="line">    client.send()</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="keyword">return</span> promise</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">myAjax(<span class="string">'/url'</span>)</span><br><span class="line">  .then(<span class="function"><span class="params">res</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(res)</span><br><span class="line">  &#125;)</span><br><span class="line">  .cathch(<span class="function"><span class="params">e</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(e)</span><br><span class="line">  &#125;)</span><br></pre></td></tr></table></figure>

<h3 id="XHR-请求的缓存问题的处理方案"><a href="#XHR-请求的缓存问题的处理方案" class="headerlink" title="XHR 请求的缓存问题的处理方案"></a>XHR 请求的缓存问题的处理方案</h3><p>由于浏览器的缓存机制，当我们使用 XMLHttpRequest 发出请求的时候，浏览器会将请求的地址与缓存中的地址进行比较，如果存在相同记录则根据不向服务器发出请求而直接返回与上一次请求相同内容。但我们有时候其实希望每次都请求而不读缓存，解决方法如下：</p>
<h4 id="方法一：URL-加时间戳"><a href="#方法一：URL-加时间戳" class="headerlink" title="方法一：URL 加时间戳"></a>方法一：URL 加时间戳</h4><p>在每次请求的 url 后面加上 ‘?’ or ‘&amp;’ 再加当前时间的字符串或其他类似的不会重复的随机字符串，这样浏览器每次发出的是不同的 url，即会当做不同的请求来处理，而不会从缓存中读取</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">var oReq = new XMLHttpRequest();</span><br><span class="line">oReq.open(&quot;GET&quot;, url + ((/\?/).test(url) ? &quot;&amp;&quot; : &quot;?&quot;) + (new Date()).getTime());</span><br><span class="line">oReq.send(null);</span><br></pre></td></tr></table></figure>

<h4 id="方法二：HTTP-的缓存策略"><a href="#方法二：HTTP-的缓存策略" class="headerlink" title="方法二：HTTP 的缓存策略"></a>方法二：HTTP 的缓存策略</h4><p>可以把 Cache-ontrol: no-cache + If-Modified-Since: 0。写成 XHR 代码就是</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">XMLHttpRequest.setRequestHeader(&quot;If-Modified-Since&quot;, &quot;0&quot;);</span><br><span class="line">XMLHttpRequest.setRequestHeader(&quot;Cache-ontrol&quot;, &quot;no-cache&quot;);</span><br><span class="line">XMLHttpRequest.send(null);</span><br></pre></td></tr></table></figure>

<h3 id="实现一个前端缓存模块"><a href="#实现一个前端缓存模块" class="headerlink" title="实现一个前端缓存模块"></a>实现一个前端缓存模块</h3><p>实现一个前端缓存模块，主要用于缓存 xhr 返回的结果，避免多余的网络请求浪费，要求：</p>
<ul>
<li>生命周期为一次页面打开</li>
<li>如果有相同的请求同时并行发起，要求其中一个能挂起并且等待另外一个请求返回并读取该缓存<h4 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h4></li>
</ul>
<p>参考了<a href="https://blog.csdn.net/qq_30114149/article/details/79419600" target="_blank" rel="noopener">封装原生 ajax（缓存+xhr 单例）</a>，有所删改。还没完成</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">export class XAjaxRequester &#123;</span><br><span class="line">  constructor() &#123;</span><br><span class="line">    this.cacheUrl = &#123;&#125; // 缓存</span><br><span class="line">  &#125;</span><br><span class="line">  updataStatus(url, res) &#123;</span><br><span class="line">    this.cacheUrl[url] = res</span><br><span class="line">  &#125;</span><br><span class="line">  get(url, successCallBack) &#123;</span><br><span class="line">    let _self = this</span><br><span class="line">    let xhr = new XMLHttpRequest()</span><br><span class="line">    // 发起请求但是没响应的时候标记为 1</span><br><span class="line">    _self.cacheUrl[url] = 1</span><br><span class="line">    if (!_self.cacheUrl[url]) &#123;</span><br><span class="line">      // 没缓存，正常执行</span><br><span class="line">      xhr.open(&apos;get&apos;, url, true)</span><br><span class="line">      xhr.onreadystatechange = function() &#123;</span><br><span class="line">        if (this.readyState === 4) &#123;</span><br><span class="line">          if (</span><br><span class="line">            (this.status &gt;= 200 &amp;&amp; this.status &lt; 300) ||</span><br><span class="line">            this.status === 304</span><br><span class="line">          ) &#123;</span><br><span class="line">            let res = this.responseText</span><br><span class="line">            if (typeof successCallBack === &apos;function&apos;) &#123;</span><br><span class="line">              successCallBack(res)</span><br><span class="line">              _self.updataStatus(url, res) // 更新缓存，发布消息给订阅者</span><br><span class="line">            &#125;</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      xhr.send(null)</span><br><span class="line">    &#125; else if (_self.cacheUrl[url] === 1) &#123;</span><br><span class="line">      // 有url，但还没缓存资源。整一个发布订阅?</span><br><span class="line">      </span><br><span class="line">    &#125; else &#123;</span><br><span class="line">      // 有缓存，直接拿</span><br><span class="line">      data = _self.cacheUrl[url]</span><br><span class="line">      successCallBack(data)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">//  使用demo</span><br><span class="line">let cacheAjax = new XAjaxRequester()</span><br><span class="line">cacheAjax.get(&apos;/url&apos;, function(res) &#123;</span><br><span class="line">  /* 成功回调 */</span><br><span class="line">&#125;)</span><br><span class="line">cacheAjax.get(&apos;/url&apos;, function(res) &#123;</span><br><span class="line">  /* 成功回调 */</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h3 id="杂的知识点"><a href="#杂的知识点" class="headerlink" title="杂的知识点"></a>杂的知识点</h3><p>在ajax应用中，通常一个页面要同时发送多个请求，如果只有一个XMLHttpRequest对象，前面的请求还未完成，后面的就会把前面的覆盖 掉，如果每次都创建一个新的XMLHttpRequest对象，也会造成浪费。解决的办法就是创建一个XMLHttpRequset的对象池，如果池里有 空闲的对象，则使用此对象，否则将创建一个新的对象。见这篇<a href="ttps://blog.csdn.net/jiedushi/article/details/2531215" target="_blank" rel="noopener">远古贴</a>。也有另一种方案是每次用完之后就delete这个变量再让变量 = null</p>
<h2 id="axios"><a href="#axios" class="headerlink" title="axios"></a>axios</h2><p>axios 是一个基于 Promise 用于浏览器和 nodejs 的 HTTP 客户端，本质上也是对原生 XHR 的封装，只不过它是 Promise 的实现版本，符合最新的 ES 规范</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">axios(&#123;</span><br><span class="line">  method: <span class="string">'post'</span>,</span><br><span class="line">  url: <span class="string">'/user/12345'</span>,</span><br><span class="line">  data: &#123;</span><br><span class="line">    firstName: <span class="string">'Fred'</span>,</span><br><span class="line">    lastName: <span class="string">'Flintstone'</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line">  .then(<span class="function"><span class="keyword">function</span>(<span class="params">response</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(response)</span><br><span class="line">  &#125;)</span><br><span class="line">  .catch(<span class="function"><span class="keyword">function</span>(<span class="params">error</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(error)</span><br><span class="line">  &#125;)</span><br></pre></td></tr></table></figure>

<h2 id="fetch"><a href="#fetch" class="headerlink" title="fetch"></a>fetch</h2><p>fetch 能实现以前 XHR 对象能做的事情。还提供了 全局 fetch()方法，该方法提供了一种简单，合理的方式来跨网络异步获取资源。但是 fetch<strong>不是 ajax 的进一步封装，而是原生 js</strong>，没有使用 XMLHttpRequest 对象<br>简单的例子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">fetch(<span class="string">'http://example.com/movies.json'</span>)</span><br><span class="line">  .then(<span class="function"><span class="keyword">function</span>(<span class="params">response</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> response.json()</span><br><span class="line">  &#125;)</span><br><span class="line">  .then(<span class="function"><span class="keyword">function</span>(<span class="params">myJson</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(myJson)</span><br><span class="line">  &#125;)</span><br></pre></td></tr></table></figure>

<h3 id="fetch-url-init"><a href="#fetch-url-init" class="headerlink" title="fetch(url, init)"></a>fetch(url, init)</h3><h4 id="参数"><a href="#参数" class="headerlink" title="参数"></a>参数</h4><p>该方法第一个参数是 url/一个 Request 对象，第二个参数（可选）是是用来自定义请求信息的<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/WindowOrWorkerGlobalScope/fetch" target="_blank" rel="noopener">init 对象</a>，该对象有 method，headers（可以是一个 Headers 对象），body（可以是一个 Body 对象），cache 等可选的属性。</p>
<h4 id="返回值"><a href="#返回值" class="headerlink" title="返回值"></a>返回值</h4><p>一个 Promise，resolve 时回传 Response 对象<br>例外：<br>AbortError：请求 abort 的情况<br>TypeError：请求的 url 有证书问题<br><strong>注意</strong>，最好使用符合<a href="https://developer.mozilla.org/zh-CN/docs/Web/Security/CSP/CSP_policy_directives" target="_blank" rel="noopener">内容安全策略 (CSP)</a>的链接而不是使用直接指向资源地址的方式来进行 Fetch 的请求</p>
<h3 id="Headers-对象"><a href="#Headers-对象" class="headerlink" title="Headers 对象"></a>Headers 对象</h3><p>可以通过 Headers() 构造函数来创建一个你自己的 headers 对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> myHeaders = <span class="keyword">new</span> Headers(&#123;</span><br><span class="line">  <span class="string">'Content-Type'</span>: <span class="string">'text/plain'</span>,</span><br><span class="line">  <span class="string">'Content-Length'</span>: content.length.toString(),</span><br><span class="line">  <span class="string">'X-Custom-Header'</span>: <span class="string">'ProcessThisImmediately'</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>Headers.prototype.has(key 名)检测是否含有某个 key 名，返回布尔值；Header.prototype.get(key 名)/getAll(key 名)获取 key 值</p>
<h3 id="Response-对象"><a href="#Response-对象" class="headerlink" title="Response 对象"></a>Response 对象</h3><p>Response 实例是在 fetch() 处理完 promises 之后返回的<br>PS：其实 Headers 和 Response 的 new 下都能接受两个参数，第一个参数是数据体，第二个参数（可选）是 init 对象</p>
<h4 id="常见属性"><a href="#常见属性" class="headerlink" title="常见属性"></a>常见属性</h4><ul>
<li>Response.status：返回的状态码</li>
<li>Response.statusText：返回的状态信息。eg，200 的时候是 OK</li>
<li>Response.ok：检查 response 的状态是否在 200-299(包括 200,299)这个范围内.该属性返回一个 Boolean 值。eg.</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">.then(<span class="function"><span class="keyword">function</span>(<span class="params">response</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span>(response.ok) &#123;</span><br><span class="line">    <span class="keyword">return</span> response.json();</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'Network response was not ok.'</span>);</span><br></pre></td></tr></table></figure>

<h3 id="Body"><a href="#Body" class="headerlink" title="Body"></a>Body</h3><p>请求和响应都能有一个 body 属性包含一个 Body 对象。body 也可以是<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Fetch_API/Using_Fetch" target="_blank" rel="noopener">很多种数据类型</a>，eg. <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/FormData" target="_blank" rel="noopener">FormData</a></p>
<blockquote>
<p>FormData 提供了一种表示<strong>表单数据</strong>的<strong>键值对</strong>的构造方式。有很多简单实用的方法。见下面例题</p>
</blockquote>
<h4 id="Body-的方法"><a href="#Body-的方法" class="headerlink" title="Body 的方法"></a>Body 的方法</h4><p><strong>这些方法都能被 Request 和 Response 使用，用来转变数据类型</strong></p>
<ul>
<li>Body.json()。接收一个 Response 流，并将其读取完成。它返回一个 <strong>Promise</strong>，Promise 的解析 resolve 结果是将文本体解析为 JSON</li>
<li>Body.formData()。将 Response 对象中的所承载的数据流读取并封装成为一个对象，该方法将返回一个 Promise 对象，该对象将产生一个 FormData 对象</li>
</ul>
<h3 id="兼容性检测"><a href="#兼容性检测" class="headerlink" title="兼容性检测"></a>兼容性检测</h3><p>可以通过检测 Headers, Request, Response 或 fetch()是否在 Window 或 Worker 域中。例如</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (self.fetch) &#123;</span><br><span class="line">  <span class="comment">// run my fetch request here</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  <span class="comment">// do something with XMLHttpRequest?</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="例题"><a href="#例题" class="headerlink" title="例题"></a>例题</h3><p>实现一个提交文件不刷新页面的方案，给出关键代码<br>看<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Fetch_API/Using_Fetch" target="_blank" rel="noopener">MDN</a>有更多实用例子（eg. 发送带凭据的请求，上传 JSON 数据，上传文件/多个文件，检测请求是否成功，自定义请求对象）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;input type=<span class="string">"file"</span> id=<span class="string">"upload"</span>&gt;</span><br><span class="line">&lt;button id=<span class="string">"btn"</span>&gt; upload &lt;<span class="regexp">/button&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">document</span>.getElementById(<span class="string">'upload-btn'</span>).onclick = funcion() &#123;</span><br><span class="line">  <span class="keyword">var</span> formData = <span class="keyword">new</span> FormData();</span><br><span class="line">  <span class="keyword">var</span> fileField = <span class="built_in">document</span>.querySelector(<span class="string">"input[type='file']"</span>);</span><br><span class="line">  formData.append(<span class="string">'file'</span>, fileField.files[<span class="number">0</span>]);</span><br><span class="line"></span><br><span class="line">  fetch(<span class="string">'https://example.com/profile/avatar'</span>, &#123;</span><br><span class="line">    method: <span class="string">'POST'</span>,</span><br><span class="line">    body: formData</span><br><span class="line">  &#125;)</span><br><span class="line">  .then(<span class="function"><span class="params">response</span> =&gt;</span> response.json())</span><br><span class="line">  .catch(<span class="function"><span class="params">error</span> =&gt;</span> <span class="built_in">console</span>.error(<span class="string">'Error:'</span>, error))</span><br><span class="line">  .then(<span class="function"><span class="params">response</span> =&gt;</span> <span class="built_in">console</span>.log(<span class="string">'Success:'</span>, response));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="WebSocket"><a href="#WebSocket" class="headerlink" title="WebSocket"></a><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/WebSocket" target="_blank" rel="noopener">WebSocket</a></h2><p>Websocket 是一个全新的、独立的协议，基于 TCP 协议，与 http 协议兼容、却不会融入 http 协议。<br>由于HTTP通信只能由客户端发起，举例来说，我们想了解今天的天气，只能是客户端向服务器发出请求，服务器返回查询结果。HTTP 协议做不到服务器主动向客户端推送信息。要解决这种场景只能轮询，但是很浪费资源。<br>所以Websocket设计出来的目的就是要取代轮询（每隔一段时间浏览器对服务器发起请求，服务器返回新数据）和 Comet 技术。<br>WebSocket<strong>在单个 TCP 连接上进行全双工通讯的协议</strong>。WebSocket API 可在用户的浏览器和服务器之间进行双向通信。用户可以向服务器发送消息并接收事件驱动的响应，而无需轮询服务器。 它可以<strong>让多个用户连接到同一个实时服务器，并通过 API 进行通信并立即获得响应。</strong><br>它的最大特点就是，【<strong>服务器可以主动向客户端推送信息，客户端也可以主动向服务器发送信息，是真正的双向平等对话，属于服务器推送技术的一种</strong>】</p>
<p>它<strong>允许用户和服务器之间的流连接，并允许即时信息交换</strong>。在聊天应用程序的示例中，通过套接字汇集消息，可以实时与一个或多个用户交换，具体取决于谁在服务器上“监听”（连接）。<br>WebSockets 适用于<strong>需要实时更新和即时信息交换</strong>的任何应用程序。一些示例包括但不限于：现场体育更新，股票行情，多人游戏，聊天应用，社交媒体等</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> socket=<span class="keyword">new</span> WebSocket(<span class="string">"/url"</span>)；</span><br><span class="line">socket.send(<span class="string">"hello world"</span>); <span class="comment">//  send() 方法来向服务器发送数据</span></span><br><span class="line">socket.onmessage=<span class="function"><span class="keyword">function</span>(<span class="params">event</span>)</span>&#123;  <span class="comment">// onmessage 事件来接收服务器返回的数据</span></span><br><span class="line">    <span class="built_in">console</span>.log(event.data);</span><br><span class="line">    <span class="built_in">console</span>.log(event.readyState);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>详见MDN及<a href="https://www.ruanyifeng.com/blog/2017/05/websocket.html" target="_blank" rel="noopener">阮一峰</a></p>
<h1 id="跨域"><a href="#跨域" class="headerlink" title="跨域"></a>跨域</h1><p><strong>同源策略</strong>：当一个资源从与该资源本身所在服务器中不同域、协议、端口请求一个资源时，就是<strong>跨域</strong>。出于安全原因，浏览器限制从脚本内发起的跨域 HTTP 请求。XMLHttpRequest 和 Fetch 的请求都会失败（response.status = 0）<br>引入这个同源策略主要是用来防止 CSRF 攻击（没有同源策略的情况下，A 网站可以被任意其他来源的 Ajax 访问到内容。如果你当前 A 网站还存在登录态，那么对方就可以通过 Ajax 获得你的任何信息。当然同源策略并不能完全阻止 CSRF)）</p>
<p><strong>解决跨域方式</strong>如下：</p>
<h2 id="JSONP"><a href="#JSONP" class="headerlink" title="JSONP"></a>JSONP</h2><p>JSONP 的原理很简单，就是利用 <code>&lt;script&gt;</code> 标签没有跨域限制的漏洞。<br>通过 <code>&lt;script&gt;</code> 标签指向一个需要访问的地址并提供一个回调函数来接收数据当需要通讯时。此时使服务器返回的是一段代码就可以了，例如下面的代码服务端可以返回一段 <code>jsonp(&#39;I am data&#39;)</code> 去调用本来就写在<code>&lt;script&gt;</code>中的函数</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"http://domain/api?param1=a&amp;param2=b&amp;callback=jsonp"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="actionscript">  <span class="function"><span class="keyword">function</span> <span class="title">jsonp</span><span class="params">(data)</span> </span>&#123;</span></span><br><span class="line"><span class="javascript">    <span class="built_in">console</span>.log(data)</span></span><br><span class="line">  &#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h3><p>优点是操作方便，兼容性好<br>缺点：</p>
<ul>
<li>只限于 get 请求</li>
<li>url 限制了参数大小</li>
<li>错误处理机制不好，调用失败的时候不会返回各种 HTTP 状态码</li>
<li>安全性。假如提供 jsonp 的服务存在页面注入漏洞，即它返回的 javascript 的内容被人控制的，结果是所有调用这个 jsonp 的网站都会存在漏洞，于是无法把危险控制在用一个域名下</li>
</ul>
<h3 id="回调函数名同名的解决方案"><a href="#回调函数名同名的解决方案" class="headerlink" title="回调函数名同名的解决方案"></a>回调函数名同名的解决方案</h3><p>在开发中可能会遇到多个 JSONP 请求的回调函数名是相同的，这时候就需要自己封装一个 JSONP，以下是简单实现:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">jsonp</span>(<span class="params">url, jsonpCallbackName, success</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> script = <span class="built_in">document</span>.createElement(<span class="string">'script'</span>)</span><br><span class="line">  script.src = url</span><br><span class="line">  script.async = <span class="literal">true</span></span><br><span class="line">  script.type = <span class="string">'text/javascript'</span></span><br><span class="line">  <span class="built_in">window</span>[jsonpCallbackName] = <span class="function"><span class="keyword">function</span>(<span class="params">data</span>) </span>&#123;</span><br><span class="line">    success &amp;&amp; success(data)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">document</span>.body.appendChild(script)</span><br><span class="line">&#125;</span><br><span class="line">jsonp(<span class="string">''</span>, <span class="string">'callback'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">value</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(value)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h2 id="CORS"><a href="#CORS" class="headerlink" title="CORS"></a><a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Access_control_CORS" target="_blank" rel="noopener">CORS</a></h2><ul>
<li>CORS 需要浏览器和后端同时支持。IE 8 和 9 需要通过 XDomainRequest 来实现。</li>
<li>浏览器会自动进行 CORS 通信，实现 CORS 通信的<strong>关键是后端</strong>。只要后端实现了 CORS，就实现了跨域。</li>
<li>服务端设置 <code>Access-Control-Allow-Origin</code> 就可以开启 CORS。 该属性表示哪些域名可以访问资源，如果设置通配符则表示所有网站都可以访问资源。</li>
</ul>
<p>虽然设置 CORS 和前端没什么关系，但是通过这种方式解决跨域问题的话，会在发送请求时出现两种情况，分别为<strong>简单请求和预检请求</strong>。</p>
<h3 id="简单请求"><a href="#简单请求" class="headerlink" title="简单请求"></a>简单请求</h3><p>有一些条件，eg. method 是 GET, HEAD, POST。详情见 MDN<br>简单请求的时候，服务看请求的 Orgin 字段（请求网站的 URL），看自己的 Access-Control-Allow-Origin 字段（值为*，表示该资源可以被任意外域访问，也可以是某个具体域名），比较两者是否相符决定定要不要给资源了</p>
<h3 id="预检请求"><a href="#预检请求" class="headerlink" title="预检请求"></a>预检请求</h3><p>另外，规范要求，对那些可能对服务器数据产生副作用的 HTTP 请求方法（特别是 GET 以外的 HTTP 请求，或者搭配某些 MIME 类型的 POST 请求），<strong>浏览器必须首先使用 OPTIONS 方法发起一个预检请求（preflight request），从而获知服务器是否允许该跨域请求</strong></p>
<p><strong>服务器确认允许之后，才发起实际的 HTTP 请求</strong>。 在预检请求的返回中，服务器端也可以通知客户端，是否需要携带身份凭证（包括 cookie 和 HTTP 认证相关数据）</p>
<p><strong>如果发起请求时设置 WithCredentials 标志设置为 true，从而向服务器发送 cookie</strong>，但是如果服务器的响应中未携带<strong>Access-Control-Allow-Credentials: true，浏览器将不会把响应内容返回给请求的发送者</strong></p>
<h3 id="附带身份凭证的请求"><a href="#附带身份凭证的请求" class="headerlink" title="附带身份凭证的请求"></a>附带身份凭证的请求</h3><p>Fetch 与 CORS 的一个有趣的特性是，可以基于 HTTP cookies 和 HTTP 认证信息发送身份凭证<br>服务器不得设置 Access-Control-Allow-Origin 的值为*，必须是某个具体的域名。<br>注意，简单 GET 请求不会被预检；如果此类带有身份凭证请求的响应中不包含该字段，这个响应将被忽略掉，并且浏览器也不会将相应内容返回给网页</p>
<h2 id="document-domain"><a href="#document-domain" class="headerlink" title="document.domain"></a>document.domain</h2><p>该方式只能用于二级域名相同的情况下，比如 <code>a.test.com</code> 和 <code>b.test.com</code> 适用于该方式。<br>只需要给页面添加 <code>document.domain = &#39;test.com&#39;</code> 表示二级域名都相同就可以实现跨域</p>
<h2 id="postMessage"><a href="#postMessage" class="headerlink" title="postMessage"></a>postMessage</h2><p>这种方式通常用于获取嵌入页面中的第三方页面数据。一个页面发送消息，另一个页面判断来源并接收消息</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 发送消息端</span></span><br><span class="line"><span class="built_in">window</span>.parent.postMessage(<span class="string">'message'</span>, <span class="string">'http://test.com'</span>)</span><br><span class="line"><span class="comment">// 接收消息端</span></span><br><span class="line"><span class="keyword">var</span> mc = <span class="keyword">new</span> MessageChannel()</span><br><span class="line">mc.addEventListener(<span class="string">'message'</span>, event =&gt; &#123;</span><br><span class="line">  <span class="keyword">var</span> origin = event.origin || event.originalEvent.origin</span><br><span class="line">  <span class="keyword">if</span> (origin === <span class="string">'http://test.com'</span>) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'验证通过'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>参考资料<br>W3School<br>MDN<br><a href="https://blog.nowcoder.net/yiizgo" target="_blank" rel="noopener">呓语</a><br><a href="https://github.com/poetries/" target="_blank" rel="noopener">poetries</a><br><a href="https://www.cnblogs.com/lanyueff/p/7771075.html" target="_blank" rel="noopener">JSONP 原理及使用</a></p>
]]></content>
      <categories>
        <category>后端</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>哈希表 &amp; 哈希算法</title>
    <url>/2020/05/26/%E5%93%88%E5%B8%8C%E8%A1%A8%20&amp;%20%E5%93%88%E5%B8%8C%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<h1 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h1><h2 id="哈希表-散列表"><a href="#哈希表-散列表" class="headerlink" title="哈希表 (散列表)"></a>哈希表 (散列表)</h2><p>在线性表中，所有的数据都是顺序存储，当我们需要在线性表中查找某一数据时，当线性表过长，需要查找的数据排序比较靠后的话，就需要花费大量的时间，导致查找性能较差</p>
<p>所以我们可以把数据存在哈希表中，通过 key 去拿到数据。这个过程可以描述为：</p>
<blockquote>
<p>拿到key(关键字) &gt; 关键字通过散列函数(哈希函数)计算出来的值则称为散列值（哈希值、Hash 值） &gt; 过散列值到**散列表（哈希表、Hash 表)**中就可以获取检索值</p>
</blockquote>
<p>如下图：<br><img src="/img/%E5%93%88%E5%B8%8C%E5%87%BD%E6%95%B0/%E5%93%88%E5%B8%8C%E8%A1%A8.png"></p>
<p>所以我们需要存的信息有：key，散列函数，散列表信息。所以，散列表是一种空间换时间的存储结构，是在算法中提升效率的一种比较常用的方式。</p>
<p>散列函数的作用就是给定一个键值，然后返回值在表中的地址：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 散列表</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">HashTable</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> table = []</span><br><span class="line">  <span class="keyword">this</span>.put = <span class="function"><span class="keyword">function</span>(<span class="params">key, value</span>) </span>&#123;&#125;</span><br><span class="line">  <span class="keyword">this</span>.get = <span class="function"><span class="keyword">function</span>(<span class="params">key</span>) </span>&#123;&#125;</span><br><span class="line">  <span class="keyword">this</span>.remove = <span class="function"><span class="keyword">function</span>(<span class="params">key</span>) </span>&#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="哈希函数"><a href="#哈希函数" class="headerlink" title="哈希函数"></a>哈希函数</h2><p><strong>哈希函数（Hash Function），也称为散列函数或杂凑函数</strong>。哈希函数是一个公开函数，可以将任意长度的消息M映射成为一个长度较短且长度固定的值H（M），称H（M）为哈希值、散列值（Hash Value）、杂凑值或者消息摘要（Message Digest）。<strong>它是一种单向密码体制，即一个从明文到密文的不可逆映射，只有加密过程，没有解密过程</strong>。</p>
<p>它的函数表达式为：<code>h = H(m)</code></p>
<p>无论输入是什么数字格式、文件有多大，输出都是固定长度的比特串。eg. 以比特币使用Sh256算法，无论输入是什么数据文件，输出就是256bit。<br>每个bit就是一位0或者1，256bit就是256个0或者1二进制数字串，用16进制数字表示的话: 16等于2的4次方，所以每一位16进制数字可以代表4位bit，所以256位bit用16进制数字表示是256除以4等于64位。<br>于是我们通常看到的哈希值，是这样的：</p>
<blockquote>
<p>00740f40257a13bf03b40f54a9fe398c79a664bb21cfa2870ab07888b21eeba8</p>
</blockquote>
<h1 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h1><ul>
<li>易压缩：对于任意大小的输入x，Hash值的长度很小，在实际应用中，函数H产生的Hash值其长度是固定的。</li>
<li>易计算：对于任意给定的消息，计算其Hash值比较容易。</li>
<li>单向性：对于给定的Hash值，要找到使得在计算上是不可行的，即求Hash的逆很困难。在给定某个哈希函数H和哈希值H（M）的情况下，得出M在计算上是不可行的。即从哈希输出无法倒推输入的原始数值。这是哈希函数安全性的基础。</li>
<li>抗碰撞性：理想的Hash函数是无碰撞的，但在实际算法的设计中很难做到这一点。有两种抗碰撞性：一种是弱抗碰撞性，即对于给定的消息，要发现另一个消息，满足在计算上是不可行的；另一种是强抗碰撞性，即对于任意一对不同的消息，使得在计算上也是不可行的。</li>
<li>高灵敏性：这是从比特位角度出发的，指的是1比特位的输入变化会造成1/2的比特位发生变化。消息M的任何改变都会导致哈希值H（M）发生改变。即如果输入有微小不同，哈希运算后的输出一定不同。</li>
</ul>
<h1 id="哈希算法的要求"><a href="#哈希算法的要求" class="headerlink" title="哈希算法的要求"></a>哈希算法的要求</h1><p>一个可靠的哈希算法，应该满足：</p>
<ul>
<li>对于给定的数据M,很容易算出哈希值X=F(M)；根据X很难反算出M</li>
<li>很难找到M和N使得F(N)=F(M)（这个可能是加密领域的，其实在储存领域，为了节省空间，key 值不同，通过散列函数计算出来的散列值（value）不一定不相同）</li>
</ul>
<p>哈希函数更多设计方法和冲突解决方法详见<a href="https://github.com/sisterAn/JavaScript-Algorithms/issues/49" target="_blank" rel="noopener">哈希表问题</a></p>
<h1 id="一个简单的例子"><a href="#一个简单的例子" class="headerlink" title="一个简单的例子"></a>一个简单的例子</h1><p>一个初始值值对应一个唯一的结果值，根据结果还推算不出来初始值，这可以通过<strong>丢掉一部分信息</strong>的加密方式实现，称为“单向加密”，也叫哈希算法。一个简单的例子：取注册者注册的时间戳的后6位再乘6取后6位，勉勉强强可以算是一个哈希算法。</p>
<h1 id="常见的哈希算法（加密领域）"><a href="#常见的哈希算法（加密领域）" class="headerlink" title="常见的哈希算法（加密领域）"></a>常见的哈希算法（加密领域）</h1><ul>
<li>SHA-1算法</li>
<li>SHA-2算法</li>
<li>SHA-512算法</li>
<li>SHA-3算法</li>
<li>RIPEMD160算法</li>
<li>…</li>
</ul>
<blockquote>
<p>这些算法的过程大概包括：输入 &gt; 消息填充 &gt; 链接变量初始化 &gt; 处理主循环模块 &gt; 得出最终的Hash值</p>
</blockquote>
<p>算法的具体细节见<a href="https://mp.weixin.qq.com/s/O0YGl4U14CNRX_t27khq8w" target="_blank" rel="noopener">哈希算法</a></p>
<p>参考资料<br><a href="https://mp.weixin.qq.com/s/O0YGl4U14CNRX_t27khq8w" target="_blank" rel="noopener">哈希算法</a><br><a href="https://github.com/sisterAn/JavaScript-Algorithms/issues/49" target="_blank" rel="noopener">前端瓶子君</a></p>
]]></content>
      <categories>
        <category>Basic knowledge</category>
      </categories>
      <tags>
        <tag>Data structure</tag>
      </tags>
  </entry>
  <entry>
    <title>webpack小结</title>
    <url>/2019/10/01/webpack%E5%B0%8F%E7%BB%93/</url>
    <content><![CDATA[<h1 id="基本篇"><a href="#基本篇" class="headerlink" title="基本篇"></a>基本篇</h1><h2 id="对webpack的看法"><a href="#对webpack的看法" class="headerlink" title="对webpack的看法"></a>对webpack的看法</h2><p>webpack是一个<strong>模块打包工具</strong>。可以用webpack<strong>管理</strong>我们的<strong>模块依赖</strong>，并编绎输出模块们所需的静态文件。它能够很好地管理、打包Web开发中所用到的HTML、Javascript、CSS以及各种静态文件（图片、字体等），让开发过程更加高效。对于不同类型的资源，webpack有对应的模块加载器。webpack模块打包器会分析模块间的依赖关系，最后 <strong>生成了优化且合并后的静态资源</strong></p>
<h2 id="entry-amp-amp-output"><a href="#entry-amp-amp-output" class="headerlink" title="entry &amp;&amp; output"></a>entry &amp;&amp; output</h2><h2 id="loader"><a href="#loader" class="headerlink" title="loader"></a>loader</h2><p>loader其实就是一个<strong>打包的方案</strong>，它知道对于某些特定的文件，webpack应该如何去打包。写在配置文件的 module 项中定义rules。</p>
<h3 id="常用的loader"><a href="#常用的loader" class="headerlink" title="常用的loader"></a>常用的loader</h3><ul>
<li>打包图片：file-loader，url-loader（会把图片打包成base64字符串直接放在打包好的js文件中。这样能使得html打开时候少点http请求，但是只适合于图片不要太大的情况，否则js文件太大也不好。所以可以加limit配置项）</li>
<li>打包样式文件：style-loader（把打包好的css挂载在html的<code>&lt;head&gt;</code>），css-loader（分析各css文件的关系，合并成一个css文件）。如果是sass或stylus文件，还需要借助sass-loader，node-sass和stylus-loader。自动添加产商前缀（提高css兼容性）的postcss-loader</li>
</ul>
<p>PS：use中的<strong>loader的执行顺序的是从下到上，从右到左，所以顺序不能弄错</strong></p>
<h2 id="plugins"><a href="#plugins" class="headerlink" title="plugins"></a>plugins</h2><p>plugin可以在webpack运行到某个时刻的时候，帮我们做一些事情（有点像生命周期函数）。写在配置文件的 plugins 项中</p>
<h3 id="常用的plugins"><a href="#常用的plugins" class="headerlink" title="常用的plugins"></a>常用的plugins</h3><ul>
<li>html-webpack-plugin。会在<strong>打包结束后</strong>，自动生成一个html文件，并把打包生成的js自动引入到这个html文件中</li>
<li>clean-webpack-plugin。不是官网写的插件。会在<strong>打包开始前</strong>，把原来的打包目录删除</li>
</ul>
<h2 id="SourceMap"><a href="#SourceMap" class="headerlink" title="SourceMap"></a>SourceMap</h2><p>当 webpack 打包源代码时，可能会很难追踪到错误和警告在源代码中的原始位置（只是显示在打包后的文件中的错误的位置），用SouceMap即可通过<strong>映射关系</strong>能知道该错误在源代码中的位置</p>
<h3 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h3><p>在development模式下默认SourceMap已经被配置进去了，若想关闭可用 devtool: none</p>
<h3 id="devtool配置项的几种写法"><a href="#devtool配置项的几种写法" class="headerlink" title="devtool配置项的几种写法"></a>devtool配置项的几种写法</h3><ul>
<li>cheap：使打包性能提升，sourcemap提示错误只提示到行不提示列</li>
<li>inline：把映射关系文件放在打包好的js文件中而不是生成一个单独的文件</li>
<li>module：使得cheap-sourcemap不仅管打包好的文件和源js的关系，还会管loader、第三方模块等的关系</li>
<li>eval：是最快的，会以eval的方式检查</li>
</ul>
<h3 id="devtool最配置"><a href="#devtool最配置" class="headerlink" title="devtool最配置"></a>devtool最配置</h3><p>在development下，用 cheap-module-eval-source-map<br>在production下，用 cheap-module-source-map </p>
<h2 id="webpack-dev-server"><a href="#webpack-dev-server" class="headerlink" title="webpack-dev-server"></a>webpack-dev-server</h2><p>为我们提供了一个简单的 web 服务器，并且能够实时重新加载；不会生成打包好的目录，是把打包好的文件存在内存中，提高效率<br>使用方法：安装，添加命令，写 devServer 项</p>
<h3 id="proxy转发"><a href="#proxy转发" class="headerlink" title="proxy转发"></a>proxy转发</h3><p>proxy配置项，其实用 Vue-cli 和 create-react-app 构建的项目的proxy功能本质就是这个配置项</p>
<h3 id="模块热替换"><a href="#模块热替换" class="headerlink" title="模块热替换"></a>模块热替换</h3><p>Hot Module Replacement，HMR。</p>
<h4 id="功能"><a href="#功能" class="headerlink" title="功能"></a>功能</h4><p>当源文件的发生改变的时候，理论上webpack-dev-server会帮我们打包更新，但是这个过程会刷新页面，使得页面上已有的样式/数据被刷新了。用HMR能实现在运行时更新各种模块，而无需进行完全刷新</p>
<h4 id="原理"><a href="#原理" class="headerlink" title="原理"></a><a href="https://www.webpackjs.com/concepts/hot-module-replacement/#%E8%BF%99%E4%B8%80%E5%88%87%E6%98%AF%E5%A6%82%E4%BD%95%E8%BF%90%E8%A1%8C%E7%9A%84-" target="_blank" rel="noopener">原理</a></h4><h4 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h4><ol>
<li>更新 webpack-dev-server 的配置：devSever配置项中写 hot: true</li>
<li>在plugins中实例化HMR插件</li>
</ol>
<p>对js文件的HMR功能需要手动多写一段代码</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="built_in">module</span>.hot) &#123; <span class="comment">// 注意</span></span><br><span class="line">  <span class="built_in">module</span>.hot.accept(<span class="string">'./number.js'</span>, () =&gt; &#123;</span><br><span class="line">    <span class="comment">// 一些操作</span></span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>css-loader 中内置了<code>if(module.hot)...</code>这样的代码，所以我们手写</p>
<h2 id="Babel"><a href="#Babel" class="headerlink" title="Babel"></a>Babel</h2><p>安装 babel-loader（其实只是打通webpack和babel的一个通道），@babel/core，@babel/preset-env（这个才是翻译器）@babel/polyfill（<strong>添加ES6中的新函数新变量</strong>，eg. Promise和Map）<br>具体配置方法见Babel官方文档</p>
<h3 id="babel-polyfill"><a href="#babel-polyfill" class="headerlink" title="@babel/polyfill"></a>@babel/polyfill</h3><ul>
<li><p>默认情况下polyfill会把所有新变量和函数都添加进打包好的文件，文件会变得较大，可写 useBuiltIns实现按需添加；可写target设定在目标浏览器，会自己判断是否添加这些新变量。（当options太长的时候可以直接把该对象放在根目录下的.babelrc ）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">options: &#123;</span><br><span class="line">          presets: [[<span class="string">"@babel/preset-env"</span>, &#123;</span><br><span class="line">            targets: &#123;</span><br><span class="line">              edge: <span class="string">"17"</span>,</span><br><span class="line">              firefox: <span class="string">"60"</span>,</span><br><span class="line">              chrome: <span class="string">"67"</span>,</span><br><span class="line">              safari: <span class="string">"11.1"</span>,</span><br><span class="line">            &#125;,</span><br><span class="line">            useBuiltIns: <span class="string">'usage'</span> &#125;]]</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure></li>
<li><p>此外，polyfill添加新变量是以全局变量的形式，会污染全局环境，当我们在打包UI组件库/类库的时候，就不能用这种配置方式。应安装@babel/plugin-transform-runtime，@babel/runtime 。配置方法见<a href="https://babeljs.io/docs/en/babel-plugin-transform-runtime" target="_blank" rel="noopener">文档</a></p>
</li>
</ul>
<h1 id="高级概念"><a href="#高级概念" class="headerlink" title="高级概念"></a>高级概念</h1><h2 id="Tree-Shaking"><a href="#Tree-Shaking" class="headerlink" title="Tree Shaking"></a><a href="https://jacleklm.github.io/2019/09/26/webpack%E4%B9%8BTree%20Shaking/" target="_blank" rel="noopener">Tree Shaking</a></h2><h2 id="Code-Splitting"><a href="#Code-Splitting" class="headerlink" title="Code Splitting"></a><a href="https://jacleklm.github.io/2019/09/26/webpack%E4%B9%8BCode%20Splitting/" target="_blank" rel="noopener">Code Splitting</a></h2><h3 id="CSS文件的代码分割"><a href="#CSS文件的代码分割" class="headerlink" title="CSS文件的代码分割"></a>CSS文件的代码分割</h3><ul>
<li>一般情况下，css会被打包在js文件中，如果想把css文件单独打包，需要用 MiniCssExtractPlugin (该插件不支持HMR，一般只在生产环境下使用，详细用法见笔记)</li>
<li>css 压缩用 optimize-css-assets-webpack-plugin</li>
</ul>
<p>Chunks：打包生成的每个文件都是一个chunk，打包生成很多个js文件，就有很多个chunk<br>output的chunkFilename配置项：如果一个打包后的文件会直接被html引用，那就是走filename，否则就是chunkFilename</p>
<h2 id="Lazy-loading"><a href="#Lazy-loading" class="headerlink" title="Lazy loading"></a><a href="https://jacleklm.github.io/2019/09/27/webpack%E4%B9%8BLazy%20Loading/" target="_blank" rel="noopener">Lazy loading</a></h2><h2 id="Shimming"><a href="#Shimming" class="headerlink" title="Shimming"></a>Shimming</h2><p><strong>应用场景</strong>：由于webpack具有模块化开发的理念，所以当index.js中有引入jQuery，而有个模块用了$变量却没import jQuery的时候，打包的时候这个模块的$还是会翻译不出，手动往每个用了$的模块去import却很麻烦。故可用shimming。<br><strong>解决方法</strong>：<br>在common中require(‘webpack’)，然后在plugin中写入new webpack.ProvidePlugin({  $: ‘jquery’  })<br>当发现模块中有 $ 时，就会自动给这个模块引入jQuery</p>
<h2 id="打包分析"><a href="#打包分析" class="headerlink" title="打包分析"></a>打包分析</h2><p>当我们用webpack打包之后，可以用打包分析的工具进行分析，看打包是否合理。看代码分割是否合理</p>
<h3 id="使用-1"><a href="#使用-1" class="headerlink" title="使用"></a>使用</h3><p>按<a href="https://github.com/webpack/analyse" target="_blank" rel="noopener">文档</a>提示加命令，会在打包过程中生成json文件描述打包过程，把这个文件传到<a href="http://webpack.github.com/analyse" target="_blank" rel="noopener">网站</a>即可；也可以用<a href="https://www.webpackjs.com/guides/code-splitting/#bundle-%E5%88%86%E6%9E%90-bundle-analysis-" target="_blank" rel="noopener">官方文档</a>建议的方法</p>
<h2 id="preloading-amp-prefetching"><a href="#preloading-amp-prefetching" class="headerlink" title="preloading &amp; prefetching"></a>preloading &amp; prefetching</h2><p>怎么看页面的代码使用率？ 用<a href="https://blog.csdn.net/Aero_K/article/details/82492634" target="_blank" rel="noopener">coverage</a><br>webpack希望我们<strong>多写异步加载的代码（多用懒加载）</strong>，能提高页面的代码使用率，eg，把具体代码写在一个js文件中，然后在index.js中写。（所以代码分割默认是async）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">document</span>.addEventListener(<span class="string">'click'</span>, () =&gt; &#123;</span><br><span class="line">  <span class="keyword">import</span>(<span class="string">'./click.js'</span>).then(<span class="function">(<span class="params">&#123;<span class="keyword">default</span>: func&#125;</span>) =&gt;</span> &#123;</span><br><span class="line">    func();</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>但是等到用户点击的时候再加载代码，加载时间可能也比较长，这时就可用<strong>preloading、prefetching</strong>：实现在页面加加载完之后，在接下来的空闲时间偷偷地把异步操作的代码下载好，这样真正用的时候只需要加载缓存即可（这个其实是prefetching；preloading是会和主代码一起加载）。<br>使用方法是在import语句中写入<a href="https://webpack.js.org/guides/code-splitting/" target="_blank" rel="noopener">魔法注释</a>即可 /* webpackPrefetch: true */ </p>
<h2 id="提升webpack打包速度的方法"><a href="#提升webpack打包速度的方法" class="headerlink" title="提升webpack打包速度的方法"></a><a href="https://jacleklm.github.io/2019/09/27/%E6%8F%90%E5%8D%87webpack%E6%89%93%E5%8C%85%E9%80%9F%E5%BA%A6%E7%9A%84%E6%96%B9%E6%B3%95/" target="_blank" rel="noopener">提升webpack打包速度的方法</a></h2><h1 id="原理篇"><a href="#原理篇" class="headerlink" title="原理篇"></a>原理篇</h1><h2 id="如何编写一个plugin"><a href="#如何编写一个plugin" class="headerlink" title="如何编写一个plugin"></a><a href="https://jacleklm.github.io/2019/09/29/%E5%A6%82%E4%BD%95%E7%BC%96%E5%86%99%E4%B8%80%E4%B8%AAplugin/" target="_blank" rel="noopener">如何编写一个plugin</a></h2><h2 id="如何编写一个loader"><a href="#如何编写一个loader" class="headerlink" title="如何编写一个loader"></a><a href="https://jacleklm.github.io/2019/09/28/%E5%A6%82%E4%BD%95%E7%BC%96%E5%86%99%E4%B8%80%E4%B8%AAloader/" target="_blank" rel="noopener">如何编写一个loader</a></h2><h2 id="Bundler源码编写"><a href="#Bundler源码编写" class="headerlink" title="Bundler源码编写"></a><a href="https://jacleklm.github.io/2019/09/30/Bundler%E6%BA%90%E7%A0%81%E7%BC%96%E5%86%99/" target="_blank" rel="noopener">Bundler源码编写</a></h2><h2 id="Vue-Cli3"><a href="#Vue-Cli3" class="headerlink" title="Vue Cli3"></a>Vue Cli3</h2><p>如果想改Vue项目中webpack配置，是直接建立vue.config.js然后按<a href="https://cli.vuejs.org/zh/config/" target="_blank" rel="noopener">文档</a>简单粗暴地去配置就可以了，对新手十分友好，vue-cli会在底层自己转变成webpack配置并实施</p>
<p>参考资料<br><a href="https://coding.imooc.com/class/316.html" target="_blank" rel="noopener">慕课网-从基础到实战 手把手带你掌握新版Webpack4.0</a><br>webpack官方文档</p>
]]></content>
      <categories>
        <category>Engineering</category>
      </categories>
      <tags>
        <tag>webpack</tag>
      </tags>
  </entry>
  <entry>
    <title>场景实现</title>
    <url>/2019/10/15/%E5%9C%BA%E6%99%AF%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<h1 id="HTML-amp-CSS"><a href="#HTML-amp-CSS" class="headerlink" title="HTML &amp; CSS"></a>HTML &amp; CSS</h1><h2 id="高度始终是宽度的-50-的盒子"><a href="#高度始终是宽度的-50-的盒子" class="headerlink" title="高度始终是宽度的 50%的盒子"></a>高度始终是宽度的 50%的盒子</h2><p>一个 div 垂直居中；其距离屏幕左右两边各 10px；其高度始终是宽度的 50%<br>div 中有文本’A’；其 font—size:20px；文本水平垂直居中</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">width: calc(<span class="number">100</span>vw - <span class="number">20</span>px);</span><br><span class="line">height: calc(<span class="number">50</span>vw - <span class="number">10</span>px);</span><br></pre></td></tr></table></figure>

<h2 id="上中下的-flex-布局，上下定高中间自适应"><a href="#上中下的-flex-布局，上下定高中间自适应" class="headerlink" title="上中下的 flex 布局，上下定高中间自适应"></a>上中下的 flex 布局，上下定高中间自适应</h2><ul>
<li>父盒子写 display:flex; flex-direction: colum; 子盒子写 flex: 1</li>
<li>父盒子写 height: calc(100vh - xx px) 或直接 100vh</li>
</ul>
<h2 id="实现一个图片容器"><a href="#实现一个图片容器" class="headerlink" title="实现一个图片容器"></a>实现一个图片容器</h2><p>要求：</p>
<ul>
<li>图片宽度大于等于容器宽度时，要按容器宽度等比缩放图片，并垂直居中显示</li>
<li>图片高度大于等于容器高度时，要按容器高度等比缩放图片，并水平居中显示</li>
<li>图片宽高均小于等于容器宽高时，要按图片宽高显示并水平和垂直居中</li>
</ul>
<h3 id="不完美的方法一"><a href="#不完美的方法一" class="headerlink" title="不完美的方法一"></a>不完美的方法一</h3><p>利用 background 可以实现前两个要求，但是无法实现第三个要求</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.div &#123;</span><br><span class="line">  height: 400px;</span><br><span class="line">  width: 700px;</span><br><span class="line">  border: 1px solid black;</span><br><span class="line">  background-repeat: no-repeat;</span><br><span class="line">  background-size: contain;</span><br><span class="line">  background-position: center;</span><br><span class="line">  background-image: url(&apos;https://cdn.segmentfault.com/v-5e7dd7fe/global/img/user-64.png&apos;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="完美实现的方法二"><a href="#完美实现的方法二" class="headerlink" title="完美实现的方法二"></a>完美实现的方法二</h3><p>flex布局实现垂直水平居中，图片设置 max-width 和 max-height 为 100% …好容易…</p>
<h2 id="CSS-实现九宫格布局"><a href="#CSS-实现九宫格布局" class="headerlink" title="CSS 实现九宫格布局"></a>CSS 实现九宫格布局</h2><p>要求：骰子的一面。hover 的时候数字的边框变红</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"wrapper"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"num"</span>&gt;</span>1<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"num"</span>&gt;</span>2<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"num"</span>&gt;</span>3<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"num"</span>&gt;</span>4<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"num"</span>&gt;</span>5<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"num"</span>&gt;</span>6<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"num"</span>&gt;</span>7<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"num"</span>&gt;</span>8<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"num"</span>&gt;</span>9<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="css">  <span class="selector-id">#wrapper</span> &#123;</span></span><br><span class="line">    display: grid;</span><br><span class="line">    grid-template-columns: 50px 50px 50px;</span><br><span class="line">    grid-template-rows: 50px 50px 50px;</span><br><span class="line">  &#125;</span><br><span class="line"><span class="css">  <span class="selector-class">.num</span> &#123;</span></span><br><span class="line">    border: blue solid 5px;</span><br><span class="line">    display: flex;</span><br><span class="line">    justify-content: center;</span><br><span class="line">    align-items: center;</span><br><span class="line">    cursor: pointer;</span><br><span class="line"><span class="css">    <span class="comment">/* border不重叠 */</span></span></span><br><span class="line">    margin-left: -5px;</span><br><span class="line">    margin-top: -5px;</span><br><span class="line">  &#125;</span><br><span class="line"><span class="css">  <span class="selector-class">.num</span><span class="selector-pseudo">:hover</span> &#123;</span></span><br><span class="line">    border-color: red;</span><br><span class="line"><span class="css">    <span class="comment">/* 由于设置了负边距，得相对定位才能在hover时显示完整边框 */</span></span></span><br><span class="line">    position: relative;</span><br><span class="line">  &#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="做一个-todolist"><a href="#做一个-todolist" class="headerlink" title="做一个 todolist"></a>做一个 todolist</h2><p>要求：回车键后能把内容添加到 list 中；list 中点 x 能删除</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">h1</span>&gt;</span>TodoList<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">""</span> <span class="attr">id</span>=<span class="string">"input"</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"content"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">"list"</span>&gt;</span><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">    <span class="keyword">let</span> input = <span class="built_in">document</span>.getElementById(<span class="string">"input"</span>);</span></span><br><span class="line"><span class="javascript">    <span class="keyword">let</span> ul = <span class="built_in">document</span>.getElementById(<span class="string">"list"</span>);</span></span><br><span class="line"><span class="actionscript">    input.addEventListener(<span class="string">"keydown"</span>, e =&gt; &#123;</span></span><br><span class="line"><span class="actionscript">      <span class="comment">// keyCode 一定得区分大小写。添加部分</span></span></span><br><span class="line"><span class="actionscript">      <span class="keyword">if</span> (e.keyCode === <span class="number">13</span> &amp;&amp; input.value !== <span class="string">""</span>) &#123;</span></span><br><span class="line"><span class="javascript">        <span class="keyword">let</span> li = <span class="built_in">document</span>.createElement(<span class="string">"li"</span>);</span></span><br><span class="line">        ul.appendChild(li);</span><br><span class="line"><span class="handlebars"><span class="xml">        li.innerHTML = `<span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"item"</span>&gt;</span>$&#123;input.value&#125;<span class="tag">&lt;/<span class="name">span</span>&gt;</span><span class="tag">&lt;<span class="name">span</span> <span class="attr">id</span>=<span class="string">"x"</span>&gt;</span>x<span class="tag">&lt;/<span class="name">span</span>&gt;</span>`;</span></span></span><br><span class="line"><span class="actionscript">        input.value = <span class="string">""</span>;</span></span><br><span class="line"><span class="actionscript">        <span class="comment">// 移除部分</span></span></span><br><span class="line"><span class="javascript">        <span class="keyword">let</span> x = li.children[<span class="number">1</span>];</span></span><br><span class="line"><span class="actionscript">        x.addEventListener(<span class="string">"click"</span>, e =&gt; &#123;</span></span><br><span class="line">          li.remove();</span><br><span class="line">        &#125;);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">  <span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="上传文件不用刷新页面的-JS-代码"><a href="#上传文件不用刷新页面的-JS-代码" class="headerlink" title="上传文件不用刷新页面的 JS 代码"></a>上传文件不用刷新页面的 JS 代码</h2><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"file"</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">button</span> <span class="attr">id</span>=<span class="string">"btn"</span>&gt;</span>upload<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">    <span class="keyword">let</span> btn = <span class="built_in">document</span>.getElementById(<span class="string">"btn"</span>);</span></span><br><span class="line"><span class="javascript">    btn.onclick = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span></span><br><span class="line"><span class="javascript">      <span class="keyword">let</span> file = <span class="built_in">document</span>.querySelector(<span class="string">"input[type='file']"</span>);</span></span><br><span class="line"><span class="actionscript">      <span class="comment">// 用formdata</span></span></span><br><span class="line"><span class="javascript">      <span class="keyword">let</span> formdata = <span class="keyword">new</span> FormData();</span></span><br><span class="line"><span class="actionscript">      formdata.append(<span class="string">"flie"</span>, file.files[<span class="number">0</span>]);</span></span><br><span class="line"></span><br><span class="line"><span class="actionscript">      fetch(<span class="string">"/"</span>, &#123;</span></span><br><span class="line"><span class="actionscript">        method: <span class="string">"POST"</span>,</span></span><br><span class="line">        body: formdata</span><br><span class="line">      &#125;)</span><br><span class="line"><span class="javascript">        .then(<span class="function"><span class="params">response</span> =&gt;</span> response.json())</span></span><br><span class="line"><span class="javascript">        .then(<span class="function"><span class="params">data</span> =&gt;</span> &#123;</span></span><br><span class="line"><span class="javascript">          <span class="built_in">console</span>.log(<span class="string">"Success:"</span>, data);</span></span><br><span class="line">        &#125;)</span><br><span class="line"><span class="javascript">        .catch(<span class="function"><span class="params">err</span> =&gt;</span> &#123;</span></span><br><span class="line"><span class="javascript">          <span class="built_in">console</span>.log(<span class="string">"Error:"</span>, err);</span></span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;;</span><br><span class="line">  <span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="上传文件读取文件名、大小、内容"><a href="#上传文件读取文件名、大小、内容" class="headerlink" title="上传文件读取文件名、大小、内容"></a>上传文件读取文件名、大小、内容</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> file = <span class="built_in">document</span>.querySelector(<span class="string">"input"</span>);</span><br><span class="line"><span class="comment">// 选择文件后触发</span></span><br><span class="line">file.onchange = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">document</span>.getElementById(<span class="string">"name"</span>).innerHTML = file.files[<span class="number">0</span>].name;</span><br><span class="line">    <span class="built_in">document</span>.getElementById(<span class="string">"size"</span>).innerHTML = file.files[<span class="number">0</span>].size;</span><br><span class="line">    <span class="built_in">document</span>.getElementById(<span class="string">"type"</span>).innerHTML = file.files[<span class="number">0</span>].type;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> reader = <span class="keyword">new</span> FileReader();</span><br><span class="line">    reader.readAsDataURL(file.files[<span class="number">0</span>]);</span><br><span class="line">    <span class="comment">// 读取的过程就相当于 加载过程，读取完会触发onload事件</span></span><br><span class="line">    reader.onload = <span class="function">(<span class="params">e</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="comment">// 读取结果 base64位数据  表示图片</span></span><br><span class="line">        <span class="built_in">console</span>.log(e.target.result);</span><br><span class="line">        <span class="built_in">document</span>.querySelector(<span class="string">"#img"</span>).src = e.target.result;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="实现鼠标拖拽-DOM"><a href="#实现鼠标拖拽-DOM" class="headerlink" title="实现鼠标拖拽 DOM"></a>实现鼠标拖拽 DOM</h2><p>见另一篇博客</p>
<h2 id="实现轮播"><a href="#实现轮播" class="headerlink" title="实现轮播"></a>实现轮播</h2><p>这篇<a href="https://www.cnblogs.com/linqb/p/9375197.html" target="_blank" rel="noopener">博文</a>总结的不错<br>需求：</p>
<ol>
<li>鼠标移入轮播图时左右两边显示上一页下一页按钮，移出时隐藏</li>
<li>鼠标点击箭头，图片发生轮播</li>
<li>点击号码，切换到指定图片</li>
<li>鼠标移出，图片每隔一定时间自动轮播</li>
<li>当图片轮播到最后或最前一张的时候，图片无缝循环切换</li>
</ol>
<p>思路：</p>
<ul>
<li>DOM：div.screen&gt;ul&gt;li*5&gt;img 放图片，ol&gt;li 显示下标，div&gt;span 显示箭头</li>
<li>样式：让 li 标签 float 实现并排效果，div.screen 要设好宽高并 overflow:hidden 只显示出一张图，轮播的原理就是设置 ul.style.left 的值<code>ul.style.left = -index * screen.offsetWidth + &#39;px&#39;</code> （变量 index 记录当前需要展示的图片的索引, screen 是外层那个 div）</li>
<li>逻辑：<ul>
<li>一个全局变量 index 记录当前需要展示的图片的索引</li>
<li>无限滚动的实现：复制第一张图放到原本图片集的最后。然后当是最后一张还向右滚动的话，就立刻改变 ul 的位置到第一张（index=0，ul.style.left = ….）；同理第一向左滚动</li>
<li>鼠标移入移出事件：鼠标移入，显示左右切换按钮，清除 setInterval 自动滚动；移出时隐藏按钮，新建 setInterval 自动滚动</li>
<li>点击按钮</li>
<li>封装一个滚动动画的函数，一个向右滚动函数 scroll，一个在滚动时能设置号码牌样式的函数 indexShow</li>
</ul>
</li>
</ul>
<h2 id="写一个动画让一个div在水平的两个点之间来回运动"><a href="#写一个动画让一个div在水平的两个点之间来回运动" class="headerlink" title="写一个动画让一个div在水平的两个点之间来回运动"></a>写一个动画让一个div在水平的两个点之间来回运动</h2><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">div</span> &#123;</span><br><span class="line">  <span class="attribute">animation</span>: myanimation <span class="number">3s</span> infinite;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">@keyframes</span> myanimation &#123;</span><br><span class="line">  0% &#123;<span class="attribute">transform</span>: <span class="built_in">translate</span>(<span class="number">0px</span>, <span class="number">0px</span>);&#125;</span><br><span class="line">  50% &#123;<span class="attribute">transform</span>: <span class="built_in">translate</span>(<span class="number">800px</span>, <span class="number">0px</span>);&#125;</span><br><span class="line">  100% &#123;<span class="attribute">transform</span>: <span class="built_in">translate</span>(<span class="number">0px</span>, <span class="number">0px</span>);&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="实现一个前端缓存模块"><a href="#实现一个前端缓存模块" class="headerlink" title="实现一个前端缓存模块"></a>实现一个前端缓存模块</h2><p>实现一个前端缓存模块，主要用于缓存 xhr 返回的结果，避免多余的网络请求浪费，要求：</p>
<ul>
<li>生命周期为一次页面打开</li>
<li>如果有相同的请求同时并行发起，要求其中一个能挂起并且等待另外一个请求返回并读取该缓存</li>
</ul>
<p>见<a href="https://jacleklm.github.io/2019/10/31/%E5%89%8D%E5%90%8E%E7%AB%AF%E4%BA%A4%E4%BA%92&%E8%B7%A8%E5%9F%9F/" target="_blank" rel="noopener">前后端交互&amp;跨域</a></p>
<h1 id="JS"><a href="#JS" class="headerlink" title="JS"></a>JS</h1><h2 id="定时器正确输出"><a href="#定时器正确输出" class="headerlink" title="定时器正确输出"></a>定时器正确输出</h2><p>改正代码 输出 0123401234</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">this</span>.i = i;</span><br><span class="line">    setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(i);</span><br><span class="line">    &#125;, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.i);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">a();</span><br></pre></td></tr></table></figure>

<p>解答：上面这种方式，i 是全局变量；for 循环产生 5 个定时器，拿到全局 i 的时候已经 i=5 了（外层这个 function 和里面的 this 是来干扰的，去掉也无妨）。解决方法就是让变量 i 有块级作用域：let；闭包。<br>可改成</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">this</span>.i = i;</span><br><span class="line">    setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(i);</span><br><span class="line">    &#125;, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="keyword">this</span>.i);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">a();</span><br></pre></td></tr></table></figure>

<p>也可以是</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">a</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">    (<span class="function"><span class="keyword">function</span>(<span class="params">i</span>) </span>&#123;</span><br><span class="line">      <span class="keyword">this</span>.i = i;</span><br><span class="line">      setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="built_in">console</span>.log(i);</span><br><span class="line">      &#125;, <span class="number">0</span>);</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="keyword">this</span>.i);</span><br><span class="line">    &#125;)(i);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">a();</span><br></pre></td></tr></table></figure>

<h1 id="编程题"><a href="#编程题" class="headerlink" title="编程题"></a>编程题</h1><p>readline()用于行的读取，最好是在一开始就读取完<br>print()用于输出结果</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>Interview</tag>
      </tags>
  </entry>
  <entry>
    <title>如何编写一个loader</title>
    <url>/2019/09/28/%E5%A6%82%E4%BD%95%E7%BC%96%E5%86%99%E4%B8%80%E4%B8%AAloader/</url>
    <content><![CDATA[<h3 id="什么是loader"><a href="#什么是loader" class="headerlink" title="什么是loader"></a>什么是loader</h3><p>loader其实是一种打包方案，loader可以将所有类型的文件转换为 webpack 能够处理的有效模块，然后你就可以利用 webpack 的打包能力，对它们进行处理。</p>
<h3 id="自己编写的loader的使用"><a href="#自己编写的loader的使用" class="headerlink" title="自己编写的loader的使用"></a>自己编写的loader的使用</h3><p>同普通loader一样，在webpack.config.js的<code>module</code>中写新的<code>rule</code>，还要在webpack.config.js新写一个配置项<code>resolveLoader</code>（代码如下所示），意思是按名称现在<code>node_modules</code>中找loader，找不到再去 ./loader 文件夹中找（自己写的loader放在该文件夹中，为replaceLoader.js）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">resolveLoader: &#123;</span><br><span class="line">  modules: [<span class="string">'node_modules'</span>, <span class="string">'./loaders'</span>]</span><br><span class="line">&#125;,</span><br><span class="line"><span class="built_in">module</span>: &#123;</span><br><span class="line">  rules: [&#123;</span><br><span class="line">    test: <span class="regexp">/\.js$/</span>,</span><br><span class="line">    use: &#123;</span><br><span class="line">      loader: <span class="string">'replaceLoader'</span>,</span><br><span class="line">      options: &#123;</span><br><span class="line">        name: <span class="string">'Jacleklm'</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="对loader传递数据"><a href="#对loader传递数据" class="headerlink" title="对loader传递数据"></a>对loader传递数据</h3><p>如上面的代码中，option即可用来传递参数，把想传递的参数写成option对象的属性。然后replaceLoader.js中用<code>this.query.属性</code>即可得到这个参数；也可以使用loader-util中的getOption方法来得到这个option对象。</p>
<h3 id="loader的编写"><a href="#loader的编写" class="headerlink" title="loader的编写"></a>loader的编写</h3><p>在这里，我们假设入口文件index.js中的代码内容仅为<code>console.log(&#39;hello world&#39;);</code>，我们想编写一个loader对这个文件进行处理，实现把<code>world</code>改成<code>Jacleklm</code>。<br>loader其实是一个函数。在编写的时候，我们可以把loader放在一个单独的文件中，然后导出即可使用。注意在这里<code>module.exports = function() { }</code>的function不能写成箭头函数，因为在loader的编写中我们会常用到this，这里用箭头函数可能会造成this的使用出现麻烦。最终代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> loaderUtils = <span class="built_in">require</span>(<span class="string">'loader-utils'</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">module</span>.exports = <span class="function"><span class="keyword">function</span> (<span class="params">source</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> options = loaderUtils.getOptions(<span class="keyword">this</span>)</span><br><span class="line">  <span class="keyword">return</span> source.replace(<span class="string">'world'</span>, options.name)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Engineering</category>
      </categories>
      <tags>
        <tag>webpack</tag>
      </tags>
  </entry>
  <entry>
    <title>实现鼠标拖拽DOM + 页面视图总结</title>
    <url>/2019/11/17/%E5%AE%9E%E7%8E%B0%E9%BC%A0%E6%A0%87%E6%8B%96%E6%8B%BDDOM/</url>
    <content><![CDATA[<h1 id="知识储备"><a href="#知识储备" class="headerlink" title="知识储备"></a>知识储备</h1><h2 id="常用的鼠标事件"><a href="#常用的鼠标事件" class="headerlink" title="常用的鼠标事件"></a>常用的鼠标事件</h2><ul>
<li>click</li>
<li>dbclick：双击</li>
<li>mouseenter：当鼠标指针<strong>首次</strong>移动到元素上时触发。不冒泡</li>
<li>mouseleave：当鼠标指针移出元素时触发。不冒泡</li>
<li><strong>mousedown</strong>：当元素上按下鼠标按钮时候触发</li>
<li><strong>mouseup</strong>：当元素上释放鼠标按钮时触发</li>
<li><strong>mousemove</strong>：当鼠标指针在元素内部移动时<strong>重复地</strong>触发</li>
<li><strong>mouseover</strong>：当鼠标指针移动到元素上时触发</li>
<li><strong>mouseout</strong>：当鼠标指针移出元素时触发</li>
</ul>
<h2 id="事件对象"><a href="#事件对象" class="headerlink" title="事件对象"></a>事件对象</h2><p>当触发某个事件时，会产生一个事件对象，这个对象包含着所有与事件有关的信息。会作为参数传递给函数，IE8以及之前的版本event对象是作为window对象的属性存在的。故有了以下常见的写法：<code>function (e) { e = e || window.event; ... }</code> 。事件对象的属性如下：<br><img src="/img/%E9%BC%A0%E6%A0%87/W3C.png"><br><img src="/img/%E9%BC%A0%E6%A0%87/IE.png"><br>应用：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">stopPro</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> e = e || <span class="built_in">window</span>.event;</span><br><span class="line">  <span class="comment">// 得到事件目标DOM对象</span></span><br><span class="line">  <span class="keyword">let</span> dom = e.target || e.srcElement;</span><br><span class="line">  <span class="comment">// 阻止冒泡</span></span><br><span class="line">  <span class="built_in">window</span>.event ? e.cancelBubble = <span class="literal">true</span> : e.stopPropagation();</span><br><span class="line">  <span class="comment">// 取消默认事件</span></span><br><span class="line">  <span class="built_in">window</span>.event ? e.returnValue = <span class="literal">false</span> : e.preventDefault();</span><br><span class="line">  <span class="comment">// 阻止默认行为(但会继续冒泡)</span></span><br><span class="line">  <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="坐标"><a href="#坐标" class="headerlink" title="坐标"></a>坐标</h2><h3 id="事件对象-1"><a href="#事件对象-1" class="headerlink" title="事件对象"></a>事件对象</h3><ul>
<li>event对象.screenX / screenY ：相对电脑屏幕的坐标，距离左边框和上边框的距离</li>
<li>event对象.clientX / clientY ：相对浏览器可视区的坐标，同理</li>
</ul>
<h3 id="DOM节点"><a href="#DOM节点" class="headerlink" title="DOM节点"></a>DOM节点</h3><h4 id="DOM节点-offsetLeft-offsetTop"><a href="#DOM节点-offsetLeft-offsetTop" class="headerlink" title="DOM节点.offsetLeft / offsetTop"></a>DOM节点.offsetLeft / offsetTop</h4><p>可以获得元素 距离上方或外层元素 的位置，<strong>本质和 style.top 是一样的</strong>。二者区别：offsetTop 返回的是数字，而 style.top 返回的是字符串，除了数字外还带有单位（px）；offsetTop 只读，而 style.top 可读写。同理offsetWidth 与 style.width、offsetHeight 与 style.height</p>
<h4 id="DOM节点-clientHeight-clientWidth"><a href="#DOM节点-clientHeight-clientWidth" class="headerlink" title="DOM节点.clientHeight / clientWidth"></a>DOM节点.clientHeight / clientWidth</h4><p>浏览器中可视区高度。width + padding（不含border）</p>
<h4 id="DOM节点-scrollHeight-scrollWidth"><a href="#DOM节点-scrollHeight-scrollWidth" class="headerlink" title="DOM节点.scrollHeight / scrollWidth"></a>DOM节点.scrollHeight / scrollWidth</h4><p>元素内容真实的宽度，内容不超出盒子高度时为盒子的clientWidth</p>
<h3 id="Window视图"><a href="#Window视图" class="headerlink" title="Window视图"></a>Window视图</h3><p>window.innerWidth / innerHeight：浏览器窗口可视区宽度（不包括浏览器控制台、菜单栏、工具栏） （低版本IE浏览器&lt; IE9不支持）</p>
<h1 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h1><h2 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h2><p>拖拽无非是鼠标按下点击元素；鼠标移动，元素移动；鼠标抬起，元素停止不动<br>所以这里我们需要绑定三个事件：mousedown、mousemove、mouseup</p>
<h2 id="实现-1"><a href="#实现-1" class="headerlink" title="实现"></a>实现</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">window</span>.onload = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="comment">//【drag元素必须是是绝对定位】</span></span><br><span class="line">  <span class="keyword">var</span> drag = <span class="built_in">document</span>.getElementById(<span class="string">'box'</span>);</span><br><span class="line">  drag.onmousedown = <span class="function"><span class="keyword">function</span> (<span class="params">e</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// IE8以及之前的版本event对象是作为window对象的属性存在的。故有了以下常见的写法：</span></span><br><span class="line">    e = e || <span class="built_in">window</span>.event;</span><br><span class="line">    <span class="comment">//鼠标点与拖拽元素边框的距离。由于DOM.offsetLeft 和DOM.style.left本质是一样的，所以可以这么写</span></span><br><span class="line">    <span class="keyword">let</span> diffX = e.clientX - drag.offsetLeft;</span><br><span class="line">    <span class="keyword">let</span> diffY = e.clientY - drag.offsetTop;</span><br><span class="line">    drag.onmousemove = <span class="function"><span class="keyword">function</span> (<span class="params">e</span>) </span>&#123;</span><br><span class="line">      e = e || <span class="built_in">window</span>.event;</span><br><span class="line">      <span class="keyword">let</span> left = e.clientX - diffX;</span><br><span class="line">      <span class="keyword">let</span> top = e.clientY - diffY;</span><br><span class="line">      <span class="comment">//避免拖出可视区外</span></span><br><span class="line">      <span class="keyword">if</span> (left &lt; <span class="number">0</span>) &#123; left = <span class="number">0</span>; &#125;</span><br><span class="line">      <span class="keyword">else</span> <span class="keyword">if</span> (left &gt; <span class="built_in">window</span>.innerWidth - drag.offsetWidth) &#123;</span><br><span class="line">        <span class="comment">//超出了就放在innerWidth的位置，注意这段代码不要写错</span></span><br><span class="line">        left = <span class="built_in">window</span>.innerWidth - drag.offsetWidth;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (top &lt; <span class="number">0</span>) top = <span class="number">0</span>;</span><br><span class="line">      <span class="keyword">else</span> <span class="keyword">if</span> (top &gt; <span class="built_in">window</span>.innerHeight - drag.offsetHeight) &#123;</span><br><span class="line">        top = <span class="built_in">window</span>, innerHeight - drag.offsetHeight;</span><br><span class="line">      &#125;</span><br><span class="line">      drag.style.left = left + <span class="string">'px'</span>;</span><br><span class="line">      drag.style.top = top + <span class="string">'px'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    drag.onmouseup = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">      drag.onmousemove = <span class="literal">null</span>;</span><br><span class="line">      drag.onmousedown = <span class="literal">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>如何编写一个plugin</title>
    <url>/2019/09/29/%E5%A6%82%E4%BD%95%E7%BC%96%E5%86%99%E4%B8%80%E4%B8%AAplugin/</url>
    <content><![CDATA[<h3 id="什么是plugin"><a href="#什么是plugin" class="headerlink" title="什么是plugin"></a>什么是plugin</h3><p>plugin能帮助我们在webpack打包生成文件的某个事件段帮我们做一些事，类似于Vue中的生命周期函数。例如html-webpack-plugin能帮我们在打包结束生成html文件，并把打包生成的脚本文件插入html中。</p>
<h3 id="plugin的编写"><a href="#plugin的编写" class="headerlink" title="plugin的编写"></a>plugin的编写</h3><p>假如我们想写一个plugin，这个plguin能帮我们在打包生成文件之后再生成一个.txt文件，里面写我们的版权信息。实现代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CopyrightWebpackPlugin</span> </span>&#123;</span><br><span class="line">  <span class="comment">//  constructor部分仅为演示用的代码</span></span><br><span class="line">  <span class="keyword">constructor</span>(options) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(options.name)</span><br><span class="line">  &#125;</span><br><span class="line">  apply(compiler) &#123;</span><br><span class="line">    compiler.hooks.emit.tapAsync(<span class="string">'CopyrightWebpackPlugin'</span>, (compilation, cb) =&gt; &#123; <span class="comment">// 在emit这个钩子的时期，执行里面的函数。异步的钩子就是用tapAsync</span></span><br><span class="line">      compilation.assets[<span class="string">'copyright.txt'</span>]= &#123; <span class="comment">// assets属性是打包生成的内容(对象)，往这个对象写新的属性即能新增文件</span></span><br><span class="line">        source: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="comment">//  文件的内容</span></span><br><span class="line">          <span class="keyword">return</span> <span class="string">'copyright by Jacleklm'</span></span><br><span class="line">        &#125;,</span><br><span class="line">        size: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="comment">//  文件的长度</span></span><br><span class="line">          <span class="keyword">return</span> <span class="number">21</span></span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      cb()</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">module</span>.exports = CopyrightWebpackPlugin</span><br></pre></td></tr></table></figure>

<h3 id="代码解读"><a href="#代码解读" class="headerlink" title="代码解读"></a>代码解读</h3><h4 id="插件是一个类"><a href="#插件是一个类" class="headerlink" title="插件是一个类"></a>插件是一个类</h4><h4 id="向插件传递参数：通过在webpack配置的new-xxx里写对象属性（如下面的代码），而插件的constructor的参数则能接收这个对象（如上面的代码的constructor部分）。"><a href="#向插件传递参数：通过在webpack配置的new-xxx里写对象属性（如下面的代码），而插件的constructor的参数则能接收这个对象（如上面的代码的constructor部分）。" class="headerlink" title="向插件传递参数：通过在webpack配置的new xxx里写对象属性（如下面的代码），而插件的constructor的参数则能接收这个对象（如上面的代码的constructor部分）。"></a>向插件传递参数：通过在webpack配置的new xxx里写对象属性（如下面的代码），而插件的constructor的参数则能接收这个对象（如上面的代码的constructor部分）。</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">plugins: [</span><br><span class="line">    <span class="keyword">new</span> CopyrightWebpackPlugin(&#123;</span><br><span class="line">      name: <span class="string">'Jacleklm'</span></span><br><span class="line">    &#125;)</span><br><span class="line">  ],</span><br></pre></td></tr></table></figure>

<h4 id="compiler"><a href="#compiler" class="headerlink" title="compiler"></a>compiler</h4><p>compiler对象是webpack的实例，compiler有很多<a href="https://www.webpackjs.com/api/compiler-hooks/#emit" target="_blank" rel="noopener">hooks（钩子）</a>，挑选合适的钩子（eg. <code>emit</code>）写要执行的操作即可。</p>
<h4 id="compilation"><a href="#compilation" class="headerlink" title="compilation"></a>compilation</h4><p>compilation里存放的是这一次打包的所有内容，eg. 打包生成的内容是放在<code>compilation.assets</code>中。</p>
]]></content>
      <categories>
        <category>Engineering</category>
      </categories>
      <tags>
        <tag>webpack</tag>
      </tags>
  </entry>
  <entry>
    <title>《深入浅出Node.js》读书笔记</title>
    <url>/2020/07/15/%E3%80%8A%E6%B7%B1%E5%85%A5%E6%B5%85%E5%87%BANode.js%E3%80%8B%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p>感受：这本书更偏向于讲 NodeJS 的大纲和原理。所以本笔记只讲原理和case，对于API不熟的请翻<a href="http://nodejs.cn/api/" target="_blank" rel="noopener">文档</a>，或看<a href="https://github.com/chyingp/nodejs-learning-guide/blob/master/%E6%A8%A1%E5%9D%97/fs.md" target="_blank" rel="noopener">小卡的笔记</a>, 或看<a href="https://blog.poetries.top/node-learning-notes/" target="_blank" rel="noopener">poetries的笔记</a> </p>
<h1 id="第一章-Node-简介"><a href="#第一章-Node-简介" class="headerlink" title="第一章 Node 简介"></a>第一章 Node 简介</h1><h2 id="Chrome-浏览器的组成和-Node-的组件组成"><a href="#Chrome-浏览器的组成和-Node-的组件组成" class="headerlink" title="Chrome 浏览器的组成和 Node 的组件组成"></a>Chrome 浏览器的组成和 Node 的组件组成</h2><p><img src="/img/Node/Chrome%E6%B5%8F%E8%A7%88%E5%99%A8%E7%9A%84%E7%BB%84%E6%88%90%E5%92%8CNode%E7%9A%84%E7%BB%84%E4%BB%B6%E7%BB%84%E6%88%90.png"></p>
<h2 id="Node-的特点"><a href="#Node-的特点" class="headerlink" title="Node 的特点"></a>Node 的特点</h2><ul>
<li>异步 I/O。eg. Ajax, fs.readFile 等，Node 中绝大数的操作都以异步的方式进行调用</li>
<li>事件与回调函数。将前端中广泛成熟点时间机制引入后端，配合 IO 使用，eg. 监听 request 事件，然后执行回调函数</li>
<li>单线程。保留了 JS 在浏览器中单线程的特点<ul>
<li>优点：不用在意状态的同步问题，没有死锁的存在，也没有上下文交互的性能开销</li>
<li>弱点：<ul>
<li>无法利用多核 CPU</li>
<li>错误会引起整个应用退出，代码健壮性值得考验</li>
<li>大量计算占用 CPU 无法继续调用异步 I/O。eg. 长时间的 CPU 占用会导致后续的异步 IO 发不出调用。浏览器中类似的问题被 Web Worker 解决，Node 也采用类似的思路：子进程 child_process</li>
</ul>
</li>
<li>子进程的出现可以解决单线程在健壮性和无法利用多核 CPU 的问题。通过将计算分发到各个子进程，可以将大量斤算分解掉，再通过进程的事件消息来传递结果，可很好地保持应用模型的简单和依赖；通过 Master-Worker 的管理方式，也可以很好地管理各个工作的进程，以达到更高的健壮性</li>
</ul>
</li>
<li>跨平台。Node 基于 libuv 实现跨平台，可以在 Linux，Window 使用。它在操作系统和 Node 上层模块系统之间构建了一层平台层架构，即 libuv。libux 也是许多系统实现跨平台的基础组件</li>
</ul>
<h2 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h2><ul>
<li>I/O 密集</li>
<li>CPU 密集是否不合适？Node 没有提供多线程用于计算支持，但是仍有两个方式充分利用 CPU:<ul>
<li>Node 可以通过编写 C++扩展的方式提高 CPU 利用，将一些 V8 不能做到性能极致的地方用 C++来实现</li>
<li>用子进程的方式，将一部分进程用来计算，然后用进程通信传递结果，将计算和 I/O 分离</li>
</ul>
</li>
</ul>
<h2 id="应用好处"><a href="#应用好处" class="headerlink" title="应用好处"></a>应用好处</h2><p>用 Node 做 Web 开发，前端工程师在 HTTP 协议栈的两端能高效灵活开发，避免了 Java 的繁琐；另一方面，又利用 Java 作为后端接口和中间件，使其有良好的稳定性。两者取长补短</p>
<h1 id="第二章-模块机制"><a href="#第二章-模块机制" class="headerlink" title="第二章 模块机制"></a>第二章 模块机制</h1><h2 id="CommonJS-规范"><a href="#CommonJS-规范" class="headerlink" title="CommonJS 规范"></a>CommonJS 规范</h2><p>解决 JS 弱结构性的问题，愿景：希望 JS 能在任何地方运行<br>eg.</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> math = <span class="built_in">require</span>(‘math’)</span><br><span class="line">…</span><br><span class="line">exports.add = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; xxx &#125;</span><br></pre></td></tr></table></figure>

<h2 id="Node-模块的实现"><a href="#Node-模块的实现" class="headerlink" title="Node 模块的实现"></a>Node 模块的实现</h2><h3 id="引入模块的实现步骤："><a href="#引入模块的实现步骤：" class="headerlink" title="引入模块的实现步骤："></a>引入模块的实现步骤：</h3><ul>
<li>路径分析</li>
<li>文件定位</li>
<li>编译执行</li>
</ul>
<blockquote>
<p>Node 提供的模块称为核心模块；用户编写的模块称为文件模块。都会优先从缓存加载 Module._cache；缓存的是 Node 编译和执行后的对象</p>
</blockquote>
<h3 id="模块编译"><a href="#模块编译" class="headerlink" title="模块编译"></a>模块编译</h3><blockquote>
<p>核心模块部分在 Node 源代码的变异过程中，编译进了二进制执行文件；启动时就被直接加载进内存中；而文件模块是在运行时动态加载，需要完成走上述三个流程<br>定位到具体文件后，Node 会新建一个模块对象，然后根据路径载入并编译。对不同的文件扩展名，载入方式不同:</p>
</blockquote>
<ul>
<li>.js。通过 fs 模块读取文件后编译执行</li>
<li>.node。这是 C/C++编写的扩展文件，通过 process.dlopen()方法加载最后编译生成的文件(但其实 C/C++模块不用编译，它本来就是编译好的)。C/C++模块的优势是执行效率；但显然 JS 编写的模块开发速度更快</li>
<li>.json。fs 模块读取后，用 JSON.parse()解析返回结果</li>
<li>其他。都被当成.js</li>
</ul>
<h2 id="核心模块-amp-编译-amp-编写核心模块"><a href="#核心模块-amp-编译-amp-编写核心模块" class="headerlink" title="核心模块 &amp; 编译 &amp; 编写核心模块"></a>核心模块 &amp; 编译 &amp; 编写核心模块</h2><p>核心模块分为 C/C++编写的和 JS 编写的两部分。Node 的 buffer，crypto，evals，fs，os 等模块都是部分通过 C/C++编写的</p>
<h2 id="C-C-扩展模块-amp-加载-amp-编写"><a href="#C-C-扩展模块-amp-加载-amp-编写" class="headerlink" title="C/C++扩展模块 &amp; 加载 &amp; 编写"></a>C/C++扩展模块 &amp; 加载 &amp; 编写</h2><p>CPU 性能更加</p>
<h2 id="模块调用栈"><a href="#模块调用栈" class="headerlink" title="模块调用栈"></a>模块调用栈</h2><p><img src="/img/Node/%E6%A8%A1%E5%9D%97%E8%B0%83%E7%94%A8%E6%A0%88.png"></p>
<h2 id="包和-NPM"><a href="#包和-NPM" class="headerlink" title="包和 NPM"></a>包和 NPM</h2><p>在模块之外，包和 NPM 是将模块联系起来的一种机制，是在模块的基础上进一步组织 JS 代码。包结构如下：</p>
<ul>
<li>package.json</li>
<li>bin。存放可执行的二进制文件</li>
<li>lib。存放 JS 代码</li>
<li>doc</li>
<li>test。单元测试用例</li>
</ul>
<h2 id="已经很少用的-AMD-和-CMD-规范"><a href="#已经很少用的-AMD-和-CMD-规范" class="headerlink" title="已经很少用的 AMD 和 CMD 规范"></a>已经很少用的 AMD 和 CMD 规范</h2><h1 id="第三章-异步-I-O"><a href="#第三章-异步-I-O" class="headerlink" title="第三章 异步 I/O"></a>第三章 异步 I/O</h1><h2 id="异步-I-O-实现现状"><a href="#异步-I-O-实现现状" class="headerlink" title="异步 I/O 实现现状"></a>异步 I/O 实现现状</h2><p>阻塞 IO 造成 CPU 等待浪费，非阻塞带来的麻烦事需要轮训去确认是否完成数据获取，它会让 CPU 处理状态判断，是对 CPU 资源的浪费<br>轮询的五种方案：read, select,poll, epoll(效率最高), kqueue。见 P53</p>
<h2 id="Node-的异步-IO"><a href="#Node-的异步-IO" class="headerlink" title="Node 的异步 IO"></a>Node 的异步 IO</h2><blockquote>
<p>事件循环、观察者、请求对象、I/O 线程池这四者共同构建成了 Node 异步 I/O 模型的基本要素<br>事件循环是异步实现的核心</p>
</blockquote>
<ul>
<li>Node 自身的执行模型是<strong>事件循环</strong>，正是它使得回调函数十分普遍。进程启动的时候，Node 会创建一个类似 whlie 的循环，每执行一次循环体的过程我们称为 Tick。如下图</li>
<li>(还有事件?) 那里即是<strong>观察者</strong></li>
</ul>
<p><img src="/img/Node/Tick.png"></p>
<h3 id="请求对象-和-I-O-线程池"><a href="#请求对象-和-I-O-线程池" class="headerlink" title="请求对象 和 I/O 线程池"></a>请求对象 和 I/O 线程池</h3><p>没太看懂。反正整个异步 IO 流程如下：</p>
<blockquote>
<p>输入输出完成端口（Input/Output Completion Port，IOCP）</p>
</blockquote>
<p><img src="/img/Node/%E5%BC%82%E6%AD%A5IO%E6%B5%81%E7%A8%8B.png"></p>
<ul>
<li>这里的单线程与 IO 线程池看起来有些悖论的杨祖。由于我们知道 JS 是单线程的，所以按常识容易理解为它不能充分利用多核 CPU。事实上，在 Node 中，除了 JS 是单线程外，Node 自身其实是多线程的，只是 IO 线程使用的 CPU 较少</li>
<li>另一点需要重视的观点则是，除了用户代码无法并行执行外，所有的 IO（磁盘 IO 和网络 IO 等）则是可以并行起来的</li>
</ul>
<h2 id="非-IO-的异步-API"><a href="#非-IO-的异步-API" class="headerlink" title="非 IO 的异步 API"></a>非 IO 的异步 API</h2><blockquote>
<p>Node 中与 IO 无关的异步 API：setTimeout(), setInterval(), setImmediate(), process.nextTick()</p>
</blockquote>
<h3 id="定时器"><a href="#定时器" class="headerlink" title="定时器"></a>定时器</h3><p>setTimeout()和 setInterval()和浏览器中是一致的，分别用于单次和多次定时执行任务。</p>
<ul>
<li>原理。它们的原理与异步 IO 比较类似，只是不需要 IO 线程池的参与。调用 setTimeout()或 setInterval()创建的定时器会被插入到定时器观察者内部的一个红黑树中。每次 Tick 执行时，会从该红黑树中迭代去除定时器对象，检查是否超过时间，如果超过，就形成一个事件，它的回调函数将立即执行。这个过程包含动用红黑树，创建定时器对象和迭代等操作，较为浪费性能</li>
<li>问题。它并非精准的。尽管时间循环十分快，但是如果某一次循环占用的时间较多，那么下次循环时，它也许已经超时很久了。eg. 通过 setTimeout 设定一个任务在 10ms 后执行，但是在 9ms 后，有一个任务占用了 5ms 的 CPU 时间片，再次轮到定时器执行时，时间就已经过期 4ms 了</li>
</ul>
<p><img src="/img/Node/settimeout.png"></p>
<h3 id="process-nextTick-和-setImmediate"><a href="#process-nextTick-和-setImmediate" class="headerlink" title="process.nextTick() 和 setImmediate()"></a>process.nextTick() 和 setImmediate()</h3><ul>
<li>每次调用 process.nextTick 函数，只会将回调函数放入队列中，在下一轮 Tick 的时候取出运行。定时器中采用红黑树操作的时间复杂度为 O(lg(n)), 而 nextTick 为 O(1)，更为高效</li>
<li>setImmediate()和 process.nextTick 十分类似，都是将回调函数延迟执行。但是执行优先级没 process.nextTick 高。似乎都属于微任务？</li>
</ul>
<h2 id="事件驱动与高性能服务器"><a href="#事件驱动与高性能服务器" class="headerlink" title="事件驱动与高性能服务器"></a>事件驱动与高性能服务器</h2><p><img src="/img/Node/web%E6%9C%8D%E5%8A%A1%E5%99%A8.png"><br>图3-15 利用Node构建Wen服务器的流程图</p>
<h1 id="第四章-异步编程"><a href="#第四章-异步编程" class="headerlink" title="第四章 异步编程"></a>第四章 异步编程</h1><h2 id="函数式编程"><a href="#函数式编程" class="headerlink" title="函数式编程"></a>函数式编程</h2><ul>
<li>高阶函数：可以把函数作为参数，或是将函数作为返回值的函数。eg. JS中的forEach, some, reduce等</li>
<li>偏函数：指创建一个调用另外一个部分——参数或变量已经预置的函数——的函数的用法。个人理解：偏函数会返返回一个函数，返回的函数的一部分是由偏函数的参数生成的。eg. 封装一个判断类型的函数<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> isType = <span class="function"><span class="keyword">function</span>(<span class="params">type</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Object</span>.toString.call(obj) === <span class="string">'[object '</span> + type + <span class="string">']'</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">Let isString = isType(<span class="string">'String'</span>)</span><br><span class="line">isString(<span class="string">'test'</span>) <span class="comment">// true</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="异步编程的优势和难点"><a href="#异步编程的优势和难点" class="headerlink" title="异步编程的优势和难点"></a>异步编程的优势和难点</h2><ul>
<li>优势：基于时间驱动的非阻塞IO模型</li>
<li>难点<ul>
<li>异常处理</li>
<li>函数嵌套太深</li>
<li>阻塞代码</li>
<li>多线程的编程。有了child_process</li>
<li>异步转同步</li>
</ul>
</li>
</ul>
<h2 id="异步并发控制"><a href="#异步并发控制" class="headerlink" title="异步并发控制"></a>异步并发控制</h2><h1 id="第五章-【重点】内存控制"><a href="#第五章-【重点】内存控制" class="headerlink" title="第五章 【重点】内存控制"></a>第五章 【重点】内存控制</h1><h2 id="【重点】V8的垃圾回收机制与内存限制"><a href="#【重点】V8的垃圾回收机制与内存限制" class="headerlink" title="【重点】V8的垃圾回收机制与内存限制"></a>【重点】V8的垃圾回收机制与内存限制</h2><h1 id="第六章-理解Buffer"><a href="#第六章-理解Buffer" class="headerlink" title="第六章 理解Buffer"></a>第六章 理解Buffer</h1>]]></content>
      <categories>
        <category>后端</category>
      </categories>
      <tags>
        <tag>Nodejs</tag>
      </tags>
  </entry>
  <entry>
    <title>提升webpack打包速度的方法</title>
    <url>/2019/09/27/%E6%8F%90%E5%8D%87webpack%E6%89%93%E5%8C%85%E9%80%9F%E5%BA%A6%E7%9A%84%E6%96%B9%E6%B3%95/</url>
    <content><![CDATA[<ul>
<li>及时更新版本（node，npm等），新版本一般都会有提高打包效率的新特性</li>
<li>在尽可能少的模块上应用<code>loader</code>。可以优化文件搜索范围（即在rule中写include或exclude），eg. 可以不用编译<code>node_modules</code>中的代码，因为里面的都是编译过的</li>
<li>plugin尽可能精简并确保可靠，也尽可能少用</li>
<li>resolve参数合理配置<br><code>import</code>或<code>require</code>的时候可以不写文件后缀，而在<code>resolva</code>中写<code>resolve.extensions</code>，并尽可能减少后缀列表长度，将出现频率高的后缀排在前面<br><code>resolve.alias</code>：设置别名的方式映射一个路径，让webpack更快找到路径</li>
<li><code>DllPluign</code><br><code>DllPlugin</code>可以将特定的类库提前打包然后引入。这种方式可以极大的减少打包类库的次数。主要当类库更新版本才有需要重新打包</li>
<li>控制包的大小<br><code>TreeShaking</code>：可以删除项目中未被引用的代码<br>懒加载：实现按需加载，多写异步代码，提高页面的代码使用率</li>
<li><code>thread-loader</code>和<code>happypack</code>可以可以利用node中的多进程， 同时利用多个CPO对项目打包；对多页面应用，使用<code>parallel-webpack</code>可以实现对多个页面一起打包</li>
<li>合理使用sourceMap:用对<code>devtool</code>配置，在开发模式建议用<code>cheap-module-eval-source-map</code>；在生产模式建议用<code>cheap-module-source-map</code></li>
<li>结合stats分析打包结果</li>
</ul>
]]></content>
      <categories>
        <category>Engineering</category>
      </categories>
      <tags>
        <tag>webpack</tag>
      </tags>
  </entry>
  <entry>
    <title>异步编程</title>
    <url>/2019/11/01/%E5%BC%82%E6%AD%A5%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<p>异步编程，就是通过利用客户端环境的 Event-Loop 机制，去异步地执行某些代码</p>
<h2 id="一些自己之前容易乱的关系"><a href="#一些自己之前容易乱的关系" class="headerlink" title="一些自己之前容易乱的关系"></a>一些自己之前容易乱的关系</h2><p>异步函数 = 异步操作，异步函数中包含回调函数做参数。回调函数只是一个普通函数。异步函数可以写成Promise形式</p>
<p>以前，异步编程的方法，大概有下面四种。<br>回调函数<br>事件监听<br>Promise 对象</p>
<h2 id="回调函数"><a href="#回调函数" class="headerlink" title="回调函数"></a>回调函数</h2><p>回调函数，就是把任务的第二段单独写在一个函数里面，等到重新执行这个任务的时候，就直接调用这个函数。英语名为 callback。<br>例如fs的读取文件</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">fs.readFile(<span class="string">'/etc/passwd'</span>, <span class="function"><span class="keyword">function</span> (<span class="params">err, data</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (err) <span class="keyword">throw</span> err;</span><br><span class="line">  <span class="built_in">console</span>.log(data);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>readFile 函数的第二个参数，就是<strong>回调函数</strong>。第一个参数是错误对象err</p>
<h2 id="Promise"><a href="#Promise" class="headerlink" title="Promise"></a>Promise</h2><p>当多个函数嵌套的时候，会出现“回调地狱”，代码难以理解。于是出现了Promise解决这个问题<br>Promise其实<strong>只是异步函数的一种新写法</strong>，允许将回调函数的横向加载，改成纵向加载</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> readFile = <span class="built_in">require</span>(<span class="string">'fs-readfile-promise'</span>)</span><br><span class="line"><span class="comment">// 或者</span></span><br><span class="line"><span class="keyword">const</span> promisify = <span class="built_in">require</span>(<span class="string">'util'</span>).promisify </span><br><span class="line"><span class="keyword">const</span> readFile = promisify(fs.readFile)</span><br><span class="line"></span><br><span class="line">readFile(<span class="string">'./promisify.js'</span>)</span><br><span class="line">.then(<span class="function"><span class="params">data</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(data.toString())</span><br><span class="line">&#125;)</span><br><span class="line">.then(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> readFile(fileB);</span><br><span class="line">&#125;)</span><br><span class="line">.then(<span class="function"><span class="keyword">function</span>(<span class="params">data</span>)</span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(data.toString());</span><br><span class="line">&#125;)</span><br><span class="line">.catch(<span class="function"><span class="params">err</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(err)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>第一种方式是使用了 fs-readfile-promise 模块，它的作用就是返回一个 Promise 版本的 readFile 函数；第二种方法能把异步函数进行promisify处理赋值给一个变量。Promise 提供 then 方法加载回调函数，catch方法捕捉执行过程中抛出的错误</p>
<h3 id="Promise对象"><a href="#Promise对象" class="headerlink" title="Promise对象"></a>Promise对象</h3><p>Promise对象代表一个异步操作，有三种状态：<code>pending</code>（进行中）、<code>fulfilled</code>（已成功）和<code>rejected</code>（已失败）。只有异步操作的结果，可以决定当前是哪一种状态<br>Promise对象的状态改变，只有两种可能：从<code>pending</code>变为<code>fulfilled</code>和从<code>pending</code>变为<code>rejected</code>。只要这两种情况发生，状态就凝固了，不会再变了，会一直保持这个结果，这时就称为 <code>resolved</code>（已定型）</p>
<h3 id="Promise实例"><a href="#Promise实例" class="headerlink" title="Promise实例"></a>Promise实例</h3><h4 id="Promise实例的生成方式"><a href="#Promise实例的生成方式" class="headerlink" title="Promise实例的生成方式"></a>Promise实例的生成方式</h4><ul>
<li>用new生成</li>
<li>promisify</li>
<li>直接require一些模块</li>
</ul>
<h4 id="监听结果的-Promise-prototype-then"><a href="#监听结果的-Promise-prototype-then" class="headerlink" title="监听结果的 Promise.prototype.then()"></a>监听结果的 Promise.prototype.then()</h4><p>普通写法</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//  普通写法。先定义一个加载图片的函数</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">loadImg</span>(<span class="params">src, callback, fail</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> img = <span class="built_in">document</span>.createElement(<span class="string">'img'</span>)</span><br><span class="line">  <span class="comment">//  load事件触发后就会调用作为第二个参数的callback函数</span></span><br><span class="line">  img.onload = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    callback(img)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//  error事件触发后就会调用作为第三个参数的fail函数</span></span><br><span class="line">  img.onerror = <span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">    fail(err)</span><br><span class="line">  &#125;</span><br><span class="line">  img.src = src</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> src = <span class="string">'http://img1.qunarzz.com/piao/fusion/1804/ff/fdf170ee89594b02.png'</span></span><br><span class="line"><span class="comment">// 正式加载</span></span><br><span class="line">loadImg(src, <span class="function"><span class="keyword">function</span> (<span class="params">img</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(img.width)</span><br><span class="line">&#125;, <span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(err)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>用Promise的写法</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">loadImg</span>(<span class="params">src</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> img = <span class="built_in">document</span>.createElement(<span class="string">'img'</span>)</span><br><span class="line">    img.onload = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">      resolve(img)</span><br><span class="line">    &#125;</span><br><span class="line">    img.onerror = <span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">      reject(err)</span><br><span class="line">    &#125;</span><br><span class="line">    img.src = src</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> src = <span class="string">'http://img1.qunarzz.com/piao/fusion/1804/ff/fdf170ee89594b02.png'</span></span><br><span class="line"><span class="keyword">var</span> result = loadImg(src)</span><br><span class="line"><span class="comment">// then里面的第一个参数其实就是resolve函数，第二个参数（可选）就是reject函数（也可以写成catch的形式）</span></span><br><span class="line">result.then(<span class="function"><span class="keyword">function</span> (<span class="params">img</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(img.width)</span><br><span class="line">&#125;, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(err)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h4 id="捕获异常的Promise-prototype-catch"><a href="#捕获异常的Promise-prototype-catch" class="headerlink" title="捕获异常的Promise.prototype.catch()"></a>捕获异常的Promise.prototype.catch()</h4><p>虽然then的第二个参数可以是reject函数，不过后来大家都默认then只接收一个参数，最后统一用catch捕获异常</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">result.then(<span class="function"><span class="keyword">function</span> (<span class="params">img</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(img.width)</span><br><span class="line">&#125;.catch(<span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(err)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h4 id="执行顺序与串联"><a href="#执行顺序与串联" class="headerlink" title="执行顺序与串联"></a>执行顺序与串联</h4><p>首先，Promise 新建后，<strong>里面的代码</strong>就会立即执行<br><strong>执行顺序</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">result.then(<span class="function"><span class="keyword">function</span> (<span class="params">img</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(img.width)</span><br><span class="line">&#125;.catch(<span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(err)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="comment">// 忽然想在图片加载后也打印下hight，可以这么写，这两个.then几乎会同时触发</span></span><br><span class="line">result.then(<span class="function"><span class="keyword">function</span> (<span class="params">img</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(img.height)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>也可以这么写</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="number">1</span>)</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">loadImg</span>(<span class="params">src</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> img = <span class="built_in">document</span>.createElement(<span class="string">'img'</span>)</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="number">2</span>)</span><br><span class="line">    img.onload = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">      resolve(img)</span><br><span class="line">    &#125;</span><br><span class="line">    img.onerror = <span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">      reject(err)</span><br><span class="line">    &#125;</span><br><span class="line">    img.src = src</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line">result.then(<span class="function"><span class="keyword">function</span> (<span class="params">img</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(img.width)</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="number">4</span>)</span><br><span class="line">  <span class="comment">// 如果要在后面继续加.then 打印img的height，必须return img，下一个.then才能接收到这个参数</span></span><br><span class="line">  <span class="keyword">return</span> img</span><br><span class="line">&#125;.then(<span class="function"><span class="keyword">function</span> (<span class="params">img</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(img.height)</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="number">5</span>)</span><br><span class="line">&#125;.catch(<span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(err)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">3</span>)</span><br><span class="line"><span class="comment">//  打印结果是1，2，3，4，5 （width和height同理）</span></span><br></pre></td></tr></table></figure>

<p>PS：Promise实例中的代码如果不涉及费时的操作，它的.then()执行顺序是比setTimeout第二个参数的为0的代码要早的<br><strong>串联</strong><br>一般用于Ajax请求，这里图片做演示<br>多个Promise实例要串联的时候，<strong>需要在.then()中return 第二个实例</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> src = <span class="string">'http://img1.qunarzz.com/piao/fusion/1804/ff/fdf170ee89594b02.png'</span></span><br><span class="line"><span class="keyword">var</span> result = loadImg(src)</span><br><span class="line"><span class="keyword">var</span> src2 = <span class="string">'http://img1.qunarzz.com/sight/p0/1603/1c/1c67791edbe2677b90.img.jpg_250x250_8f1dcbbd.jpg'</span></span><br><span class="line"><span class="keyword">var</span> result2 = loadImg(src2)</span><br><span class="line">result.then(<span class="function"><span class="keyword">function</span> (<span class="params">img</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'第一个图片加载完成'</span>, img.width)</span><br><span class="line">  <span class="comment">// 如果这种多个.then()没有返回一个premise实例，那就还是用原来那个promise实例</span></span><br><span class="line">  <span class="keyword">return</span> result2</span><br><span class="line">&#125;).then(<span class="function"><span class="keyword">function</span> (<span class="params">img2</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'第二个图片加载完成'</span>, img2.width)</span><br><span class="line">  <span class="built_in">document</span>.getElementById(<span class="string">'div'</span>).appendChild(img2)</span><br><span class="line">&#125;).catch(<span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(err)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h3 id="Promise-all-amp-Promise-race"><a href="#Promise-all-amp-Promise-race" class="headerlink" title="Promise.all() &amp; Promise.race()"></a>Promise.all() &amp; Promise.race()</h3><p>这两个方法用于将多个 Promise 实例，包装成一个新的 Promise 实例。都接收一个promise对象的数组。all()是待全部完成之后，统一执行success，race()是只要有一个完成，就执行success</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> src = <span class="string">'http://img1.qunarzz.com/piao/fusion/1804/ff/fdf170ee89594b02.png'</span></span><br><span class="line"><span class="keyword">var</span> result = loadImg(src)</span><br><span class="line"><span class="keyword">var</span> src2 = <span class="string">'http://img1.qunarzz.com/sight/p0/1603/1.jpg0_8f1dcbbd.jpg'</span></span><br><span class="line"><span class="keyword">var</span> result2 = loadImg(src2)</span><br><span class="line"><span class="built_in">Promise</span>.all([result, result2]).then(<span class="function"><span class="params">datas</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'all'</span>, datas[<span class="number">0</span>])</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'all'</span>, datas[<span class="number">1</span>])</span><br><span class="line">&#125;)</span><br><span class="line"><span class="built_in">Promise</span>.race([result, result2]).then(<span class="function"><span class="params">data</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'race'</span>, data)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>执行结果为：<br><img src="/img/%E5%BC%82%E6%AD%A5%E7%BC%96%E7%A8%8B/promise.png"></p>
<h2 id="Generator-函数"><a href="#Generator-函数" class="headerlink" title="Generator 函数"></a>Generator 函数</h2><h3 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h3><p>Generator 函数是一个状态机，封装了多个内部状态。形式上，有两个特征，一是，function关键字与函数名之间有一个<strong>星号</strong>；二是，函数体内部使用<strong>yield表达式</strong>，定义不同的内部状态。<strong>一定要return</strong>，否则返回的对象的value属性值为undefined<br>调用 Generator 函数，会返回一个内部指针，而不是返回结果<br>用next方法，会移动内部指针（即执行异步任务的第一段），指向下一个遇到的 yield 语句<br>每次调用 next 方法，会返回一个对象，表示当前阶段的信息。value 属性是 yield 语句后面表达式的值，表示当前阶段的值；done 属性是一个布尔值，表示 Generator 函数是否执行完毕，即是否还有下一个阶段</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">gen</span>(<span class="params">x</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> y = <span class="keyword">yield</span> x + <span class="number">2</span>;</span><br><span class="line">  <span class="keyword">return</span> y;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> g = gen(<span class="number">1</span>);</span><br><span class="line">g.next() <span class="comment">// &#123; value: 3, done: false &#125;</span></span><br><span class="line">g.next() <span class="comment">// &#123; value: undefined, done: true &#125;</span></span><br></pre></td></tr></table></figure>

<p>当**next()**传入参数的时候，会被当作上个阶段异步任务的返回结果，此阶段的value就等于这个参数（一般用于yield用完了的时候）<br>【next()本来就有打印的功能？】</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">gen</span>(<span class="params">x</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> y = <span class="keyword">yield</span> x + <span class="number">2</span>;</span><br><span class="line">  <span class="keyword">return</span> y;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> g = gen(<span class="number">1</span>);</span><br><span class="line">g.next() <span class="comment">// &#123; value: 3, done: false &#125;</span></span><br><span class="line">g.next(<span class="number">5</span>) <span class="comment">// &#123; value: 5, done: true &#125;</span></span><br></pre></td></tr></table></figure>

<p>yield表达式只能用在 Generator 函数里面，用在其他地方都会报错<br>在 Generator 函数里，yield表达式如果用在另一个表达式之中，必须放在圆括号里面 </p>
<p>虽然 Generator 函数将异步操作表示得很简洁，但是<strong>流程管理却不方便</strong>（即何时执行第一阶段、何时执行第二阶段）。所以有了以下两种方案：</p>
<h3 id="Thunk-函数-Generator方案"><a href="#Thunk-函数-Generator方案" class="headerlink" title="Thunk 函数 + Generator方案"></a>Thunk 函数 + Generator方案</h3><p>Thunk 函数是自动执行 Generator 函数的一种方法。是一个实现“传名调用”的临时函数，把这个临时函数作为参数传入真正的函数即可</p>
<p>JavaScript 语言是传值调用。JS中的Thunk替换的是多参数函数而不是表达式，将其替换成一个只接受<strong>回调函数作为参数</strong>的单参数函数。<br>单参数版本，就叫做 Thunk 函数</p>
<h4 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h4><p>Thunkify 模块</p>
<p><strong>Thunk 函数现在可以用于 Generator 函数的自动流程管理</strong></p>
<h3 id="co-函数库-Generator方案"><a href="#co-函数库-Generator方案" class="headerlink" title="co 函数库 + Generator方案"></a>co 函数库 + Generator方案</h3><p>co 模块用于 Generator 函数的自动执行。co函数返回一个Promise对象，因此可以用then方法添加回调函数。</p>
<h2 id="async-await"><a href="#async-await" class="headerlink" title="async / await"></a>async / await</h2><h3 id="与Generator比"><a href="#与Generator比" class="headerlink" title="与Generator比"></a>与Generator比</h3><p>异步编程的最高境界，就是根本不用关心它是不是异步。很多人认为Async就是异步操作的终极解决方案<br>如果认真看完两种自动化的 Generator 的方法 ，会有一种蛋疼的感觉（因为有这种感觉所以这两种方案没有仔细总结在博客里）。Generator 设计的初衷就是为了流程控制，而我们却给它加上了自动的流程机。还不如使用 promise.then()。Async 函数作为<strong>自带流程机的 Generator 语法糖</strong>，就完美得解决了这个矛盾</p>
<p><strong>async 函数就是将 Generator 函数的星号（*）替换成 async，将 yield 替换成 await，仅此而已</strong><br>Generator版：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fs = <span class="built_in">require</span>(<span class="string">'fs'</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> readFile = <span class="function"><span class="keyword">function</span> (<span class="params">fileName</span>)</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>)</span>&#123;</span><br><span class="line">    fs.readFile(fileName, <span class="function"><span class="keyword">function</span>(<span class="params">error, data</span>)</span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (error) reject(error);</span><br><span class="line">      resolve(data);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> gen = <span class="function"><span class="keyword">function</span>* (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> f1 = <span class="keyword">yield</span> readFile(<span class="string">'/etc/fstab'</span>);</span><br><span class="line">  <span class="keyword">var</span> f2 = <span class="keyword">yield</span> readFile(<span class="string">'/etc/shells'</span>);</span><br><span class="line">  <span class="built_in">console</span>.log(f1.toString());</span><br><span class="line">  <span class="built_in">console</span>.log(f2.toString());</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>async 函数版本</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> asyncReadFile = <span class="keyword">async</span> <span class="function"><span class="keyword">function</span> (<span class="params"></span>)</span>&#123;</span><br><span class="line">  <span class="keyword">var</span> f1 = <span class="keyword">await</span> readFile(<span class="string">'/etc/fstab'</span>);</span><br><span class="line">  <span class="keyword">var</span> f2 = <span class="keyword">await</span> readFile(<span class="string">'/etc/shells'</span>);</span><br><span class="line">  <span class="built_in">console</span>.log(f1.toString());</span><br><span class="line">  <span class="built_in">console</span>.log(f2.toString());</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="与Promise比"><a href="#与Promise比" class="headerlink" title="与Promise比"></a>与Promise比</h3><p>相比Promise，.then()只是将callback拆分了，实际代码的语义化，执行顺序仍旧不清晰。async/await才是最直接的同步写法。解决JS单线程造成的编写的代码视觉顺序和实际执行顺序不同的问题</p>
<h3 id="async-await用法总结"><a href="#async-await用法总结" class="headerlink" title="async / await用法总结"></a>async / await用法总结</h3><p>例如可以把Promise章节的代码改写为</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">loadImg</span>(<span class="params">src</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> img = <span class="built_in">document</span>.createElement(<span class="string">'img'</span>)</span><br><span class="line">    img.onload = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">      resolve(img)</span><br><span class="line">    &#125;</span><br><span class="line">    img.onerror = <span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">      reject(err)</span><br><span class="line">    &#125;</span><br><span class="line">    img.src = src</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> src = <span class="string">'http://img1.qunarzz.com/piao/fusion/1804/ff/fdf170ee89594b02.png'</span></span><br><span class="line"><span class="keyword">var</span> src2 = <span class="string">'http://img1.qunarzz.com/sight/p0/1603/1c/1c67791edbe2677b90.img.jpg_250x250_8f1dcbbd.jpg'</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> load = <span class="keyword">async</span> <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> result = <span class="keyword">await</span> loadImg(src)</span><br><span class="line">  <span class="built_in">console</span>.log(result)</span><br><span class="line">  <span class="keyword">const</span> result2 = <span class="keyword">await</span> loadImg(src2)</span><br><span class="line">  <span class="built_in">console</span>.log(result2)</span><br><span class="line">&#125;</span><br><span class="line">load()</span><br></pre></td></tr></table></figure>

<p><strong>async</strong>：</p>
<ul>
<li>用于定义异步函数，<strong>函数可以返回一个 Promise 对象</strong>（所以函数执行的时候可以使用 then 方法添加回调函数）</li>
<li>async函数执行的时候，<strong>一旦遇到 await 就会先返回，等到触发的异步操作完成，再接着执行函数体内后面的语句</strong></li>
<li>async函数中的forEach如果参数函数也是async函数的话，会报错，因为forEach内循环的async函数其实是并发的，而不是先后顺序的。正确的写法是采用 for 循环，再在里面写async函数。By the way，确实希望多个请求并发执行，可以使用 Promise.all 方法<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">dbFuc</span>(<span class="params">db</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> docs = [&#123;&#125;, &#123;&#125;, &#123;&#125;];</span><br><span class="line">  <span class="keyword">let</span> promises = docs.map(<span class="function">(<span class="params">doc</span>) =&gt;</span> db.post(doc));</span><br><span class="line"></span><br><span class="line">  <span class="keyword">let</span> results = <span class="keyword">await</span> <span class="built_in">Promise</span>.all(promises);</span><br><span class="line">  <span class="built_in">console</span>.log(results);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<p><strong>await</strong>：</p>
<ul>
<li><p>用于需要等待一段时间才能出结果的情况（eg. 将异步函数的执行结果赋值给变量），<strong>await后面必须跟一个Promise实例</strong>（或说异步函数）</p>
</li>
<li><p>await 命令后面的 Promise 对象，运行结果可能是 rejected，所以最好把 await 命令放在 try…catch 代码块中（或更实用的写法，在后面直接加.catch()的写法）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">myFunction</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">await</span> somethingThatReturnsAPromise().catch(<span class="function"><span class="keyword">function</span> (<span class="params">err</span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(err);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p><strong>await 命令只能用在 async 函数中</strong>，如果用在普通函数中，就会报错。PS: 其实await也能作为某些异步函数的运行前缀，例如用puppteer做爬虫的时候可以写<code>await page.focus(&#39;#kw&#39;)</code>，但自己写的async函数似乎不可以，还没明白</p>
</li>
</ul>
<h3 id="执行顺序"><a href="#执行顺序" class="headerlink" title="执行顺序"></a>执行顺序</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> page = &#123;</span><br><span class="line">  load: <span class="keyword">async</span> <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="number">3</span>)</span><br><span class="line">    <span class="keyword">const</span> result = <span class="keyword">await</span> loadImg(src)</span><br><span class="line">    <span class="built_in">console</span>.log(result)</span><br><span class="line">    <span class="keyword">const</span> result2 = <span class="keyword">await</span> loadImg(src2)</span><br><span class="line">    <span class="built_in">console</span>.log(result2)</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="number">4</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">1</span>)</span><br><span class="line">page.load()</span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">2</span>)</span><br><span class="line"><span class="comment">// 1, 3, 2, &lt;img xxx&gt;, &lt;img xxx&gt;, 4</span></span><br></pre></td></tr></table></figure>

<p>参考资料<br><a href="https://es6.ruanyifeng.com/#docs/generator-async" target="_blank" rel="noopener">ECMAScript6标准入门</a><br><a href="http://www.ruanyifeng.com/blog/2015/04/generator.html" target="_blank" rel="noopener">阮一峰博客-《深入掌握 ECMAScript 6 异步编程》系列文章</a><br><a href="https://coding.imooc.com/class/190.html" target="_blank" rel="noopener">慕课网-揭秘一线互联网企业前端JavaScript高级面试</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>性能优化</title>
    <url>/2019/11/06/%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96/</url>
    <content><![CDATA[<h2 id="性能优化概括"><a href="#性能优化概括" class="headerlink" title="性能优化概括"></a>性能优化概括</h2><p>主要的是：</p>
<ul>
<li>降低请求量：合并资源，减少 HTTP 请求数，minify / gzip 压缩，webP，lazyLoad。</li>
<li>加快请求速度：预解析DNS，减少域名数，并行加载，CDN 分发。</li>
<li>缓存：HTTP 协议缓存请求，离线缓存 manifest，离线数据缓存localStorage。（HTML 文件每次都向服务器询问是否有更新，JS/CSS/Image资源文件则不请求更新，直接使用本地缓存）</li>
<li>渲染：JS/CSS优化，加载顺序，服务端渲染，pipeline。</li>
</ul>
<h2 id="加载相关"><a href="#加载相关" class="headerlink" title="加载相关"></a>加载相关</h2><h3 id="为什么要强调-CSS-要放在-header-里，js-放在尾部"><a href="#为什么要强调-CSS-要放在-header-里，js-放在尾部" class="headerlink" title="为什么要强调 CSS 要放在 header 里，js 放在尾部"></a>为什么要强调 CSS 要放在 header 里，js 放在尾部</h3><p>构建 Render 树需要 DOM 和 CSSOM。由于 CSS 不会阻塞文档的解析，但是会阻塞文档渲染。把 CSS 放在头部可以先生成 CSSOM 树，后续渲染 DOM 的时候，可以一次性构建 Render 树，只需要渲染一次；如果把 CSS 放在后面，会先解析一次 DOM，加载 CSS 之后，会重新渲染之前的 DOM，需要两次渲染。所以 css 放头部能减少渲染时间</p>
<p>而 script 引的 js 文件会阻塞浏览器的解析，也就是说发现一个外链脚本时，<strong>需等待脚本下载完成并执行后才会继续解析 HTML</strong></p>
<p>普通的脚本会阻塞浏览器解析，加上 defer 或 async 属性，脚本就变成异步，可等到解析完毕再执行</p>
<ul>
<li>async 异步执行，异步下载完毕后就会执行，不确保执行顺序，一定在 onload 前，但不确定在 DOMContentLoaded 事件的前后</li>
<li>defer 延迟执行，相当于放在 body 最后（理论上在 DOMContentLoaded 事件前）</li>
<li>执行 js 代码过长会卡住渲染，对于需要很多时间计算的代码可以考虑用 Web Worker,它可以让我们另开一个线程执行脚本，不影响渲染</li>
</ul>
<h3 id="load-amp-DOMContentLoaded-amp-domready"><a href="#load-amp-DOMContentLoaded-amp-domready" class="headerlink" title="load &amp; DOMContentLoaded &amp; domready"></a>load &amp; DOMContentLoaded &amp; domready</h3><ul>
<li>DOMContentLoaded 事件触发时，仅当 DOM 加载完成，不包括样式表，图片</li>
<li>domready 事件在 DOM 加载后、资源加载之前被触发，在本地浏览器的 DOMContentLoaded 事件的形式被调用</li>
<li>load 事件触发时，页面上所有的 DOM，样式表，脚本，图片都已加载完成</li>
</ul>
<h3 id="白屏、首屏"><a href="#白屏、首屏" class="headerlink" title="白屏、首屏"></a>白屏、首屏</h3><h4 id="白屏"><a href="#白屏" class="headerlink" title="白屏"></a>白屏</h4><p>白屏时间指的是浏览器开始显示内容的时间，一般认为浏览器开始渲染 body 或者解析完 head 标签的时候就是页面白屏结束的时间<br><strong>优化</strong>：</p>
<ul>
<li>加快 js 的执行速度，比如无限滚动的页面，可以用 js 先渲染一个屏幕范围内的东西</li>
<li>减少文件体积</li>
<li>首屏同步渲染 html,后续的滚屏再异步加载和渲染</li>
</ul>
<h4 id="首屏"><a href="#首屏" class="headerlink" title="首屏"></a>首屏</h4><p>首屏时间是指用户打开网站开始，到浏览器首屏内容渲染完成的时间<br><strong>优化</strong>：</p>
<ul>
<li>图片懒加载</li>
<li>http 静态资源尽量用多个子域名（能开多个 TCP 链接）；</li>
<li>在 js，css，image 等资源响应的 httpheaders 里，设置 expires，last-modified</li>
<li>雪碧图（能减少 HTTP Requests 的数量）</li>
</ul>
<h2 id="渲染相关"><a href="#渲染相关" class="headerlink" title="渲染相关"></a>渲染相关</h2><h3 id="不考虑缓存和优化网络协议的前提下，可以通过哪些方式来最快的渲染页面"><a href="#不考虑缓存和优化网络协议的前提下，可以通过哪些方式来最快的渲染页面" class="headerlink" title="不考虑缓存和优化网络协议的前提下，可以通过哪些方式来最快的渲染页面"></a>不考虑缓存和优化网络协议的前提下，可以通过哪些方式来最快的渲染页面</h3><p>无非就是减少生成渲染树的时间了，从 DOM+CSSOM 入手，解决方案如下：</p>
<ul>
<li>从文件大小考虑</li>
<li>从 script 标签使用上来考虑 async 和 differ</li>
<li>从需要下载的内容是否需要在首屏使用上来考虑</li>
<li>CSS 选择器优化，HTML 扁平层级</li>
</ul>
<h3 id="如何减少重绘回流"><a href="#如何减少重绘回流" class="headerlink" title="如何减少重绘回流"></a>如何减少重绘回流</h3><ul>
<li>使用 transform 替代 top</li>
<li>使用 visibility 替换 display: none ，因为前者只会引起重绘，后者会引发回流（改变了布局）</li>
<li>不要使用 table 布局，可能很小的一个小改动会造成整个 table 的重新布局</li>
<li>CSS 选择符从右往左匹配查找，避免节点层级过多</li>
<li>将频繁重绘或者回流的节点设置为图层，图层能够阻止该节点的渲染行为影响别的节点。比如对于 video 标签来说，浏览器会自动将该节点变为图层。通过以下几个常用属性可以生成新图层：<ul>
<li>position: fixed</li>
<li>video、iframe 标签</li>
<li>通过动画实现的 opacity 动画转换</li>
</ul>
</li>
</ul>
<h3 id="为什么操作-DOM-性能差"><a href="#为什么操作-DOM-性能差" class="headerlink" title="为什么操作 DOM 性能差"></a>为什么操作 DOM 性能差</h3><p>DOM 属于渲染引擎中的东西，而 JS 属于 JS 引擎。当我们通过 JS 操作 DOM 的时候，涉及到两个线程之间的通信；并且操作 DOM 可能还会带来重绘回流的情况</p>
<h3 id="如何插入几万个-DOM-并不卡住界面"><a href="#如何插入几万个-DOM-并不卡住界面" class="headerlink" title="如何插入几万个 DOM 并不卡住界面"></a>如何插入几万个 DOM 并不卡住界面</h3><p>有两种实现思路：时间分片 和 虚拟列表<br>详见：<a href="https://mp.weixin.qq.com/s/yTuoa7ug7kJf14yv5-Nliw" target="_blank" rel="noopener">高性能渲染十万条数据（时间分片）</a></p>
<h4 id="window-requestAnimationFrame"><a href="#window-requestAnimationFrame" class="headerlink" title="window.requestAnimationFrame()"></a>window.requestAnimationFrame()</h4><p>通过 <a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Window/requestAnimationFrame" target="_blank" rel="noopener">requestAnimationFrame()</a> 去循环的插入 DOM 来。每 16 ms 刷新一次（浏览器是 60Hz 的刷新率，FPS (表示每秒画面更新的次数) 是 60frame/s, 每 1000 / 60 = 16.6ms 才会更新一次）。其实不一定是16ms，因为每个人的屏幕刷新频率不一定都是60，总之 <strong>requestAnimationFrame 是跟随系统刷新时间走的，它保证回调函数在屏幕每一次刷新间隔中只能被执行一次</strong>，这样就不会引起丢帧的情况<br>该方法接收一个回调函数作为参数，回调函数会在浏览器下一次重绘之前执行，也可以用来更新动画</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"viewport"</span> <span class="attr">content</span>=<span class="string">"width=device-width, initial-scale=1.0"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">title</span>&gt;</span>Document<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">"container"</span>&gt;</span><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="actionscript">    <span class="comment">//需要插入的容器</span></span></span><br><span class="line"><span class="javascript">    <span class="keyword">let</span> ul = <span class="built_in">document</span>.getElementById(<span class="string">'container'</span>)</span></span><br><span class="line"><span class="actionscript">    <span class="comment">// 插入十万条数据</span></span></span><br><span class="line"><span class="javascript">    <span class="keyword">let</span> total = <span class="number">100000</span></span></span><br><span class="line"><span class="actionscript">    <span class="comment">// 一次插入 20 条</span></span></span><br><span class="line"><span class="javascript">    <span class="keyword">let</span> once = <span class="number">20</span></span></span><br><span class="line"><span class="actionscript">    <span class="comment">//总页数</span></span></span><br><span class="line"><span class="javascript">    <span class="keyword">let</span> page = total / once</span></span><br><span class="line"><span class="actionscript">    <span class="comment">//每条记录的索引</span></span></span><br><span class="line"><span class="javascript">    <span class="keyword">let</span> index = <span class="number">0</span></span></span><br><span class="line"><span class="actionscript">    <span class="comment">//循环加载数据</span></span></span><br><span class="line"><span class="actionscript">    <span class="function"><span class="keyword">function</span> <span class="title">loop</span><span class="params">(curTotal, curIndex)</span> </span>&#123;</span></span><br><span class="line">      if (curTotal &lt;= 0) &#123;</span><br><span class="line"><span class="actionscript">        <span class="keyword">return</span> <span class="literal">false</span></span></span><br><span class="line">      &#125;</span><br><span class="line"><span class="actionscript">      <span class="comment">// 每页多少条</span></span></span><br><span class="line"><span class="javascript">      <span class="keyword">let</span> pageCount = <span class="built_in">Math</span>.min(curTotal, once)</span></span><br><span class="line"><span class="javascript">      <span class="built_in">window</span>.requestAnimationFrame(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span></span><br><span class="line"><span class="javascript">        <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; pageCount; i++) &#123;</span></span><br><span class="line"><span class="javascript">          <span class="keyword">let</span> li = <span class="built_in">document</span>.createElement(<span class="string">'li'</span>)</span></span><br><span class="line"><span class="javascript">          li.innerText = curIndex + i + <span class="string">' : '</span> + <span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * total)</span></span><br><span class="line">          ul.appendChild(li)</span><br><span class="line">        &#125;</span><br><span class="line">        loop(curTotal - pageCount, curIndex + pageCount)</span><br><span class="line">      &#125;)</span><br><span class="line">    &#125;</span><br><span class="line">    loop(total, index)</span><br><span class="line">  <span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="再优化版：DocumentFragment"><a href="#再优化版：DocumentFragment" class="headerlink" title="再优化版：DocumentFragment"></a>再优化版：DocumentFragment</h4><p>见<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/DocumentFragment/DocumentFragment" target="_blank" rel="noopener">MDN</a></p>
<blockquote>
<p>DocumentFragment，文档片段接口，表示一个没有父级文件的最小文档对象。它被作为一个轻量版的 Document使用，用于存储已排好版的或尚未打理好格式的XML片段。最大的区别是因为 DocumentFragment不是真实DOM树的一部分，<strong>它的变化不会触发DOM树的（重新渲染) ，且不会导致性能等问题</strong>。<br>可以使用 document.createDocumentFragment 方法或者构造函数来创建一个空的 DocumentFragment</p>
</blockquote>
<p>所以我们可以认为 <code>DocumentFragment</code> 是存在内存中的，所以将子元素插入到<code>DocumentFragment</code>时不会引起页面回流<br>所以上面的 loop 函数函数可以改成：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">loop</span>(<span class="params">curTotal, curIndex</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span>(curTotal &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">let</span> pageCount = <span class="built_in">Math</span>.min(curTotal, once)</span><br><span class="line">  <span class="built_in">window</span>.requestAnimationFrame(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">let</span> fragment = <span class="built_in">document</span>.createDocumentFragment()</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">let</span> i =<span class="number">0</span>;i &lt; pageCount;i++) &#123;</span><br><span class="line">      <span class="keyword">let</span> li = <span class="built_in">document</span>.createElement(<span class="string">'li'</span>)</span><br><span class="line">      li.innerText = curIndex + i + <span class="string">' : '</span> + (<span class="built_in">Math</span>.random() * total)</span><br><span class="line">      fragment.appendChild(li)</span><br><span class="line">    &#125;</span><br><span class="line">    ul.appendChild(fragment)</span><br><span class="line">    loop(curTotal - pageCount, curIndex + pageCount)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="虚拟滚动"><a href="#虚拟滚动" class="headerlink" title="虚拟滚动"></a>虚拟滚动</h4><p>这种技术的原理就是只渲染可视区域内的内容，非可见区域的那就完全不渲染了，当用户在滚动的时候就实时去替换渲染的内容。当我们滚动页面的时候就会实时去更新 DOM。react-virtualized 就有这个功能</p>
<h2 id="图片优化"><a href="#图片优化" class="headerlink" title="图片优化"></a>图片优化</h2><h3 id="大小优化"><a href="#大小优化" class="headerlink" title="大小优化"></a>大小优化</h3><p>计算图片大小：<code>对于一个</code>100px _ 100px<code>的图片，图像中有</code>10000px<code>的点，每一个</code>px<code>有</code>4<code>个通道（</code>rgba<code>），每一个通道</code>1<code>个字节（</code>1byte = 8bit<code>），所以该图片大小为</code>10000 _ 4 / 1024` = 39KB</p>
<ul>
<li>减少像素点</li>
<li>减少每个像素点能够显示的颜色</li>
</ul>
<h3 id="加载优化"><a href="#加载优化" class="headerlink" title="加载优化"></a>加载优化</h3><ul>
<li>类修饰图片用 CSS 去代替</li>
<li>懒加载</li>
<li>图片都用 <strong>CDN 加载</strong>。计算出适配屏幕的宽度，然后去请求相应裁剪好的图片</li>
<li>小图使用 base64 格式</li>
<li>将多个图标文件整合到一张图片中（雪碧图）</li>
<li>选择正确的图片格式：<ul>
<li>尽量使用 WebP 格式。因为 WebP 格式具有更好的图像数据压缩算法，能带来更小的图片体积，而且拥有肉眼识别无差异的图像质量。</li>
<li>小图使用 PNG，其实对于大部分图标这类图片，完全可以使用 SVG 代替</li>
<li>照片使用 JPEG</li>
</ul>
</li>
</ul>
<h2 id="防抖节流"><a href="#防抖节流" class="headerlink" title="防抖节流"></a>防抖节流</h2><h3 id="防抖"><a href="#防抖" class="headerlink" title="防抖"></a>防抖</h3><p>如果在频繁的事件回调中做复杂计算，很有可能导致页面卡顿，不如将多次计算合并为一次计算，只在一个精确点做操作。eg. 有一个按钮点击会触发网络请求，但是我们并不希望每次点击都发起网络请求，而是当用户点击按钮一段时间后没有再次点击的情况才去发起网络请求</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// func是用户传入需要防抖的函数，wait是等待时间</span></span><br><span class="line"><span class="keyword">const</span> debounce = <span class="function">(<span class="params">func, wait = <span class="number">50</span></span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 缓存一个定时器</span></span><br><span class="line">  <span class="keyword">let</span> timer = <span class="number">0</span>;</span><br><span class="line">  <span class="comment">// 这里返回的函数是每次用户实际调用的防抖函数</span></span><br><span class="line">  <span class="comment">// 如果已经设定过定时器了就清空上一次的定时器</span></span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">...args</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// apply第二个参数是数组</span></span><br><span class="line">    <span class="keyword">if</span> (timer) clearTimeout(timer);</span><br><span class="line">    timer = setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      func.apply(<span class="keyword">this</span>, args);</span><br><span class="line">    &#125;, wait);</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">debounce(<span class="function"><span class="params">str</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(str);</span><br><span class="line">&#125;, <span class="number">1000</span>)(<span class="string">"666"</span>); <span class="comment">// 1s后出现 666</span></span><br></pre></td></tr></table></figure>

<h3 id="节流"><a href="#节流" class="headerlink" title="节流"></a>节流</h3><p><strong>防抖是将多次执行变为最后一次执行，节流是将多次执行变成每隔一段时间执行</strong>。eg. 滚动事件中会发起网络请求，我们并不希望用户在滚动过程中一直发起请求，而是隔一段时间发起一次</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// func是用户传入需要节流的函数</span></span><br><span class="line"><span class="comment">// wait是等待时间</span></span><br><span class="line"><span class="keyword">const</span> throttle = <span class="function">(<span class="params">func, wait = <span class="number">50</span></span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 上一次执行该函数的时间</span></span><br><span class="line">  <span class="keyword">let</span> lastTime = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">...args</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 当前时间</span></span><br><span class="line">    <span class="keyword">let</span> now = <span class="keyword">new</span> <span class="built_in">Date</span>();</span><br><span class="line">    <span class="comment">// 将当前时间和上一次执行函数时间对比</span></span><br><span class="line">    <span class="comment">// 如果差值大于设置的等待时间就执行函数</span></span><br><span class="line">    <span class="keyword">if</span> (now - lastTime &gt; wait) &#123;</span><br><span class="line">      lastTime = now;</span><br><span class="line">      func.apply(<span class="keyword">this</span>, args);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 测试。setInterval设置了一个每隔50ms执行的，但是节流函数能强制把它改成每隔1000ms执行</span></span><br><span class="line">setInterval(</span><br><span class="line">  throttle(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"我要把你改成1000ms才执行"</span>);</span><br><span class="line">  &#125;, <span class="number">1000</span>),</span><br><span class="line">  <span class="number">50</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<h2 id="预加载"><a href="#预加载" class="headerlink" title="预加载"></a>预加载</h2><p>可以用<code>rel=&quot;preload&quot;</code>开启，强制浏览器请求资源</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;link rel=<span class="string">"preload"</span> href=<span class="string">"http://blog.poetries.top"</span>&gt;</span><br></pre></td></tr></table></figure>

<h2 id="预渲染"><a href="#预渲染" class="headerlink" title="预渲染"></a>预渲染</h2><p>可以通过预渲染<strong>将下载的文件预先在后台渲染</strong>，可以使用以下代码开启预渲染</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;link rel=<span class="string">"prerender"</span> href=<span class="string">"http://blog.poetries.top"</span>&gt;</span><br></pre></td></tr></table></figure>

<p>预渲染虽然可以提高页面的加载速度，但是要<strong>确保该页面大概率会被用户在之后打开</strong>，否则就是白白浪费资源去渲染</p>
<h2 id="DNS-预解析"><a href="#DNS-预解析" class="headerlink" title="DNS 预解析"></a>DNS 预解析</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;link rel=<span class="string">"dns-prefetch"</span> href=<span class="string">"//blog.poetries.top"</span>&gt;</span><br></pre></td></tr></table></figure>

<h2 id="懒执行"><a href="#懒执行" class="headerlink" title="懒执行"></a>懒执行</h2><p>懒执行就是<strong>将某些逻辑延迟到使用时再计算</strong><br>该技术可以用于首屏优化，对于某些耗时逻辑并不需要在首屏就使用的，就可以使用懒执行<br>懒执行需要唤醒，一般可以通过定时器或者事件的调用来唤醒</p>
<h2 id="懒加载"><a href="#懒加载" class="headerlink" title="懒加载"></a>懒加载</h2><p>懒加载就是<strong>将不关键的资源延后加载</strong><br>懒加载的原理就是只加载自定义区域（通常是可视区域，但也可以是即将进入可视区域）内需要加载的东西</p>
<blockquote>
<p>对于图片来说，先设置图片标签的 src 属性为一张占位图，将真实的图片资源放入一个自定义属性中。当进入视口区时，就将自定义属性替换为 src 属性，这样图片就会去下载资源，实现了图片懒加载</p>
</blockquote>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">img</span></span></span><br><span class="line"><span class="tag">  <span class="attr">src</span>=<span class="string">""</span></span></span><br><span class="line"><span class="tag">  <span class="attr">data-src</span>=<span class="string">"https://clubimg.club.vmall.com/data/attachment/forum/201911/10/101520zmot0v4cy3r1movs.jpg"</span></span></span><br><span class="line"><span class="tag">  <span class="attr">alt</span>=<span class="string">""</span></span></span><br><span class="line"><span class="tag">/&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 有若干图片 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">  <span class="keyword">let</span> img = <span class="built_in">document</span>.querySelectorAll(<span class="string">"img"</span>);</span></span><br><span class="line"><span class="javascript">  <span class="keyword">let</span> n = <span class="number">0</span>; <span class="comment">//存储图片加载到的位置，避免每次都从第一张图片开始遍历</span></span></span><br><span class="line"><span class="actionscript">  lazyload(); <span class="comment">//页面载入完毕加载可是区域内的图片</span></span></span><br><span class="line"><span class="javascript">  <span class="built_in">window</span>.onscroll = lazyload;</span></span><br><span class="line"><span class="actionscript">  <span class="function"><span class="keyword">function</span> <span class="title">lazyload</span><span class="params">()</span> </span>&#123;</span></span><br><span class="line"><span class="actionscript">    <span class="comment">//监听页面滚动事件</span></span></span><br><span class="line"><span class="javascript">    <span class="keyword">let</span> clientHeight = <span class="built_in">document</span>.documentElement.clientHeight; <span class="comment">//可见区域高度</span></span></span><br><span class="line"><span class="javascript">    <span class="keyword">let</span> scrollTop =</span></span><br><span class="line"><span class="javascript">      <span class="built_in">document</span>.documentElement.scrollTop || <span class="built_in">document</span>.body.scrollTop; <span class="comment">//滚动条距离顶部高度</span></span></span><br><span class="line"><span class="javascript">    <span class="keyword">for</span> (<span class="keyword">let</span> i = n; i &lt; img.length; i++) &#123;</span></span><br><span class="line"><span class="actionscript">      <span class="comment">// 图片未出现时距离顶部的距离大于滚动条距顶部的距离+可视区的高度</span></span></span><br><span class="line">      if (img[i].offsetTop &lt; clientHeight + scrollTop) &#123;</span><br><span class="line"><span class="actionscript">        img[i].src = img[i].getAttribute(<span class="string">"data-src"</span>);</span></span><br><span class="line">        n = i + 1;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>懒加载不仅可以用于图片，也可以使用在别的资源上。比如进入可视区域才开始播放视频等等</p>
<h2 id="CDN"><a href="#CDN" class="headerlink" title="CDN"></a>CDN</h2><p>CDN（Content Delivery Network，内容分发网络）将源站的内容发布到接近用户的网络“边缘”，用户可以就近获取所需数据（缓存数据），不仅降低了网络的拥塞状况、提高请求的响应速度，也能够减少源站的负载压力。<br>因此，我们可以将<strong>静态资源尽量使用 CDN 加载</strong>，由于浏览器对于单个域名有并发请求上限，可以考虑使用多个 CDN 域名。并且对于 CDN 加载静态资源需要注意<strong>CDN 域名要与主站不同</strong>，否则每次请求都会带上主站的 Cookie，平白消耗流量</p>
<p>参考资料<br>掘金小册-前端面试之道</p>
<h3 id="CDN的原理"><a href="#CDN的原理" class="headerlink" title="CDN的原理"></a>CDN的原理</h3><p>参考 <a href="https://juejin.im/post/5d105e1af265da1b71530095" target="_blank" rel="noopener">博客1</a>，<a href="https://yq.aliyun.com/articles/67298?spm=a2c4e.11153940.0.0.597476cfqRaHgb" target="_blank" rel="noopener">博客2</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>Performance</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统</title>
    <url>/2019/10/18/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E6%A6%82%E8%BF%B0/</url>
    <content><![CDATA[<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><h2 id="What-amp-Why"><a href="#What-amp-Why" class="headerlink" title="What &amp; Why"></a>What &amp; Why</h2><p>what：操作系统是管理计算机硬件和软件资源，提供用户交互界面的<strong>计算机程序</strong><br>Why：统一界面，操作硬件，简易地使用计算机</p>
<h2 id="操作系统的基本功能"><a href="#操作系统的基本功能" class="headerlink" title="操作系统的基本功能"></a>操作系统的基本功能</h2><ul>
<li>统一管理着计算机资源。eg. 处理器资源、IO 设备资源、存储器资源、文件资源</li>
<li>实现了对计算机资源的抽象。eg. 用户无需面向硬件接口编程； IO 设备管理软件，提供读写接口； 文件管理软件，提供操作文件接口</li>
<li>提供了用户与计算机之间的接口。eg. 图像窗口形式， 命令形式， 系统调用形式<br><img src="/img/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E5%9F%BA%E6%9C%AC%E5%8A%9F%E8%83%BD.png"></li>
</ul>
<h2 id="操作系统的演进"><a href="#操作系统的演进" class="headerlink" title="操作系统的演进"></a>操作系统的演进</h2><p>无操作系统—批处理系统（有多道程序设计的概念）—分时系统（能及时调试程序）</p>
<blockquote>
<p><strong>多道程序设计</strong>：多道程序设计使得批处理系统<strong>可以一次处理多个任务</strong>，提升计算机资源的利用率。 是指在计算机内存中同时存放多个程序， 在计算机的管理程序之下相互穿插运行，对多道程序的管理是<strong>操作系统的重要功能</strong>，分为：<strong>进程管理</strong>，<strong>存储管理</strong>，<strong>作业管理</strong>，<strong>文件管理</strong>（文件存储空间的管理、目录管理、文件读写管理和保护等。），<strong>设备管理</strong>（完成用户的 I/O 请求，方便用户使用各种设备，并提高设备的利用率。</p>
</blockquote>
<p>主要包括缓冲管理、设备分配、设备处理、虛拟设备等。</p>
<h2 id="相关概念"><a href="#相关概念" class="headerlink" title="相关概念"></a>相关概念</h2><h3 id="并发性"><a href="#并发性" class="headerlink" title="并发性"></a>并发性</h3><p><strong>并行</strong>是指两个或多个事件可以在<strong>同一个时刻</strong>发生，表现为同一时刻可以处理多个指令。eg. 一个双处理器，一个处理器处理一个事件<br><strong>并发</strong>是指两个或多个事件可以在同一个时间间隔发生， 宏观上表现为在<strong>一段时间内</strong>能同时运行多个程序。eg. 一个单处理器，在极短的时间间隔内交替运行多道程序，可视为并发。<br>并行需要硬件支持，如多流水性、多核处理器或分布式计算系统；而操作系统通过引入进程和线程就能使得程序能都并发运行</p>
<h3 id="共享性"><a href="#共享性" class="headerlink" title="共享性"></a>共享性</h3><p>表现为操作系统中的资源可被多个并发的程序共同使用（资源共享）。eg. 主存中的资源<br>有两种共享方式：<strong>互斥共享</strong>（当资源被程序 A 占用时，其他程序想使用的话只能等待。eg. 打印机）和<strong>同时共享</strong>（某种资源在一段时间内并发地被多个程序访问，这种“同时”是宏观的）</p>
<h3 id="虚拟性"><a href="#虚拟性" class="headerlink" title="虚拟性"></a>虚拟性</h3><p>虚拟性表现为把一个物理实体转变为若干个逻辑实体（物理实体是真实存在的，逻辑实体是虚拟的）。<br>虚拟的技术主要有：</p>
<ul>
<li>时分复用技术。 资源在时间上进行复用，不同程序并发使用。其实现是借助了<strong>虚拟处理器技术</strong>（ 多个进程轮流占用处理器，每次只执行一小个时间片并快速切换）和<strong>虚拟设备技术</strong>（物理设备虚拟为多个逻辑设备，每个程序栈用一个逻辑设备，多个程序通过逻辑设备并发访问）</li>
<li>空分复用技术。 空分复用技术用来实现虚拟磁盘（物理磁盘虚拟为逻辑磁盘（C、D、E 盘））、虚拟内存（ 在逻辑上扩大程序的存储容量， 使用比实际内存更大的容量）等。</li>
</ul>
<h3 id="异步性"><a href="#异步性" class="headerlink" title="异步性"></a>异步性</h3><p>异步指进程不是一次性执行完毕，而是走走停停，以不可知的速度向前推进。（能实现多进程并发执行，因为有异步性进程能在使用资源前能等待）</p>
<h1 id="Further"><a href="#Further" class="headerlink" title="Further"></a>Further</h1><h2 id="进程与线程"><a href="#进程与线程" class="headerlink" title="进程与线程"></a>进程与线程</h2><ul>
<li><strong>进程是系统进行资源分配和调度的基本单位（一般一个程序对应一个进程）</strong>。 引入多道程序设计的概念后， 进程作为程序独立运行的载体保障程序正常执行， 进程的存在使得操作系统资源的利用率大幅提升</li>
<li><strong>线程是操作系统进行运算调度的最小单位</strong>。 一个进程中可以有多个线程，它们共享进程资源， 是进程中实际运行工作的单位。<strong>eg</strong>. QQ 和浏览器是两个进程，浏览器进程里面有很多线程，例如 HTTP 请求线程、事件响应线程、渲染线程等等，线程的并发执行使得在浏览器中点击一个新链接从而发起 HTTP 请求时，浏览器还可以响应用户的其它事件。二者区别如下：</li>
</ul>
<p><img src="/img/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%BF%9B%E7%A8%8B%E7%BA%BF%E7%A8%8B.png"></p>
<h2 id="进程管理"><a href="#进程管理" class="headerlink" title="进程管理"></a>进程管理</h2><h3 id="进程实体"><a href="#进程实体" class="headerlink" title="进程实体"></a>进程实体</h3><p><strong>主存中</strong>的进程形态表现为一段连续存储的空间，叫<strong>进程控制块</strong> (Process Control Block, PCB)。里面记录了很多信息（eg. 状态，标识符，优先级，内存指针…）。PCB 用于描述和控制进程运行的通用数据结构， 记录进程当前状态和控制进程运行的全部信息， <strong>PCB 的使得进程是能够独立运行的基本单位。</strong></p>
<h2 id="进程的五状态模型"><a href="#进程的五状态模型" class="headerlink" title="进程的五状态模型"></a>进程的五状态模型</h2><ul>
<li>就绪（ready）： 其他资源都准备好、只差 CPU 资源的状态为就绪状态。 在一个系统中多个处于就绪状态的进程通常排成一个队列，称为就绪队列</li>
<li>执行（running）： 进程获得 CPU，其程序正在执行称为执行状态（单处理的某个时候只能有一个进程是该状态）</li>
<li>阻塞（waiting）： 进程因某种原因（eg. 有设备未准备就绪，例如打印机的排队造成的阻塞）而放弃 CPU 的状态称为阻塞状态。同理有阻塞队列</li>
<li>*创建： 创建进程时拥有 PCB 但其他资源尚未就绪的状态称为创建状态</li>
<li>*终止： 进程结束由系统清理或者归还 PCB 的状态称为终止状态</li>
</ul>
<p><img src="/img/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%BF%9B%E7%A8%8B%E7%8A%B6%E6%80%81.png"></p>
<h2 id="进程同步"><a href="#进程同步" class="headerlink" title="进程同步"></a>进程同步</h2><h3 id="Why"><a href="#Why" class="headerlink" title="Why"></a>Why</h3><p>关于临界资源（临界资源指的是一些虽作为共享资源却又无法同时被多个线程共同访问的共享资源，要等这个进程用完并释放，下个进程才能用）的两个问题</p>
<ul>
<li>生产者-消费者问题</li>
<li>哲学家进餐问题</li>
</ul>
<p>两个问题的原因都是：彼此相互之间没有通信。应该对竞争资源在多进程间进行使用次序的协调，使得并发执行的多个进程之间可以有效使用资源和相互合作。这就是<strong>进程间的同步</strong></p>
<h3 id="进程间同步的原则"><a href="#进程间同步的原则" class="headerlink" title="进程间同步的原则"></a>进程间同步的原则</h3><ul>
<li>空闲让进：资源无占用，允许使用</li>
<li>忙则等待：资源有占用，请求进程等待</li>
<li>有限等待：保证有限等待时间能够使用资源</li>
<li>让权等待：等待时，进程需要让出 CPU</li>
</ul>
<p>进程同步的方法：消息队列；共享存储；信息量</p>
<h3 id="线程同步"><a href="#线程同步" class="headerlink" title="线程同步"></a>线程同步</h3><p>由于进程内的线程共享进程资源，所以同理进程内多线程也需要同步。同步方法为：互斥量；读写锁；自旋锁；条件变量</p>
<p>参考资料<br><a href="https://coding.imooc.com/class/355.html" target="_blank" rel="noopener">慕课网</a><br><a href="https://github.com/CyC2018/CS-Notes" target="_blank" rel="noopener">CS-notes</a></p>
]]></content>
      <categories>
        <category>Basic knowledge</category>
      </categories>
      <tags>
        <tag>Operating system</tag>
      </tags>
  </entry>
  <entry>
    <title>正则表达式</title>
    <url>/2019/10/16/%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/</url>
    <content><![CDATA[<h3 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h3><p>/pattern/flags</p>
<h4 id="flags"><a href="#flags" class="headerlink" title="flags"></a>flags</h4><ul>
<li><strong>g</strong>：全局匹配;找到所有匹配，而不是在第一个匹配后停止</li>
<li>i：忽略大小写</li>
<li>m：多行; 将开始和结束字符（^和$）视为在多行上工作（也就是，分别匹配每一行的开始和结束（由 \n 或 \r 分割），而不只是只匹配整个输入字符串的最开始和最末尾处</li>
<li>…</li>
</ul>
<h4 id="常用字符类别"><a href="#常用字符类别" class="headerlink" title="常用字符类别"></a>常用字符类别</h4><ul>
<li>. ：匹配任意单个字符。例如，/.y/ 匹配 “yes make my day” 中的 “my” 和 “ay”，但是不匹配 “yes”</li>
<li>\d ：匹配任意阿拉伯数字。等价于<code>[0-9]</code>。例如，<code>/\d/</code> 或 <code>/[0-9]/</code> 匹配 “B2 is the suite number.” 中的 ‘2’</li>
<li>\D ：匹配任意一个不是阿拉伯数字的字符。等价于<code>[^0-9]</code>。例如，<code>/\D/</code> 或 <code>/[^0-9]/</code> 匹配 “B2 is the suite number.” 中的 ‘B’</li>
<li>\w ：匹配任意来自基本拉丁字母表中的字母数字字符，还包括下划线。例如，/\w/ 匹配 “apple” 中的 ‘a’，”$5.28” 中的 ‘5’ 和 “3D” 中的 ‘3’</li>
<li>\W ：匹配任意不是基本拉丁字母表中单词（字母数字下划线）字符的字符。例如，/\W/ 匹配 “50%” 中的 ‘%’</li>
<li>\s ：匹配一个空白符，包括空格、制表符、换页符、换行符</li>
<li>\S ：匹配一个非空白符</li>
<li>\ ：按照字面意义解释。例如，* 是一个特殊字符，表示匹配某个字符 0 或多次，如 <code>/a*/</code> 意味着 0 或多个 “a”。 为了匹配字面意义上的 * ，在它前面加上一个反斜杠，例如，<code>/a\*/</code>匹配 ‘a*’</li>
</ul>
<h4 id="字符集合"><a href="#字符集合" class="headerlink" title="字符集合"></a>字符集合</h4><ul>
<li><p><code>[xyz]</code> : 字符集合，匹配集合中的任意一个字符（可以理解为多个 | 连用）。例如，<code>[abcd]</code> 等价于 <code>[a-d]</code>，匹配”brisket”中的’b’和”chop”中的’c’.</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 想匹配字符串"Let's take LeetCode contest" 中的 " Let's "</span></span><br><span class="line"><span class="keyword">var</span> str = <span class="string">"Let's take LeetCode contest"</span></span><br><span class="line"><span class="built_in">console</span>.log(str.match(<span class="regexp">/\w+/</span>)[<span class="number">0</span>])  <span class="comment">// Let</span></span><br><span class="line"><span class="built_in">console</span>.log(str.match(<span class="regexp">/[\w']+/</span>)[<span class="number">0</span>])  <span class="comment">// Let's </span></span><br><span class="line"><span class="built_in">console</span>.log(str.match(<span class="regexp">/(\w|')+/</span>)[<span class="number">0</span>])  <span class="comment">// Let's</span></span><br></pre></td></tr></table></figure></li>
<li><p><code>[^xyz]</code> : 反字符集合</p>
</li>
</ul>
<h4 id="边界"><a href="#边界" class="headerlink" title="边界"></a>边界</h4><ul>
<li>^ : 匹配输入开始。例如，/^A/ 不匹配 “an A” 中的 “A”，但匹配 “An A” 中的 “A”</li>
<li>$ : 匹配输入结尾。例如，/t$/ 不匹配 “eater” 中的 “t”，但匹配 “eat” 中的 “t”</li>
<li>\b : 匹配一个零宽单词边界（zero-width word boundary），如一个字母与一个空格之间。例如，/\bno/ 匹配 “at noon” 中的 “no”，/ly\b/ 匹配 “possibly yesterday.” 中的 “ly”</li>
<li>\B : 匹配一个零宽非单词边界（zero-width non-word boundary），如两个字母之间或两个空格之间.例如，/\Bon/ 匹配 “at noon” 中的 “on”，/ye\B/ 匹配 “possibly yesterday.” 中的 “ye”</li>
</ul>
<h4 id="分组和反向引用（重要）"><a href="#分组和反向引用（重要）" class="headerlink" title="分组和反向引用（重要）"></a>分组和反向引用（重要）</h4><ul>
<li><p>(x) ：捕获括号，匹配 x 并且捕获匹配项，可以说有<strong>捕获</strong>的功能也有<strong>“边界”</strong>的功能。例如，<code>/(foo)/</code> 匹配且捕获 “foo bar.” 中的 “foo”。被匹配的子字符串可以在结果数组的元素 <code>[1], ..., [n]</code> 中找到，或在被定义的 RegExp 对象的属性 <code>$1, ..., $9</code> 中找到，这种$的语法一般只在replace中用.</p>
</li>
<li><p>*PS**：注意是从$1开始的，而不是$0</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> re = <span class="regexp">/(\w+)\s(\w+)/</span>;</span><br><span class="line"><span class="keyword">var</span> str = <span class="string">"John Smith"</span>;</span><br><span class="line"><span class="keyword">var</span> result = re.exec(str)</span><br><span class="line"><span class="keyword">var</span> newstr = str.replace(re, <span class="string">"$2, $1"</span>);</span><br><span class="line"><span class="built_in">console</span>.log(result[<span class="number">0</span>]);  <span class="comment">// John Smith</span></span><br><span class="line"><span class="built_in">console</span>.log(newstr);  <span class="comment">// Smith, John</span></span><br></pre></td></tr></table></figure></li>
<li><p>\n ：n是一个正整数。反向引用，向正则表达式中第 n 个括号（从左开始数）中匹配的子字符串，<strong>个人理解</strong>（1）是用在有捕获括号的时候，定义$是指向哪个捕获项，就算不写\1也会默认$1是捕获的第一项；（2）可以用这个\1代表第一个捕获项，做一些事，eg. \1+</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> s = <span class="string">'abcabcabcabc'</span></span><br><span class="line"><span class="keyword">var</span> reg = <span class="regexp">/^(\w+)\1+$/</span></span><br><span class="line"><span class="built_in">console</span>.log(reg.test(s))  <span class="comment">// true</span></span><br></pre></td></tr></table></figure></li>
<li><p><code>(?:x)</code> ：匹配 x 不会捕获匹配项。这被称为非捕获括号（non-capturing parentheses）。匹配项不能够从结果数组的元素 <code>[1], ..., [n]</code> 或已被定义的 RegExp 对象的属性 <code>$1, ..., $9</code> 再次访问到。</p>
</li>
</ul>
<h4 id="数量词"><a href="#数量词" class="headerlink" title="数量词"></a>数量词</h4><ul>
<li>x* ：匹配前面的模式 x 0 或多次。例如，/bo*/ 匹配 “A ghost booooed” 中的 “boooo”，”A bird warbled” 中的 “b”，但是不匹配 “A goat grunted”</li>
<li>x+ ：匹配前面的模式 x 1 或多次</li>
<li><code>x*?</code> ：像上面的 * 和 + 一样匹配前面的模式 x，然而匹配是<strong>最小可能匹配</strong>。例如，/“.*?”/ 匹配 ‘“foo” “bar”‘ 中的 ‘“foo”‘，而 * 后面没有 ? 时匹配 ‘“foo” “bar”</li>
<li>x? : 匹配前面的模式 x 0 或 1 次。例如，/e?le?/ 匹配 “angel” 中的 “el”，”angle” 中的 “le”</li>
<li>x(?=y) ：只有当 x 后面紧跟着 y 时，才匹配 x。 例如，/Jack(?=Sprat)/ 只有在 ‘Jack’ 后面紧跟着 ‘Sprat’ 时，才会匹配它。/Jack(?=Sprat|Frost)/ 只有在 ‘Jack’ 后面紧跟着 ‘Sprat’ 或 ‘Frost’ 时，才会匹配它。然而，’Sprat’ 或 ‘Frost’ 都不是匹配结果的一部分</li>
<li>x(?!y) ：只有当 x 后面不是紧跟着 y 时，才匹配 x</li>
<li>x|y ：匹配x或y</li>
</ul>
<h4 id="在线正则表达式测试"><a href="#在线正则表达式测试" class="headerlink" title="在线正则表达式测试"></a><a href="http://tool.oschina.net/regex/" target="_blank" rel="noopener">在线正则表达式测试</a></h4><h4 id="正则表达式的方法"><a href="#正则表达式的方法" class="headerlink" title="正则表达式的方法"></a>正则表达式的方法</h4><ul>
<li>对RegExp的方法<ul>
<li>exec()  一个在字符串中执行查找匹配的RegExp方法，它返回一个数组（未匹配到则返回 null）。语法是 <em>regexp</em>.exec(<em>string</em>)</li>
<li>test()  一个在字符串中测试是否匹配的RegExp方法，它返回 true 或 false。用于只想知道目标字符串与某个模式是否匹配而不想知文本内容。语法是 <em>regexp</em>.test(<em>string</em>)</li>
</ul>
</li>
<li>对String的方法<ul>
<li>match()  和exex()其实一毛一样，但语法是<em>string</em>.test(<em>regexp</em>)。一个在字符串中执行查找匹配的String方法，它返回一个数组，在未匹配到时会返回 null</li>
<li>matchAll  一个在字符串中执行查找所有匹配的String方法，它返回一个迭代器（iterator）</li>
<li>search    一个在字符串中测试匹配的String方法，它返回匹配到的位置索引，或者在失败时返回-1</li>
<li>replace    一个在字符串中执行查找匹配的String方法，并且使用替换字符串替换掉匹配到的子字符串</li>
<li>split    一个使用正则表达式或者一个固定字符串分隔一个字符串，并将分隔后的子字符串存储到数组中的 String 方法</li>
</ul>
</li>
</ul>
<p>参考资料<br><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/RegExp" target="_blank" rel="noopener">RegExp</a><br><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Regular_Expressions" target="_blank" rel="noopener">正则表达式</a></p>
]]></content>
      <categories>
        <category>Basic knowledge</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>Data structure</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构与算法</title>
    <url>/2019/10/19/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>吹爆这个博客：<a href="http://www.conardli.top/docs/" target="_blank" rel="noopener">awesome-coding-js</a>，强烈建议刷完这个博客</p>
<h3 id="字符串"><a href="#字符串" class="headerlink" title="字符串"></a>字符串</h3><p>常用API：<br>操作方法。拼接concat(), 裁剪slice(), substr(), substring()，拆分变数组split()，repeat()（接收一个大于等于0的整数为参数，返回复制这个整数倍次数的字符串）<br>位置方法。indexof(), lastIndexof()<br>模式匹配方法。match()（接受一个正则做参数，返回匹配结果数组有）, search()(少用), replace()（第一个参数是正则或字符串，第二个是用来替换的字符串。要实现多次替换必须是用带g的正则）, startsWith()（用来检测字符串是否已制定的前缀开头，返回布尔值。接收一个或者两个参数，第一个是前缀，第二是开始查找的位置）</p>
<h3 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h3><p>常用API：<br>includes()。判断一个数组是否包含一个指定的值（接收这个值为参数，返回布尔值）<br><strong>栈方法（先进后出）</strong>：push()，pop()（会删除最后一项并作为返回值）<br><strong>队列方法（先进先出）</strong>：shift()（会删除第一项并作为返回值），unshift()（在数组前端添加任意个项并返回新数组的长度）<br><strong>排序方法</strong>：reverse(), sort()（可以接收一个比较函数为参数，比较函数返回的值小于0，a会被排到b前面；没有传入比较函数，就只是单纯把数组toString()，然后用字符串的方法比较排序）<br><strong>操作方法</strong>：concat()（拼接。返回新数组。不影响原来数组），slice()（裁剪。返回新数组。不影响原来数组。一个参数时返回该位置到末尾的所有项；两个参数时返回两个位置之间的项，但不包含结束位置的项），splice()（可删除，替换，插入。会影响原始数组。始终会返回一个数组，该数组包含从原始数组中删除的项）<br><strong>位置方法</strong>：indexOf()，lastIndexOf()。找到的话将返回位置，没找到的话会返回-1</p>
<h5 id="迭代方法"><a href="#迭代方法" class="headerlink" title="迭代方法"></a>迭代方法</h5><p><strong>不生成数组的迭代</strong><br>every()：测试一个数组内的所有元素是否都能通过某个指定函数的测试。它返回一个布尔值。它的参数是一个返回值为布尔值的函数，所以用箭头函数表示很方便。<br>eg. <code>[12, 5, 8, 130, 44].every(x =&gt; x &gt;= 10); // false</code><br>some()：同理，不过只要一个元素能通过就会返回true。<br>**reduce()**：累加器（骚操作神器），一定要return。对数值数组可以求和，对字符串数组相当于join()。更强大的功能见<a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array/Reduce" target="_blank" rel="noopener">MDN</a>的例子（累加对象里的键值；将二维数组转为一维；计算数组中每个元素出现的次数；按属性对Object分类；数组去重；绑定包含在对象数组中的数组）<br>eg. <code>[1, 2, 3, 4].reduce((accumulator, currentValue) =&gt; accumulator + currentValue;)  //将会得到10</code><br>reduceRight()：同理，不过是从右往左开始累加<br>forEach()。很好用不解释<br><strong>生成数组的迭代</strong><br>map()：和forEach类似。不过会返回新生成的数组（本来就是一个数组）保存操作结果<br>filter()：和every()类似，不过会生成新数组来包含这些通过测试的item</p>
<h5 id="借用apply"><a href="#借用apply" class="headerlink" title="借用apply"></a>借用apply</h5><p>获取数组中最小值的方法： Math.min.apply(null, arr)</p>
<h5 id="ES6"><a href="#ES6" class="headerlink" title="ES6"></a>ES6</h5><p>Array.from() 将类似数组的对象转为数组（arguement，Nodelist），也能将普通对象的key值取出来生成一个数组</p>
<h3 id="正则"><a href="#正则" class="headerlink" title="正则"></a><a href="https://jacleklm.github.io/2019/10/16/%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/#more" target="_blank" rel="noopener">正则</a></h3><h3 id="递归"><a href="#递归" class="headerlink" title="递归"></a>递归</h3><p>说实话递归用的不溜</p>
<ul>
<li>一般是一个数组保存结果，写一个递归函数，让这个函数执行一下，返回保存的结果<br>递归函数都喜欢设置第一个参数为暂时的处理结果（可以是数组），第二个参数是要处理的东西。递归函数内部，喜欢用if来判断终点，else来处理正常情况。这个正常情况最后都会递归一下。</li>
<li>递归函数内进行一些操作之后，最后再是边界判断决定是要return递归还是return结果。最终return也可以是递归函数的执行结果</li>
</ul>
<h3 id="排序算法"><a href="#排序算法" class="headerlink" title="排序算法"></a>排序算法</h3><p>sort()虽然好用，但底层是一次完整的遍历排序，耗性能，做排序算法题能不用就不用。如果题目中要用数组的最大值/最小值，先想想能不能用冒泡/选择 排序。</p>
<h4 id="比较"><a href="#比较" class="headerlink" title="比较"></a>比较</h4><p><strong>对于评述算法优劣术语的说明</strong><br><strong>稳定</strong>：如果a原本在b前面，而a=b，排序之后a仍然在b的前面；<br><strong>不稳定</strong>：如果a原本在b的前面，而a=b，排序之后a可能会出现在b的后面；</p>
<p><strong>内排序</strong>：所有排序操作都在内存中完成；<br><strong>外排序</strong>：由于数据太大，因此把数据放在磁盘中，而排序通过磁盘和内存的数据传输才能进行；</p>
<p><strong>时间复杂度</strong>: 一个算法执行所耗费的时间。对于一个算法来说，可能会计算出如下操作次数 aN +1，N 代表数据量。那么该算法的时间复杂度就是 O(N)。因为我们在计算时间复杂度的时候，数据量通常是非常大的，这时候低阶项和常数项可以忽略不计。<br><strong>空间复杂度</strong>: 运行完一个程序所需内存的大小。空间复杂度表示算法的存储空间与数据规模之间的增长关系。eg:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">function fun(n) &#123;</span><br><span class="line">  let a = [];</span><br><span class="line">  for (let i = 0; i &lt; n; i++) &#123;</span><br><span class="line">    a.push(i);</span><br><span class="line">  &#125;</span><br><span class="line">  return a;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>以上代码我们可以清晰的看出代码执行的空间为 O(1+n) = O(n)，即为 i 及数组 a 占用的储存空间<br><img src="/img/%E7%AE%97%E6%B3%95/%E6%8E%92%E5%BA%8F2.png"></p>
<h4 id="二分查找"><a href="#二分查找" class="headerlink" title="二分查找"></a>二分查找</h4><p><strong>一个排好序的数组，在其找item === flag的item的index。要start，end，middle</strong><br>首先先找到长度中间位置，通过与中间位置的数比较，比中间值大在右边找，比中间值小在左边找。然后再在两边各自寻找中间值，持续进行，直到找到全部位置</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//假设arr已经排好序</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">binarySearch</span>(<span class="params">arr, flag, start, end</span>) </span>&#123;</span><br><span class="line">  start = start || <span class="number">0</span></span><br><span class="line">  end = end || arr.length - <span class="number">1</span></span><br><span class="line">  <span class="keyword">let</span> m = <span class="built_in">Math</span>.floor((realStart + realEnd) / <span class="number">2</span>)</span><br><span class="line">  <span class="keyword">if</span> (arr[m] === flag) &#123;</span><br><span class="line">    <span class="keyword">return</span> m;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (flag &lt; arr[m]) &#123;</span><br><span class="line">    <span class="keyword">return</span> binarySearch(arr, flag, start, m - <span class="number">1</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (flag &gt; arr[m]) &#123;</span><br><span class="line">    <span class="keyword">return</span> binarySearch(arr, flag, m + <span class="number">1</span>, end)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>非递归的方法也写一个吧</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">binarySearch</span>(<span class="params">arr, flag</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> end = arr.length - <span class="number">1</span>,</span><br><span class="line">    start = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">while</span> (start &lt;= end) &#123;</span><br><span class="line">    <span class="keyword">let</span> m = <span class="built_in">Math</span>.floor((start + end) / <span class="number">2</span>);</span><br><span class="line">    <span class="keyword">if</span> (flag === arr[m]) &#123;</span><br><span class="line">      <span class="keyword">return</span> m;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (flag &gt; arr[m]) &#123;</span><br><span class="line">      start = m + <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (flag &lt; arr[m]) &#123;</span><br><span class="line">      end = m - <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="冒泡排序-两两比较，大者排后"><a href="#冒泡排序-两两比较，大者排后" class="headerlink" title="冒泡排序(两两比较，大者排后)"></a>冒泡排序(两两比较，大者排后)</h4><blockquote>
<p>冒泡排序的原理如下，从第一个元素开始，把当前元素和下一个索引元素进行比较。如果当前元素大，那么就交换位置，重复操作直到比较到最后一个元素，那么此时最后一个元素就是该数组中最大的数。下一轮重复以上操作，但是此时最后一个元素已经是最大数了，所以不需要再比较最后一个元素，只需要比较到 length - 1 的位置。实现代码如下：</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> (arr) =&gt; &#123;</span><br><span class="line">  <span class="comment">// 冒泡排序</span></span><br><span class="line">  <span class="comment">// i是每一轮操作的终点，j是每一次操作的具体位置，tmp为被操作元素</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = arr.length - <span class="number">1</span>, tmp; i &gt; <span class="number">0</span>; i--) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">0</span>; j &lt; i; j++) &#123;</span><br><span class="line">      tmp = arr[j]</span><br><span class="line">      <span class="keyword">if</span> (tmp &gt; arr[j + <span class="number">1</span>]) &#123;</span><br><span class="line">        arr[j] = arr[j + <span class="number">1</span>]</span><br><span class="line">        arr[j + <span class="number">1</span>] = tmp</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> arr</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>怎么实现降序? 将内层循环的if条件语句改为小于号即可</p>
</blockquote>
<blockquote>
<p>该算法的操作次数是一个等差数列 n + (n - 1) + (n - 2) + … + 1 ，去掉常数项以后得出时间复杂度是O(n * n)</p>
</blockquote>
<h4 id="选择排序（设置最小值索引）"><a href="#选择排序（设置最小值索引）" class="headerlink" title="选择排序（设置最小值索引）"></a>选择排序（设置最小值索引）</h4><blockquote>
<p>选择排序的原理如下。遍历数组，设置最小值的索引为遍历的起始位置，如果取出的值比当前最小值小，就替换最小值索引，遍历完成后，将第一个元素和最小值索引上的值交换。如上操作后，第一个元素就是数组中的最小值，下次遍历就可以从索引 1 开始重复上述操作<br>口语化说法：遍历数组，找到最小值的元素放到数组的第一位；第二轮遍历，找到剩下的元素的最小值元素放到数组第二位；…（每次都能知道最小值）</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> (arr) &#123;</span><br><span class="line">  <span class="comment">//  两个for（第一个for是总遍历确定位置，第二个for是确定位置之后再遍历剩下的项），还需要一个变量储存最小，一个变量方便互换位置</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; arr.length - <span class="number">1</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">let</span> min = arr[i]</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> j = i + <span class="number">1</span>; j &lt; arr.length; j++) &#123;</span><br><span class="line">      <span class="keyword">if</span> (arr[j] &lt; min) &#123;</span><br><span class="line">        <span class="keyword">let</span> c = min</span><br><span class="line">        min = arr[j]</span><br><span class="line">        arr[j] = c</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    arr[i] = min</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> arr</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="快速排序"><a href="#快速排序" class="headerlink" title="快速排序"></a>快速排序</h4><p>是利用二分查找对冒泡排序的改进，原理如下：随机选取一个数组中的值作为基准值，遍历数组把每项与基准值对比大小。比基准值小的放数组左边，大的放右边。然后将数组以基准值的位置分为两部分，继续递归以上操作</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">quickSort</span>(<span class="params">arr</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (arr.length &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> arr;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//中间位置</span></span><br><span class="line">  <span class="keyword">let</span> middle = <span class="built_in">Math</span>.floor(arr.length / <span class="number">2</span>);</span><br><span class="line">  <span class="keyword">let</span> flag = arr.splice(middle, <span class="number">1</span>)[<span class="number">0</span>];<span class="comment">//取出中间元素</span></span><br><span class="line">  <span class="keyword">let</span> left = [], right = [];</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; arr.length; i++) &#123;<span class="comment">// 注意这里是arr.length</span></span><br><span class="line">    <span class="keyword">if</span> (arr[i] &lt; flag) &#123; <span class="comment">// 这个if也是导致快排不稳定的原因：两个相同的数，如果有一个被用来做flag，另一个很可能会被换位置</span></span><br><span class="line">      left.push(arr[i]);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      right.push(arr[i]);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> quickSort(left).concat(flag, quickSort(right));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="插入排序（类似于摸扑克牌）"><a href="#插入排序（类似于摸扑克牌）" class="headerlink" title="插入排序（类似于摸扑克牌）"></a><a href="https://blog.csdn.net/sinat_32290679/article/details/78619848" target="_blank" rel="noopener">插入排序（类似于摸扑克牌）</a></h4><p>从第一个元素开始，该元素认为已经被排序了，取出下一个元素。在已经排序的元素序列中从后向前扫描，如果大于新元素，那么就把这个元素移动到下一个位置。直到找到已排序的元素小于或者等于新元素的位置，将新元素插入下一个位置。依次进行。（其实就是最开头的元素当作是有序数列，后面的元素是无序的，然后从第一个开始往前面插入）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">insertSort</span>(<span class="params">arr</span>) </span>&#123;</span><br><span class="line">  <span class="comment">//插入排序</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">1</span>; j &lt; arr.length; j++) &#123;</span><br><span class="line">    <span class="keyword">let</span> key = arr[j];</span><br><span class="line">    i = j - <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span> (i &gt;= <span class="number">0</span> &amp;&amp; arr[i] &gt; key) &#123;</span><br><span class="line">      arr[i + <span class="number">1</span>] = arr[i];</span><br><span class="line">      i--;</span><br><span class="line">    &#125;</span><br><span class="line">    arr[i + <span class="number">1</span>] = key;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> arr;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="归并排序"><a href="#归并排序" class="headerlink" title="归并排序"></a>归并排序</h4><p>一种稳定排序方法，将已有序的子序列合并，得到完全有序的序列，即先使每个子序列有序，再使序列段间有序。<br>其实也是二分的思想，只不过是在<strong>二分的基础上，先分段，段内再排，然后把每一段拼接起来</strong>。<br>这篇<a href="https://www.jianshu.com/p/33cffa1ce613" target="_blank" rel="noopener">博客</a>里有非常仔细的图片分析。这个需要新申请一个数组来做，所以自然是O(n)的空间复杂度啦</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">mergeSort</span>(<span class="params">arr</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 从上到下递归</span></span><br><span class="line">  <span class="keyword">let</span> len = arr.length</span><br><span class="line">  <span class="keyword">if</span> (len &lt; <span class="number">2</span>) <span class="keyword">return</span> arr</span><br><span class="line">  <span class="keyword">let</span> middle = <span class="built_in">Math</span>.floor(len / <span class="number">2</span>)</span><br><span class="line">  <span class="keyword">let</span> left = arr.slice(<span class="number">0</span>, middle)</span><br><span class="line">  <span class="keyword">let</span> right = arr.slice(middle)</span><br><span class="line">  <span class="keyword">return</span> merge(mergeSort(left), mergeSort(right))</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">merge</span>(<span class="params">left, right</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> result = []</span><br><span class="line">  <span class="keyword">while</span> (left.length &amp;&amp; right.length) &#123;</span><br><span class="line">    <span class="keyword">if</span> (left[<span class="number">0</span>] &lt;= right[<span class="number">0</span>]) &#123;</span><br><span class="line">      result.push(left.shift())</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      result.push(right.shift())</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">while</span> (left.length) &#123;</span><br><span class="line">    result.push(left.shift())</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">while</span> (right.length) &#123;</span><br><span class="line">    result.push(right.shift())</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> result</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="堆排序"><a href="#堆排序" class="headerlink" title="堆排序"></a>堆排序</h4><p>见后面堆数据结构</p>
<h3 id="栈"><a href="#栈" class="headerlink" title="栈"></a>栈</h3><p>栈是一个线性结构，特点是只能在某一端添加或删除数据，遵循先进后出的原则。</p>
<blockquote>
<p>实现：可以把栈看成是数组的一个子集，所以这里使用数组来实现</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Stack</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">this</span>.stack = []</span><br><span class="line">  &#125;</span><br><span class="line">  push(item) &#123;</span><br><span class="line">    <span class="keyword">this</span>.stack.push(item)</span><br><span class="line">  &#125;</span><br><span class="line">  pop() &#123;</span><br><span class="line">    <span class="keyword">this</span>.stack.pop()</span><br><span class="line">  &#125;</span><br><span class="line">  peek() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.stack[<span class="keyword">this</span>.getCount() - <span class="number">1</span>]</span><br><span class="line">  &#125;</span><br><span class="line">  getCount() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.stack.length</span><br><span class="line">  &#125;</span><br><span class="line">  isEmpty() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.getCount() === <span class="number">0</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="队列"><a href="#队列" class="headerlink" title="队列"></a>队列</h3><p>队列是一个线性结构，特点是在某一端添加数据，在另一端删除数据，遵循先进先出的原则</p>
<blockquote>
<p>单链队列的实现：用数组</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Queue</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">this</span>.queue = []</span><br><span class="line">  &#125;</span><br><span class="line">  enQueue(item) &#123;</span><br><span class="line">    <span class="keyword">this</span>.queue.push(item)</span><br><span class="line">  &#125;</span><br><span class="line">  deQueue() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.queue.shift()</span><br><span class="line">  &#125;</span><br><span class="line">  getHeader() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.queue[<span class="number">0</span>]</span><br><span class="line">  &#125;</span><br><span class="line">  getLength() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.queue.length</span><br><span class="line">  &#125;</span><br><span class="line">  isEmpty() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.getLength() === <span class="number">0</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>因为单链队列在出队操作的时候需要 O(n) 的时间复杂度，所以引入了<strong>循环队列</strong>（也是LeetCode-622题目）。循环队列的出队操作平均是 O(1) 的时间复杂度。循环队列的一个好处是我们可以利用这个队列之前用过的空间。在一个普通队列里，一旦一个队列满了，我们就不能插入下一个元素，即使在队列前面仍有空间。但是使用循环队列，我们能使用这些空间去存储新的值。<br>可以看视频加深理解，实现代码如下：</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyCircularQueue</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(k) &#123;</span><br><span class="line">	<span class="comment">//  创建一个长度为k的数组</span></span><br><span class="line">    <span class="keyword">this</span>.list = <span class="built_in">Array</span>(k)</span><br><span class="line">    <span class="comment">//  队头指针</span></span><br><span class="line">    <span class="keyword">this</span>.front = <span class="number">0</span></span><br><span class="line">    <span class="comment">//  队尾指针</span></span><br><span class="line">    <span class="keyword">this</span>.rear = <span class="number">0</span></span><br><span class="line">    <span class="comment">//  队列长度</span></span><br><span class="line">    <span class="keyword">this</span>.max = k</span><br><span class="line">  &#125;</span><br><span class="line">  enQueue(num) &#123;</span><br><span class="line">    <span class="keyword">if</span> (isFull()) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">this</span>.list[<span class="keyword">this</span>.rear] = num <span class="comment">//  【在尾指针处入队，记得入队之后尾指针要后移一位】</span></span><br><span class="line">      <span class="keyword">this</span>.rear = (<span class="keyword">this</span>.rear + <span class="number">1</span>) % <span class="keyword">this</span>.max</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  deQueue() &#123; <span class="comment">//  【在头指针处入队，然后同样后移头指针】</span></span><br><span class="line">    <span class="keyword">let</span> v = <span class="keyword">this</span>.list[<span class="keyword">this</span>.front]</span><br><span class="line">    <span class="keyword">this</span>.list[<span class="keyword">this</span>.front] = <span class="string">''</span></span><br><span class="line">    <span class="keyword">this</span>.front = (<span class="keyword">this</span>.front + <span class="number">1</span>) % <span class="keyword">this</span>.max</span><br><span class="line">    <span class="keyword">return</span> v</span><br><span class="line">  &#125;</span><br><span class="line">  isEmpty() &#123; <span class="comment">// Empty是头指针跟尾指针在同个位置，且这个位置是空</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.rear === <span class="keyword">this</span>.front &amp;&amp; !<span class="keyword">this</span>.list[<span class="keyword">this</span>.front]</span><br><span class="line">  &#125;</span><br><span class="line">  isFull() &#123; <span class="comment">// Full是头指针跟尾指针在同个位置，且这个位置不是空</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.rear === <span class="keyword">this</span>.front &amp;&amp; !!<span class="keyword">this</span>.list[<span class="keyword">this</span>.front]</span><br><span class="line">  &#125;</span><br><span class="line">  Front() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.list[<span class="keyword">this</span>.front]</span><br><span class="line">  &#125;</span><br><span class="line">  Rear() &#123; <span class="comment">//  有可能上次入队操作使得尾指针移到了循环的开始位置</span></span><br><span class="line">    <span class="keyword">let</span> rear = <span class="keyword">this</span>.rear - <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.list[rear &lt; <span class="number">0</span> ? <span class="keyword">this</span>.max - <span class="number">1</span> : rear]</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="链表"><a href="#链表" class="headerlink" title="链表"></a>链表</h3><h4 id="链表和数组的区别"><a href="#链表和数组的区别" class="headerlink" title="链表和数组的区别"></a>链表和数组的区别</h4><ul>
<li>链表是链式的存储结构；数组是顺序的存储结构（ 链表通过指针来连接元素与元素，数组则是把所有元素按次序依次存储）</li>
<li>链表的插入删除元素相对数组较为简单，不需要移动元素，但是寻找某个元素较为困难；数组寻找某个元素较为简单，但插入与删除比较复杂， 都需要把其他元素向前或者向后</li>
</ul>
<h4 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h4><ul>
<li>链表是一组由节点组成的集合，链表节点（也称数据元素的节点）又包含数据域（存数据）和指针域（指向下一个节点）。链表的尾元素指向一个null节点（为空）。eg. 可以简写为 Head-&gt;1-&gt;2-&gt;3-&gt;4-&gt;5-&gt;NULL</li>
<li>通常在在第一个节点前设置一个节点，称为头节点（不一定是必要元素，当是空表时可以是空）。其数据域可以无意义也可以存放链表长度等信息</li>
<li>链表结构可以充分利用计算机内存空间，实现灵活的内存动态管理。但是链表失去了数组随机读取的优点，同时链表由于增加了结点的指针域，空间开销比较大</li>
</ul>
<h4 id="单向链表的实现"><a href="#单向链表的实现" class="headerlink" title="单向链表的实现"></a>单向链表的实现</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Node</span> </span>&#123; <span class="comment">// Node类表示节点</span></span><br><span class="line">  <span class="keyword">constructor</span>(v, next) &#123;</span><br><span class="line">    <span class="keyword">this</span>.value = v</span><br><span class="line">    <span class="keyword">this</span>.next = next</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NodeList</span> </span>&#123; <span class="comment">// LinkList类表示链表</span></span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">this</span>.size = <span class="number">0</span></span><br><span class="line">    <span class="comment">// 虚拟头部</span></span><br><span class="line">    <span class="keyword">this</span>.dummyNode = <span class="keyword">new</span> Node(<span class="literal">null</span>, <span class="literal">null</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  checkIndex(index) &#123;</span><br><span class="line">    <span class="keyword">if</span> (index &lt; <span class="number">0</span> || index &gt; <span class="keyword">this</span>.size) <span class="keyword">throw</span> <span class="built_in">Error</span>(<span class="string">'Index error'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  find(header, index, currentIndex) &#123;</span><br><span class="line">    <span class="keyword">if</span> (index === currentIndex) <span class="keyword">return</span> header</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.find(header.next, index, currentIndex + <span class="number">1</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  addNode(v, index) &#123;</span><br><span class="line">    <span class="keyword">this</span>.checkIndex(index)</span><br><span class="line">    <span class="keyword">let</span> pre = find(<span class="keyword">this</span>.dummyNode, index, <span class="number">0</span>)</span><br><span class="line">    pre.next = <span class="keyword">new</span> Node(v, pre.next)</span><br><span class="line">    <span class="keyword">this</span>.size++</span><br><span class="line">    <span class="keyword">return</span> pre.next</span><br><span class="line">  &#125;</span><br><span class="line">  insertNode(v, index) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.addNode(v, index)</span><br><span class="line">  &#125;</span><br><span class="line">  addToFirst(v) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.addNode(v, <span class="number">0</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  addToLast(v) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.find(v, <span class="keyword">this</span>.size)</span><br><span class="line">  &#125;</span><br><span class="line">  removeNode(index, isLast) &#123;</span><br><span class="line">    <span class="keyword">this</span>.checkIndex(index)</span><br><span class="line">    index = isLast ? index - <span class="number">1</span> : index</span><br><span class="line">    <span class="keyword">let</span> prev = <span class="keyword">this</span>.find(<span class="keyword">this</span>.dummyNode, index, <span class="number">0</span>)</span><br><span class="line">    <span class="keyword">let</span> node = prev.next</span><br><span class="line">    prev.next = node.next</span><br><span class="line">    node.next = <span class="literal">null</span></span><br><span class="line">    <span class="keyword">this</span>.size--</span><br><span class="line">    <span class="keyword">return</span> node</span><br><span class="line">  &#125;</span><br><span class="line">  removeFirstNode() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.removeNode(<span class="number">0</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  removeLastNode() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.removeNode(<span class="keyword">this</span>.size, <span class="literal">true</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  getNode(index) &#123;</span><br><span class="line">    <span class="keyword">this</span>.checkIndex(index)</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>.isEmpty()) <span class="keyword">return</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.find(<span class="keyword">this</span>.dummyNode, index, <span class="number">0</span>).next</span><br><span class="line">  &#125;</span><br><span class="line">  isEmpty() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.size === <span class="number">0</span></span><br><span class="line">  &#125;</span><br><span class="line">  getSize() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.size</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="判断是否是环形链表（LeetCode-141）"><a href="#判断是否是环形链表（LeetCode-141）" class="headerlink" title="判断是否是环形链表（LeetCode-141）"></a>判断是否是环形链表（LeetCode-141）</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> (head) =&gt; &#123;</span><br><span class="line">  <span class="comment">//  双指针法(快慢)</span></span><br><span class="line">  <span class="keyword">let</span> fast = head;</span><br><span class="line">  <span class="keyword">let</span> slow = head;</span><br><span class="line">  <span class="keyword">while</span>(fast &amp;&amp; fast.next) &#123;</span><br><span class="line">    fast = fast.next.next;</span><br><span class="line">    slow = slow.next;</span><br><span class="line">    <span class="keyword">if</span> (fast === slow) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="反转链表（LeetCode-206），简单但是面试常问。可以画个图方便理解，需要三个变量（前，中，后）"><a href="#反转链表（LeetCode-206），简单但是面试常问。可以画个图方便理解，需要三个变量（前，中，后）" class="headerlink" title="反转链表（LeetCode-206），简单但是面试常问。可以画个图方便理解，需要三个变量（前，中，后）"></a>反转链表（LeetCode-206），简单但是面试常问。可以画个图方便理解，需要三个变量（前，中，后）</h4><p>注意不要弄混 反转 和 遍历。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction = <span class="function">(<span class="params">head</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 判断下变量边界问题</span></span><br><span class="line">  <span class="keyword">if</span> (!head || !head.next) <span class="keyword">return</span> head</span><br><span class="line">  <span class="comment">// 设置初始为空（因为反转后这里就是尾巴的null）</span></span><br><span class="line">  <span class="keyword">let</span> pre = <span class="literal">null</span></span><br><span class="line">  <span class="keyword">let</span> curr = head</span><br><span class="line">  <span class="keyword">let</span> next</span><br><span class="line">  <span class="keyword">while</span> (curr) &#123;</span><br><span class="line">    next = curr.next</span><br><span class="line">    curr.next = pre</span><br><span class="line">    pre = curr</span><br><span class="line">    curr = next</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> pre <span class="comment">//  因为反转完成后curr在尾部的null，所以是返回pre</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="矩阵"><a href="#矩阵" class="headerlink" title="矩阵"></a>矩阵</h3><p>在leetCode中一般是用二维数组表示，螺旋矩阵是<strong>面试常考题</strong></p>
<h3 id="树"><a href="#树" class="headerlink" title="树"></a>树</h3><p>树的定义：树由一组以边连接的节点组成。一些<strong>概念</strong>：根节点，左子树/右子树，子节点/父节点，叶子节点（最底部的节点），键值，从xx（节点）到xx的路径，树的层次（根节点是第0层），任何一层的节点都可以看做是子树的根<br>树的高度（从下往上）和深度（从上往下）。根节点的深度为1，叶子节点的高度为1<br>层数：根节点为第一层，往下一次递增</p>
<h4 id="二叉树"><a href="#二叉树" class="headerlink" title="二叉树"></a>二叉树</h4><p>一种特殊的树，子节点不超过两个。新增概念：左节点和右节点。</p>
<h4 id="二叉搜索树（查找效率高）"><a href="#二叉搜索树（查找效率高）" class="headerlink" title="二叉搜索树（查找效率高）"></a>二叉搜索树（查找效率高）</h4><p>特殊的二叉树。 每个节点的值都比他的左子树的值大，比右子树的值小。<br>其实已经是一种排序了。 因为二分搜索树的特性，父节点一定比所有左子节点大，比所有右子节点小（因为二叉搜索树就是由一堆数字经过函数之后构造的，所以一定满足这个定义）</p>
<h4 id="给定一个数组，创建对应的二叉树和二叉搜索树，见-常见算法题及剑指offer"><a href="#给定一个数组，创建对应的二叉树和二叉搜索树，见-常见算法题及剑指offer" class="headerlink" title="给定一个数组，创建对应的二叉树和二叉搜索树，见 常见算法题及剑指offer"></a>给定一个数组，创建对应的二叉树和二叉搜索树，见 <a href="https://jacleklm.github.io/2019/10/21/%E5%B8%B8%E8%A7%81%E7%AE%97%E6%B3%95%E9%A2%98%E5%8F%8A%E5%89%91%E6%8C%87offer/" target="_blank" rel="noopener">常见算法题及剑指offer</a></h4><h4 id="二叉搜索树的实现及其他操作"><a href="#二叉搜索树的实现及其他操作" class="headerlink" title="二叉搜索树的实现及其他操作"></a>二叉搜索树的实现及其他操作</h4><ul>
<li><p>二叉搜索树的实现</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Node</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(value) &#123;</span><br><span class="line">    <span class="keyword">this</span>.value = value</span><br><span class="line">    <span class="keyword">this</span>.left = <span class="literal">null</span></span><br><span class="line">    <span class="keyword">this</span>.right = <span class="literal">null</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BST</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">this</span>.root = <span class="literal">null</span></span><br><span class="line">    <span class="keyword">this</span>.size = <span class="number">0</span></span><br><span class="line">  &#125;</span><br><span class="line">  getSize() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.size</span><br><span class="line">  &#125;</span><br><span class="line">  isEmpty() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.size === <span class="number">0</span></span><br><span class="line">  &#125;</span><br><span class="line">  addNode(v) &#123;</span><br><span class="line">    <span class="keyword">this</span>.root = <span class="keyword">this</span>._addChild(<span class="keyword">this</span>.root, v)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 添加节点时，需要比较添加的节点值和当前</span></span><br><span class="line">  <span class="comment">// 节点值的大小</span></span><br><span class="line">  _addChild(node, v) &#123;</span><br><span class="line">    <span class="keyword">if</span> (!node) &#123;</span><br><span class="line">      <span class="keyword">this</span>.size++</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> Node(v)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (node.value &gt; v) &#123;</span><br><span class="line">      node.left = <span class="keyword">this</span>._addChild(node.left, v)</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (node.value &lt; v) &#123;</span><br><span class="line">      node.right = <span class="keyword">this</span>._addChild(node.right, v)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> node</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p><a href="https://jacleklm.github.io/2019/09/26/%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E9%81%8D%E5%8E%86/" target="_blank" rel="noopener">树的遍历</a></p>
<blockquote>
<p>以上的这几种遍历都可以称之为深度遍历，对应的还有种遍历叫做广度遍历，也就是一层层地遍历树。对于广度遍历来说，我们需要利用之前讲过的队列结构来完成</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">breadthTraversal() &#123;</span><br><span class="line">    <span class="keyword">if</span> (!<span class="keyword">this</span>.root) <span class="keyword">return</span> <span class="literal">null</span></span><br><span class="line">    <span class="keyword">let</span> q = []</span><br><span class="line">    <span class="comment">// 将根节点入队</span></span><br><span class="line">    q.push(<span class="keyword">this</span>.root)</span><br><span class="line">    <span class="comment">// 循环判断队列是否为空，为空代表树遍历完毕</span></span><br><span class="line">    <span class="keyword">while</span> (q.length !== <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="comment">// 将队首出队，判断是否有左右子树</span></span><br><span class="line">      <span class="comment">// 有的话，就先左后右入队</span></span><br><span class="line">      <span class="keyword">let</span> n = q.shift()</span><br><span class="line">      <span class="built_in">console</span>.log(n.value)</span><br><span class="line">      <span class="keyword">if</span> (n.left) q.push(n.left)</span><br><span class="line">      <span class="keyword">if</span> (n.right) q.push(n.right)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure></li>
<li><p>寻找最小值或最大数。因为二分搜索树的特性，所以最小值一定在根节点的最左边，最大值相反</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">getMin() &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">this</span>._getMin(<span class="keyword">this</span>.root).value</span><br><span class="line">&#125;</span><br><span class="line">_getMin(node) &#123;</span><br><span class="line">  <span class="keyword">if</span> (!node.left) <span class="keyword">return</span> node</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">this</span>._getMin(node.left)</span><br><span class="line">&#125;</span><br><span class="line">getMax() &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">this</span>._getMax(<span class="keyword">this</span>.root).value</span><br><span class="line">&#125;</span><br><span class="line">_getMax(node) &#123;</span><br><span class="line">  <span class="keyword">if</span> (!node.right) <span class="keyword">return</span> node</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">this</span>._getMin(node.right)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>最复杂的部分：删除节点。因为有三种情况：要删除的节点没有子树；有一条子树；两条子树</p>
<ul>
<li><p>简单的情况：删除最小/最大的节点，下面以删除最小节点为例。对于删除最小节点来说，是不存在第三种情况的，删除最大节点操作是和删除最小节点相反的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">deleteMin() &#123;</span><br><span class="line">  <span class="keyword">this</span>.root = <span class="keyword">this</span>._deleteMin(<span class="keyword">this</span>.root)</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.root)</span><br><span class="line">&#125;</span><br><span class="line">_deleteMin(node) &#123;</span><br><span class="line">  <span class="comment">//  一直递归找到最小节点</span></span><br><span class="line">  <span class="comment">//  找到的时候判断时候有右节点，有的话就return右节点把原来的节点给重写了</span></span><br><span class="line">  <span class="comment">//  没有右节点，也return右节点（null）重写原来节点，相当于删除了</span></span><br><span class="line">  <span class="keyword">if</span> ((node != <span class="literal">null</span>) &amp;&amp; !node.left) <span class="keyword">return</span> node.right</span><br><span class="line">  node.left = <span class="keyword">this</span>._deleteMin(<span class="keyword">this</span>.left)</span><br><span class="line">  <span class="comment">// 最后需要重新维护下节点的 `size`</span></span><br><span class="line">  node.size = <span class="keyword">this</span>._getSize(node.left) + <span class="keyword">this</span>._getSize(node.right) + <span class="number">1</span></span><br><span class="line">  <span class="comment">//  返回根节点</span></span><br><span class="line">  <span class="keyword">return</span> node</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>删除任意节点。思路：取出被删除节点右子树中最小的节点来替换被传出的节点。<br>当遇到这种情况时，需要取出当前节点的后继节点（也就是当前节点右子树的最小节点）来替换需要删除的节点。然后将需要删除节点的左子树赋值给后继结点，右子树删除后继结点后赋值给他。因为二分搜索树的特性，父节点一定比所有左子节点大，比所有右子节点小。那么当需要删除父节点时，势必需要拿出一个比父节点大的节点来替换父节点。这个节点肯定不存在于左子树，必然存在于右子树。然后又需要保持父节点都是比右子节点小的，那么就可以取出右子树中最小的那个节点来替换父节点</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">delect(v) &#123;</span><br><span class="line"><span class="keyword">this</span>.root = <span class="keyword">this</span>._delect(<span class="keyword">this</span>.root, v)</span><br><span class="line">&#125;</span><br><span class="line">_delect(node, v) &#123;</span><br><span class="line">  <span class="keyword">if</span> (!node) <span class="keyword">return</span> <span class="literal">null</span></span><br><span class="line">  <span class="comment">// 寻找的节点比当前节点小，去左子树找</span></span><br><span class="line">  <span class="keyword">if</span> (node.value &lt; v) &#123;</span><br><span class="line">    node.right = <span class="keyword">this</span>._delect(node.right, v)</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (node.value &gt; v) &#123;</span><br><span class="line">    <span class="comment">// 寻找的节点比当前节点大，去右子树找</span></span><br><span class="line">    node.left = <span class="keyword">this</span>._delect(node.left, v)</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// 进入这个条件说明已经找到节点</span></span><br><span class="line">    <span class="comment">// 先判断节点是否拥有拥有左右子树中的一个</span></span><br><span class="line">    <span class="comment">// 是的话，将子树返回出去，这里和 `_delectMin` 的操作一样</span></span><br><span class="line">    <span class="keyword">if</span> (!node.left) <span class="keyword">return</span> node.right</span><br><span class="line">    <span class="keyword">if</span> (!node.right) <span class="keyword">return</span> node.left</span><br><span class="line">    <span class="comment">// 进入这里，代表节点拥有左右子树</span></span><br><span class="line">    <span class="comment">// 先取出当前节点的后继结点，也就是取当前节点右子树的最小值</span></span><br><span class="line">    <span class="keyword">let</span> min = <span class="keyword">this</span>._getMin(node.right)</span><br><span class="line">    <span class="comment">// 取出最小值后，删除最小值</span></span><br><span class="line">    <span class="comment">// 然后把删除节点后的子树赋值给最小值节点</span></span><br><span class="line">    min.right = <span class="keyword">this</span>._delectMin(node.right)</span><br><span class="line">    <span class="comment">// 左子树不动</span></span><br><span class="line">    min.left = node.left</span><br><span class="line">    node = min</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 维护 size</span></span><br><span class="line">  node.size = <span class="keyword">this</span>._getSize(node.left) + <span class="keyword">this</span>._getSize(node.right) + <span class="number">1</span></span><br><span class="line">  <span class="keyword">return</span> node</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<h3 id="堆"><a href="#堆" class="headerlink" title="堆"></a>堆</h3><ul>
<li><p>堆通常是一个可以被看做一棵树的数组对象（有索引），堆可以理解为有特殊特征的二叉树：必须是完全二叉树（n-1层必须是满二叉树），即除了最底层，其他层的节点都被元素填满；任一节点的值是其子树所有节点的最大值或最小值（称为最大堆/最小堆）<br><img src="/img/%E7%AE%97%E6%B3%95/%E5%A0%86.png"></p>
</li>
<li><p>对任一索引为i的节点，其左子节点索引为 <code>2*i + 1</code>，右子节点为<code>2*i + 2</code>，父节点为<code>(i-1) / 2</code></p>
</li>
<li><p>构建最大堆。即把一个普通的二叉树排序好变成一个最大堆。将节点与父节点对比大小，如果比父节点大，就和父节点交换位置（maxHeapify）；遍历所有节点进行该操作之后就能构建一个最大堆。<strong>构建一次最大堆能选出一个最大值</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Heap</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(data) &#123; <span class="comment">//  data应该是一个数组（保存未排序的二叉树）</span></span><br><span class="line">    <span class="keyword">this</span>.data = data</span><br><span class="line">  &#125;</span><br><span class="line">  sort() &#123;</span><br><span class="line">    <span class="keyword">let</span> iArr = <span class="keyword">this</span>.data</span><br><span class="line">    <span class="keyword">let</span> n = iArr.length</span><br><span class="line">    <span class="keyword">if</span> (n &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> iArr</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="built_in">Math</span>.floor(n / <span class="number">2</span>); i &gt;= <span class="number">0</span>; i--) &#123;<span class="comment">// 构建最大堆从最后一个父节点开始循环</span></span><br><span class="line">        Heap.maxHeapify(iArr, i, n)</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">0</span>; j &lt; n; j++) &#123;<span class="comment">//  不断构建最大堆的过程</span></span><br><span class="line">        <span class="comment">//  因为每次构建都会扔掉一个，所以是n-1-j</span></span><br><span class="line">        Heap.swap(iArr, <span class="number">0</span>, n - <span class="number">1</span> - j) </span><br><span class="line">        <span class="comment">//  每次交换位置扔完之后应该是从顶点开始，所以第二个参数是0</span></span><br><span class="line">        Heap.maxHeapify(iArr, <span class="number">0</span>, n - <span class="number">1</span> - j - <span class="number">1</span>)</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> iArr</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 交换两个元素</span></span><br><span class="line">  <span class="keyword">static</span> swap(arr, a, b) &#123;</span><br><span class="line">    <span class="keyword">if</span> (a === b) &#123;</span><br><span class="line">      <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">let</span> c = arr[a]</span><br><span class="line">    arr[a] = arr[b]</span><br><span class="line">    arr[b] = c</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 构建最大堆的过程</span></span><br><span class="line">  <span class="comment">// 堆排序的时候需要把原来的根节点与最右的叶子节点互换位置后扔出数组，这里不是真的扔出去，而是引入了size，对索引大于size的item忽略不操作。</span></span><br><span class="line">  <span class="comment">//  所以整个构建最大堆的过程数组的长度不会变</span></span><br><span class="line">  <span class="keyword">static</span> maxHeapify(Arr, i, size) &#123;</span><br><span class="line">    <span class="comment">// i的左节点（索引），这是堆的固有规律</span></span><br><span class="line">    <span class="keyword">let</span> l = i * <span class="number">2</span> + <span class="number">1</span></span><br><span class="line">    <span class="comment">// i的右节点</span></span><br><span class="line">    <span class="keyword">let</span> r = i * <span class="number">2</span> + <span class="number">2</span></span><br><span class="line">    <span class="keyword">let</span> largest = i</span><br><span class="line">    <span class="comment">// 父节点i和左节点l做比较取最大（此时只是比较并用largest变量保存最大的，并没有真正在数组中交换）</span></span><br><span class="line">    <span class="keyword">if</span> (l &lt;= size &amp;&amp; Arr[l] &gt; Arr[largest]) &#123;</span><br><span class="line">      largest = l</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 父节点i和右节点r做比较取最大（此时只是比较并用largest变量保存最大的，并没有真正在数组中交换）</span></span><br><span class="line">    <span class="keyword">if</span> (r &lt;= size &amp;&amp; Arr[r] &gt; Arr[largest]) &#123;</span><br><span class="line">      largest = r</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 在数组中交换</span></span><br><span class="line">    <span class="keyword">if</span> (largest !== i) &#123;</span><br><span class="line">      Heap.swap(Arr, i, largest)</span><br><span class="line">      <span class="comment">// 交换完之后确保子树也是满足最大堆的条件</span></span><br><span class="line">      Heap.maxHeapify(Arr, largest, size)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>堆排序。在一次构建最大堆结束之后，把根节点与最右的叶子节<strong>互换位置</strong>（swap），然后移除现在这个叶子节点（原来的根节点）。再构建一次最大堆，<strong>即找到了第二大的值</strong> … 直到最后只剩下一个节点，就完成了堆排序。</p>
</li>
<li><p><em>堆查找</em>。见例题超级丑数。题意有点绕</p>
</li>
</ul>
<p>堆和堆栈的概念区别</p>
<h3 id="贪婪算法"><a href="#贪婪算法" class="headerlink" title="贪婪算法"></a>贪婪算法</h3><p>贪心算法（又称贪婪算法，其实是一种思想）是指，在对问题求解时，总是做出在当<strong>前看来是最好的选择</strong>。也就是说，不从整体最优上加以考虑，他所做出的是在某种意义上的局部最优解<br>贪心算法不是对所有问题都能得到整体最优解，关键是贪心策略的选择，选择的贪心策略必须具备无后效性，即某个状态以前的过程不会影响以后的状态，只与当前状态有关</p>
<h3 id="动态规划"><a href="#动态规划" class="headerlink" title="动态规划"></a>动态规划</h3><h4 id="概念-1"><a href="#概念-1" class="headerlink" title="概念"></a>概念</h4><p>借助10级台阶有多少种走法（每次只能走一步或两步）的例子来理解。下面是重要概念：</p>
<ul>
<li>状态转移方程。F(10) = F(9) + F(8)</li>
<li>最优子结构。F(10) = F(9) + F(8)。称F(9)和F(8)是F(10)的最优子结构</li>
<li>边界。往前推导的时候，会发现到F(1)和F(2)的之后就无法继续推导了，称这两者是问题的边界</li>
</ul>
<p>总结：动态规划算法是通过拆分问题，定义问题状态和状态之间的关系，使得问题能够以递推（或者说分治）的方式去解决<br><strong>解法</strong>：先观察题目写出状态转移方程，再判断问题的边界。特别<strong>善于解决不同路径和最短路径的问题</strong><br><strong>具体写法</strong>：构建数组(n+1) / 矩阵，填满已知部分（边界）（填满的过程也可以用for循环 (n+1) ），然后for循环设定好状态转移方程，最后return结构。用这种做法更好，<strong>递归容易超时？</strong><br>见leetcode-62<br>自己的感想：<strong>见两道例题</strong></p>
<p>参考资料<br><a href="https://blog.nowcoder.net/yiizgo" target="_blank" rel="noopener">呓语</a><br><a href="https://github.com/poetries/" target="_blank" rel="noopener">poetries</a><br>《数据结构与算法JS描述》<br><a href="https://coding.imooc.com/class/315.html" target="_blank" rel="noopener">慕课网</a></p>
]]></content>
      <categories>
        <category>Basic knowledge</category>
      </categories>
      <tags>
        <tag>Data structure</tag>
      </tags>
  </entry>
  <entry>
    <title>模块化开发</title>
    <url>/2019/11/07/%E6%A8%A1%E5%9D%97%E5%8C%96/</url>
    <content><![CDATA[<h2 id="模块化的好处"><a href="#模块化的好处" class="headerlink" title="模块化的好处"></a>模块化的好处</h2><p>解决命名冲突、提高代码的复用性和可维护性</p>
<h2 id="模块化的实现方式"><a href="#模块化的实现方式" class="headerlink" title="模块化的实现方式"></a>模块化的实现方式</h2><h3 id="立即执行函数"><a href="#立即执行函数" class="headerlink" title="立即执行函数"></a>立即执行函数</h3><p>在早期，使用立即执行函数实现模块化是常见的手段，通过函数作用域解决了命名冲突、污染全局作用域的问题。少用了</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">globalVariable</span>)</span>&#123;</span><br><span class="line">   globalVariable.test = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;&#125;</span><br><span class="line">   <span class="comment">// ... 声明各种变量、函数都不会污染全局作用域</span></span><br><span class="line">&#125;)(globalVariable)</span><br></pre></td></tr></table></figure>

<h3 id="AMD-和-CMD"><a href="#AMD-和-CMD" class="headerlink" title="AMD 和 CMD"></a>AMD 和 CMD</h3><p>其实这两种实现方式也已经很少见到</p>
<h3 id="CommonJS"><a href="#CommonJS" class="headerlink" title="CommonJS"></a>CommonJS</h3><p>Node.js，webpack<br>每个文件是一个模块，有自己的作用域；在模块内部module变量代表模块本身；<code>module.exports</code>属性代表模块的对外接口（通过写<code>module.exports</code>的属性/其本身来导出数据，<code>require</code>的时候是得到<code>module</code>，用<code>module</code>的属性/其本身来获取数据）</p>
<p>模块中写<code>exports</code>会默认等于<code>module.exports</code>，也可以用<code>exports</code>的属性（不能是本身，会重写exports）来导出数据</p>
<h3 id="ES-Module"><a href="#ES-Module" class="headerlink" title="ES Module"></a>ES Module</h3><p>ES6模块功能主要由两个命令构成：import和export。import命令用于输入其他模块提供的功能。export命令用于规范模块的对外接口</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 输出变量</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">var</span> name = <span class="string">'weiqinl'</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">var</span> year = <span class="string">'2018'</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">// 输出一个对象（推荐）</span></span><br><span class="line"><span class="keyword">var</span> name = <span class="string">'weiqinl'</span></span><br><span class="line"><span class="keyword">var</span> year = <span class="string">'2018'</span></span><br><span class="line"><span class="keyword">export</span> &#123; name, year&#125;</span><br><span class="line"> </span><br><span class="line"> </span><br><span class="line"><span class="comment">// 输出函数或类</span></span><br><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">a, b</span>) </span>&#123;</span><br><span class="line"><span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// export default 命令</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'foo'</span>)</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">// 正常命令</span></span><br><span class="line"><span class="keyword">import</span> &#123; name, year &#125; <span class="keyword">from</span> <span class="string">'./module.js'</span> <span class="comment">//后缀.js不能省略</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">// 如果遇到export default命令导出的模块</span></span><br><span class="line"><span class="keyword">import</span> ed <span class="keyword">from</span> <span class="string">'./export-default.js'</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>Performance</tag>
      </tags>
  </entry>
  <entry>
    <title>框架入门及Vue</title>
    <url>/2019/10/22/%E6%A1%86%E6%9E%B6%E5%85%A5%E9%97%A8%E5%8F%8AVue/</url>
    <content><![CDATA[<h1 id="框架入门及-Vue"><a href="#框架入门及-Vue" class="headerlink" title="框架入门及 Vue"></a>框架入门及 Vue</h1><h2 id="MVC、MVP-与-MVVM"><a href="#MVC、MVP-与-MVVM" class="headerlink" title="MVC、MVP 与 MVVM"></a>MVC、MVP 与 MVVM</h2><h3 id="MVC"><a href="#MVC" class="headerlink" title="MVC"></a>MVC</h3><p>M（Model）：数据保存<br>V（View）：用户页面<br>C（Controller）：业务逻辑<br>所有通信都是单向的。</p>
<ol>
<li>View 传指令到 Controller</li>
<li>Controller 完成业务逻辑后，要求 Model 改变状态</li>
<li>Model 将新的数据发送到 View，用户得到反馈</li>
</ol>
<h3 id="MVP"><a href="#MVP" class="headerlink" title="MVP"></a>MVP</h3><p><img src="/img/%E6%A1%86%E6%9E%B6%E4%B8%8EVue/MVP.png"><br>传统前端开发过程使用的 MVP 设计模式（eg.jQuery）。Model 数据层，View 视图层（DOM 式，负责绘制 UI 元素、与用户进行交互），Presenter 呈现层（处理与用户交互的逻辑）。</p>
<ol>
<li>各部分之间的通信是双向的，View 与 Model 不发生联系，都通过 Presenter 传递，所以 P 是 MVP 模式的核心，P 里的业务逻辑是 M 和 V 之间的中转站。</li>
<li>在 MVP 设计模式中，M 层很远；在编写代码的时候其实都在写 P，而 P 中的代码大部分都是在操作 DOM。写大型项目的时候，其实 P 层中百分之七八十的代码都是在操作 DOM</li>
</ol>
<h3 id="MVVM"><a href="#MVVM" class="headerlink" title="MVVM"></a>MVVM</h3><p><img src="/img/%E6%A1%86%E6%9E%B6%E4%B8%8EVue/MVVM.png"><br>P 层变成了 VM 层，并且 VM 层不需要我们去编写（是 Vue 自带的）</p>
<ol>
<li>在 MVVM 架构下，View 和 Model 之间并没有直接的联系，而是通过 ViewModel 进行交互，Model 和 ViewModel 之间的交互是双向的，因此 View 数据的变化会同步到 Model 中，而 Model 数据的变化也会立即反应到 View 上（VM 层一方面会监听数据的变化来改变视图，另一方面会监听 V 层的事件触发，然后帮我们调用我们写的逻辑代码来改变 M 层的数据）。用 MVVM 开发的时候，最重的一层其实是 M 层。所以可以这么理解，用 jQuery 开发的时候我们是面向 DOM 进行编程，而用 Vue 是面对数据进行开发，这样在编写大型项目的时候能节省很多代码。</li>
<li>ViewModel 通过<strong>双向数据绑定</strong>把 View 层和 Model 层连接了起来，而 View 和 Model 之间的同步工作完全是自动的，无需人为干涉，因此开发者只需关注业务逻辑，不需要手动操作 DOM，不需要关注数据状态的同步问题，复杂的数据状态维护完全由 MVVM 来统一管理。</li>
<li>对于 MVVM 来说，其实最重要的并不是通过双向绑定或者其他的方式将 View 与 ViewModel 绑定起来，<strong>而是通过 ViewModel 将视图中的状态和用户的行为分离出一个抽象，这才是 MVVM 的精髓。</strong></li>
</ol>
<h2 id="多页面应用-and-单页面应用"><a href="#多页面应用-and-单页面应用" class="headerlink" title="多页面应用 and 单页面应用"></a>多页面应用 and 单页面应用</h2><ul>
<li>多页面应用：每一次页面跳转的时候，后台的服务器（后端）都会返回一个新的 html 文档。只经过一个 http 请求，速度快；搜索引擎排名效果好。但是每次切换页面都需要一次 http 请求，所以切换速度慢。</li>
<li>单页面应用：在 Vue 项目中， 实现页面跳转不用<code>&lt;a&gt;</code>，而是<code>&lt;router-link to=xxx&gt;</code>，页面跳转是通过 JS 感知 url 的变化，来先清除页面的组件，再渲染新组件实现的。SEO 只识别 html 的内容，所以 SEO 效果会比较差。但是仍有其他方法解决些缺点</li>
</ul>
<h2 id="基础知识"><a href="#基础知识" class="headerlink" title="基础知识"></a>基础知识</h2><ol>
<li>Vue 实例、模板（在项目中的 App.vue 是一个 Vue 实例，其他的页面和组件其实是模板。Vue 实例的属性可用 $+属性名 加以区分）</li>
<li>Vue 实例的数据用 data 属性保存，在 template 中可用插值表达式插入；时间用 v-on；方法用 method 属性保存</li>
<li>template 内不要放逻辑，可用计算属性 computed 代替；侦听某一个数据的变化并对此作出反应用侦听器 watch</li>
<li>v-if，v-show 用于数据的显示；v-for 用于数据做循环展示</li>
<li>样式绑定 v-bind（有多种语法，eg. 数组语法，对象语法）</li>
</ol>
<h3 id="watch-的用法"><a href="#watch-的用法" class="headerlink" title="watch 的用法"></a>watch 的用法</h3><h4 id="监听多个变量"><a href="#监听多个变量" class="headerlink" title="监听多个变量"></a>监听多个变量</h4><p>监听单个字符串或数字变量的话就直接写就可以了，如果是监听多个，要结合计算属性，把要监听的多个变量封装成一个对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// realName 和 idCard 是data中的两个变量</span></span><br><span class="line">computed: &#123;</span><br><span class="line">  submitElement() &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123;realName, idCard&#125; = <span class="keyword">this</span></span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      realName,</span><br><span class="line">      idCard</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;,</span><br><span class="line">watch: &#123;</span><br><span class="line">  submitElement: &#123;</span><br><span class="line">    handler: <span class="function"><span class="keyword">function</span> (<span class="params">val</span>) </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (val.realName.trim().length &gt; <span class="number">0</span> &amp;&amp; <span class="keyword">this</span>.idCard.trim() &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">this</span>.isSubmit = <span class="literal">true</span></span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">this</span>.isSubmit = <span class="literal">false</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    deep: <span class="literal">true</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="监听对象"><a href="#监听对象" class="headerlink" title="监听对象"></a>监听对象</h4><p>监听变量是字符串或数字的话就直接写就可以了，如果监听的是对象，要 计算属性 + 用 deep: true 实现对对象内部属性的监听，也见深度监听</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">watch:&#123;</span><br><span class="line">  childrens:&#123;</span><br><span class="line">    handler:<span class="function"><span class="keyword">function</span>(<span class="params">val,oldval</span>)</span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(val.name)</span><br><span class="line">    &#125;,</span><br><span class="line">    deep:<span class="literal">true</span><span class="comment">//对象内部的属性监听，也叫深度监听</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">'childrens.name'</span>:<span class="function"><span class="keyword">function</span>(<span class="params">val,oldval</span>)</span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(val+<span class="string">"aaa"</span>)</span><br><span class="line">  &#125;,<span class="comment">//键路径必须加上引号</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="watch-和-updated-的区别"><a href="#watch-和-updated-的区别" class="headerlink" title="watch 和 updated 的区别"></a>watch 和 updated 的区别</h4><ul>
<li>updated 是数据发生变化且界面更新完毕执行；不能监听路由数据；<strong>所有数据</strong>发生变化都会调用（耗费性能）</li>
<li>【网易雷火】所以想实现监听一个变量的变化并在这个变量变化导致的页面更新后做一些事情，只用 updated 是不够的。所以怎么实现到现在我仍旧不知道</li>
</ul>
<h3 id="Vue-生命周期"><a href="#Vue-生命周期" class="headerlink" title="Vue 生命周期"></a>Vue 生命周期</h3><p>Vue 实例从创建到销毁的过程，就是生命周期（有创建、初始化数据、挂载 Dom、渲染、更新销毁等一系列过程）。分为 8 个阶段：创建前/后、载入前/后、更新前/后、销毁前/销毁后。</p>
<ul>
<li>在 beforeCreate 阶段，vue 实例的挂载元素 el 和数据对象 data 都为 undefined，还未初始化。在 created 阶段，vue 实例的数据对象 data 有了，el 还没有</li>
<li>在 beforeMount 阶段，vue 实例的$el 和 data 都初始化了，但还是挂载之前为虚拟的 dom 节点，data.message 还未替换。在 mounted 阶段，vue 实例挂载完成，data.message 成功<strong>渲染</strong>。</li>
<li>beforeUpdate：数据发生改变页面还没被重新渲染之前</li>
<li>销毁前/后：执行 destory()</li>
</ul>
<p>它的生命周期有多个生命周期钩子，即是某一个时间点会自动执行的函数，借用这些钩子我们能在 Vue 实例的生命周期中写一些逻辑完善我们的需求</p>
<h3 id="组件"><a href="#组件" class="headerlink" title="组件"></a>组件</h3><h4 id="组件注册"><a href="#组件注册" class="headerlink" title="组件注册"></a>组件注册</h4><p>子组件 export default 中写 name 属性；父组件用 import 并写 components 属性即可引用</p>
<h4 id="传值"><a href="#传值" class="headerlink" title="传值"></a>传值</h4><ul>
<li>父子组件间的传值<ul>
<li>父传给子：父绑定属性的方式传给子，子用 props 方法接收</li>
<li>子传给父：$emit 来传递自定义事件和参数，父通过监听这个自定义的事件接收</li>
</ul>
</li>
<li>非父子组件间的传值(兄弟组件传值)<ul>
<li>小型项目用 enentBus（发布订阅模式），就是创建一个事件中心，相当于中转站，可以用它来传递事件和接收事件。在 Vue.prototype.bus=new Vue()。 在 Vue 的原型加一个 bus 属性，指向一个 Vue 实例。这样 Vue 实例都会有这个 bus 属性，而且指向的都是同一个实例。然后在一个组件的 bus 去$emit(), 另一个组件的 bus 的去 on 监听事件</li>
<li>大型项目用 Vuex</li>
</ul>
</li>
</ul>
<h4 id="插槽"><a href="#插槽" class="headerlink" title="插槽"></a>插槽</h4><p><strong>使用场景</strong>：<strong>父组件向子组件传递 DOM</strong>，可用插槽。即是在<strong>父组件中</strong>用某个子组件标签，标签之间就可以写插槽，可以写<strong>任何</strong>模板代码（可以是一点 html 代码，甚至可以引用另外一个组件）。然后在<strong>子组件中</strong>这部分内容就用<code>&lt;slot&gt;</code>代替</p>
<p><strong>具名插槽</strong>：父组件中写一个 slot 属性。子组件写成<code>&lt;slot name = &#39;该属性&#39;&gt;</code> 即可。就能实现传递多个插槽<br><strong>作用域插槽</strong>：子组件 v-for 一个<code>&lt;slot&gt;</code>，则这个插槽必须是作用域插槽。在父组件写法会有不同</p>
<h3 id="Vue-中操作-DOM"><a href="#Vue-中操作-DOM" class="headerlink" title="Vue 中操作 DOM"></a>Vue 中操作 DOM</h3><p>先在 DOM 元素节点/组件上加 ref 属性和属性值，然后 Vue 实例中用 vm.$refs.属性值 就能引用这个 DOM 节点。</p>
<h3 id="Vue-中的动画"><a href="#Vue-中的动画" class="headerlink" title="Vue 中的动画"></a>Vue 中的动画</h3><p><strong>不太熟练</strong></p>
<h4 id="CSS-动画"><a href="#CSS-动画" class="headerlink" title="CSS 动画"></a>CSS 动画</h4><ul>
<li>transition</li>
<li>animation<ul>
<li>自定义</li>
<li>用 Animate.css 库。ink src 引入 css 库，在<tiansition>中写 enter-active-class=类名，类名开头必须是 animated，然后再写具体哪个动画</tiansition></li>
</ul>
</li>
</ul>
<h4 id="JS-动画：用-Velocity-js"><a href="#JS-动画：用-Velocity-js" class="headerlink" title="JS 动画：用 Velocity.js"></a>JS 动画：用 Velocity.js</h4><h2 id="Vue-cli"><a href="#Vue-cli" class="headerlink" title="Vue-cli"></a>Vue-cli</h2><p>在真实的 Vue 项目开发过程中，我们会借助 webpack 构建大型项目的开发目录，然后在开发完成之后进行打包，把代码打包生成一个线上可运行的最终代码。每个开发人员自己去配置 webpack 的开发环境是挺难的，所以有 Vue-cli 脚手架工具，用来快速构建 Vue 项目，这个脚手架自带了 webpack 的配置</p>
<h2 id="Vue-router"><a href="#Vue-router" class="headerlink" title="Vue-router"></a>Vue-router</h2><p>路由就是根据网址的不同，返回不同组件给用户。组件中的<code>&lt;router-view&gt;</code>显示的是当前路由地址所对应的内容。在 router/index.js 中定义当用户访问某个路径 path 时候，给用户展示某个组件</p>
<h3 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a>基本用法</h3><p>在 router/index.js 中定义当用户访问某个路径 path 时候，给用户展示某个组件<br>在组件中用标签<code>&lt;router-link to=&quot;/&quot; tag=&quot;div&quot;&gt;</code></p>
<h3 id="动态路由匹配"><a href="#动态路由匹配" class="headerlink" title="动态路由匹配"></a><a href="https://router.vuejs.org/zh/guide/essentials/dynamic-matching.html#%E5%93%8D%E5%BA%94%E8%B7%AF%E7%94%B1%E5%8F%82%E6%95%B0%E7%9A%84%E5%8F%98%E5%8C%96" target="_blank" rel="noopener">动态路由匹配</a></h3><p>例如，我们有一个 User 组件，对于所有 ID 各不相同的用户，都要使用这个组件来渲染。那么，我们可以在 vue-router 的路由路径中使用“动态路径参数”(dynamic segment) 来达到这个效果。 把配置文件中的 path 写成<code>path: &#39;/user/:id&#39; // 动态路径参数 以冒号开头</code></p>
<h3 id="编程式导航"><a href="#编程式导航" class="headerlink" title="编程式导航"></a><a href="https://router.vuejs.org/zh/guide/essentials/navigation.html" target="_blank" rel="noopener">编程式导航</a></h3><p>页面跳转还可以用编程式导航。在 Vue 实例内部，你可以通过 $router 访问路由实例。因此你可以调用 this.$router.push(location, onComplete?, onAbort?)</p>
<h3 id="命名路由"><a href="#命名路由" class="headerlink" title="命名路由"></a>命名路由</h3><p>通过 name 来标识一个路由，而不是路径。在 index.js 配置中加 name 属性后，可用传对象访问<code>&lt;router-link ：to=&quot;{ name: &#39;user&#39;, params: { userId: 123 }}&quot; tag=&quot;div&quot;&gt;</code></p>
<h3 id="重定向和别名"><a href="#重定向和别名" class="headerlink" title="重定向和别名"></a>重定向和别名</h3><p>“重定向”的意思是，当用户访问 /a 时，URL 将会被替换成 /b，然后匹配路由为 /b，在配置中加 redirect 属性即可<br>“别名”的意思是， /a 的别名是 /b，意味着，当用户访问 /b 时，URL 会保持为 /b，但是路由匹配则为 /a，就像用户访问 /a 一样。加 alias 属性即可</p>
<h3 id="路由懒加载"><a href="#路由懒加载" class="headerlink" title="路由懒加载"></a><a href="https://router.vuejs.org/zh/guide/advanced/lazy-loading.html" target="_blank" rel="noopener">路由懒加载</a></h3><p>在组件中引用组件的时候，如果子组件很多，我们可以用懒加载实现按需加载。<br>在引用组件的时候写成<code>const Foo = () =&gt; import(&#39;./Foo.vue&#39;)</code>，router 的配置文件不用改</p>
<h3 id="导航守卫"><a href="#导航守卫" class="headerlink" title="导航守卫"></a><a href="https://router.vuejs.org/zh/guide/advanced/navigation-guards.html#%E5%85%A8%E5%B1%80%E5%89%8D%E7%BD%AE%E5%AE%88%E5%8D%AB" target="_blank" rel="noopener">导航守卫</a></h3><p>导航守卫主要用来通过跳转或取消的方式守卫导航，其实是<strong>vue 路由的钩子函数</strong>，是写在 router 的配置文件中<br>首页可以控制导航跳转，beforeEach，afterEach 等，一般用于页面 title 的修改。一些需要登录才能调整页面的重定向功能。</p>
<ul>
<li>beforeEach 主要有 3 个参数 to，from，next。</li>
<li>to：route 即将进入的目标路由对象。</li>
<li>from：route 当前导航正要离开的路由。</li>
<li>next：function 一定要调用该方法 resolve 这个钩子。执行效果依赖 next 方法的调用参数。可以控制网页的跳转</li>
</ul>
<h3 id="keep-alive"><a href="#keep-alive" class="headerlink" title="keep-alive"></a>keep-alive</h3><p>在<code>&lt;router-view&gt;</code>外包裹<code>&lt;keep-alive&gt;</code>，能实现路由的内容（几个组件）被加载过一次之后，就把路由中的内容放在内存之中，下次再进路由的时候不需要重新渲染组件，在内存中并显示就可以。<br>此外该标签可以加个 exclude=”Detail”，就是进入 Detail 组件的时候 kepp-alive 不会生效</p>
<h2 id="Vuex（看到官方文档项目结构结束就差不多了）"><a href="#Vuex（看到官方文档项目结构结束就差不多了）" class="headerlink" title="Vuex（看到官方文档项目结构结束就差不多了）"></a><a href="https://vuex.vuejs.org/zh/" target="_blank" rel="noopener">Vuex</a>（看到官方文档项目结构结束就差不多了）</h2><p>Vuex 是 Vue 官方推荐的数据框架，能用于不同组件间的数据共享（一般用来非父子组件的数据共享）<br>可以理解为储存公用数据的仓库（store），各个组件都能一起共享数据。<br><img src="/img/%E6%A1%86%E6%9E%B6%E4%B8%8EVue/Vuex.png"></p>
<ul>
<li>actioin 是放异步操作</li>
<li>mutations 是放同步的对数据的改变</li>
<li>state 是驱动应用的数据源</li>
</ul>
<h3 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h3><p>在/src 目录下新建 store/index.js（可以继续新建 mutations.js, state.js 导入 index.js 中），在 src/main.js import 这个文件<br>此时在组件中可以不用绑定属性不用 props 了，直接用 Vuex 的 mapState，mapMutations 等简化写法<br>可以结合 localStorage 保存一些数据</p>
<ul>
<li>store.Vuex 使用单一状态树,即每个应用将仅仅包含一个 store 实例，但单一状态树和模块化并不冲突。存放的数据状态，不可以直接修改里面的数据</li>
<li>mutations 定义的方法动态修改 Vuex 的 store 中的状态或数据</li>
<li>actions 可以理解为通过将 mutations 里面处里数据的方法变成可异步的处理数据的方法，简单的说就是异步操作数据。view 层通过 store.dispath 来分发 action</li>
<li>getter.类似 vue 的计算属性，主要用来过滤一些数据</li>
<li>module.项目特别复杂的时候，可以让每一个模块拥有自己的 state、mutation、action、getters，使得结构非常清晰，方便管理</li>
</ul>
<h2 id="Mock-数据"><a href="#Mock-数据" class="headerlink" title="Mock 数据"></a>Mock 数据</h2><ul>
<li>static 目录建立 mock 文件夹（因为在整个工程里，只有 static 目录能被外部访问）</li>
<li>proxy 转发。让对 api/xx.json 的请求转到 mock/xx.json</li>
<li>Vue 是推荐用<strong>axios</strong>第三方模块来获取 Ajax 数据，安装之后就可以用 axios.get()方法（其实类似于 fetch）。写好方法后让方法在 mounted 的时候执行</li>
</ul>
<h1 id="一些功能的原理"><a href="#一些功能的原理" class="headerlink" title="一些功能的原理"></a>一些功能的原理</h1><h2 id="Vue-实现数据双向绑定的原理：Object-defineProperty"><a href="#Vue-实现数据双向绑定的原理：Object-defineProperty" class="headerlink" title="Vue 实现数据双向绑定的原理：Object.defineProperty()"></a>Vue 实现数据双向绑定的原理：Object.defineProperty()</h2><ul>
<li>vue 实现数据双向绑定主要是：采用数据劫持结合观察者模式的方式。Vue 会遍历数据对象的属性，通过 Object.defineProperty() 来劫持各个属性的 setter，getter，在数据变动时发布消息给订阅者，触发相应监听回调</li>
<li>vue 的数据双向绑定 将 MVVM 作为数据绑定的入口，整合 Observer，Compile 和 Watcher 三者，通过 Observer 来监听自己的 model 的数据变化，通过 Compile 来解析编译模板指令（vue 中是用来解析 插值表达式），最终利用 watcher 搭起 observer 和 Compile 之间的通信桥梁，达到数据变化 —&gt;视图更新；视图交互变化（input）—&gt;数据 model 变更双向绑定效果</li>
</ul>
<h3 id="手写实现双向数据绑定"><a href="#手写实现双向数据绑定" class="headerlink" title="手写实现双向数据绑定"></a>手写实现双向数据绑定</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//双向数据绑定</span></span><br><span class="line"><span class="keyword">let</span> obj = &#123;&#125;</span><br><span class="line"><span class="keyword">let</span> input = <span class="built_in">document</span>.getElementById(<span class="string">'input'</span>)</span><br><span class="line"><span class="keyword">let</span> span = <span class="built_in">document</span>.getElementById(<span class="string">'span'</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">//数据劫持</span></span><br><span class="line"><span class="built_in">Object</span>.defineProperty(obj, <span class="string">'text'</span>, &#123;</span><br><span class="line">  configurable: <span class="literal">true</span>,</span><br><span class="line">  enumerable: <span class="literal">true</span>,</span><br><span class="line">  <span class="keyword">get</span>() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'获取数据'</span>)</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="keyword">set</span>(newVal) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'数据更新'</span>)</span><br><span class="line">    input.value = newVal <span class="comment">// 要双向绑定所以有这句</span></span><br><span class="line">    span.innerHTML = newVal</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">//监听</span></span><br><span class="line">input.addEventListener(<span class="string">'keyup'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">  obj.text = e.target.value</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><ul>
<li>监听不到对象属性的增删、数组元素和长度的变化</li>
<li>同时会在 vue 初始化的时候把所有的观察者都建立好，才能观察到数据对象属性的变化</li>
</ul>
<h3 id="Vue3-0-的改进"><a href="#Vue3-0-的改进" class="headerlink" title="Vue3.0 的改进"></a>Vue3.0 的改进</h3><ul>
<li>采用了 ES2015 的 Proxy 来代替 Object.defineProperty，</li>
<li>可以做到监听对象属性的增删和数组元素和长度的修改，还可以监听 Map、Set、WeakSet、WeakMap，</li>
<li>同时还实现了惰性的监听，不会在初始化的时候创建所有的 Observer，而是会在用到的时候才去监听。</li>
<li>但是，虽然主流的浏览器都支持 Proxy，ie 系列却还是不兼容，所以对 ie 还是 Object.defineProperty()</li>
</ul>
<h2 id="v-for-的原理"><a href="#v-for-的原理" class="headerlink" title="v-for 的原理"></a>v-for 的原理</h2><h2 id="computed-的原理"><a href="#computed-的原理" class="headerlink" title="computed 的原理"></a>computed 的原理</h2><ul>
<li>初始化 data， 使用 Object.defineProperty 把这些属性全部转为 getter/setter。</li>
<li>初始化 computed, 遍历 computed 里的每个属性，<strong>每个 computed 属性都是一个 watch 实例</strong>。每个属性提供的函数作为属性的 - getter，使用 Object.defineProperty 转化。</li>
<li>Object.defineProperty getter 依赖收集。用于依赖发生变化时，触发属性重新计算。</li>
<li>若出现当前 computed 计算属性嵌套其他 computed 计算属性时，先进行其他的依赖收集</li>
</ul>
<h2 id="杂"><a href="#杂" class="headerlink" title="杂"></a>杂</h2><h3 id="v-if-与-v-show-的区别"><a href="#v-if-与-v-show-的区别" class="headerlink" title="v-if 与 v-show 的区别"></a>v-if 与 v-show 的区别</h3><p>v-if 按照条件是否渲染，v-show 是 display 的 block 或 none；</p>
<h3 id="v-el-作用"><a href="#v-el-作用" class="headerlink" title="v-el 作用"></a>v-el 作用</h3><p>提供一个在页面上已存在的 DOM 元素作为 Vue 实例的挂载目标.可以是 CSS 选择器，也可以是一个 HTMLElement 实例</p>
<h3 id="vue-的优点是什么？"><a href="#vue-的优点是什么？" class="headerlink" title="vue 的优点是什么？"></a>vue 的优点是什么？</h3><ul>
<li>低耦合。视图（View）可以独立于 Model 变化和修改，一个 ViewModel 可以绑定到不同的”View”上，当 View 变化的时候 Model 可以不变，当 Model 变化的时候 View 也可以不变</li>
<li>可重用性。你可以把一些视图逻辑放在一个 ViewModel 里面，让很多 view 重用这段视图逻辑</li>
<li>可测试。界面素来是比较难于测试的，而现在测试可以针对 ViewModel 来写</li>
</ul>
<h3 id="Vue-组件-data-为什么必须是函数"><a href="#Vue-组件-data-为什么必须是函数" class="headerlink" title="Vue 组件 data 为什么必须是函数"></a>Vue 组件 data 为什么必须是函数</h3><p>每个组件都是 Vue 的实例。组件共享 data 属性，当 data 的值是同一个引用类型的值时，改变其中一个会影响其他</p>
<h3 id="为什么官方建议数据异步请求在-mounted-事件进行"><a href="#为什么官方建议数据异步请求在-mounted-事件进行" class="headerlink" title="为什么官方建议数据异步请求在 mounted 事件进行"></a>为什么官方建议数据异步请求在 mounted 事件进行</h3><p>请求是需要时间的，而且这个时间具有不稳定性，很可能 vue 的虚拟 DOM 准备好了，你的数据才请求到，然后又得更新一遍虚拟 DOM，再渲染，极大地延长了白屏时间，用户体验很不好。而在 mounted 事件请求数据呢，静态页面会先渲染好，等数据好了，再更新部分 DOM 即可</p>
<p>参考资料<br><a href="https://blog.nowcoder.net/yiizgo" target="_blank" rel="noopener">呓语</a><br><a href="https://github.com/poetries/" target="_blank" rel="noopener">poetries</a><br>Vue，Vue-cli，Vue-router，Vuex 的官方文档</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>框架</tag>
      </tags>
  </entry>
  <entry>
    <title>洗牌算法</title>
    <url>/2020/03/08/%E6%B4%97%E7%89%8C%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<blockquote>
<p>如何真正的打乱数组?</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line">arr.sort(<span class="function"><span class="params">()</span> =&gt;</span> <span class="built_in">Math</span>.random() - <span class="number">0.5</span>)</span><br></pre></td></tr></table></figure>

<p>上述代码看似可以打乱，但是不是真正的打乱。做下测试：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> RandomShuffle = <span class="function">(<span class="params">arr</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> arr.sort(<span class="function"><span class="params">()</span> =&gt;</span> <span class="built_in">Math</span>.random() - <span class="number">0.5</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">const</span> testResult = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> times = <span class="number">100000</span></span><br><span class="line">  <span class="keyword">let</span> res = &#123;&#125;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; times; i++) &#123;</span><br><span class="line">    <span class="keyword">let</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line">    RandomShuffle(arr)</span><br><span class="line">    <span class="keyword">let</span> key = <span class="built_in">JSON</span>.stringify(arr)</span><br><span class="line">    res[key] ? res[key]++ : (res[key] = <span class="number">1</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 为了方便展示，转换成百分比</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> key <span class="keyword">in</span> res) &#123;</span><br><span class="line">    res[key] = (res[key] / times) * <span class="number">100</span> + <span class="string">'%'</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">console</span>.log(res)</span><br><span class="line">&#125;</span><br><span class="line">testResult()</span><br><span class="line"><span class="comment">// 结果</span></span><br><span class="line">[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]: <span class="string">"37.66%"</span></span><br><span class="line">[<span class="number">2</span>,<span class="number">1</span>,<span class="number">3</span>]: <span class="string">"12.57%"</span></span><br><span class="line">[<span class="number">1</span>,<span class="number">3</span>,<span class="number">2</span>]: <span class="string">"6.109%"</span></span><br><span class="line">[<span class="number">3</span>,<span class="number">1</span>,<span class="number">2</span>]: <span class="string">"6.343999999999999%"</span></span><br><span class="line">[<span class="number">2</span>,<span class="number">3</span>,<span class="number">1</span>]: <span class="string">"6.22%"</span></span><br><span class="line">[<span class="number">3</span>,<span class="number">2</span>,<span class="number">1</span>]: <span class="string">"31.097%"</span></span><br></pre></td></tr></table></figure>

<p>可以看出末尾数字是 3 的概率为 50%，而不是想象中的 33%。原因是因为 sort 函数底层是使用插入排序实现的。所以初始为[1,2,3]的时候，插入排序从 2 开始，和前面做比较，此时有 50%的几率顺序 50%的几率逆序，所以[1,2,3][2,1,3]出现几率都为 50%。接下来的操作如下图所示：<br><img src="/img/%E6%B4%97%E7%89%8C%E7%AE%97%E6%B3%95/%E6%B4%97%E7%89%8C%E7%AE%97%E6%B3%95.png"></p>
<blockquote>
<p>Knuth-Durstenfeld 洗牌算法</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> KnuthDurstenfeldShuffle = <span class="function"><span class="params">arr</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = arr.length - <span class="number">1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">    <span class="keyword">let</span> randomIndex = <span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * (i + <span class="number">1</span>))</span><br><span class="line">    <span class="comment">// 用结构赋值两项交换之前最好加 ;</span></span><br><span class="line">    ;[arr[i], arr[randomIndex]] = [arr[randomIndex], arr[i]]</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> arr</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>思路：</p>
<ul>
<li>选取数组(长度 n)中最后一个元素(arr[length-1])，将其与 n 个元素中的任意一个交换，此时最后一个元素已经确定</li>
<li>选取倒数第二个元素(arr[length-2])，将其与 n-1 个元素中的任意一个交换</li>
<li>重复第 1 2 步，直到剩下 1 个元素为止</li>
</ul>
<blockquote>
<p>Fisher-Yates 洗牌算法</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 似乎有误</span></span><br><span class="line"><span class="keyword">const</span> FisherYatesShuffle = <span class="function"><span class="params">arr</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> result = [];</span><br><span class="line">  <span class="keyword">while</span> (arr.length &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">let</span> randomIndex = <span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * arr.length)</span><br><span class="line">    result.push(arr[randomIndex])</span><br><span class="line">    arr.splice(randomIndex, <span class="number">1</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> result</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 结果</span></span><br><span class="line">[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]: <span class="string">"16.646%"</span></span><br><span class="line">[<span class="number">1</span>,<span class="number">3</span>,<span class="number">2</span>]: <span class="string">"16.633%"</span></span><br><span class="line">[<span class="number">2</span>,<span class="number">1</span>,<span class="number">3</span>]: <span class="string">"16.55%"</span></span><br><span class="line">[<span class="number">2</span>,<span class="number">3</span>,<span class="number">1</span>]: <span class="string">"16.787%"</span></span><br><span class="line">[<span class="number">3</span>,<span class="number">1</span>,<span class="number">2</span>]: <span class="string">"16.662%"</span></span><br><span class="line">[<span class="number">3</span>,<span class="number">2</span>,<span class="number">1</span>]: <span class="string">"16.722%"</span></span><br></pre></td></tr></table></figure>

<p>思路：</p>
<ul>
<li>从还没处理的数组（假如还剩 n 个）中，产生一个[0, n]之间的随机数 random</li>
<li>从剩下的 n 个元素中把第 random 个元素取出到新数组中</li>
<li>删除原数组第 random 个元素</li>
<li>重复第 2 3 步直到所有元素取完</li>
<li>最终返回一个新的打乱的数组</li>
</ul>
<p>参考资料<br><a href="https://dannykbsoul.github.io/2020/03/03/%E6%B4%97%E7%89%8C%E7%AE%97%E6%B3%95/" target="_blank" rel="noopener">洗牌算法</a><br><a href="https://coolshell.cn/articles/8593.html" target="_blank" rel="noopener">如何测试洗牌程序</a><br><a href="https://github.com/ccforward/cc/issues/44" target="_blank" rel="noopener">洗牌算法(shuffle)的 js 实现</a></p>
]]></content>
      <categories>
        <category>Basic knowledge</category>
      </categories>
      <tags>
        <tag>Data structure</tag>
      </tags>
  </entry>
  <entry>
    <title>博客项目小结</title>
    <url>/2020/01/29/%E5%8D%9A%E5%AE%A2%E9%A1%B9%E7%9B%AE%E5%B0%8F%E7%BB%93/</url>
    <content><![CDATA[<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><h2 id="来源"><a href="#来源" class="headerlink" title="来源"></a>来源</h2><p>该项目是<a href="https://jspang.com/" target="_blank" rel="noopener">技术胖</a>的一个项目，涉及了前台中台后台，觉得是个不错的练手项目.</p>
<h2 id="概述-1"><a href="#概述-1" class="headerlink" title="概述"></a>概述</h2><p>博客前台： React Hooks + Next.js + marked + highlight + Ant Design<br>数据中台： Egg.js（阿里的一个基于 Koa2 的框架） + MySQL<br>管理后台： React Hooks + Ant Design （不用 Next.js）</p>
<blockquote>
<p>Next.js 是一个轻量级的 React 服务端渲染应用框架。有了它我们可以简单轻松的实现 React 的服务端渲染，从而加快首屏打开速度，也可以作 SEO（搜索引擎优化了）。并且路由，webpack 配置等也是框架配置好的。<br>个人看法：配置太全了，自己写着玩的项目还好，企业级的项目用起来可能不够灵活。</p>
</blockquote>
<h1 id="前台"><a href="#前台" class="headerlink" title="前台"></a>前台</h1><h2 id="布局"><a href="#布局" class="headerlink" title="布局"></a>布局</h2><p>组件内的布局基本都是用 Antd 的 栅格-响应式布局 ，<code>&lt;Col&gt;</code> 的 xs，sm 等这些属性本质上是媒介查询；给 <code>&lt;Row&gt;</code> 添加属性实现 flex 布局 <code>type=&quot;flex&quot; justify=&quot;center&quot;</code></p>
<p>组件一般都是用 Antd 的组件组合而成</p>
<h2 id="请求数据"><a href="#请求数据" class="headerlink" title="请求数据"></a>请求数据</h2><p>数据来源于请求中台接口</p>
<h3 id="函数组件的-getInitialProps-方法写一个-promise-并返回-promise-的结果（Next-js-框架）"><a href="#函数组件的-getInitialProps-方法写一个-promise-并返回-promise-的结果（Next-js-框架）" class="headerlink" title="函数组件的 getInitialProps 方法写一个 promise 并返回 promise 的结果（Next.js 框架）"></a>函数组件的 <code>getInitialProps</code> 方法写一个 promise 并返回 promise 的结果（Next.js 框架）</h3><p><a href="https://nextjs.frontendx.cn/docs/#%E8%8E%B7%E5%8F%96%E6%95%B0%E6%8D%AE%E4%BB%A5%E5%8F%8A%E7%BB%84%E4%BB%B6%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F" target="_blank" rel="noopener">文档</a><br>该方法返回的应该是一个普通的 JS 对象；不能用于子组件中，只能用于 pages 页面组件中；该方法只会加载在服务端，所以在该方法中 console 是不会显示的；</p>
<p>该方法用于一开始就请求一次的数据（之后不用请求了），然后 promise 的 resolve 的结果这个对象，会变成函数组件的 props 对象。例子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函数组件</span></span><br><span class="line"><span class="keyword">const</span> Home = <span class="function"><span class="params">props</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> [mylist, setMylist] = useState(props.data)</span><br><span class="line">  <span class="comment">// ...组件逻辑</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 函数组件外</span></span><br><span class="line">Home.getInitialProps = <span class="keyword">async</span> () =&gt; &#123;</span><br><span class="line">  <span class="keyword">const</span> promise = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="params">resolve</span> =&gt;</span> &#123;</span><br><span class="line">    axios(servicePath.getArticleList).then(<span class="function"><span class="params">res</span> =&gt;</span> &#123;</span><br><span class="line">      resolve(res.data) <span class="comment">// resovle()的结果这个对象变成上面的props对象</span></span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">await</span> promise</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这种写法其实和写在最普通的 useEffect 的写法效果一样</p>
<h3 id="Hook"><a href="#Hook" class="headerlink" title="Hook"></a>Hook</h3><h2 id="跨域"><a href="#跨域" class="headerlink" title="跨域"></a>跨域</h2><p>中台安装 egg-cors ，并做相应配置。本质是设置 Access-Control-Allow-Origin</p>
<h3 id="跨域白名单"><a href="#跨域白名单" class="headerlink" title="跨域白名单"></a>跨域白名单</h3><p><em>Egg.js 的跨域不支持白名单，只支持单个域名，这样前后台项目不能一起跑。解决方案是需要自己写一个中间件实现白名单，这是别人写的<a href="https://www.jianshu.com/p/fa5f80924e97" target="_blank" rel="noopener">中间件</a>，<a href="https://github.com/temool/egg-origin" target="_blank" rel="noopener">Github</a>，这里直接用了这个中间件</em></p>
<blockquote>
<p>最后线上版本舍弃了这个插件，见博客项目上线部署博文的 跨域</p>
</blockquote>
<h2 id="涉及-id-的页面"><a href="#涉及-id-的页面" class="headerlink" title="涉及 id 的页面"></a>涉及 id 的页面</h2><p>像不同种类的列表页，文章详情页这种涉及 id 的页面，路由设置 和 数据获取 稍微复杂一些</p>
<p>例如，列表页中每篇文章的标题，是一个带 id 的 <code>&lt;Link&gt;</code></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">className</span>=<span class="string">"list-title"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">Link</span> <span class="attr">href</span>=<span class="string">&#123;&#123;</span> <span class="attr">pathname:</span> '/<span class="attr">detailed</span>', <span class="attr">query:</span> &#123; <span class="attr">id:</span> <span class="attr">item.id</span> &#125; &#125;&#125;&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">a</span>&gt;</span>&#123;item.title&#125;<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">Link</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>点击后跳转带详情页，所以详情页的 <code>getInitialProps</code> 获取数据的时候是根据路由的 id 去 post</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">Detailed.getInitialProps = <span class="keyword">async</span> context =&gt; &#123;</span><br><span class="line">  <span class="keyword">let</span> id = context.query.id</span><br><span class="line">  <span class="keyword">const</span> promise = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="params">resolve</span> =&gt;</span> &#123;</span><br><span class="line">    axios(servicePath.getArticleById + id).then(<span class="function"><span class="params">res</span> =&gt;</span> &#123;</span><br><span class="line">      resolve(res.data.data[<span class="number">0</span>])</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">await</span> promise</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对应的接口则是由文章的 id 在数据库查询</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> getArticleById() &#123;</span><br><span class="line">    <span class="keyword">const</span> id = <span class="keyword">this</span>.ctx.params.id;</span><br><span class="line">    <span class="keyword">const</span> sql = <span class="string">'SELECT article.id as id,'</span> +</span><br><span class="line">    <span class="string">'article.title as title,'</span> +</span><br><span class="line">    <span class="string">'article.introduce as introduce,'</span> +</span><br><span class="line">    <span class="string">'article.article_content as article_content,'</span> +</span><br><span class="line">    <span class="string">'article.view_count as view_count ,'</span> +</span><br><span class="line">    <span class="string">'type.typeName as typeName ,'</span> +</span><br><span class="line">    <span class="string">'type.id as typeId '</span> +</span><br><span class="line">    <span class="string">'FROM article LEFT JOIN type ON article.type_id = type.Id '</span> +</span><br><span class="line">    <span class="string">'WHERE article.id='</span> + id;</span><br><span class="line">    <span class="keyword">const</span> result = <span class="keyword">await</span> <span class="keyword">this</span>.app.mysql.query(sql);</span><br><span class="line">    <span class="keyword">this</span>.ctx.body = &#123; <span class="attr">data</span>: result &#125;;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>PS：前端在路由跳转的时候（<code>Router.push()</code> 或 <code>&lt;Link href=‘xx’&gt;</code>)，路由地址最后得是 <code>xx?id=1</code> 这样，而不是 <code>xx/?id=1</code>。但是后端在配置接口的时候依旧是 <code>xx/:id</code></p>
<h2 id="详情页-中对-Markdown-语法的解析"><a href="#详情页-中对-Markdown-语法的解析" class="headerlink" title="详情页 中对 Markdown 语法的解析"></a>详情页 中对 Markdown 语法的解析</h2><p>把从数据库取出来的 markdown 语法的字符串，编译成正常的文章并显示样式，是用 <a href="https://github.com/markedjs/marked" target="_blank" rel="noopener">marked</a> + <a href="https://github.com/highlightjs/highlight.js/" target="_blank" rel="noopener">heighlight.js</a></p>
<h2 id="详情页-中的-导航目录组件"><a href="#详情页-中的-导航目录组件" class="headerlink" title="详情页 中的 导航目录组件"></a>详情页 中的 导航目录组件</h2><p>效果如下所示：<br><img src="/img/%E5%8D%9A%E5%AE%A2%E9%A1%B9%E7%9B%AE%E6%80%BB%E7%BB%93/%E5%AF%BC%E8%88%AA%E7%BB%84%E4%BB%B6.png"><br>把这个组件固定在页面右侧是用了 Antd 的 Affix 固钉 组件，Anchor 锚点</p>
<p>解析文章内容生成目录是用了阿里的一个插件 tocify.tsx</p>
<h1 id="中台"><a href="#中台" class="headerlink" title="中台"></a>中台</h1><h2 id="概况-amp-RESTful-规范"><a href="#概况-amp-RESTful-规范" class="headerlink" title="概况 &amp; RESTful 规范"></a>概况 &amp; RESTful 规范</h2><p>配置了两套路由，<code>/default/</code>开头的用于前台，<code>/admin/</code>开头的用于后台<br>所有数据的获得和业务逻辑的操作都是通过中台实现的，也就是说中台只提供接口，这里的设计我们采用 RESTful 的规则，让 egg 为前端提供 Api 接口，实现中台主要的功能</p>
<blockquote>
<p>RESTful 是目前最流行的网络应用程序设计风格和开发方式，大量使用在移动端 App 上和前后端分离的接口设计。这种形式更直观并且接口也有了一定的约束性。</p>
</blockquote>
<p>约束的请求方式和对应的操作:</p>
<ul>
<li>GET(SELECT) ： 从服务端取出资源，可以同时取出一项或者多项。</li>
<li>POST(CREATE) ：在服务器新建一个资源。</li>
<li>PUT(UPDATE) ：在服务器更新资源（客户端提供改变后的完整资源）。</li>
<li>DELETE(DELETE) ：从服务器删除资源。</li>
</ul>
<h2 id="数据库"><a href="#数据库" class="headerlink" title="数据库"></a>数据库</h2><p>数据库采用的是关系型数据库 MySQL，安装了官方带的 WorkBench 对数据库进行可视化管理。把 Egg.js 项目进行连接数据库的配置后即能使用</p>
<h3 id="数据库设计"><a href="#数据库设计" class="headerlink" title="数据库设计"></a>数据库设计</h3><p>表 和 Cloum 如下：</p>
<ul>
<li><p>type</p>
<ul>
<li>id : 类型编号 int 类型</li>
<li>typeName: 文章类型名称 varchar 类型</li>
<li>orderNum: 类型排序编号 int 类型</li>
</ul>
</li>
<li><p>article</p>
<ul>
<li>id : 文章编号 int 类型</li>
<li>type_id : 文章类型编号 int 类型</li>
<li>title : 文章标题，varchar 类型</li>
<li>article_cointent : 文章主体内容，text 类型， TEXT(n)，这里的 n 是文本长度，可以设置一个较大值</li>
<li>introduce： 文章简介，text 类型</li>
<li>addTime : 文章发布时间，int(11)类型</li>
<li>view_count ：浏览次数， int 类型</li>
</ul>
</li>
<li><p>admin_user</p>
<ul>
<li>id</li>
<li>userName</li>
<li>password</li>
</ul>
</li>
</ul>
<h3 id="数据库语法"><a href="#数据库语法" class="headerlink" title="数据库语法"></a>数据库语法</h3><p>参考<br><a href="https://www.w3school.com.cn/sql/sql_join_left.asp" target="_blank" rel="noopener">W3School</a><br><a href="https://www.cnblogs.com/cathyqq/p/5197626.html" target="_blank" rel="noopener">SQL 时间戳日期时间转换</a></p>
<blockquote>
<p>常用例子</p>
</blockquote>
<ul>
<li>增，直接对某个表进行 insert</li>
<li>删，直接根据 id，对某个表进行 delete</li>
<li>改，直接对某个表进行 udate</li>
<li>查，eg：</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">'SELECT article.id as id, article.title as title, article.article_content as article_content, FROM_UNIXTIME(article.addTime,'</span>%Y-%m-%d<span class="string">' ) as addTime, type.typeName as typeName, FROM article LEFT JOIN type ON article.type_id = type.Id ORDER BY article.addTime DESC'</span>; <span class="comment">// DESC是逆序的意思，ASC是正序`</span></span><br><span class="line">也可以加<span class="string">`'WHERE type_id= 2`</span> 这样的语法</span><br></pre></td></tr></table></figure>

<h1 id="后台"><a href="#后台" class="headerlink" title="后台"></a>后台</h1><p>直接用 React + Antd 写的，不用 Next.js</p>
<p>React 项目中的根目录的 index.js 应该是渲染一个路由组件（自己配的），由这个路由组件再去加载其他页面组件。例如路由组件可以如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> React <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"><span class="keyword">import</span> &#123; BrowserRouter <span class="keyword">as</span> Router, Route &#125; <span class="keyword">from</span> <span class="string">'react-router-dom'</span></span><br><span class="line"><span class="keyword">import</span> Login <span class="keyword">from</span> <span class="string">'./Login'</span></span><br><span class="line"><span class="keyword">import</span> AdminIndex <span class="keyword">from</span> <span class="string">'./AdminIndex'</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Main</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    &lt;Router&gt;</span><br><span class="line">      &lt;Route path=<span class="string">"/login/"</span> exact component=&#123;Login&#125; /&gt;</span><br><span class="line">      &lt;Route path=<span class="string">"/index/"</span> component=&#123;AdminIndex&#125; /&gt;</span><br><span class="line">    &lt;<span class="regexp">/Router&gt;</span></span><br><span class="line"><span class="regexp">  )</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br><span class="line"><span class="regexp">export default Main</span></span><br></pre></td></tr></table></figure>

<h2 id="登录页面"><a href="#登录页面" class="headerlink" title="登录页面"></a>登录页面</h2><h3 id="样式"><a href="#样式" class="headerlink" title="样式"></a>样式</h3><p>样式上比较简单，是由 Antd 的 Card, Input, Icon, Button, Spin, message 组件写的</p>
<h3 id="逻辑"><a href="#逻辑" class="headerlink" title="逻辑"></a>逻辑</h3><p>目前没有 注册 这个操作，用户名密码及 id 是采用直接在 workbench 插入新数据的方式注册<br>点击登录按钮的时候，向接口 post 用户名和密码，向数据库查询这个用户名和密码。如果登录成功就返回登录成功的信息和一个 token，组件就把 token 存到 localstorage 中（<code>localStorage.setItem(&#39;openId&#39;, res.data.openId)</code>），并跳转到首页（<code>props.history.push(&#39;/index&#39;)</code>）</p>
<p>附上接口代码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 判断用户名密码是否正确</span></span><br><span class="line">  <span class="keyword">async</span> checkLogin() &#123;</span><br><span class="line">    <span class="keyword">const</span> userName = <span class="keyword">this</span>.ctx.request.body.userName;</span><br><span class="line">    <span class="keyword">const</span> password = <span class="keyword">this</span>.ctx.request.body.password; <span class="comment">// 这里也可以进行加密和解密</span></span><br><span class="line">    <span class="keyword">const</span> sql = <span class="string">" SELECT userName FROM admin_user WHERE userName = '"</span> + userName + <span class="string">"' AND password = '"</span> + password + <span class="string">"'"</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> res = <span class="keyword">await</span> <span class="keyword">this</span>.app.mysql.query(sql);</span><br><span class="line">    <span class="keyword">if</span> (res.length &gt; <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="comment">// 登录成功,进行session缓存</span></span><br><span class="line">      <span class="keyword">const</span> openId = <span class="keyword">new</span> <span class="built_in">Date</span>().getTime(); <span class="comment">// 传给前端的token，用当前时间的时间戳做token</span></span><br><span class="line">      <span class="keyword">this</span>.ctx.session.openId = &#123; openId &#125;; <span class="comment">// 并且把这个token放到session中</span></span><br><span class="line">      <span class="keyword">this</span>.ctx.body = &#123; <span class="attr">data</span>: <span class="string">'登录成功'</span>, openId &#125;;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">this</span>.ctx.body = &#123; <span class="attr">data</span>: <span class="string">'登录失败'</span> &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h2 id="路由守卫"><a href="#路由守卫" class="headerlink" title="路由守卫"></a>路由守卫</h2><p>登录后，我们生成了 session，通过后台是不是存在对应的 session，作一个中台的路由守卫。如果没有登录，是不允许访问后台对应的接口，也没办法作对应的操作。这样就实现了接口的安全<br>其实就是路由守卫，没有 token 的时候不让访问接口（等同于不让访问某些页面）。在这里我们是通过 egg.js 的中间件实现的（详情版见<a href="https://jspang.com/detailed?id=52#toc2123" target="_blank" rel="noopener">教程</a>)</p>
<ul>
<li>写一个中间件文件，并应用在中台路由中 （目前只有<code>getTypeInfo</code>这个接口有守卫</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>.exports = <span class="function"><span class="params">options</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">adminauth</span>(<span class="params">ctx, next</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(ctx.session.openId)</span><br><span class="line">    <span class="keyword">if</span> (ctx.session.openId) &#123;</span><br><span class="line">      <span class="keyword">await</span> next()</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      ctx.body = &#123; <span class="attr">data</span>: <span class="string">'没有登录'</span> &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 中台路由：对某个接口进行路由守卫</span></span><br><span class="line">router.get(<span class="string">'/admin/getTypeInfo'</span>, adminauth, controller.admin.main.getTypeInfo)</span><br></pre></td></tr></table></figure>

<ul>
<li>在正常情况下前后台是不能共享<code>session</code>的，需要要在 egg 端的<code>/config/config.default.js</code>里的<code>config.cor</code>配置项增加<code>credentials:true</code>；并且前台的请求中，需要带 <code>withCredentials: true</code>。</li>
</ul>
<h2 id="管理页"><a href="#管理页" class="headerlink" title="管理页"></a>管理页</h2><p>布局上用 Antd 的 <a href="https://ant.design/components/layout-cn/" target="_blank" rel="noopener">layout-侧边布局</a>。再做相应的路由配置就能分页</p>
<h2 id="管理页之添加文章"><a href="#管理页之添加文章" class="headerlink" title="管理页之添加文章"></a>管理页之添加文章</h2><p>直接在 编辑文章 分页进行编辑并保存<br>前端：把文章的数据都打包好之后 POST 给后端接口<br>后端：数据库对表进行插入数据的操作。id 方面是先找到最大 id，然后让新文章的 id 等于最大值+1</p>
<h2 id="管理页之修改文章"><a href="#管理页之修改文章" class="headerlink" title="管理页之修改文章"></a>管理页之修改文章</h2><p>在 文章列表 分页，点击某篇文章的 修改 按钮，就会进行路由跳转到 编辑文章 分页并附上文章 id 保存在路由中</p>
<p>所以在 编辑文章 分页还有这样一个逻辑：有一个<code>getArticleById</code>方法，会根据 id 向接口请求对应的文章的数据并显示在页面上，这个方法会在 useEffect 中执行让它在页面刚刷新的时候就执行（如果路由有带 id 的话）。此外，改页面的 发布文章 这个按钮也会判断是否有 id，无的话就是请求发布文章的接口，有的话就是请求更新文章的接口</p>
<h1 id="项目优化计划"><a href="#项目优化计划" class="headerlink" title="项目优化计划"></a>项目优化计划</h1><h2 id="前台-1"><a href="#前台-1" class="headerlink" title="前台"></a>前台</h2><ul>
<li><input disabled type="checkbox"> markdown 语法中图片的解析和存储<pre><code>markdown 语法中图片是直接写成 `![](/img/二叉树的遍历/二叉树的遍历_1.png)` 这种形式，完全不做配置的话会出现下图这种 404 的情况
![](/img/博客项目总结/img.png)
</code></pre>
</li>
<li><input disabled type="checkbox"> 文章简介的实现</li>
<li><input disabled type="checkbox"> 访问人数的实现。目前的访问人数是随机生成的</li>
<li><input disabled type="checkbox"> UI 美化</li>
</ul>
<h2 id="后台-1"><a href="#后台-1" class="headerlink" title="后台"></a>后台</h2><ul>
<li><input disabled type="checkbox"> 登录账号和密码加密</li>
<li><input disabled type="checkbox"> 实现手机号注册？或者说这种管理中台一般是管理者才能使用，管理者账号直接写在数据库就行了，不必要有注册功能</li>
<li><input disabled type="checkbox"> 页面 UI 优化，使该系统美观一些</li>
<li><input disabled type="checkbox"> 添加文章能插入图片</li>
</ul>
<h1 id="上线及部署"><a href="#上线及部署" class="headerlink" title="上线及部署"></a>上线及部署</h1><p>见另一篇文章</p>
<p>参考资料<br><a href="https://jspang.com/" target="_blank" rel="noopener">技术胖博客</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>项目小结</tag>
      </tags>
  </entry>
  <entry>
    <title>浏览器兼容性</title>
    <url>/2019/11/25/%E6%B5%8F%E8%A7%88%E5%99%A8%E5%85%BC%E5%AE%B9%E6%80%A7/</url>
    <content><![CDATA[<h1 id="常见兼容性问题"><a href="#常见兼容性问题" class="headerlink" title="常见兼容性问题"></a>常见兼容性问题</h1><ol>
<li>png24 位的图片在 iE6 浏览器上出现背景，解决方案是做成 PNG8</li>
<li>浏览器默认的 margin 和 padding 不同。解决方案是加一个全局的*{margin:0;padding:0;}来统一，但是全局效率很低，一般是如下这样解决：</li>
</ol>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">body</span>,<span class="selector-tag">ul</span>,<span class="selector-tag">li</span>,<span class="selector-tag">ol</span>,<span class="selector-tag">dl</span>,<span class="selector-tag">dt</span>,<span class="selector-tag">dd</span>,<span class="selector-tag">form</span>,<span class="selector-tag">input</span>,<span class="selector-tag">h1</span>,<span class="selector-tag">h2</span>,<span class="selector-tag">h3</span>,<span class="selector-tag">h4</span>,<span class="selector-tag">h5</span>,<span class="selector-tag">h6</span>,<span class="selector-tag">p</span>&#123; <span class="attribute">margin</span>:<span class="number">0</span>; <span class="attribute">padding</span>:<span class="number">0</span>; &#125;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>IE 下，event 对象有 x,y 属性，但是没有 pageX，pageY 属性</li>
<li>Firefox 下，event 对象有 pageX,pageY 属性，但是没有 x,y 属性</li>
</ol>
<p>参考资料<br><a href="https://www.jianshu.com/p/6afd596440bb" target="_blank" rel="noopener">前端浏览器兼容性问题总结</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>Browser</tag>
      </tags>
  </entry>
  <entry>
    <title>渲染方案</title>
    <url>/2020/05/27/%E6%B8%B2%E6%9F%93%E6%96%B9%E6%A1%88/</url>
    <content><![CDATA[<h1 id="长列表-无限下拉列表-渲染"><a href="#长列表-无限下拉列表-渲染" class="headerlink" title="长列表 / 无限下拉列表 渲染"></a>长列表 / 无限下拉列表 渲染</h1><p>考虑到性能，我们不可能将一个长列表（甚至是一个无限下拉列表）的所有列表元素都进行渲染，应该是只渲染部分数据并随着下拉渲染新数据</p>
<h2 id="方案一：Intersection-Observer-padding"><a href="#方案一：Intersection-Observer-padding" class="headerlink" title="方案一：Intersection Observer + padding"></a>方案一：Intersection Observer + padding</h2><p>该方案来自<a href="https://mp.weixin.qq.com/s/X2ltQyp3W0othLv_m6-Ydg" target="_blank" rel="noopener">云音乐-一个简洁、有趣的无限下拉方案</a></p>
<h3 id="Intersection-Observer"><a href="#Intersection-Observer" class="headerlink" title="Intersection Observer"></a>Intersection Observer</h3><p>详见<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Intersection_Observer_API" target="_blank" rel="noopener">MDN</a>，建议全都读完</p>
<p>一直以来，<strong>检测元素的可视状态或者两个元素的相对可视状态</strong>都不是件容易事。传统的各种方案不但复杂，而且性能成本很高，比如需要监听滚动事件，然后查询 DOM , 获取元素高度、位置，计算距离视窗高度等等。</p>
<p>这就是 Intersection Observer 要解决的问题。它为开发人员提供一种便捷的新方法来异步查询元素相对于其他元素或视窗的位置，消除了昂贵的 DOM 查询和样式读取成本</p>
<p>它不能告诉你的一件事情是 (两个元素的)重叠部分的准确像素个数或者重叠的像素属于哪一个元素。然而这个API覆盖最广的最常用的使用方式是 <strong>如果两个元素发生的交集部分在N%左右，我需要做处理一些事情(执行回调)</strong></p>
<blockquote>
<p>在 Safari 上兼容性较差，需要 12.2 及以上才兼容，否则需配合 polyfill  使用</p>
</blockquote>
<p>一些应用场景：</p>
<ul>
<li>当页面滚动时，懒加载图片或其他内容。</li>
<li>实现“可无限滚动”网站，也就是当用户滚动网页时直接加载更多内容，无需翻页。</li>
<li>为计算广告收益，检测其广告元素的曝光情况。</li>
<li>根据用户是否已滚动到相应区域来灵活开始执行任务或动画</li>
</ul>
<h4 id="具体用法"><a href="#具体用法" class="headerlink" title="具体用法"></a>具体用法</h4><ol>
<li>创建一个 IntersectionObserver对象。</li>
</ol>
<ul>
<li>root必须是目标元素的父级元素；如果未指定或者为null，则默认为浏览器视窗</li>
<li>rootMargin是root元素的外边距。类似于css中的 margin 属性</li>
<li>threshold。目标(target)元素与根(root)元素之间的交叉度是交叉比(intersection ratio)。这是目标(target)元素相对于根(root)的交集百分比的表示，它的取值在0.0和1.0之间。阈值为1.0意味着目标元素完全出现在root选项指定的元素中可见时，回调函数将会被执行。可以是单一的number也可以是number数组<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> options = &#123;</span><br><span class="line">  root: <span class="built_in">document</span>.querySelector(<span class="string">'#scrollArea'</span>), </span><br><span class="line">  rootMargin: <span class="string">'0px'</span>, </span><br><span class="line">  threshold: <span class="number">1.0</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> observer = <span class="keyword">new</span> IntersectionObserver(callback, options);</span><br></pre></td></tr></table></figure></li>
</ul>
<ol start="2">
<li><p>每个观察者配置一个目标。每当目标满足该IntersectionObserver指定的threshold值，回调被调用</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> target = <span class="built_in">document</span>.querySelector(<span class="string">'#listItem'</span>);</span><br><span class="line">observer.observe(target);</span><br></pre></td></tr></table></figure></li>
<li><p>回调函数。回调接收 IntersectionObserverEntry对象和观察者的列表：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> callback = <span class="function"><span class="keyword">function</span>(<span class="params">entries, observer</span>) </span>&#123; </span><br><span class="line">  entries.forEach(<span class="function"><span class="params">entry</span> =&gt;</span> &#123; </span><br><span class="line">    <span class="comment">// 这里的 entry 是 IntersectionObserverEntry 对象，entry.target 是目标元素</span></span><br><span class="line">    <span class="comment">//   entry.boundingClientRect</span></span><br><span class="line">    <span class="comment">//   entry.intersectionRatio</span></span><br><span class="line">    <span class="comment">//   entry.intersectionRect</span></span><br><span class="line">    <span class="comment">//   entry.isIntersecting</span></span><br><span class="line">    <span class="comment">//   entry.rootBounds</span></span><br><span class="line">    <span class="comment">//   entry.target</span></span><br><span class="line">    <span class="comment">//   entry.time</span></span><br><span class="line">  &#125;);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
</ol>
<p>entry的属性大概是这样：<br><img src="/img/%E5%88%97%E8%A1%A8%E6%B8%B2%E6%9F%93/entry.png"><br>详见<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/IntersectionObserverEntry" target="_blank" rel="noopener">MDN-IntersectionObserverEntry</a></p>
<h3 id="实现思路"><a href="#实现思路" class="headerlink" title="实现思路"></a>实现思路</h3><p>总体思路：利用 Intersection Observer 来监测相关元素的滚动位置，异步监听，尽可能得减少 DOM 操作，触发回调，然后去获取新的数据来更新页面元素，并且用调整容器 padding 来替代了本该越来越多的 DOM 元素，最终实现列表滚动、无限下拉。具体如下：</p>
<ol>
<li><p>监听一个固定长度列表的首尾元素是否进入视窗</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 观察者创建</span><br><span class="line">this.observer = new IntersectionObserver(callback, options);</span><br><span class="line"></span><br><span class="line">// 观察列表第一个以及最后一个元素</span><br><span class="line">this.observer.observe(this.firstItem);</span><br><span class="line">this.observer.observe(this.lastItem);</span><br><span class="line"></span><br><span class="line">const callback = (entries) =&gt; &#123;</span><br><span class="line">  entries.forEach((entry) =&gt; &#123;</span><br><span class="line">    if (entry.target.id === firstItemId) &#123;</span><br><span class="line">      // 当第一个元素进入视窗</span><br><span class="line">    &#125; else if (entry.target.id === lastItemId) &#123;</span><br><span class="line">      // 当最后一个元素进入视窗</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
<li><p>更新当前页面内渲染的第一个元素对应的序号(firstIndex)。我们用一个数组来维护需要渲染到页面中的数据。数组的长度会随着不断请求新的数据而不断变大，而渲染的始终是其中一定数量的元素，比如 20 个。那么：</p>
</li>
</ol>
<ul>
<li>最开始渲染的是数组中序号为 0 - 19 的元素，即此时对应的 firstIndex 为 0；</li>
<li>当序号为 19 的元素（即上一步的 lastItem ）进入视窗时，我们就会往后渲染 10 个元素，即渲染序号为 10 - 29 的元素，那么此时的 firstIndex 为 10；</li>
<li>下一次就是，当序号为 29 的元素进入视窗时，继续往后渲染 10个元素，即渲染序号为 20 - 39 的元素，那么此时的 firstIndex 为 20，以此类推。。。<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 我们对原先的 firstIndex 做了缓存</span><br><span class="line">const &#123; currentIndex &#125; = this.domDataCache;</span><br><span class="line"></span><br><span class="line">// 以全部容器内所有元素的一半作为每一次渲染的增量</span><br><span class="line">const increment = Math.floor(this.listSize / 2);</span><br><span class="line"></span><br><span class="line">let firstIndex;</span><br><span class="line"></span><br><span class="line">if (isScrollDown) &#123;</span><br><span class="line">  // 向下滚动时序号增加</span><br><span class="line">  firstIndex = currentIndex + increment;</span><br><span class="line">&#125; else &#123;</span><br><span class="line">  // 向上滚动时序号减少</span><br><span class="line">  firstIndex = currentIndex - increment;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<ol start="3">
<li><p>根据上述序号，获取目标数据元素，列表内容重新渲染成对应内容</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const renderFunction = (firstIndex) =&gt; &#123;</span><br><span class="line">  // offset = firstIndex, limit = 10 =&gt; getData</span><br><span class="line">  // getData Done =&gt;  new dataItems =&gt; render DOM</span><br><span class="line"> &#125;;</span><br></pre></td></tr></table></figure></li>
<li><p>容器 padding 调整，模拟滚动实现<br>其实不用这一步也可以，直接获取了 10 个新的数据元素之后，再塞 10 个新的 DOM 元素到页面中去来渲染这些数据。但是 DOM 会越来越多，页面DOM太多可能会有性能问题。所以这里的方案是：用原来已有的 DOM 元素去渲染，替换掉已经离开视窗、不可见的数据元素；而本该由更多 DOM 元素进一步撑开容器高度的部分，我们用 padding 填充来模拟实现。</p>
</li>
</ol>
<ul>
<li><p>向下滚动</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// padding的增量 = 每一个item的高度 x 新的数据项的数目</span><br><span class="line">const remPaddingsVal = itemHeight * (Math.floor(this.listSize / 2));</span><br><span class="line"></span><br><span class="line">if (isScrollDown) &#123;</span><br><span class="line">  // paddingTop新增，填充顶部位置</span><br><span class="line">  newCurrentPaddingTop = currentPaddingTop + remPaddingsVal;</span><br><span class="line"></span><br><span class="line">  if (currentPaddingBottom === 0) &#123;</span><br><span class="line">    newCurrentPaddingBottom = 0;</span><br><span class="line">  &#125; else &#123;</span><br><span class="line">    // 如果原来有paddingBottom则减去，会有滚动到底部的元素进行替代</span><br><span class="line">    newCurrentPaddingBottom = currentPaddingBottom - remPaddingsVal;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>向上滚动</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// padding的增量 = 每一个item的高度 x 新的数据项的数目</span><br><span class="line">const remPaddingsVal = itemHeight * (Math.floor(this.listSize / 2));</span><br><span class="line"></span><br><span class="line">if (!isScrollDown) &#123;</span><br><span class="line">  // paddingBottom新增，填充底部位置</span><br><span class="line">  newCurrentPaddingBottom = currentPaddingBottom + remPaddingsVal;</span><br><span class="line"></span><br><span class="line">  if (currentPaddingTop === 0) &#123;</span><br><span class="line">    newCurrentPaddingTop = 0;</span><br><span class="line">  &#125; else &#123;</span><br><span class="line">    // 如果原来有paddingTop则减去，会有滚动到顶部的元素进行替代</span><br><span class="line">    newCurrentPaddingTop = currentPaddingTop - remPaddingsVal;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>最后是 padding 设置更新以及相关缓存数据更新</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 容器padding重新设置</span><br><span class="line">this.updateContainerPadding(&#123;</span><br><span class="line">  newCurrentPaddingBottom,</span><br><span class="line">  newCurrentPaddingTop</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">// DOM元素相关数据缓存更新</span><br><span class="line">this.updateDomDataCache(&#123;</span><br><span class="line">    currentPaddingTop: newCurrentPaddingTop,</span><br><span class="line">  currentPaddingBottom: newCurrentPaddingBottom</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li>
</ul>
<blockquote>
<p>完整代码实现参考<a href="https://github.com/Guohjia/listScroll" target="_blank" rel="noopener">https://github.com/Guohjia/listScroll</a></p>
</blockquote>
<h3 id="缺陷"><a href="#缺陷" class="headerlink" title="缺陷"></a>缺陷</h3><ul>
<li>padding 的计算依赖列表项固定的高度</li>
<li>这是一个同步渲染的方案，也就是目前容器 padding 的计算调整，无法计算异步获取的数据，只跟用户的滚动行为有关。解决思路：<ul>
<li>思路1：利用 Skeleton Screen Loading 来同步渲染数据元素，不受数据异步获取的影响。即在数据请求还未完成时，先使用一些图片进行占位，待内容加载完成之后再进行替换。</li>
<li>思路2：滚动到目标位置，阻塞容器 padding 的设置（即无限下拉的发生）直至数据请求完毕，用 loading gif 提示用户加载状态，但这个方案相对复杂，你需要全面考虑用户难以预测的滚动行为来设置容器的 padding。</li>
</ul>
</li>
</ul>
<h2 id="方案二：较为有名的库-iScroll"><a href="#方案二：较为有名的库-iScroll" class="headerlink" title="方案二：较为有名的库 - iScroll"></a>方案二：较为有名的库 - iScroll</h2><p>思路：<br>iScroll 通过对传统滚动事件的监听，获取滚动距离，然后：</p>
<ol>
<li>设置父元素的 translate 来实现整体内容的上移（下移）；</li>
<li>再基于这个滚动距离进行相应计算，得知相应子元素已经被滚动到视窗外，并且判断是否应该将这些离开视窗的子元素移动到末尾，从而再对它们进行 translate 的设置来移动到末尾。这就像是一个循环队列一样，随着滚动的进行，顶部元素先出视窗，但又将移动到末尾，从而实现无限下拉</li>
</ol>
<h2 id="方案一和二对比"><a href="#方案一和二对比" class="headerlink" title="方案一和二对比"></a>方案一和二对比</h2><ul>
<li>实现对比：一个是 Intersection Observer 的监听，来通知子元素离开视窗，只要定量设置父元素 padding 就行；另一个是对传统滚动事件的监听，滚动距离的获取，再进行一系列计算，去设置父元素以及子元素的 translate。显而易见，前者看起来更加简洁明了一些</li>
<li>性能对比：我知道说到对比，你脑海中肯定一下子会想到性能问题。其实性能对比的关键就是 Intersection Observer。因为单就 padding 设置还是 translate 设置，性能方面的差距是甚小的，只是个人感觉 padding 会简洁些？而 Intersection Observer 其实抽离了所有滚动层面的相关逻辑，你不再需要对滚动距离等相应 DOM 属性进行获取，也不再需要进行一系列滚动距离相关的复杂计算，并且同步的滚动事件触发变成异步的，你也不再需要另外去做防抖之类的逻辑，这在性能方面还是有所提升的</li>
</ul>
<h1 id="tree组件的渲染优化"><a href="#tree组件的渲染优化" class="headerlink" title="tree组件的渲染优化"></a>tree组件的渲染优化</h1><h1 id="高性能渲染10万个DOM"><a href="#高性能渲染10万个DOM" class="headerlink" title="高性能渲染10万个DOM"></a>高性能渲染10万个DOM</h1><p>见之前的博客<a href="https://jacleklm.github.io/2019/11/06/%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96/" target="_blank" rel="noopener">性能优化-如何插入几万个 DOM 并不卡住界面</a></p>
<p>参考资料<br><a href="https://mp.weixin.qq.com/s/X2ltQyp3W0othLv_m6-Ydg" target="_blank" rel="noopener">云音乐-一个简洁、有趣的无限下拉方案</a><br><a href="https://zhuanlan.zhihu.com/p/55528376" target="_blank" rel="noopener">前端tree组件，10000个树节点，从14.65s到0.49s</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>解决方案</tag>
      </tags>
  </entry>
  <entry>
    <title>用 node.js 开发一个可交互的命令行应用</title>
    <url>/2020/11/03/%E7%94%A8%20node.js%20%E5%BC%80%E5%8F%91%E4%B8%80%E4%B8%AA%E5%8F%AF%E4%BA%A4%E4%BA%92%E7%9A%84%E5%91%BD%E4%BB%A4%E8%A1%8C%E5%BA%94%E7%94%A8/</url>
    <content><![CDATA[<h1 id="原生-node"><a href="#原生-node" class="headerlink" title="原生 node"></a>原生 node</h1><ul>
<li>用 process.argv 直接读取</li>
<li>process.stdin.on 劫持输入</li>
<li><a href="http://nodejs.cn/api/readline.html" target="_blank" rel="noopener">readline</a> 模块 进行逐行读取。该模块提供了一个接口，用于一次一行地读取可读流（例如 <code>process.stdin</code>）中的数据<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> app = <span class="built_in">require</span>(<span class="string">'./app'</span>)</span><br><span class="line"><span class="keyword">const</span> readline = <span class="built_in">require</span>(<span class="string">'readline'</span>);</span><br><span class="line"><span class="keyword">const</span> rl = readline.createInterface(&#123;</span><br><span class="line">  input: process.stdin,</span><br><span class="line">  output: process.stdout</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">rl.question(<span class="string">'请输入当前的年份和月份，然后按回车键。eg: 202010. '</span>, (month) =&gt; &#123;</span><br><span class="line">  rl.question(<span class="string">'请输入本月小结会的月份和日期，然后按回车键。eg: 1130. '</span>, (date) =&gt; &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'数据统计中, 请勿触碰键盘...'</span>);</span><br><span class="line">    app(month, date);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 一旦调用此代码，Node.js 应用程序将不会终止，直到 readline.Interface 关闭（ rl.close() ），因为接口在 input 流上等待接收数据</span></span><br><span class="line">    <span class="comment">// rl.close();</span></span><br><span class="line"></span><br><span class="line">    rl.on(<span class="string">'close'</span>, () =&gt; &#123; <span class="comment">// close事件，ctrl + C 也会触发。详见 http://nodejs.cn/api/readline.html</span></span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">"Goodbye 👋"</span>);</span><br><span class="line">      <span class="comment">// exit the process</span></span><br><span class="line">      process.exit(<span class="number">0</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">    </span><br><span class="line">  &#125;)</span><br></pre></td></tr></table></figure></li>
</ul>
<h1 id="借助-prompt-包"><a href="#借助-prompt-包" class="headerlink" title="借助 prompt 包"></a>借助 prompt 包</h1><p>如果你 打算在Node.js中构建可靠的CLI工具，则 <a href="https://www.npmjs.com/package/prompt" target="_blank" rel="noopener">prompt</a> 可能是一个很好的选择。比readline更好用。<br>能像处理页面表单一样处理命令行输入</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> prompt = <span class="built_in">require</span>(<span class="string">'prompt'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// start the prompt</span></span><br><span class="line">prompt.start();</span><br><span class="line"><span class="comment">// define properties schemavar schema = &#123;</span></span><br><span class="line">    properties: &#123;</span><br><span class="line">        name: &#123;</span><br><span class="line">            pattern: <span class="regexp">/^[a-zA-Z\s\-]+$/</span>,</span><br><span class="line">            message: <span class="string">'Name must be only letters, spaces, or dashes'</span>,</span><br><span class="line">            required: <span class="literal">true</span></span><br><span class="line">        &#125;,</span><br><span class="line">        password: &#123;</span><br><span class="line">            hidden: <span class="literal">true</span>  <span class="comment">// 这里能隐藏密码</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ask user for the input</span></span><br><span class="line">prompt.get(schema, (err, result) =&gt; &#123;</span><br><span class="line">    <span class="keyword">if</span> (err) &#123;</span><br><span class="line">        <span class="keyword">throw</span> err;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// print user credentials</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;result.name&#125;</span> / <span class="subst">$&#123;result.password&#125;</span>`</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>借助 Commander 包<br>这个感觉更像是封装命令行命令了，不仅仅是初始化时读取输入<br>代码:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> program = <span class="built_in">require</span>(<span class="string">'commander'</span>);</span><br><span class="line">program</span><br><span class="line">  .version(<span class="string">'0.0.1'</span>)  <span class="comment">// node index --version 或 node index -V 能打印出我们定义的版本号。相当于 -V 和 -h 是自带的，其他的可以通过 .option() 来自定义</span></span><br><span class="line">  .option(<span class="string">'-l, --list [list]'</span>, <span class="string">'list of customers in CSV file'</span>) <span class="comment">// 自定义的</span></span><br><span class="line">  .parse(process.argv)</span><br><span class="line"><span class="built_in">console</span>.log(program.list);</span><br></pre></td></tr></table></figure>

<p>执行：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&gt; node index --version 或 node index -V 能打印出我们定义的版本号</span><br><span class="line"><span class="number">0.0</span><span class="number">.1</span></span><br><span class="line"></span><br><span class="line">&gt; node index --help</span><br><span class="line">Usage: index [options]</span><br><span class="line">Options:</span><br><span class="line">  -V, --version      output the version number</span><br><span class="line">  -l, --list [list]  list <span class="keyword">of</span> customers <span class="keyword">in</span> CSV file</span><br><span class="line">  -h, --help         display help <span class="keyword">for</span> command</span><br></pre></td></tr></table></figure>

<p>参考资料<br><a href="https://blog.csdn.net/ccf19881030/article/details/109111128" target="_blank" rel="noopener">如何从Node.js中的命令行读取输入</a><br><a href="https://juejin.im/entry/6844903474316967950" target="_blank" rel="noopener">用 node.js 开发一个可交互的命令行应用: commander</a></p>
]]></content>
      <categories>
        <category>Engineering</category>
      </categories>
      <tags>
        <tag>Node</tag>
      </tags>
  </entry>
  <entry>
    <title>浏览器</title>
    <url>/2019/10/29/%E6%B5%8F%E8%A7%88%E5%99%A8/</url>
    <content><![CDATA[<h2 id="浏览器渲染"><a href="#浏览器渲染" class="headerlink" title="浏览器渲染"></a>浏览器渲染</h2><h3 id="渲染流程"><a href="#渲染流程" class="headerlink" title="渲染流程"></a>渲染流程</h3><ul>
<li>接收到 HTML 文件，转化为 DOM 树（字节数据=&gt;字符串=&gt;Token（最小单位代码）=&gt;Node=&gt;DOM）<br>当然，在解析 HTML 文件的时候，浏览器还会遇到 CSS 和 JS 文件，这时候浏览器也会去下载并解析这些文件（即遇到 script 标签会暂停 DOM 的构建）</li>
<li>将 CSS 文件转换为 CSSOM 树（字节数据=&gt;字符串=&gt;Token（最小单位代码）=&gt;Node=&gt;CSSOM）<br>在这一过程中，浏览器会确定下每一个节点的<strong>样式</strong>到底是什么，并且这一过程其实是<strong>很消耗资源</strong>的<br>eg. span 和 div &gt; a &gt; span，后者的解析成本会更高<br>我们应该尽可能的避免写<strong>过于具体</strong>的 CSS 选择器，然后对于 HTML 来说也尽量少的添加无意义标签，保证<strong>层级扁平</strong></li>
<li>将 DOM 树和 CSSOM 树组成成渲染树（Render Tree）<br>渲染树只会包括<strong>需要显示的</strong>节点和这些节点的样式信息，如果某个节点是<code>display: none</code>的，那么就不会在渲染树中显示</li>
<li>根据渲染树来进行布局（也可以叫做回流），然后调用 GPU 绘制，合成图层，显示在屏幕上</li>
</ul>
<h3 id="什么情况下会阻塞渲染"><a href="#什么情况下会阻塞渲染" class="headerlink" title="什么情况下会阻塞渲染"></a>什么情况下会阻塞渲染</h3><ul>
<li>首先渲染的前提是生成渲染树，所以 HTML 和 CSS 肯定会阻塞渲染<br>想渲染的越快，越应该<strong>降低一开始需要渲染的文件大小</strong>，并且做到<strong>HTML 扁平层级，优化 CSS 选择器</strong></li>
<li>然后当浏览器在解析到 script 标签时，会暂停构建 DOM，完成后才会从暂停的地方重新开始<br>所以，如果想首屏渲染的越快，就越<strong>不应该在首屏就加载 JS 文件</strong>，这也是都建议将 script 标签放在 body 标签底部的原因</li>
</ul>
<h3 id="重绘和回流"><a href="#重绘和回流" class="headerlink" title="重绘和回流"></a>重绘和回流</h3><p>见 CSS 知识点</p>
<h2 id="浏览器缓存机制"><a href="#浏览器缓存机制" class="headerlink" title="浏览器缓存机制"></a>浏览器缓存机制</h2><ul>
<li>缓存可以说是性能优化中<strong>简单高效</strong>的一种优化方式了，它可以<strong>显著减少网络传输所带来的损耗</strong>。</li>
<li>对于一个数据请求来说，可以分为发起网络请求、后端处理、浏览器响应三个步骤。浏览器缓存可以帮助我们在第一和第三步骤中优化性能。比如说直接使用缓存而不发起请求，或者发起了请求但后端存储的数据和前端一致，那么就没有必要再将数据回传回来，这样就减少了响应数据。</li>
</ul>
<h3 id="缓存位置"><a href="#缓存位置" class="headerlink" title="缓存位置"></a>缓存位置</h3><p>从缓存位置上来说分为四种，并且各自有优先级，当依次查找缓存且都没有命中的时候，才会去请求网络。顺序是：</p>
<ol>
<li>Service Worker</li>
<li>Memory Cache</li>
<li>Disk Cache</li>
<li>Push Cache</li>
<li>都找不到，就发起网络请求获取数据</li>
</ol>
<h4 id="Service-Worker"><a href="#Service-Worker" class="headerlink" title="Service Worker"></a>Service Worker</h4><ul>
<li>service Worker 的缓存与浏览器其他内建的缓存机制不同，它可以让我们自由控制缓存哪些文件、如何匹配缓存、如何读取缓存，并且缓存是持续性的。</li>
<li>当 Service Worker 没有命中缓存的时候，我们需要去调用 fetch 函数获取数据。也就是说，如果我们没有在 Service Worker 命中缓存的话，会根据缓存查找优先级去查找数据。但是不管我们是从 Memory Cache 中还是从网络请求中获取的数据，浏览器都会显示我们是从 Service Worker 中获取的内容。</li>
</ul>
<h4 id="Memory-Cache"><a href="#Memory-Cache" class="headerlink" title="Memory Cache"></a>Memory Cache</h4><ul>
<li>Memory Cache 也就是内存中的缓存，读取内存中的数据肯定比磁盘快。但是内存缓存虽然读取高效，可是缓存持续性很短，会随着进程的释放而释放。 一旦我们关闭 Tab 页面，内存中的缓存也就被释放了。</li>
<li>当我们访问过页面以后，再次刷新页面，可以发现很多数据都来自于内存缓存</li>
</ul>
<blockquote>
<p>那么既然内存缓存这么高效，我们是不是能让数据都存放在内存中呢？</p>
</blockquote>
<ul>
<li>先说结论，这是不可能的。首先计算机中的内存一定比硬盘容量小得多，操作系统需要精打细算内存的使用，所以能让我们使用的内存必然不多。内存中其实可以存储大部分的文件，比如说 JS、HTML、CSS、图片等等</li>
<li>当然，我通过一些实践和猜测也得出了一些结论：<br>对于大文件来说，大概率是不存储在内存中的，反之优先当前系统内存使用率高的话，文件优先存储进硬盘</li>
</ul>
<h4 id="Disk-Cache"><a href="#Disk-Cache" class="headerlink" title="Disk Cache"></a>Disk Cache</h4><ul>
<li>Disk Cache 也就是存储在硬盘中的缓存，读取速度慢点，但是什么都能存储到磁盘中，比之 Memory Cache 胜在容量和存储时效性上。</li>
<li>在所有浏览器缓存中，Disk Cache 覆盖面基本是最大的。它会根据 HTTP Herder 中的字段判断哪些资源需要缓存，哪些资源可以不请求直接使用，哪些资源已经过期需要重新请求。并且即使在跨站点的情况下，相同地址的资源一旦被硬盘缓存下来，就不会再次去请求数据</li>
</ul>
<h4 id="Push-Cache"><a href="#Push-Cache" class="headerlink" title="Push Cache"></a>Push Cache</h4><p>Push Cache 是 HTTP/2 中的内容，当以上三种缓存都没有命中时，它才会被使用。并且缓存时间也很短暂，只在会话（Session）中存在，一旦会话结束就被释放</p>
<h4 id="网络请求"><a href="#网络请求" class="headerlink" title="网络请求"></a>网络请求</h4><ul>
<li>如果所有缓存都没有命中的话，那么只能发起请求来获取资源了。</li>
<li>那么为了性能上的考虑，大部分的接口都应该选择好缓存策略，接下来我们就来讨论缓存策略这部分的内容</li>
</ul>
<h3 id="缓存策略"><a href="#缓存策略" class="headerlink" title="缓存策略"></a>缓存策略</h3><blockquote>
<p>通常浏览器缓存策略分为两种：强缓存和协商缓存，并且缓存策略都是通过设置 HTTP Header 来实现的</p>
</blockquote>
<h4 id="强缓存"><a href="#强缓存" class="headerlink" title="强缓存"></a>强缓存</h4><p>强缓存可以通过设置两种 HTTP Header 实现：Expires 和 Cache-Control 。强缓存表示在缓存期间不需要请求，state code 为 200<br><strong>Expires</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">Expires: Wed, <span class="number">22</span> Oct <span class="number">2018</span> <span class="number">08</span>:<span class="number">41</span>:<span class="number">00</span> GMT</span><br></pre></td></tr></table></figure>

<blockquote>
<p>Expires 是 HTTP/1 的产物，表示资源会在 Wed, 22 Oct 2018 08:41:00 GMT 后过期，需要再次请求。并且 Expires 受限于本地时间，如果修改了本地时间，可能会造成缓存失效。</p>
</blockquote>
<p><strong>Cache-control</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">Cache-control: max-age=<span class="number">30</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p>Cache-Control 出现于 HTTP/1.1，优先级高于 Expires 。该属性值表示资源会在 30 秒后过期，需要再次请求。<br>Cache-Control 可以在请求头或者响应头中设置，并且可以组合使用多种指令</p>
</blockquote>
<h4 id="协商缓存"><a href="#协商缓存" class="headerlink" title="协商缓存"></a>协商缓存</h4><ul>
<li>如果缓存过期了，就需要发起请求验证资源是否有更新。协商缓存可以通过设置两种 HTTP Header 实现：Last-Modified 和 ETag</li>
<li>当浏览器发起请求验证资源时，如果资源没有做改变，那么服务端就会返回 304 状态码，并且更新浏览器缓存有效期</li>
</ul>
<p><strong>Last-Modified 和 If-Modified-Since</strong></p>
<blockquote>
<p>Last-Modified 表示本地文件最后修改日期，If-Modified-Since 会将 Last-Modified 的值发送给服务器，询问服务器在该日期后资源是否有更新，有更新的话就会将新的资源发送回来，否则返回 304 状态码</p>
</blockquote>
<p>但是 Last-Modified 存在一些弊端：</p>
<ul>
<li>如果本地打开缓存文件，即使没有对文件进行修改，但还是会造成 Last-Modified 被修改</li>
<li>Last-Modified 只能以秒计时，如果在不可感知的时间内修改完成文件，那么服务端会认为资源还是命中了，不会返回正确的资源</li>
</ul>
<p><strong>ETag 和 If-None-Match</strong></p>
<blockquote>
<p>ETag 类似于文件指纹，If-None-Match 会将当前 ETag 发送给服务器，询问该资源 ETag 是否变动，有变动的话就将新的资源发送回来。并且 ETag 优先级比 Last-Modified 高。</p>
</blockquote>
<h3 id="实际场景应用缓存策略"><a href="#实际场景应用缓存策略" class="headerlink" title="实际场景应用缓存策略"></a>实际场景应用缓存策略</h3><h4 id="频繁变动的资源"><a href="#频繁变动的资源" class="headerlink" title="频繁变动的资源"></a>频繁变动的资源</h4><p>对于频繁变动的资源，首先需要使用 Cache-Control: no-cache 使浏览器每次都请求服务器，然后配合 ETag 或者 Last-Modified 来验证资源是否有效。这样的做法虽然不能节省请求数量，但是能显著减少响应数据大小。</p>
<h4 id="代码文件"><a href="#代码文件" class="headerlink" title="代码文件"></a>代码文件</h4><blockquote>
<p>这里特指除了 HTML 外的代码文件，因为 HTML 文件一般不缓存或者缓存时间很短</p>
</blockquote>
<p>一般来说，现在都会使用工具来打包代码，那么我们就可以对文件名进行哈希处理，只有当代码修改后才会生成新的文件名。基于此，我们就可以给代码文件设置缓存有效期一年 Cache-Control: max-age=31536000，这样只有当 HTML 文件中引入的文件名发生了改变才会去下载最新的代码文件，否则就一直使用缓存</p>
<h2 id="浏览器存储"><a href="#浏览器存储" class="headerlink" title="浏览器存储"></a>浏览器存储</h2><h3 id="浏览器的存储方式"><a href="#浏览器的存储方式" class="headerlink" title="浏览器的存储方式"></a>浏览器的存储方式</h3><p>cookie，localStorage，sessionStorage，indexDB</p>
<table>
<thead>
<tr>
<th align="center">特性</th>
<th align="center">cookie</th>
<th align="center">localStorage</th>
<th align="center">sessionStorage</th>
<th align="center">indexDB</th>
</tr>
</thead>
<tbody><tr>
<td align="center">数据生命周期</td>
<td align="center">一般由服务器生成，可以设置过期时间</td>
<td align="center">除非被清理，否则一直存在</td>
<td align="center">页面关闭就清理</td>
<td align="center">除非被清理，否则一直存在</td>
</tr>
<tr>
<td align="center">数据存储大小</td>
<td align="center">4K</td>
<td align="center">5M</td>
<td align="center">5M</td>
<td align="center">无限</td>
</tr>
<tr>
<td align="center">与服务端通信</td>
<td align="center">每次都会携带在 header 中，对于请求性能影响</td>
<td align="center">不参与</td>
<td align="center">不参与</td>
<td align="center">不参与</td>
</tr>
</tbody></table>
<p>结论：如果没有大量数据存储需求的话，可以使用 localStorage 和 sessionStorage 。对于不怎么改变的数据尽量使用 localStorage 存储，否则可以用 sessionStorage 存储</p>
<h3 id="Service-Worker-1"><a href="#Service-Worker-1" class="headerlink" title="Service Worker"></a>Service Worker</h3><p>Service Worker 是运行在浏览器背后的<strong>独立线程</strong>（可以理解为是 Web Worker 的一种），一般用来实现缓存功能。<br>所以也和 Web Worker 一样不能访问 DOM，不能用 localStorage, XMLHttpRequest。<br>使用 Service Worker 的话，传输协议必须为 <strong>HTTPS</strong>。因为 Service Worker 中涉及到请求拦截，所以必须使用 HTTPS 协议来保障安全</p>
<p>Service Worker 实现缓存功能一般分为三个步骤：</p>
<ul>
<li>先注册 Service Worker</li>
<li>监听到 install 事件以后就可以缓存需要的文件</li>
<li>下次用户访问的时候就可以通过拦截请求的方式查询是否存在缓存，存在缓存的话就可以直接读取缓存文件，否则就去请求数据。以下是这个步骤的实现：</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//比如在index.js里注册一个Service Worker</span></span><br><span class="line"><span class="keyword">if</span> (navigator.serviceWorker) &#123;</span><br><span class="line">  navigator.serviceWorker</span><br><span class="line">    .register(<span class="string">"xx.js"</span>)</span><br><span class="line">    .then(<span class="function"><span class="keyword">function</span>(<span class="params">registration</span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">"注册成功"</span>);</span><br><span class="line">    &#125;)</span><br><span class="line">    .catch(<span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">"注册失败"</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//xx.js子线程文件</span></span><br><span class="line"><span class="comment">//监听install事件，注册后会缓存所指定的文件。</span></span><br><span class="line">self.addEventListener(<span class="string">"install"</span>, e =&gt; &#123;</span><br><span class="line">  e.wiatUntil(</span><br><span class="line">    caches.open(<span class="string">"my-cache"</span>).then(<span class="function"><span class="keyword">function</span>(<span class="params">cache</span>) </span>&#123;</span><br><span class="line">      <span class="keyword">return</span> cache.addAll([<span class="string">"./index.html"</span>, <span class="string">"./index.js"</span>]);</span><br><span class="line">    &#125;)</span><br><span class="line">  );</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">//监听fetch事件进行请求拦截，如果缓存中已经有要请求的数据就直接用缓存，否则去请求数据</span></span><br><span class="line">self.addEventListener(<span class="string">"fetch"</span>, e =&gt; &#123;</span><br><span class="line">  e.respondWith(</span><br><span class="line">    cache.match(e.request).then(<span class="function"><span class="keyword">function</span>(<span class="params">response</span>) </span>&#123;</span><br><span class="line">      <span class="comment">//1. 如果请求的资源已被缓存，则直接返回</span></span><br><span class="line">      <span class="keyword">if</span> (response) &#123;</span><br><span class="line">        <span class="keyword">return</span> response;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="comment">//2. 没有，则发起请求并缓存结果</span></span><br><span class="line">      <span class="keyword">let</span> requestClone = e.request.clone();</span><br><span class="line">      <span class="keyword">return</span> fetch(requestClone).then(<span class="function"><span class="params">netRes</span> =&gt;</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (!netRes || netRes.status !== <span class="number">200</span>) <span class="keyword">return</span> netRes;</span><br><span class="line">        <span class="keyword">let</span> responseClone = netRes.clone();</span><br><span class="line">        caches</span><br><span class="line">          .open(cacheName)</span><br><span class="line">          .then(<span class="function"><span class="params">cache</span> =&gt;</span> cache.put(requestClone, responseClone));</span><br><span class="line">        <span class="keyword">return</span> netRes;</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125;)</span><br><span class="line">  );</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>参考资料<br>掘金小册-前端面试之道-浏览器渲染原理<br>极客时间-浏览器工作原理与实践<br><a href="https://www.cnblogs.com/bill-shooting/archive/2018/07/21/9347441.html" target="_blank" rel="noopener">浏览器缓存和 Service Worker</a></p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>Browser</tag>
      </tags>
  </entry>
  <entry>
    <title>计算机组成原理</title>
    <url>/2019/10/17/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%84%E6%88%90%E5%8E%9F%E7%90%86/</url>
    <content><![CDATA[<h2 id="概述篇"><a href="#概述篇" class="headerlink" title="概述篇"></a>概述篇</h2><h3 id="计算机发展的四个阶段"><a href="#计算机发展的四个阶段" class="headerlink" title="计算机发展的四个阶段"></a>计算机发展的四个阶段</h3><ul>
<li>1946-1957，电子管计算机</li>
<li>1957-1964， 晶体管计算机</li>
<li>1964-1980， 集成电路计算机（计算机具备进入千家万户的条件；出现操作系统）</li>
<li>1980-至今， 超大规模集成电路计算机</li>
</ul>
<p>未来可能有：生物计算机， 量子计算机（腾讯量子实验室，阿里达摩院）<br>微型计算机的发展历史：从集成电路计算机开始，两个特点：受限于性能（单核到多核CPU）；摩尔定律（集成电路性能每18-24个月就提升一倍）</p>
<h3 id="计算机的分类"><a href="#计算机的分类" class="headerlink" title="计算机的分类"></a>计算机的分类</h3><ul>
<li>超级计算机，eg. 中大东校区的天河二号。 标记他们运算速度的单位是TFlop/s</li>
<li>大型计算机。IBM占据很大的份额。阿里提出了“去I（IBM）O（Oracle）E（EMC）”行动，因为它们代表了高维护费用的存储系统，并且不够灵活。</li>
<li>迷你计算机。普通服务器。去IOE就是把大型机替换成普通服务器，eg. 阿里云</li>
<li>工作站。高端的通用微型计算机，类似于普通电脑但性能强劲</li>
<li>微型计算机，即个人计算机</li>
</ul>
<p>从构成的本质上来讲，个人计算机与前面的分类无异</p>
<h3 id="计算机的体系与结构"><a href="#计算机的体系与结构" class="headerlink" title="计算机的体系与结构"></a>计算机的体系与结构</h3><h4 id="冯诺伊曼体系"><a href="#冯诺伊曼体系" class="headerlink" title="冯诺伊曼体系"></a>冯诺伊曼体系</h4><ul>
<li>定义： 将程序指令和数据一起存储的计算机设计概念结构。结论： 存储程序指令，设计通用电路（因为早期计算机仅含固定用途程序，改变程序得更改结构、重新设计电路）</li>
<li>冯诺依曼体系建造的计算机一定要有： 存储器、 控制器、 运算器、 输入/输出设备。如下图：<br><img src="/img/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%84%E6%88%90%E5%8E%9F%E7%90%86/%E5%86%AF%E8%AF%BA%E4%BE%9D%E6%9B%BC.png"></li>
<li>瓶颈：CPU和存储器速率之间的问题无法调和（CPU处理速度很快，经常空转等待数据传输）</li>
</ul>
<h4 id="现代计算机的结构"><a href="#现代计算机的结构" class="headerlink" title="现代计算机的结构"></a>现代计算机的结构</h4><p>在冯诺依曼体系的基础上解决CPU与存储设备之间的性能差异问题<br><img src="/img/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%84%E6%88%90%E5%8E%9F%E7%90%86/%E7%8E%B0%E4%BB%A3%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%93%E6%9E%84.png"></p>
<h3 id="计算机的层次与编程语言"><a href="#计算机的层次与编程语言" class="headerlink" title="计算机的层次与编程语言"></a>计算机的层次与编程语言</h3><h4 id="程序翻译与程序解释"><a href="#程序翻译与程序解释" class="headerlink" title="程序翻译与程序解释"></a>程序翻译与程序解释</h4><p>程序翻译（编译）：使用编译器，把整个程序由高级语言L1（C++等）翻译成<strong>计算机语言</strong>L0（计算机能执行的，二进制等）<br>程序解释：把L1语言（Python，JS，PHP）的程序转变成使用L0语言实现另一个程序（翻译器），再L0解释器把目标程序执行。<br>Java是先编译再解释</p>
<h4 id="计算机的层次与编程语言-1"><a href="#计算机的层次与编程语言-1" class="headerlink" title="计算机的层次与编程语言"></a>计算机的层次与编程语言</h4><p><img src="/img/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%84%E6%88%90%E5%8E%9F%E7%90%86/%E8%AE%A1%E7%AE%97%E6%9C%BA%E5%B1%82%E6%AC%A1.png"></p>
<ul>
<li>硬件逻辑层： 门、触发器等逻辑电路组成。 属于电子工程的领域</li>
<li>微程序机器层： 编程语言是微指令集。 微指令所组成的微程序直接交由硬件执行</li>
<li>传统机器层： 编程语言是CPU指令集（机器指令）。编程语言和硬件是直接相关。<br>注： 一条机器指令对应一个微程序， 一个微程序对应一组微指令</li>
<li>操作系统层是在软件和硬件之间的适配层</li>
<li>汇编语言层。 编程语言是<strong>汇编语言</strong>（一种符号语言，其实可以理解为一种过渡态语言）。 汇编语言可以用汇编器翻译成可直接执行的机器语言</li>
<li>高级语言层。 <strong>高级语言</strong>（eg. Java等）</li>
</ul>
<h3 id="计算机的计算单位"><a href="#计算机的计算单位" class="headerlink" title="计算机的计算单位"></a>计算机的计算单位</h3><h4 id="容量单位"><a href="#容量单位" class="headerlink" title="容量单位"></a>容量单位</h4><p>用8个比特位bit（0或1）表示一个字节（Byte），后面就有kb，MB，GB，TB（都是1024倍关系）等。但是硬盘商是用1000做倍数关系，所以500G硬盘格式化后只有465G。</p>
<h4 id="速度单位"><a href="#速度单位" class="headerlink" title="速度单位"></a>速度单位</h4><ul>
<li>100M宽带，即是网络常用单位(Mbps)。 100M/s = 100Mbps = 100Mbit/s， 而 100Mbit/s=(100/8)MB/s=12.5MB/s。故100M宽带峰值下载速度只有12M/s</li>
<li>CPU的速度一般体现为CPU的时钟频率（Hz）， 主流CPU是2GHz以上</li>
</ul>
<h3 id="计算机的字符与编码集"><a href="#计算机的字符与编码集" class="headerlink" title="计算机的字符与编码集"></a>计算机的字符与编码集</h3><h4 id="字符编码集的历史"><a href="#字符编码集的历史" class="headerlink" title="字符编码集的历史"></a>字符编码集的历史</h4><ul>
<li>ASCII码（读为 ask 2 码：用7个bits就可以完全表示ASCII码。包含了基本的字母，符号等</li>
<li>Extended ASCII码：改为8个bits。添加了常见的数学运算符，欧洲字符，表格符等，后来又有了国际化</li>
</ul>
<h4 id="中文编码集"><a href="#中文编码集" class="headerlink" title="中文编码集"></a>中文编码集</h4><ul>
<li>初始版：GB2312。收录了汉字和中文符号等。</li>
<li>GBK： 向下兼容GB2312，向上支持国际ISO标准。支持全部中日韩汉字。但是GBK和GB2312毕竟还是不能全球都用的。</li>
<li>兼容全球的字符集Unicode。 其定义了世界通用的符号集， 用UTF-*实现了编码。eg. UTF-8（编程推荐使用）以字节为单位对Unicode进行编码。</li>
</ul>
<h2 id="组成篇"><a href="#组成篇" class="headerlink" title="组成篇"></a>组成篇</h2><h3 id="计算机的总线"><a href="#计算机的总线" class="headerlink" title="计算机的总线"></a>计算机的总线</h3><h4 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h4><p>连接计算机不同设备的数据线。eg. 通用串行总线（USB）， PCI总线， ISA总线等<br><img src="/img/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%84%E6%88%90%E5%8E%9F%E7%90%86/%E6%80%BB%E7%BA%BF.png"></p>
<h4 id="分类"><a href="#分类" class="headerlink" title="分类"></a>分类</h4><p>片内总线（芯片内部的总线）和系统总线（各设备之间的，分为 数据总线，地址总线和控制总线）</p>
<h4 id="总线的仲裁"><a href="#总线的仲裁" class="headerlink" title="总线的仲裁"></a>总线的仲裁</h4><p>原因：需要决定不同设备使用总线的优先顺序<br>方法：链式查询；计时器定时查询；独立请求（各有优缺点，详情略）</p>
<h3 id="计算机的输入输出设备（I-O设备）"><a href="#计算机的输入输出设备（I-O设备）" class="headerlink" title="计算机的输入输出设备（I/O设备）"></a>计算机的输入输出设备（I/O设备）</h3><h4 id="常见的输入输出设备"><a href="#常见的输入输出设备" class="headerlink" title="常见的输入输出设备"></a>常见的输入输出设备</h4><p>键盘，鼠标，扫描仪；显示器，打印机，投影仪</p>
<h4 id="输入输出接口的通用设计"><a href="#输入输出接口的通用设计" class="headerlink" title="输入输出接口的通用设计"></a>输入输出接口的通用设计</h4><p>数据线， 状态线， 命令线，设备选择线</p>
<h4 id="CPU与IO设备的通信"><a href="#CPU与IO设备的通信" class="headerlink" title="CPU与IO设备的通信"></a>CPU与IO设备的通信</h4><p>注意：CPU速度与IO设备速度不一致，CPU快得多<br>方式：程序中断； DMA（直接存储器访问）</p>
<h3 id="计算机存储器"><a href="#计算机存储器" class="headerlink" title="计算机存储器"></a>计算机存储器</h3><h4 id="存储器的分类"><a href="#存储器的分类" class="headerlink" title="存储器的分类"></a>存储器的分类</h4><p><img src="/img/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%84%E6%88%90%E5%8E%9F%E7%90%86/%E5%AD%98%E5%82%A8%E5%99%A8%E5%88%86%E7%B1%BB1.png"><br><img src="/img/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%84%E6%88%90%E5%8E%9F%E7%90%86/%E5%AD%98%E5%82%A8%E5%99%A8%E5%88%86%E7%B1%BB2.png"></p>
<h4 id="存储器的层次结构"><a href="#存储器的层次结构" class="headerlink" title="存储器的层次结构"></a>存储器的层次结构</h4><p>读写速度（eg. 5400转）和存储容量（eg.1T）都会影响存储器的价格，所有有每比特位置价格的概念<br>存储器的层次结构：</p>
<ul>
<li>缓存。速度快价格高， 解决主存速度不足的问题。是在CPU与驻村之间增加一层速度快（容量小）的Cache。 局部性原理是指CPU访问存储器时，无论是存取指令还是存取数据，所访问的存储单元都趋于聚集在一个较小的连续区域中。</li>
<li>主存。中等，eg. 内存条</li>
<li>辅存。速度慢价格低， 解决主存容量不足的问题。eg. 硬盘</li>
</ul>
<p><img src="/img/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%84%E6%88%90%E5%8E%9F%E7%90%86/%E5%AD%98%E5%82%A8%E5%99%A8.png"></p>
<h4 id="计算机的主存储器"><a href="#计算机的主存储器" class="headerlink" title="计算机的主存储器"></a>计算机的主存储器</h4><p>计算机断电，内存数据丢失。这是因为内存条是用RAM，通过电容存储数据，必须隔一段时间刷新一次（有电才能刷新）<br>对32位系统，地址总线有32位，支持的内存最大为2^32=4 * 2^30 = 4GB ; 64为则大大提高，为2^34 GB<br>PS：硬盘有固态硬盘（SSD 盘，新式硬盘）、机械硬盘（HDD 传统硬盘）、混合硬盘（HHD 一块基于传统机械硬盘诞生出来的新硬盘）。SSD采用闪存颗粒来存储，HDD采用磁性碟片来存储，混合硬盘(HHD: Hybrid Hard Disk)是把磁性硬盘和闪存集成到一起的一种硬盘</p>
<h4 id="计算机的辅助存储器"><a href="#计算机的辅助存储器" class="headerlink" title="计算机的辅助存储器"></a>计算机的辅助存储器</h4><p>计算机断电， 磁盘数据不会丢失。因为磁盘是盘片（ 表面是可磁化的硬磁特性材料）和读写磁头组成（ 移动磁头径向运动读取磁道信息）。磁盘的读取算法忽略没记</p>
<h4 id="计算机的高速缓存"><a href="#计算机的高速缓存" class="headerlink" title="计算机的高速缓存"></a>计算机的高速缓存</h4><p>对于主存，有：</p>
<ul>
<li>字： 是指存放在一个存储单元中的二进制代码组合（一个字有32位就是一个字占32个bit的意思）。字的地址包含两个部分： 前m位指定字块的地址（块地址）； 后b位指定字在字块中的地址（块内地址），如下图。字块是由字构成的（eg. 一个字块共B个字）</li>
<li>字块： 存储在连续的存储单元中而被看作是一个单元的一组字。主存是由字块构成的（eg. 主存共M个字块）。主存内的总字数为B*M<br><img src="/img/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%84%E6%88%90%E5%8E%9F%E7%90%86/%E5%AD%97.png"> 有2^m = M ; 2^b = B</li>
</ul>
<p>问题：假设主存用户空间容量为4G，字块大小为4M，字长为32位，则对于字<br>地址中的块地址m和块内地址b的位数，至少应该是多少？<br><img src="/img/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%84%E6%88%90%E5%8E%9F%E7%90%86/%E9%A2%98.png"></p>
<h5 id="高速缓存的工作原理"><a href="#高速缓存的工作原理" class="headerlink" title="高速缓存的工作原理"></a>高速缓存的工作原理</h5><p>对于高速缓存，原理类似于主存<br>衡量高速缓存性能：命中率、访问效率和平均访问时间。具体算法略（大概就是CPU要的数据刚好就在高速缓存中，而不需要高速缓存区主存拿）</p>
<h5 id="高速缓存的替换策略"><a href="#高速缓存的替换策略" class="headerlink" title="高速缓存的替换策略"></a>高速缓存的替换策略</h5><p>高速缓存的替换时机，怎么使得CPU要的数据尽可能在高速缓存中，用了几种算法，详情略</p>
<h3 id="计算机的指令系统"><a href="#计算机的指令系统" class="headerlink" title="计算机的指令系统"></a>计算机的指令系统</h3><h4 id="机器指令的形式"><a href="#机器指令的形式" class="headerlink" title="机器指令的形式"></a>机器指令的形式</h4><p>机器指令主要由两部分组成：</p>
<ul>
<li>操作码： 操作码指明指令所要完成的操作； 操作码的位数反映了机器的操作种类（eg. 8位则是2^8 = 256 种操作）</li>
<li>地址码： 地址码直接给出操作数或者操作数的地址。 分三地址指令、二地址指令和一地址指令</li>
</ul>
<h4 id="机器指令的操作类型"><a href="#机器指令的操作类型" class="headerlink" title="机器指令的操作类型"></a>机器指令的操作类型</h4><p>数据传输类型： 存在与寄存器之间、寄存器与存储单元、存储单元之间传送。 数据读写、交换地址数据、清零置一等操作<br>算术逻辑操作： 操作数之间的加减乘除、位运算等<br>移位操作： 完成数据在算术逻辑单元的必要操作<br>控制指令： 等待指令、停机指令、空操作指令、中断指令等</p>
<h4 id="机器指令的寻址方式"><a href="#机器指令的寻址方式" class="headerlink" title="机器指令的寻址方式"></a>机器指令的寻址方式</h4><p>指令寻址：顺序寻址，跳跃寻址<br>数据寻址：立即寻址，直接寻址，间接寻址</p>
<h3 id="计算机的控制器和运算器"><a href="#计算机的控制器和运算器" class="headerlink" title="计算机的控制器和运算器"></a>计算机的控制器和运算器</h3><p>CPU由控制器，运算器，高速缓存等组成。<br>控制器是协调和控制计算机运行的。由程序计数器，时序发生器，指令译码器，各种类型的寄存器，总线所组成<br>运算器是用来进行数据运算加工的。由数据缓冲器，ALU，通用寄存器，状态寄存器，总线所组成</p>
<h4 id="计算机指令的执行过程"><a href="#计算机指令的执行过程" class="headerlink" title="计算机指令的执行过程"></a>计算机指令的执行过程</h4><ol>
<li>取指令： 从缓存取指令； 送到指令寄存器</li>
<li>分析指令： 指令译码器译码； 发出控制信号； 程序计数器+1</li>
<li>执行指令： 装载数据到寄存器； ALU处理数据； 记录运算状态； 送出运算结果</li>
</ol>
<h4 id="CPU的流水线设计"><a href="#CPU的流水线设计" class="headerlink" title="CPU的流水线设计"></a>CPU的流水线设计</h4><p>即可以同时对多条指令进行流水处理，而不是串行处理<br><img src="/img/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%84%E6%88%90%E5%8E%9F%E7%90%86/CPU.png"></p>
<p>参考资料<br><a href="https://coding.imooc.com/class/355.html" target="_blank" rel="noopener">慕课网</a><br>《计算机科学导论》</p>
]]></content>
      <categories>
        <category>Basic knowledge</category>
      </categories>
      <tags>
        <tag>Computer</tag>
      </tags>
  </entry>
  <entry>
    <title>网络概述</title>
    <url>/2019/10/02/%E7%BD%91%E7%BB%9C%E6%A6%82%E8%BF%B0/</url>
    <content><![CDATA[<blockquote>
<p>转载声明：该文章有一部分转载自<a href="https://github.com/CavsZhouyou/Front-End-Interview-Notebook/blob/master/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C.md#5-dns-%E4%B8%BA%E4%BB%80%E4%B9%88%E4%BD%BF%E7%94%A8-udp-%E5%8D%8F%E8%AE%AE%E4%BD%9C%E4%B8%BA%E4%BC%A0%E8%BE%93%E5%B1%82%E5%8D%8F%E8%AE%AE" target="_blank" rel="noopener">CavsZhouyou</a>。欢迎大家去原博主观看，总结的更好</p>
</blockquote>
<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><p>详细资料可以参考： <a href="http://www.ruanyifeng.com/blog/2012/05/internet_protocol_suite_part_i.html" target="_blank" rel="noopener">《互联网协议入门（一）》</a> <a href="http://www.ruanyifeng.com/blog/2012/06/internet_protocol_suite_part_ii.html" target="_blank" rel="noopener">《互联网协议入门（二）》</a></p>
<h2 id="OSI-七层模型"><a href="#OSI-七层模型" class="headerlink" title="OSI 七层模型"></a>OSI 七层模型</h2><p>应用层 ：为应用程序（进程）提供服务，并管理应用程序之间的通信（SMTP、HTTP、FTP、DNS）<br>表示层 ：处理数据的标识问题，比如编码、格式转化、加密解密等<br>会话层 ：负责建立管理和断开通信连接，实现数据同步<br>传输层 ：负责向<strong>两个主机中进程之间的通讯</strong>提供服务,<strong>端到端</strong>传输数据，同时处理传输错误、控制流量等（TCP UDP）<br>网络层 ：负责为分组交换网上的不同<strong>主机</strong>提供通信服务。地址管理、路由选择（IP 协议）<br>数据链路层 ：数据分割成帧，mac 寻址、差错校验、信息纠正等。（以太网）<br>物理层 ：利用传输介质为数据链路层提供物理连接<br>发送端从应用层 → 物理层 打包发送<br>接收层从物理层 → 应用层 解析获取</p>
<h2 id="计算机网络的分类"><a href="#计算机网络的分类" class="headerlink" title="计算机网络的分类"></a>计算机网络的分类</h2><p>按网络的作用范围分类，可分为广域网（WAN，几十到几千 km，跨省跨国），城域网（MAN，5-50km，城市间），局域网（LAN，1km 以内）</p>
<h2 id="现代互联网的网络拓扑"><a href="#现代互联网的网络拓扑" class="headerlink" title="现代互联网的网络拓扑"></a>现代互联网的网络拓扑</h2><p>如图所示（这种各层的连接组成的结构就称为拓扑）</p>
<ul>
<li>边缘部分：eg.家庭或企业</li>
<li>核心部分：各 ISP（Internet Service Provider，互联网服务提供商）之前的连接（地区 ISP，主干 ISP）</li>
</ul>
<p>客户-服务器（C/S）模式：eg. 常规的 http 请求<br>对等连接（P2P）模式：当两台计算机上都运行了 P2P 的时候，两台计算机可以直接连接。eg. 迅雷的 P2P 下载</p>
<h1 id="物理层"><a href="#物理层" class="headerlink" title="物理层"></a>物理层</h1><h2 id="通信方式"><a href="#通信方式" class="headerlink" title="通信方式"></a>通信方式</h2><p>根据信息在传输线上的传送方向，分为以下三种通信方式：</p>
<ul>
<li>单工通信：单向传输</li>
<li>半双工通信：双向交替传输</li>
<li>全双工通信：双向同时传输</li>
</ul>
<h2 id="比特流"><a href="#比特流" class="headerlink" title="比特流"></a>比特流</h2><p><strong>比特流是物理层传递信息的方式</strong>。1 为高电频，0 为低电频，两者的变化就可以形成比特流，如下图<br><img src="/img/%E7%BD%91%E7%BB%9C%E6%A6%82%E8%BF%B0/%E6%AF%94%E7%89%B9%E6%B5%81.png"></p>
<h2 id="信道"><a href="#信道" class="headerlink" title="信道"></a>信道</h2><p>信道是往一个方向传送信息的媒体。（一条通信电路包含一个接收信道和一个发送信道）</p>
<h3 id="分类"><a href="#分类" class="headerlink" title="分类"></a>分类</h3><p>单工信道（只有一个方向的，eg. 有线电视），半双工信道（双方都能发送和接收，但不能同时发送/同时接收），全双工信道</p>
<h3 id="信道复用技术"><a href="#信道复用技术" class="headerlink" title="信道复用技术"></a>信道复用技术</h3><p><img src="/img/%E7%BD%91%E7%BB%9C%E6%A6%82%E8%BF%B0/%E4%BF%A1%E9%81%93%E5%A4%8D%E7%94%A8.png"><br>复用技术有：频分复用、时分复用、统计时分复用、波分复用、码分复用</p>
<h1 id="数据链路层"><a href="#数据链路层" class="headerlink" title="数据链路层"></a>数据链路层</h1><h2 id="数据链路层的基本特征"><a href="#数据链路层的基本特征" class="headerlink" title="数据链路层的基本特征"></a>数据链路层的基本特征</h2><ul>
<li><strong>封装成帧</strong>。<strong>帧是数据链路层数据的基本单位</strong>。发送端在网络层传下来的数据（IP 数据报）前后添加特定标记形成“帧”（这些标记一般是一段比特流）；接收端根据前后特定标记识别出“帧”<br><img src="/img/%E7%BD%91%E7%BB%9C%E6%A6%82%E8%BF%B0/%E5%B8%A7.png"></li>
<li><strong>透明传输</strong>。“透明”在计算机领域是非常重要的一个术语， 表示一个实际存在的事物看起来好像不存在一样，对链路层来说就是：即使控制字符在帧数据中，但是要当做不存在去处理。eg. 帧数据中，刚好有一段比特流长的跟帧尾部一毛一样。但是链路层会在这段比特流前加一个转义标志是，做到视而不见所以不会引起解析出错。</li>
<li><strong>差错检测</strong>。9-9 最大传输单元 MTU（数据链路层）</li>
</ul>
<h2 id="MAC-地址"><a href="#MAC-地址" class="headerlink" title="MAC 地址"></a>MAC 地址</h2><p>也称为物理地址、硬件地址，是链路层地址，每个设备都有唯一的 MAC 地址，长度为 6 个字节，即 48 位 bit（16 进制表示）。在命令行工具输入 ipconfig /all 可查看</p>
<h2 id="以太网协议"><a href="#以太网协议" class="headerlink" title="以太网协议"></a>以太网协议</h2><p>以太网（Ethernet）是一种使用广泛的局域网技术，是应用于数据链路层的协议。使用以太网可以完成<strong>相邻设备</strong>的数据帧传输。</p>
<h3 id="以太网的数据格式"><a href="#以太网的数据格式" class="headerlink" title="以太网的数据格式"></a>以太网的数据格式</h3><p><img src="/img/%E7%BD%91%E7%BB%9C%E6%A6%82%E8%BF%B0/%E4%BB%A5%E5%A4%AA%E7%BD%91.png"></p>
<h3 id="MAC-地址表"><a href="#MAC-地址表" class="headerlink" title="MAC 地址表"></a>MAC 地址表</h3><p>存一些 MAC 地址和硬件接口的对应关系。单纯靠以太网只能完成相邻设备的传输。 eg. A-B-C （只能说 A 通过以太网传到 B，不能说 A 通过以太网直接传到 C，因为 A 的 MAC 地址表中没有 C 的地址）</p>
<h1 id="网络层"><a href="#网络层" class="headerlink" title="网络层"></a>网络层</h1><p>负责传输的 IP 协议,作用是把数据包传输给对方(根据 IP 地址和 Mac 地址)<br>互联网中的传输是路由选择机制的，经过多台计算机和网络设备中转才连接到对方，并且我们只能知道起点和终点，过程无法悉知。</p>
<h2 id="虚拟互连网络"><a href="#虚拟互连网络" class="headerlink" title="虚拟互连网络"></a>虚拟互连网络</h2><p>使用 IP 协议，可以把异构的物理网络连接起来，使得在网络层看起来好像是一个统一的网络<br><img src="/img/%E7%BD%91%E7%BB%9C%E6%A6%82%E8%BF%B0/%E8%99%9A%E6%8B%9F%E7%BD%91%E7%BB%9C.png"></p>
<h2 id="IP-地址"><a href="#IP-地址" class="headerlink" title="IP 地址"></a>IP 地址</h2><p>IP 地址是节点被分配到的地址（可换）。长度为 32 位，常分为 4 个 8 位。常使用点分十进制来表示（每一位是 0~255）。eg. 255.111.11.71。<br>根据 ARP 协议能从 IP 地址查到 Mac 地址。Mac 地址才是通信目的地</p>
<h2 id="路由"><a href="#路由" class="headerlink" title="路由"></a>路由</h2><p>交换机是在数据链路层，路由在网络层</p>
<h3 id="路由器转发流程"><a href="#路由器转发流程" class="headerlink" title="路由器转发流程"></a>路由器转发流程</h3><p>有如下的网络拓扑结构<br><img src="/img/%E7%BD%91%E7%BB%9C%E6%A6%82%E8%BF%B0/%E8%B7%AF%E7%94%B1.png"><br>如计算机 A 想发信息给计算机 C，则流程为：</p>
<ol>
<li>A 发出目的地为 C 的 IP 数据报，<strong>查询路由表</strong>发现下一跳为 E，于是 A 将数据发给 E</li>
<li>E<strong>查询路由表</strong>发现下一跳为 F，于是 E 将数据发给 F</li>
<li>F<strong>查询路由表</strong>发现目的地 C<strong>直接连接</strong>，于是发给 C</li>
</ol>
<h3 id="路由器选择协议"><a href="#路由器选择协议" class="headerlink" title="路由器选择协议"></a>路由器选择协议</h3><p>互联网可以划分为许多较小的自治系统 AS，一个 AS 可以使用一种和别的 AS 不同的路由选择协议<br><strong>路由器是连接因特网中各局域网、广域网的设备，它会根据信道的情况自动选择和设定路由，以最佳路径，先后顺序发送信号</strong>。确定最佳路径方法就是路由选择协议。可以把路由选择协议划分为两大类：</p>
<ul>
<li>自治系统内部的路由选择：RIP 和 OSPF</li>
<li>自治系统间的路由选择：BGP</li>
</ul>
<h1 id="传输层"><a href="#传输层" class="headerlink" title="传输层"></a>传输层</h1><h2 id="UDP-与-TCP-的区别"><a href="#UDP-与-TCP-的区别" class="headerlink" title="UDP 与 TCP 的区别"></a>UDP 与 TCP 的区别</h2><ul>
<li>UDP 协议 是面向无连接的，不需要在正式传递数据之前先连接起双方；UDP 协议只是数据报文的搬运工，不保证有序（面向报文，即对传下来的报文只是加 UDP 首部）且不丢失的传递到对端；并且 UDP 协议也没有任何控制流量的算法；（支持一对一、一对多、多对一和多对多的交互通信；）总的来说 UDP 相较于 TCP 更加的<strong>轻便高效</strong>。一般可以用于直播、即时通讯、即时游戏等。</li>
<li>TCP 无论是建立连接还是断开连接都需要先需要进行握手。在传输数据的过程中，通过各种算法(通过序号、确认号、定时重传、检验和等机制，来提供可靠的数据传输服务)保证数据的可靠性，有序性（面向字节流，将大块数据分割成以报文段位单位的数据包进行传输）。每一条 TCP 连接只能是点对点的（一对一）。TCP 提供了拥塞控制机制，在网络拥塞的时候会控制发送数据的速率，有助于减少数据包的丢失和减轻网络中的拥塞程度。TCP 提供了流量控制机制，保证了通信双方的发送和接收速率相同。如果接收方可接收的缓存很小时，发送方会降低发送 速率，避免因为缓存填满而造成的数据包的丢失。当然带来的问题就是相比 UDP 来说不那么的高效</li>
</ul>
<h2 id="TCP-报文段的的首部格式"><a href="#TCP-报文段的的首部格式" class="headerlink" title="TCP 报文段的的首部格式"></a>TCP 报文段的的首部格式</h2><ul>
<li>序号：对报文段的编号，例如序号为 301</li>
<li>确认号：期望收到的下一个报文段的序号，eg. 302</li>
<li>*数据偏移：数据部分距离报文段起始处的偏移量，实际上指的是首部的长度</li>
<li>确认 ACK：当 ACK=1 时确认号字段有效，否则无效。TCP 规定，在连接建立后所有传送的报文段都必须把 ACK 置 1。</li>
<li>同步 SYN：在连接建立时用来同步序号。当 SYN=1，ACK=0 时表示这是一个连接请求报文段。若对方同意建立连接，则响应报文中 SYN=1，ACK=1。</li>
<li>终止 FIN：用来释放一个连接，当 FIN=1 时，表示此报文段的发送方的数据已发送完毕，并要求释放连接。</li>
<li>窗口：表示当前允许对方发送的最大数据量，单位是字节</li>
</ul>
<h2 id="TCP-的三次握手"><a href="#TCP-的三次握手" class="headerlink" title="TCP 的三次握手"></a>TCP 的三次握手</h2><p><img src="/img/%E7%BD%91%E7%BB%9C%E6%A6%82%E8%BF%B0/TCP3.png"><br>起初，两端都为 CLOSED 状态。在通信开始前，双方都会创建 TCP 连接（因为 HTTP 只有请求和相应）。 服务器创建完 TCP 连接 后便进入 LISTEN 状态，此时开始等待客户端发送数据。</p>
<ul>
<li>第一次：客户端向服务器端发送连接请求报文，会包含 SYN=1（标志位），Seq=X（初始序号，某个数），进入 SYN-SENT 状态；</li>
<li>第二次：服务端收到请求报文后，发送应答：SYN=1，Seq=Y（初始序号），ACK（确认号）=X+1，进入 SYN-RECEIVED 状态；</li>
<li>第三次：客户端收到同意连接的应答后，发确认报文：ACK=Y+1。进入 ESTABLISHED 状态。服务端收到受也进入 ESTABLISHED 状态。</li>
</ul>
<p><strong>要三次握手的原因</strong>：防止出现失效的连接请求报文段被服务端接收的情况，从而产生错误。<br>客户端发送了一个连接请求 A，但是因为网络原因造成了超时，这时 TCP 会启动超时重传的机制再次发送一个连接请求 B。此时请求顺利到达服务端，服务端应答完就建立了请求，然后接收数据后释放了连接。<br>假设这时候连接请求 A 在两端关闭后终于抵达了服务端，那么此时服务端会认为客户端又需要建立 TCP 连接，从而应答了该请求并进入 ESTABLISHED 状态。但是客户端其实是 CLOSED 的状态，那么就会导致服务端一直等待，造成资源的浪费。</p>
<h2 id="TCP-断开连接的四次挥手"><a href="#TCP-断开连接的四次挥手" class="headerlink" title="TCP 断开连接的四次挥手"></a>TCP 断开连接的四次挥手</h2><p><img src="/img/%E7%BD%91%E7%BB%9C%E6%A6%82%E8%BF%B0/TCP4.png"><br>TCP 是全双工的，在断开连接时两端都需要发送 FIN 和 ACK。<br>第一次握手：若客户端 A 认为数据发送完成，则它需要向服务端 B 发送连接释放请求。<br>第二次握手：B 收到连接释放请求后，会告诉应用层要释放 TCP 链接。然后会发送 ACK 包，并进入 CLOSE_WAIT 状态，此时表明 A 到 B 的连接已经释放，<strong>不再接收 A 发的数据了</strong>。但是因为 TCP 连接是双向的，所以 B 仍旧可以发送数据给 A。<br>第三次握手：<strong>B 如果此时还有没发完的数据会继续发送，完毕后会向 A 发送连接释放请求</strong>，然后 B 便进入 LAST-ACK 状态。<br>第四次握手：A 收到释放请求后，向 B 发送确认应答，此时 A 进入 TIME-WAIT 状态。该状态会持续 2MSL（报文最大生存时间，指报文段在网络中生存的时间，超时会被抛弃） 时间，若该时间段内没有 B 的重发请求的话，就进入 CLOSED 状态。当 B 收到确认应答后，也便进入 CLOSED 状态。</p>
<p>PS：通过延迟确认的技术（通常有时间限制，否则对方会误认为需要重传），可以将第二次和第三次握手合并，延迟 ACK 包的发送。<br><strong>为什么客户端 A 要进入 TIME-WAIT 状态，等待 2MSL 时间后才进入 CLOSED 状态？</strong><br>为了保证 B 能收到 A 的确认应答。若 A 发完确认应答后直接进入 CLOSED 状态，如果确认应答因为网络问题一直没有到达，那么会造成 B 不能正常关闭。（B 若 1MSL 后没收到，会重新发送 FIN 包。所以来回是 2MSL）</p>
<h2 id="TCP-的流量控制"><a href="#TCP-的流量控制" class="headerlink" title="TCP 的流量控制"></a>TCP 的流量控制</h2><p>流量控制是为了控制发送方发送速率，保证接收方来得及接收。是通过滑动窗口和拥堵窗口实现的</p>
<ul>
<li>滑动窗口主要是用于接收方，保证接收方能够接受数据。接收方通过报文告知发送方当前接收窗口剩余大小，发送窗口根据该值提调整自身窗口来控制发送速率。（将窗口字段设置为 0，则发送方不能发送数据）<br><img src="/img/%E7%BD%91%E7%BB%9C%E6%A6%82%E8%BF%B0/%E7%AA%97%E5%8F%A3.png"></li>
<li>*拥堵窗口，主要用于网络，防止过多的数据拥堵网络，避免负载过大的情况</li>
</ul>
<h2 id="连续-ARQ-协议"><a href="#连续-ARQ-协议" class="headerlink" title="连续 ARQ 协议"></a>连续 ARQ 协议</h2><p>连续 ARQ 协议是为了解决停止等待 ARQ 协议对于信道的利用率过低的问题。它通过连续发送一组分组，然后再等待对分组的 确认回答，对于如何处理分组中可能出现的差错恢复情况，一般可以使用滑动窗口协议和选择重传协议来实现。</p>
<h3 id="滑动窗口协议"><a href="#滑动窗口协议" class="headerlink" title="滑动窗口协议"></a>滑动窗口协议</h3><p>使用滑动窗口协议，在发送方维持了一个发送窗口，发送窗口以前的分组是已经发送并确认了的分组，发送窗口中包含了已经发 送但未确认的分组和允许发送但还未发送的分组，发送窗口以后的分组是缓存中还不允许发送的分组。当发送方向接收方发送分 组时，会依次发送窗口内的所有分组，并且设置一个定时器，这个定时器可以理解为是最早发送但未收到确认的分组。如果在定 时器的时间内收到某一个分组的确认回答，则滑动窗口，将窗口的首部移动到确认分组的后一个位置，此时如果还有已发送但没 有确认的分组，则重新设置定时器，如果没有了则关闭定时器。如果定时器超时，则重新发送所有已经发送但还未收到确认的分 组。</p>
<p>接收方使用的是累计确认的机制，对于所有按序到达的分组，接收方返回一个分组的肯定回答。如果收到了一个乱序的分组，那 么接方会直接丢弃，并返回一个最近的按序到达的分组的肯定回答。使用累计确认保证了确认号以前的分组都已经按序到达了， 所以发送窗口可以移动到已确认分组的后面。</p>
<p>滑动窗口协议的缺点是因为使用了累计确认的机制，如果出现了只是窗口中的第一个分组丢失，而后面的分组都按序到达的情况 的话，那么滑动窗口协议会重新发送所有的分组，这样就造成了大量不必要分组的丢弃和重传。</p>
<h3 id="选择重传协议"><a href="#选择重传协议" class="headerlink" title="选择重传协议"></a>选择重传协议</h3><p>因为滑动窗口使用累计确认的方式，所以会造成很多不必要分组的重传。使用选择重传协议可以解决这个问题。</p>
<p>选择重传协议在发送方维护了一个发送窗口。发送窗口的以前是已经发送并确认的分组，窗口内包含了已发送但未被确认的分组， 已确认的乱序分组，和允许发送但还未发送的分组，发送窗口以后的是缓存中还不允许发送的分组。选择重传协议与滑动窗口协 议最大的不同是，发送方发送分组时，为一个分组都创建了一个定时器。当发送方接受到一个分组的确认应答后，取消该分组的 定时器，并判断接受该分组后，是否存在由窗口首部为首的连续的确认分组，如果有则向后移动窗口的位置，如果没有则将该分 组标识为已接收的乱序分组。当某一个分组定时器到时后，则重新传递这个分组。</p>
<p>在接收方，它会确认每一个正确接收的分组，不管这个分组是按序的还是乱序的，乱序的分组将被缓存下来，直到所有的乱序分 组都到达形成一个有序序列后，再将这一段分组交付给上层。对于不能被正确接收的分组，接收方直接忽略该分组。</p>
<p>详细资料可以参考： <a href="https://blog.csdn.net/guoweimelon/article/details/50879588" target="_blank" rel="noopener">《TCP 连续 ARQ 协议和滑动窗口协议》</a></p>
<h2 id="TCP-的可靠运输机制"><a href="#TCP-的可靠运输机制" class="headerlink" title="TCP 的可靠运输机制"></a>TCP 的可靠运输机制</h2><p>TCP 的可靠运输机制是基于连续 ARQ 协议和滑动窗口协议的。</p>
<p>TCP 协议在发送方维持了一个发送窗口，发送窗口以前的报文段是已经发送并确认了的报文段，发送窗口中包含了已经发送但 未确认的报文段和允许发送但还未发送的报文段，发送窗口以后的报文段是缓存中还不允许发送的报文段。当发送方向接收方发 送报文时，会依次发送窗口内的所有报文段，并且设置一个定时器，这个定时器可以理解为是最早发送但未收到确认的报文段。 如果在定时器的时间内收到某一个报文段的确认回答，则滑动窗口，将窗口的首部向后滑动到确认报文段的后一个位置，此时如 果还有已发送但没有确认的报文段，则重新设置定时器，如果没有了则关闭定时器。如果定时器超时，则重新发送所有已经发送 但还未收到确认的报文段，并将超时的间隔设置为以前的两倍。当发送方收到接收方的三个冗余的确认应答后，这是一种指示， 说明该报文段以后的报文段很有可能发生丢失了，那么发送方会启用快速重传的机制，就是当前定时器结束前，发送所有的已发 送但确认的报文段。</p>
<p>接收方使用的是累计确认的机制，对于所有按序到达的报文段，接收方返回一个报文段的肯定回答。如果收到了一个乱序的报文 段，那么接方会直接丢弃，并返回一个最近的按序到达的报文段的肯定回答。使用累计确认保证了返回的确认号之前的报文段都 已经按序到达了，所以发送窗口可以移动到已确认报文段的后面。</p>
<p>发送窗口的大小是变化的，它是由接收窗口剩余大小和网络中拥塞程度来决定的，TCP 就是通过控制发送窗口的长度来控制报文 段的发送速率。</p>
<p>但是 TCP 协议并不完全和滑动窗口协议相同，因为许多的 TCP 实现会将失序的报文段给缓存起来，并且发生重传时，只会重 传一个报文段，因此 TCP 协议的可靠传输机制更像是窗口滑动协议和选择重传协议的一个混合体。制）</p>
<h2 id="TCP-的拥塞控制机制"><a href="#TCP-的拥塞控制机制" class="headerlink" title="TCP 的拥塞控制机制"></a>TCP 的拥塞控制机制</h2><p>TCP 的拥塞控制主要是根据网络中的拥塞情况来控制发送方数据的发送速率，如果网络处于拥塞的状态，发送方就减小发送的 速率，这样一方面是为了避免继续增加网络中的拥塞程度，另一方面也是为了避免网络拥塞可能造成的报文段丢失。</p>
<p>TCP 的拥塞控制主要使用了四个机制，分别是慢启动、拥塞避免、快速重传和快速恢复。</p>
<p>慢启动的基本思想是，因为在发送方刚开始发送数据的时候，并不知道网络中的拥塞程度，所以先以较低的速率发送，进行试探 ，每次收到一个确认报文，就将发动窗口的长度加一，这样每个 RTT 时间后，发送窗口的长度就会加倍。当发送窗口的大小达 到一个阈值的时候就进入拥塞避免算法。</p>
<p>拥塞避免算法是为了避免可能发生的拥塞，将发送窗口的大小由每过一个 RTT 增长一倍，变为每过一个 RTT ，长度只加一。 这样将窗口的增长速率由指数增长，变为加法线性增长。</p>
<p>快速重传指的是，当发送方收到三个冗余的确认应答时，因为 TCP 使用的是累计确认的机制，所以很有可能是发生了报文段的 丢失，因此采用立即重传的机制，在定时器结束前发送所有已发送但还未接收到确认应答的报文段。</p>
<p>快速恢复是对快速重传的后续处理，因为网络中可能已经出现了拥塞情况，所以会将慢启动的阀值减小为原来的一半，然后将拥 塞窗口的值置为减半后的阀值，然后开始执行拥塞避免算法，使得拥塞窗口缓慢地加性增大。简单来理解就是，乘性减，加性增。</p>
<p>TCP 认为网络拥塞的主要依据是报文段的重传次数，它会根据网络中的拥塞程度，通过调整慢启动的阀值，然后交替使用上面四 种机制来达到拥塞控制的目的。</p>
<p>详细资料可以参考： <a href="https://www.jianshu.com/p/7d59f9292b03" target="_blank" rel="noopener">《TCP 的拥塞控制机制》</a> <a href="http://www.voidcn.com/article/p-vrdkquop-ms.html" target="_blank" rel="noopener">《网络基本功：TCP 拥塞控制机制》</a><br><a href="https://juejin.im/post/5b7be0b2e51d4538db34a51e#heading-1" target="_blank" rel="noopener">《搞定计算机网络面试，看这篇就够了（补充版）》</a></p>
<h1 id="应用层"><a href="#应用层" class="headerlink" title="应用层"></a>应用层</h1><h2 id="常用端口"><a href="#常用端口" class="headerlink" title="常用端口"></a>常用端口</h2><p><img src="/img/%E7%BD%91%E7%BB%9C%E6%A6%82%E8%BF%B0/%E7%AB%AF%E5%8F%A3.png"></p>
<h2 id="输入-URL-到页面渲染的整个流程"><a href="#输入-URL-到页面渲染的整个流程" class="headerlink" title="输入 URL 到页面渲染的整个流程"></a>输入 URL 到页面渲染的整个流程</h2><ul>
<li>首先浏览器主进程接管，开了一个下载线程</li>
<li>DNS 寻址</li>
<li>然后进行 HTTP 请求：先 DNS 查询获取 IP 地址，接着是 TCP 三次握手，如果是 https 的话，还会 TCP 握手结束后就会进行 TLS 握手，然后就开始正式的传输数据</li>
<li>接收到响应的文件之后，<strong>首先浏览器会判断状态码是什么</strong>，如果是 200 那就继续解析，如果 400 或 500 的话就会报错，如果 300 的话会进行重定向</li>
<li>拿到文件后浏览器开始解析文件，通过文件的编码格式正确地去解码文件</li>
<li>将解析完的内容转交给渲染进程管理（见浏览器这篇博文）。解析 DOM 时，解析绘制过程中，当浏览器遇到 link 标签或者 script、img 等标签，浏览器会去下载这些内容，遇到时候缓存的使用缓存，不适用缓存的重新下载资源</li>
<li>绘制结束后，关闭 TCP 连接，会有四次挥手</li>
</ul>
<h2 id="DNS-协议"><a href="#DNS-协议" class="headerlink" title="DNS 协议"></a>DNS 协议</h2><p>DNS 是一个分布式数据库（每个站点只保留它自己的那部分数据），提供了主机名和 IP 地址之间相互转换的服务</p>
<h3 id="域名的层级结构"><a href="#域名的层级结构" class="headerlink" title="域名的层级结构"></a>域名的层级结构</h3><p>DNS 系统采用的是分布式的解析方案，整个 DNS 架构是一种层次树状结构（树的根节点是根域名），这个树状结构称为 DNS 域名空间。</p>
<p>域名具有层次结构，从上到下（从右往左）依次为：根域名（.root, 一般情况下都是省略不写的）；顶级域名，top-level domain，缩写为 TLD（.com，.cn，.net 等）；次级域名，second-level domain，缩写为 SLD（.baidu，.gooele 等）；主机名，host / 三级域名（www, mial 等）</p>
<p>根据域名的层级结构，管理不同层级域名的服务器，可以分为根域名服务器、顶级域名服务器和权威域名服务器</p>
<h3 id="DNS-的查询过程"><a href="#DNS-的查询过程" class="headerlink" title="DNS 的查询过程"></a>DNS 的查询过程</h3><p>DNS 是基于 UDP 做的查询。DNS 的查询过程一般为，我们首先将 DNS 请求发送到本地 DNS 服务器，由本地 DNS 服务器来代为请求。</p>
<ul>
<li>从”根域名服务器”查到”顶级域名服务器”的 NS 记录（Name Server，负责管理该域名的服务器地址）和 A 记录（address，即 IP 地址）。</li>
<li>从”顶级域名服务器”查到”次级域名服务器”的 NS 记录和 A 记录。</li>
<li>从”次级域名服务器”查出”主机名”的 IP 地址。</li>
</ul>
<p>比如我们如果想要查询 <code>www.google.com</code> 的 IP 地址，我们首先会将请求发送到本地的 DNS 服务器中，本地 DNS 服务器会判断是否存在该域名的缓存；如果不存在，则向根<strong>域名服务器</strong>发送一个请求，它会返回负责 <code>.com</code> 的<strong>顶级域名服务器</strong>的 IP 地址的列表；然后本地 DNS 服务器再向其中一个负责 <code>.com</code> 的顶级域名服务器发送一个请求，它会返回负责 <code>.baidu</code> 的<strong>权威域名服务器</strong>的 IP 地址列表。然后本地 DNS 服务器再向其中一个权威域名服务器发送一个请求，它会返回一个对应的主机名的 IP 地址列表。（一个域名可能有多个 IP 地址，所以返回的是一个列表)</p>
<p>详细步骤见<a href="http://www.ruanyifeng.com/blog/2016/06/dns.html" target="_blank" rel="noopener">阮一峰-DNS 原理入门</a>，<a href="http://www.ruanyifeng.com/blog/2018/05/root-domain.html" target="_blank" rel="noopener">阮一峰-根域名的知识</a></p>
<h3 id="DNS-记录和报文"><a href="#DNS-记录和报文" class="headerlink" title="DNS 记录和报文"></a>DNS 记录和报文</h3><p>DNS 服务器中以资源记录的形式存储信息，每一个 DNS 响应报文一般包含多条资源记录。一条资源记录的具体的格式为</p>
<p>（Name，Value，Type，TTL）</p>
<p>其中 TTL 是资源记录的生存时间，它定义了资源记录能够被其他的 DNS 服务器缓存多长时间。</p>
<p>常用的一共有四种 Type 的值，分别是 A、NS、CNAME 和 MX ，不同 Type 的值，对应资源记录代表的意义不同。</p>
<ul>
<li>如果 Type = A（Address），则 Name 是主机名，Value 是主机名对应的 IP 地址。因此一条记录为 A 的资源记录，提供了标 准的主机名到 IP 地址的映射。</li>
<li>如果 Type = NS（Name Server），则 Name 是个域名，Value 是负责该域名的 DNS 服务器的主机名。这个记录主要用于 DNS 链式 查询时，返回下一级需要查询的 DNS 服务器的信息。</li>
<li>如果 Type = CNAME（Canonical Name），则 Name 为别名，Value 为该主机的规范主机名。该条记录用于向查询的主机返回一个主机名 对应的规范主机名，从而告诉查询主机去查询这个主机名的 IP 地址。主机别名主要是为了通过给一些复杂的主机名提供 一个便于记忆的简单的别名。</li>
<li>如果 Type = MX（Mail eXchange），则 Name 为一个邮件服务器的别名，Value 为邮件服务器的规范主机名。它的作用和 CNAME 是一 样的，都是为了解决规范主机名不利于记忆的缺点。</li>
</ul>
<h3 id="递归查询和迭代查询"><a href="#递归查询和迭代查询" class="headerlink" title="递归查询和迭代查询"></a>递归查询和迭代查询</h3><p>递归查询指的是查询请求发出后，域名服务器代为向下一级域名服务器发出请求，最后向用户返回查询的最终结果。使用递归查询，用户只需要发出一次查询请求。</p>
<p>迭代查询指的是查询请求后，域名服务器返回单次查询的结果。下一级的查询由用户自己请求。使用迭代查询，用户需要发出多次的查询请求。</p>
<p>一般我们向本地 DNS 服务器发送请求的方式就是递归查询，因为我们只需要发出一次请求，然后本地 DNS 服务器返回给我们最终的请求结果。而本地 DNS 服务器向其他域名服务器请求的过程是迭代查询的过程，因为每一次域名服务器只返回单次查询的结果，下一级的查询由本地 DNS 服务器自己进行。</p>
<h3 id="DNS-缓存"><a href="#DNS-缓存" class="headerlink" title="DNS 缓存"></a>DNS 缓存</h3><p>在一个请求中，当某个 DNS 服务器接收到一个 DNS 回答后，它能够将回答中的信息缓存在本地存储器中。返回的资源记录中的 TTL 代表了该条记录的缓存的时间。</p>
<h3 id="负载均衡"><a href="#负载均衡" class="headerlink" title="负载均衡"></a>负载均衡</h3><h4 id="负载均衡的概念"><a href="#负载均衡的概念" class="headerlink" title="负载均衡的概念"></a>负载均衡的概念</h4><p>在分布式系统中，负载均衡（Load Balancing）是一种将任务分派到多个服务端进程的方法，通常用于<strong>将工作负载分布到多个服务器</strong>来提高网站、应用、数据库或其他服务的性能和可靠性。能用于HTTP，HTTPS，DNS，TCP，UDP等的转发</p>
<p>实现负载均衡主要有两个目的。第一个目的是将任务的处理负载均摊到不同的进程，以减少单一进程的负载，以达到处理能力水平扩容的目的。第二个目的则是提高容错能力</p>
<h4 id="负载均衡器如何选择要转发的后端服务器？"><a href="#负载均衡器如何选择要转发的后端服务器？" class="headerlink" title="负载均衡器如何选择要转发的后端服务器？"></a>负载均衡器如何选择要转发的后端服务器？</h4><p>负载均衡器一般根据两个因素来决定要将请求转发到哪个服务器。首先，确保所选择的服务器能够对请求做出响应，然后根据预先配置的规则从健康服务器池（healthy pool）中进行选择。</p>
<p>因为，负载均衡器应当只选择能正常做出响应的后端服务器，因此就需要有一种判断后端服务器是否「健康」的方法。为了监视后台服务器的运行状况，运行状态检查服务会定期尝试使用转发规则定义的协议和端口去连接后端服务器。如果，服务器无法通过健康检查，就会从池中剔除，保证流量不会被转发到该服务器，直到其再次通过健康检查为止。</p>
<h4 id="负载均衡算法"><a href="#负载均衡算法" class="headerlink" title="负载均衡算法"></a>负载均衡算法</h4><p>负载均衡算法决定了后端的哪些健康服务器会被选中。几个常用的算法：</p>
<ul>
<li>Round Robin（轮询）：为第一个请求选择列表中的第一个服务器，然后按顺序向下移动列表直到结尾，然后循环。</li>
<li>Least Connections（最小连接）：优先选择连接数最少的服务器，在普遍会话较长的情况下推荐使用。</li>
<li>Source：根据请求源的 IP 的散列（hash）来选择要转发的服务器。这种方式可以一定程度上保证特定用户能连接到相同的服务器。</li>
</ul>
<h4 id="DNS-实现负载均衡"><a href="#DNS-实现负载均衡" class="headerlink" title="DNS 实现负载均衡"></a>DNS 实现负载均衡</h4><p>DNS 可以用于在冗余的服务器上实现负载平衡。因为现在一般的大型网站使用多台服务器提供服务，因此一个域名可能会对应多个服务器地址。当用户发起网站域名的 DNS 请求的时候，DNS 服务器返回这个域名所对应的服务器 IP 地址的集合，但在每个回答中，会循环这些 IP 地址的顺序，用户一般会选择<strong>排在前面的地址</strong>发送请求。以此将用户的请求均衡的分配到各个不同的服务器上，这样来实现负载均衡</p>
<h3 id="DNS-为什么使用-UDP-协议作为传输层协议"><a href="#DNS-为什么使用-UDP-协议作为传输层协议" class="headerlink" title="DNS 为什么使用 UDP 协议作为传输层协议"></a>DNS 为什么使用 UDP 协议作为传输层协议</h3><p>DNS 使用 UDP 协议作为传输层协议的主要原因是为了避免使用 TCP 协议时造成的连接时延。因为为了得到一个域名的 IP 地<br>址，往往会向多个域名服务器查询，如果使用 TCP 协议，那么每次请求都会存在连接时延，这样使 DNS 服务变得很慢，因为大<br>多数的地址查询请求，都是浏览器请求页面时发出的，这样会造成网页的等待时间过长。</p>
<p>使用 UDP 协议作为 DNS 协议会有一个问题，由于历史原因，物理链路的最小 MTU = 576，所以为了限制报文长度不超过 576，<br><strong>UDP 的报文段的长度被限制在 512 个字节以内</strong>，这样一旦 DNS 的查询或者应答报文，超过了 512 字节，那么基于 UDP 的<br>DNS 协议就会被截断为 512 字节，那么有可能用户得到的 DNS 应答就是不完整的。这里 DNS 报文的长度一旦超过限制，并不<br>会像 TCP 协议那样被拆分成多个报文段传输，因为 UDP 协议不会维护连接状态，所以我们没有办法确定那几个报文段属于同一<br>个数据，UDP 只会将多余的数据给截取掉。<strong>为了解决这个问题，我们可以使用 TCP 协议去请求报文</strong>。</p>
<p>DNS 还存在的一个问题是安全问题，就是我们没有办法确定我们得到的应答，一定是一个安全的应答，因为应答可以被他人伪造，<br>所以现在有了 DNS over HTTPS 来解决这个问题</p>
<p>详细资料可参考： <a href="https://www.zhihu.com/question/310145373" target="_blank" rel="noopener">为什么 DNS 使用 UDP 而不是 TCP</a></p>
<h2 id="CDN"><a href="#CDN" class="headerlink" title="CDN"></a>CDN</h2><h3 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h3><p>CDN 是一个内容分发网络，通过对源网站资源的缓存，利用本身多台位于不同地域、不同运营商的服务器，向用户提供资就近访问的<br>功能。也就是说，用户的请求并不是直接发送给源网站，而是发送给 CDN 服务器，由 CND 服务器将请求定位到最近的含有该资源<br>的服务器上去请求。这样有利于提高网站的访问速度，同时通过这种方式也减轻了源服务器的访问压力。</p>
<h3 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h3><p>详细资料可参考：<a href="https://zhuanlan.zhihu.com/p/28939811" target="_blank" rel="noopener">CDN知识详解</a></p>
<p>参考资料<br>《图解 HTTP》<br><a href="https://coding.imooc.com/class/355.html" target="_blank" rel="noopener">慕课网</a><br><a href="https://github.com/CyC2018/CS-Notes" target="_blank" rel="noopener">CS-notes</a><br><a href="https://blog.nowcoder.net/yiizgo" target="_blank" rel="noopener">呓语</a><br><a href="https://github.com/poetries/" target="_blank" rel="noopener">poetries</a><br><a href="https://github.com/CavsZhouyou/Front-End-Interview-Notebook/blob/master/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C.md" target="_blank" rel="noopener">CavsZhouyou/Front-End-Interview-Notebook</a><br><a href="http://www.ruanyifeng.com/blog/2016/06/dns.html" target="_blank" rel="noopener">阮一峰-DNS 原理入门</a><br><a href="http://www.ruanyifeng.com/blog/2018/05/root-domain.html" target="_blank" rel="noopener">阮一峰-根域名的知识</a><br><a href="https://zhuanlan.zhihu.com/p/32841479" target="_blank" rel="noopener">什么是负载均衡</a></p>
]]></content>
      <categories>
        <category>Basic knowledge</category>
      </categories>
      <tags>
        <tag>Network</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式</title>
    <url>/2019/10/26/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<p>PS：一般单例模式和观察者模式问的最多。一般能说出5个设计模式就可以</p>
<h2 id="面向对象的JS"><a href="#面向对象的JS" class="headerlink" title="面向对象的JS"></a>面向对象的JS</h2><h3 id="语言类型"><a href="#语言类型" class="headerlink" title="语言类型"></a>语言类型</h3><p>静态类型语言在编译时便确定变量的类型，而动态类型语言要到程序运行的时候，待变量被赋予某个值之后，才会有某种类型。JS是典型的动态类型语言。同时JS也是弱类型语言，Java是强类型语言<br>强类型语言是一旦变量的类型被确定，就不能转化的语言。实际上所谓的貌似转化，都是通过中间变量来达到，原本的变量的类型肯定是没有变化的<br>弱类型语言则反之，一个变量的类型是由其应用上下文确定的。比如语言直接支持字符串和整数可以直接用 + 号搞定。当然，在支持运算符重载的强类型语言中也能通过外部实现的方式在形式上做到这一点，不过这个是完全不一样的内涵<br>通常的说，java/python都算是强类型的，而VB/Perl/C都是弱类型的<br><img src="/img/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E7%B1%BB%E5%9E%8B.png"></p>
<h3 id="面向对象三要素"><a href="#面向对象三要素" class="headerlink" title="面向对象三要素"></a>面向对象三要素</h3><h4 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h4><p>ES6实现继承很方便了，不解释</p>
<h4 id="封装"><a href="#封装" class="headerlink" title="封装"></a>封装</h4><p>数据的权限和保密（eg. 变量 or 对象）<br>封装的三个访问权限关键字：public 完全开放，protected 对子类开放，private 对自己开放。对三个关键字可以对是属性的一种描述。<br>但JS没有着这种关键字（有些语言有，eg. TypeScript，Java），只能通过作用域模拟出public ，private 两种封装性。<br><strong>封装的作用</strong>：</p>
<ul>
<li>减少耦合，不该外露的不外露</li>
<li>利于数据、接口的权限管理</li>
<li>ES6目前不支持，<strong>一般认为 _ 开头的属性是private</strong></li>
</ul>
<h4 id="多态"><a href="#多态" class="headerlink" title="多态"></a>多态</h4><p>同一操作作用于不同的对象上，可以产生不同的解释和不同的执行结果。（eg. 继承自同一父后又自己写方法，这是两个继承自同一父的两个子类的同个方法可能执行结果不同）<br>作用：保持子类的开放性和灵活性；面向接口编程</p>
<h3 id="为何使用面向对象"><a href="#为何使用面向对象" class="headerlink" title="为何使用面向对象"></a>为何使用面向对象</h3><ul>
<li>因为计算机的程序执行：顺序、判断、循环 ——实现了结构化；</li>
<li>而使用面向对象编程能——数据结构化； </li>
<li>对于计算机，结构化的才是最简单的，所以编程应该 简单&amp;抽象。</li>
</ul>
<p>PS：jQuery其实是一个类</p>
<h3 id="UML类图"><a href="#UML类图" class="headerlink" title="UML类图"></a>UML类图</h3><p>画UML类图帮助理解设计模式，也能帮助面对对象编程。可以去<a href="www.processon.com">processon</a>画<br><img src="/img/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/UML.png"></p>
<h2 id="设计"><a href="#设计" class="headerlink" title="设计"></a>设计</h2><h3 id="何为设计"><a href="#何为设计" class="headerlink" title="何为设计"></a>何为设计</h3><p>按照哪一种思路或者标准来实现功能。功能相同，可以有不同的设计方案实现。伴随着需求增加，设计的作用才能体现出来</p>
<h3 id="《UNIX-LINUX设计哲学》"><a href="#《UNIX-LINUX设计哲学》" class="headerlink" title="《UNIX/LINUX设计哲学》"></a>《UNIX/LINUX设计哲学》</h3><ul>
<li>准则1：小即是美</li>
<li>准则2：让每个程序制作好一件事</li>
<li>准则3：快速建立原型（满足最基本需求）</li>
<li>准则4：舍弃高效率而取可移植性（可通用性）</li>
<li>准则5：采用纯文本来存储数据</li>
<li>准则6：充分利用软件的杠杆效应（软件复用）</li>
<li>准则7：使用shell脚本来提高杠杆效应和可移植性</li>
<li>准则8：避免强制性的用户界面</li>
<li>准则9：让每个程序成为过滤器</li>
<li>小准则<ul>
<li>允许用户定制环境</li>
<li>尽量使操作系统内核小而轻量化</li>
<li>使用小写字母并尽量简短</li>
<li>沉默是金</li>
<li>各部分之和大于整体</li>
<li>寻求90%的解决方案</li>
</ul>
</li>
</ul>
<h3 id="SOLID五大设计原则"><a href="#SOLID五大设计原则" class="headerlink" title="SOLID五大设计原则"></a>SOLID五大设计原则</h3><ul>
<li><strong>S：单一职责原则</strong>。每个程序（或函数/函数的一部分）只做好一件事，如果功能过于复杂就拆分，每个部分保持独立</li>
<li><strong>O：开放封闭原则</strong>。对扩展开放，对修改封闭；增加需求时，扩展新代码，而非修改原来的代码。是软件设计的终极目标。</li>
<li>L：*李氏置换原则。子类能覆盖父类；父类能出现的地方子类都能出现；JS用的少（弱类型&amp;继承使用较少）</li>
<li>I：*接口独立原则。保持接口的单一独立，避免出现“胖接口”；JS中没有接口（typescript例外），使用较少</li>
<li>D：*依赖导致原则。面向接口编程，依赖于抽象而不依赖于具体；使用方只关注接口而不关注具体类的实现。JS使用少</li>
</ul>
<h3 id="设计模式简介"><a href="#设计模式简介" class="headerlink" title="设计模式简介"></a>设计模式简介</h3><ul>
<li>创建型<ul>
<li>工厂模式</li>
<li>单例模式</li>
<li>原型模式</li>
</ul>
</li>
<li>结构性<ul>
<li>适配器模式</li>
<li>装饰器模式</li>
<li>代理模式</li>
<li>外观模式</li>
<li>*桥接模式</li>
<li>*组合模式</li>
<li>*享元模式</li>
</ul>
</li>
<li>行为型<ul>
<li>*策略模式</li>
<li>*模板方法模式</li>
<li>观察者模式（发布-订阅）</li>
<li>迭代器模式</li>
<li>*职责连模式</li>
<li>*命令模式</li>
<li>*备忘录模式</li>
<li>状态模式</li>
<li>*访问者模式</li>
<li>*中介者模式</li>
<li>*解释器模式</li>
</ul>
</li>
</ul>
<p>学习目的：</p>
<ul>
<li>明白每个设计的道理和用意</li>
<li>通过经典应用体会它的真正的使用场景</li>
<li><strong>自己编码时多思考，尽量模仿</strong></li>
</ul>
<h3 id="例题1"><a href="#例题1" class="headerlink" title="例题1"></a>例题1</h3><p>打车时，可以打专车（每公里2元）或快车（1元）。任何车都有车牌号和名称。行程开始时，显示车辆信息；行程结束时，显示打车金额（假定行程5公里）。要求画出UML类图和用ES6语法写出该示例。<strong>先画UML类图再写代码</strong><br><img src="/img/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/3-8.png"></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Car</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(num, name) &#123;</span><br><span class="line">    <span class="keyword">this</span>.num = num</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">FastCar</span> <span class="keyword">extends</span> <span class="title">Car</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(num, name) &#123;</span><br><span class="line">    <span class="keyword">super</span>(num, name)</span><br><span class="line">    <span class="keyword">this</span>.price = <span class="number">1</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SpecialCar</span> <span class="keyword">extends</span> <span class="title">Car</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(num, name) &#123;</span><br><span class="line">    <span class="keyword">super</span>(num, name)</span><br><span class="line">    <span class="keyword">this</span>.price = <span class="number">2</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Trip</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(car, long) &#123;</span><br><span class="line">    <span class="keyword">this</span>.car = car</span><br><span class="line">    <span class="keyword">this</span>.long = long</span><br><span class="line">  &#125;</span><br><span class="line">  start() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`行程开始，名称：<span class="subst">$&#123;<span class="keyword">this</span>.car.name&#125;</span>，车牌号：<span class="subst">$&#123;<span class="keyword">this</span>.car.num&#125;</span>`</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  end() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`行程结束，价格：<span class="subst">$&#123;<span class="keyword">this</span>.car.price * <span class="keyword">this</span>.long&#125;</span>`</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> car = <span class="keyword">new</span> SpecialCar(<span class="number">100</span>, <span class="string">'广州专车'</span>)</span><br><span class="line"><span class="keyword">let</span> trip = <span class="keyword">new</span> Trip(car, <span class="number">5</span>)</span><br><span class="line">trip.start()</span><br><span class="line">trip.end()</span><br></pre></td></tr></table></figure>

<h3 id="例题2"><a href="#例题2" class="headerlink" title="例题2"></a>例题2</h3><p>某停车场，分为3层，每层100个车位，；每个车位都能监控到车辆的驶入和离开。车辆进入前，显示每层的空余车辆数；车辆进入时，摄像头可识别车牌号和时间；车辆出来时，出口显示器显示车牌号和停车时长。画UML类图<br><img src="/img/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/3-11.png"></p>
<h2 id="工厂模式"><a href="#工厂模式" class="headerlink" title="工厂模式"></a>工厂模式</h2><h3 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h3><p>将new操作单独封装（初始化实例）；遇到new时，就要考虑是否用工厂模式<br>eg. 我们去饭店点了个一个菜，后面厨师会做实例化这个菜的各种复杂操作，但是提供给我们的却只有“点菜”这个接口</p>
<h3 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h3><p>隐藏了创建实例的复杂度，只需要提供一个借口，简单清晰</p>
<h3 id="UML类图及实现代码"><a href="#UML类图及实现代码" class="headerlink" title="UML类图及实现代码"></a>UML类图及实现代码</h3><p><img src="/img/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E5%B7%A5%E5%8E%82%E6%A8%A1%E5%BC%8F.png"></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Product</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(name) &#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">  &#125;</span><br><span class="line">  init() &#123;</span><br><span class="line">    alert(<span class="string">'init'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  fn1() &#123;</span><br><span class="line">    alert(<span class="string">'fn1'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Creator</span> </span>&#123;</span><br><span class="line">  create(name) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> Product(name)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> creator = <span class="keyword">new</span> Creator()</span><br><span class="line"><span class="keyword">let</span> p = creator.create(<span class="string">'p1'</span>)</span><br><span class="line">p.init()</span><br></pre></td></tr></table></figure>

<h3 id="场景"><a href="#场景" class="headerlink" title="场景"></a>场景</h3><ul>
<li>React.createElement</li>
<li>vue异步组件的创建<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">createComponent</span> (<span class="params"></span></span></span><br><span class="line"><span class="function"><span class="params">  Ctor: Class&lt;Component&gt; | Function | Object | void,</span></span></span><br><span class="line"><span class="function"><span class="params">  data: ?VNodeData,</span></span></span><br><span class="line"><span class="function"><span class="params">  context: Component,</span></span></span><br><span class="line"><span class="function"><span class="params">  children: ?Array&lt;VNode&gt;,</span></span></span><br><span class="line"><span class="function"><span class="params">  tag?: string</span></span></span><br><span class="line"><span class="function"><span class="params"></span>): <span class="title">VNode</span> | <span class="title">Array</span>&lt;<span class="title">VNode</span>&gt; | <span class="title">void</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 逻辑处理...</span></span><br><span class="line">  </span><br><span class="line">  <span class="keyword">const</span> vnode = <span class="keyword">new</span> VNode(</span><br><span class="line">    <span class="string">`vue-component-<span class="subst">$&#123;Ctor.cid&#125;</span><span class="subst">$&#123;name ? <span class="string">`-<span class="subst">$&#123;name&#125;</span>`</span> : <span class="string">''</span>&#125;</span>`</span>,</span><br><span class="line">    data, <span class="literal">undefined</span>, <span class="literal">undefined</span>, <span class="literal">undefined</span>, context,</span><br><span class="line">    &#123; Ctor, propsData, listeners, tag, children &#125;,</span><br><span class="line">    asyncFactory</span><br><span class="line">  )</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> vnode</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<blockquote>
<p>在上述代码中，我们可以看到我们只需要调用 createComponent 传入参数就能创建一个组件实例，但是创建这个实例是很复杂的一个过程，工厂帮助我们隐藏了这个复杂的过程，只需要一句代码调用就能实现功能</p>
</blockquote>
<h2 id="单例模式"><a href="#单例模式" class="headerlink" title="单例模式"></a>单例模式</h2><h3 id="介绍-1"><a href="#介绍-1" class="headerlink" title="介绍"></a>介绍</h3><p>系统中被唯一使用，一个类只有一个实例<br>eg. 淘宝页面中，登录框、购物车只会有一个；全局缓存，全局状态管理等这些只需要一个对象，就可以使用单例函数</p>
<h3 id="实现代码"><a href="#实现代码" class="headerlink" title="实现代码"></a>实现代码</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*说明：单例模式要用到pravite关键字，但JS没有。我们只需要用一个变量确保实例只创建一次就行</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SingleObject</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123; &#125;</span><br><span class="line">&#125;</span><br><span class="line">SingleObject.getInstance = (<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> instnce</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!instnce) &#123;</span><br><span class="line">      instnce = <span class="keyword">new</span> SingleObject</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> instnce</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)()</span><br><span class="line"><span class="comment">// 下面这种把 instance 写成类的静态属性的方式也可以，更和TS中的写法一致</span></span><br><span class="line"><span class="comment">// 有些库的单例写法会把 instane 挂在 global 中 ，global.instance = xxx。当然这种写法建议用一个复杂一点的变量名，eg: CG_I18N_EMITER</span></span><br><span class="line">SingleObject.instnce = <span class="literal">undefined</span></span><br><span class="line">SingleObject.getInstance = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (!<span class="keyword">this</span>.instnce) &#123;</span><br><span class="line">    <span class="keyword">this</span>.instnce = <span class="keyword">new</span> SingleObject()</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">this</span>.instnce</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//  一定要用自定义的getInstance方法来创建实例，不能用new</span></span><br><span class="line"><span class="keyword">let</span> obj1 = SingleObject.getInstance()</span><br><span class="line"><span class="keyword">let</span> obj2 = SingleObject.getInstance()</span><br><span class="line"><span class="built_in">console</span>.log(obj1 === obj2) <span class="comment">// true</span></span><br><span class="line"><span class="comment">//  用了new</span></span><br><span class="line"><span class="keyword">let</span> obj3 = <span class="keyword">new</span> SingleObject()</span><br><span class="line"><span class="built_in">console</span>.log(obj1 === obj3) <span class="comment">// false</span></span><br></pre></td></tr></table></figure>

<h3 id="场景-1"><a href="#场景-1" class="headerlink" title="场景"></a>场景</h3><ul>
<li>jQuery只有一个$</li>
<li>模拟登录框</li>
<li>Vuex源码，通过一个外部变量来控制只安装一次 Vuex<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> Vue <span class="comment">// bind on install</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">install</span> (<span class="params">_Vue</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (Vue &amp;&amp; _Vue === Vue) &#123;</span><br><span class="line">    <span class="comment">// 如果发现 Vue 有值，就不重新创建实例了</span></span><br><span class="line">    <span class="keyword">return</span></span><br><span class="line">  &#125;</span><br><span class="line">  Vue = _Vue</span><br><span class="line">  applyMixin(Vue)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="适配器模式"><a href="#适配器模式" class="headerlink" title="适配器模式"></a>适配器模式</h2><h3 id="介绍-2"><a href="#介绍-2" class="headerlink" title="介绍"></a>介绍</h3><p>旧接口格式和使用者不兼容，中间加一个适配转换接口<br>eg. 电源插口转换器；</p>
<h3 id="UML类图及实现代码-1"><a href="#UML类图及实现代码-1" class="headerlink" title="UML类图及实现代码"></a>UML类图及实现代码</h3><p><img src="/img/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E9%80%82%E9%85%8D%E5%99%A8%E6%A8%A1%E5%BC%8F.png"></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Adaptee</span> </span>&#123;</span><br><span class="line">  specificRequest() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">'德国标准插头'</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Target</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">this</span>.adaptee = <span class="keyword">new</span> Adaptee()</span><br><span class="line">  &#125;</span><br><span class="line">  request() &#123;</span><br><span class="line">    <span class="keyword">let</span> info = <span class="keyword">this</span>.adaptee.specificRequest()</span><br><span class="line">    <span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;info&#125;</span>的转换器——中国标准插头`</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> target = <span class="keyword">new</span> Target()</span><br><span class="line"><span class="keyword">let</span> res = target.request()</span><br><span class="line"><span class="built_in">console</span>.log(res) <span class="comment">//  德国标准插头的转换器——中国标准插头</span></span><br></pre></td></tr></table></figure>

<h3 id="场景-2"><a href="#场景-2" class="headerlink" title="场景"></a>场景</h3><ul>
<li>封装旧接口</li>
<li>Vue的计算属性<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"app"</span>&gt;</span>信息列表<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">p</span>&gt;</span>信息：&#123;&#123;message&#125;&#125;<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">p</span>&gt;</span>逆序信息：&#123;&#123;newMessage&#125;&#125;<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> vm = <span class="keyword">new</span> Vue(&#123;</span></span><br><span class="line"><span class="actionscript">      el: <span class="string">'#app'</span>,</span></span><br><span class="line">      data: &#123;</span><br><span class="line"><span class="actionscript">        message: <span class="string">'hello'</span></span></span><br><span class="line">      &#125;,</span><br><span class="line">      computed: &#123;</span><br><span class="line"><span class="actionscript">        newMessage: <span class="function"><span class="keyword">function</span> <span class="params">()</span> </span>&#123;</span></span><br><span class="line"><span class="actionscript">          <span class="keyword">return</span> <span class="keyword">this</span>.message.split(<span class="string">''</span>).reverse().join(<span class="string">''</span>)</span></span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  <span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="装饰器模式"><a href="#装饰器模式" class="headerlink" title="装饰器模式"></a>装饰器模式</h2><h3 id="介绍-3"><a href="#介绍-3" class="headerlink" title="介绍"></a>介绍</h3><p>不需要改变对象原有结构和功能（接口）的前提下，为对象添加新功能<br>eg.  就像我们经常需要给手机戴个保护套防摔一样，不改变手机自身，给手机添加了保护套提供防摔功能</p>
<h3 id="UML类图及实现代码-2"><a href="#UML类图及实现代码-2" class="headerlink" title="UML类图及实现代码"></a>UML类图及实现代码</h3><p><img src="/img/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E8%A3%85%E9%A5%B0%E5%99%A8%E6%A8%A1%E5%BC%8F.png"></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Circle</span> </span>&#123;</span><br><span class="line">  draw() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'画一个圆形'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Decorator</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(circle) &#123;</span><br><span class="line">    <span class="keyword">this</span>.circle = circle</span><br><span class="line">  &#125;</span><br><span class="line">  draw() &#123;</span><br><span class="line">    <span class="keyword">this</span>.circle.draw()</span><br><span class="line">    <span class="keyword">this</span>.setRedBorder(circle) <span class="comment">// 这里要传递“原来的功能”为参数，记得是“装饰”</span></span><br><span class="line">  &#125;</span><br><span class="line">  setRedBorder(circle) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'设置红色边框'</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> circle = <span class="keyword">new</span> Circle()</span><br><span class="line">circle.draw() <span class="comment">// 画一个圆形</span></span><br><span class="line"><span class="keyword">let</span> dec = <span class="keyword">new</span> Decorator(circle)</span><br><span class="line">dec.draw()  <span class="comment">// 画一个圆形  // 设置红色边框</span></span><br></pre></td></tr></table></figure>

<h3 id="场景-3"><a href="#场景-3" class="headerlink" title="场景"></a>场景</h3><p>ES7装饰器语法</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">@testDec <span class="comment">// 用这个@的语法对下面的类做装饰</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Demo</span> </span>&#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">testDec</span>(<span class="params">target</span>) </span>&#123;</span><br><span class="line">  target.isDec = <span class="literal">true</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(Demo.isDec) <span class="comment">// true</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//  传递参数的情况</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">testDec</span>(<span class="params">isDec</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">target</span>) </span>&#123;</span><br><span class="line">    target.isDec = <span class="literal">true</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">@testDec</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Demo</span> </span>&#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(Demo.isDec) <span class="comment">// true</span></span><br></pre></td></tr></table></figure>

<h2 id="代理模式"><a href="#代理模式" class="headerlink" title="代理模式"></a>代理模式</h2><h3 id="介绍-4"><a href="#介绍-4" class="headerlink" title="介绍"></a>介绍</h3><p>使用者无权访问目标对象，可以通过中间加代理的方法，通过代理做授权和控制<br>eg. 代购；明星和经纪人；事件代理</p>
<h3 id="UML类图及实现代码-3"><a href="#UML类图及实现代码-3" class="headerlink" title="UML类图及实现代码"></a>UML类图及实现代码</h3><p><img src="/img/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E4%BB%A3%E7%90%86%E6%A8%A1%E5%BC%8F.png"></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">RealImg</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(filename) &#123;</span><br><span class="line">    <span class="keyword">this</span>.filename = filename</span><br><span class="line">  &#125;</span><br><span class="line">  display() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`展示<span class="subst">$&#123;<span class="keyword">this</span>.filename&#125;</span>图片`</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ProxyImg</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(filename) &#123;</span><br><span class="line">    <span class="keyword">this</span>.realimg = <span class="keyword">new</span> RealImg(filename)</span><br><span class="line">  &#125;</span><br><span class="line">  display() &#123;</span><br><span class="line">    <span class="keyword">this</span>.realimg.display()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> proxyImg = <span class="keyword">new</span> ProxyImg(<span class="string">'1.png'</span>)</span><br><span class="line">proxyImg.display() <span class="comment">// 展示1.png图片</span></span><br></pre></td></tr></table></figure>

<h3 id="代理模式-vs-装饰器模式"><a href="#代理模式-vs-装饰器模式" class="headerlink" title="代理模式 vs 装饰器模式"></a>代理模式 vs 装饰器模式</h3><p>装饰器模式：扩展功能，原有功能不变且可以直接使用<br>代理模式：显示原有功能，但是经过限制或者阉割之后的（ES6的proxy也是如此）</p>
<h3 id="场景-4"><a href="#场景-4" class="headerlink" title="场景"></a>场景</h3><ul>
<li>ES6 proxy</li>
<li>网页事件代理<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">"ul"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>1<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>2<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>3<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>4<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>5<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">    <span class="keyword">let</span> ul = <span class="built_in">document</span>.querySelector(<span class="string">'#ul'</span>)</span></span><br><span class="line"><span class="actionscript">    ul.addEventListener(<span class="string">'click'</span>, (event) =&gt; &#123;</span></span><br><span class="line"><span class="javascript">        <span class="built_in">console</span>.log(event.target);</span></span><br><span class="line">    &#125;)</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<blockquote>
<p>因为存在太多的 li，不可能每个都去绑定事件。这时候可以通过给父节点绑定一个事件，让父节点作为代理去拿到真实点击的节点</p>
</blockquote>
<h2 id="观察者模式"><a href="#观察者模式" class="headerlink" title="观察者模式"></a>观察者模式</h2><h3 id="介绍-5"><a href="#介绍-5" class="headerlink" title="介绍"></a>介绍</h3><p>通过一对一或者一对多的依赖关系，当对象发生改变时，订阅方都会收到通知，即 发布 &amp; 订阅（发布给订阅者）<br>eg. 肯德基很多个人点餐完成，每准备好一个顾客的餐那个顾客就会收到通知；双11某商品降价通知</p>
<h3 id="UML类图及实现代码-4"><a href="#UML类图及实现代码-4" class="headerlink" title="UML类图及实现代码"></a>UML类图及实现代码</h3><p><img src="/img/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E8%A7%82%E5%AF%9F%E8%80%85%E6%A8%A1%E5%BC%8F.png"></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 主题，保存状态，状态变化之后触发所有观察者对象</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Subject</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(name) &#123;</span><br><span class="line">    <span class="keyword">this</span>.state = <span class="number">0</span></span><br><span class="line">    <span class="keyword">this</span>.observers = []</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 添加观察者</span></span><br><span class="line">  addOvservers(observer) &#123;</span><br><span class="line">    <span class="keyword">this</span>.observers.push(observer)</span><br><span class="line">  &#125;</span><br><span class="line">  getState() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.state</span><br><span class="line">  &#125;</span><br><span class="line">  setState(state) &#123;</span><br><span class="line">    <span class="keyword">this</span>.state = state</span><br><span class="line">    <span class="keyword">this</span>.notifyAllObservers()</span><br><span class="line">  &#125;</span><br><span class="line">  notifyAllObservers() &#123;</span><br><span class="line">    <span class="keyword">this</span>.observers.forEach(<span class="function">(<span class="params">observer</span>) =&gt;</span> &#123;</span><br><span class="line">      observer.update()</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 观察者</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Observer</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(name, subject) &#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name</span><br><span class="line">    <span class="keyword">this</span>.subject = subject</span><br><span class="line">    <span class="keyword">this</span>.subject.addOvservers(<span class="keyword">this</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  update() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;<span class="keyword">this</span>.subject.name&#125;</span> updata, now state is <span class="subst">$&#123;<span class="keyword">this</span>.subject.getState()&#125;</span>`</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> s = <span class="keyword">new</span> Subject(<span class="string">'subject1'</span>)</span><br><span class="line"><span class="keyword">let</span> o1 = <span class="keyword">new</span> Observer(<span class="string">'o1'</span>, s)</span><br><span class="line"><span class="keyword">let</span> o2 = <span class="keyword">new</span> Observer(<span class="string">'o2'</span>, s)</span><br><span class="line">s.setState(<span class="number">2</span>) <span class="comment">// subject1 updata, now state is 2  // subject1 updata, now state is 2</span></span><br></pre></td></tr></table></figure>

<h3 id="场景-5"><a href="#场景-5" class="headerlink" title="场景"></a>场景</h3><ul>
<li>网页的addEventListener</li>
<li>Vue的双向数据绑定、父子传值$emit、生命周期触发、Vue的watch</li>
<li>Promise的 .then（监听promise的状态变化）</li>
<li>Node.js自定义事件EventEmitter（这个其实和Vue的$emit是一样的），http（其实底也是EventEmitter）</li>
</ul>
<h2 id="外观模式"><a href="#外观模式" class="headerlink" title="外观模式"></a>外观模式</h2><h3 id="介绍-6"><a href="#介绍-6" class="headerlink" title="介绍"></a>介绍</h3><p>为子系统中的一组接口提供了一个高层接口，使用者使用这个高层接口</p>
<h3 id="UML类图-1"><a href="#UML类图-1" class="headerlink" title="UML类图"></a>UML类图</h3><p><img src="/img/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E5%A4%96%E8%A7%82%E6%A8%A1%E5%BC%8F.png"></p>
<h3 id="场景-6"><a href="#场景-6" class="headerlink" title="场景"></a>场景</h3><p>实现一个兼容多种浏览器的添加事件方法</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">addEvent</span>(<span class="params">elm, evType, fn, useCapture</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (elm.addEventListener) &#123;</span><br><span class="line">    elm.addEventListener(evType, fn, useCapture)</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (elm.attachEvent) &#123;</span><br><span class="line">    <span class="keyword">var</span> r = elm.attachEvent(<span class="string">"on"</span> + evType, fn)</span><br><span class="line">    <span class="keyword">return</span> r</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    elm[<span class="string">"on"</span> + evType] = fn</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>对于不同的浏览器，添加事件的方式可能会存在兼容问题。如果每次都需要去这样写一遍的话肯定是不能接受的，所以我们将这些判断逻辑统一封装在一个接口中，外部需要添加事件只需要调用 addEvent 即可</p>
</blockquote>
<h2 id="迭代器模式"><a href="#迭代器模式" class="headerlink" title="迭代器模式"></a>迭代器模式</h2><h3 id="介绍-7"><a href="#介绍-7" class="headerlink" title="介绍"></a>介绍</h3><p>顺序遍历有序集合（Array，NodeList，Set等，对象不算，不是有序），使用者无需知道集合的内部结构（被封装了，不知道它是数组还是NodeList还是啥的，也不知道它的长度）。（所以 Array.prototype.forEach(), for循环 这些都不算迭代模式）</p>
<h3 id="UML类图及实现代码-5"><a href="#UML类图及实现代码-5" class="headerlink" title="UML类图及实现代码"></a>UML类图及实现代码</h3><p><img src="/img/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E8%BF%AD%E4%BB%A3%E5%99%A8%E6%A8%A1%E5%BC%8F.png"></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Iterator</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(conatiner) &#123;</span><br><span class="line">    <span class="keyword">this</span>.list = conatiner.list</span><br><span class="line">    <span class="keyword">this</span>.index = <span class="number">0</span></span><br><span class="line">  &#125;</span><br><span class="line">  next() &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>.hasNext()) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">this</span>.list[<span class="keyword">this</span>.index++]</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span></span><br><span class="line">  &#125;</span><br><span class="line">  hasNext() &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>.index &gt;= <span class="keyword">this</span>.list.length) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Container</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(list) &#123;</span><br><span class="line">    <span class="keyword">this</span>.list = list</span><br><span class="line">  &#125;</span><br><span class="line">  getIterator() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> Iterator(<span class="keyword">this</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 测试代码</span></span><br><span class="line"><span class="keyword">let</span> container = <span class="keyword">new</span> Container([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>])</span><br><span class="line"><span class="keyword">let</span> iterator = container.getIterator()</span><br><span class="line"><span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(iterator.next())</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="场景-7"><a href="#场景-7" class="headerlink" title="场景"></a>场景</h3><ul>
<li>jQuery each</li>
<li>ES6 Iterator</li>
</ul>
<h2 id="状态模式"><a href="#状态模式" class="headerlink" title="状态模式"></a>状态模式</h2><h3 id="介绍-8"><a href="#介绍-8" class="headerlink" title="介绍"></a>介绍</h3><p>一个对象有状态变化，每次状态变化都会触发一个逻辑，当状态有很多种的时候不能总是用 if…else 来控制<br>eg. 交通信号灯不同颜色的变化</p>
<h3 id="UML类图及实现代码-6"><a href="#UML类图及实现代码-6" class="headerlink" title="UML类图及实现代码"></a>UML类图及实现代码</h3><p><img src="/img/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/%E7%8A%B6%E6%80%81%E6%A8%A1%E5%BC%8F.png"></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 状态（红灯、绿灯、黄灯）</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">State</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(color) &#123;</span><br><span class="line">    <span class="keyword">this</span>.color = color</span><br><span class="line">  &#125;</span><br><span class="line">  handle(context) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`turn to <span class="subst">$&#123;<span class="keyword">this</span>.color&#125;</span> light`</span>)</span><br><span class="line">    context.setState(<span class="keyword">this</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 主体</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Context</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">this</span>.state = <span class="literal">null</span></span><br><span class="line">  &#125;</span><br><span class="line">  getState() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.state</span><br><span class="line">  &#125;</span><br><span class="line">  setState(state) &#123;</span><br><span class="line">    <span class="keyword">this</span>.state = state</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> context = <span class="keyword">new</span> Context()</span><br><span class="line"><span class="keyword">let</span> green = <span class="keyword">new</span> State(<span class="string">'green'</span>)</span><br><span class="line"><span class="keyword">let</span> yellow = <span class="keyword">new</span> State(<span class="string">'yellow'</span>)</span><br><span class="line"><span class="keyword">let</span> red = <span class="keyword">new</span> State(<span class="string">'red'</span>)</span><br><span class="line"><span class="comment">// 绿灯亮了</span></span><br><span class="line">green.handle(context)</span><br><span class="line"><span class="built_in">console</span>.log(context.getState()) <span class="comment">// 打印状态</span></span><br><span class="line"><span class="comment">// 黄灯亮了</span></span><br><span class="line">yellow.handle(context)</span><br><span class="line"><span class="built_in">console</span>.log(context.getState()) <span class="comment">// 打印状态</span></span><br></pre></td></tr></table></figure>

<h3 id="场景-8"><a href="#场景-8" class="headerlink" title="场景"></a>场景</h3><ul>
<li>有限状态机</li>
<li>写一个简单的Promise</li>
</ul>
<h2 id="一些很不错的博客贴-amp-amp-小测"><a href="#一些很不错的博客贴-amp-amp-小测" class="headerlink" title="一些很不错的博客贴 &amp;&amp; 小测"></a>一些很不错的博客贴 &amp;&amp; 小测</h2><h3 id="如何消除层出不穷的-switch…case-————-开放封闭原则"><a href="#如何消除层出不穷的-switch…case-————-开放封闭原则" class="headerlink" title="如何消除层出不穷的 switch…case ———— 开放封闭原则"></a>如何消除层出不穷的 switch…case ———— 开放封闭原则</h3><p><a href="https://xie.infoq.cn/article/7830562df8028398eec1b6ee3" target="_blank" rel="noopener">核心稳定、易扩展——开放关闭原则</a><br><a href="https://mp.weixin.qq.com/s/FHbHhjtfJOc2NPfge6xw2Q" target="_blank" rel="noopener">备用链接</a></p>
<h3 id="React设计模式与最佳实践"><a href="#React设计模式与最佳实践" class="headerlink" title="React设计模式与最佳实践"></a>React设计模式与最佳实践</h3><p>比较有历史的一本<a href="https://juejin.im/book/6844733754326401038" target="_blank" rel="noopener">小册</a></p>
<p>参考资料<br><a href="https://coding.imooc.com/class/255.html" target="_blank" rel="noopener">慕课网-Javascript 设计模式系统讲解与应用</a><br><a href="http://blog.poetries.top/FE-Interview-Questions/excellent/#_34-6-%E5%8F%91%E5%B8%83-%E8%AE%A2%E9%98%85%E6%A8%A1%E5%BC%8F" target="_blank" rel="noopener">poetries</a><br>《JavaScript设计模式与开发实践》</p>
]]></content>
      <categories>
        <category>Basic knowledge</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>部分刷题记录</title>
    <url>/2019/10/21/%E9%83%A8%E5%88%86%E5%88%B7%E9%A2%98%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h1 id="LeetCode"><a href="#LeetCode" class="headerlink" title="LeetCode"></a>LeetCode</h1><h2 id="分类"><a href="#分类" class="headerlink" title="分类"></a>分类</h2><h3 id="做题方法"><a href="#做题方法" class="headerlink" title="做题方法"></a>做题方法</h3><p>发现输入和输出的规律，寻找突破点<br>当给的例子太简单的时候可以自己多画一些复杂的例子<br>选择合适的数据结构和程序结构（算法）</p>
<h4 id="小技巧"><a href="#小技巧" class="headerlink" title="小技巧"></a>小技巧</h4><p>if..else.. 比较简单的话可以简写成 ？ ：的形式<br>switch case 的写法<br>对数组用 forEach，参数函数用了 return false，这里似乎只是回跳出循环而不是整个 return false？ 用 while 语句就不会有这种问题。似乎是因为 forEach 不支持 return false，此外 forEach 也无法终止</p>
<h3 id="字符串"><a href="#字符串" class="headerlink" title="字符串"></a>字符串</h3><p><strong>557 反转字符串中的单词 III</strong><br>给定一个字符串，你需要反转字符串中每个单词的字符顺序，同时仍保留空格和单词的初始顺序。<br>示例  1:<br>输入: “Let’s take LeetCode contest”<br>输出: “s’teL ekat edoCteeL tsetnoc” <br>注意：在字符串中，每个单词由单个空格分隔，并且字符串中不会有任何额外的空格。<br>思路 1：先用利用空格分割每个单词，单词变数组，再在每个数组内进行反转</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> reverseWords = <span class="function"><span class="keyword">function</span>(<span class="params">s</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> s</span><br><span class="line">    .split(<span class="string">" "</span>)</span><br><span class="line">    .map(<span class="function"><span class="params">item</span> =&gt;</span></span><br><span class="line">      item</span><br><span class="line">        .split(<span class="string">""</span>)</span><br><span class="line">        .reverse()</span><br><span class="line">        .join(<span class="string">""</span>)</span><br><span class="line">    )</span><br><span class="line">    .join(<span class="string">" "</span>);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>思路 2：先把每个字符都颠倒过来,tsetnoc edoCteeL ekat s’teL，之后按照空格分隔开， 重新排序</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> reverseWords = <span class="function"><span class="keyword">function</span>(<span class="params">s</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> s</span><br><span class="line">    .split(<span class="string">""</span>)</span><br><span class="line">    .reverse()</span><br><span class="line">    .join(<span class="string">""</span>)</span><br><span class="line">    .split(<span class="string">" "</span>)</span><br><span class="line">    .reverse()</span><br><span class="line">    .join(<span class="string">" "</span>);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>696 计数二进制子串</strong><br>给定一个字符串 s，计算具有相同数量 0 和 1 的非空(连续)子字符串的数量，并且这些子字符串中的所有 0 和所有 1 都是组合在一起的。<br>重复出现的子串要计算它们出现的次数。<br>示例 1 :<br>输入: “00110011”<br>输出: 6<br>解释: 有 6 个子串具有相同数量的连续 1 和 0：“0011”，“01”，“1100”，“10”，“0011” 和 “01”。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 思路1:0011001 每次都起始字符开始检查，直到遇到非它的。比如说第一次，00截止，长度为2，那么看起始字符的开始段是不是满足0011；</span></span><br><span class="line"><span class="comment"> *      第二次，以第二个字符为起始字符，直到遇到非他的，同样操作...</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">s</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> result = [];</span><br><span class="line">  <span class="comment">//  定义检查函数</span></span><br><span class="line">  <span class="keyword">let</span> matches = <span class="function"><span class="params">s</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> j = s.match(<span class="regexp">/^(0+|1+)/</span>)[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">let</span> i = (j[<span class="number">0</span>] ^ <span class="number">1</span>).toString().repeat(j.length); <span class="comment">//  这里的^是按位异或，效果是能让0和1互相转化</span></span><br><span class="line">    <span class="keyword">let</span> all = j + i;</span><br><span class="line">    <span class="keyword">if</span> (s.startsWith(all)) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="comment">//  对字符串遍历检查</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; s.length - <span class="number">1</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">let</span> r = matches(s.slice(i));</span><br><span class="line">    <span class="keyword">if</span> (r) &#123;</span><br><span class="line">      result.push(r);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> result.length;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *   思路2</span></span><br><span class="line"><span class="comment"> *   000111必定有三个子串</span></span><br><span class="line"><span class="comment"> *   00011必定有两个子串</span></span><br><span class="line"><span class="comment"> *   0111必定有1个子串</span></span><br><span class="line"><span class="comment"> *   以此类推, 每两组数据之间长度最短的值为子串的数量</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">s</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> n = <span class="number">0</span>,</span><br><span class="line">    arr = s.match(<span class="regexp">/(1+)|(0+)/g</span>);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; arr.length - <span class="number">1</span>; i++) &#123;</span><br><span class="line">    n += <span class="built_in">Math</span>.min(arr[i].length, arr[i + <span class="number">1</span>].length);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> n;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h3><p><strong>17 九宫格键盘的字母组合</strong><br>给定一个仅包含数字 2-9 的字符串，返回所有它能表示的字母组合。<br>给出数字到字母的映射(九宫格)。注意 1 不对应任何字母。<br>示例:<br>输入：”23”<br>输出：[“ad”, “ae”, “af”, “bd”, “be”, “bf”, “cd”, “ce”, “cf”].</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *   思路：递归</span></span><br><span class="line"><span class="comment"> *   会发现只有2个输入的时候是两个中的字母去组合；3个输入的时候，eg. “234”，是先把“23”结果算出来再与“4”结果得到结果，是一种递归。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">str</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 如果小于1返回空（LeetCode测试用例）</span></span><br><span class="line">  <span class="keyword">if</span> (str.length &lt; <span class="number">1</span>) <span class="keyword">return</span> [];</span><br><span class="line">  <span class="comment">//  建立电话号码键盘映射</span></span><br><span class="line">  <span class="keyword">let</span> map = [<span class="string">""</span>, <span class="number">1</span>, <span class="string">"abc"</span>, <span class="string">"def"</span>, <span class="string">"ghi"</span>, <span class="string">"jkl"</span>, <span class="string">"mno"</span>, <span class="string">"pqrs"</span>, <span class="string">"tuv"</span>, <span class="string">"wxyz"</span>];</span><br><span class="line">  <span class="comment">// 如果只给了一个按键，直接把按键内容取出来并按单个字符分组就可以了（LeetCode测试用例）</span></span><br><span class="line">  <span class="keyword">if</span> (str.length &lt; <span class="number">2</span>) <span class="keyword">return</span> map[str].split(<span class="string">""</span>);</span><br><span class="line">  <span class="comment">//  把'234'变成['abc','def','ghi']</span></span><br><span class="line">  <span class="keyword">let</span> num = str.split(<span class="string">""</span>);</span><br><span class="line">  <span class="keyword">let</span> code = [];</span><br><span class="line">  num.forEach(<span class="function"><span class="params">item</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (map[item]) &#123;</span><br><span class="line">      code.push(map[item]);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;);</span><br><span class="line">  <span class="comment">//  对变好的数组进行组合运算</span></span><br><span class="line">  <span class="keyword">let</span> group = <span class="function"><span class="params">arr</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">//  储存结果的数组</span></span><br><span class="line">    <span class="keyword">let</span> tmp = [];</span><br><span class="line">    <span class="comment">//  第一个for运算arr的第一项，第二个for运算arr的第二项</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>, il = arr[<span class="number">0</span>].length; i &lt; il; i++) &#123;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">0</span>, jl = arr[<span class="number">1</span>].length; j &lt; jl; j++) &#123;</span><br><span class="line">        <span class="comment">// 这个写法使得arr的项无论是数组还是字符串都能实现拆分组合</span></span><br><span class="line">        tmp.push(<span class="string">`<span class="subst">$&#123;arr[<span class="number">0</span>][i]&#125;</span><span class="subst">$&#123;arr[<span class="number">1</span>][j]&#125;</span>`</span>);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    arr.splice(<span class="number">0</span>, <span class="number">2</span>, tmp);</span><br><span class="line">    <span class="keyword">if</span> (arr.length &gt; <span class="number">1</span>) &#123;</span><br><span class="line">      group(arr);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> tmp;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> arr[<span class="number">0</span>];</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">return</span> group(code);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>914 卡牌分组</strong><br>给定一副牌，每张牌上都写着一个整数。<br>此时，你需要选定一个数字 X，使我们可以将整副牌按下述规则分成 1 组或更多组：<br>每组都有  X  张牌。<br>组内所有的牌上都写着相同的整数。<br>仅当你可选的 X &gt;= 2 时返回  true。<br>示例 1：<br>输入：[1,2,3,4,4,3,2,1]<br>输出：true<br>解释：可行的分组是 [1,1]，[2,2]，[3,3]，[4,4]<br>示例 2：<br>输入：[1,1,1,2,2,2,3,3]<br>输出：false<br>解释：没有满足要求的分组。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *   思路：记录每种牌数量后，再比较每两种牌的最大公约数是否大于1（辗转相除法）</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 函数体</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">arr</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">//  存每种卡牌的总数</span></span><br><span class="line">  <span class="keyword">let</span> total = [];</span><br><span class="line">  <span class="comment">//  创建一个对象来记录每种卡牌的数目(用forEach（需要自己建对象）,reduce都可以)</span></span><br><span class="line">  <span class="keyword">let</span> num = arr.reduce(<span class="function">(<span class="params">acc, item</span>) =&gt;</span> &#123;</span><br><span class="line">    item <span class="keyword">in</span> acc ? acc[item]++ : (acc[item] = <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> acc;</span><br><span class="line">  &#125;, &#123;&#125;);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i <span class="keyword">in</span> num) &#123;</span><br><span class="line">    total.push(num[i]);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//  求两个数最大公约数的函数</span></span><br><span class="line">  <span class="keyword">let</span> eva = <span class="function">(<span class="params">a, b</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (b === <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> a;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> eva(b, a % b);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">while</span> (total.length &gt; <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="keyword">let</span> a = total.shift();</span><br><span class="line">    <span class="keyword">let</span> b = total.shift();</span><br><span class="line">    <span class="keyword">let</span> c = eva(a, b);</span><br><span class="line">    <span class="keyword">if</span> (c == <span class="number">1</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      total.unshift(c);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> total.length ? total[<span class="number">0</span>] &gt; <span class="number">1</span> : <span class="literal">false</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>605 种花问题</strong><br>种花的规则是花不能相邻。给定一个花坛（表示为一个数组包含 0 和 1，其中 0 表示没种植花，1 表示种植了花），和一个数  n 。能否在不打破种植规则的情况下种入  n  朵花？能则返回 True，不能则返回 False。<br>示例 1:<br>输入: flowerbed = [1,0,0,0,1], n = 1<br>输出: True<br>示例 2:<br>输入: flowerbed = [1,0,0,0,1], n = 2<br>输出: False</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction = <span class="function">(<span class="params">&#123; flowerbed, n &#125;</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> result = <span class="number">0</span>;</span><br><span class="line">  <span class="comment">//  给数组右边再补一个0,因为判断原来最后一位的时候其实可以默认这一位的虚拟后一位是0</span></span><br><span class="line">  flowerbed.push(<span class="number">0</span>);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; flowerbed.length - <span class="number">1</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (flowerbed[i] === <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (flowerbed[i + <span class="number">1</span>] === <span class="number">0</span>) &#123;</span><br><span class="line">        result++;</span><br><span class="line">        i++;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      i++;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> result &gt;= n;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>89 格雷编码</strong><br>输入: 2<br>输出: [0,1,3,2]<br>解释:<br>00 - 0<br>01 - 1<br>11 - 3<br>10 - 2<br><img src="/img/%E7%AE%97%E6%B3%95/%E6%A0%BC%E9%9B%B7%E7%BC%96%E7%A0%81.png"></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *   首先理解这个题意就有点难了...</span></span><br><span class="line"><span class="comment"> *   如果你百度过1和3的格雷编码长什么样子，或许能找到规律。从直接外观看，规律如下：</span></span><br><span class="line"><span class="comment"> *   输入n之后，列数就是n，行数是2^n；第一列总是前一半是0，后一半是1；后面列你会发现，总体的上部分是n-1的输出结果复制粘贴，</span></span><br><span class="line"><span class="comment"> *   下部分是复制粘贴后上下颠倒。</span></span><br><span class="line"><span class="comment"> *   思路：由上述规律，想知道n的格雷编码序列，必须知道n-1的序列，是一个递归的思想</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">n</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 输入为0的时候</span></span><br><span class="line">  <span class="keyword">if</span> (n === <span class="number">0</span>) <span class="keyword">return</span> [<span class="string">"0"</span>];</span><br><span class="line">  <span class="comment">// 递归函数，用来算输入为n的格雷编码序列</span></span><br><span class="line">  <span class="keyword">let</span> getGrayCode = <span class="function"><span class="params">n</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> result = [];</span><br><span class="line">    <span class="keyword">if</span> (n === <span class="number">1</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> [<span class="string">"0"</span>, <span class="string">"1"</span>];</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">let</span> pre = getGrayCode(n - <span class="number">1</span>);</span><br><span class="line">      <span class="comment">// 格雷编码的行数（长度）。由于我们输出是数组，index是长度-1</span></span><br><span class="line">      <span class="keyword">let</span> resultLength = <span class="built_in">Math</span>.pow(<span class="number">2</span>, n) - <span class="number">1</span>;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>, len = pre.length; i &lt; len; i++) &#123;</span><br><span class="line">        result[i] = <span class="string">`0<span class="subst">$&#123;pre[i]&#125;</span>`</span>;</span><br><span class="line">        result[resultLength - i] = <span class="string">`1<span class="subst">$&#123;pre[i]&#125;</span>`</span>;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">return</span> getGrayCode(n).map(<span class="function"><span class="params">item</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">//  把结果按二进制解析</span></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">parseInt</span>(item, <span class="number">2</span>);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="正则"><a href="#正则" class="headerlink" title="正则"></a>正则</h3><p><strong>459 重复的子字符串</strong><br>给定一个非空的字符串，判断它是否可以由它的一个子串重复多次构成。给定的字符串只含有小写英文字母，并且长度不超过 10000。<br>输入: “abcabcabcabc”<br>输出: True<br>解释: 可由子字符串 “abc” 重复四次构成。 (或者子字符串 “abcabc” 重复两次构成。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 思路：正则表达式</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">s</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">var</span> reg = <span class="regexp">/^(\w+)\1+$/</span>;</span><br><span class="line">  <span class="comment">//  这个字符串无论从前还是从后开始匹配，都满足是一个匹配项通过 + 得到的</span></span><br><span class="line">  <span class="keyword">return</span> reg.test(s);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>10 实现一个简单的正则表达式（困难）</strong><br>s 可能为空，且只包含从 a-z 的小写字母。<br>p 可能为空，且只包含从 a-z 的小写字母，以及字符 . 和 <em>。要求 p 看做是一个正则，能匹配整个 s<br>示例 1:<br>输入:<br>s = “aa”<br>p = “a”<br>输出: false<br>解释: “a” 无法匹配 “aa” 整个字符串。<br>示例 2:<br>输入:<br>s = “aa”<br>p = “a</em>“<br>输出: true</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction = <span class="function">(<span class="params">&#123; s, p &#125;</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> isMatch = <span class="function">(<span class="params">s, p</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">// 边界情况，如果s和p都为空，说明处理结束了，返回true，否则返回false</span></span><br><span class="line">    <span class="keyword">if</span> (p.length &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> !s.length;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 判断p模式字符串的第一个字符和s字符串的第一个字符是不是匹配</span></span><br><span class="line">    <span class="keyword">let</span> match = <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">if</span> (s.length &gt; <span class="number">0</span> &amp;&amp; (p[<span class="number">0</span>] === s[<span class="number">0</span>] || p[<span class="number">0</span>] === <span class="string">"."</span>)) &#123;</span><br><span class="line">      match = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// p有模式的</span></span><br><span class="line">    <span class="keyword">if</span> (p.length &gt; <span class="number">1</span> &amp;&amp; p[<span class="number">1</span>] === <span class="string">"*"</span>) &#123;</span><br><span class="line">      <span class="comment">// 第一种情况：s*匹配0个字符</span></span><br><span class="line">      <span class="comment">// 第二种情况：s*匹配1个字符，递归下去，用来表示s*匹配多个s</span></span><br><span class="line">      <span class="keyword">return</span> isMatch(s, p.slice(<span class="number">2</span>)) || (match &amp;&amp; isMatch(s.slice(<span class="number">1</span>), p));</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> match &amp;&amp; isMatch(s.slice(<span class="number">1</span>), p.slice(<span class="number">1</span>));</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">return</span> isMatch(s, p);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="递归"><a href="#递归" class="headerlink" title="递归"></a>递归</h3><p><strong>93 复原 IP 地址</strong><br>给定一个只包含数字的字符串，复原它并返回所有可能的 IP 地址格式。（IP 地址有 4 个段，每一个段值为 0~255）<br>示例:<br>输入: “25525511135”<br>输出: [“255.255.11.135”, “255.255.111.35”]</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** 思路: IP三个点分成四部分 每部分0-255 每部分最多不超过3位</span></span><br><span class="line"><span class="comment"> *  示例 25525511135 第一部分可能是 2 25 255</span></span><br><span class="line"><span class="comment"> *  如果第一部分是2 那么 第二部分 5 55</span></span><br><span class="line"><span class="comment"> *  如果第二部分 5 那么 第三部分 剩下的超过6位 不满足</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">s</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 保存所有符合条件的IP地址</span></span><br><span class="line">  <span class="keyword">let</span> result = [];</span><br><span class="line">  <span class="comment">// 递归函数  上次处理结果，待处理字符串</span></span><br><span class="line">  <span class="keyword">let</span> search = <span class="function">(<span class="params">curResult, sub</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">// 非法输入过滤，LeetCode测试用例(111111111111111111111111111111111111111111111111111111111111)</span></span><br><span class="line">    <span class="keyword">if</span> (sub.length &gt; <span class="number">12</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 边界条件: 已经分为四部分且四部分长度相等</span></span><br><span class="line">    <span class="keyword">if</span> (curResult.length === <span class="number">4</span> &amp;&amp; curResult.join(<span class="string">""</span>) === s) &#123;</span><br><span class="line">      result.push(curResult.join(<span class="string">"."</span>));</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// 正常的处理过程</span></span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>, len = <span class="built_in">Math</span>.min(<span class="number">3</span>, sub.length), tmp; i &lt; len; i++) &#123;</span><br><span class="line">        <span class="comment">// 待处理子串，取 1位 2位 3位</span></span><br><span class="line">        tmp = sub.substr(<span class="number">0</span>, i + <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">if</span> (tmp &lt; <span class="number">256</span>) &#123;</span><br><span class="line">          <span class="comment">// 转换下数据类型，如 01为1（LeetCode测试用例）</span></span><br><span class="line">          search(curResult.concat(tmp * <span class="number">1</span>), sub.substr(i + <span class="number">1</span>));</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line">  search([], s);</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>30 串联所有单词的子串（困难）</strong><br>给定一个字符串  s  和一些长度相同的单词  words。找出 s 中恰好可以由  words 中所有单词串联形成的子串的起始位置。<br>注意子串要与  words 中的单词完全匹配，中间不能有其他字符，但不需要考虑  words  中单词串联的顺序。<br>示例 1：<br>输入：<br>s = “barfoothefoobarman”,<br>words = <code>[&quot;foo&quot;,&quot;bar&quot;]</code><br>输出：<code>[0,9]</code><br>解释：<br>从索引 0 和 9 开始的子串分别是 “barfoo” 和 “foobar” 。<br>输出的顺序不重要, <code>[9,0]</code> 也是有效答案。<br>示例 2：<br>输入：<br>s = “wordgoodgoodgoodbestword”,words = <code>[&quot;word&quot;,&quot;good&quot;,&quot;best&quot;,&quot;word&quot;]</code><br>输出：[]</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** 思路:对words数组的item进行一个排列组合生成字符串数组，eg.words = [a,b,c]  </span></span><br><span class="line"><span class="comment">    递归排列组合[a]  第一位 b c中选一位放在第二位</span></span><br><span class="line"><span class="comment">    [b] 第一位 a c中选一位放在第二位</span></span><br><span class="line"><span class="comment">    [c] 第一位 a b中选一位放在第二位</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function">(<span class="params">&#123; s, words &#125;</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 结果</span></span><br><span class="line">  <span class="keyword">let</span> result = [];</span><br><span class="line">  <span class="keyword">let</span> num = words.length;</span><br><span class="line">  <span class="comment">// 生成递归的函数</span></span><br><span class="line">  <span class="keyword">let</span> range = <span class="function">(<span class="params">curResult, wordsArr</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">// 边界，计算完毕</span></span><br><span class="line">    <span class="keyword">if</span> (curResult.length === num) &#123;</span><br><span class="line">      result.push(curResult);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      wordsArr.forEach(<span class="function">(<span class="params">item, index</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="keyword">let</span> tmp = [...wordsArr];</span><br><span class="line">        <span class="comment">// 丢出第一位，生成第二位待选数组</span></span><br><span class="line">        tmp.splice(index, <span class="number">1</span>);</span><br><span class="line">        range(curResult.concat(item), tmp);</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line">  range([], words);</span><br><span class="line">  <span class="comment">// 子串获取完成，检查有没有匹配的子串</span></span><br><span class="line">  <span class="comment">// [0, 9, -1] filter 之后[0,9]</span></span><br><span class="line">  <span class="comment">// 同时最后的结果要数组去重（用reduce），LeetCode测试用例words是["word","good","best","good"]的时候，会</span></span><br><span class="line">  <span class="comment">// 以为这两个good是不同的而多排列组合一遍</span></span><br><span class="line">  <span class="keyword">return</span> result</span><br><span class="line">    .map(<span class="function"><span class="params">item</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> s.indexOf(item.join(<span class="string">""</span>));</span><br><span class="line">    &#125;)</span><br><span class="line">    .filter(<span class="function"><span class="params">item</span> =&gt;</span> item !== <span class="number">-1</span>)</span><br><span class="line">    .sort()</span><br><span class="line">    .reduce(<span class="function">(<span class="params">acc, curr</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (acc.indexOf(curr) === <span class="number">-1</span>) &#123;</span><br><span class="line">        acc.push(curr);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> acc;</span><br><span class="line">    &#125;, []);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>还有另一种解法，有点长，没写在这里，面试问到这种解法算我输好吧</p>
<h3 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h3><p>sort()虽然好用，但底层是一次完整的遍历排序，耗性能，做排序算法题能不用就不用。如果题目中要用数组的最大值/最小值，先想想能不能用冒泡/选择 排序。</p>
<h4 id="用-sort-的简单排序"><a href="#用-sort-的简单排序" class="headerlink" title="用 sort()的简单排序"></a>用 sort()的简单排序</h4><p><strong>922 奇偶数欧西数组 II</strong><br>给定一个非负整数数组  A， A 中一半整数是奇数，一半整数是偶数。<br>对数组进行排序，以便当  A[i] 为奇数时，i  也是奇数；当  A[i]  为偶数时， i 也是偶数。<br>你可以返回任何满足上述条件的数组作为答案。<br>示例：<br>输入：[4,2,5,7]<br>输出：[4,5,2,7]<br>解释：[4,7,2,5]，[2,5,4,7]，[2,7,4,5] 也会被接受。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">arr</span> =&gt;</span> &#123;</span><br><span class="line">  arr.sort(<span class="function">(<span class="params">a, b</span>) =&gt;</span> a - b);</span><br><span class="line">  <span class="keyword">let</span> result = [];</span><br><span class="line">  <span class="comment">//  声明两个变量来保存下标</span></span><br><span class="line">  <span class="keyword">let</span> odd = <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">let</span> even = <span class="number">0</span>;</span><br><span class="line">  arr.forEach(<span class="function"><span class="params">item</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (item % <span class="number">2</span> === <span class="number">1</span>) &#123;</span><br><span class="line">      result[odd] = item;</span><br><span class="line">      odd += <span class="number">2</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      result[even] = item;</span><br><span class="line">      even += <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;);</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h4 id="冒泡排序"><a href="#冒泡排序" class="headerlink" title="冒泡排序"></a>冒泡排序</h4><p><strong>164 最大间距（困难）</strong><br>给定一个无序的数组，找出数组在排序之后，相邻元素之间最大的差值。<br>如果数组元素个数小于 2，则返回 0。<br>示例  1:<br>输入: <code>[3,6,9,1]</code><br>输出: 3<br>解释: 排序后的数组是 <code>[1,3,6,9]</code>, 其中相邻元素 (3,6) 和 (6,9) 之间都存在最大差值 3。<br>请尝试在线性时间复杂度和空间复杂度的条件下解决此问题。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *   思路：这里的解法1用了一次sort了一次for，时间复杂度相当于是O（n2 + n）。但其实用冒泡排序的话，在排序的时候就已经</span></span><br><span class="line"><span class="comment"> *   能知道前一项跟后一项的差值了。故用冒泡能实现时间复杂度是O（n2）。</span></span><br><span class="line"><span class="comment"> *   具体操作是在每一轮冒泡排序结束后都保存一下目前冒泡终点和终点+1项的差值</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">// 常规解法，时间复杂度不符合题目要求</span></span><br><span class="line"><span class="comment">// let exportFunction = (nums) =&gt; &#123;</span></span><br><span class="line"><span class="comment">//   if (nums.length &lt; 0) return 0</span></span><br><span class="line"><span class="comment">//   nums.sort((a, b) =&gt; a - b)</span></span><br><span class="line"><span class="comment">//   let max = 0</span></span><br><span class="line"><span class="comment">//   for (let i = 0; i &lt; nums.length - 1; i++) &#123;</span></span><br><span class="line"><span class="comment">//     let now = nums[i + 1] - nums[i]</span></span><br><span class="line"><span class="comment">//     if (now &gt; max) &#123;</span></span><br><span class="line"><span class="comment">//       max = now</span></span><br><span class="line"><span class="comment">//     &#125;</span></span><br><span class="line"><span class="comment">//   &#125;</span></span><br><span class="line"><span class="comment">//   return max</span></span><br><span class="line"><span class="comment">// &#125;</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">nums</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (nums.length &lt; <span class="number">2</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">let</span> len = nums.length - <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">let</span> max = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">let</span> space;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = len; i &gt; <span class="number">0</span>; i--) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">0</span>; j &lt; i; j++) &#123;</span><br><span class="line">      <span class="keyword">let</span> tmp = nums[j];</span><br><span class="line">      <span class="keyword">if</span> (tmp &gt; nums[j + <span class="number">1</span>]) &#123;</span><br><span class="line">        nums[j] = nums[j + <span class="number">1</span>];</span><br><span class="line">        nums[j + <span class="number">1</span>] = tmp;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 是在每一轮冒泡排序结束后都保存一下目前冒泡终点和终点+1项的差值</span></span><br><span class="line">    <span class="keyword">if</span> (i &lt; len) &#123;</span><br><span class="line">      space = nums[i + <span class="number">1</span>] - nums[i];</span><br><span class="line">      <span class="keyword">if</span> (space &gt; max) &#123;</span><br><span class="line">        max = space;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">Math</span>.max(max, nums[<span class="number">1</span>] - nums[<span class="number">0</span>]);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>215 数组中的第 K 个最大元素</strong><br>在未排序的数组中找到第 k 个最大的元素。请注意，你需要找的是数组排序后的第 k 个最大的元素，而不是第 k 个不同的元素。<br>示例 1:<br>输入: [3,2,1,5,6,4] 和 k = 2<br>输出: 5</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *   思路：冒泡排序，当k比较小的时候只要几轮就能结束，性能好</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function">(<span class="params">&#123; nums, k &#125;</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> len = nums.length - <span class="number">1</span>;</span><br><span class="line">  <span class="comment">//  当k刚好想让我们取最小值的时候，直接返回数组最小值</span></span><br><span class="line">  <span class="keyword">if</span> (nums.length === k) <span class="keyword">return</span> <span class="built_in">Math</span>.min.apply(<span class="literal">null</span>, nums);</span><br><span class="line">  <span class="comment">//  其他情况就用冒泡排序节约性能</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = len; i &gt; <span class="number">0</span>; i--) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">0</span>; j &lt; i; j++) &#123;</span><br><span class="line">      <span class="keyword">let</span> tmp = nums[j];</span><br><span class="line">      <span class="keyword">if</span> (tmp &gt; nums[j + <span class="number">1</span>]) &#123;</span><br><span class="line">        nums[j] = nums[j + <span class="number">1</span>];</span><br><span class="line">        nums[j + <span class="number">1</span>] = tmp;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (len - i + <span class="number">1</span> === k) &#123;</span><br><span class="line">      <span class="keyword">return</span> nums[i];</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 解法二：快排.基于数组的第k个数字来调整, 使得比第k个数字小的所有数字都位于数组的左边,比数组大的元素都位于数组的右边。这样调整后 , 位于数组中左边第k个数字就是第k大的元素</span></span><br><span class="line"><span class="keyword">var</span> findKthLargest = <span class="function"><span class="keyword">function</span>(<span class="params">nums, k</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> start = <span class="number">0</span></span><br><span class="line">  <span class="keyword">let</span> end = nums.length - <span class="number">1</span></span><br><span class="line">  <span class="keyword">let</span> index = partition(nums, start, end)</span><br><span class="line">  <span class="keyword">while</span>(index !== k - <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (index &gt; k - <span class="number">1</span>) &#123;</span><br><span class="line">      index = partition(nums, start, index - <span class="number">1</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (index &lt; k - <span class="number">1</span>) &#123;</span><br><span class="line">      index = partition(nums, index + <span class="number">1</span>, end)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> nums[k - <span class="number">1</span>]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">partition</span>(<span class="params">nums, start, end</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> target = nums[start]</span><br><span class="line">  <span class="keyword">while</span> (start &lt; end) &#123;</span><br><span class="line">    <span class="keyword">while</span> (nums[end] &lt;= target &amp;&amp; start &lt; end) &#123;</span><br><span class="line">      end--</span><br><span class="line">    &#125;</span><br><span class="line">    nums[start] = nums[end]</span><br><span class="line">    <span class="keyword">while</span> (nums[start] &gt; target &amp;&amp; start &lt; end) &#123;</span><br><span class="line">      start++</span><br><span class="line">    &#125;</span><br><span class="line">    nums[end] = nums[start]</span><br><span class="line">  &#125;</span><br><span class="line">  nums[start] = target</span><br><span class="line">  <span class="keyword">return</span> start <span class="comment">// 目标位置</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="选择排序"><a href="#选择排序" class="headerlink" title="选择排序"></a>选择排序</h4><p><strong>41 缺失的第一个正数</strong><br>给定一个未排序的整数数组，找出其中没有出现的最小的正整数。<br>示例  1:<br>输入: [1,2,0]<br>输出: 3<br>示例  2:<br>输入: [3,4,-1,1]<br>输出: 2</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *   思路：涉及到最小值的排序，用选择排序</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 第一种算法,性能不是很好（用了一个sort和一个for）,这里的sort其实并不是很必要，没必要把所有的都排好</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">arr</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">//  过滤掉负数和0，排序</span></span><br><span class="line">  arr = arr.filter(<span class="function"><span class="params">item</span> =&gt;</span> item &gt; <span class="number">0</span>);</span><br><span class="line">  arr.sort(<span class="function">(<span class="params">a, b</span>) =&gt;</span> a - b);</span><br><span class="line">  <span class="keyword">if</span> (arr.length) &#123;</span><br><span class="line">    <span class="keyword">if</span> (arr[<span class="number">0</span>] &gt; <span class="number">1</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>, len = arr.length - <span class="number">1</span>; i &lt; len; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (arr[i + <span class="number">1</span>] - arr[i] &gt; <span class="number">1</span>) &#123;</span><br><span class="line">          <span class="keyword">return</span> arr[i] + <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="comment">//  如果都是不符合，则是这种[1,2,3,4,5,6],应：</span></span><br><span class="line">      <span class="keyword">return</span> arr.pop() + <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//  第二种算法，借用选择排序</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">arr</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">//  过滤掉负数</span></span><br><span class="line">  arr = arr.filter(<span class="function"><span class="params">item</span> =&gt;</span> item &gt; <span class="number">0</span>);</span><br><span class="line">  <span class="comment">//  借用选择排序，先找到第一个最小值，不是1则返回1；不是1则比相邻元素差值，直到找到答案就能停止遍历。</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>, len = arr.length; i &lt; len; i++) &#123;</span><br><span class="line">    <span class="keyword">let</span> min = arr[i];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> j = i + <span class="number">1</span>; j &lt; arr.length; j++) &#123;</span><br><span class="line">      <span class="keyword">if</span> (arr[j] &lt; min) &#123;</span><br><span class="line">        <span class="keyword">let</span> c = min;</span><br><span class="line">        min = arr[j];</span><br><span class="line">        arr[j] = c;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    arr[i] = min;</span><br><span class="line">    <span class="keyword">if</span> (i &gt; <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (arr[i] - arr[i - <span class="number">1</span>] &gt; <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> arr[i - <span class="number">1</span>] + <span class="number">1</span>;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (min !== <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> arr.length ? arr.pop() + <span class="number">1</span> : <span class="number">1</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h4 id="快速排序"><a href="#快速排序" class="headerlink" title="快速排序"></a>快速排序</h4><p><strong>75 颜色分类</strong><br>给定一个包含红色、白色和蓝色，一共  n 个元素的数组，原地对它们进行排序，使得相同颜色的元素相邻，并按照红色、白色、蓝色顺序排列。<br>此题中，我们使用整数 0、 1 和 2 分别表示红色、白色和蓝色。<br>示例:<br>输入: [2,0,2,1,1,0]<br>输出: [0,0,1,1,2,2]</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">``</span><span class="string">`</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">### 栈</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">**20 有效的括号**</span></span><br><span class="line"><span class="string">给定一个只包括 '('，')'，'&#123;'，'&#125;'，'['，']'  的字符串，判断字符串是否有效。</span></span><br><span class="line"><span class="string">有效字符串需满足：</span></span><br><span class="line"><span class="string">左括号必须用相同类型的右括号闭合。</span></span><br><span class="line"><span class="string">左括号必须以正确的顺序闭合。</span></span><br><span class="line"><span class="string">注意空字符串可被认为是有效字符串。</span></span><br><span class="line"><span class="string">示例 1:</span></span><br><span class="line"><span class="string">输入: "()[]&#123;&#125;"</span></span><br><span class="line"><span class="string">输出: true</span></span><br><span class="line"><span class="string">示例 2:</span></span><br><span class="line"><span class="string">输入: "(]"</span></span><br><span class="line"><span class="string">输出: false</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">`</span><span class="string">``</span>javascript</span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">s</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 建立映射</span></span><br><span class="line">  <span class="keyword">let</span> map = &#123;</span><br><span class="line">    <span class="string">"("</span>: <span class="number">-1</span>,</span><br><span class="line">    <span class="string">")"</span>: <span class="number">1</span>,</span><br><span class="line">    <span class="string">"&#123;"</span>: <span class="number">-2</span>,</span><br><span class="line">    <span class="string">"&#125;"</span>: <span class="number">2</span>,</span><br><span class="line">    <span class="string">"["</span>: <span class="number">-3</span>,</span><br><span class="line">    <span class="string">"]"</span>: <span class="number">3</span></span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">let</span> stack = [];</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; s.length; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (map[s[i]] &lt; <span class="number">0</span>) &#123;</span><br><span class="line">      stack.push([s[i]]);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">let</span> last = stack.pop();</span><br><span class="line">      <span class="keyword">if</span> (map[last] + map[s[i]] !== <span class="number">0</span>) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (stack.length !== <span class="number">0</span>) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>682 棒球比赛</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">arr</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 保存结果，实际是一个栈</span></span><br><span class="line">  <span class="keyword">let</span> result = [],</span><br><span class="line">    <span class="comment">//  建立两个变量储存上个结果和上上个结果</span></span><br><span class="line">    pre1,</span><br><span class="line">    pre2;</span><br><span class="line">  <span class="comment">//  遍历，实现操作</span></span><br><span class="line">  arr.forEach(<span class="function"><span class="params">item</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">switch</span> (item) &#123;</span><br><span class="line">      <span class="keyword">case</span> <span class="string">"+"</span>:</span><br><span class="line">        pre1 = result.pop();</span><br><span class="line">        pre2 = result.pop();</span><br><span class="line">        result.push(pre2, pre1, pre1 + pre2);</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">      <span class="keyword">case</span> <span class="string">"D"</span>:</span><br><span class="line">        pre1 = result.pop();</span><br><span class="line">        result.push(pre1, pre1 * <span class="number">2</span>);</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">      <span class="keyword">case</span> <span class="string">"C"</span>:</span><br><span class="line">        result.pop();</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">      <span class="keyword">default</span>:</span><br><span class="line">        result.push(<span class="built_in">Number</span>(item));</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;);</span><br><span class="line">  <span class="keyword">return</span> result.reduce(<span class="function">(<span class="params">acc, curr</span>) =&gt;</span> acc + curr);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>85 最大矩形（困难）</strong><br>给定一个仅包含  0 和 1 的二维二进制矩阵，找出只包含 1 的最大矩形，并返回其面积。<br>示例:<br>输入:<br>[<br>[“1”,”0”,”1”,”0”,”0”],<br>[“1”,”0”,”1”,”1”,”1”],<br>[“1”,”1”,”1”,”1”,”1”],<br>[“1”,”0”,”0”,”1”,”0”]<br>]<br>输出: 6</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">``</span><span class="string">`</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">### 队列</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">[622 设计循环队列](https://jacleklm.github.io/2019/10/19/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/#more)</span></span><br><span class="line"><span class="string">**621 任务调度器**</span></span><br><span class="line"><span class="string">A - Z 字母表示的 26 种不同种类的任务(执行顺序不限),每个任务都可以在 1 个单位时间内执行完,CPU 在任何一个单位时间内都可以执行一个任务，或者在待命状态。然而，两个相同种类的任务之间必须有长度为  n 的冷却时间，因此至少有连续 n 个单位时间内 CPU 在执行不同的任务，或者在待命状态。你需要计算完成所有任务所需要的最短时间。</span></span><br><span class="line"><span class="string">示例 1：</span></span><br><span class="line"><span class="string">输入: tasks = `</span>[<span class="string">"A"</span>,<span class="string">"A"</span>,<span class="string">"A"</span>,<span class="string">"B"</span>,<span class="string">"B"</span>,<span class="string">"B"</span>]<span class="string">`, n = 2</span></span><br><span class="line"><span class="string">输出: 8</span></span><br><span class="line"><span class="string">执行顺序: A -&gt; B -&gt; (待命) -&gt; A -&gt; B -&gt; (待命) -&gt; A -&gt; B.</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">`</span><span class="string">``</span>javascript</span><br><span class="line"><span class="comment">/** 思路: LeetCode上的题解，其实不是特别理解</span></span><br><span class="line"><span class="comment"> * 最小长度 = （单个字母出现的最大次数 - 1）* （n + 1）+ 出现最大次数的字母总数</span></span><br><span class="line"><span class="comment">   最小长度 小于 任务长度时， 返回任务长度</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function">(<span class="params">&#123; tasks, n &#125;</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (n == <span class="number">0</span>) <span class="keyword">return</span> tasks.length;</span><br><span class="line">  <span class="keyword">let</span> times = <span class="keyword">new</span> <span class="built_in">Array</span>(<span class="number">26</span>).fill(<span class="number">0</span>);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> c <span class="keyword">of</span> tasks) &#123;</span><br><span class="line">    times[c.charCodeAt(<span class="number">0</span>) - <span class="string">"A"</span>.charCodeAt(<span class="number">0</span>)]++;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">let</span> maxTimes = times[<span class="number">0</span>],</span><br><span class="line">    maxItems = [<span class="number">0</span>];</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">1</span>; i &lt; <span class="number">26</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (times[i] == maxTimes) &#123;</span><br><span class="line">      maxItems.push(i);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (times[i] &gt; maxTimes) &#123;</span><br><span class="line">      maxTimes = times[i];</span><br><span class="line">      maxItems = [i];</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">let</span> ans = (maxTimes - <span class="number">1</span>) * (n + <span class="number">1</span>) + maxItems.length;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">Math</span>.max(ans, tasks.length);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="矩阵"><a href="#矩阵" class="headerlink" title="矩阵"></a>矩阵</h3><p><strong>54 螺旋矩阵</strong><br>给定一个包含  m x n  个元素的矩阵（m 行, n 列），请按照顺时针螺旋顺序，返回矩阵中的所有元素。<br>示例  1:<br>输入:<br>[<br>[ 1, 2, 3 ],<br>[ 4, 5, 6 ],<br>[ 7, 8, 9 ]<br>]<br>输出: [1,2,3,6,9,8,7,4,5]</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** 思路:用递归解决</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">matrix</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> result = [];</span><br><span class="line">  <span class="keyword">let</span> spiral = <span class="function">(<span class="params">arr, r</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>, len = arr.length; i &lt; len; i++) &#123;</span><br><span class="line">      <span class="keyword">if</span> (i === <span class="number">0</span>) &#123;</span><br><span class="line">        r = r.concat(arr[i]);</span><br><span class="line">      &#125; <span class="keyword">else</span> <span class="keyword">if</span> (i === len - <span class="number">1</span>) &#123;</span><br><span class="line">        r = r.concat(arr[i].reverse());</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (arr[i].length) &#123;</span><br><span class="line">          <span class="comment">// 增加边界检查（Leetcode测试用例）,防止只有两行的矩阵</span></span><br><span class="line">          r.push(arr[i].pop());</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    arr.shift();</span><br><span class="line">    arr.pop();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> j = arr.length - <span class="number">1</span>; j &gt;= <span class="number">0</span>; j--) &#123;</span><br><span class="line">      <span class="comment">// 增加边界检查（Leetcode测试用例）,防止只有两行的矩阵</span></span><br><span class="line">      <span class="keyword">if</span> (arr[j].length) &#123;</span><br><span class="line">        r.push(arr[j].shift());</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (arr.length) &#123;</span><br><span class="line">      <span class="keyword">return</span> spiral(arr, r);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> r;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">return</span> spiral(matrix, result);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>48 旋转图像</strong><br>给定一个 n × n 的二维矩阵表示一个图像。<br>将图像顺时针旋转 90 度。<br>说明：<br>你必须在原地旋转图像，这意味着你需要直接修改输入的二维矩阵。请不要使用另一个矩阵来旋转图像。<br>示例:<br>给定 matrix =<br>[<br>[1,2,3],<br>[4,5,6],<br>[7,8,9]<br>],<br>原地旋转输入矩阵，使其变为:<br>[<br>[7,4,1],<br>[8,5,2],<br>[9,6,3]<br>]<br>思路： 旋转其实是两次翻转（一次横轴和一次对角线轴）；并且注意每次翻转并不用遍历全部，找到边界<br><img src="/img/%E7%AE%97%E6%B3%95/%E7%9F%A9%E9%98%B5.png"></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">arr</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">//  获取矩阵的维度</span></span><br><span class="line">  <span class="keyword">let</span> v = arr.length;</span><br><span class="line">  <span class="comment">//  开始垂直翻转（其实是重写数组项）</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; v / <span class="number">2</span>; i++) &#123;</span><br><span class="line">    <span class="comment">//  其实垂直翻转，在纵轴上遍历一半就可以了</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">0</span>, tmp; j &lt; v; j++) &#123;</span><br><span class="line">      tmp = arr[i][j];</span><br><span class="line">      arr[i][j] = arr[v - i - <span class="number">1</span>][j];</span><br><span class="line">      arr[v - i - <span class="number">1</span>][j] = tmp;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//  开始斜轴翻转</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; v; i++) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">0</span>, tmp; j &lt; i; j++) &#123;</span><br><span class="line">      <span class="comment">//  这里仔细观察，会发现横轴遍历到小于斜轴处就可以了</span></span><br><span class="line">      tmp = arr[i][j];</span><br><span class="line">      arr[i][j] = arr[j][i];</span><br><span class="line">      arr[j][i] = tmp;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> arr;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="二叉树"><a href="#二叉树" class="headerlink" title="二叉树"></a>二叉树</h3><p><strong>101 对称二叉树的判断</strong><br>给定一个二叉树，检查它是否是镜像对称的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">例如，二叉树 [<span class="number">1</span>,<span class="number">2</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">4</span>,<span class="number">3</span>] 是对称的。</span><br><span class="line">    <span class="number">1</span></span><br><span class="line">   / \</span><br><span class="line">  <span class="number">2</span>   <span class="number">2</span></span><br><span class="line"> / \ / \</span><br><span class="line"><span class="number">3</span>  <span class="number">4</span> <span class="number">4</span>  <span class="number">3</span></span><br><span class="line">但是下面这个 [<span class="number">1</span>,<span class="number">2</span>,<span class="number">2</span>,<span class="literal">null</span>,<span class="number">3</span>,<span class="literal">null</span>,<span class="number">3</span>] 则不是镜像对称的:</span><br><span class="line">    <span class="number">1</span></span><br><span class="line">   / \</span><br><span class="line">  <span class="number">2</span>   <span class="number">2</span></span><br><span class="line">   \   \</span><br><span class="line">   <span class="number">3</span>    <span class="number">3</span></span><br></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *   思路：这里其实是【给一个数组构造出对应的二叉树】，并且这个二叉树有一个方法能判断本身是否是对称的</span></span><br><span class="line"><span class="comment"> *   (leetCode上只要写这个方法这部分代码就可以了)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Node</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(val) &#123;</span><br><span class="line">    <span class="keyword">this</span>.val = val;</span><br><span class="line">    <span class="keyword">this</span>.left = <span class="keyword">this</span>.right = <span class="literal">undefined</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Tree</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(data) &#123;</span><br><span class="line">    <span class="comment">//  data是传进来的数组</span></span><br><span class="line">    <span class="comment">//  创建一个数组临时储存所有节点</span></span><br><span class="line">    <span class="keyword">let</span> nodeList = [];</span><br><span class="line">    <span class="comment">//  顶节点</span></span><br><span class="line">    <span class="keyword">let</span> root;</span><br><span class="line">    <span class="comment">//  开始遍历</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; data.length; i++) &#123;</span><br><span class="line">      <span class="keyword">let</span> node = <span class="keyword">new</span> Node(data[i]);</span><br><span class="line">      nodeList.push(node);</span><br><span class="line">      <span class="keyword">if</span> (i &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">//  计算当前节点属于哪一层，sqrt是平方根</span></span><br><span class="line">        <span class="keyword">let</span> n = <span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.sqrt(i + <span class="number">1</span>));</span><br><span class="line">        <span class="comment">//  计算当前层的起始点，pow() 方法可返回 x 的 y 次幂的值</span></span><br><span class="line">        <span class="keyword">let</span> q = <span class="built_in">Math</span>.pow(<span class="number">2</span>, n) - <span class="number">1</span>;</span><br><span class="line">        <span class="comment">//  计算上一层的起始点</span></span><br><span class="line">        <span class="keyword">let</span> p = <span class="built_in">Math</span>.pow(<span class="number">2</span>, n - <span class="number">1</span>) - <span class="number">1</span>;</span><br><span class="line">        <span class="comment">//  找出当前层的父节点</span></span><br><span class="line">        <span class="keyword">let</span> parent = nodeList[p + <span class="built_in">Math</span>.floor((i - q) / <span class="number">2</span>)];</span><br><span class="line">        <span class="comment">//  将当前节点和上一层的父节点做关联</span></span><br><span class="line">        <span class="keyword">if</span> (parent.left) &#123;</span><br><span class="line">          parent.right = node;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          parent.left = node;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    root = nodeList.shift();</span><br><span class="line">    nodeList.length = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">//  构造函数要返回最顶端的根节点,return后就【完成构建了】</span></span><br><span class="line">    <span class="keyword">return</span> root;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">//  在这课二叉树中定义了一个静态方法，用来验证该二叉树是否是对称二叉树（递归）</span></span><br><span class="line">  <span class="keyword">static</span> isSymmetry(root) &#123;</span><br><span class="line">    <span class="keyword">if</span> (!root) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">let</span> walk = <span class="function">(<span class="params">left, right</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="comment">//  判断终点</span></span><br><span class="line">      <span class="keyword">if</span> (!left &amp;&amp; !right) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">      <span class="comment">//  判断不对称的情况</span></span><br><span class="line">      <span class="keyword">if</span> ((left &amp;&amp; !right) || (!left &amp;&amp; right) || left.val !== right.val)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">      <span class="comment">//  若无异常，则进入下一轮递归</span></span><br><span class="line">      <span class="keyword">return</span> walk(left.left, right.right) &amp;&amp; walk(left.right, right.left);</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="keyword">return</span> walk(root.left, root.right);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>98 验证二叉搜索树</strong><br>给定一个二叉树，判断其是否是一个有效的二叉搜索树</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">示例 <span class="number">1</span>:</span><br><span class="line">输入:</span><br><span class="line">    <span class="number">2</span></span><br><span class="line">   / \</span><br><span class="line">  <span class="number">1</span>   <span class="number">3</span></span><br><span class="line">输出: <span class="literal">true</span></span><br><span class="line">示例 <span class="number">2</span>:</span><br><span class="line">输入:</span><br><span class="line">    <span class="number">5</span></span><br><span class="line">   / \</span><br><span class="line">  <span class="number">1</span>   <span class="number">4</span></span><br><span class="line">     / \</span><br><span class="line">    <span class="number">3</span>   <span class="number">6</span></span><br><span class="line">输出: <span class="literal">false</span></span><br></pre></td></tr></table></figure>

<p>解释: 输入为: [5,1,4,null,null,3,6]。<br>  根节点的值为 5 ，但是其右子节点值为 4 。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *   思路：【给一个数组，构造出二叉搜索树】，并且这个二叉搜索树有一个方法来验证自身是否是二叉搜索树</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Node</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(val) &#123;</span><br><span class="line">    <span class="keyword">this</span>.val = val;</span><br><span class="line">    <span class="keyword">this</span>.left = <span class="keyword">this</span>.right = <span class="literal">undefined</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Tree</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(data) &#123;</span><br><span class="line">    <span class="comment">//  data是接收的数组</span></span><br><span class="line">    <span class="keyword">let</span> root = <span class="keyword">new</span> Node(data.shift());</span><br><span class="line">    data.forEach(<span class="function"><span class="params">item</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">this</span>.insert(root, item);</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">return</span> root;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//  定义插入方法</span></span><br><span class="line">  insert(node, data) &#123;</span><br><span class="line">    <span class="keyword">if</span> (node.val &gt; data) &#123;</span><br><span class="line">      <span class="keyword">if</span> (node.left === <span class="literal">undefined</span>) &#123;</span><br><span class="line">        node.left = <span class="keyword">new</span> Node(data);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">this</span>.insert(node.left, data);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (node.right === <span class="literal">undefined</span>) &#123;</span><br><span class="line">        node.right = <span class="keyword">new</span> Node(data);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">this</span>.insert(node.right, data);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//  定义验证方法,思路是遍历整个树，如果所有节点都满足node.left.val &lt; node.val &lt;node.right.val 即可</span></span><br><span class="line">  <span class="keyword">static</span> walk(root) &#123;</span><br><span class="line">    <span class="keyword">if</span> (!root.left &amp;&amp; !root.right) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (</span><br><span class="line">      (root.left &amp;&amp; root.val &lt; root.left.val) ||</span><br><span class="line">      (root.right &amp;&amp; root.val &gt; root.right.val)</span><br><span class="line">    ) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> Tree.walk(root.left) &amp;&amp; Tree.walk(root.right);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="堆"><a href="#堆" class="headerlink" title="堆"></a>堆</h3><p><strong>451 根据字符出现频率排序</strong>（堆排序）<br>给定一个字符串，请将字符串里的字符按照出现的频率降序排列。<br>示例 1:<br>输入:<br>“tree”<br>输出:<br>“eert”<br>解释:<br>‘e’出现两次，’r’和’t’都只出现一次。<br>因此’e’必须出现在’r’和’t’之前。此外，”eetr”也是一个有效的答案。<br>示例 2:<br>输入:<br>“cccaaa”<br>输出:<br>“cccaaa”<br>解释:<br>‘c’和’a’都出现三次。此外，”aaaccc”也是有效的答案。<br>注意”cacaca”是不正确的，因为相同的字母必须放在一起。<br><strong>注意</strong>：如果没有时间度复杂度的要求的话，用 sort()和 reduce 应该可以做出来。如果要求时间复杂度用堆排序可以优化性能</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** 思路: 第一步统计；第二步排序；第三步输出</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">// 常规解法，不考虑时间复杂度度与空间复杂度</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">s</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> result = [];</span><br><span class="line">  <span class="keyword">let</span> store = s</span><br><span class="line">    .split(<span class="string">""</span>)</span><br><span class="line">    .sort()</span><br><span class="line">    .reduce(<span class="function">(<span class="params">acc, curr</span>) =&gt;</span> &#123;</span><br><span class="line">      curr <span class="keyword">in</span> acc ? acc[curr]++ : (acc[curr] = <span class="number">1</span>);</span><br><span class="line">      <span class="keyword">return</span> acc;</span><br><span class="line">    &#125;, &#123;&#125;);</span><br><span class="line">  <span class="keyword">for</span> (i <span class="keyword">in</span> store) &#123;</span><br><span class="line">    result.push(i.repeat(store[i]));</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> result.sort(<span class="function">(<span class="params">a, b</span>) =&gt;</span> b.length - a.length).join(<span class="string">""</span>);</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//  用堆排序，时间复杂度优化。在我们原来的堆的代码中改constructor的代码（此时传入的是字符串）对字符串进行统计</span></span><br><span class="line"><span class="comment">//  新增一个toString方法，在这个方法里用我们的this.sort()进行堆排序后再输出</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Heap</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(str) &#123;</span><br><span class="line">    <span class="keyword">let</span> map = <span class="keyword">new</span> <span class="built_in">Map</span>();</span><br><span class="line">    str.split(<span class="string">""</span>).forEach(<span class="function"><span class="params">item</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (map.has(item)) &#123;</span><br><span class="line">        map.set(item, map.get(item) + <span class="number">1</span>);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        map.set(item, <span class="number">1</span>);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">this</span>.map = map;</span><br><span class="line">    <span class="keyword">this</span>.data = <span class="built_in">Array</span>.from(map.values());</span><br><span class="line">  &#125;</span><br><span class="line">  sort() &#123;</span><br><span class="line">    <span class="keyword">let</span> iArr = <span class="keyword">this</span>.data;</span><br><span class="line">    <span class="keyword">let</span> n = iArr.length;</span><br><span class="line">    <span class="keyword">if</span> (n &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> iArr;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="built_in">Math</span>.floor(n / <span class="number">2</span>); i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line">        <span class="comment">// 构建最大堆从最后一个父节点开始循环</span></span><br><span class="line">        Heap.maxHeapify(iArr, i, n);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">0</span>; j &lt; n; j++) &#123;</span><br><span class="line">        <span class="comment">//  不断构建最大堆的过程</span></span><br><span class="line">        <span class="comment">//  因为每次构建都会扔掉一个，所以是n-1-j</span></span><br><span class="line">        Heap.swap(iArr, <span class="number">0</span>, n - <span class="number">1</span> - j);</span><br><span class="line">        <span class="comment">//  每次交换位置扔完之后应该是从顶点开始，所以第二个参数是0</span></span><br><span class="line">        Heap.maxHeapify(iArr, <span class="number">0</span>, n - <span class="number">1</span> - j - <span class="number">1</span>);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> iArr;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  toString() &#123;</span><br><span class="line">    <span class="keyword">let</span> arr = <span class="keyword">this</span>.sort();</span><br><span class="line">    <span class="keyword">let</span> str = [];</span><br><span class="line">    <span class="keyword">while</span> (arr.length) &#123;</span><br><span class="line">      <span class="keyword">let</span> top = arr.pop();</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">let</span> [k, v] <span class="keyword">of</span> <span class="keyword">this</span>.map) &#123;</span><br><span class="line">        <span class="keyword">if</span> (v === top) &#123;</span><br><span class="line">          str.push(k.repeat(v));</span><br><span class="line">          <span class="keyword">this</span>.map.delete(k);</span><br><span class="line">          <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> str.join(<span class="string">""</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 交换两个元素</span></span><br><span class="line">  <span class="keyword">static</span> swap(arr, a, b) &#123;</span><br><span class="line">    <span class="keyword">if</span> (a === b) &#123;</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">let</span> c = arr[a];</span><br><span class="line">    arr[a] = arr[b];</span><br><span class="line">    arr[b] = c;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 构建最大堆的过程</span></span><br><span class="line">  <span class="keyword">static</span> maxHeapify(Arr, i, size) &#123;</span><br><span class="line">    <span class="comment">// i的左节点（索引），这是堆的固有规律</span></span><br><span class="line">    <span class="keyword">let</span> l = i * <span class="number">2</span> + <span class="number">1</span>;</span><br><span class="line">    <span class="comment">// i的右节点</span></span><br><span class="line">    <span class="keyword">let</span> r = i * <span class="number">2</span> + <span class="number">2</span>;</span><br><span class="line">    <span class="keyword">let</span> largest = i;</span><br><span class="line">    <span class="comment">// 父节点i和左节点l做比较取最大</span></span><br><span class="line">    <span class="keyword">if</span> (l &lt;= size &amp;&amp; Arr[l] &gt; Arr[largest]) &#123;</span><br><span class="line">      largest = l;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 右节点和最大值比较</span></span><br><span class="line">    <span class="keyword">if</span> (r &lt;= size &amp;&amp; Arr[r] &gt; Arr[largest]) &#123;</span><br><span class="line">      largest = r;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (largest !== i) &#123;</span><br><span class="line">      Heap.swap(Arr, i, largest);</span><br><span class="line">      <span class="comment">//  交换完之后确保子树也是满足最大堆的条件</span></span><br><span class="line">      Heap.maxHeapify(Arr, largest, size);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>313 超级丑数</strong>（堆查找）<br>编写一段程序来查找第 n 个超级丑数。<br>超级丑数是指其所有质因数都是长度为  k  的质数列表  primes  中的正整数。<br>示例:<br>输入: n = 12, primes = [2,7,13,19]<br>输出: 32<br>解释: 给定长度为 4 的质数列表 primes = [2,7,13,19]，前 12 个超级丑数序列为：[1,2,4,7,8,13,14,16,19,26,28,32] 。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">``</span><span class="string">`</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">### 贪婪算法</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">**122 买卖股票的最佳时机 II**</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">`</span><span class="string">``</span>javascript</span><br><span class="line"><span class="comment">/** 思路: 从最低点买入，到价格最高点卖出，不断买卖（在保证单词利益的基础上，实现多次交易）</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">prices</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 用一个变量保存利润</span></span><br><span class="line">  <span class="keyword">let</span> count = <span class="number">0</span>;</span><br><span class="line">  <span class="comment">// i是买入的时间点，j是卖出的时间点</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>, len = prices.length; i &lt; len; i++) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> j = i; j &lt; len - <span class="number">1</span>; j++) &#123;</span><br><span class="line">      <span class="keyword">if</span> (prices[j + <span class="number">1</span>] &gt; prices[j]) &#123;</span><br><span class="line">        <span class="comment">//  发现有利可图，立刻把股票卖了。在j这一天又买入（i = j）</span></span><br><span class="line">        count += prices[j + <span class="number">1</span>] - prices[j];</span><br><span class="line">        i = j;</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        i = j;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> count;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>860 柠檬水找零</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** 思路: 贪心算法，每次找零优先把手头最大的钱扔出去</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">bills</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">//  钱箱，显示自己的零钱</span></span><br><span class="line">  <span class="keyword">let</span> hand = [];</span><br><span class="line">  <span class="keyword">while</span> (bills.length) &#123;</span><br><span class="line">    <span class="keyword">let</span> money = bills.shift();</span><br><span class="line">    <span class="keyword">if</span> (money === <span class="number">5</span>) &#123;</span><br><span class="line">      hand.push(money);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      hand.sort(<span class="function">(<span class="params">a, b</span>) =&gt;</span> b - a);</span><br><span class="line">      <span class="keyword">let</span> change = money - <span class="number">5</span>;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>, len = hand.length; i &lt; len; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (hand[i] &lt;= change) &#123;</span><br><span class="line">          change -= hand[i];</span><br><span class="line">          hand.splice(i, <span class="number">1</span>);</span><br><span class="line">          i--;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (change == <span class="number">0</span>) &#123;</span><br><span class="line">          <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (change !== <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        hand.push(money);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="动态规划"><a href="#动态规划" class="headerlink" title="动态规划"></a>动态规划</h3><p><strong>53 最大子序和</strong><br>给定一个整数数组 nums ，找到一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。<br>示例:<br>输入: [-2,1,-3,4,-1,2,1,-5,4],<br>输出: 6<br>解释:  连续子数组  [4,-1,2,1] 的和最大，为  6</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 思路：动态规划，状态转移方程：sum[i] = max&#123;sum[i-1]+nums[i],nums[i]&#125; （(sum[i]记录以nums[i]为子序列末端的最大序子列连续和)）</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">nums</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> max = nums[<span class="number">0</span>];</span><br><span class="line">  <span class="keyword">let</span> sum = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; nums.length; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (sum &gt; <span class="number">0</span>) &#123;</span><br><span class="line">      sum += nums[i];</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      sum = nums[i];</span><br><span class="line">    &#125;</span><br><span class="line">    max = <span class="built_in">Math</span>.max(max, sum);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> max;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>121 买卖股票的最佳时机</strong><br>给一个数组为各天股票价格，只有一次买入和一次卖出的机会，求利润最大值<br>示例 1:<br>输入: [7,1,5,3,6,4]<br>输出: 5<br>解释: 在第 2 天（股票价格 = 1）的时候买入，在第 5 天（股票价格 = 6）的时候卖出，最大利润 = 6-1 = 5 。<br>注意利润不能是 7-1 = 6, 因为卖出价格需要大于买入价格</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">prices</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> min = <span class="built_in">Number</span>.MAX_SAFE_INTEGER;</span><br><span class="line">  <span class="keyword">let</span> sum = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; prices.length; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (prices[i] &lt;= min) &#123;</span><br><span class="line">      min = <span class="built_in">Math</span>.min(prices[i], min);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      sum = <span class="built_in">Math</span>.max(prices[i] - min, sum);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> sum;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>找出 0 或 1 的字符串中 0 和 1 连续出现的最大次数</strong><br>示例：<br>输入：’0001111100’<br>输出： 5</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">str</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> max = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">let</span> sum = <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; str.length - <span class="number">1</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (str[i] === str[i + <span class="number">1</span>]) &#123;</span><br><span class="line">      sum++;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      sum = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    max = <span class="built_in">Math</span>.max(max, sum);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> max;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>62 不同路径</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**注意：机器人不会走一些钻牛角尖的路线。eg，m=2，n=3的话，它不会[0,0],[0,1],[1,1],[1,0],[2,0],[2,1]</span></span><br><span class="line"><span class="comment"> * 思路: 状态转移方程为 d[i][j] = d[i][j-1] + d[i-1][j]；因为机器人不会钻牛角尖，会认为 d[0][j]=1,d[i][0]=1</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function">(<span class="params">&#123; m, n &#125;</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 构建矩阵肯定是先构建纵，这里认为纵为n。然后进行 1 的填充</span></span><br><span class="line">  <span class="keyword">let</span> dp = <span class="keyword">new</span> <span class="built_in">Array</span>(n);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">    dp[i] = <span class="keyword">new</span> <span class="built_in">Array</span>(m);</span><br><span class="line">    dp[i][<span class="number">0</span>] = <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">0</span>; j &lt; m; j++) &#123;</span><br><span class="line">    dp[<span class="number">0</span>][j] = <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> l = <span class="number">1</span>; l &lt; n; l++) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> k = <span class="number">1</span>; k &lt; m; k++) &#123;</span><br><span class="line">      dp[l][k] = dp[l][k - <span class="number">1</span>] + dp[l - <span class="number">1</span>][k];</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> dp[n - <span class="number">1</span>][m - <span class="number">1</span>];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="刷题"><a href="#刷题" class="headerlink" title="刷题"></a>刷题</h2><ul>
<li>要理解成链表是客观存在的，变量只是一个指针</li>
<li>链表边界如果老是处理不好，考虑下用数组储存。虽然浪费空间但是能跑起来就行</li>
</ul>
<h3 id="3-无重复字符的最长子串"><a href="#3-无重复字符的最长子串" class="headerlink" title="3. 无重复字符的最长子串"></a>3. 无重复字符的最长子串</h3><p>给定一个字符串，请你找出其中不含有重复字符的 最长子串 的长度。<br>示例 1: 输入: “abcabcbb”。输出: 3 （abc）<br>示例 3: 输入: “pwwkew”。输出: 3 （wke）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">s</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> sum = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">let</span> max = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">let</span> store = [];</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; s.length; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (store.indexOf(s[i]) === <span class="number">-1</span>) &#123;</span><br><span class="line">      store.push(s[i]);</span><br><span class="line">      sum++;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// store = [s[i]]</span></span><br><span class="line">      <span class="comment">// sum = 1</span></span><br><span class="line">      store = store.slice(store.indexOf(s[i]) + <span class="number">1</span>); <span class="comment">// 这一步比较精妙，使得字符串能舍弃同个部分后继承</span></span><br><span class="line">      store.push(s[i]);</span><br><span class="line">      sum = store.length;</span><br><span class="line">    &#125;</span><br><span class="line">    max = <span class="built_in">Math</span>.max(max, sum);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> max;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="19-删除链表的倒数第-N-个节点"><a href="#19-删除链表的倒数第-N-个节点" class="headerlink" title="19 删除链表的倒数第 N 个节点"></a>19 删除链表的倒数第 N 个节点</h3><p>可以用双循环法，第一次循环得到链表长度，第二次循环删除节点。也可以借用数组，如下所示：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// leetcode的另一种方法，借用数组</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function">(<span class="params">head, n</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> store = [];</span><br><span class="line">  <span class="keyword">let</span> curr = head;</span><br><span class="line">  <span class="keyword">while</span> (curr) &#123;</span><br><span class="line">    store.push(curr);</span><br><span class="line">    curr = curr.next;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">let</span> pre = store[store.length - n - <span class="number">1</span>]; <span class="comment">// 获取到需要删除的节点的【前一个】</span></span><br><span class="line">  pre ? (pre.next = pre.next.next) : (head = head.next); <span class="comment">//【删除操作】</span></span><br><span class="line">  <span class="keyword">return</span> head;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="21-合并两个有序链表"><a href="#21-合并两个有序链表" class="headerlink" title="21 合并两个有序链表"></a>21 合并两个有序链表</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 思路：合并两个有序链表，有点像归并排序的后半部分操作。所以类似的我们也新建一个链表储存合并的结果</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function">(<span class="params">l1, l2</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> head = <span class="keyword">new</span> ListNode(<span class="number">-1</span>);</span><br><span class="line">  <span class="comment">// 【利用js对象地址传递的方式动态更新范围链表的尾结点插入操作】</span></span><br><span class="line">  <span class="keyword">let</span> tmp = head;</span><br><span class="line">  <span class="keyword">while</span> (l1 !== <span class="literal">null</span> &amp;&amp; l2 !== <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (l1.val &lt; l2.val) &#123;</span><br><span class="line">      <span class="comment">// 这部分连接链表的操作</span></span><br><span class="line">      tmp.next = l1;</span><br><span class="line">      l1 = l1.next;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      tmp.next = l2;</span><br><span class="line">      l2 = l2.next;</span><br><span class="line">    &#125;</span><br><span class="line">    tmp = tmp.next;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 排完了，剩下的尾巴的第一个是谁就接上去</span></span><br><span class="line">  tmp.next = l1 ? l1 : l2;</span><br><span class="line">  <span class="comment">// 返回哨兵结点的next即为所求</span></span><br><span class="line">  <span class="keyword">return</span> head.next;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="59-螺旋矩阵-II"><a href="#59-螺旋矩阵-II" class="headerlink" title="59 螺旋矩阵 II"></a>59 螺旋矩阵 II</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** 思路: 当输入的为4的时候，可以看出是先输入1234，再567，再8910，再1112，1314，15，16。所以步数数组是[4,3,3,2,2,1,1]</span></span><br><span class="line"><span class="comment"> *  再提出一个当前输入点的概念，用步数数组的index % 4 判断当前输入点的走向</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 函数体</span></span><br><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">n</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="comment">// 构建结果矩阵的空矩阵</span></span><br><span class="line">  <span class="keyword">let</span> result = [];</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">    result[i] = <span class="keyword">new</span> <span class="built_in">Array</span>();</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 构建步数数组</span></span><br><span class="line">  <span class="keyword">let</span> step = [];</span><br><span class="line">  step.push(n);</span><br><span class="line">  <span class="keyword">while</span> (n &gt; <span class="number">1</span>) &#123;</span><br><span class="line">    step.push(n - <span class="number">1</span>);</span><br><span class="line">    step.push(n - <span class="number">1</span>);</span><br><span class="line">    n--;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 当前输入点和当前输入值</span></span><br><span class="line">  <span class="keyword">let</span> currPoint = [<span class="number">0</span>, <span class="number">-1</span>];</span><br><span class="line">  <span class="keyword">let</span> value = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; step.length; i++) &#123;</span><br><span class="line">    <span class="keyword">let</span> P = i % <span class="number">4</span>;</span><br><span class="line">    <span class="comment">// 当P=0时候，是从左往右输入;1是从上往下；2是从左往右；3是从下往上</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">0</span>; j &lt; step[i]; j++) &#123;</span><br><span class="line">      value++;</span><br><span class="line">      <span class="keyword">switch</span> (P) &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">0</span>:</span><br><span class="line">          currPoint[<span class="number">1</span>] = currPoint[<span class="number">1</span>] + <span class="number">1</span>;</span><br><span class="line">          <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">          currPoint[<span class="number">0</span>] = currPoint[<span class="number">0</span>] + <span class="number">1</span>;</span><br><span class="line">          <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">2</span>:</span><br><span class="line">          currPoint[<span class="number">1</span>] = currPoint[<span class="number">1</span>] - <span class="number">1</span>;</span><br><span class="line">          <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">3</span>:</span><br><span class="line">          currPoint[<span class="number">0</span>] = currPoint[<span class="number">0</span>] - <span class="number">1</span>;</span><br><span class="line">          <span class="keyword">break</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      result[currPoint[<span class="number">0</span>]][currPoint[<span class="number">1</span>]] = value;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="78-子集"><a href="#78-子集" class="headerlink" title="78 子集"></a>78 子集</h3><p>给定一组不含重复元素的整数数组  nums，返回该数组所有可能的子集（幂集）。<br>说明：解集不能包含重复的子集。<br>示例:<br>输入: nums = <code>[1,2,3]</code><br>输出:<br>[<br>[3],<br>  [1],<br>  [2],<br>  [1,2,3],<br>  [1,3],<br>  [2,3],<br>  [1,2],<br>  []<br>]</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">nums</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> result = [[]];</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; nums.length; i++) &#123;</span><br><span class="line">    <span class="keyword">let</span> len = result.length;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">0</span>; j &lt; len; j++) &#123;</span><br><span class="line">      result.push(result[j].concat([nums[i]]));</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="102-二叉树的层次遍历-剑指offer把二叉树打印成多行"><a href="#102-二叉树的层次遍历-剑指offer把二叉树打印成多行" class="headerlink" title="102. 二叉树的层次遍历 === 剑指offer把二叉树打印成多行"></a>102. 二叉树的层次遍历 === 剑指offer把二叉树打印成多行</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">root</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (!root) <span class="keyword">return</span> [];</span><br><span class="line">  <span class="keyword">let</span> result = [];</span><br><span class="line">  <span class="keyword">let</span> que = [root];</span><br><span class="line">  <span class="keyword">while</span> (que.length) &#123;</span><br><span class="line">    <span class="comment">// 这一层的打印结果</span></span><br><span class="line">    <span class="keyword">let</span> thisResult = [];</span><br><span class="line">    <span class="comment">// 下一层的节点</span></span><br><span class="line">    <span class="keyword">let</span> nextque = [];</span><br><span class="line">    <span class="keyword">while</span> (que.length) &#123;</span><br><span class="line">      <span class="keyword">let</span> node = que.shift();</span><br><span class="line">      thisResult.push(node.val);</span><br><span class="line">      <span class="keyword">if</span> (node.left) nextque.push(node.left);</span><br><span class="line">      <span class="keyword">if</span> (node.right) nextque.push(node.right);</span><br><span class="line">    &#125;</span><br><span class="line">    que = nextque;</span><br><span class="line">    result.push(thisResult);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="617-合并二叉树"><a href="#617-合并二叉树" class="headerlink" title="617 合并二叉树"></a>617 合并二叉树</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> mergeTrees = <span class="function">(<span class="params">&#123; t1, t2 &#125;</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (t1 === <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> t2;</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (t2 === <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> t1;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    t1.val = t1.val + t2.val;</span><br><span class="line">    t1.left = mergeTrees(t1.left, t2.left);</span><br><span class="line">    t1.right = mergeTrees(t1.right, t2.right);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> t1;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="单链表冒泡排序"><a href="#单链表冒泡排序" class="headerlink" title="单链表冒泡排序"></a>单链表冒泡排序</h3><h1 id="剑指-offer"><a href="#剑指-offer" class="headerlink" title="剑指 offer"></a>剑指 offer</h1><h2 id="字符串-1"><a href="#字符串-1" class="headerlink" title="字符串"></a>字符串</h2><h3 id="整数中-1-出现的次数"><a href="#整数中-1-出现的次数" class="headerlink" title="整数中 1 出现的次数"></a>整数中 1 出现的次数</h3><p>考点是<strong>时间效率</strong><br>暴力低效法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">NumberOf1Between1AndN_Solution</span>(<span class="params">n</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (n == <span class="number">0</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">let</span> str = <span class="string">"1"</span>;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">2</span>; i &lt; n + <span class="number">1</span>; i++) &#123;</span><br><span class="line">    str += i;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">let</span> count = <span class="number">0</span>;</span><br><span class="line">  str.split(<span class="string">""</span>).forEach(<span class="function"><span class="params">item</span> =&gt;</span> &#123;</span><br><span class="line">    &#123;</span><br><span class="line">      <span class="keyword">if</span> (item === <span class="string">"1"</span>) count++;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;);</span><br><span class="line">  <span class="keyword">return</span> count;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="数组-1"><a href="#数组-1" class="headerlink" title="数组"></a>数组</h2><h3 id="数字在排序数组中出现的次数"><a href="#数字在排序数组中出现的次数" class="headerlink" title="数字在排序数组中出现的次数"></a>数字在排序数组中出现的次数</h3><p>统计一个数字在<strong>排序数组</strong>中出现的次数</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">GetNumberOfK</span>(<span class="params">data, k</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (data.indexOf(k) == <span class="number">-1</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">let</span> count = data.lastIndexOf(k) - data.indexOf(k) + <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">return</span> count;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="数组中只出现一次的数字（易）"><a href="#数组中只出现一次的数字（易）" class="headerlink" title="数组中只出现一次的数字（易）"></a>数组中只出现一次的数字（易）</h3><p>一个整型数组里除了两个数字之外，其他的数字都出现了两次。请写程序找出这两个只出现一次的数字</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">FindNumsAppearOnce</span>(<span class="params">array</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> result = [];</span><br><span class="line">  array.sort();</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; array.length; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (array[i] === array[i + <span class="number">1</span>]) &#123;</span><br><span class="line">      i++;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      result.push(array[i]);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="栈和队列"><a href="#栈和队列" class="headerlink" title="栈和队列"></a>栈和队列</h2><h3 id="两个栈来实现队列"><a href="#两个栈来实现队列" class="headerlink" title="两个栈来实现队列"></a>两个栈来实现队列</h3><p>用两个栈来实现一个队列，完成队列的 Push 和 Pop 操作。 队列中的元素为 int 类型(一种数据类型，用于定义整数类型变量的标识符)<br><strong>思路</strong>：stack2 反向接在 stack1 的前面，中间部分都是 stack 的头（画图理解）。stack1 为入队栈，stack2 为出队栈</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> stack1 = [];</span><br><span class="line"><span class="keyword">let</span> stack2 = [];</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">push</span>(<span class="params">node</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (stack2.length) &#123;</span><br><span class="line">    stack1.push(stack2.pop());</span><br><span class="line">  &#125;</span><br><span class="line">  stack1.push(node);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">pop</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (stack1.length) &#123;</span><br><span class="line">    stack2.push(stack1.pop());</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> stack2.pop();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="栈的压入和弹出序列"><a href="#栈的压入和弹出序列" class="headerlink" title="栈的压入和弹出序列"></a>栈的压入和弹出序列</h3><blockquote>
<p>注意这个知识点</p>
</blockquote>
<p>1,2,3,4,5 是某栈的压入顺序，序列 4,5,3,2,1 是该压栈序列对应的一个弹出序列（先入 1234 出 4，进 5，出 5321），但 4,3,5,1,2 就不可能是该压栈序列的弹出序列。同理 4,3,5,1,2 可是返回 true（先入 1234 出 43，入 5 出 512）</p>
<p>思路：借助辅助栈，模拟压出弹出的过程。第一个希望被弹出的数字是 4,因此 4 需要先压入到辅助栈里面。压入栈的顺序由压栈序列确定了，也就是在把 4 压入进栈之前，数字 1、2、3 都需要先压入到栈里面。此时栈里包含 4 个数字，分别是 1、2、3、4,其中 4 于栈顶。把 4 弹出栈后，剩下的三个数字是 1、2 和 3。接下来希望被弹出的数字是 5，由 于它不是栈顶数字，因此我们接着在第一个序列中把 4 以后数字压入辅助栈 中，直到压入了数字 5。这个时候 5 位于栈顶，就可以被弹出来了。接下来希望被弹出的三个数字依次是 3、2 和 1。由于每次操作前它们都位于栈顶， 因此直接弹出即可。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">IsPopOrder</span>(<span class="params">pushV, popV</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (pushV.length == <span class="number">0</span> || popV.length == <span class="number">0</span>) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  <span class="keyword">let</span> stack = [];</span><br><span class="line">  <span class="keyword">let</span> index = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; pushV.length; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (pushV[i] === popV[index]) &#123;</span><br><span class="line">      index++;</span><br><span class="line">      <span class="keyword">while</span> (stack.length &gt; <span class="number">0</span> &amp;&amp; stack[stack.length - <span class="number">1</span>] == popV[index]) &#123;</span><br><span class="line">        stack.pop();</span><br><span class="line">        index++;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      stack.push(pushV[i]);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (stack.length === <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="滑动窗口的最大值"><a href="#滑动窗口的最大值" class="headerlink" title="滑动窗口的最大值"></a>滑动窗口的最大值</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">maxInWindows</span>(<span class="params">num, size</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (!size || size &gt; num.length) <span class="keyword">return</span> [];</span><br><span class="line">  <span class="keyword">let</span> curr = num.slice(<span class="number">0</span>, size);</span><br><span class="line">  <span class="keyword">let</span> result = [];</span><br><span class="line">  result.push(<span class="built_in">Math</span>.max.apply(<span class="literal">null</span>, curr));</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = size; i &lt; num.length; i++) &#123;</span><br><span class="line">    curr.push(num[i]);</span><br><span class="line">    curr.shift();</span><br><span class="line">    result.push(<span class="built_in">Math</span>.max.apply(<span class="literal">null</span>, curr));</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="链表"><a href="#链表" class="headerlink" title="链表"></a>链表</h2><h3 id="复杂链表的复制"><a href="#复杂链表的复制" class="headerlink" title="复杂链表的复制"></a>复杂链表的复制</h3><p>输入一个复杂链表（每个节点中有节点值，以及两个指针，一个指向下一个节点，另一个特殊指针指向任意一个节点），返回结果为复制后复杂链表的 head。（注意，输出结果中请不要返回参数中的节点引用，否则判题程序会直接返回空）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*function RandomListNode(x)&#123;</span></span><br><span class="line"><span class="comment">    this.label = x;</span></span><br><span class="line"><span class="comment">    this.next = null;</span></span><br><span class="line"><span class="comment">    this.random = null;</span></span><br><span class="line"><span class="comment">&#125;*/</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Clone</span>(<span class="params">pHead</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (pHead === <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">let</span> node = <span class="keyword">new</span> RandomListNode(pHead.label);</span><br><span class="line">  node.random = pHead.random;</span><br><span class="line">  node.next = Clone(pHead.next);</span><br><span class="line">  <span class="keyword">return</span> node;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="二叉搜索树与双向链表（有点难）"><a href="#二叉搜索树与双向链表（有点难）" class="headerlink" title="二叉搜索树与双向链表（有点难）"></a>二叉搜索树与双向链表（有点难）</h3><p>输入一棵二叉搜索树，将该二叉搜索树转换成一个排序的双向链表。要求不能创建任何新的结点，只能调整树中结点指针的指向<br>思路：递归</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Convert</span>(<span class="params">root</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 递归的终点判断</span></span><br><span class="line">  <span class="keyword">if</span> (!root) <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">  <span class="keyword">if</span> (!root.left &amp;&amp; !root.right) <span class="keyword">return</span> root;</span><br><span class="line">  <span class="comment">//构建左子树为双向链表，返回链表头节点left</span></span><br><span class="line">  <span class="keyword">var</span> left = Convert(root.left);</span><br><span class="line">  <span class="keyword">var</span> tmp = left;</span><br><span class="line">  <span class="comment">//若左子树存在，则用tmp定位到左双向链表最后一个节点</span></span><br><span class="line">  <span class="keyword">while</span> (tmp &amp;&amp; tmp.right) &#123;</span><br><span class="line">    tmp = tmp.right;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//若左子树存在，将左双向链表最后一个节点和当前节点连接起来</span></span><br><span class="line">  <span class="comment">//注意这里，tmp与left的存在与否完全相同</span></span><br><span class="line">  <span class="keyword">if</span> (tmp) &#123;</span><br><span class="line">    tmp.right = root;</span><br><span class="line">    root.left = tmp;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//构建右子树为双向链表，返回链表头结点right</span></span><br><span class="line">  <span class="keyword">var</span> right = Convert(root.right);</span><br><span class="line">  <span class="comment">//将右双向链表头节点和当前节点连接起来</span></span><br><span class="line">  <span class="keyword">if</span> (right) &#123;</span><br><span class="line">    right.left = root;</span><br><span class="line">    root.right = right;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//若有左子树，返回的永远是最左边的叶子节点（也就是最小的节点），若无左子树，则根节点就是最小的节点</span></span><br><span class="line">  <span class="keyword">return</span> left ? left : root;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="删除排序链表中的重复节点"><a href="#删除排序链表中的重复节点" class="headerlink" title="删除排序链表中的重复节点"></a>删除排序链表中的重复节点</h3><p>额外空间，双指针</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">deleteDuplication</span>(<span class="params">pHead</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> head = <span class="keyword">new</span> ListNode(<span class="number">-1</span>);</span><br><span class="line">  head.next = pHead;</span><br><span class="line">  <span class="keyword">let</span> pre = head;</span><br><span class="line">  <span class="keyword">let</span> cur = head.next;</span><br><span class="line">  <span class="keyword">while</span> (cur) &#123;</span><br><span class="line">    <span class="keyword">if</span> (cur.next !== <span class="literal">null</span> &amp;&amp; cur.val === cur.next.val) &#123;</span><br><span class="line">      <span class="keyword">while</span> (cur.next !== <span class="literal">null</span> &amp;&amp; cur.val === cur.next.val) &#123;</span><br><span class="line">        cur = cur.next;</span><br><span class="line">      &#125;</span><br><span class="line">      pre.next = cur.next;</span><br><span class="line">      cur = cur.next;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      pre = pre.next;</span><br><span class="line">      cur = cur.next;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> head.next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="二叉树-1"><a href="#二叉树-1" class="headerlink" title="二叉树"></a>二叉树</h2><h3 id="二叉树的深度"><a href="#二叉树的深度" class="headerlink" title="二叉树的深度"></a>二叉树的深度</h3><p>输入一棵二叉树，求该树的深度。从根结点到叶结点依次经过的结点（含根、叶结点）形成树的一条路径，最长路径的长度为树的深度</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">TreeDepth</span>(<span class="params">pRoot</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (!pRoot) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">let</span> left = TreeDepth(pRoot.left);</span><br><span class="line">  <span class="keyword">let</span> right = TreeDepth(pRoot.right);</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">Math</span>.max(left, right) + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="平衡二叉树"><a href="#平衡二叉树" class="headerlink" title="平衡二叉树"></a>平衡二叉树</h3><p>输入一棵二叉树，判断该二叉树是否是平衡二叉树。eg. <code>[1,2,3,4,5,6,7]</code>是平衡的</p>
<blockquote>
<p>平衡二叉树（Balanced Binary Tree），具有以下性质：它是一棵空树或它的左右两个子树的高度差的绝对值不超过 1，并且左右两个子树都是一棵平衡二叉树</p>
</blockquote>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span></span></span><br></pre></td></tr></table></figure>

<h3 id="重建二叉树"><a href="#重建二叉树" class="headerlink" title="重建二叉树"></a>重建二叉树</h3><p>输入某二叉树的前序遍历和中序遍历的结果，请重建出该二叉树.如输入前序遍历序列{1,2,4,7,3,5,6,8}和中序遍历序列{4,7,2,1,5,3,8,6}<br><strong>思路</strong>：寻找能把左右子树区分开的 index，结合递归</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">reConstructBinaryTree</span>(<span class="params">pre, vin</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (pre.length == <span class="number">0</span> || vin.length == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//前序第一个为根节点 也是中序左右子树的分割点</span></span><br><span class="line">  <span class="keyword">var</span> index = vin.indexOf(pre[<span class="number">0</span>]);</span><br><span class="line">  <span class="keyword">var</span> left = vin.slice(<span class="number">0</span>, index); <span class="comment">//中序左子树</span></span><br><span class="line">  <span class="keyword">var</span> right = vin.slice(index + <span class="number">1</span>); <span class="comment">//中序右子树</span></span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    val: pre[<span class="number">0</span>],</span><br><span class="line">    <span class="comment">//递归左右子树的前序，中序</span></span><br><span class="line">    left: reConstructBinaryTree(pre.slice(<span class="number">1</span>, index + <span class="number">1</span>), left),</span><br><span class="line">    right: reConstructBinaryTree(pre.slice(index + <span class="number">1</span>), right)</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="树的子结构"><a href="#树的子结构" class="headerlink" title="树的子结构"></a>树的子结构</h3><p>输入两棵二叉树 A，B，判断 B 是不是 A 的子结构</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 先序遍历用于打印树的结构。先序遍历两个树，看结果是否包含</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">HasSubtree</span>(<span class="params">pRoot1, pRoot2</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (!pRoot2) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> preTraversal(pRoot1).includes(preTraversal(pRoot2));</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">preTraversal</span>(<span class="params">root</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> result = [];</span><br><span class="line">  <span class="comment">// 【这种立刻执行的函数，上一句最后要有 ; 不然会有bug！】</span></span><br><span class="line">  (<span class="function"><span class="keyword">function</span> <span class="title">pre</span>(<span class="params">node</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (node) &#123;</span><br><span class="line">      result.push(node.val);</span><br><span class="line">      pre(node.left);</span><br><span class="line">      pre(node.right);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)(root);</span><br><span class="line">  <span class="keyword">return</span> result.join(<span class="string">","</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="二叉树的镜像操作"><a href="#二叉树的镜像操作" class="headerlink" title="二叉树的镜像操作"></a>二叉树的镜像操作</h3><p>递归版：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Mirror</span>(<span class="params">root</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (root === <span class="literal">null</span>) <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">  [root.left, root.right] = [root.right, root.left];</span><br><span class="line">  Mirror(root.left);</span><br><span class="line">  Mirror(root.right);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>非递归版，有点像层次遍历不过是用栈</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Mirror</span>(<span class="params">root</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (root === <span class="literal">null</span>) <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">  <span class="keyword">let</span> stack = [root];</span><br><span class="line">  <span class="keyword">while</span> (stack.length) &#123;</span><br><span class="line">    <span class="keyword">let</span> node = stack.pop();</span><br><span class="line">    [node.left, node.right] = [node.right, node.left];</span><br><span class="line">    <span class="keyword">if</span> (node.left) stack.push(node.left);</span><br><span class="line">    <span class="keyword">if</span> (node.right) stack.push(node.right);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="判断是否是对称二叉树"><a href="#判断是否是对称二叉树" class="headerlink" title="判断是否是对称二叉树"></a>判断是否是对称二叉树</h3><p>同 leetCode</p>
<h3 id="二叉搜索树的后序遍历序列"><a href="#二叉搜索树的后序遍历序列" class="headerlink" title="二叉搜索树的后序遍历序列"></a>二叉搜索树的后序遍历序列</h3><p>输入一个整数数组，判断该数组是不是某二叉搜索树的后序遍历的结果。如果是则输出 Yes,否则输出 No。假设输入的数组的任意两个数字都互不相同<br>思路：后序遍历是 左-右-中，所以数组第一个是二叉树最小值，最后一个是 root。左子树所有节点都小于 root。<br>依次与数组最后一个值比较，一直小于最后一个数的是左子树的，计数，剩下的是右子树的，依次判断剩下的是否全部大于最后一个数，若否，则为 false。类似于二分法</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">VerifySquenceOfBST</span>(<span class="params">sequence</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (sequence.length === <span class="number">0</span>) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">judge</span>(<span class="params">sequence, sta, end</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (sta &gt;= end) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// end是根节点</span></span><br><span class="line">    <span class="keyword">let</span> i = end;</span><br><span class="line">    <span class="comment">// 通过这个while能用i把左右子树区分开，并且右子树满足搜索树</span></span><br><span class="line">    <span class="keyword">while</span> (i &gt; sta &amp;&amp; sequence[i - <span class="number">1</span>] &gt; sequence[end]) &#123;</span><br><span class="line">      i--;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 验证左子树是否满足都小于根节点</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> j = i - <span class="number">1</span>; j &gt;= sta; j--) &#123;</span><br><span class="line">      <span class="keyword">if</span> (sequence[j] &gt; sequence[end]) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> judge(sequence, sta, i - <span class="number">1</span>) &amp;&amp; judge(sequence, i + <span class="number">1</span>, end);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> judge(sequence, <span class="number">0</span>, sequence.length - <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="二叉树中和为某一值的路径"><a href="#二叉树中和为某一值的路径" class="headerlink" title="二叉树中和为某一值的路径"></a>二叉树中和为某一值的路径</h3><p>输入一颗二叉树的根节点和一个整数，打印出二叉树中结点值的和为输入整数的所有路径。路径定义为从树的根结点开始往下一直到叶结点所经过的结点形成一条路径。(注意: 在返回值的 list 中，数组长度大的数组靠前)<br><strong>思路：</strong></p>
<ol>
<li>前序遍历二叉树，每次更新当前路径的和 curtSum</li>
<li>判断当前结点是否是叶子结点，以及 curtSum 是否等于 expectNumber。如果是，把当前路径保存在 res 结果中</li>
<li>若不符合条件，则弹出此结点</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">FindPath</span>(<span class="params">root, expectNumber</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> result = [];</span><br><span class="line">  <span class="keyword">if</span> (root === <span class="literal">null</span>) <span class="keyword">return</span> result;</span><br><span class="line">  _find(root, expectNumber, [], <span class="number">0</span>, result);</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">_find</span>(<span class="params">root, expectNumber, path, currSum, result</span>) </span>&#123;</span><br><span class="line">  currSum += root.val;</span><br><span class="line">  path.push(root.val);</span><br><span class="line">  <span class="keyword">if</span> (currSum === expectNumber &amp;&amp; root.left === <span class="literal">null</span> &amp;&amp; root.right === <span class="literal">null</span>) &#123;</span><br><span class="line">    result.push([...path]);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (root.left !== <span class="literal">null</span>) &#123;</span><br><span class="line">    _find(root.left, expectNumber, path, currSum, result);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (root.right !== <span class="literal">null</span>) &#123;</span><br><span class="line">    _find(root.right, expectNumber, path, currSum, result);</span><br><span class="line">  &#125;</span><br><span class="line">  path.pop();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="二叉树的下一个结点"><a href="#二叉树的下一个结点" class="headerlink" title="二叉树的下一个结点"></a>二叉树的下一个结点</h3><p>给定一个二叉树和其中的一个结点，请找出中序遍历顺序的下一个结点并且返回。注意，树中的结点不仅包含左右子结点，同时包含指向父结点的指针<br><strong>思路：</strong><br>（画一个4层的树容易理解）</p>
<ol>
<li>若该节点存在右子树：则下一个节点为右子树最左子节点</li>
<li>若该节点不存在右子树：这时分两种情况：</li>
</ol>
<ul>
<li>2.1 该节点为父节点的左子节点，则下一个节点为其父节点</li>
<li>2.2 该节点为父节点的右子节点，则沿着父节点向上遍历，直到找到一个节点的父节点的左子节点为该节点，则该节点的父节点下一个节点</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">GetNext</span>(<span class="params">pNode</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span>(!pNode) <span class="keyword">return</span> pNode</span><br><span class="line">  <span class="comment">// 1. 存在右子树的情况</span></span><br><span class="line">  <span class="keyword">if</span>(pNode.right) &#123;</span><br><span class="line">    pNode = pNode.right</span><br><span class="line">    <span class="keyword">while</span>(pNode.left) &#123;</span><br><span class="line">      pNode = pNode.left</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> pNode</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 2. 不存在右子树</span></span><br><span class="line">  <span class="keyword">while</span>(pNode.next) &#123;</span><br><span class="line">    <span class="keyword">if</span>(pNode === pNode.next.left) &#123;</span><br><span class="line">      <span class="comment">// 2.1</span></span><br><span class="line">      <span class="keyword">return</span> pNode.next</span><br><span class="line">    &#125;</span><br><span class="line">    pNode = pNode.next</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">null</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="查找和排序"><a href="#查找和排序" class="headerlink" title="查找和排序"></a>查找和排序</h2><h3 id="旋转数组的最小数字"><a href="#旋转数组的最小数字" class="headerlink" title="旋转数组的最小数字"></a>旋转数组的最小数字</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> <span class="built_in">Math</span>.min.apply(<span class="literal">null</span>, rotateArray);</span><br></pre></td></tr></table></figure>

<h3 id="最小的-k-个数"><a href="#最小的-k-个数" class="headerlink" title="最小的 k 个数"></a>最小的 k 个数</h3><p>输入 n 个整数，找出其中最小的 K 个数。例如输入 4,5,1,6,2,7,3,8 这 8 个数字，则最小的 4 个数字是 1,2,3,4<br>思路：找最小的，显然是选择排序</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">GetLeastNumbers_Solution</span>(<span class="params">input, k</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (input.length &lt; k) <span class="keyword">return</span> [];</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; input.length - <span class="number">1</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">let</span> min = input[i];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> j = i + <span class="number">1</span>; j &lt; input.length; j++) &#123;</span><br><span class="line">      <span class="keyword">if</span> (input[j] &lt; min) &#123;</span><br><span class="line">        <span class="keyword">let</span> c = min;</span><br><span class="line">        min = input[j];</span><br><span class="line">        input[j] = c;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    input[i] = min;</span><br><span class="line">    <span class="keyword">if</span> (i === k) <span class="keyword">return</span> input.slice(<span class="number">0</span>, k);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> input;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="递归和循环"><a href="#递归和循环" class="headerlink" title="递归和循环"></a>递归和循环</h2><h3 id="斐波那契数列"><a href="#斐波那契数列" class="headerlink" title="斐波那契数列"></a>斐波那契数列</h3><p>输入一个整数 n，请你输出斐波那契数列的第 n 项（从 0 开始，第 0 项为 0）</p>
<blockquote>
<p>斐波那契数列。满足如下的动态规划方程：F(1)=1，F(2)=1, F(n)=F(n-1)+F(n-2)</p>
</blockquote>
<p>普通递归容易导致内存溢出而超时，尾递归能算出结果。动态规划解法最好<br>递归解法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">n</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (n === <span class="number">0</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">if</span> (n === <span class="number">1</span>) <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">return</span> exportFunction(n - <span class="number">1</span>) + exportFunction(n - <span class="number">2</span>);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>尾递归：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">tailFibonacci</span>(<span class="params">n, ac1, ac2</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (n == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (n == <span class="number">1</span> || n == <span class="number">2</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> ac2;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> tailFibonacci(n - <span class="number">1</span>, ac2, ac1 + ac2);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Fibonacci</span>(<span class="params">n</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> tailFibonacci(n, <span class="number">1</span>, <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>动态规划解法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">n</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> dp = <span class="keyword">new</span> <span class="built_in">Array</span>(n + <span class="number">1</span>);</span><br><span class="line">  dp[<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">  dp[<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">2</span>; i &lt; n + <span class="number">1</span>; i++) &#123;</span><br><span class="line">    dp[i] = dp[i - <span class="number">1</span>] + dp[i - <span class="number">2</span>];</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> dp[n];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="跳台阶"><a href="#跳台阶" class="headerlink" title="跳台阶"></a>跳台阶</h3><p>同理动态规划</p>
<h3 id="变态跳台阶"><a href="#变态跳台阶" class="headerlink" title="变态跳台阶"></a>变态跳台阶</h3><p>一只青蛙一次可以跳上 1 级台阶，也可以跳上 2 级……它也可以跳上 n 级<br><strong>思路</strong>：状态转移方程 F(n)=F(n-1)+F(n-2)+…F(2)+F(1)+1; 由上式得 F(n)=2F(n-1);F(1)=1 ;可得到 Fn(n)=2^(n-1)。<br>思路也可以是：由于可以 n 阶，每一跳可以跳 1,2,3….n 阶，但是每一跳不可以跳 0 阶，这就相当于是在 n(n&gt;1)个相同的球分堆（组），有多少种分类情况。转成一个排列组合问题，答案是 2^(n-1)</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">jumpFloorII</span>(<span class="params">number</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (number &lt;= <span class="number">1</span>) <span class="keyword">return</span> number;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">Math</span>.pow(<span class="number">2</span>, number - <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="矩阵覆盖"><a href="#矩阵覆盖" class="headerlink" title="矩阵覆盖"></a>矩阵覆盖</h3><p>用 n 个<code>2*1</code>的小矩形无重叠地覆盖一个<code>2*n</code>的大矩形，总共有多少种方法？<br>可以发现依旧是斐波那契数列，用动态规划解答即可</p>
<h2 id="动态和贪心"><a href="#动态和贪心" class="headerlink" title="动态和贪心"></a>动态和贪心</h2><h3 id="剪绳子"><a href="#剪绳子" class="headerlink" title="剪绳子"></a>剪绳子</h3><p>给你一根长度为 n 的绳子，请把绳子剪成 m 段（m、n 都是整数，n&gt;1 并且 m&gt;1），每段绳子的长度记为 k[0],k[1],…,k[m]。请问 k[0] x k[1] x … x k[m]可能的最大乘积是多少？例如，当绳子的长度是 8 时，我们把它剪成长度分别为 2、3、3 的三段，此时得到的最大乘积是 18</p>
<p>动态规划思路：</p>
<p>当 n 大于等于 5 时，我们尽可能多的剪长度为 3 的绳子；当剩下的绳子长度为 4 时，把绳子剪成两段长度为 2 的绳子。 为什么选 2，3 为最小的子问题？因为 2，3 包含于各个问题中，<strong>如果再往下剪得话，乘积就会变小</strong>（所以是临界状态）。所以在 dp 数组中，dp[2]=2，dp[3]=3。<br>状态转移方程 父绳乘积最大值 = 子绳 1 乘积最大值 * 子绳 2 乘积最大值（这个状态转移方程使得题目中剪成 m 段这个说法可以略去），然后做 for 循环，max = Math.max(max, curr)</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">cutRope</span>(<span class="params">number</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (number === <span class="number">2</span>) <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">if</span> (number === <span class="number">3</span>) <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">  <span class="keyword">let</span> dp = [<span class="number">1</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">4</span>; i &lt; number + <span class="number">1</span>; i++) &#123;</span><br><span class="line">    <span class="comment">// 用一个变量记录不同剪法乘积的最大值</span></span><br><span class="line">    <span class="keyword">let</span> max = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">1</span>; j &lt;= i / <span class="number">2</span>; j++) &#123;</span><br><span class="line">      <span class="comment">// 父绳乘积最大值 = 子绳1乘积最大值 * 子绳2乘积最大值</span></span><br><span class="line">      <span class="keyword">let</span> curr = dp[j] * dp[i - j];</span><br><span class="line">      max = <span class="built_in">Math</span>.max(max, curr);</span><br><span class="line">    &#125;</span><br><span class="line">    dp[i] = max;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> dp[number];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>贪心思路：由动态规划的思路，我们知道当绳子最小为 3 或 2 的时候就不能分了。那么绳子应该优先按 3 去分得到的乘积才是最大，剩下的尾巴再去 2 分（并且如果尾巴是 1 的话，说明最后一段是 4，那这段应该按 2 x 2 分）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">cutRope</span>(<span class="params">number</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (number === <span class="number">2</span>) <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">if</span> (number === <span class="number">3</span>) <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">  <span class="keyword">let</span> timesOf3 = <span class="built_in">Math</span>.floor(number / <span class="number">3</span>);</span><br><span class="line">  <span class="comment">// 当最后尾巴为1时，说明最后一段是4，应分割成 2 x 2更好，因为2*2=4 &gt; 3=3*1</span></span><br><span class="line">  <span class="keyword">if</span> (number - timesOf3 * <span class="number">3</span> === <span class="number">1</span>) timesOf3--;</span><br><span class="line">  <span class="comment">// 3分完最后尾巴2分</span></span><br><span class="line">  timesOf2 = <span class="built_in">Math</span>.floor((number - timesOf3 * <span class="number">3</span>) / <span class="number">2</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">Math</span>.pow(<span class="number">3</span>, timesOf3) * <span class="built_in">Math</span>.pow(<span class="number">2</span>, timesOf2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="数学题"><a href="#数学题" class="headerlink" title="数学题"></a>数学题</h2><h3 id="二进制中-1-的个数"><a href="#二进制中-1-的个数" class="headerlink" title="二进制中 1 的个数"></a>二进制中 1 的个数</h3><p>输入一个整数，输出该数二进制表示中 1 的个数。其中负数用补码表示<br><strong>牛客网大佬的思路：</strong>如果一个整数不为 0，那么这个整数至少有一位是 1。如果我们把这个整数减 1，那么原来处在整数最右边的 1 就会变为 0，原来在 1 后面的所有的 0 都会变成 1(如果最右边的 1 后面还有 0 的话)。其余所有位将不会受到影响。eg，二进制 1100，减去 1 会变成 1011。此时我们做与操作，1100&amp;1011 = 1000。也就是说，把一个整数减去 1，再和原整数做与运算，会把该整数最右边一个 1 变成 0.那么一个整数的二进制有多少个 1，就可以进行多少次这样的操作</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction = <span class="function"><span class="params">n</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">let</span> count = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">while</span> (n != <span class="number">0</span>) &#123;</span><br><span class="line">    count++;</span><br><span class="line">    n = n &amp; (n - <span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> count;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="和为-S-的连续正数序列"><a href="#和为-S-的连续正数序列" class="headerlink" title="和为 S 的连续正数序列"></a>和为 S 的连续正数序列</h3><p>输出所有和为 S 的连续正数序列。序列内按照从小至大的顺序，序列间按照开始数字从小到大的顺序。每个子序列长度至少为 2</p>
<h1 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h1><h2 id="比较版本号"><a href="#比较版本号" class="headerlink" title="比较版本号"></a>比较版本号</h2><p>实现一个方法，用于比较两个版本号（version1、version2）,如果version1 &gt; version2，返回1；如果version1 &lt; version2，返回-1，其他情况返回0。版本号规则<code>x.y.z</code>，xyz均为大于等于0的整数，至少有x位</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> exportFunction1 = <span class="function">(<span class="params">&#123; curV, reqV &#125;</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (curV &amp;&amp; reqV) &#123;</span><br><span class="line">    <span class="comment">//将两个版本号拆成数字</span></span><br><span class="line">    <span class="keyword">var</span> arr1 = curV.split(<span class="string">'.'</span>),</span><br><span class="line">      arr2 = reqV.split(<span class="string">'.'</span>)</span><br><span class="line">    <span class="keyword">var</span> minLength = <span class="built_in">Math</span>.min(arr1.length, arr2.length),</span><br><span class="line">      position = <span class="number">0</span>,</span><br><span class="line">      diff = <span class="number">0</span></span><br><span class="line">    <span class="comment">//依次比较版本号每一位大</span></span><br><span class="line">    <span class="keyword">while</span> (</span><br><span class="line">      position &lt; minLength &amp;&amp;</span><br><span class="line">      (diff = <span class="built_in">parseInt</span>(arr1[position]) - <span class="built_in">parseInt</span>(arr2[position])) == <span class="number">0</span></span><br><span class="line">    ) &#123;</span><br><span class="line">      position++</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(diff == <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> arr1.length - arr2.length &gt; <span class="number">0</span> ? <span class="number">1</span> : <span class="number">0</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> diff &gt; <span class="number">0</span> ? <span class="number">1</span> : <span class="number">-1</span></span><br><span class="line">    &#125; </span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">//输入为空</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'版本号不能为空'</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="随机生成字符串"><a href="#随机生成字符串" class="headerlink" title="随机生成字符串"></a>随机生成字符串</h2><p>实现一个随机符串生成函数 randomStr()，要求如下：</p>
<ul>
<li>生成的随机的字符串应该以字母开头，并包含 <code>[a-z][0-9]</code> 这些字符。</li>
<li>生成的字符串长度为 8。</li>
<li>生成的字符串不能够在程序运行的生命周期中存在重复的情形</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 解法一： 三十六进制实际上就是（0-9，a-z），十个数字加26个英文字母: "0.ryv9e51n6s3u8adqksxscerk9"  </span></span><br><span class="line"><span class="keyword">const</span> randomStr = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">Math</span>.random().toString(<span class="number">36</span>).slice(<span class="number">2</span>, <span class="number">10</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 解法2: 笨方法。</span></span><br><span class="line"><span class="keyword">const</span> randomStr1 = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> map = arr = [<span class="string">'0'</span>, <span class="string">'1'</span>, <span class="string">'2'</span>, <span class="string">'3'</span>, <span class="string">'4'</span>, <span class="string">'5'</span>, <span class="string">'6'</span>, <span class="string">'7'</span>, <span class="string">'8'</span>, <span class="string">'9'</span>, <span class="string">'a'</span>, <span class="string">'b'</span>, <span class="string">'c'</span>, <span class="string">'d'</span>, <span class="string">'e'</span>, <span class="string">'f'</span>, <span class="string">'g'</span>, <span class="string">'h'</span>, <span class="string">'i'</span>, <span class="string">'j'</span>, <span class="string">'k'</span>, <span class="string">'l'</span>, <span class="string">'m'</span>, <span class="string">'n'</span>, <span class="string">'o'</span>, <span class="string">'p'</span>, <span class="string">'q'</span>, <span class="string">'r'</span>, <span class="string">'s'</span>, <span class="string">'t'</span>, <span class="string">'u'</span>, <span class="string">'v'</span>, <span class="string">'w'</span>, <span class="string">'x'</span>, <span class="string">'y'</span>, <span class="string">'z'</span>, <span class="string">'A'</span>, <span class="string">'B'</span>, <span class="string">'C'</span>, <span class="string">'D'</span>, <span class="string">'E'</span>, <span class="string">'F'</span>, <span class="string">'G'</span>, <span class="string">'H'</span>, <span class="string">'I'</span>, <span class="string">'J'</span>, <span class="string">'K'</span>, <span class="string">'L'</span>, <span class="string">'M'</span>, <span class="string">'N'</span>, <span class="string">'O'</span>, <span class="string">'P'</span>, <span class="string">'Q'</span>, <span class="string">'R'</span>, <span class="string">'S'</span>, <span class="string">'T'</span>, <span class="string">'U'</span>, <span class="string">'V'</span>, <span class="string">'W'</span>, <span class="string">'X'</span>, <span class="string">'Y'</span>, <span class="string">'Z'</span>];</span><br><span class="line">  <span class="keyword">let</span> res = <span class="string">''</span></span><br><span class="line">  <span class="keyword">for</span>(<span class="keyword">let</span> i = <span class="number">0</span>;i &lt; <span class="number">8</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">const</span> randomIndex = <span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * <span class="number">37</span>)</span><br><span class="line">    res = res + map[randomIndex]</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> res</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><a href="https://blog.nowcoder.net/yiizgo" target="_blank" rel="noopener">呓语</a><br><a href="https://leetcode-cn.com/" target="_blank" rel="noopener">LeetCode</a><br><a href="https://coding.imooc.com/class/315.html" target="_blank" rel="noopener">慕课网</a></p>
]]></content>
      <categories>
        <category>Basic knowledge</category>
      </categories>
      <tags>
        <tag>Data structure</tag>
      </tags>
  </entry>
</search>
